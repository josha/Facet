# ColorWell fresh-context exercise (RC-19) — 2026-08-21

A zero-context agent built `Facet.Controls.ColorWell` in a disposable clone
using only the repository's public contributor guidance: suite 6837 green in
the clone (+38), every checker clean, the hardening gate byte-identical to its
pre-change baseline. The exercise code was NOT merged (branch stays in the
clone); what merged is the harvest: the derived Controls-count pin, the
scaffold stamping all registrations, the corrected examples, the loud-failure
require-cycle guard, the true activate-precedence rule, the root README, and
the fast-loop documentation (commits 8ed3cb8, ce40a79, d8aa27e, b3652ac).
`journal.md` is the author's candid record; `verifier-protocol.md` is the
separate human-facing walk (7 steps; its 3 MISMATCH + 2 UNCLEAR all fixed or
explained in the same commits). Two live-Studio rows remain with the batched
pass, as the exercise itself recorded.
