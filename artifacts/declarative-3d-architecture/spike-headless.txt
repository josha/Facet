✓ mount materializes the declared hierarchy with composed world transforms
✓ a reactive color change writes exactly one prop and nothing else
✓ a transaction batching N writes flushes once with bounded adapter writes
✓ a local transform change rewrites only its own subtree, and a Model moves as one pivot
✓ a welded child is written once and never transform-written again
✓ keyed add creates only the new item; survivors keep their handles
✓ keyed remove disposes only the removed item's scope and instances
✓ keyed reorder recreates nothing and writes only changed transforms
✓ dispose returns core and adapter counters to baseline, reverse order, idempotent
✓ unknown class errors at mount naming the node path
✓ duplicate keys error at mount naming both paths
✓ a throwing item build is quarantined: the item is absent, siblings live, lastError names it
✓ a throwing observer callback never wedges the flush loop
✓ unmount mid-flight (dispose during a pending update) leaks nothing
✓ a non-welded child under a welded part is refused at mount naming the rigidity rule
✓ a build that writes its own items source still converges: children match the final list
✓ per-item resources owned by the build scope die with the item and the mount
✓ a runtime duplicate key discards that update wholesale and reports it; prior state stays live
declarative-3d spike: 18/18 cases passed
