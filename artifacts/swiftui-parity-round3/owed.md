# SwiftUI parity round 3 — the owed ledger, status-anchored

The `Still owed, ranked` table of
[`docs/plans/swiftui-parity-round3.md`](../../docs/plans/swiftui-parity-round3.md)
carried into a form a gate can read. Every row of that table is here, in its
original rank order, plus the items round 3 itself created.

**What this file is for.** The round-3 gate's `owed-ledger-honest` check reads it
so that closing an item silently is a gate failure rather than an edit nobody
notices. The status vocabulary is the repo's:

| Status | Meaning |
|---|---|
| `CLOSED_ROUND3` | closed by this round, with live evidence named in the row — the check verifies the named evidence file exists |
| `OPEN` | still owed, headless-closable, nobody has taken it |
| `PENDING_HUMAN` | needs a human in a Studio session; cannot be closed from a desk |
| `PENDING_PHYSICAL` | needs real hardware (a phone, a pad); no emulator substitutes |

**What this file is NOT.** It is not a claim that the `OPEN` rows are still
broken. A check written that way goes red on the day somebody legitimately fixes
one — the failure mode `tools/check_traversal_evidence.py`'s own docstring was
written about — and would then be edited to agree with the change. The
`CLOSED_ROUND3` rows are the half that is asserted; the rest is a to-do list the
gate refuses to let anyone quietly shorten.

## The brief's ranked table (round-3 design, "Still owed, ranked")

| # | Item | Status | Evidence / blocked on |
|---|---|---|---|
| **O-1** | `traversal-document-order` re-record | `CLOSED_ROUND3` | Re-recorded live in Studio under Play 2026-08-14 (`a78fd88`): the players list disabled (Tab is its hotkey and is otherwise undeliverable), the driver fetched on the SERVER (`HttpService:GetAsync` is server-only), and the fixture engaged by a real tap first — it is a passive responder that binds no Tab until engaged, so driving keys at it measures nothing and reads exactly like a traversal failure. `tools/check_traversal_evidence.py` exits 0 and the stage's `studio-evidence` row is PASS. `TD15-consumer-canary` remains `PENDING`. |
| **O-2** | Places were stale | `CLOSED_ROUND3` | all fifteen rebuilt; `examples/places/LuauUI-Showcase.rbxl`. The underlying defect (no build stamp; nothing compares a `.rbxl` to its sources) is booked, and O-15 is the one place this round left un-rebuilt |
| **O-3** | Studio device canary on the rebuilt showcase | `PENDING_PHYSICAL` | **half run 2026-08-14** — `studio/canary-2026-08-14.md` (11,769 bytes, tracked, and MORE conservative than this row: *nothing here is an input claim, a touch claim, or a device claim*). Re-verified 2026-08-15 that no newer studio artifact exists. Item-by-item: **A (flow_wrap) CLOSED** — wrap ON = 4 lines/right edge 324/diagnostics empty, wrap OFF = 1 line/right edge 1183/`overflows this hstack by 806px`, two PNGs on disk. **D2 engine questions CLOSED** — `ClipsDescendants` does NOT crop a `Path2D` (GetBoundingRect 147x147 in both a clipping and a non-clipping panel), which is a live-only defect class no diagnostic can see, and `UpdateControlPoint` succeeds when parented. **D2 phone cost OPEN** — ~75 propWrites/frame is a DESKTOP Studio number the canary itself refuses to call a phone budget. **D (hidden/lifecycle) OPEN** — needs delivered input; Edit has no `PlayerGui`. **E (the chrome) OPEN** — needs Play and a real pad. Blocked on a reproduced environment failure: `start_stop_play` times out then answers *Start play hasn't finished yet* for the life of the instance, and `execute_luau` answers *Target is not reachable* in both datamodels, on two instances |
| **O-4** | Chrome unreachable by keyboard and gamepad | `CLOSED_ROUND3` | `tests/gallery_chrome.spec.luau` (38 cases, 27 mutations); `examples/gallery/client/showcase_chrome.luau` |
| **O-5** | `api.md` round-2 prose skim | `CLOSED_ROUND3` | `docs/reference/api.md`. Closed 2026-08-13 by the agent that owned the file, and the closure carries its own finding: the row's promised "12 concrete defects, listed below" **never existed** anywhere in the repo, so the skim was redone from scratch against a live schema dump and found **15**, including `recycleInstances` and `incrementalLayout` — ON BY DEFAULT and documented nowhere. D3's own documentation defect 1 (`rowSelectable`/`rowMovable`/`rowDeletable` absent from api.md) is closed with it |
| **O-6** | Shrink gap (a): a shrinkable label can land outside its box | `CLOSED_ROUND3` | **ruled 2026-08-13, shipped 2026-08-14** — the director chose neither pure option but a three-rung cascade, COMPACT -> TRUNCATE -> CLIP, quoted verbatim at `src/layout/shrink.luau:110-141` with this row's own measurement (`x0 w120 / x120 w120 / x240 w100`) as the recorded defect. `shrinkFloorOf` (`src/layout/shrink.luau:152-179`) implements the rungs; the clip crops the RECT rather than using engine `ClipsDescendants` (`src/layout/solver.luau:3234-3247`) so a headless sweep can still see it. `tests/text_degrade_cascade.spec.luau` (registered `tests/run.luau:60`) carries both rulings plus the negative control *a stack that declared NO shrinkWeight is untouched — it still paints outside its box*; `artifacts/rulings-1-and-5/mutation-evidence.md` records 12 of 12 mutations biting. O-7's `ctx.fitProbe` is measure-side only and did not weaken it. Commits `fb76787`, `fb8b770`, `2c3f9b6`, all ancestors of HEAD |
| **O-7** | Shrink gap (b): `shrinkWeight` flips the `ViewThatFits` winner at 10 of 28 widths | `CLOSED_ROUND3` | **ruled 2026-08-14** (director: "follow swiftui's behavior"). Researched before implementing: Apple's own reference for `ViewThatFits` selects "the first child whose **ideal size** on the constrained axes fits within the proposed size", and an ideal size is the one a view reports under an unspecified proposal — so truncation, `lineLimit` and `minimumScaleFactor` are all invisible to the choice there, and `shrinkWeight` is LuauUI's member of that family. `ctx.fitProbe` suppresses the measure-side shrink for the duration of the fit question and nothing else; the winner still shrinks after it wins. `src/layout/solver.luau`, `tests/layout_vocabulary.spec.luau` cases (b)/(c)/(d) — (b) sweeps ALL 28 widths — and `tests/container_relative_frame.spec.luau` (the scope key moves from FOUR writers to FIVE). Round 2's §2.4 sentence is corrected in place. Citations and the one deliberate remaining divergence: `docs/lessons/a-candidate-is-judged-at-its-ideal-size.md` |
| **O-8** | Table tray-focus: trays are in no focus group | `CLOSED_ROUND3` | `src/controls/table.luau` + `tests/table_input.spec.luau` (`e54d671`). Closed together with O-9 because they were one defect with two hosts |
| **O-9** | Keyboard/pad Delete on an unswiped hosted row | `CLOSED_ROUND3` | `src/controls/virtual_list.luau` (`e725c68`) plus its showcase demo (`2774513`); recorded in the round-3 design |
| **O-10** | A BLOCK table publishes a scroll path it has no host for | `CLOSED_ROUND3` | **ruled 2026-08-14 (option A)** — `api.scrollPath()` returns `nil` when `spec.scrolls == false` (`src/controls/table.luau:3674-3680`), and the design half (an open tray riding along through a page scroll) binds the tray-close to the REAL scrolling ancestor via `hostScrollPathOf` -> `controller.scrollHostFor` (`:3607-3616`, `:3653-3657`). `tests/table.spec.luau:2964` — *Table: a BLOCK table publishes no scroll path of its own (ruling 3)* — including *THE SYMPTOM: a PAGE scroll closes an open tray in a BLOCK table*. Commit `4285c13`, ancestor of HEAD |
| **O-11** | Reduced-motion settings surface | `CLOSED_ROUND3` | `examples/gallery/client/settings_panel.luau`, driven by `tests/gallery_chrome.spec.luau` groups (6)-(8); `LuauUIShowcaseAPI.motion("reduced")` |
| **O-12** | Row-actions menu not clamped to the viewport | `CLOSED_ROUND3` | **ruled 2026-08-14 (option A: flip, not clamp — a clamp lets the menu cover its own trigger)** — `computeMenuAnchor()` (`src/controls/row_actions.luau:1930-1947`) flips the menu above its trigger when `below + menuH > viewportH` and there is room above; this row's measurement (trigger y=508 on a 600px viewport) is quoted at `:1896-1899`. `tests/row_actions_input.spec.luau:1739-1900` (registered `tests/run.luau:420`) carries the flip, the conditional-ness, the predicted-height-equals-painted-height case and the negative control *NEGATIVE CONTROL: with no room ABOVE either, it stays BELOW rather than flipping*. Commits `fe1822d` + `bcd2da6`. **Residual, deliberately NOT this row:** only the VERTICAL edge is handled — `computeMenuAnchor` returns `rect.x` unconditionally, so there is no horizontal clamp; that was never in the ruling's scope and is a new item |
| **O-13** | The "Edit item" wrap rule (`renderer.luau:452`) | `CLOSED_ROUND3` | **ruled 2026-08-14 (option A: the longest-word rule)** — and the row's `renderer.luau:452` pointer is stale twice over: the rule moved files AND the decision moved layers. The string-only half (`isSingleWord`) is at `src/render/layout_node.luau:166/308/455`; the DECISION is now in the solver at `src/layout/solver.luau:1184-1205` under the header *THE WRAP RULE — WRAPPING IS RIGHT ONLY WHEN THE LONGEST WORD FITS*, because only the solver knows the drawable width. The solver publishes `textFacts.wraps` (`:535-542`); the paint seam is `src/render/renderer.luau:208-212`/`:1665-1676`. `tests/text_degrade_cascade.spec.luau:302-421` — *ruling 5: wrapping is attempted only where it can succeed*, including *the verdict is RE-DECIDED, never latched* and *a ZERO-width box gives no verdict*; mutations M5/M6/M7/M11/M12 bite. Commits `fb76787` + `4a948d0`. **This closure is also the cause of O-22's re-break** — see that row |
| **O-14** | The overflow sweep could not see cross-axis findings | `CLOSED_ROUND3` | `tests/overflow_sweep.spec.luau`. The phrase FILTER was DELETED rather than widened (a second phrase would have been the same defect), so the sweep now asks about every solver finding at eight viewports and four text preferences; 697 findings, 393 of them previously invisible to the file whose job is to see them. All five recorded non-main-axis findings re-verified — five of five real, two worse than recorded, one drifted — plus eleven nobody had recorded |

## Owed by round 3's own work

| # | Item | Status | Evidence / blocked on |
|---|---|---|---|
| **O-15** | `demo_picker.DEMOS` registration for `lifecycle_hidden`, and the showcase place rebuilt after it | `CLOSED_ROUND3` | **both halves closed.** The registration half landed `d5d48d2`; the place rebuild — the half this row said was still owed — landed **`d2929a2` 2026-08-15 08:56** (*build: pin rojo in LuauUI, and bake the IAS property into every place*), and `d5d48d2` is an ancestor of it. `examples/gallery/client/demo_picker.luau:159-169` carries the `lifecycle-hidden` entry naming O-15 by number; `examples/places/LuauUI-Showcase.rbxl` is committed and clean. The blocker this row recorded is also gone: `examples/showcase.project.json` maps exactly `../src`, `gallery/examples`, `gallery/scenarios`, `gallery/client` and `themes`, and ZERO dirty or untracked paths fall inside any of them. **A DIFFERENT staleness has opened since and is O-2's booked build-stamp defect, not this row:** 38 files under `src/` and 18 under `examples/gallery/` are newer than the `.rbxl`, and the `time_curves` and `foreign_content` entries are at HEAD but absent from the built place |
| **O-16** | The always-on sweep cannot see the `virtualSlot` class | `CLOSED_ROUND3` | `tests/overflow_sweep.spec.luau` — closed by O-14's filter deletion, which was the general fix rather than adding this class's phrase to a list. Its `BREADTH CONTROL: a layered overlap and a lying itemExtent both reach the collector` case is the proof, and it is a control case rather than a coverage claim |
| **O-17** | `tests/lib/tiers.luau` records `overflow_sweep.spec` at 1036 ms / 37 surfaces / six viewports | `CLOSED_ROUND3` | **measured 2026-08-15, and the record was the smaller half of the finding.** `lune run tools/lune/time_specs`, three runs, per-spec median, 213 specs / 5561 cases / wall 78.9-79.9 s: `overflow_sweep.spec` is **16342 ms**, not 1036 and not the ledger's own 10.0 s guess; every one of the eleven entries had drifted (perf_lab 16024 -> 24204, theme_drift 1356 -> 2037, wardrobe 326 -> 497). Its `why` now states the real shape — 46 surfaces x 9 viewports x 4 preferences, x 8 packages over a 10-cell tier, x a locale pass over 2 preferences on the 21 surfaces that have a second form. **THE DEEPER FINDING: the rule itself was false.** "the eleven costliest files and nothing else" is a claim about every file the list does not name, so the list could never hold it — `virtual_list_row_actions.spec` (1737) and `row_actions_scenario.spec` (1458) had grown costlier than three files it excludes, unnoticed. **THE CALL: not derived, CHECKED, and the rank became a THRESHOLD.** Deriving inside the suite would cost a full timed run between two keystrokes, which is what the fast tier exists to avoid; instead `tiers.THRESHOLD_MS = 1200` makes the rule checkable from the outside and `tools/lune/check_tier_costs` enforces three things against a fresh `time_specs` JSON — every exclusion timed, every recorded ms within 50%, nothing over the threshold left in the fast tier. All three mutation-proved (`artifacts/swiftui-parity-round3/tier-costs-2026-08-15.md`). **HONEST RIDER, the same one O-29 carries:** the checker is wired into no gate row yet, so today it catches drift when somebody runs it |
| **O-18** | A vertical pan that begins on a hosted row still fires that row's `onActivate` | `PENDING_PHYSICAL` | **RESTORED TO `PENDING_PHYSICAL` 2026-08-15 after a coordinator challenge, and the challenge was right.** The headless fix is NOT in question and stands: `824ab94` arms `suppressActivatePath` on the VERTICAL lock as it already did on the horizontal one (`src/controls/virtual_list.luau`), pinned by `tests/virtual_list_row_actions.spec.luau:2700` — *CARRIED DEFECT 3: A VERTICAL PAN IS NOT A TAP* — with three escape controls, registered `tests/run.luau:197`. **What was wrong was calling it VERIFIED.** A live probe exists and DOES carry a positive control — `docs/plans/table-hosted-row-actions-stage2.md:228-237`: with the arm removed, 12px and 45px vertical pans on a showcase row both left the row unread, *while a plain tap on the same row cleared it in both builds, so the oracle was live and sensitive*. That control is what makes the null result readable rather than empty, and it answers the reachability objection. **But I did not run it** — it belongs to the session that wrote `824ab94`, I hold no transcript, and I closed this row on a relayed claim. **And on the repo's own terms the device answer is still owed**: the probe's own record ends *a REAL mouse drag and a real finger remain untested, and this is what 'awaiting a device answer' now means*, and the docstring `824ab94` itself added to the test says *A LIVE MEASUREMENT STILL BELONGS TO IT* because `fake_target` models handler WIRING, not the engine's input sinking. The injected class arrives as `UserInputType.Touch` and Roblox's own drag threshold already suppresses `Activated` past ~12px for it, so that probe narrowed the question to one input class; it did not close it. **Owed: a real finger on a phone and a real mouse drag.** The guardrail is specific rather than bureaucratic — this project's worst recent defect was a list that could not be swiped at all on a phone and looked perfect on desktop |
| **O-19** | The locale axis, and 390x844, are not swept | `CLOSED_ROUND3` | **both landed 2026-08-15** — `tests/overflow_sweep.spec.luau` (the LOCALE AXIS block, `LOCALE_WAIVERS`, and the `REACH` case) plus `phone-390x844` in `tests/lib/device_views.luau`, so both always-on sweeps visit it. Evidence + triage: `artifacts/swiftui-parity-round3/locale-axis-2026-08-15.md`. The axis is a second MOUNT rather than an `env:set` (a fixture reads its copy when it builds), by the two mechanisms the corpus has — a `copy` export through the shipping pseudo-localizer, or `deps.locale`/`steps.locale` on a proof — tiered at 2 preferences x every viewport with no theme cross product; **REACH IS DERIVED**, not claimed: an English text digest against the expanded one, so the 38 surfaces with no locale form are printed rather than counted as swept (21 of 59 have one). **WHAT IT FOUND:** 31 rows on the first run, **19 of which were the instrument's own noise** — surfaces it mounted but could not move — leaving a 12-row ledger; `p4_foyer`'s TopBar at **128px and +0** where English is 10px at +14; `p2_cartwheel`, which carried NO English waiver at all; and, from 390x844, three themed ceilings re-recorded worse (Pixel Quest key 29 -> 32, Glossy Touch tile 37 -> 39) plus two new `time_curves` bar-track rows. **The TopBar repair is a measured NEGATIVE RESULT** and is recorded as such: `wrap = true` fixed six other rows that day and here traded a hidden badge for a worse page (`HomeBody` 16px -> 76px, `/Foyer/Root` newly over), so it was reverted twice and the row is a design call on the proof's chrome. Cost +15% (14.2 s -> 16.3 s), measured. Mutation-proved three ways |
| **O-20** | A presented modal still costs 2 solves per geometry change | `CLOSED_ROUND3` | **FIXED 2026-08-15, and the row's own title is the misdiagnosis.** Evidence, transcripts and mutation table: `artifacts/swiftui-parity-round3/o20-modal-solve-residual.md`; the optimization log's L-29-residual-2 entry is amended in place. **IT WAS NEVER THE MODAL** — `presenter.present` of the same blueprint costs exactly what `presenter.presentModal` costs and a modal over a plain tree costs 1, so nothing in `src/present/` was ever involved; the ADR-0032 regression this row feared could not have shown up here. **AND IT WAS NEVER 2** — 2 is the FIRST rotation of a fresh surface, the steady state was **3** (measured 2, 3, 3, 3), which the rider could not see because it measured one rotation. **THE TRIGGER** is a LAYOUT prop bound to a memo over the viewport: the role-pick modal's two CTA sub-labels republish `width = { type = "fixed", px = … }` as a fresh table equal BY VALUE to the last one — the identity-not-value class L-29's own fix was built around, one layer down (a node prop, not an env fact). **THE FRAMEWORK CHARGED TWO EXTRA FULL SOLVES FOR THAT ONE PROP WRITE**, both fixed in `src/render/renderer.luau`: `feedbackArmed` outlived the flush that set it (`THE ARM DOES NOT OUTLIVE ITS FLUSH` — a solve whose listeners published nothing still armed, the core never restarted the pass, and the arm fired in the NEXT flush against a stale mark), and `refresh` re-solved for `measure` dirt a settle-phase solve had already consumed (`THE STALE-LAYOUT-DIRT TEST`). Shipped **1, 1, 1, 1** per rotation, tier 1 headless — no Studio and no device claim is made. **THE INSTRUMENT WAS REPAIRED WITH THE DEFECT**, because this row was right that it could not fail in the direction anyone cared about: `luauui_resize_solve_contract.spec.luau` now asserts an EQUALITY over FOUR consecutive rotations (`"1,1,1,1"`), and the sibling `>= 1 and <= 2` in the same file is `toBe(1)`. Proven rather than argued — with the arm leak restored the repaired check reports `1,2,2,2`, on which the old first-rotation ceiling of 2 passes. **A SECOND, UNBOOKED DEFECT FELL OUT OF THE FIRST**: the leaked arm also promoted the first bound-value change after ANY geometry change from an incremental solve to a full one — 101 arranged nodes where 8 were needed, on `tests/perf_principles.spec`'s own incremental-layout fixture, invisible to it because `stats().lastArranged` reports the LAST solve of a frame rather than every solve in it. **MUTATIONS**: M1 (drop the disarm callback) and M2 (drop the stale-dirt test) each redden named cases in BOTH suites; M3 (skip always) reddens 112 including the negative control — but only after that control was repaired, because as first written it drove the px through the VIEWPORT, which the settle solve answers whichever way `refresh` decides, so it stayed green while 108 other cases reddened. **M4 is a published NULL RESULT**: deleting the full-solve guard reddens nothing, correctly so on today's code, and the guard stays with that recorded at its declaration |
| **O-21** | The 35 large-text overflow defects the new axis found | `CLOSED_ROUND3` | **the director's ruling of 2026-08-15 — "fix the ones that hide text; waive the rest" — is carried to the end.** Evidence: `artifacts/swiftui-parity-round3/o21-waivers-closed.md`. **THE COUNT WAS RE-DERIVED BEFORE ANYTHING WAS TOUCHED** and reproduced round one's split exactly: 31 neutral waivers, 16 carrying a verdict that costs a player copy or a labelled control, 15 carrying none. **ALL SIXTEEN ARE CLOSED — thirteen FIXED in the fixture, two RECLASSIFIED because the instrument was wrong, and `p4_foyer`'s tab row fixed a SECOND time when the first repair was measured under the locale axis.** `WAIVERS` 31 -> **13**, `LOCALE_WAIVERS` 12 -> **4**, the theme ledger 127 -> **116**, `DEFAULT_WAIVERS` 4 -> **3**; every waiver left standing is measured cosmetic in BOTH passes with its one-line reason in the header. **ROUND ONE'S LEAD WAS HALF WRONG AND THE WRONG HALF WAS LOAD-BEARING**: the pages really could not fit a 640x320 landscape phone, but the standing objection was to NESTING a page scroller, and two shapes get around it without touching the subject — a PERPENDICULAR page scroller (`card_rail`'s rail owns x, its new page owns y) and a SIBLING scroller (`Chrome` beside `ListPane`, the arrangement `scroll_host` exists to prove is legal), the latter made conditional on `adaptive.conditions.isShort` so every viewport that already fitted is unmoved. Zero rows needed a design change nobody could make. **THE TWO INSTRUMENT DEFECTS, both manufacturing hidden text out of nothing:** paint order was `adapter.paths()`, which is sorted ALPHABETICALLY — `BellZ/BadgeWhen/.../Count` sorts before `BellZ/Bell` while the renderer paints the badge ON TOP of it, so a notification count was reported covered by the button it decorates (`node.z`, what `syncZOrder` wrote, is the only thing entitled to the word); and `surface = "plain"` was counted opaque, when `chrome_slots.classify` returns nil for it before any class rule — `sponsor_avatars`' 16 "covered" initials were covered by the hairline RIM drawn around them. A third fix intersects a victim rect with its ScrollView ancestors' windows first, because a row its own list has clipped away is not on glass to be covered. **A STALE NOTE FOUND AND CORRECTED IN PLACE**: `p4_foyer`'s TopBar refused the shrink pair in a measured 2026-08-13 block (*a text node's floor is its longest word... a hand-chosen `minMax` cap is still the only lever*). Ruling 1 of 2026-08-14 put an ELLIPSIS rung under that floor, so a bare `shrinkWeight` now reaches it — measured as the minimal set, and the `minMax` written under it was DELETED. **THREE NULL RESULTS PUBLISHED, THREE DELETIONS**: a weight on the wardrobe card's column, a `When` hiding `row_actions`' swipe hint, and that `minMax` each reddened nothing and each came back out. **THE THEME AXIS BIT**: `card_rail`'s first derived plate height was clean under the default theme at every viewport and preference and **590 findings red under Compact Pointer at the DEFAULT preference** — the probe mounts one theme and so cannot test that axis; the height now sums the package's own `chromeInsets.panel` and `chromeOutsets.panel` (Fantasy Ornate is the one package declaring an outset). **MUTATION-PROVED**: 13 of 17 fixture mutations redden ONE named sweep case each, 3 of 4 probe mutations move the readability verdicts, and the fourth (paint order) is published as a null result on today's corpus that BITES on the pre-fix one — reverting `node.z` at `b377fe9` resurrects 4 TEXT-COVERED verdicts, all of them the foyer badge. **LIVE, Studio/Play, through the demo picker with `mounted` asked and no direct mounts**: `card-rail` `ok:true` with a real `/CardRail/Page` ScrollingFrame and a nested clipping rail whose derived height is 62px — the headless number to the pixel — and the `Rally` badge ending 20px inside the clip; `variable-extents` and `table-virtualized` `ok:true` with their new `Chrome` scrollers at HUG height on a tall window, i.e. the conditional off exactly where there is room. No device claim: the defects live at 640x320 at +14 and `GuiService.PreferredTextSize` is not script-writable. **ONE FINDING BOOKED, NOT PATCHED**: `p4_foyer`'s `SearchSlot` ViewThatFits gets a 0px slot on a 320px phone at +14 and its fallback icon button paints at its natural 44px INSIDE it, half over `Refresh`. Pre-existing at `b377fe9`, unchanged here, and invisible to both instruments (no stack overflows; the probe counts only Text as an occlusion victim) — a framework question about whether a losing-by-fallback candidate may paint outside its offer. Suites: LuauUI **5618 / 0** (HEAD baseline 5618 / 0 — no case count moved, this round deleted waiver rows and edited fixtures), Rascal Rally **3280 / 0**. No `src/` file touched |
| **O-22** | The neutral flat baseline is uncharacterized since the popup-catcher fix | `CLOSED_ROUND3` | **CLOSED 2026-08-15 by regenerate-and-CHARACTERIZE, and the 200 problems decompose with no remainder: 1 + 198 + 1.** Evidence: `artifacts/swiftui-parity-round3/flat-baseline-characterization.md` (the decomposition, the dump-vs-invariant argument, and the mutation transcripts), regenerated dump `artifacts/rich-skinning-v2/rows/neutral-render-dump.json` (sha `64cd1481…`), and `tools/lune/check_flat_baseline` now PASS at **1461 flat nodes byte-compared**. **THE DECOMPOSITION.** 1 = the reproducibility pin (the stored artifact was stale — a chore, not an accusation). 198 = `textWrapped` from `fb76787`, i.e. O-13's own closure. 1 = **a rect nobody had named**: `06_tile_game` `/TileGame/Page/rack` 358 -> 353 on the phone, bisected across 167 commits in 8 worktree builds to `02b9df1` (the column-major Grid refactor, whose message says *No baseline moved* — it moved by one node). Measure summed the UNROUNDED lane share while arrange painted the floored one, so the container had been reporting a width it never drew; all seven rack tiles are byte-identical at all three viewports and 358-353 = 5 is the over-report to the pixel (<= lanes-1 = 6). **THE CALL: NOT a re-pin, and not an invariant INSTEAD of the dump — both, split by instrument.** The reproducibility pin carries no claim and cannot be an invariant ("is this file current" has no rule to compare against); the 0.6.0 baseline is the claim and was NOT moved (declined a third time). The rule form already existed here — `ALLOWED_ADDED_PROPS` cost `textSize` and `textFont` one entry each — and `textWrapped` joins it as the third member of that family, so a legitimate new prop will not re-break this row. What re-broke it was that nobody RAN the check while shipping `fb76787`. **ONE MECHANISM CHANGE, and it is strictly tighter:** an added-prop entry may now pin its VALUE, and `textWrapped` is pinned to `false` — a `true` would mean the minimal-write seam had paid a write to restore the engine default (312 of 312 nodes carry `false`; the nodes that actually wrap carry no key at all). **MUTATION-PROVED:** inverting the seam's nil-default reddens **435 named cases**, and the CONTROL — the same mutation with the value pin removed — reddens **zero**, which is what makes the pin load-bearing rather than decorative; widening the rack's gap reddens **19 named child rects** while the container stays correctly waived (the entry is an exact path, not a prefix). **A SECOND FINDING, fixed here:** the frozen 0.6.0 baseline was GITIGNORED under a rule whose own comment says *it is REGENERABLE* — it is not, and until today it existed on one laptop while six gate rows read it. Now tracked, with a named absent-input message instead of a Lune stack trace. The alarming half of that hypothesis was FALSE and is recorded as such: the exit code was already 1, so no gate ever passed on a missing input |
| **O-23** | `progress_ring`'s five-dot spinner overflows a 320px portrait phone in four of the eight shipped packages | `CLOSED_ROUND3` | **fixed 2026-08-14 in `d9f036b`** — the design call this row said was owed to `newProgressView` was made: the spinner's label takes `shrinkWeight = 1` (`src/controls/progress_view.luau:519`), with the rationale that flat themes do not move because shrink is only spent when the row is over-subscribed. `tests/progress_circular.spec.luau:579` — *a LONG label on a NARROW screen still fits — every presentation, every package* — drives all 8 packages x 3 presentations at 320x640 and asserts zero `controller.diagnostics()` findings, and its docstring records why the pre-existing themed case could not see it (a short label on a wide screen, so every row fit by accident). Corroborated at `artifacts/swiftui-parity-round3/theme-sweep-tier.md:140-145` |
| **O-24** | "verified across every shipped theme" has no always-on instrument | `CLOSED_ROUND3` | **closed 2026-08-14 in `d9f036b`** — the general fix this row asked for (a package axis on the sweep itself) shipped. `tests/overflow_sweep.spec.luau:1365-1370` installs `themeMetrics` inside the always-on per-surface sweep: 8 viewports x 4 preferences x 8 packages, with the themed tier being every viewport at +0 plus the largest preference at the two narrowest. It declares its own holes rather than overclaiming — the `COVERAGE` case at `:1912` derives the swept cells from the same functions the sweep calls, pins `#PACKAGES == 8`, and PRINTS the 22 uncovered (viewport, preference) cells on every run. The three hand-written per-file sweeps still exist but are no longer the only instrument |
| **O-25** | Under Pixel Quest a Table's derived row box is 16px shorter than the cell Text the table itself authors | `CLOSED_ROUND3` | **fixed at the source in `src/controls/table.luau`** — the derived row height now adds `chromeInsets.selection` and the line cap subtracts it (`:414`, `:435-438`, `:907-926`), so the cell never pays for the frame. Pinned at `tests/paradigm_table.spec.luau:830` (*a package that carves a border GROWS the row — the cell never pays for the frame*, 4 packages x touch/pointer x offsets {0,4,10,14}, asserting `r.text.h <= r.cell.h`) and `:846` (*...and a FLAT package's rows are byte-identical to the untethered ones*). The instrument was hardened first: `:748-755` records that asserting the CELL rather than only the row is what makes it bite. **The `PRE-EXISTING` waiver this row pointed at is GONE, and its deletion is the closure evidence** — `0f09154`, *the waiver fired nowhere, so it is gone — which is what a waiver is for*; `tests/row_capabilities_scenario.spec.luau:313` now asserts a surface files NOTHING under any of the eight packages |
| **O-27** | Is the `composition` declared-fallback finding a defect? The two sides of the lockstep rule disagreed | `CLOSED_ROUND3` | **ruled 2026-08-14** (option A: it is correct behaviour). The finding now carries `designed = true`, set in the one place that files it (`src/layout/solver.luau`, composition arrange branch); LuauUI's always-on sweep skips designed findings and its composition WAIVER is deleted (a permanent exclusion has no ceiling and no expiry), and Rascal Rally's `luauui_large_text_results.spec.luau` reads the same field instead of matching the sentence. Plain-language contract: `docs/reference/api.md` § "Findings that are not defects". Proved by `tests/composition.spec.luau` (three cases incl. a negative control that reddens if EVERY finding is marked) |
| **O-28** | A value control inside a row trapped directional navigation | `CLOSED_ROUND3` | **ruled 2026-08-14** (option A: a value control yields the arrow at its limit). A declared adjust axis's directional keys move to their own `AdjustAxis` action so a yield can be told apart from a bumper press; the yield reuses the navigation path the arrow key itself uses, and neutralises the action because the key-up it gave away never comes back. `src/present/presenter.luau`, `tests/rating.spec.luau` (six cases), and the showcase demo on the shipped playlist — `tests/examples_gallery.spec.luau`, "GAMEPAD: the d-pad walks PAST the rating and into the swiped-open tray", which replaces that file's now-false "the d-pad cannot get past this" note |
| **O-29** | `presenter.luau` crossed the 200k live-sync cap | `CLOSED_ROUND3` | **both halves closed, and the half-measure is MUTATION-PROVEN.** `src/present/presenter.luau` is **182,395** chars on disk today (against the 207,333 this row recorded), taken under by the extraction wave `faa822e`/`fed869a`/`1402758`; no module in `src/` reaches the cap. The cheap half-measure this row said was *not built* now exists — `tools/check_source_size.py` (`1dd4a75`, with `ad3f3f8` fixing the `>=` off-by-one so a file at EXACTLY 200,000 fails). It exits 0 with `KNOWN_OVER` empty, **and it bites**: importing the module and lowering only `CAP` to 180,000 turns it red on the four files over that mark (`row_actions` 191,063, `renderer` 185,633, `table` 185,558, `presenter` 182,395), measured 2026-08-15. **Two honest riders:** (1) headroom is thin — seven modules sit between 175k and 192k, `row_actions.luau` within 8,937 chars of the cap; (2) **the checker is wired into NO gate row** — `grep -c check_source_size phases.json` is 0 and it appears in no `run =` in `tools/lune/gate_manifest.luau`, so nothing runs it automatically. Wiring it is a round-4 gate row |
| **O-26** | `effectiveTransparency` has no showcase surface, and deliberately so | `CLOSED_ROUND3` | **Closed 2026-08-15 by making the framework paint with it** (director ruling; [ADR-0035](../../docs/adr/ADR-0035-preferred-transparency.md)). The row was right that a demo honouring the preference by hand would advertise a capability the framework did not have, so the closure is the paint path, and the demo ships with it. WHICH path is a measurement, not a taste call: `Scrim backdrop` is the only rule in the generated sheet whose `BackgroundTransparency` is strictly between 0 and 1, and the bespoke painter mirrors it exactly — every other framework background is already 0 or already 1, and multiplying a 1 REVEALS it rather than dimming it, which is what refuses the blanket sweep. No engine property changed hands: in each mode the writer that already owned it keeps it and only the value moves, so `NATIVE_SHEET_OWNED` and the authority manifest are untouched — measured live, `GetStyled` follows the preference while the raw property stays 0.0000. ADR-0029 is not re-opened: it refused rule mutation for a per-node FADE on 3-frame latency and one-rule-per-node, and a preference is human-frequency and per-selector by nature. The authored/presentation line: the preference never scales an authored `opacity`, because `withAnimation` drives that term and folding it in would retarget a running fade. Demo: `examples/gallery/scenarios/preferred_transparency.luau`, in `scenarios/init.luau` ORDER and `demo_picker.DEMOS`, sweeping clean, with the backdrop that moves beside a panel and an authored fade that do not. Evidence: `artifacts/adr-0035-preferred-transparency/live-probes.md` (five live tables, both paint modes) and `.../mutation-evidence.md` (eight mutations, plus one published null result that turned out to be a mutation that did not compile) |
| **O-30** | `newAsyncImage` and `canvasGroup` are demonstrated only outside the demo picker | `CLOSED_ROUND3` | **closed 2026-08-15 in `cda3667`** — evidence `artifacts/swiftui-parity-round3/o30-showcase-demos.md`. THE CLAIM WAS RE-VERIFIED BEFORE IT WAS ACTED ON and held on both halves: the census of every caller is in the artifact, and no `demo_picker.DEMOS` entry reached either prop. **`async-images`** is now in the picker, which cost the host two things it never needed while only a sweep mounted it — `ctx.bindResourceTransport` in the MINIMAL ctx (the fixture calls that seam at BUILD, so without it the demo took the pcall in `show()` and warned) and a page that SCROLLS: its `page-not-scrollable` waiver (25px) and themed ledger row (143px, every package) are **deleted rather than re-recorded**, because a surface a player can select does not get to overflow. **`canvas-group`** is a new fixture, because the fade group had no surface whose SUBJECT it was: three concentric plates twice at one opacity — faded as ONE group, and faded one plate at a time — plus a `transition = { enter = "fade" }` over a `canvasGroup = true` node that writes no opacity, and the framework's own refusal for `UI.Text{ opacity = … }` CAPTURED AT BUILD rather than quoted. **LIVE, from the picker, in Play**: `1:ready 2:ready 3:failed` with two thumbnails resolved through the real `PreloadAsync` transport, and FOUR real engine `CanvasGroup`s (one 657x132 over the left stack, three at 132/92/52 on the right) all at `GroupTransparency = 0.6`. Ten new cases, six mutations each reddening ONE named case, suite measured in a pristine worktree carrying exactly the committed hunks: 5561/0 |
| **O-31** | A virtualised collection renders the WRONG ROWS when the consumer forgets `bindNativeScroll`, and says nothing | `CLOSED_ROUND3` | **closed 2026-08-15 — evidence `artifacts/swiftui-parity-round3/o31-native-scroll-autobind.md`. THE SEAM THIS ROW PROPOSED WAS WRONG, AND THAT IS THE FINDING.** `buildFocusGroups` is not "the one seam that hands a control its own mounted path": the presenter calls a contribution's `focusGroups` ONLY from `focus_map.autoGroups`, which runs only when the consumer left `opts.navigationGroups` ABSENT. Measured, one control, four presenter configurations — `focusGroups` fires **2, 0, 0, 0** while `bindController` fires 1,1,1,1 and `syncGeometry` fires 3,3,3,3. A fix built there would have fixed the default screen and left a consumer who declares a focus map exactly as broken, and that consumer is worse off than this row knew: with `focusGroups` never called the control never learns its path at all, so `bindNativeScroll(controller)` REFUSES outright — a second, unrecorded defect the probe found. **NOT ARGUED, RUN**: mutation M8b builds the fix this row asked for and reddens exactly two cases, the two discriminators, and nothing else. **WHAT SHIPPED** is `src/controls/native_scroll_binding.luau` — one shared arbitration, `install` the only part that differs — reached through the two seams that ARE unconditional (`bindController` + `syncGeometry`, the latter carrying the control's own mounted node every refresh). **`src/present/` WAS NOT TOUCHED**: the presenter already delivers both facts to every contribution in every configuration, so this row's named scope over-estimated the change. **IDENTITY, NOT PATH, IS THE MOUNT KEY** (measured: the node table is stable across refreshes and fresh after a remount, including back to the SAME path), which makes the mirror self-healing across a `UI.When` flip that RascalRally hand-rolls and its own comment calls stale. **NO OPT-OUT KEY WAS ADDED**: the one collection whose scrolling is genuinely owned elsewhere is a `newTable{ scrolls = false }`, which already publishes `api.scrollPath() == nil` (ruling 3, O-10) — the automatic path reads the same answer, mirrors nothing, and still binds the tray-close to the real scrolling ancestor. `bindNativeScroll` stays public, now IDEMPOTENT, so every existing caller is un-doubled and unchanged. **LIVE A/B, real engine, parallel mount, NEITHER SIDE CALLING THE BIND**: with the mount notice removed a `CanvasPosition.Y := 2000` left the grid at `c1..c40`; shipped, the same scroll moved it to `c201..c240`. VirtualList `r1..r10 -> r51..r60` and Table `r1..r18 -> r49..r68` in the same session; every ScreenGui destroyed and verified absent in the same call. **TEN MUTATIONS**, each on a tree restored first, plus one published measurement error (M8's first run over-proved with 659 reds that were a nil upvalue in my own mutation, not the seam) and one weakness M4 exposed in my own test before it was strengthened. Suites: LuauUI **5596 -> 5608 / 0** in a pristine worktree at `b377fe9`; RascalRally **3276 -> 3280 / 0**, four cases in `tests/luauui_motion_and_scroll_contract.spec.luau` block C-SCROLL |

## The four PENDING_PHYSICAL device rows, named individually

The device ledger a Studio/physical session would close. None is claimed:

- **A (flow-wrap)** — the `flow_wrap` fixture has never been mounted in Studio under Play. The cross-axis rule it reproduces came from a live Studio probe on 2026-08-13; the fixture itself did not.
- **D2 (circular progress)** — whether the ENGINE crops a `Path2D` stroke inside a clip host is unanswered (`GetBoundingRect` reports geometry that knows nothing about a host's crop), and the per-frame `SetControlPoints` cost on a phone is unmeasured. `progress_ring` is the surface to drive.
- **D (`hidden` / lifecycle hooks)** — whether Roblox itself already refuses input to an invisible `GuiButton` is undocumented, which is why the framework holds the rule rather than relying on the engine.
- **E (the chrome)** — does a real `Backquote` reach IAS, and does a physical `ButtonY` fire `Activate` end to end. Injected keys are known-unreliable for some classes, and a real pad button cannot be pressed headlessly.
