# Director visual round 3 — feedback while reviewing DV2 output (2026-07-31, verbatim)

Three findings + one carried observation. Anchoring rule unchanged.

| # | Director's words | Reading |
|---|---|---|
| DV3-1 | "move the lap/race drama icons up to the very top, to the right of settings in landscape mode" | DIRECT LAYOUT INSTRUCTION: in landscape the chip group docks in the TOPBAR band, immediately right of the settings gear cluster. (Portrait placement per DV2-2 stands unless it conflicts — flag if it does) |
| DV3-2 | "something weird in race results — what's this tiny scrolling view? it's not legible" (image: the recap line with a clipped blue half-disc left and a grey dot right) | The standings band renders as a SLIVER-HEIGHT scroll host when sparse (a row swatch clipped to ~a half-circle + a scrollbar dot). A band must never render as an illegible sliver: when it has no rows it collapses entirely (or the honest empty line); when populated it takes full row heights. Ties into amendment A16's escalation |
| DV3-3 | "the list of racers has some stray colored ring next to each racer that flashes/changes... that's distracting" | A ring form beside each racer cycling hue as the 4 NPC sponsors churn plays. Diagnose against LEGACY's per-row play-churn expression (M12 tell = a ramping stroke ON the row + the gate pill; NOT a free-floating hue-cycling ring). Suspects: the tell/wash channel painted on the wrong element, the omen-ring slot leaking, or the TickRing pill flickering on record churn. One meaning per channel; kill the stray form |
| DV3-4 (carried, mine) | — | The DV2-4 scrim at black@0.35 still READS lighter than legacy's backdrop in the landscape capture (`DV2-verify-results-sponsor-iphone16-landscape.png`); scrim covers the safe content area only (OWN-D38: world visible in the topbar band). Re-check the extracted legacy value + whether legacy layers scrim+plate; match the READ, not just one property |
| DV3-5 | "it looks like the race countdown/go appears offscreen in portrait mode" | S13 StartCountdown solves OFFSCREEN in portrait. Spec: map-canvas-centre (maximized) / upper-middle (minimized); legacy placement = `StoryTokens.countdownBox/Centre` from SponsorGui:1848-1862. Diagnose the portrait solve (suspect: the box computed against the landscape map rect, or an anchor against the pre-DV2-2 chip band), fix, and pin the countdown rect INSIDE the viewport on every view row × both poses |
