# Director visual round 4 (2026-07-31, verbatim)

| # | Director's words | Reading / disposition |
|---|---|---|
| DV4-1 | "in the testing, i still see colored rings around the rows" (zoomed image: 1px identity-hued outlines on Flash/Bruno rows) | The DV-5 "resting identity hairline @0.7" itself — a wrong extraction (legacy captures show NO resting outline). **FIXED by the lead directly**: rest stroke fully transparent; identity = swatch only; stroke channel = tell ramp + drop verdict only. Pin flipped, suite 2768 green, confirmed in pixels post-restart. Remaining single ring = the framework FocusRingFloat (correct channel; suppresses under a real pointer — sitting check) |
| DV4-2 | "we need to improve the design of the race finish screen, especially in landscape on mobile. most of it isn't readable stuck in a tiny scroll view — it's not that there's one row being clipped, there's more info: the design is just bad — and there's a lot of empty space on the screen. leverage the ui-designer to figure out how to make this better." | **DIRECTOR COMMISSIONS A REDESIGN** of S16 (results/finish), landscape-mobile-first. Parity-to-legacy is explicitly NOT the bar for this screen's landscape composition. The ui-designer produces the improved design (inside the ratified content set + celebration/skip semantics); the running legacy-ladder extraction was redirected to ground-truth duty only. The director judging the built result on screen ratifies the UI-SPEC gate for this commissioned redesign |
| DV4-3 | "this also seems like exactly the type of thing luauui should solve well — we have a screen with information we declare, and want to ensure the ui auto-adapts well everywhere" | **FRAMEWORK DIRECTIVE**: declared-content adaptive composition is a LuauUI-owned mechanism, not per-screen ladder code. The game declares prioritized regions + minimum-viable forms + hierarchy; the framework chooses the arrangement per size class/height (the screen-level sibling of AdaptiveStack/ViewThatFits). The results screen's hand-authored collapse ladder (ResultsLayoutModel height guesses) is the smell this rule exists to kill. Disposition: S16 v2 is SPECIFIED as declared content + adaptation intent; the mechanism gap goes to the responsibility ledger as FRAMEWORK, implemented in LuauUI (public API + tests + docs + fixture proof) and consumed by the game — per the standing declarative-responsibility test |
