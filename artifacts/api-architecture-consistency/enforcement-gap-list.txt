check_registration EXPECTED-FAIL — api.md coverage gaps found by ENF-1 (a)+(b)
captured 2026-08-02T22:18:54Z · command: lune run tools/lune/check_registration_cli
These are the DOC package's work (DOC-2/-3/-8 and the rest). The checker ships
ENFORCING and exits 1 until they land; tests/extension_checker.spec.luau holds it
to 'no problem OTHER than these', so the list may only shrink.

check_registration: FAIL [UI-AGENT-001] — 14 problem(s):
  - public export 'text' has no `### `text`` heading in docs/reference/api.md (a mention in prose or a code sample is not a reference entry)
  - public export 'strokeData' has no `### `strokeData`` heading in docs/reference/api.md (a mention in prose or a code sample is not a reference entry)
  - nested public member 'motion.newValueReveal' is not documented in docs/reference/api.md — write it QUALIFIED (`motion.newValueReveal`) or give it a heading; a namespace's members are public surface, not implementation
  - nested public member 'text.fit' is not documented in docs/reference/api.md — write it QUALIFIED (`text.fit`) or give it a heading; a namespace's members are public surface, not implementation
  - nested public member 'text.measure' is not documented in docs/reference/api.md — write it QUALIFIED (`text.measure`) or give it a heading; a namespace's members are public surface, not implementation
  - nested public member 'text.size' is not documented in docs/reference/api.md — write it QUALIFIED (`text.size`) or give it a heading; a namespace's members are public surface, not implementation
  - nested public member 'tokens.dangerPair' is not documented in docs/reference/api.md — write it QUALIFIED (`tokens.dangerPair`) or give it a heading; a namespace's members are public surface, not implementation
  - nested public member 'adaptive.DEFAULT_STACK_ABOVE' is not documented in docs/reference/api.md — write it QUALIFIED (`adaptive.DEFAULT_STACK_ABOVE`) or give it a heading; a namespace's members are public surface, not implementation
  - nested public member 'spatial.PHASES' is not documented in docs/reference/api.md — write it QUALIFIED (`spatial.PHASES`) or give it a heading; a namespace's members are public surface, not implementation
  - nested public member 'spatial.of' is not documented in docs/reference/api.md — write it QUALIFIED (`spatial.of`) or give it a heading; a namespace's members are public surface, not implementation
  - nested public member 'spatial.HANDEDNESS' is not documented in docs/reference/api.md — write it QUALIFIED (`spatial.HANDEDNESS`) or give it a heading; a namespace's members are public surface, not implementation
  - nested public member 'touchGestures.normalize' is not documented in docs/reference/api.md — write it QUALIFIED (`touchGestures.normalize`) or give it a heading; a namespace's members are public surface, not implementation
  - nested public member 'interactionTokens.promotionForPointerType' is not documented in docs/reference/api.md — write it QUALIFIED (`interactionTokens.promotionForPointerType`) or give it a heading; a namespace's members are public surface, not implementation
  - nested public member 'valueModel.defaultFormat' is not documented in docs/reference/api.md — write it QUALIFIED (`valueModel.defaultFormat`) or give it a heading; a namespace's members are public surface, not implementation
hint: docs/extending/ has the playbook for each extension class; tools/lune/scaffold stamps complete skeletons
