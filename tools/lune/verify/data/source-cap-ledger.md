# Source-cap ledger — the modules inside the warning band

**What this file is.** `tools/check_source_size.py` fails any module in `src/`
that reaches **190,000** characters unless it has a row here, and fails outright
at **200,000** — the length at which Roblox refuses a `Script.Source` write and
a module silently stops live-syncing into an open Studio session
(`docs/lessons/the-200k-source-cap-is-on-writing-not-loading.md`).

**Why a ledger rather than a warning.** A warning is a thing you scroll past.
Before the band existed (release-candidate review MAINT-1, 2026-08-17) the
check's only size branch was `>= CAP`, so the first signal was the commit that
made a file unsyncable — and the remedy the checker's own header prescribes
(find a seam by the mutable-upvalue test, prove it with a live Studio A/B) is a
multi-hour mission, not an edit. Five modules were inside 10 KB of the cap and
`renderer.luau` was **350 characters** from it. The band converts that cliff
into a requirement to have done the thinking while it is still cheap.

**What a row must carry**, all three mechanically enforced:

1. **its size when the row was last touched** — within 2,000 characters of the
   file's current size, or the check fails and asks for both the number and a
   re-read of the analysis. A file that moved is a file whose seam analysis is
   worth looking at again;
2. **a seam analysis** — the one-way extraction candidates by name, or the
   recorded reason none exists. "One-way" is the checker header's own test: a
   block that reads no mutable upvalue of its host, so moving it cannot create a
   require back;
3. **the trigger that ends the deferral** — the condition under which the next
   extraction happens, not a date. **State it the way the rows below do — as a
   CONDITION THAT HAS OR HAS NOT ARRIVED, never as a number the file has already
   passed.** A trigger permanently in the fired state carries no signal about
   when to act, which is the same class as a check that cannot fail: the
   `solver.luau` row was written at 188,050 with a trigger of 188,000, and the
   next round put 420 characters in without an extraction preceding them,
   because nothing could warn it. If a number is right, it is a number ABOVE the
   recorded size and the reasoning for the gap is stated; otherwise the trigger
   is "ARRIVED" and the next extraction is named.

**What an extraction owes on the way out** (learned 2026-08-21, the first split
this ledger took):

**AN EXTRACTION OUT OF AN `EXTRACTION_LOCKED` MODULE INHERITS NONE OF ITS
EXEMPTIONS.** `tools/check_comment_codes.py` holds a tuple of the five files this
document governs and counts their private comment codes without gating them —
the sweep is owed to the extraction that owns them. Move a block out of one of
those files into a NEW module and the new module is not in the tuple, so codes
that were exempt a line ago are live orphans against a ceiling of zero. It
happened on the first split (two codes rode `recordTextFacts` and `sides` into
`layout/measure_facts.luau`) and the check went red on the split commit while
the report beside it certified a pass. **Run `check_comment_codes` on the split
commit itself**, and when the extraction lands, **take the file out of the
tuple** — an exemption owed to an extraction ends when the extraction does.

## The band

| module | size | seam analysis | next-extraction trigger |
|---|---|---|---|
| `src/controls/row_actions.luau` | 195,894 | **THE dump() EXTRACTION TAKEN, 2026-08-29 (task EXTRACT2) — AND THE PRIOR TWO RECORDINGS OF THIS ROW WERE MEASURED WRONG, not merely stale.** `src/controls/row_actions_dump.luau` (2,855 characters) now holds the schema build; `dump()` itself was 928 characters closing over six names, not the "42 KB — 22% of the file" / "closes over ~a dozen live upvalues" this row claimed when it was written (`b0c040d`, 2026-08-17, file at 192,979) and repeated at its 2026-08-22 re-recording. Measured directly at `b0c040d` itself: `dump()` was already 928 bytes there — the number was never real, at any point this row has existed. The likely mechanism (recorded because this document's rule about invented numbers does not stop at its own rows, and this project has hit the identical failure mode before — `tools/check_source_size.py`'s own header, on `grid.luau`: "the estimate counted the two plan functions and not the ~430 lines of block comment"): 22% of 192,979 is ~42,455 characters, almost exactly the span from `dump()`'s declaration to end-of-file — the TAIL of the file, not the function. `commitDestructive (7 KB)` has the same shape: the real function is 291 bytes; the other ~7,900 characters are the menu-architecture comment block that FOLLOWS it (documenting `buildMenuRows` onward), not `commitDestructive` itself. `doCommitAction` (~9.7 KB measured) is the one figure that held up. **NO FURTHER EXTRACTION IS AVAILABLE, independently confirmed three ways rather than merely asserted.** (1) This file's OWN header (lines 12-48): "THE ONE ENGINE... ~65 nested functions share ~60 mutable upvalues (`spring`, `controller`, `drag`, `committed`, `commitLatched`, `commitEpoch`, `menuHandle`, `rootPath`, `restorePending`, …) deliberately... threading a state record through them is a rewrite of code four device rounds hardened, not an extraction." (2) The same header's "THE ONE ENGINE" section: "the state signals, axis lock, velocity tracker, coordinator claim, commit ladder, menu and keyboard bindings are all shared code, never a fork." (3) `tools/check_source_size.py`'s own header: "FIRST ROW CLEARED: `src/controls/row_actions.luau`, 2026-08-14, 234,757 -> 183,738, in six commits and without touching `buildEngine` — the ONE ENGINE whose ~3,230 lines share ~60 mutable upvalues on purpose." The six siblings named there (spec/coordinator/reorder/metrics/trays/root), plus `row_actions_dump` now, are the whole periphery. `doCommitAction`+`commitDestructive` (the irrevocable-commit machine, confirmed still coupled), the action-menu subsystem (`menuHandle` is one of the header's own NAMED must-stay upvalues — `menuHandle`/`presentModalFn`/`dismissFn` are reassigned exactly the way the header forbids threading out), the four gesture handlers (`onPointerDown`/`Move`/`Up`/`Cancel`, the drag state machine) and `syncGeometry`/`deriveNaturals`/`handleActivate` were each measured directly (task EXTRACT2) against the same one-way test this document judges every split by, and every one reads or writes a mutable upvalue of `buildEngine`. Comment-trimming was considered and rejected on the grounds `check_source_size.py` already recorded for `virtual_list.luau`: "TRIMMING 3.4 KB OF COMMENT... is the wrong lever... the comments... carry several device rounds of reasoning." | **TRIGGER: ARRIVED, in the only sense left available, and the size relief it presumed did not exist.** This file crossed 196,000 (the previous trigger) at `c37fe9d`; the dump() extraction — done in full, per the row's own instructions — recovered 146 characters net (196,040 pre-extraction -> 195,894), because the function it named was never the 42 KB the trigger's math assumed. The honest next step is not another same-shaped extraction round: it is a director-approved rewrite of `buildEngine`'s ~60-upvalue model (the header's own "a rewrite... not an extraction," its own flagged mission) or accepting the file keeps tightening toward the 200,000 write cap on ordinary maintenance. **Immediately if this file passes 198,000** — 2,106 away, closer to the hard cap than any module has been left in this document — is the number that can still warn someone; below it, every further character spent in this file should go to whichever caller needs it least. |
| `src/render/renderer.luau` | 196,849 | **RE-RECORDED AT THE ROUND'S CLOSE, 2026-09-01 (P3 landed): 193,797 -> 196,849 (+3,052), measured after `stylua`.** P3 spent it on five things and nothing else: the `layoutNodeReuse` attach opt (the differential oracle's switch, exactly as `measureReuse` is), the store's four declarations beside the incremental-layout and measure-reuse state, `markNodeDirty` (axis 2 — every dirty path plus its ancestors, of EVERY class, accumulated in the walk `refresh` already makes), the build site with the dirty-SEQUENCE refusal that closes the settle-solve hole, and `stats.lastNodeBuilds`. **THE REASONING WENT TO `src/render/layout_node.luau`** — the four axes, the weak-key premise, why the key is the node TABLE and why the theme key is APPLICATION rather than emission — which is where that file's 121 KB of headroom is and what the `measure_reuse.luau` precedent says to do; the prose here was cut to its load-bearing claims twice on the way in. **Headroom 3,151, against the 1,187 the file had when the round opened.** The extraction bought 5,016 and P3 spent 3,052 of it. **THE DRAG BRIDGE'S ONE-WAY HALF IS OUT, 2026-09-01 (FacetBench Plan 3, perf campaign P3): 198,813 -> 193,797, headroom 1,187 -> 6,203.** The row below said the trigger had ARRIVED ON SIZE ALONE and that the next change here of any size takes the DRAG BRIDGE first; P3 needed room in this file, so it did. `src/render/autoscroll_bridge.luau` (10,261 chars) holds the drag-to-edge autoscroll bridge: `setPointerDrag`, `scrollChainAt`, `autoscrollOptsFor` and `stepAutoscroll`, with the chain rule, the band policy and the transparency argument travelling with them. **WHY THIS HALF AND NOT THE WHOLE ~12 KB.** The row names four functions across two blocks. This one scores ZERO on the one-way test in the strongest form any seam in this repository has reached: its two reassigned scalars (`pointerDrag`, `autoscrollClock`) were declared inside it and read nowhere else, so the STATE LEFT WITH THE CODE (`present/catchers.luau`'s precedent) and no record was needed; the nine names that cross are four by-reference tables, two requires, one `local function`, the `controller` table and `adapter`, which is `attach`'s own parameter — each declared once and never reassigned; and the module requires NOTHING, so the one-way claim is total rather than argued. The REGISTRY half (`dragRegistry`/`attachDetector`, ~5 KB) is NOT taken and is the next named seam: `dragRegistryInstance` is read from eleven places in this file — the tap-echo guard, the structural refresh, three public controller methods and `dispose` — so it needs the shared-record move `controls/table.luau` proved, which is a different commit from a verbatim move. **THE EVIDENCE, BECAUSE A SPLIT IS A BEHAVIOUR CLAIM, AND THE FIRST HALF OF IT IS AN ADMISSION.** The suite went 7,907 -> 7,926 with NO spec edited and none red — which proved that nothing ELSE moved and said NOTHING about the block that did, because **there was no case in the suite that drove `controller.stepAutoscroll` at all**: `tests/autoscroll.spec` covers the pure model (`input/autoscroll`) and `tests/collection_list.spec` covers `virtual_list`'s own stepper, and neither reaches the renderer's chain. So the +19 is `tests/autoscroll_bridge_seam.spec.luau`: twelve mechanised seam rules (READ == DECLARED == PASSED both ways, the mutable-upvalue test over the nine names, the no-rebind test from the module's side, no module-level mutable state, EXPORTED == BOUND, one consumer, a require-nothing module, each with a negative control) plus SEVEN cases that drive the real bridge through `renderer.attach` and the fake target on a nested chain: the dwell, the canvas write, the innermost winner, the `autoscroll = false` opt-out, retraction, the leave-the-chain disarm, and the CHAIN RULE — a pinned inner list is transparent and the page behind it wins. **Six mutations were proved to bite** (drop `canTravel`; ignore the opt-out; drop the retraction reset; reverse the chain order; drop the clip-only guard; drop the non-winner disarm), and two of them only bite because the fixture was corrected after the first attempt did not: the inner list is 40 rows and not 10, because a short one hits its own end on the first armed step and `canTravel`'s reset then masks the retraction reset entirely. A mutation that does not bite is a case that proves nothing. **RE-RECORDED AGAIN 2026-09-01 (P1 review fixes): 197,811 -> 198,496. HEADROOM IS 1,504 CHARACTERS — the closest this file has ever been recorded to the write cap.** The review round added only the `analyzeBoundaries` reuse/store null-out and its reasoning, both trimmed once already, with the rest of the explanation pushed into `src/layout/measure_reuse.luau` and `tests/measure_reuse.spec.luau`. **THE TRIGGER HAS NOW ARRIVED ON SIZE ALONE, not only on shape: the next change here of ANY size takes the DRAG BRIDGE extraction (~12 KB) first.** A comment is no longer a safe edit at this distance. **RE-RECORDED 2026-09-01 (FacetBench Plan 3, perf campaign P1): 195,798 -> 197,811 (+2,013), measured after `stylua`.** The round added the per-SURFACE cross-solve measure store beside the incremental-layout reuse state (`lastResult`/`dirtyContains`): one `require`, one attach opt (`measureReuse`, the differential oracle's switch), the store's declaration, and the solve-wide stamp that invalidates it. `src/layout/measure_reuse.luau` carries the four invalidation axes, which is where that reasoning went instead of here. **THE SEAM ANALYSIS BELOW IS RE-READ AND UNCHANGED, and so is its trigger**: nothing here touched `structuralSync`, `ensureTree`, the recycle pool, the removal sweep or the parked-props record, so the DRAG BRIDGE extraction the row names is still NOT ARRIVED. **But the headroom is now 2,189 characters, the tightest this file has been since the commit-walk split** — the next round that spends more than a comment here takes the drag bridge (~12 KB) first, whatever it is changing. **RE-RECORDED 2026-08-23 (framework-gaps-phase2, task FIX-4): 192,666 -> 195,798 (+3,132), measured after `stylua`.** The round added the `edgeFloor` opt-in edge-padding knob (director ruling): a new `RendererAttachOpts` field with its own doc block, an `ATTACH_OPTS_KEYS` entry, an attach-time refusal for `edgeFloor` + `rootPolicy = "edgeToEdge"` together, and the floor's application inside `solveAndApply` right beside the existing `bandSafeContent` gutter floor it composes with (`max`, never a second addition) — none of it touching `structuralSync`/`ensureTree`/the drag bridge, so the trigger this row already names (DRAG BRIDGE first) is UNCHANGED and still NOT ARRIVED. **Still 4,202 clear of the 200,000 write cap**, 5,798 past the 190,000 warning line. **RE-RECORDED 2026-08-23 (framework-gaps-phase2, task W3-C): 190,088 -> 192,666 (+2,578), measured after `stylua`.** The round added three small, narrow reads/writes near the file's OTHER end from the recycle-pool/parked-props machinery this row's own trigger watches — `mountedPathOf` (gap 15, a `handles`-keyed lookup beside `stageHost`/`foreignHost`), `screenRectOf`'s optional `relativeTo` second argument (gap 37, one more branch on the existing function), and two dirty-filter `or entry.prop == "offset"` clauses (gap 16, alongside the existing opacity/scale/rotation checks) — none of it touching `structuralSync`/`ensureTree`/the drag bridge, so the trigger this row already names (DRAG BRIDGE first, for a recycle-pool/parked-props/removal-sweep change) is UNCHANGED and still NOT ARRIVED. **Still 7,334 clear of the 200,000 write cap**, 2,666 past the 190,000 warning line the file has been past since G8+G9. **THE COMMIT-WALK SEAM WAS TAKEN, 2026-08-21, and it is the row this file was waiting on.** `render/commit_walks.luau` (36.5 KB at close; 26.7 KB when the split landed) holds the six write loops that spend a finished solve: `textScale` and `padding` (the two typography paint seams), `textVerdicts` (the compact-ladder and wrap verdicts), `visible` (the hidden-candidate paint walk), `hitRects` (the 44px target floor) and `scrollRegions` (the native canvas, its autoscroll and its indicator opt-out). **THE PRESCRIPTION BELOW NAMED A SEAM THAT HAD ALREADY LEFT, and that is the first correction this row owes.** It read "lift the RECT WRITE half of `solveAndApply` — the diff-and-write loop — once `lastRects` is owned by a record object"; the rect diff-and-write loop went to `render/rect_pass.luau` on 2026-08-17 (`c2b68e5`), ONE DAY BEFORE this row was written, and it took `lastRects` with it as a by-reference table rather than needing a record at all. So what shipped is the REST of the same half — the five other write loops the commit span runs beside it — and the technique the prescription predicted is the one that was used. **The second correction: this seam cost NEITHER a record NOR an accessor either**, for the reason `measure_facts` did not: all twenty-one names that cross (twenty at the split, plus `inputSinks` when ruling R23 needed the census's own value) were ALREADY tables, functions or a parameter, each declared once and never reassigned. The mutable-upvalue test is not a promise here — `tests/commit_walks_seam.spec.luau` runs it against all twenty names over the live sources. **WHAT IS LEFT, honestly ranked.** `structuralSync` (49 KB with the pool, the removal sweep and the parked-props machinery) and `ensureTree` (22 KB) still fail the test by construction — both reassign or read reassigned locals (`captureParkedProps`, `restoreParkedProps`, `recyclePooled`, `parked`, `hint`) and both mutate the live handle map they are handed. What remains of `solveAndApply` (~19 KB) is the SOLVE and its ordering, which is the renderer's own decision and is where the reasoning for that order lives. The next honest candidates are the DRAG bridge (`dragRegistry`/`attachDetector`/`autoscrollOptsFor`/`scrollChainAt`, ~12 KB, reading `dragRegistryInstance` and `pointerDrag` — two reassigned scalars, i.e. exactly the `table.luau` record move) and the pure `rectsEqual`/`findNode` helpers (~1 KB, real but small). | **TRIGGER: NOT ARRIVED, restated 2026-09-01 at P3's close.** Headroom is 3,151 — nearly 3x what the round opened with, and one feature short of the band being a problem again. The next extraction is owed by SHAPE, not by size: **the REGISTRY half of the drag bridge (`dragRegistry`/`attachDetector`, ~5 KB) comes out before the next round that changes drag acquisition, the tap-echo guard or the recycle pool / removal sweep / parked-props record**, and it costs a shared record for `dragRegistryInstance` because eleven sites in this file read that scalar. Failing that, **this file passes 198,500** — 1,651 above the recorded size, and half of what the P3 round itself cost, so a round that reaches it has already spent more than the last one did. **BACK INSIDE THE BAND 2026-08-22 (framework-gaps-phase2, task G8+G9): 183,684 -> 190,088.** The round added `rootPolicy = "bandSafeContent"` — a fourth policy branch, its `platformChrome` read, a comparator for the one NESTED geometry fact (`sameChrome`), the geometry-key swap that narrows a band-riding surface to the fact its policy actually reads, and the gutter floor — plus the reasoning each of those needs. Most of the growth is COMMENT and it was trimmed twice on the way in; the deferral argument now lives once, in ADR-0046 §2 and `docs/handoff/G8G9-OWED-LIVE-WORK.md` §5, with a pointer here. **THE ROW WAS ALSO MALFORMED AND IS FIXED IN THE SAME EDIT:** it was hard-wrapped across four physical lines, so `check_source_size`'s parser saw three cells and skipped it — which is why this file reported as having NO row rather than as a drifted one. A ledger row that cannot be parsed is the thing this document's own head calls a place text goes to die, and the fix is one physical line. **TRIGGER: ARRIVED.** `ensureTree` is the largest single block and is NOT one-way — it reads and writes the recycle pool, the handle map and the per-node prop signatures, so it would carry a record of five reassigned upvalues. The honest next seam is smaller and pure: the POINTER/SCROLL QUERY block (`isPointerZone`, `scrollChainAt`, `autoscrollOptsFor`, `paintsNothing`, ~6 KB) reads the solved rects and the adapter and WRITES NOTHING, which is the one-way test this document decides every split by. **The rule is unchanged: PRECEDE the change** — the next round that opens this file for anything larger than a comment takes it first. **RE-RECORDED at the round's close, measured after the last `stylua` pass: 183,684, which is 16,316 to the write cap and 6,316 clear of the warning line.** Phase B spent 27 characters in this file, the comment sweep 441, the vendor sweep 51 and fix round 1's ruling R23 (which the input-sink census had to learn) 1,162 — one `inputSinks` line on the walks' construction call — and every other character of the hit-floor rule (ADR-0041, B-19) went into `commit_walks.luau`, which is what buying the room was for. **THE SPLIT LANDED AND THIS ROW LEAVES THE BAND: 198,974 -> 182,003, which CLEARS THE 190,000 WARNING LINE BY 7,997 and turns 1,026 characters of headroom into 17,997.** The file entered this round as the closest any module in this repository has been to the `Script.Source` write cap, with its extraction OWED BEFORE the next change of any size — and the extraction PRECEDED this round's own product change rather than accompanying it, which is what the previous wording demanded. **The evidence, because a split is a behaviour claim.** (1) The full suite is 7,000 -> 7,016 with the verdict SET diffed line-for-line against a content-pinned baseline: ZERO removals, and the sixteen added rows are the seam spec's own — nothing else moved. (2) NO SPEC WAS EDITED. One went red and it was the instrument rather than the verdict: `button_shape.spec` pins the line `adapter.setProp(handle, "textWrapped", wraps, "layout")` in "the renderer's source", and that line moved with the walk. `tests/lib/renderer_source.luau` — whose own header says ADD A PART HERE WHENEVER MORE OF THE RENDERER MOVES OUT — gained the part, and the pin went green reading the same line in its new file. That is the cost this file's splits always pay, and it is why the instrument exists. (3) A differential oracle over 800 seeded blueprint trees, each driven THREE times (mount, a viewport change, a ten-foot flip) because five of the six walks are minimal-write and the interesting output is the writes the later frames DO NOT make: the adapter's ORDERED operation log plus every per-path field the walks own (rect, z, hitRect, visible, textSize, textFont, padding, label, icon, compactLabel, textWrapped, the scroll region and its window) plus the work counters — **328,839 comparison lines, `diff` exit 0, byte-identical**. Its three negative controls bite (the floor stops inflating: 456 lines; the indicator opt-out is ignored: 6; the text scale is dropped: 86). **The seam spec is 16 cases and THIRTEEN mutations were proved to bite**, including the two this seam needed that no previous one did: a THIRTEENTH shared write (the twelve-name write surface is what the two files share, and `paintHeld` is presenter-owned and must stay read-only here), and a per-path record whose removal-sweep clear is deleted — the lifetime claim, mechanised, because the renderer keeps these records precisely for the half it owns. **TRIGGER: NOT ARRIVED, and the gap is stated. 18 KB of headroom is the largest this file has had since the cap was measured**, so the next extraction is not owed by size. It is owed by SHAPE: **the next mission that changes the recycle pool, the removal sweep or the parked-props record takes the DRAG BRIDGE first** (~12 KB, and the two reassigned scalars become one record, the move `table.luau` proved), because that is the block whose growth would put this file back in the band. Failing that, **this file passes 190,000** — 7,997 above the recorded size, which is roughly two features of the size the ADAPT-FIX wave was. **THE EXEMPTION IS PAID OFF IN THE SAME ROUND, which is this document's own new rule and it cost 441 characters**: `renderer.luau` is OUT of `check_comment_codes`' `EXTRACTION_LOCKED` tuple. Forty-one private codes became prose — 33 that resolved nowhere and 8 that resolved but would have pushed the 25/25 total ratchet to 33 — so the check now reads this file live, at 0 orphans and a total unchanged. The tuple is three modules and 130 codes where it was four and 171. **THE SAME DEBT EXISTS TWICE AND ONLY ONE OF THEM IS NAMED IN THIS DOCUMENT**: `tools/check_brand_drift.py` carries a second allowlist over the SAME five files with the SAME removal rule ("when the renderer extraction lands"), and it was RED when this round ran it — one match here and one in `layout/measure_facts.luau`, which is the SOLVER split's own escape riding out of its locked host and nobody's guard catching it at the commit that caused it. **THE DURATION WAS INVENTED AND IS CORRECTED HERE (review MEDIUM-2): measured `f0fc77e` 17:53 to `753e088` 21:03, the guard was red for 3 HOURS 10 MINUTES, not three days** — and this round's own `4cc5cb0` added the second violation 1h54m into that window. Everything substantive about the claim stands; it is the durability figure that was made up, in the document this project treats as ground truth. Both are swept and both allowlist positions are closed; the entry for this file is gone. **A file that leaves an extraction lock owes EVERY list keyed on that lock, not the one the last round happened to hit.** **AND THE ROUND ALMOST CERTIFIED A CHECK IT HAD NOT RUN**: `check_comment_codes` walks `git ls-files`, so on the split commit it could not see the new module at all and reported PASS; the three codes that rode out of here into `commit_walks.luau` (`ADAPT-7`, `RS-A16`, `NS-A2`) were live orphans the moment that file was tracked. Run it AT the commit, on the tracked tree, exactly as the rule above says. |
| `src/present/presenter.luau` | 178,325 | **THE SURFACE-LIFECYCLE SEAM WAS TAKEN, 2026-08-22, and it is the seam this row named.** `present/surface_lifecycle.luau` (25,808) carries what the analysis below predicted, to the name: `syncFocusVisuals` and the three armed-follows-focus scalars it owns (`lastArmedAim`, `armedSeen`, `prevFocusPath`), `refresh`/`refreshBody` with `focusDerivation` + `syncFocusMap`, and the dismissal pair (`dismiss`, `back`). They are ONE subject read three times — `refresh` ends its loop by calling the focus-visual sync and `dismiss` un-paints the surface leaving the stack, the two halves of a decision this file spent seven director rounds reducing to one copy — so splitting them across two modules would have re-created the duplication the round-7 rule exists to prevent. **`makeHandle` was not touched and is not a candidate at any size**: it is now 85,693 characters, 48% of the file, and the room comes from around it, never from inside. **THE COST WAS ONE RECORD.** Twenty names cross and nineteen were already by-reference tables, functions declared once, or the presenter instance. The twentieth is `displayLayer`, the cross-surface z counter — `makeHandle` INCREMENTS it, `dismiss` RESETS it on an empty stack — so the scalar became `{ value = 0 }` shared by reference, the `table.luau` technique this document names. **AND THE SEAM PAID FOR ITSELF ON THE WAY OUT, which is the finding worth carrying forward.** Three forward declarations (`syncDisclosureFocus`, `syncHelpFocus`, `dismissDisclosure`) existed ONLY because the focus observer sat above the plate machinery that assigns them; with the observer gone they are plain `local function`s at their definition sites, so the presenter has THREE FEWER mutable upvalues than before the split — which is what let the new module take every collaborator as a FINAL value instead of through a late-binding record. An extraction can leave its host MORE one-way than it found it, and that is worth looking for before reaching for a record. (`dismiss` also carried its red-team `displayLayer` comment TWICE, verbatim, eleven lines each; one copy is gone.) **WHAT IS LEFT, honestly ranked.** `makeHandle` (85,693) is the surface lifetime itself — mount, render, transitions, the tap handler, the critical-screen fallback, all reading the presenter's live stacks — and that judgement, first recorded by the checker's own header, still stands. The next honest candidate is the **FULL-VALUE DISCLOSURE plate** (~26 KB): the `DISCLOSE_*` constants, `safeBox`, `disclosureTargetIn`, `handleOwning`, `disposeDisclosure`/`dismissDisclosure`/`clampDisclosure`/`presentDisclosure`, `plateShows`, `syncDisclosureFocus`, `syncHelpFocus`, `discloseHoverEngage` and `revalidateDisclosure`. It is NOT one-way as written and the reason is stated so nobody re-derives it: `disclosurePlate` and `disclosureDwell` are two reassigned scalars (the same record move this round just made once), and BOTH are read by `self.tick`'s dwell timer and clamp re-run, so the record has to be threaded there too or that slice of `tick` travels with them. Behind it, the TOAST region is smaller (~7 KB) and costs MORE: six reassigned scalars (`toastTime`, `toastPosition`, `toastTransition`, `toastLayer`, `toastSchedule`, `syncToastLayer`), which is a record with six fields rather than one. | **RE-RECORDED 2026-08-22 (framework-gaps-phase2, task PRES-X), MEASURED LAST, after `stylua`: 196,639 -> 178,325. THAT CLEARS THE 190,000 WARNING LINE BY 11,675 and leaves 21,675 characters to the write cap, against the 3,353 this file had when the round opened.** The row is now ADVISORY rather than enforced (`check_source_size` reads a row only for a file inside the band) and is kept because the next reader deserves the analysis rather than a blank page. **The previous size in this cell was 196,647; the file measured 196,639 bytes at `571ba4f` by `check_source_size`'s own measure (`len(read_bytes())`) — an eight-character discrepancy in a recorded number, corrected here rather than quietly overwritten.** **The extraction PRECEDED the change it was owed to** — the HUD band-policy round opens this file next — which is what this row's previous wording demanded and what `renderer.luau`, `solver.luau` and `table.luau` each learned the hard way one file over. **The evidence, because a split is a behaviour claim.** (1) The full suite is 7,135 -> 7,155 with the verdict SET diffed line-for-line against a content-pinned pair (`e514ebc~1` vs `e514ebc`): ZERO removals, and the twenty added rows are the seam spec's own — nothing else moved. (2) Rascal Rally is 3,483 -> 3,483 and its transcript is BYTE-IDENTICAL. (3) **An earlier arm of the identical source change, taken before the seam spec existed, produced a BYTE-IDENTICAL FACET TRANSCRIPT — 7,125 rows, `diff` exit 0** — which is the strongest form this claim has: not "the same verdicts" but the same file. NO SPEC WAS EDITED and none went red; unlike `renderer.luau`'s split, no test pins a line of this file's source. (4) `tests/surface_lifecycle_seam.spec.luau` is 20 cases in 5 describe blocks and **FIFTEEN mutations were proved to bite**, including the two this seam needed that no previous one did: a second writer through the shared record, and a bare `displayLayer = 0` on either side of the seam (which type-checks fine against `any`, silently unshares the object, and is only observable a hundred present/dismiss cycles later). **ONE MUTATION WAS A FINDING RATHER THAN A DRILL:** the "exactly ONE consumer" rule was pinned to the literal `require("./surface_lifecycle")`, so a planted second consumer one directory over (`require("../present/surface_lifecycle")` in `src/controls/virtual_list.luau`) left it GREEN. It now matches the path's tail. **`measure_facts_seam.spec` has the same sibling-only shape and the same blind spot, and is left OWED** — that file belonged to another lane this round. **A CORRECTION TO THE EXTRACTION COMMIT'S OWN MESSAGE (`e514ebc`): it says the seam spec is "25 cases". Measured, it is 20 cases in 5 describe blocks — 25 is the number of TRANSCRIPT LINES, five of which are the block headers.** Recorded here because this document's rule about invented numbers does not stop at its own rows. **THE `EXTRACTION_LOCKED` DEBT IS NOT PAID AND IS BOOKED HERE RATHER THAN LEFT SILENT.** The seven private comment codes that rode out of this file became prose on the split commit, per this document's rule, and `check_comment_codes` was run AT that commit on the tracked tree (the new module carries ZERO codes). What did NOT happen is the other half: this file leaves the band but is still in the checker's `EXTRACTION_LOCKED` tuple, holding **49 private codes of which 38 are orphans**. Taking it out is a prose sweep of 38 sites plus an eleven-row rise in `TOTAL_CEILING`, and it was not folded into a behaviour-neutral prep round whose whole evidence is a line-for-line transcript diff — a 38-site comment rewrite would have buried that diff. **It is OWED to the next round that opens this file for anything larger than a comment**, ahead of the disclosure extraction. **TRIGGER: NOT ARRIVED, and the gap is stated.** 21,675 characters of headroom is the most this file has had since the cap was measured, so the next extraction is not owed by size. It is owed by SHAPE: **the next mission that changes the disclosure plate, its dwell timer or its clamp takes the FULL-VALUE DISCLOSURE region first** (~26 KB, and the two reassigned scalars become one record threaded into `tick`), because that is the block whose growth would put this file back in the band. Failing that, **this file passes 186,000** — 7,675 above the recorded size, which is roughly two features of the size the ADAPT-FIX wave was, and deliberately a number this file has not already passed.

## Cleared the band

A module that lands under 190,000 stops being enforced — `check_source_size`
reads a row only for a file inside the band. The row stays anyway: the next
reader of a file that took a hard split deserves the analysis and the seam that
is next, not a blank page and a re-derivation.

| module | size | seam analysis | next-extraction trigger |
|---|---|---|---|
| `src/controls/table.luau` | 191,925 | **THE BUILDER SEAM WAS TAKEN, 2026-08-18, and it is the row this table was waiting on.** `rowBlueprint` + `rowPointerHandlers` — with `isHandleZonePress`, the ≡ handle's mouse-presenting wrapper, `cellContentFor` and the drag-promotion token only they read — went to `controls/table_rows.luau` behind a 34-field parameter object, exactly the shape the previous trigger predicted would make them one-way. **What it cost was TWO SCALARS**: `rowDrag` and `suppressActivatePath` were reassigned locals, and a reassigned local cannot be shared by reference at all — they are now one record (`rowGesture`) held by both halves. The mutable TABLE they also share (`rowActionsByKey`) cost nothing, because a table already IS a reference. `tests/table_rows_seam.spec.luau` mechanises the property: READ == DECLARED == PASSED as three set comparisons over the live sources, the shared write surface pinned to those two names, and the require asserted not to come back — nine cases, three mutations proved to bite. **The two earlier seams stand**: the DIR-4 width clamp (`table_columns.luau`, 2026-08-17) and the pure decisions that left before it (`virtual_extents`, `row_capability`, `row_actions_state`, `native_scroll_binding`, `text_fit`). What is left is still ONE `newTable` closure, and every large block in it still reads mutable instance upvalues — but the technique that answers that is no longer theoretical in this file, it is shipped and tested. | **RE-RECORDED 2026-08-18 after wave TABLE stage 2: 173,384 -> 185,949, which is 14,051 characters of headroom and 4,051 below the 190,000 warning line.** The column priority-collapse paradigm (ADAPT-18/19) spent 12.6 KB of what the builder extraction bought, and it paid part of that back the same day: the disclosure — the plate's facts, its list and its sheet — went to `controls/table_disclosure.luau` (10.9 KB) the moment writing it in place put this file back inside the band. That is the technique working as intended rather than a surprise, and it is the third seam this file has taken. **The margin is now thin enough to name: the NEXT feature of this size must extract before it writes.** This row is now advisory rather than enforced (`check_source_size` reads a row only for a file inside the band), and it is kept because the next reader deserves the analysis rather than a blank page. **THAT SEAM WAS TAKEN THE SAME DAY.** The trigger it set ("this file passes 185,000") fired on the very next feature, which is what a trigger is for: the header band — the headings, their sort marks, `gripFor`, `forwardUnclaimedGripTap` — went to `controls/table_header.luau` (18.3 KB), and the analysis's own prediction held exactly: `resizeDrag` was the one reassigned scalar and became a record (`resizeGesture`), while `renderController` — reassigned too, but only READ across the seam — took an accessor instead, the `screen_paint` split. THE NEXT SEAM, and it is smaller: what is left inside `newTable` is the drag MODEL (`applyDragPosition`/`liveDragRects`/`cancelDrag`/`insertSlotAt`, ~4 KB), the selection and grab model (~6 KB), `api.handleActivate` (~10 KB) and `buildFocusGroups` (~5 KB). The activate ROUTER is the one with a shape — it is a chain of independent path matchers, each of which could be a rule in a table — but it reads eight mutable upvalues and moving it means passing them, which is a refactor rather than a move. Trigger: this file passes 188,000, or the next mission that adds a route to `handleActivate`. **RE-RECORDED 2026-08-22 (bug round FIX-SHOW, Bug C): 189,197 -> 189,566, and 369 of that is this round's.** The header band could not see edit mode — its gutters and the row's `cellsEditPadding` derived the leading inset twice and only one of them was edit-aware — and the repair was written to cost this file as little as possible: the DERIVATION lives in `controls/table_header.luau` and the PUBLICATION in `controls/table_rows.luau`, both outside the band, and what landed here is one owned signal and five dependency lines. 434 characters below the warning line, and the extraction named above is still the one owed. **RE-RECORDED 2026-08-20 (review round 1, ISSUE-3), and the correction is the finding: this row said 181,588 while the file was 185,190 — a drift of 3,602, which is 1.8x the allowance that would have FAILED it in band, and invisible to the checker only because the split had moved this row OUT of the enforced band. That is exactly the defect this wave's own opening commit corrected on `renderer.luau` — "the ledger telling the truth about a file somebody else grew" — reproduced on the file the wave itself grew. The number above is now the size after the review's fix round.** **AND THE 188,000 TRIGGER HAS FIRED: 189,197, which is 811 below the warning line.** The fix round spent the remaining slack on the one-state disclosure contract (review HIGH-1) and the reasoning that has to travel with it. THE NEXT CHANGE OF ANY SIZE MUST BE PRECEDED BY AN EXTRACTION, not accompanied by it — and the candidate is now cheaper than anything above it, because this round created it: the COLLAPSE/DISCLOSURE STATE (`hiddenColumns`, `anyColumnHidden`, `moreLabel`, `columnHidden`, `columnById` and the `MoreWhen` band, ~4 KB) belongs in `controls/table_disclosure.luau`, the file already named for it, and is one-way TODAY: it reads `columnLayout`, `scope`, `core`, `UI` and `bandPadding`, and NOT ONE mutable scalar — so it needs neither a record nor an accessor, which no previous seam here could say. **RE-RECORDED 2026-08-29 (framework-gaps-phase2-followups, task SHOWCASE3): 189,566 -> 191,925 (+2,359).** The row band's own vertical seam (`Main`'s `gap`, between the auto Edit/Done toolbar, the header and the disclosure band) was a flat `controls.table.rowGap` LITERAL (2px), never touched by any shipped package, so a themed control's own `shadow = "raised"` glow on the toolbar/chip/header plates (up to `chromeBleed` px past each box) overlapped its neighbour almost entirely under Glossy Mobile at a narrow phone (director repro). The fix is a new reactive memo (`rowBandGap`, beside `bandPadding` — the SAME class of fact, read the SAME way) that maxes the literal against the live snapshot's `chromeBleed`, and one call site swap (`gap = "controls.table.rowGap"` -> `gap = rowBandGap`). No seam moved; this is pure growth from one memo and its rationale, which is why it is recorded rather than trimmed. |
| `src/layout/solver.luau` | 199,890 | **RE-RECORDED AGAIN 2026-09-01 (perf campaign P2, the copy-on-write rect map, and its review round): 198,439 -> 199,852 -> 199,890 (+1,451), measured after `stylua`. The review round bought its own room: `table.freeze` on the solved rect went in and the handover comment was rewritten plainer and 134 characters shorter for it. HEADROOM IS 110 CHARACTERS — the smallest this file has ever had, and it is the last change that lands here without an extraction in front of it.** The round DELETED `replaySubtree` (the recursion and its comment, ~1,150 characters) and spent the room on the skip's own reasoning, the map's ownership contract at `out`, and two counters — `rectInserts` and `walkedIds`, the second replacing `skippedIds` one for one. The task brief that ordered it named the constraint and forbade a split inside it, so the size math was taken BEFORE the first character was written and the comments were cut to their load-bearing claims rather than the file's other rows being raided for room. **THE TRIGGER IS NOT MERELY ARRIVED, IT IS THE NEXT COMMIT'S FIRST TASK: `flowPartition`/`flowPlan` (~3 KB) or the placement arithmetic (~2 KB) comes out BEFORE anything else is added here, including a comment** — 148 characters is four lines of prose, so the next round has no room to think in. The seam analysis below is re-read and UNCHANGED: this round touched the same file-scoped mutual recursion it describes, and both named candidates are untouched and still pure. **RE-RECORDED AGAIN 2026-09-01 (P1 review fixes): 197,476 -> 198,439. HEADROOM IS 1,561 CHARACTERS.** The review round added the diagnostics `table.freeze` on the cross-solve entry and its reasoning, trimmed once. **THE TRIGGER HAS ARRIVED ON SIZE: the next change here takes `flowPartition`/`flowPlan` (~3 KB) or the placement arithmetic (~2 KB) first** — both still pure, both still one-way, and either buys back more than this round spent. **RE-RECORDED 2026-09-01 (FacetBench Plan 3, perf campaign P1): 194,325 -> 197,476 (+3,151), measured after `stylua`.** The round gave `measure` a CROSS-SOLVE arm — the reuse arm `arrange` has had since incremental layout Stage 2 and `measure` never grew, which the profiling attribution (`FacetBench/docs/profiling/2026-09-01-attribution.md`) measured at 44 % of a step. Every character of it is inside `measure` itself plus one `Ctx` field and one line of `solve`: the per-node slate is adopted from a store the RENDERER owns and the reasoning lives in the new `src/layout/measure_reuse.luau` (one-way by construction — it reads `node.id`/`node.containerRelativeInside` and holds no solver local), which is where the growth went instead of here. **THE SEAM ANALYSIS BELOW IS RE-READ AND UNCHANGED**: the file is still one file-scoped mutual recursion (`measure`/`measureUncached`/`contentSize`/`arrange`/`arrangeStack` all read the per-solve `ctx` and call each other), this round touched exactly that recursion, and the two named next candidates — `flowPartition`/`flowPlan` (~3 KB) and the placement arithmetic (~2 KB) — are untouched and still pure. **2,524 clear of the 200,000 cap**, so the trigger below is UNCHANGED: the next round that opens this file for more than a comment takes one of those two first. **THE MEASURE-FACTS SEAM WAS TAKEN, 2026-08-21, and it is the row this file was waiting on.** `layout/measure_facts.luau` carries what the analysis below predicted, to the name: `Dim` and the dimension readers (`CONTENT`, `dim`, `SPACER_FILL`/`mainDimOf`, `sides`), `textTypography` + `recordTextFacts` + the text-fact record, and the memo's per-node classification (`MEMO_ARM_DEPTH`, `HEIGHT_COUPLED_KINDS`, the `PLAN_*` verdicts and `memoPlan`). **THE PREDICTION THAT MATTERED HELD: the argument list was already written.** `SHRINK_DEPS` and `GRID_DEPS` had been threading four of these into `./shrink` and `./grid` as explicit arguments since those splits, so this is the first seam in this document that cost NEITHER a record (`table.luau`'s `rowGesture`, `virtual_list`'s `hostedDelivery`) NOR an accessor (`table_header`'s `renderController`) — every input already arrived on the per-solve `ctx` or as a parameter. **Two corrections to the analysis it replaces**, both found by writing it: `definite` and `roundRect` DO NOT EXIST in this file (`roundPx` comes from `../num` and there is no `definite`), and `Dim` had to travel WITH `dim`/`mainDimOf` or the new module would have re-declared it — the solver re-exports it (`export type Dim = measureFacts.Dim`) so there is one schema rather than two, which is the same copy-goes-stale rule the seam exists to keep. **WHAT IS LEFT IS STILL ONE FILE-SCOPED MUTUAL RECURSION**: `measure` / `measureUncached` / `contentSize` / `arrange` / `arrangeStack` all read the per-solve `ctx` AND call each other, so none of them is one-way at any size, and `chosenCandidate` + the composition resolution (~12 KB) read `ctx.fitProbe`, `ctx.fitCuts`, `ctx.compositions` and `ctx.metrics` and call `measure` back — moving either creates a require cycle rather than a seam. A SECOND HELPER FOLLOWED IN THE SAME ROUND and is worth recording as the pattern rather than the exception: `axisAbsorbs` — the relayout-BOUNDARY predicate that mirrors `resolveAxis` case for case — moved to `measure_facts.luau` the moment it grew a SECOND reader (the containment finding's "can this parent grow?" gate). It reads a `Dim` and two limits, which is what that file is for, and a second copy of a predicate that mirrors the resolver is how a boundary rule ends up disagreeing with the resolver. The next candidates are real but small, and both are pure: `flowPartition`/`flowPlan` (~3 KB — `flowPartition` is ALREADY exported as `solver.flowPartition` because it reads no node and no ctx, which is the whole test), and the placement arithmetic `alignOffset`/`offsetPx`/`offsetFill`/`distributionOf`/`ANCHOR_FACTORS` (~2 KB, read by the arrange branches and by nothing else). | **RE-RECORDED 2026-08-21 (the solver split + containment round, second and final measurement): 197,810 -> 186,040 by the split, then -> 188,050 once the round SPENT part of the room it bought. That CLEARS THE 190,000 WARNING LINE by 1,950 and leaves 11,950 characters to the write cap, against the 2,190 this file started the round with.** The file entered this round with **2,190** — the number the previous trigger recorded as the whole remaining margin — and the extraction PRECEDED the round's own changes rather than accompanying them, which is what this row's previous wording demanded and what `renderer.luau`, `presenter.luau` and `table.luau` each learned the hard way one file over. **The evidence, because a split is a behaviour claim**: the full suite is 6,915 -> 6,928 passed with the verdict SET diffed line-for-line against a content-pinned baseline — the thirteen added rows are the seam spec's own and NOTHING ELSE MOVED, i.e. zero re-verdicts; and a differential oracle over 800 seeded trees (rects, `compact`, `textState`, all thirteen published text facts, the diagnostic set and the work counters, 10,838 comparison lines) is byte-identical between the pre-split and post-split trees. `tests/measure_facts_seam.spec.luau` mechanises what keeps it one-way: READ == DECLARED == HOSTED as set comparisons over the live sources (this seam has no parameter object — everything arrives on the solve's own `ctx` — so the third set is the solver's own `Ctx` block), the four-name shared write surface, EXPORTED == BOUND in both directions, the one consumer, the one-way require, and that the module holds no module-level mutable state. Eleven mutations proved to bite, and TWO OF THEM WERE FINDINGS RATHER THAN DRILLS: the "bound but unused" rule was decoration until the binding line (`local X = measureFacts.X`, which names X twice) was excluded from its own count, and once it could report it reported — `PLAN_KEYED`'s only reader left with `memoPlan`, so the export was dropped. This row is now ADVISORY rather than enforced (`check_source_size` reads a row only for a file inside the band) and is kept because the next reader deserves the analysis rather than a blank page. **TRIGGER: ARRIVED — RE-STATED 2026-08-21 (fix round 1, review HIGH-2), because the number this row first carried was ALREADY FIRED WHEN IT WAS WRITTEN.** It read "this file passes 188,000" beside its own recorded 188,050: fifty characters past, on the day it was written, which is a trigger that can never warn anyone — the same class as a check that cannot fail, and the identical failure the `presenter.luau` row above documents in capitals. **The consequence had already materialised before the review found it**: `099e28f` put **420** characters into this file (188,050 -> 188,470) with no extraction preceding them, and neither clause of the trigger could say so. This fix round's own comment sweep added **385** more (188,470 -> 188,435 after the twelve codes became prose and one line became two) under the same fired trigger — recorded here rather than quietly absorbed, and it is the LAST change that gets that pass, because it exists to pay down the debt the split itself created (see the extraction rule at the head of this file) and moves no geometry. **So the trigger is not a number any more, it is a state, and the state is ARRIVED: the next round that opens this file takes `flowPartition` + `flowPlan` FIRST** (~3 KB; `flowPartition` is already exported as `solver.flowPartition` precisely because it reads no node and no `ctx`, which is the whole one-way test, and `flowPlan` reads only `dim`/`mainDimOf`/`sides` and the partition — all four now in `./measure_facts`), with the placement arithmetic (`alignOffset`/`offsetPx`/`offsetFill`/`distributionOf`/`ANCHOR_FACTORS`, ~2 KB) behind it. Not "or when it passes N": PRECEDE the change, the way every other row in this document says it.  **WHAT THE ROOM WAS SPENT ON, and it is the point of buying it: the two containment guarantees the director ordered in the same breath as the split.** (1) The expand plate's chrome now crosses the composition seam (`resolveCtx.expandPlateInset` / `expandPlateStraddle`, ~1.1 KB here), which closes the band DIR5 left CONTESTED — a form whose natural width landed within the plate's own chrome of the gutter allowance mounted a panel wider than that allowance, reproduced headlessly at 380 against 358 and now impossible by construction, because the cap the form is measured against is derived from the SAME declaration `blueprint` builds the plate from (`layout/expand_plate.luau`). (2) The settle-time containment finding (~1.3 KB here, the rule itself in `layout/placement_audit.luau`): every other overflow diagnostic in this file compares a MEASURE against an offer, and none compared the RECT a child was finally given, which is why a box placed outside its parent by arithmetic was invisible. Its scope was MEASURED rather than chosen — the first draft policed every parent and produced 21,916 findings on a green library — and what shipped is the stack CROSS axis, silent on every shipped fixture and example. **The evidence for both: geometry, `compact`, `textState`, all thirteen text facts and the work counters are BYTE-IDENTICAL across the whole round on the 800-tree differential oracle, and the diagnostics channel is strictly ADDITIVE (0 lost, 60 gained, all 60 containment findings). The suite is 6,915 -> 6,937 with ZERO removals from the verdict set.** **STILL BOOKED, with the headroom measured**: the R18 hit-floor reserve (reserve `markW` PLUS the 44px floor's overhang rather than `markW`, task-m2fix concern 1) is a solver-side change to shipped geometry and was not attempted — 11,950 characters to the cap and 1,950 below the warning line is room for it, but it is a product change with its own fixture round rather than something to fold into a containment wave. | **RE-RECORDED 2026-08-28 (framework-gaps-phase2-followups, task THEME fix round): 181,383 -> 190,394, AND THE FILE IS BACK INSIDE THE BAND BY 394 CHARACTERS.** The re-record is owed and the split of it is stated rather than pooled: **7,393 of the 9,011 arrived BEFORE this round** and went unrecorded — `96b622d` ("a box declares its height in CONTENT terms", gap 3 / ADR-0049) is the largest of them, and `check_source_size` had been RED on this row for every commit since, which is a re-record nobody took rather than growth nobody noticed. **This round's own share is 1,618**: the `sizeRefused` field on `Node` and the diagnostic that spends it (ADR-0059 Decision 6) — a nine-line type entry and a twenty-line `arrange` block beside the `chromeYield` report it is the sibling of, both of them the report half of a repair the renderer has already made. **It was NOT trimmed to land under 190,000**, which this document's own header names as the behaviour the band exists to prevent; the honest move at 394 over is to be in the band with a current row. **THE SEAM ANALYSIS IS RE-READ AND UNCHANGED**: the mutual recursion below (`measure` / `measureUncached` / `contentSize` / `arrange` / `arrangeStack`, all reading the per-solve `ctx` and calling each other) is still what makes every large block here non-one-way, and this round touched only the DIAGNOSTIC tail of `arrange`, which reads `ctx.diagnostics` and `ctx.hiddenDepth` and calls nothing back. **THE NEXT CANDIDATE IS THEREFORE THAT TAIL**, and it is named here for the first time: `arrange`'s diagnostic block (~9 KB — the collapsed-content-box reports, the chromeYield pair, `sizeRefused`, the nested-scroll and placement findings) writes into `ctx.diagnostics` and reads a solved rect and the node, so it is one-way TODAY and needs no record. **TRIGGER: the next round that opens this file for anything larger than a comment takes the DIAGNOSTIC TAIL first**, or immediately if this file passes 193,000 (a deliberate gap above the recorded size, and a number this file has not already passed). **THE TWO SEAMS THIS ROW NAMED WERE TAKEN, 2026-08-22, AND THE STATE IS NO LONGER ARRIVED.** `layout/flow.luau` (9,248) carries `flowPartition` + `flowPlan` under the names `flow.partition` / `flow.plan`, and `layout/placement.luau` (4,754) carries `ANCHOR_FACTORS`/`offsetPx`/`offsetFill`/`alignOffset`/`distributionOf` — exactly the two candidates this row predicted, in the order it demanded, as their OWN behaviour-neutral commit `802eaa28d` PRECEDING every feature edit of the round that took them. **THE PREDICTION HELD TWICE.** `flowPartition` really did read no node and no `ctx` (it was already exported as `solver.flowPartition` for that reason), and `flowPlan`'s three inputs really were `measure`/`dim`/`sides` — which is `GRID_DEPS` exactly, so it takes the SAME record rather than a second forwarder (`local FLOW_DEPS: flowLib.Deps = GRID_DEPS`): the first seam in this document to cost neither a record, nor an accessor, nor even a new one. The placement block cost nothing at all — five functions on numbers, and `alignOffset` had already been crossing a module boundary BY VALUE since the grid split, which is the standing proof it read nothing of its host. Both are re-bound as plain locals in the solver because every one of them is on the per-child arrange path. **THE EVIDENCE:** the full suite is 7,215 -> 7,215 with ZERO verdict changes at `802eaa28d` measured through a content-pinned `mkpair` at that ref, against the 7,215 baseline at `0a612bf`. **THE NUMBERS:** 188,435 -> 182,611 by the split (measured at `802eaa28d`), then -> 188,638 once the round spent the room it bought on `textSize = "fit"` and the `ViewThatFits` ideal-size fix (ADR-0044) — 11,362 to the write cap and 1,362 below the 190,000 warning line, i.e. the round ended with MORE margin than it started with, which is what taking a seam first is for. **TRIGGER: RE-ARMED AS A STATE, and the next two candidates are named.** They are smaller and harder than the two just taken: `chosenCandidate` + the composition resolution (~12 KB) read `ctx.fitProbe`, `ctx.fitCuts`, `ctx.compositions` and `ctx.metrics` AND call `measure` back, so moving either creates a require cycle rather than a seam unless `measure` travels as a Dep the way `FLOW_DEPS` now proves is cheap — which is the one thing this round changed about the analysis: a forwarder record is no longer a cost, it is a thing the file already has. The mutual recursion (`measure`/`measureUncached`/`contentSize`/`arrange`/`arrangeStack`) is still one-way at no size. **The rule is unchanged: PRECEDE the change.** **RE-RECORDED 2026-08-22 AT 190,272 (fix round 1), and the number crossed the 190,000 WARNING LINE again — which is the row doing its job rather than a surprise.** The round's own feature spend was recorded above at 188,638; fix round 1 added the nested-scroller diagnostic this row's own booked ruling G5c had been waiting for (~1.6 KB with its rationale). **The rationale was then MOVED OUT rather than kept**: the measurement that justifies the rule's narrowness — the first draft fired on twelve shipped scenarios — lives in `tests/container_relative_frame.spec.luau` beside the four cases and three negative controls that enforce it, and this file keeps the rule and a pointer. That is the cheapest 720 characters available and it is also the right home: a measurement belongs with the check that reproduces it. **9,728 to the write cap.** The trigger below is unchanged and still a STATE, and the next round that opens this file should expect to pay for its own room the way this one did. **THE THIRD SEAM THIS ROW NAMED WAS TAKEN, 2026-08-22 (framework-gaps-phase2, task G8+G9): 190,272 -> 181,383, out of the band by 8,617 and 18,617 from the write cap.** `layout/composition_resolve.luau` (14,138 at the split) carries the composition resolution — the block this row named as the next candidate, under the one condition it stated: *"unless `measure` travels as a Dep the way `FLOW_DEPS` now proves is cheap"*. It does, and it is the ONLY thing that travelled: `compositionResolve.Deps` has one field, and the four `ctx` reads (`compositions`, `diagnostics`, `hiddenDepth`, `metrics`) arrive on the per-solve context that was already a parameter. **THE PREDICTION HELD AND THE COST WAS ZERO RECORDS.** No reassigned local crossed, so there was nothing to wrap the way `table.luau`'s `rowGesture` and `virtual_list`'s `hostedDelivery` had to be; the block reads and writes no mutable upvalue of the solver at all, which is the one-way test written down before the split. `chosenCandidate` — the other half of the pair this row named — did NOT travel and is still not one-way: it reads `ctx.fitProbe`/`ctx.fitCuts` and calls `measure` back, and taking it would have widened the Deps record for no room this round needed. **THE EVIDENCE:** `tests/composition_resolve_seam.spec.luau` is 13 cases mechanising READ == DECLARED == HOSTED, the one-name write surface, the one-field Deps record PASSED at both call sites, the one consumer (by the tree-wide enumeration, not a directory list), the one-way require and the no-module-mutable-state rule; **six mutations were proved to bite**, one per rule. The extraction PRECEDED every feature edit of the round, as this document's head demands. **TRIGGER: UNCHANGED AND STILL A STATE.** The remaining candidates are the ones this row already named and they are still not one-way at any size: `chosenCandidate` (~4 KB, reads two probe fields and calls `measure` back — takeable now that a Deps record is proven cheap, but only worth it for a round that needs the room), and the mutual recursion `measure`/`measureUncached`/`contentSize`/`arrange`/`arrangeStack`, which is one file at every size. **The rule is unchanged: PRECEDE the change.** **RE-RECORDED 2026-08-29 (framework-gaps-phase2-followups, task SHOWCASE3): 190,394 -> 194,325 (+3,931).** `arrange`'s own scroll branch (the diagnostic-tail candidate this row already named) gained the leading-edge chrome-bleed reservation `render/layout_node.luau`'s outermost-clipper mechanism could never floor-check pre-solve (`9eb095a`, reverted 2026-08-29 for collapsing a squeezed scroll container's content box to 0px): `chrome_slots.spendInset`, already required, spends `node.leadingBleed` against the REAL resolved rect, entirely inside the scroll arrange branch — it reads `rect`/`pt`/`pr`/`pb`/`pl`/`isX` and writes only `out[node.id].contentSize`/`.padding` and the local `cursor`, so it neither reads nor writes any of the five mutual-recursion functions or `ctx` beyond what this branch already threads. **THE SEAM ANALYSIS IS RE-READ AND UNCHANGED**: this IS the diagnostic tail the previous re-record named as the next candidate, and it is still one-way at no size — the growth is the floor-aware arithmetic itself plus a `Node` type field and its own `chrome_slots` import (already partially spent: the import and one comment reused prose the previous round left). **TRIGGER: unchanged and still a state** — the mutual recursion is one file at every size; the next round that opens this file for anything larger than a comment re-reads this analysis first. |
| `src/controls/virtual_list.luau` | 157,291 | **THE HOSTED BLOCK WAS TAKEN, 2026-08-22: 192,187 -> 145,913 by the split, then -> 157,291 once the round SPENT part of the room it bought. That is 42,709 characters below the write cap and 32,709 below the warning line, against the 805 this file had left to its own last-warning number when the round opened.** `controls/virtual_list_hosted.luau` (56,887) carries what this row predicted, to the name: `hostedItemExtentIn` through `hostedRestoreFocusOnDisengage` — the swipe dispatcher and its axis lock, the per-row engine cache and its retention policy, the shared tray overlay, the presentation slide, the commit collapse, the list-wide Delete/menu key context and the focus restore on disengage. **THE PREDICTION THAT MATTERED HELD, AND COST TWO CELLS MORE THAN IT SAID.** The six reassigned contribution handoffs plus `boundController` became one `hostedDelivery` record both halves hold — `table.luau`'s `rowGesture` technique, and the row was right that a reassigned local cannot be shared by reference at all. Writing it found two more of the same class the analysis had missed: `mountedScrollPath` (the focus-group derivation and `syncGeometry` both write it, and every overlay path is spelled from it) and `suppressActivatePath`, which is the ONE cell BOTH halves write and therefore the only one that could not have been an accessor instead — the alternative technique this same file already ships for `virtual_reorder`. **WHAT IS LEFT, honestly ranked, if this file ever re-enters the band.** `buildFocusGroups` (10,374) reads `hostedDelivery.scrollPath` and the hosted engines and is not one-way; `dump()` (4,074) closes over roughly twenty upvalues and is not worth a file on its own. Five earlier seams stand — `virtual_window`, `virtual_reorder`, `native_scroll_binding`, `card_rail`, `scroll_snap` — and `gap_metric.luau` joined them this round as a SHARED seam rather than a size one: the gutter vocabulary the list and the grid both spend, in one place so the two collections cannot drift into two refusals. | **CLEARED THE BAND 2026-08-22 (framework-gaps-phase2, lane R1b). The trigger this row carried — "treat it as ARRIVED: the next change of any size to this file is PRECEDED by the extraction, not accompanied by it" — WAS HONOURED, and it is the first row in this document whose trigger was obeyed rather than discovered afterwards.** The extraction landed as its own commit before one line of the round's feature work. **The evidence, because a split is a behaviour claim:** the full suite went 7,062 -> 7,077 with the verdict set diffed line for line against a content-pinned baseline pair — ZERO verdicts removed, and the fifteen added rows all the seam spec's own. `tests/virtual_list_hosted_seam.spec.luau` mechanises what keeps it one-way: READ == DECLARED == PASSED as set comparisons over the live sources, the shared WRITE surface pinned to one dep and one field inside it, a NO-STALE-TWIN rule over the nine historic names, the one-way require, and the single spelling of the two mounted-path ids. **TWO OF ITS RULES REPORTED BEFORE THE COMMIT LANDED, which is the only evidence a seam spec is not decoration:** the write scan read a table constructor's `id = ...,` as a dep reassignment (a statement assignment does not end in a comma; a constructor field does), and the surface pin caught that three of the twelve returned names are ABSENT on a list with no `rowActions` — now its own case, and the proof the construction site may be called unconditionally. **AND THE EXEMPTION ENDED WITH THE EXTRACTION**, which this document's head demands and which cost more than the head says: removing the file from `check_comment_codes`' extraction-locked tuple surfaced SIXTEEN private codes, not the fourteen orphans alone, because a file leaving that tuple brings its RESOLVABLE codes into a count that is a RATCHET — "make the orphans resolvable" would have moved the total from 25 to 41 and failed anyway. All sixteen became prose, which is what the `solver.luau` round did one file over. **A TRAP FOR THE NEXT SPLIT, and it cost a red run: `check_comment_codes` reads `git ls-files`, so a brand-new module is INVISIBLE to it until the commit that adds it lands.** It passed on the split commit for exactly that reason and went red on the next run, with four codes that had ridden out of a file whose exemption had just ended. Run it AGAIN after the commit, not only on it. **WHAT THE ROOM WAS SPENT ON (11,378 of the 46,274 it bought):** `viewportExtent = "auto"`, `crossExtent = "hug"|"measured"` and the theme-token gutters — ADR-0043, the purity audit's §4 and §5, whose whole point is that a consumer stops re-deriving a number this control already holds. |
| `src/client/screen_target.luau` | 172,740 | **THE setProp SEAM WAS TAKEN, 2026-08-28 (task EXTRACT), and it is the row this file was waiting on across four deferred rounds.** `src/client/screen_props.luau` (33,675 at close, before the I5 fix below) carries exactly what this cell predicted: the whole per-class property-write chain, behind a `Ctx` parameter object of 27 fields at the extraction commit, 28 once the I5 fix below added `refitIconArt`. **THE PREDICTION HELD AND THE COST WAS ZERO RECORDS** — not a record (`table.luau`'s `rowGesture`), not an accessor (`table_header`'s `renderController`), nothing: every one of the 28 names `setProp` closes over at HEAD (27 at the extraction commit; `refitIconArt` is the 28th, added by the I5 fix that landed the same round) is either a construction-time VALUE (`style`, `nativeMode`, the palette, two text-input facts, none ever reassigned — confirmed by grep across the whole file) or a FUNCTION bound once from a sibling subsystem (`chrome`, `paint`, `presentation`) or a host helper (`syncTags`, `assertBespokePaint`, `claimPaint`, `restorePaint`, `mirrorToHitExpander`, `writeFieldText`) — so `setProp` reassigns not one of them and writes only through its own `handle`/`instance` parameters. `tests/screen_props_seam.spec.luau` mechanises the property exactly as `table_rows_seam.spec.luau` does for its family (READ == DECLARED == PASSED as three set comparisons over the live sources, the ONE construction site, the one-way require) with one new rule this seam's shape earned: the shared WRITE surface is asserted EMPTY rather than pinned to a name, because there is nothing to pin. **A CORRECTION TO THIS ROW'S OWN PRIOR CLAIM, because a re-read is what a mechanised scan is for**: earlier entries here said the four large blocks "each read `style`, `nativeMode`, `instancesByPath`, `assertBespokePaint` and the live handle map" — measured, `setProp` never touches `instancesByPath`/`handlesByPath`/the handle map at all; that clause was true of `create`/`syncTags`/`setFocusVisual` and was generalised to `setProp` without being checked against it. `ensureInstance(handle)` — the one line `setProp` shared with those three — stays in `screen_target.luau` itself: the host still owns the one materialisation authority, ahead of every prop write, exactly as `setHitRect` already reads it. **THE EVIDENCE, because a split is a behaviour claim.** Measured through a content-pinned `mkpair` pair at this round's close (`PIN_FACET c83d858…`, `PIN_RR ee80241…`), run sequentially and solo: **Facet 7,634 passed, 0 failed**; **Rascal Rally 3,538 passed, 0 failed**. Eleven of the new cases are this round's own — nine in `tests/screen_props_seam.spec.luau` (the READ==DECLARED==PASSED mechanised seam, across two `describe` blocks), one in `tests/chrome_inset_yield.spec.luau` (the I5 wiring pin) and one in `tests/paint_extensions.spec.luau` (the circle-corner pin) — and RR's own suite is untouched by this round (internal-client-file lockstep grep is a clean negative: `screen_target`/`screen_props`/`screen_paint`/`screen_chrome` are reached by RR nowhere except one already-stale line-number citation in a comment, `FacetSponsor/TableMetrics.luau:1295`, predating this round and not fixed here as out of scope for a behaviour-neutral extraction). **A round-trip caught a real defect of its own**: the first pinned run reddened 3 cases (`tests/extension_checker.spec.luau`'s registration/four-input/paradigm trio), all three the SAME root cause — `tests/screen_props_seam.spec.luau` was written but never added to `tests/run.luau`, which `check_registration`'s live-repository rule exists to catch; fixed by registering it, re-pinned, re-ran clean. **A SECOND round-trip caught a syntax defect in the same new file**: a literal `{ ... }` inside a backtick-interpolated assert message was parsed as a second interpolation slot evaluating the vararg operator outside a vararg function, crashing `tests/run.luau`'s `require` at load — caught by the pinned pair rather than the standalone regex checks the file's own logic was verified against, which never re-loaded the file as a whole after the edit that introduced it. Both are recorded here rather than smoothed into a single clean number, because a ledger that only shows the last measurement teaches nothing about what the next split should watch for., closing the debt this row's own text named as "owed across three deferred fixes": `screen_props.luau`'s `textSize` branch now calls `refitIconArt(handle)`, and `tests/chrome_inset_yield.spec.luau` pins it (red-first: confirmed absent before the call was added, present after). **AND ONE MORE GAP CLOSED, now that this file is cheap to open**: task PAINT's disclosed boundary (a bare `shape = "circle"` node with no surface at all got no real fallback `UICorner`, so a direct fill — `tint`, the one other fallback writer that reaches a bare `GuiObject` — painted a square) is fixed in `screen_paint.luau`'s `applyTint`, gated on `declaredSurface == nil` so a node `applySurface` already owns is untouched; `tests/paint_extensions.spec.luau` pins it, red-first. **THE TAX PAID, and it is smaller than any prior seam's**: `tests/lib/adapter_source.luau` gained a `PARTS` entry (fixing `theme_layer_application.spec` and `button_shape.spec`'s `adapter_source.live()` reads for free), `tools/lune/check_prop_parity.luau`'s `SRC_ADAPTER` became `SRC_ADAPTER_PARTS` (the whole `prop == "<name>"` view moved out from under it), three specs that read `screen_target.luau` ALONE for a branch that moved switched to `screen_props.luau` (`control_feedback.spec`'s two enabled-seam cases, `chrome_padding_refit.spec`'s `propBranch` helper, `render_target_contract.spec`'s role-branch pin), and one `gate_manifest` row's `canSetTextInputType` grep moved with it. **WHAT IS LEFT.** `create` (~19.9 KB), `syncTags` (~20.6 KB) and `setFocusVisual` (~15.8 KB) are still inside the one `screen_target.new` closure and still fail the mutable-upvalue test by construction: `create` WRITES the handle map (registering every new node) and `syncTags`/`setFocusVisual` each read `style`, `nativeMode`, `instancesByPath`/`handlesByPath` and the chrome/paint subsystems the same way `setProp` did — the `setProp` technique (a `Ctx` parameter object) is proven cheap and would apply to either, but neither is owed by size: **27,260 characters clear of the 200,000 write cap and 17,260 clear of the 190,000 warning line**, the most headroom this file has had since the cap was measured. | **TRIGGER: this file passes 190,000** (a deliberate number the file has not already passed, and roughly the size of the extraction just taken — large enough that an ordinary fix round does not fire it, near enough that the next seam is thought about while it is still cheap), or the next mission that changes `create`'s handle-registration path or `syncTags`'s tag-diff loop takes one of them first, now that the parameter-object technique is proven. **EARLIER, SUPERSEDED: CONN ROUND 2, 2026-08-28: 193,154 -> 196,770 (+3,616), past the 192,000 trigger and past this row's own "the next round opening this file for anything larger than a comment takes the named extraction FIRST" rule (task THEME's fix round, same day, deferred a ONE-CALL fix under that exact rule).** Landed anyway, as a bug-fix round, not a refactor mission — the same exception FIX-2 itself used three days earlier at this same trigger — because the work was controller-assigned, small, and multiplies the debt of deferring it a second and third time on top of THEME's own deferred `refitIconArt` call: (1) `adapter.adopt` resets `handle.pointer` to nil (one line, closing review-conn-verdicts.md's Finding 4 residual: a life declaring no pointer handlers must not inherit a prior occupant's, root-causing a permanent hit-expander misroute `setHitRect` could not otherwise fix); (2) `setHitRect`'s `hitExpanderPointerConn` wiring moved outside its `if expander == nil` gate onto `handle_conn.reconnectOnChange`, matching `screen_pointer.luau`'s sibling fix; (3) eight `*Conn` fields (`hoverStateConn`, `discloseEnterConn`/`-LeaveConn`/`-TouchConn`, `textChangedConn`, `focusedConn`, `focusLostConn`, `oskReturnConn`) swept onto `handle_conn.reconnect`/`reconnectOnChange`, closing task CONN round 1's own disclosed deferral; (4) `setFocusVisual`'s ten-foot lift cap now also bounds against the nearest scroll/clip host's own window (`AbsoluteWindowSize` on a `ScrollingFrame`), closing a live MISC-found overflow (a focused row's 1.05x pop crossing a ScrollingFrame's scrollbar reserve). Comments were trimmed twice on the way in, the same move every prior round at this trigger made. **THE NAMED EXTRACTION IS NOW OWED ACROSS THREE DEFERRED FIXES** (THEME's `refitIconArt`, both of this round's) and should be the next thing a round with the budget for a genuine refactor takes, not a fourth bug-fix round leaning on the same exception. **3,230 clear of the 200,000 write cap.** **FIX-2 ROUND, 2026-08-23: 190,181 -> 193,147.** The float focus ring's geometry (`setFocusVisual`) got two device-bug fixes: the ten-foot lift scale is now read BEFORE the float is sized (it used to size the float from the pre-lift rect, leaving a gap on the right/bottom edge under `FocusScale`), and a native-mode phantom `::UICorner` (invisible to `FindFirstChildOfClass`) now falls back to the same role -> radius mapping `applySurface`'s bespoke `corner()` uses, so a native-styled control's float ring is rounded again. 2,966 over the 2,000 allowance and past the recorded 192,000 trigger — no extraction taken this round (a live device-repro round, not a refactor mission); the seam analysis below is unchanged and still names `setProp`'s per-class chain as the only large one-way candidate. **SIX seams TAKEN** — `screen_chrome`, `screen_scroll_indicators`, `screen_paint`, `screen_presentation`, `screen_pointer` and now `screen_vocabulary` — and the sixth is the one this row had been describing as OWED. **THE VOCABULARY SEAM WAS TAKEN, 2026-08-22 (SCREEN-X), and it is what this row was waiting on.** `src/client/screen_vocabulary.luau` (7,694 characters) holds exactly what the analysis below named: the authored-word -> engine-value maps `CLASS_TO_INSTANCE`, `TEXT_ALIGN_MAP` and `SCALE_MODE_ENGINE`, the `TINT_REPAINTING_PROPS` repaint set, the pure `toColor3` and `fontFaceFor`, the two `FacetStage*` instance names and the three stage/foreign helpers (`writeStageProp`, `markStageDisposed`, `markForeignDisposed`). **IT COST NEITHER A RECORD NOR AN ACCESSOR** — the cheapest seam this document has taken. The blocks are declared before `screen_target.new` exists, so they read no upvalue of it; every line moved VERBATIM with the comment that explains it, and the host binds the same eleven local names off the sibling, so no call site changed a character. What it DID cost is the tax this row predicted and three pins nobody had counted: a part in `tests/lib/adapter_source.luau` (whose `live()` the source-scanning specs read), `tools/lune/check_primitives`' `ADAPTER_SOURCE` and its spec (they read `CLASS_TO_INSTANCE` as text, by path), one `gate_manifest` grep pinned on `ScrollView = "ScrollingFrame"` by path, and two specs that read `screen_target.luau` ALONE for a fact that had moved — `tests/stage.spec` now reads `adapter_source.live()` (strictly stronger: the no-bespoke-seam-write pin was half-blind to every sibling) and `tests/control_feedback.spec` reads the vocabulary for `TextField = "TextBox"`. **WHAT IS LEFT.** Everything large is INSIDE the one `screen_target.new` closure and fails the mutable-upvalue test by construction: `setProp` (25.8 KB), `syncTags` (20.6 KB), `create` (18.6 KB) and `setFocusVisual` (11.6 KB) each read `style`, `nativeMode`, `instancesByPath`, `assertBespokePaint` and the live handle map. | **ONE FIX IS BOOKED AGAINST THIS ROW AND IS WAITING ON THE EXTRACTION (task THEME fix round, 2026-08-28; review finding I5).** ADR-0059 Decision 1b makes the chrome text lift's floor the host's live `TextSize`, and `screen_chrome` re-reads it only from the chrome reconcile and from `refitIconArt`, which the adapter drives from `applyRect`. `render/rect_pass.luau` skips `setRect` for an unchanged rect, so a node whose TEXT SIZE moved while its RECT did not — a `fixed`-px themed control at a new `preferredTextSize` step — keeps the previous frame's lift box while the measure seam has already changed its answer. **The fix is ONE call**: `refitIconArt(handle)` in this file's `textSize` branch of `setProp`, beside the `syncTags` it already makes there. It is not taken here because this row's rule is that the next round opening this file for anything larger than a comment takes the named extraction FIRST, and a fix round is not a refactor mission — the same trade this row already records costing a one-character tag fix a full round. The drift is written down at both seams (`screen_chrome.luau`'s `lineBox` block, ADR-0059's amendment log) and the Studio capture it needs is on the task's owed list. **BACK IN THE BAND ON THE FIX ROUND, DELIBERATELY: 189,985 -> 190,181, 181 characters over the 190,000 line.** The review asked for a headless behavioural regression for tag removal, and the honest way to get one was to move the whole ruling OUT of the adapter into `src/render/tag_sync.luau` (pure, no requires, loadable by a spec — which `screen_vocabulary` is not, because it touches `Enum` at module scope). What this file kept is one require and a five-line call, and the require's three-line comment is what put it back inside the band. IT WAS NOT TRIMMED TO GET UNDER THE LINE, and that is the entry: the band is not a failure state, it is a requirement to have a current seam analysis and a live trigger, and this row has both — freshly re-read twice in one round. Trimming an explanation to move a byte counter is the behaviour this ledger's own header warns about. **The trigger stays 192,000**, 1,819 characters away. **Earlier this round: the sticky-tag fix first rode the room the extraction bought and gave three characters back, 189,988 -> 189,985**, when the predicate still lived in `screen_vocabulary`. The dead tag-removal test became `screen_vocabulary.ownsTag(tag)` — a call is shorter than the `string.sub` it replaced, and the prefix, its length and the paragraph explaining why they may never disagree again all live in the sibling. That is the extraction earning its keep on the very next commit. **CLEARED THE BAND 2026-08-22 (SCREEN-X): 193,795 -> 189,988**, all of it this round's, and the 12 characters of daylight under 190,000 are the honest number rather than a comfortable one — this file is one comment away from being enforced again, so the row STAYS and the next reader should expect to be inside the band the moment they add prose. **The next trigger is 192,000** (a deliberate gap above the recorded size: two thirds of the way back to the band, which is far enough that an ordinary fix round does not fire it and near enough that the next seam is thought about while it is still cheap). **THE NEXT EXTRACTION IS NAMED and its precondition has not changed**: `setProp`'s per-class property chain is the only large candidate and it is NOT one-way as written — it becomes so when the paint/style context it reads is a parameter object rather than four upvalues, which is a refactor of the adapter rather than a move, and must PRECEDE rather than accompany the change that needs the room. **WHY THIS EXTRACTION HAPPENED WHEN IT DID.** The row below said the next change of any size to this file had to be preceded by it; a framework-wide shipping defect then landed on line 566 (`string.sub(tag, 1, 5)` against the six-character `"facet-"`, ADR-0038's rename, every `facet-*` tag permanently sticky) and its one-character fix was BLOCKED behind this row for a full round. That is the trigger doing its job and also the trigger's whole cost: a defect on the framework's own demo place waited for a refactor. **Earlier entries, newest first.** **Native-default fix round 2: 193,714 -> 193,795, and 81 of that was somebody else's.** That round's own edit went the other way: a re-review reverted the product default in one line by substituting a value into `native_style.paintPlan`'s argument, so the seam took the target's whole `opts` table and read the paint opt itself — 30 characters SHORTER, and it made the pin a count of ZERO rather than of one. The file still rose because a paint-family commit landed between the two rounds. It left this file 205 characters from the 194,000 trigger, which is what made the vocabulary module above OWED rather than optional. **Native-default fix round 1: 193,992 -> 193,714.** The round below left this file 8 characters from the trigger; a fresh-context review then asked for the paint DECISION to be extracted so the branch could have a headless witness, and the extraction gave 278 characters back in the same edit — `native_style.paintPlan` took the default, the capability probe, the escape hatch and the transitions seed, leaving one call and a `plan.native` test where there had been a resolve, a two-clause gate and a three-line transitions expression. 286 from the trigger at the time, and the sentence that entry earned its credibility with stands: the seam that answered that review is NOT the seam this row is about. **Paint-family lockstep round, ADR-0040 row B-17: 193,314 -> 193,992**, of which 678 were that round's — leaving EIGHT characters before the 194,000 trigger, which is what made the vocabulary module owed by the next round to open the file rather than by a future one. That round's own edit was three lines of code and its comments were cut twice to land under the trigger, which is the shape a trigger is meant to produce: the derivation lives in `themes/snapshot.paintForDisplay` and this file only spends it. **Native-paint default flip, ADR-0040 row B-15: 193,077 -> 193,314**, of which 108 were that round's. Its edit went the right way for this file — the default-resolution branch left the closure for a seam rather than a line, which is why a +237 net drift was the whole cost of a product-behaviour change — and the seam analysis beside this row was re-read then and STANDS unchanged. 686 from the trigger at the time. **R5 fix round 1: 190,697 -> 193,077**, the REVEAL wave plus `adapter.dispose`, the target teardown seam `client.host` needs; 923 from the trigger at the time. **THE EXTRACTION ITSELF.** Take the **vocabulary module** (`screen_vocabulary.luau`, ~7 KB, one-way today and costing only an `adapter_source` part) on the next mission that touches any of those tables, or immediately if this file passes 194,000. `setProp`'s per-class property chain is the only large candidate and it is NOT one-way as written — it becomes so when the paint/style context it reads is a parameter object rather than four upvalues, which is a refactor of the adapter rather than a move, and must PRECEDE rather than accompany the change that needs the room. |

## Modules approaching the band

Not required to have a row, listed so the next reader knows where the pressure
is going next. Re-measure with `python3 tools/check_source_size.py`.

