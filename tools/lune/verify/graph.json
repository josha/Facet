{
 "generatedAt": "2026-08-31T01:29:44Z",
 "generatedFrom": {
  "manifest": "tools/lune/gate_manifest.luau",
  "phases": "phases.json",
  "suiteResults": "suite_real.json"
 },
 "maintainedAt": "2026-08-31T02:53:57Z",
 "phases": [
  {
   "artifactDir": "artifacts/phase-0-foundation",
   "gateArtifact": "artifacts/phase-0-foundation/gate.json",
   "id": "phase-0-foundation",
   "next": "phase-1-minimal-screen"
  },
  {
   "artifactDir": "artifacts/phase-1-minimal-screen",
   "gateArtifact": "artifacts/phase-1-minimal-screen/gate.json",
   "id": "phase-1-minimal-screen",
   "next": "phase-2-settings-parity"
  },
  {
   "artifactDir": "artifacts/phase-2-settings-parity",
   "gateArtifact": "artifacts/phase-2-settings-parity/gate.json",
   "id": "phase-2-settings-parity",
   "next": "phase-3-pilot"
  },
  {
   "artifactDir": "artifacts/phase-3-pilot",
   "gateArtifact": "artifacts/phase-3-pilot/gate.json",
   "id": "phase-3-pilot",
   "next": "part-2-director"
  },
  {
   "artifactDir": "artifacts/part-2-director",
   "gateArtifact": "artifacts/part-2-director/gate.json",
   "id": "part-2-director",
   "next": "phase-4-hardening"
  },
  {
   "artifactDir": "artifacts/phase-4-hardening",
   "gateArtifact": "artifacts/phase-4-hardening/gate.json",
   "id": "phase-4-hardening",
   "next": "input-adaptation-audit"
  },
  {
   "artifactDir": "artifacts/input-adaptation-audit",
   "gateArtifact": "artifacts/input-adaptation-audit/gate.json",
   "id": "input-adaptation-audit",
   "next": "expansion-textinput"
  },
  {
   "artifactDir": "artifacts/expansion-textinput",
   "gateArtifact": "artifacts/expansion-textinput/gate.json",
   "id": "expansion-textinput",
   "next": "input-paradigms"
  },
  {
   "artifactDir": "artifacts/input-paradigms",
   "gateArtifact": "artifacts/input-paradigms/gate.json",
   "id": "input-paradigms",
   "next": "native-substrate"
  },
  {
   "artifactDir": "artifacts/native-substrate",
   "gateArtifact": "artifacts/native-substrate/gate.json",
   "id": "native-substrate",
   "next": "native-stylesheets"
  },
  {
   "artifactDir": "artifacts/native-stylesheets",
   "gateArtifact": "artifacts/native-stylesheets/gate.json",
   "id": "native-stylesheets",
   "next": "authoring-adaptive-ui"
  },
  {
   "artifactDir": "artifacts/authoring-adaptive-ui",
   "gateArtifact": "artifacts/authoring-adaptive-ui/gate.json",
   "id": "authoring-adaptive-ui",
   "next": "theme-packages-and-skinning"
  },
  {
   "artifactDir": "artifacts/theme-packages-and-skinning",
   "gateArtifact": "artifacts/theme-packages-and-skinning/gate.json",
   "id": "theme-packages-and-skinning",
   "next": "rich-skinning-v2"
  },
  {
   "artifactDir": "artifacts/rich-skinning-v2",
   "gateArtifact": "artifacts/rich-skinning-v2/gate.json",
   "id": "rich-skinning-v2",
   "next": "cross-platform-proof"
  },
  {
   "artifactDir": "artifacts/cross-platform-proof",
   "gateArtifact": "artifacts/cross-platform-proof/gate.json",
   "id": "cross-platform-proof",
   "next": "sponsor-framework-gaps"
  },
  {
   "artifactDir": "artifacts/sponsor-framework-gaps",
   "gateArtifact": "artifacts/sponsor-framework-gaps/gate.json",
   "id": "sponsor-framework-gaps",
   "next": "code-simplicity-cleanup"
  },
  {
   "artifactDir": "artifacts/code-simplicity-cleanup",
   "gateArtifact": "artifacts/code-simplicity-cleanup/gate.json",
   "id": "code-simplicity-cleanup",
   "next": "api-architecture-consistency"
  },
  {
   "artifactDir": "artifacts/api-architecture-consistency",
   "gateArtifact": "artifacts/api-architecture-consistency/gate.json",
   "id": "api-architecture-consistency",
   "next": "desktop-keyboard-navigation"
  },
  {
   "artifactDir": "artifacts/desktop-keyboard-navigation",
   "gateArtifact": "artifacts/desktop-keyboard-navigation/gate.json",
   "id": "desktop-keyboard-navigation",
   "next": "traversal-document-order"
  },
  {
   "artifactDir": "artifacts/traversal-document-order",
   "gateArtifact": "artifacts/traversal-document-order/gate.json",
   "id": "traversal-document-order",
   "next": "large-text-accessibility"
  },
  {
   "artifactDir": "artifacts/large-text-accessibility",
   "gateArtifact": "artifacts/large-text-accessibility/gate.json",
   "id": "large-text-accessibility",
   "next": "performance-stress-places"
  },
  {
   "artifactDir": "artifacts/performance-stress-places",
   "gateArtifact": "artifacts/performance-stress-places/gate.json",
   "id": "performance-stress-places",
   "next": "example-quality-pass"
  },
  {
   "artifactDir": "artifacts/example-quality-pass",
   "gateArtifact": "artifacts/example-quality-pass/gate.json",
   "id": "example-quality-pass",
   "next": "reference-app-validation"
  },
  {
   "artifactDir": "artifacts/reference-app-validation",
   "gateArtifact": "artifacts/reference-app-validation/gate.json",
   "id": "reference-app-validation",
   "next": "row-actions"
  },
  {
   "artifactDir": "artifacts/row-actions",
   "gateArtifact": "artifacts/row-actions/gate.json",
   "id": "row-actions",
   "next": "parity-round-2"
  },
  {
   "artifactDir": "artifacts/parity-round-2",
   "gateArtifact": "artifacts/parity-round-2/gate.json",
   "id": "parity-round-2",
   "next": "declarative-3d-architecture"
  },
  {
   "artifactDir": "artifacts/declarative-3d-architecture",
   "gateArtifact": "artifacts/declarative-3d-architecture/gate.json",
   "id": "declarative-3d-architecture",
   "next": "parity-round-3"
  },
  {
   "artifactDir": "artifacts/parity-round-3",
   "gateArtifact": "artifacts/parity-round-3/gate.json",
   "id": "parity-round-3",
   "next": "parity-round-4"
  },
  {
   "artifactDir": "artifacts/parity-round-4",
   "gateArtifact": "artifacts/parity-round-4/gate.json",
   "id": "parity-round-4",
   "next": "navigation-and-menus"
  },
  {
   "artifactDir": "artifacts/navigation-and-menus",
   "gateArtifact": "artifacts/navigation-and-menus/gate.json",
   "id": "navigation-and-menus",
   "next": "release-candidate-review"
  },
  {
   "artifactDir": "artifacts/release-candidate-review",
   "gateArtifact": "artifacts/release-candidate-review/gate.json",
   "id": "release-candidate-review",
   "next": "example-games-and-standalones"
  },
  {
   "artifactDir": "artifacts/example-games-and-standalones",
   "gateArtifact": "artifacts/example-games-and-standalones/gate.json",
   "id": "example-games-and-standalones",
   "next": "distribution-readiness"
  },
  {
   "artifactDir": "artifacts/distribution-readiness",
   "gateArtifact": "artifacts/distribution-readiness/gate.json",
   "id": "distribution-readiness",
   "next": null
  }
 ],
 "producers": [
  {
   "command": "lune run tools/lune/check_public_surface",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_public_surface",
   "inputs": [
    "tools/lune/_probe_public_surface.luau",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "tools/bench.sh",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "perf",
   "fixtures": [],
   "id": "bench",
   "inputs": [
    "tools/bench.sh",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "measure",
   "note": "",
   "optional": false,
   "serialize": true,
   "tiers": {
    "fast": false,
    "full": false,
    "release": true
   },
   "timeoutS": 900
  },
  {
   "command": "tools/build_model.sh",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "build_model",
   "inputs": [
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "build",
   "note": "required by the release graph",
   "optional": false,
   "serialize": true,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "tools/build_places.sh",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "build_places",
   "inputs": [
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "build",
   "note": "required by the release graph",
   "optional": false,
   "serialize": true,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 1800
  },
  {
   "command": "tools/build_reference_places.sh",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "build_reference_places",
   "inputs": [
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "build",
   "note": "required by the release graph",
   "optional": false,
   "serialize": true,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 1800
  },
  {
   "command": "tools/build_themes.sh",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "build_themes",
   "inputs": [
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "build",
   "note": "required by the release graph",
   "optional": false,
   "serialize": true,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/build_word_lists.py --check",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "build_word_lists-check",
   "inputs": [
    "tools/build_word_lists.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/build_word_lists.py --selftest",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "build_word_lists-selftest",
   "inputs": [
    "tools/build_word_lists.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "lune run tools/lune/check_boundary",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_boundary",
   "inputs": [
    "tools/lune/check_boundary.luau",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_brand_drift.py",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_brand_drift",
   "inputs": [
    "tools/check_brand_drift.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_brand_drift.py --selftest",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_brand_drift-selftest",
   "inputs": [
    "tools/check_brand_drift.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_brand_drift.py --skip-builds",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_brand_drift-skip-builds",
   "inputs": [
    "tools/check_brand_drift.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_call_shape_drift.py",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_call_shape_drift",
   "inputs": [
    "tools/check_call_shape_drift.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_call_shape_drift.py --selftest",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_call_shape_drift-selftest",
   "inputs": [
    "tools/check_call_shape_drift.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_comment_codes.py",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_comment_codes",
   "inputs": [
    "tools/check_comment_codes.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_comment_codes.py --selftest",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_comment_codes-selftest",
   "inputs": [
    "tools/check_comment_codes.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_device_captures.py",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "device",
   "fixtures": [
    "artifacts/cross-platform-proof/device/**",
    "artifacts/phase-4/perf.json"
   ],
   "id": "check_device_captures",
   "inputs": [
    "tools/check_device_captures.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_device_sweep.py --selftest",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "device",
   "fixtures": [
    "artifacts/cross-platform-proof/device/**"
   ],
   "id": "check_device_sweep-selftest",
   "inputs": [
    "tools/check_device_sweep.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_doc_style.py",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_doc_style",
   "inputs": [
    "tools/check_doc_style.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_doc_style.py --selftest",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_doc_style-selftest",
   "inputs": [
    "tools/check_doc_style.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "lune run tools/lune/check_docs_cli",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_docs_cli",
   "inputs": [
    "tools/lune/check_docs_cli.luau",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "lune run tools/lune/check_elision_census",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_elision_census",
   "inputs": [
    "tools/lune/check_elision_census.luau",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_eq6_evidence.py",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "studio",
   "fixtures": [
    "artifacts/example-quality-pass/studio/large-text.json"
   ],
   "id": "check_eq6_evidence",
   "inputs": [
    "tools/check_eq6_evidence.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "lune run tools/lune/check_example_drift_cli",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_example_drift_cli",
   "inputs": [
    "tools/lune/check_example_drift_cli.luau",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "lune run tools/lune/check_flat_baseline",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/theme-packages-and-skinning/final-neutral-dump.json",
    "artifacts/rich-skinning-v2/rows/neutral-render-dump.json"
   ],
   "id": "check_flat_baseline",
   "inputs": [
    "tools/lune/check_flat_baseline.luau",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_input_authority.py",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_input_authority",
   "inputs": [
    "tools/check_input_authority.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_input_authority.py --selftest",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_input_authority-selftest",
   "inputs": [
    "tools/check_input_authority.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_library_purity.py",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_library_purity",
   "inputs": [
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "required by the release graph",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "lune run tools/lune/check_links_cli",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_links_cli",
   "inputs": [
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "required by the release graph",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "lune run tools/lune/check_links_cli --selftest",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_links_cli-selftest",
   "inputs": [
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "required by the release graph",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "lune run tools/lune/check_maintainer_map_cli",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_maintainer_map_cli",
   "inputs": [
    "tools/lune/check_maintainer_map_cli.luau",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "lune run tools/lune/check_maintainer_map_cli -- --selftest",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_maintainer_map_cli-selftest",
   "inputs": [
    "tools/lune/check_maintainer_map_cli.luau",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_manifest_integrity.py",
   "declaredEvidence": false,
   "dependsOn": [
    "suite"
   ],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_manifest_integrity",
   "inputs": [
    "tools/check_manifest_integrity.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": true,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_matrix_rows.py",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "studio",
   "fixtures": [
    "artifacts/authoring-adaptive-ui/matrix/five-view-matrix.json"
   ],
   "id": "check_matrix_rows",
   "inputs": [
    "tools/check_matrix_rows.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_no_fusion.py",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_no_fusion",
   "inputs": [
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "required by the release graph",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_no_fusion.py --selftest",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_no_fusion-selftest",
   "inputs": [
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "required by the release graph",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_no_screen_key_bindings.py",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_no_screen_key_bindings",
   "inputs": [
    "tools/check_no_screen_key_bindings.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_no_screen_key_bindings.py --selftest",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_no_screen_key_bindings-selftest",
   "inputs": [
    "tools/check_no_screen_key_bindings.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_perf_budgets.py",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "device",
   "fixtures": [
    "artifacts/phase-4/perf.json",
    "artifacts/cross-platform-proof/device/**"
   ],
   "id": "check_perf_budgets",
   "inputs": [
    "tools/check_perf_budgets.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_perf_captures.py",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "device",
   "fixtures": [
    "artifacts/performance-stress-places/studio/**",
    "artifacts/cross-platform-proof/device/**",
    "artifacts/performance-stress-places/acceptance.md"
   ],
   "id": "check_perf_captures",
   "inputs": [
    "tools/check_perf_captures.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_perf_gate_evidence.py budgets",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "studio",
   "fixtures": [
    "artifacts/performance-stress-places/**"
   ],
   "id": "check_perf_gate_evidence-budgets",
   "inputs": [
    "tools/check_perf_gate_evidence.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_perf_gate_evidence.py device-matrix",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "studio",
   "fixtures": [
    "artifacts/performance-stress-places/**"
   ],
   "id": "check_perf_gate_evidence-device-matrix",
   "inputs": [
    "tools/check_perf_gate_evidence.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_perf_gate_evidence.py falsifiable",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "studio",
   "fixtures": [
    "artifacts/performance-stress-places/**"
   ],
   "id": "check_perf_gate_evidence-falsifiable",
   "inputs": [
    "tools/check_perf_gate_evidence.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_perf_gate_evidence.py headless-linkage",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "studio",
   "fixtures": [
    "artifacts/performance-stress-places/**"
   ],
   "id": "check_perf_gate_evidence-headless-linkage",
   "inputs": [
    "tools/check_perf_gate_evidence.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_perf_gate_evidence.py large-text",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "studio",
   "fixtures": [
    "artifacts/performance-stress-places/**"
   ],
   "id": "check_perf_gate_evidence-large-text",
   "inputs": [
    "tools/check_perf_gate_evidence.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_perf_gate_evidence.py native-reference",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "studio",
   "fixtures": [
    "artifacts/performance-stress-places/**"
   ],
   "id": "check_perf_gate_evidence-native-reference",
   "inputs": [
    "tools/check_perf_gate_evidence.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_perf_gate_evidence.py perf-gate",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "studio",
   "fixtures": [
    "artifacts/performance-stress-places/**"
   ],
   "id": "check_perf_gate_evidence-perf-gate",
   "inputs": [
    "tools/check_perf_gate_evidence.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_perf_gate_evidence.py prior-gates",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "studio",
   "fixtures": [
    "artifacts/performance-stress-places/**"
   ],
   "id": "check_perf_gate_evidence-prior-gates",
   "inputs": [
    "tools/check_perf_gate_evidence.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_perf_gate_evidence.py scopes",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "studio",
   "fixtures": [
    "artifacts/performance-stress-places/**"
   ],
   "id": "check_perf_gate_evidence-scopes",
   "inputs": [
    "tools/check_perf_gate_evidence.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_perf_gate_evidence.py studio",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "studio",
   "fixtures": [
    "artifacts/performance-stress-places/**"
   ],
   "id": "check_perf_gate_evidence-studio",
   "inputs": [
    "tools/check_perf_gate_evidence.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_perf_gate_evidence.py theme-cost",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "studio",
   "fixtures": [
    "artifacts/performance-stress-places/**"
   ],
   "id": "check_perf_gate_evidence-theme-cost",
   "inputs": [
    "tools/check_perf_gate_evidence.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_perf_metrics.py",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "device",
   "fixtures": [
    "artifacts/phase-4/perf.json"
   ],
   "id": "check_perf_metrics",
   "inputs": [
    "tools/check_perf_metrics.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_perf_place.py",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "studio",
   "fixtures": [
    "artifacts/performance-stress-places/place.json"
   ],
   "id": "check_perf_place",
   "inputs": [
    "tools/check_perf_place.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_perf_scenes.py",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/phase-4/perf.json"
   ],
   "id": "check_perf_scenes",
   "inputs": [
    "tools/check_perf_scenes.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_perf_scenes.py --themes",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/phase-4/perf.json"
   ],
   "id": "check_perf_scenes-themes",
   "inputs": [
    "tools/check_perf_scenes.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "lune run tools/lune/check_prop_parity_cli",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_prop_parity_cli",
   "inputs": [
    "tools/lune/check_prop_parity_cli.luau",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_public_allowlist.py",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_public_allowlist",
   "inputs": [
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "required by the release graph",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "lune run tools/lune/check_registration_cli",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_registration_cli",
   "inputs": [
    "tools/lune/check_registration_cli.luau",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": true,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_reuse_ledger.py",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_reuse_ledger",
   "inputs": [
    "tools/check_reuse_ledger.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_reuse_ledger.py --selftest",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_reuse_ledger-selftest",
   "inputs": [
    "tools/check_reuse_ledger.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_row_actions_matrix.py",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "device",
   "fixtures": [
    "artifacts/row-actions/device-matrix.md"
   ],
   "id": "check_row_actions_matrix",
   "inputs": [
    "tools/check_row_actions_matrix.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_sf_rows.py artifacts/sponsor-framework-gaps/rows/sf-a1.json SF-A1 SF-A2 SF-A3 SF-A4",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/sponsor-framework-gaps/rows/sf-a1.json"
   ],
   "id": "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-a1-b7f5d0",
   "inputs": [
    "tools/check_sf_rows.py",
    "artifacts/sponsor-framework-gaps/rows/sf-a1.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_sf_rows.py artifacts/sponsor-framework-gaps/rows/sf-a4.json SF-A4",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/sponsor-framework-gaps/rows/sf-a4.json"
   ],
   "id": "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-a4-478080",
   "inputs": [
    "tools/check_sf_rows.py",
    "artifacts/sponsor-framework-gaps/rows/sf-a4.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_sf_rows.py artifacts/sponsor-framework-gaps/rows/sf-c1.json SF-C1",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/sponsor-framework-gaps/rows/sf-c1.json"
   ],
   "id": "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-c1-73e76f",
   "inputs": [
    "tools/check_sf_rows.py",
    "artifacts/sponsor-framework-gaps/rows/sf-c1.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_sf_rows.py artifacts/sponsor-framework-gaps/rows/sf-c2.json SF-C2",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/sponsor-framework-gaps/rows/sf-c2.json"
   ],
   "id": "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-c2-725d54",
   "inputs": [
    "tools/check_sf_rows.py",
    "artifacts/sponsor-framework-gaps/rows/sf-c2.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_sf_rows.py artifacts/sponsor-framework-gaps/rows/sf-d1.json SF-D1 SF-D4 SF-D5",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/sponsor-framework-gaps/rows/sf-d1.json"
   ],
   "id": "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-d1-1c5ceb",
   "inputs": [
    "tools/check_sf_rows.py",
    "artifacts/sponsor-framework-gaps/rows/sf-d1.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_sf_rows.py artifacts/sponsor-framework-gaps/rows/sf-d3.json SF-D3",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/sponsor-framework-gaps/rows/sf-d3.json"
   ],
   "id": "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-d3-46e41d",
   "inputs": [
    "tools/check_sf_rows.py",
    "artifacts/sponsor-framework-gaps/rows/sf-d3.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_sf_rows.py artifacts/sponsor-framework-gaps/rows/sf-d4.json SF-D4",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/sponsor-framework-gaps/rows/sf-d4.json"
   ],
   "id": "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-d4-9a25f3",
   "inputs": [
    "tools/check_sf_rows.py",
    "artifacts/sponsor-framework-gaps/rows/sf-d4.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_sf_rows.py artifacts/sponsor-framework-gaps/rows/sf-d5.json SF-D5",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/sponsor-framework-gaps/rows/sf-d5.json"
   ],
   "id": "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-d5-329c3e",
   "inputs": [
    "tools/check_sf_rows.py",
    "artifacts/sponsor-framework-gaps/rows/sf-d5.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_sf_rows.py artifacts/sponsor-framework-gaps/rows/sf-f1.json SF-F1",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/sponsor-framework-gaps/rows/sf-f1.json"
   ],
   "id": "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-f1-d7b43f",
   "inputs": [
    "tools/check_sf_rows.py",
    "artifacts/sponsor-framework-gaps/rows/sf-f1.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_sf_rows.py artifacts/sponsor-framework-gaps/rows/sf-l1.json SF-L1 SF-L3",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/sponsor-framework-gaps/rows/sf-l1.json"
   ],
   "id": "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-l1-edbdee",
   "inputs": [
    "tools/check_sf_rows.py",
    "artifacts/sponsor-framework-gaps/rows/sf-l1.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_sf_rows.py artifacts/sponsor-framework-gaps/rows/sf-l2.json SF-L2",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/sponsor-framework-gaps/rows/sf-l2.json"
   ],
   "id": "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-l2-dcc9ef",
   "inputs": [
    "tools/check_sf_rows.py",
    "artifacts/sponsor-framework-gaps/rows/sf-l2.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_sf_rows.py artifacts/sponsor-framework-gaps/rows/sf-l3.json SF-L3",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/sponsor-framework-gaps/rows/sf-l3.json"
   ],
   "id": "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-l3-fed491",
   "inputs": [
    "tools/check_sf_rows.py",
    "artifacts/sponsor-framework-gaps/rows/sf-l3.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_sf_rows.py artifacts/sponsor-framework-gaps/rows/sf-m1.json SF-M1 SF-M2 SF-M3 SF-M7",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/sponsor-framework-gaps/rows/sf-m1.json"
   ],
   "id": "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-m1-c62ec7",
   "inputs": [
    "tools/check_sf_rows.py",
    "artifacts/sponsor-framework-gaps/rows/sf-m1.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_sf_rows.py artifacts/sponsor-framework-gaps/rows/sf-m2.json SF-M2",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/sponsor-framework-gaps/rows/sf-m2.json"
   ],
   "id": "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-m2-5a0768",
   "inputs": [
    "tools/check_sf_rows.py",
    "artifacts/sponsor-framework-gaps/rows/sf-m2.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_sf_rows.py artifacts/sponsor-framework-gaps/rows/sf-m4.json SF-M4",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/sponsor-framework-gaps/rows/sf-m4.json"
   ],
   "id": "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-m4-9a0448",
   "inputs": [
    "tools/check_sf_rows.py",
    "artifacts/sponsor-framework-gaps/rows/sf-m4.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_sf_rows.py artifacts/sponsor-framework-gaps/rows/sf-m5.json SF-M5",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/sponsor-framework-gaps/rows/sf-m5.json"
   ],
   "id": "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-m5-8cdae7",
   "inputs": [
    "tools/check_sf_rows.py",
    "artifacts/sponsor-framework-gaps/rows/sf-m5.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_sf_rows.py artifacts/sponsor-framework-gaps/rows/sf-m6.json SF-M6",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/sponsor-framework-gaps/rows/sf-m6.json"
   ],
   "id": "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-m6-a9c408",
   "inputs": [
    "tools/check_sf_rows.py",
    "artifacts/sponsor-framework-gaps/rows/sf-m6.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_sf_rows.py artifacts/sponsor-framework-gaps/rows/sf-m7.json SF-M7",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/sponsor-framework-gaps/rows/sf-m7.json"
   ],
   "id": "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-m7-c31a70",
   "inputs": [
    "tools/check_sf_rows.py",
    "artifacts/sponsor-framework-gaps/rows/sf-m7.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_sf_rows.py artifacts/sponsor-framework-gaps/rows/sf-m8.json SF-M8 SF-C4",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/sponsor-framework-gaps/rows/sf-m8.json"
   ],
   "id": "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-m8-45ab23",
   "inputs": [
    "tools/check_sf_rows.py",
    "artifacts/sponsor-framework-gaps/rows/sf-m8.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_sf_rows.py artifacts/sponsor-framework-gaps/rows/sf-p1.json SF-P1",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/sponsor-framework-gaps/rows/sf-p1.json"
   ],
   "id": "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-p1-71afb8",
   "inputs": [
    "tools/check_sf_rows.py",
    "artifacts/sponsor-framework-gaps/rows/sf-p1.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_sf_rows.py artifacts/sponsor-framework-gaps/rows/sf-p2.json SF-P2",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/sponsor-framework-gaps/rows/sf-p2.json"
   ],
   "id": "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-p2-a5c718",
   "inputs": [
    "tools/check_sf_rows.py",
    "artifacts/sponsor-framework-gaps/rows/sf-p2.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_sf_rows.py artifacts/sponsor-framework-gaps/rows/sf-p3.json SF-P3",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/sponsor-framework-gaps/rows/sf-p3.json"
   ],
   "id": "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-p3-1a56dc",
   "inputs": [
    "tools/check_sf_rows.py",
    "artifacts/sponsor-framework-gaps/rows/sf-p3.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_sf_rows.py artifacts/sponsor-framework-gaps/rows/sf-p4.json SF-P4",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/sponsor-framework-gaps/rows/sf-p4.json"
   ],
   "id": "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-p4-d3903b",
   "inputs": [
    "tools/check_sf_rows.py",
    "artifacts/sponsor-framework-gaps/rows/sf-p4.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_sf_rows.py artifacts/sponsor-framework-gaps/rows/sf-p5.json SF-P5",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/sponsor-framework-gaps/rows/sf-p5.json"
   ],
   "id": "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-p5-f34f1a",
   "inputs": [
    "tools/check_sf_rows.py",
    "artifacts/sponsor-framework-gaps/rows/sf-p5.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_sf_rows.py artifacts/sponsor-framework-gaps/rows/sf-t1.json SF-T1 SF-T2",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/sponsor-framework-gaps/rows/sf-t1.json"
   ],
   "id": "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-t1-006c64",
   "inputs": [
    "tools/check_sf_rows.py",
    "artifacts/sponsor-framework-gaps/rows/sf-t1.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_sf_rows.py artifacts/sponsor-framework-gaps/rows/sf-t2.json SF-T2",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/sponsor-framework-gaps/rows/sf-t2.json"
   ],
   "id": "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-t2-1fee50",
   "inputs": [
    "tools/check_sf_rows.py",
    "artifacts/sponsor-framework-gaps/rows/sf-t2.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_sf_rows.py artifacts/sponsor-framework-gaps/rows/sf-t3.json SF-T3",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/sponsor-framework-gaps/rows/sf-t3.json"
   ],
   "id": "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-t3-b16b62",
   "inputs": [
    "tools/check_sf_rows.py",
    "artifacts/sponsor-framework-gaps/rows/sf-t3.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_sf_rows.py artifacts/sponsor-framework-gaps/rows/sf-w1.json SF-W1",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/sponsor-framework-gaps/rows/sf-w1.json"
   ],
   "id": "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-w1-e07115",
   "inputs": [
    "tools/check_sf_rows.py",
    "artifacts/sponsor-framework-gaps/rows/sf-w1.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_source_size.py",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_source_size",
   "inputs": [
    "tools/check_source_size.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_spike.py artifacts/declarative-3d-architecture/costs.json mount-100-item-keyed-collection=MEASURED single-reactive-update=MEASURED keyed-remove-10-of-100=MEASURED keyed-reorder-full-reversal=MEASURED welded-assembly-move-20-parts=MEASURED model-pivot-move-11-instances=MEASURED teardown=MEASURED",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "studio",
   "fixtures": [
    "artifacts/declarative-3d-architecture/costs.json"
   ],
   "id": "check_spike-artifacts-declarative-3d-architecture-costs-61165b",
   "inputs": [
    "tools/check_spike.py",
    "artifacts/declarative-3d-architecture/costs.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_spike.py artifacts/declarative-3d-architecture/studio-topology.json server-root-once=PASS keyed-collection-mount=PASS client-root-once=PASS no-double-build=PASS client-root-invisible-to-server=PASS server-roots-alive-on-server=PASS anchor-created=PASS client-decoration-bound=PASS streaming-like-loss=PASS streaming-like-reentry=PASS streaming-like-second-cycle=PASS keyed-remove-survivors=PASS keyed-reorder-zero-churn=PASS single-update-bounded=PASS assembly-move-one-write=PASS assembly-welded-ride-engine-truth=PASS model-pivot-one-native-move=PASS external-destroy-tolerated=PASS destroy-cleanup=PASS",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "studio",
   "fixtures": [
    "artifacts/declarative-3d-architecture/studio-topology.json"
   ],
   "id": "check_spike-artifacts-declarative-3d-architecture-studio-694f2a",
   "inputs": [
    "tools/check_spike.py",
    "artifacts/declarative-3d-architecture/studio-topology.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_spike.py artifacts/studio/expansion-textinput-probe.json reflection-TextInputType-security-locked reflection-Enum-TextInputType-values focus-lifecycle-round-trip handshake-gameProcessedEvent-while-focused handshake-negative-control",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "studio",
   "fixtures": [
    "artifacts/studio/expansion-textinput-probe.json"
   ],
   "id": "check_spike-artifacts-studio-expansion-textinput-probe-j-378ee4",
   "inputs": [
    "tools/check_spike.py",
    "artifacts/studio/expansion-textinput-probe.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_spike.py artifacts/studio/ias-spike.json api-surface=PASS fire-requires-scriptable=PASS real-input-priority-sink=PASS scriptable-fire-bypasses-arbitration=PASS_WITH_DESIGN_IMPACT context-disposal-disconnects=PASS",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "studio",
   "fixtures": [
    "artifacts/studio/ias-spike.json"
   ],
   "id": "check_spike-artifacts-studio-ias-spike-json-api-surface-c265ac",
   "inputs": [
    "tools/check_spike.py",
    "artifacts/studio/ias-spike.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_spike.py artifacts/studio/part2-table-phaseb.json \"plain mouse click replaces selection=PASS\" \"ctrl-click toggles into multi selection=PASS\" \"shift-click extends contiguous range=PASS\" \"grip drag=PASS\" \"row drag-and-drop=PASS\" \"keyboard focus navigation through real IAS bindings=PASS\"",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "studio",
   "fixtures": [
    "artifacts/studio/part2-table-phaseb.json"
   ],
   "id": "check_spike-artifacts-studio-part2-table-phaseb-json-pla-c5d328",
   "inputs": [
    "tools/check_spike.py",
    "artifacts/studio/part2-table-phaseb.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_spike.py artifacts/studio/phase1-gallery.json engine-string-requires=PASS client-bootstrap=PASS real-inputaction-slice=PASS live-env-adaptation=PASS screengui-policy=PASS",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "studio",
   "fixtures": [
    "artifacts/studio/phase1-gallery.json"
   ],
   "id": "check_spike-artifacts-studio-phase1-gallery-json-engine-4934a9",
   "inputs": [
    "tools/check_spike.py",
    "artifacts/studio/phase1-gallery.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_spike.py artifacts/studio/phase2-port-drive.json flag-gating=PASS gear-topbar-dock=PASS modal-render=PASS real-mouse=PASS real-keyboard-activate=PASS server-persistence-roundtrip=PASS D1-escape=RESOLVED",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "studio",
   "fixtures": [
    "artifacts/studio/phase2-port-drive.json"
   ],
   "id": "check_spike-artifacts-studio-phase2-port-drive-json-flag-f8d169",
   "inputs": [
    "tools/check_spike.py",
    "artifacts/studio/phase2-port-drive.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_spike.py artifacts/studio/phase2-port-drive.json real-mouse=PASS real-keyboard-activate=PASS modal-render=PASS",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "studio",
   "fixtures": [
    "artifacts/studio/phase2-port-drive.json"
   ],
   "id": "check_spike-artifacts-studio-phase2-port-drive-json-real-9df8e9",
   "inputs": [
    "tools/check_spike.py",
    "artifacts/studio/phase2-port-drive.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_spike.py artifacts/studio/property-authority-spike.json explicit-write-defeats-style style-application-is-signal-silent",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "studio",
   "fixtures": [
    "artifacts/studio/property-authority-spike.json"
   ],
   "id": "check_spike-artifacts-studio-property-authority-spike-js-0120ab",
   "inputs": [
    "tools/check_spike.py",
    "artifacts/studio/property-authority-spike.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "lune run tools/lune/check_surface_ledger",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "tools/lune/verify/data/surface-ledger.md"
   ],
   "id": "check_surface_ledger",
   "inputs": [
    "tools/lune/check_surface_ledger.luau",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "lune run tools/lune/check_theme_drift_cli",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "check_theme_drift_cli",
   "inputs": [
    "tools/lune/check_theme_drift_cli.luau",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_traversal_evidence.py",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "studio",
   "fixtures": [
    "artifacts/traversal-document-order/studio/**"
   ],
   "id": "check_traversal_evidence",
   "inputs": [
    "tools/check_traversal_evidence.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_types.py",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/release-candidate-review/perf/types.json"
   ],
   "id": "check_types",
   "inputs": [
    "tools/check_types.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "runs alone: it generates and deletes a probe file inside tests/",
   "optional": false,
   "serialize": true,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_types.py --selftest",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/release-candidate-review/perf/types.json"
   ],
   "id": "check_types-selftest",
   "inputs": [
    "tools/check_types.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "runs alone: it rewrites src/init.luau and the type witness, then restores them",
   "optional": false,
   "serialize": true,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_verdicts.py artifacts/expansion-textinput/platform-research.json=FINDINGS artifacts/expansion-textinput/runtime-verification.json=PASS artifacts/expansion-textinput/architecture-verification.json=PASS artifacts/expansion-textinput/phase-gate-verification.json=PASS",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/expansion-textinput/platform-research.json",
    "artifacts/expansion-textinput/runtime-verification.json",
    "artifacts/expansion-textinput/architecture-verification.json",
    "artifacts/expansion-textinput/phase-gate-verification.json"
   ],
   "id": "check_verdicts-artifacts-expansion-textinput-platform-resea-b2e918",
   "inputs": [
    "tools/check_verdicts.py",
    "artifacts/expansion-textinput/platform-research.json",
    "artifacts/expansion-textinput/runtime-verification.json",
    "artifacts/expansion-textinput/architecture-verification.json",
    "artifacts/expansion-textinput/phase-gate-verification.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_verdicts.py artifacts/part-2/opus-verification.json=PASS \"artifacts/part-2/opus-ws1-verification.json=ALL requirement-affecting findings resolved\" \"artifacts/part-2/opus-ws2-verification.json=ALL requirement-affecting findings resolved\" \"artifacts/part-2/opus-ws3-verification.json=ALL requirement-affecting findings resolved\" \"artifacts/part-2/opus-ws4-verification.json=ALL requirement-affecting findings resolved\"",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/part-2/opus-verification.json"
   ],
   "id": "check_verdicts-artifacts-part-2-opus-verification-json-PASS-1ef0c4",
   "inputs": [
    "tools/check_verdicts.py",
    "artifacts/part-2/opus-verification.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_verdicts.py artifacts/phase-0/opus-verification.json=PASS",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/phase-0/opus-verification.json"
   ],
   "id": "check_verdicts-artifacts-phase-0-opus-verification-json-PAS-86d658",
   "inputs": [
    "tools/check_verdicts.py",
    "artifacts/phase-0/opus-verification.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_verdicts.py artifacts/phase-1/opus-verification.json=PASS",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/phase-1/opus-verification.json"
   ],
   "id": "check_verdicts-artifacts-phase-1-opus-verification-json-PAS-0becfb",
   "inputs": [
    "tools/check_verdicts.py",
    "artifacts/phase-1/opus-verification.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_verdicts.py artifacts/phase-2/opus-verification.json=PASS",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/phase-2/opus-verification.json"
   ],
   "id": "check_verdicts-artifacts-phase-2-opus-verification-json-PAS-6c97e1",
   "inputs": [
    "tools/check_verdicts.py",
    "artifacts/phase-2/opus-verification.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_verdicts.py artifacts/phase-3/opus-verification.json=PASS",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/phase-3/opus-verification.json"
   ],
   "id": "check_verdicts-artifacts-phase-3-opus-verification-json-PAS-0bd394",
   "inputs": [
    "tools/check_verdicts.py",
    "artifacts/phase-3/opus-verification.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_verdicts.py artifacts/phase-4/opus-verification.json=ALL_REQUIREMENT_AFFECTING_FINDINGS_RESOLVED artifacts/phase-4/opus-runtime-verification.json=ALL_REQUIREMENT_AFFECTING_FINDINGS_RESOLVED artifacts/phase-4/opus-platform-research.json=ALL_REQUIREMENT_AFFECTING_FINDINGS_RESOLVED artifacts/phase-4/opus-architecture-verification.json=ALL_REQUIREMENT_AFFECTING_FINDINGS_RESOLVED artifacts/phase-4/opus-phase-gate-verification.json=ALL_REQUIREMENT_AFFECTING_FINDINGS_RESOLVED",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/phase-4/opus-verification.json",
    "artifacts/phase-4/opus-runtime-verification.json",
    "artifacts/phase-4/opus-platform-research.json",
    "artifacts/phase-4/opus-architecture-verification.json",
    "artifacts/phase-4/opus-phase-gate-verification.json"
   ],
   "id": "check_verdicts-artifacts-phase-4-opus-verification-json-ALL-6c8acd",
   "inputs": [
    "tools/check_verdicts.py",
    "artifacts/phase-4/opus-verification.json",
    "artifacts/phase-4/opus-runtime-verification.json",
    "artifacts/phase-4/opus-platform-research.json",
    "artifacts/phase-4/opus-architecture-verification.json",
    "artifacts/phase-4/opus-phase-gate-verification.json",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/check_xp_matrix.py",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "device",
   "fixtures": [
    "artifacts/cross-platform-proof/**"
   ],
   "id": "check_xp_matrix",
   "inputs": [
    "tools/check_xp_matrix.py",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "lune run tests/conformance/cli custom --require-all-pass",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "cli-custom-require-all-pass",
   "inputs": [
    "tests/conformance/cli.luau",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "lune run tests/conformance/cli imperative",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "cli-imperative",
   "inputs": [
    "tests/conformance/cli.luau",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "lune run tests/conformance/corpus_cli",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "corpus_cli",
   "inputs": [
    "tests/conformance/corpus_cli.luau",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "tools/doctor.sh",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "doctor",
   "inputs": [
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "required by the release graph",
   "optional": false,
   "serialize": true,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "tools/faults.sh",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "faults",
   "inputs": [
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "required by the release graph",
   "optional": false,
   "serialize": true,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 900
  },
  {
   "command": "tools/fuzz.sh layout",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "fuzz-layout",
   "inputs": [
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "required by the release graph",
   "optional": false,
   "serialize": true,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "tools/fuzz.sh replication",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "fuzz-replication",
   "inputs": [
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "required by the release graph",
   "optional": false,
   "serialize": true,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "tools/fuzz.sh scheduler",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "fuzz-scheduler",
   "inputs": [
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "required by the release graph",
   "optional": false,
   "serialize": true,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "tools/package.sh verify",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "package",
   "fixtures": [],
   "id": "package-verify",
   "inputs": [
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "required by the release graph",
   "optional": false,
   "serialize": true,
   "tiers": {
    "fast": false,
    "full": false,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "tools/perf.sh",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "perf",
   "fixtures": [],
   "id": "perf",
   "inputs": [
    "tools/perf.sh",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "measure",
   "note": "",
   "optional": false,
   "serialize": true,
   "tiers": {
    "fast": false,
    "full": false,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "lune run tools/lune/prove_perf_gate",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "perf",
   "fixtures": [
    "artifacts/cross-platform-proof/rows/xp-a6-regression-proof.json",
    "artifacts/phase-4/perf.json"
   ],
   "id": "prove_perf_gate",
   "inputs": [
    "tools/lune/prove_perf_gate.luau",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "measure",
   "note": "",
   "optional": false,
   "serialize": true,
   "tiers": {
    "fast": false,
    "full": false,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "cd ../../../games/RascalRally/code && tools/suite_transcript.sh",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "external",
   "fixtures": [],
   "id": "rascalrally-suite",
   "inputs": [
    "../../../games/RascalRally/code/src/**",
    "../../../games/RascalRally/code/tests/**",
    "src/**",
    "tests/**"
   ],
   "kind": "external",
   "note": "required by the release graph",
   "optional": false,
   "serialize": true,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 3600
  },
  {
   "command": "tools/render.sh",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "perf",
   "fixtures": [],
   "id": "render",
   "inputs": [
    "tools/render.sh",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "measure",
   "note": "",
   "optional": false,
   "serialize": true,
   "tiers": {
    "fast": false,
    "full": false,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "tools/soak.sh",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "soak",
   "inputs": [
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "required by the release graph",
   "optional": false,
   "serialize": true,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 900
  },
  {
   "command": "stylua --check src tests tools bench examples",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "stylua-check-check-src-tests-tools-bench-examples",
   "inputs": [
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "stylua --check src tests tools examples",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "stylua-check-check-src-tests-tools-examples",
   "inputs": [
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "./run-tests.sh",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "suite",
   "inputs": [
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "examples/showcase.project.json",
    "examples/gallery.project.json",
    "examples/performance.project.json",
    "run-tests.sh",
    "rokit.toml"
   ],
   "kind": "suite",
   "note": "the one full deterministic suite run; writes structured per-case results",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 3600
  },
  {
   "command": "./run-tests.sh --fast",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "suite-fast",
   "inputs": [
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "examples/showcase.project.json",
    "examples/gallery.project.json",
    "examples/performance.project.json",
    "run-tests.sh",
    "rokit.toml"
   ],
   "kind": "suite",
   "note": "the inner-loop tier; its result is tier 'fast' and is REFUSED as suite evidence",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": true,
    "full": false,
    "release": false
   },
   "timeoutS": 1200
  },
  {
   "command": "tools/suite_cache_selftest.sh",
   "declaredEvidence": false,
   "dependsOn": [
    "suite"
   ],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "suite_cache_selftest",
   "inputs": [
    "tools/suite_cache_selftest.sh",
    "src/**",
    "tests/**",
    "examples/gallery/**",
    "examples/reference/**",
    "examples/themes/**",
    "examples/consumer/**",
    "examples/performance/**",
    "examples/table_phaseb/**",
    "docs/**",
    "tools/**",
    "bench/**",
    "package/**",
    "skills/**",
    "rokit.toml",
    "run-tests.sh",
    "phases.json",
    "requirements.json",
    "README.md",
    "AGENTS.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": true,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 3600
  },
  {
   "command": "lune run tools/lune/theme_sync_cli -- --dump artifacts/theme-packages-and-skinning/theme-sync/parchment-live-dump.json --package examples/themes/fantasy_parchment.luau --check",
   "declaredEvidence": true,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [
    "artifacts/theme-packages-and-skinning/theme-sync/parchment-live-dump.json"
   ],
   "id": "theme_sync_cli-dump-artifacts-theme-packages-and-skinning-t-6f81a3",
   "inputs": [
    "tools/lune/theme_sync_cli.luau",
    "artifacts/theme-packages-and-skinning/theme-sync/parchment-live-dump.json",
    "examples/themes/fantasy_parchment.luau",
    "tools/**"
   ],
   "kind": "scanner",
   "note": "",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": false,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "lune run tools/lune/verify/selftest",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "deterministic",
   "fixtures": [],
   "id": "verify-selftest",
   "inputs": [
    "tools/lune/verify/**"
   ],
   "kind": "scanner",
   "note": "the graph's own negative controls: every refusal the result store can make",
   "optional": false,
   "serialize": false,
   "tiers": {
    "fast": true,
    "full": true,
    "release": true
   },
   "timeoutS": 600
  },
  {
   "command": "python3 tools/archive_private.py verify",
   "declaredEvidence": false,
   "dependsOn": [],
   "environmentClass": "external",
   "fixtures": [],
   "id": "archive-integrity",
   "inputs": [
    "tools/archive_private.py"
   ],
   "kind": "external",
   "note": "the private archive still holds the bytes every receipt in this graph names",
   "optional": false,
   "serialize": true,
   "tiers": {
    "fast": false,
    "full": false,
    "release": true
   },
   "timeoutS": 600
  }
 ],
 "rows": [
  {
   "check": {
    "producers": [
     "doctor"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/doctor.json",
   "id": "phase-0-foundation::doctor",
   "name": "doctor",
   "note": "doctor: doctor must exit zero; and artifacts/doctor.json must exist. Requirements: UI-AGENT-001.",
   "phase": "phase-0-foundation",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/test.json",
   "id": "phase-0-foundation::test-suite",
   "name": "test-suite",
   "note": "test-suite: suite must exit zero; and artifacts/test.json must exist. Requirements: UI-AGENT-001.",
   "phase": "phase-0-foundation",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "cli-custom-require-all-pass"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/conformance-custom.json",
   "id": "phase-0-foundation::conformance-custom-core",
   "name": "conformance-custom-core",
   "note": "conformance-custom-core: cli-custom-require-all-pass must exit zero; and artifacts/conformance-custom.json must exist. Requirements: UI-RUNTIME-001, UI-RUNTIME-002, UI-RUNTIME-003, UI-LIFE-001, UI-LIFE-002.",
   "phase": "phase-0-foundation",
   "releaseBlocking": false,
   "requirements": [
    "UI-RUNTIME-001",
    "UI-RUNTIME-002",
    "UI-RUNTIME-003",
    "UI-LIFE-001",
    "UI-LIFE-002"
   ],
   "state": null
  },
  {
   "check": {},
   "class": "retired",
   "evidence": null,
   "id": "phase-0-foundation::conformance-fusion-adapter",
   "name": "conformance-fusion-adapter",
   "note": "conformance-fusion-adapter: retired in this stage; see retiredReason.",
   "phase": "phase-0-foundation",
   "releaseBlocking": false,
   "requirements": [
    "UI-RUNTIME-001",
    "UI-RUNTIME-002",
    "UI-RUNTIME-003",
    "UI-LIFE-001",
    "UI-LIFE-002"
   ],
   "retiredReason": "workstream K removed the vendored third-party reactive core and the adapter that bridged it, and archived the scorecard this row read; the conformance corpus still runs against the custom core and the imperative baseline",
   "state": "RETIRED"
  },
  {
   "check": {
    "producers": [
     "cli-imperative"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/conformance-imperative.json",
   "id": "phase-0-foundation::conformance-imperative-baseline",
   "name": "conformance-imperative-baseline",
   "note": "conformance-imperative-baseline: cli-imperative must exit zero; and artifacts/conformance-imperative.json must exist. Requirements: UI-RUNTIME-001, UI-RUNTIME-002, UI-RUNTIME-003, UI-LIFE-001, UI-LIFE-002.",
   "phase": "phase-0-foundation",
   "releaseBlocking": false,
   "requirements": [
    "UI-RUNTIME-001",
    "UI-RUNTIME-002",
    "UI-RUNTIME-003",
    "UI-LIFE-001",
    "UI-LIFE-002"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "render"
    ],
    "shell": "test -z \"$(grep -rEn 'task\\.wait|task\\.delay|:Wait\\(|coroutine\\.yield|GetTextBoundsAsync' src/layout/ | grep -vE ':[0-9]+:[[:space:]]*--')\""
   },
   "class": "evidence-pin",
   "evidence": "artifacts/render/summary.json",
   "id": "phase-0-foundation::layout-spike",
   "name": "layout-spike",
   "note": "layout-spike: render must exit zero; its file-existence and text assertions must hold; and artifacts/render/summary.json must exist. Requirements: UI-LAYOUT-001, UI-DIAG-001.",
   "phase": "phase-0-foundation",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001",
    "UI-DIAG-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "replication::two clients, one semantic snapshot::independent environments and presentation state converge on the same semantics"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/test.json",
   "id": "phase-0-foundation::replication-boundary-spike",
   "name": "replication-boundary-spike",
   "note": "replication-boundary-spike: 1 named suite case must pass; suite must exit zero; and artifacts/test.json must exist. Requirements: UI-REPL-001, UI-REPL-002, UI-ADAPT-001, UI-SERVER-001.",
   "phase": "phase-0-foundation",
   "releaseBlocking": false,
   "requirements": [
    "UI-REPL-001",
    "UI-REPL-002",
    "UI-ADAPT-001",
    "UI-SERVER-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_boundary"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/boundary.json",
   "id": "phase-0-foundation::client-boundary-static",
   "name": "client-boundary-static",
   "note": "client-boundary-static: check_boundary must exit zero; and artifacts/boundary.json must exist. Requirements: UI-BOUND-001.",
   "phase": "phase-0-foundation",
   "releaseBlocking": false,
   "requirements": [
    "UI-BOUND-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_spike-artifacts-studio-property-authority-spike-js-0120ab"
    ],
    "receipt": "tools/lune/verify/evidence/phase-0-foundation--property-authority-spike.json"
   },
   "class": "exit0",
   "evidence": "artifacts/studio/property-authority-spike.json",
   "id": "phase-0-foundation::property-authority-spike",
   "name": "property-authority-spike",
   "note": "property-authority-spike: check_spike-artifacts-studio-property-authority-spike-js-0120ab must exit zero; and artifacts/studio/property-authority-spike.json must exist. Requirements: UI-STYLE-001.",
   "phase": "phase-0-foundation",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_spike-artifacts-studio-ias-spike-json-api-surface-c265ac"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/studio/ias-spike.json",
   "id": "phase-0-foundation::input-action-spike",
   "name": "input-action-spike",
   "note": "input-action-spike: check_spike-artifacts-studio-ias-spike-json-api-surface-c265ac must exit zero; and artifacts/studio/ias-spike.json must exist. Requirements: UI-INPUT-001, UI-INPUT-003, UI-INPUT-004.",
   "phase": "phase-0-foundation",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-001",
    "UI-INPUT-003",
    "UI-INPUT-004"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "focus::logical focus::modal traps navigation with wrap and restores previous focus on pop"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/test.json",
   "id": "phase-0-foundation::modal-focus-spike",
   "name": "modal-focus-spike",
   "note": "modal-focus-spike: 1 named suite case must pass; suite must exit zero; and artifacts/test.json must exist. Requirements: UI-FOCUS-001.",
   "phase": "phase-0-foundation",
   "releaseBlocking": false,
   "requirements": [
    "UI-FOCUS-001"
   ],
   "state": null
  },
  {
   "check": {
    "receipt": "tools/lune/verify/evidence/phase-0-foundation--engine-fidelity-fixtures.json"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/studio/engine-fidelity.json",
   "id": "phase-0-foundation::engine-fidelity-fixtures",
   "name": "engine-fidelity-fixtures",
   "note": "engine-fidelity-fixtures: and artifacts/studio/engine-fidelity.json must exist. Requirements: UI-FID-001, UI-DIAG-001.",
   "phase": "phase-0-foundation",
   "releaseBlocking": false,
   "requirements": [
    "UI-FID-001",
    "UI-DIAG-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_verdicts-artifacts-phase-0-opus-verification-json-PAS-86d658"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/phase-0/opus-verification.json",
   "id": "phase-0-foundation::opus-verification",
   "name": "opus-verification",
   "note": "opus-verification: check_verdicts-artifacts-phase-0-opus-verification-json-PAS-86d658 must exit zero; and artifacts/phase-0/opus-verification.json must exist. Requirements: UI-AGENT-001.",
   "phase": "phase-0-foundation",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "doctor"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/doctor.json",
   "id": "phase-1-minimal-screen::doctor",
   "name": "doctor",
   "note": "doctor: doctor must exit zero; and artifacts/doctor.json must exist. Requirements: UI-AGENT-001.",
   "phase": "phase-1-minimal-screen",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/test.json",
   "id": "phase-1-minimal-screen::test-suite",
   "name": "test-suite",
   "note": "test-suite: suite must exit zero; and artifacts/test.json must exist. Requirements: UI-AGENT-001.",
   "phase": "phase-1-minimal-screen",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_boundary"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/boundary.json",
   "id": "phase-1-minimal-screen::client-boundary-static",
   "name": "client-boundary-static",
   "note": "client-boundary-static: check_boundary must exit zero; and artifacts/boundary.json must exist. Requirements: UI-BOUND-001.",
   "phase": "phase-1-minimal-screen",
   "releaseBlocking": false,
   "requirements": [
    "UI-BOUND-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "slice_mutation::slice: UI-driven authoritative mutation::one action carries keyboard, touch, and gamepad bindings simultaneously"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/test.json",
   "id": "phase-1-minimal-screen::settings-subset-multi-input",
   "name": "settings-subset-multi-input",
   "note": "settings-subset-multi-input: 1 named suite case must pass; suite must exit zero; and artifacts/test.json must exist. Requirements: UI-INPUT-001, UI-INPUT-002.",
   "phase": "phase-1-minimal-screen",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-001",
    "UI-INPUT-002"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite",
     "render"
    ],
    "resultIds": [
     "replication::two clients, one semantic snapshot::independent environments and presentation state converge on the same semantics"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/render/summary.json",
   "id": "phase-1-minimal-screen::two-clients-independent-layouts",
   "name": "two-clients-independent-layouts",
   "note": "two-clients-independent-layouts: 1 named suite case must pass; suite, render must exit zero; and artifacts/render/summary.json must exist. Requirements: UI-ADAPT-001, UI-SERVER-001.",
   "phase": "phase-1-minimal-screen",
   "releaseBlocking": false,
   "requirements": [
    "UI-ADAPT-001",
    "UI-SERVER-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "slice_mutation::slice: UI-driven authoritative mutation::toggle + save + delayed server confirm reconciles cleanly"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/test.json",
   "id": "phase-1-minimal-screen::authoritative-mutation-slice",
   "name": "authoritative-mutation-slice",
   "note": "authoritative-mutation-slice: 1 named suite case must pass; suite must exit zero; and artifacts/test.json must exist. Requirements: UI-REPL-002.",
   "phase": "phase-1-minimal-screen",
   "releaseBlocking": false,
   "requirements": [
    "UI-REPL-002"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "slice_mutation::slice: UI-driven authoritative mutation::rejection restores the draft; no permanent pending state"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/test.json",
   "id": "phase-1-minimal-screen::no-permanent-pending-or-divergence",
   "name": "no-permanent-pending-or-divergence",
   "note": "no-permanent-pending-or-divergence: 1 named suite case must pass; suite must exit zero; and artifacts/test.json must exist. Requirements: UI-REPL-001, UI-REPL-002.",
   "phase": "phase-1-minimal-screen",
   "releaseBlocking": false,
   "requirements": [
    "UI-REPL-001",
    "UI-REPL-002"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "presenter::presenter: screens and modals::a modal sinks navigation from the base screen and traps focus",
     "presenter::presenter: screens and modals::Cancel dismisses the modal, restores focus, and disposes its context"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/test.json",
   "id": "phase-1-minimal-screen::modal-context-priority-sink-disposal",
   "name": "modal-context-priority-sink-disposal",
   "note": "modal-context-priority-sink-disposal: 2 named suite cases must pass; suite must exit zero; and artifacts/test.json must exist. Requirements: UI-INPUT-004, UI-FOCUS-001.",
   "phase": "phase-1-minimal-screen",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-004",
    "UI-FOCUS-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "hint_no_remount::hint changes without remount::preferredInput flip updates the hint text prop only; zero factory reruns"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/test.json",
   "id": "phase-1-minimal-screen::preferred-input-affordance-no-remount",
   "name": "preferred-input-affordance-no-remount",
   "note": "preferred-input-affordance-no-remount: 1 named suite case must pass; suite must exit zero; and artifacts/test.json must exist. Requirements: UI-INPUT-002.",
   "phase": "phase-1-minimal-screen",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-002"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "soak"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/soak/summary.json",
   "id": "phase-1-minimal-screen::churn-registry-neutral",
   "name": "churn-registry-neutral",
   "note": "churn-registry-neutral: soak must exit zero; and artifacts/soak/summary.json must exist. Requirements: UI-LIFE-002.",
   "phase": "phase-1-minimal-screen",
   "releaseBlocking": false,
   "requirements": [
    "UI-LIFE-002"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "receipt": "tools/lune/verify/evidence/phase-1-minimal-screen--no-factory-rerun-trace.json",
    "resultIds": [
     "mount::mount: factory-once and dirty classes::a non-structural prop change dirties ONLY its node with declared classes and reruns no factory"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/studio/phase1-gallery.json",
   "id": "phase-1-minimal-screen::no-factory-rerun-trace",
   "name": "no-factory-rerun-trace",
   "note": "no-factory-rerun-trace: 1 named suite case must pass; suite must exit zero; and artifacts/studio/phase1-gallery.json must exist. Requirements: UI-RUNTIME-001.",
   "phase": "phase-1-minimal-screen",
   "releaseBlocking": false,
   "requirements": [
    "UI-RUNTIME-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_spike-artifacts-studio-phase1-gallery-json-engine-4934a9"
    ],
    "receipt": "tools/lune/verify/evidence/phase-1-minimal-screen--engine-fidelity.json"
   },
   "class": "exit0",
   "evidence": "artifacts/studio/phase1-gallery.json",
   "id": "phase-1-minimal-screen::engine-fidelity",
   "name": "engine-fidelity",
   "note": "engine-fidelity: check_spike-artifacts-studio-phase1-gallery-json-engine-4934a9 must exit zero; and artifacts/studio/phase1-gallery.json must exist. Requirements: UI-FID-001.",
   "phase": "phase-1-minimal-screen",
   "releaseBlocking": false,
   "requirements": [
    "UI-FID-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "bench"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/bench.json",
   "id": "phase-1-minimal-screen::bench-reproducible",
   "name": "bench-reproducible",
   "note": "bench-reproducible: bench must exit zero; and artifacts/bench.json must exist. Requirements: UI-PERF-001.",
   "phase": "phase-1-minimal-screen",
   "releaseBlocking": false,
   "requirements": [
    "UI-PERF-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_spike-artifacts-studio-phase2-port-drive-json-real-9df8e9"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/studio/phase2-port-drive.json",
   "id": "phase-1-minimal-screen::gallery-visible-viewport-checks",
   "name": "gallery-visible-viewport-checks",
   "note": "gallery-visible-viewport-checks: check_spike-artifacts-studio-phase2-port-drive-json-real-9df8e9 must exit zero; and artifacts/studio/phase2-port-drive.json must exist. Requirements: UI-FID-001.",
   "phase": "phase-1-minimal-screen",
   "releaseBlocking": false,
   "requirements": [
    "UI-FID-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_verdicts-artifacts-phase-1-opus-verification-json-PAS-0becfb"
    ],
    "shell": "test -f tests/focus_structural.spec.luau"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/phase-1/opus-verification.json",
   "id": "phase-1-minimal-screen::opus-verification",
   "name": "opus-verification",
   "note": "opus-verification: check_verdicts-artifacts-phase-1-opus-verification-json-PAS-0becfb must exit zero; its file-existence and text assertions must hold; and artifacts/phase-1/opus-verification.json must exist. Requirements: UI-AGENT-001.",
   "phase": "phase-1-minimal-screen",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "cd ../../../games/RascalRally/code"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "phase-2-settings-parity::game-suite-green",
   "name": "game-suite-green",
   "note": "game-suite-green: rascalrally-suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "phase-2-settings-parity",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/test.json",
   "id": "phase-2-settings-parity::library-suite-green",
   "name": "library-suite-green",
   "note": "library-suite-green: suite must exit zero; and artifacts/test.json must exist. Requirements: UI-AGENT-001.",
   "phase": "phase-2-settings-parity",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "cd ../../../games/RascalRally/code && grep -q '✓.*renders title + both entries with whole-row touch targets and the capped panel' \"$FACET_VERIFY_RR_TRANSCRIPT\" && grep -q '✓.*focus lands on the first entry; the scrim is never focusable' \"$FACET_VERIFY_RR_TRANSCRIPT\""
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "phase-2-settings-parity::behavior-parity-slice",
   "name": "behavior-parity-slice",
   "note": "behavior-parity-slice: rascalrally-suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-ADAPT-001, UI-FOCUS-001, UI-INPUT-004.",
   "phase": "phase-2-settings-parity",
   "releaseBlocking": false,
   "requirements": [
    "UI-ADAPT-001",
    "UI-FOCUS-001",
    "UI-INPUT-004"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "cd ../../../games/RascalRally/code && grep -q '✓.*toggling reduced motion repaints from STATE without any rebuild or remount' \"$FACET_VERIFY_RR_TRANSCRIPT\""
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "phase-2-settings-parity::state-driven-repaint-no-rebuild",
   "name": "state-driven-repaint-no-rebuild",
   "note": "state-driven-repaint-no-rebuild: rascalrally-suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-RUNTIME-001.",
   "phase": "phase-2-settings-parity",
   "releaseBlocking": false,
   "requirements": [
    "UI-RUNTIME-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "cd ../../../games/RascalRally/code && grep -q '✓.*open/close churn is registry-neutral' \"$FACET_VERIFY_RR_TRANSCRIPT\""
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "phase-2-settings-parity::open-close-memory-neutral",
   "name": "open-close-memory-neutral",
   "note": "open-close-memory-neutral: rascalrally-suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-LIFE-002.",
   "phase": "phase-2-settings-parity",
   "releaseBlocking": false,
   "requirements": [
    "UI-LIFE-002"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "cd ../../../games/RascalRally/code && grep -q '✓.*tap-outside (scrim) activates close routing' \"$FACET_VERIFY_RR_TRANSCRIPT\""
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "phase-2-settings-parity::touch-and-action-paths",
   "name": "touch-and-action-paths",
   "note": "touch-and-action-paths: rascalrally-suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-INPUT-001, UI-INPUT-003.",
   "phase": "phase-2-settings-parity",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-001",
    "UI-INPUT-003"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "cd ../../../games/RascalRally/code && grep -q '✓.*panel width tracks narrow phones below the cap' \"$FACET_VERIFY_RR_TRANSCRIPT\""
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "phase-2-settings-parity::multi-viewport-adaptation",
   "name": "multi-viewport-adaptation",
   "note": "multi-viewport-adaptation: rascalrally-suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-ADAPT-001.",
   "phase": "phase-2-settings-parity",
   "releaseBlocking": false,
   "requirements": [
    "UI-ADAPT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "focus_structural::confirm-before-ingest convergence (F2)::an optimistic view reconciled on snapshot ingest converges regardless of delivery order"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/test.json",
   "id": "phase-2-settings-parity::replication-convergence-carryover",
   "name": "replication-convergence-carryover",
   "note": "replication-convergence-carryover: 1 named suite case must pass; suite must exit zero; and artifacts/test.json must exist. Requirements: UI-REPL-001, UI-REPL-002.",
   "phase": "phase-2-settings-parity",
   "releaseBlocking": false,
   "requirements": [
    "UI-REPL-001",
    "UI-REPL-002"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "test -f ../../../games/RascalRally/code/src/client/SettingsGui.luau && grep -q 'settingsGui = SettingsGui.new(player)' ../../../games/RascalRally/code/src/client/init.client.luau && grep -q 'FacetFlags.settingsOn()' ../../../games/RascalRally/code/src/client/init.client.luau && grep -q 'UseFacetSettings' ../../../games/RascalRally/code/src/client/FacetFlags.luau"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "phase-2-settings-parity::old-settings-intact-and-default",
   "name": "old-settings-intact-and-default",
   "note": "old-settings-intact-and-default: its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "phase-2-settings-parity",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "bench"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/bench.json",
   "id": "phase-2-settings-parity::ui-cost-budget",
   "name": "ui-cost-budget",
   "note": "ui-cost-budget: bench must exit zero; and artifacts/bench.json must exist. Requirements: UI-PERF-001.",
   "phase": "phase-2-settings-parity",
   "releaseBlocking": false,
   "requirements": [
    "UI-PERF-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "f=../../../games/RascalRally/docs/FACET_SETTINGS_PORT.md && test -f \"$f\" && grep -q \"^## Behavior checklist\" \"$f\" && grep -q \"^## Code and complexity comparison\" \"$f\" && grep -q \"^## Defect log\" \"$f\" && grep -q \"^## Rollback and old-path deletion criteria\" \"$f\""
   },
   "class": "evidence-pin",
   "evidence": "../../../games/RascalRally/docs/FACET_SETTINGS_PORT.md",
   "id": "phase-2-settings-parity::port-doc-and-rollback",
   "name": "port-doc-and-rollback",
   "note": "port-doc-and-rollback: its file-existence and text assertions must hold; and ../../../games/RascalRally/docs/FACET_SETTINGS_PORT.md must exist. Requirements: UI-AGENT-001.",
   "phase": "phase-2-settings-parity",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_spike-artifacts-studio-phase2-port-drive-json-flag-f8d169"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/studio/phase2-port-drive.json",
   "id": "phase-2-settings-parity::studio-port-run",
   "name": "studio-port-run",
   "note": "studio-port-run: check_spike-artifacts-studio-phase2-port-drive-json-flag-f8d169 must exit zero; and artifacts/studio/phase2-port-drive.json must exist. Requirements: UI-FID-001.",
   "phase": "phase-2-settings-parity",
   "releaseBlocking": false,
   "requirements": [
    "UI-FID-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_verdicts-artifacts-phase-2-opus-verification-json-PAS-6c97e1"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/phase-2/opus-verification.json",
   "id": "phase-2-settings-parity::opus-verification",
   "name": "opus-verification",
   "note": "opus-verification: check_verdicts-artifacts-phase-2-opus-verification-json-PAS-6c97e1 must exit zero; and artifacts/phase-2/opus-verification.json must exist. Requirements: UI-AGENT-001.",
   "phase": "phase-2-settings-parity",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "cd ../../../games/RascalRally/code"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "phase-3-pilot::game-suite-green",
   "name": "game-suite-green",
   "note": "game-suite-green: rascalrally-suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "phase-3-pilot",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/test.json",
   "id": "phase-3-pilot::library-suite-green",
   "name": "library-suite-green",
   "note": "library-suite-green: suite must exit zero; and artifacts/test.json must exist. Requirements: UI-AGENT-001.",
   "phase": "phase-3-pilot",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "cd ../../../games/RascalRally/code && grep -q '✓.*every kart card is reachable by keyboard and by tap' \"$FACET_VERIFY_RR_TRANSCRIPT\""
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "phase-3-pilot::all-interactions-reachable-across-bindings",
   "name": "all-interactions-reachable-across-bindings",
   "note": "all-interactions-reachable-across-bindings: rascalrally-suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-INPUT-001, UI-FOCUS-001.",
   "phase": "phase-3-pilot",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-001",
    "UI-FOCUS-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "cd ../../../games/RascalRally/code && grep -q '✓.*first pick grants and equips through the validated mutation' \"$FACET_VERIFY_RR_TRANSCRIPT\" && cd ../../../games/RascalRally/code && grep -q '✓.*an invalid pick is rejected server-side and changes nothing' \"$FACET_VERIFY_RR_TRANSCRIPT\""
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "phase-3-pilot::validated-mutation-flow",
   "name": "validated-mutation-flow",
   "note": "validated-mutation-flow: rascalrally-suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-REPL-002, UI-SERVER-001.",
   "phase": "phase-3-pilot",
   "releaseBlocking": false,
   "requirements": [
    "UI-REPL-002",
    "UI-SERVER-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "cd ../../../games/RascalRally/code && grep -q '✓.*labels repaint with zero factory reruns' \"$FACET_VERIFY_RR_TRANSCRIPT\""
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "phase-3-pilot::no-factory-rerun-in-pilot",
   "name": "no-factory-rerun-in-pilot",
   "note": "no-factory-rerun-in-pilot: rascalrally-suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-RUNTIME-001.",
   "phase": "phase-3-pilot",
   "releaseBlocking": false,
   "requirements": [
    "UI-RUNTIME-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "cd ../../../games/RascalRally/code && grep -q '✓.*adapts across viewports: cards fill width on phone and desktop' \"$FACET_VERIFY_RR_TRANSCRIPT\""
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "phase-3-pilot::multi-viewport-adaptation",
   "name": "multi-viewport-adaptation",
   "note": "multi-viewport-adaptation: rascalrally-suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-ADAPT-001.",
   "phase": "phase-3-pilot",
   "releaseBlocking": false,
   "requirements": [
    "UI-ADAPT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "cd ../../../games/RascalRally/code && grep -q '✓.*screen dumps are deterministic and diagnosable' \"$FACET_VERIFY_RR_TRANSCRIPT\""
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "phase-3-pilot::diagnosable-from-dumps",
   "name": "diagnosable-from-dumps",
   "note": "diagnosable-from-dumps: rascalrally-suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-DIAG-001.",
   "phase": "phase-3-pilot",
   "releaseBlocking": false,
   "requirements": [
    "UI-DIAG-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "soak",
     "bench",
     "rascalrally-suite"
    ],
    "shell": "(cd ../../../games/RascalRally/code && cat \"$FACET_VERIFY_RR_TRANSCRIPT\" | grep -q '✓.*open/close churn is registry-neutral')"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/bench.json",
   "id": "phase-3-pilot::no-leak-regression",
   "name": "no-leak-regression",
   "note": "no-leak-regression: soak, bench, rascalrally-suite must exit zero; its file-existence and text assertions must hold; and artifacts/bench.json must exist. Requirements: UI-LIFE-002, UI-PERF-001.",
   "phase": "phase-3-pilot",
   "releaseBlocking": false,
   "requirements": [
    "UI-LIFE-002",
    "UI-PERF-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "layout::layout: ScrollView fill stretch (phase-3 regression guard)::cross-axis fill children stretch to the scroll viewport"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "phase-3-pilot::framework-defects-fixed-in-framework",
   "name": "framework-defects-fixed-in-framework",
   "note": "framework-defects-fixed-in-framework: 1 named suite case must pass; suite must exit zero. Requirements: UI-AGENT-001.",
   "phase": "phase-3-pilot",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_verdicts-artifacts-phase-3-opus-verification-json-PAS-0bd394"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/phase-3/opus-verification.json",
   "id": "phase-3-pilot::opus-verification",
   "name": "opus-verification",
   "note": "opus-verification: check_verdicts-artifacts-phase-3-opus-verification-json-PAS-0bd394 must exit zero; and artifacts/phase-3/opus-verification.json must exist. Requirements: UI-AGENT-001.",
   "phase": "phase-3-pilot",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "table::Table: column-owned columns (director round 5)::header taps cycle the owner-held sortOrder; the table never sorts the data",
     "table::Table: column-owned columns (director round 5)::per-column alignment positions cell content; vertical center is the default",
     "table::Table: column-owned columns (director round 5)::columns own their cells: `value` renders a default Text, `cell` renders custom",
     "table::Table: column-owned columns (director round 5)::width sugar: a number is fixed px; {min, ideal, max} is a minMax band",
     "table::Table: column resize interaction (Phase B)::grip drag previews a guide line and commits the width on release",
     "table::Table: row drag-and-drop (Phase B)::a drag shows ghost + indicator with the row IN PLACE; drop converts to post-removal slots",
     "table::Table: row drag-and-drop (Phase B)::an invalid drop (outside the table) commits nothing",
     "table::Table: row drag-and-drop (Phase B)::a system interrupt (Roblox menu opened) aborts the drag cleanly",
     "table::Table: standard-model focus and selection (director round 2)::arrow navigation moves the SELECTION with focus (the standard table model)",
     "table::Table: modifier selection semantics (Phase B)::shift-click extends a contiguous range from the anchor",
     "table::Table: column resize interaction (Phase B)::gamepad/keyboard: a focused column handle resizes its column via the Adjust action",
     "pointer::Pointer seam: dynamic hints and opt-in bindings::sinkNavigation stops nav keys from reaching lower contexts (the avatar)",
     "focus::logical focus::navigates the screen ring and wraps at the edges (director 2026-07-20)"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/test.json",
   "id": "part-2-director::ws1-table-phase-b-suite",
   "name": "ws1-table-phase-b-suite",
   "note": "ws1-table-phase-b-suite: 13 named suite cases must pass; suite must exit zero; and artifacts/test.json must exist. Requirements: UI-INPUT-001, UI-RUNTIME-001.",
   "phase": "part-2-director",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-001",
    "UI-RUNTIME-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "pointer::Pointer seam: rectOf and anchored overlays::Anchor overlay: a dynamic offset re-arranges with rect writes only (no remount)"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "part-2-director::ws1-pointer-seam-minimal-writes",
   "name": "ws1-pointer-seam-minimal-writes",
   "note": "ws1-pointer-seam-minimal-writes: 1 named suite case must pass; suite must exit zero. Requirements: UI-PERF-001, UI-RUNTIME-001.",
   "phase": "part-2-director",
   "releaseBlocking": false,
   "requirements": [
    "UI-PERF-001",
    "UI-RUNTIME-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "bench"
    ],
    "shell": "grep -q 'table-resize-drag' artifacts/bench.json"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/bench.json",
   "id": "part-2-director::ws1-adr-and-bench",
   "name": "ws1-adr-and-bench",
   "note": "ws1-adr-and-bench: bench must exit zero; its file-existence and text assertions must hold; and artifacts/bench.json must exist. Requirements: UI-AGENT-001, UI-PERF-001.",
   "phase": "part-2-director",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001",
    "UI-PERF-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_spike-artifacts-studio-part2-table-phaseb-json-pla-c5d328"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/studio/part2-table-phaseb.json",
   "id": "part-2-director::ws1-studio-drive",
   "name": "ws1-studio-drive",
   "note": "ws1-studio-drive: check_spike-artifacts-studio-part2-table-phaseb-json-pla-c5d328 must exit zero; and artifacts/studio/part2-table-phaseb.json must exist. Requirements: UI-FID-001.",
   "phase": "part-2-director",
   "releaseBlocking": false,
   "requirements": [
    "UI-FID-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "receipt": "tools/lune/verify/evidence/part-2-director--ws2-edit-preview-harness.json",
    "resultIds": [
     "preview::Preview: device profiles::applying a profile re-renders an attached tree with rect writes only"
    ],
    "shell": "test -f src/client/edit_preview.luau && test -f src/preview/device_profiles.luau && test -f tools/studio/Facet_EditPreview.plugin.lua"
   },
   "class": "result-ids",
   "evidence": "artifacts/studio/part2-edit-preview.json",
   "id": "part-2-director::ws2-edit-preview-harness",
   "name": "ws2-edit-preview-harness",
   "note": "ws2-edit-preview-harness: 1 named suite case must pass; suite must exit zero; its file-existence and text assertions must hold; and artifacts/studio/part2-edit-preview.json must exist. Requirements: UI-AGENT-001.",
   "phase": "part-2-director",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite",
     "bench"
    ],
    "receipt": "tools/lune/verify/evidence/part-2-director--ws3-billboard-target.json",
    "resultIds": [
     "render_target_contract::RenderTargetAdapter contract::the FakeTarget satisfies every required and optional method",
     "render_target_contract::RenderTargetAdapter contract::a billboard-sized pixel canvas renders a nameplate and updates paint-only"
    ],
    "shell": "test -f src/client/billboard_target.luau && test -f src/render/target_contract.luau && grep -q 'billboard-nameplate-storm' artifacts/bench.json"
   },
   "class": "result-ids",
   "evidence": "artifacts/studio/part2-billboard.json",
   "id": "part-2-director::ws3-billboard-target",
   "name": "ws3-billboard-target",
   "note": "ws3-billboard-target: 2 named suite cases must pass; suite, bench must exit zero; its file-existence and text assertions must hold; and artifacts/studio/part2-billboard.json must exist. Requirements: UI-EXT-001.",
   "phase": "part-2-director",
   "releaseBlocking": false,
   "requirements": [
    "UI-EXT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "receipt": "tools/lune/verify/evidence/part-2-director--ws4-game-table-surface.json",
    "shell": "cd ../../../games/RascalRally/code && grep -q '✓.*renders every racer headerless, in rank order, with custom cells' \"$FACET_VERIFY_RR_TRANSCRIPT\" && grep -q '✓.*a position swap reorders rows without remounting them' \"$FACET_VERIFY_RR_TRANSCRIPT\" && grep -q 'FacetFlags.racerListOn()' src/client/init.client.luau && grep -q 'UseFacetRacerList' src/client/FacetFlags.luau && test -f src/client/FacetRacerListScreen.luau"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/studio/part2-racer-list.json",
   "id": "part-2-director::ws4-game-table-surface",
   "name": "ws4-game-table-surface",
   "note": "ws4-game-table-surface: rascalrally-suite must exit zero; its file-existence and text assertions must hold; and artifacts/studio/part2-racer-list.json must exist. Requirements: UI-AGENT-001.",
   "phase": "part-2-director",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_verdicts-artifacts-part-2-opus-verification-json-PASS-1ef0c4"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/part-2/opus-verification.json",
   "id": "part-2-director::opus-verification",
   "name": "opus-verification",
   "note": "opus-verification: check_verdicts-artifacts-part-2-opus-verification-json-PASS-1ef0c4 must exit zero; and artifacts/part-2/opus-verification.json must exist. Requirements: UI-AGENT-001.",
   "phase": "part-2-director",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/test.json",
   "id": "phase-4-hardening::library-suite-green",
   "name": "library-suite-green",
   "note": "library-suite-green: suite must exit zero; and artifacts/test.json must exist. Requirements: UI-AGENT-001.",
   "phase": "phase-4-hardening",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "cd ../../../games/RascalRally/code"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "phase-4-hardening::game-suite-green",
   "name": "game-suite-green",
   "note": "game-suite-green: rascalrally-suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "phase-4-hardening",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "test -f ../../../games/RascalRally/code/src/client/SettingsGui.luau && grep -q 'FacetFlags.settingsOn()' ../../../games/RascalRally/code/src/client/init.client.luau && grep -q 'UseFacetSettings' ../../../games/RascalRally/code/src/client/FacetFlags.luau"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "phase-4-hardening::old-settings-intact-and-default",
   "name": "old-settings-intact-and-default",
   "note": "old-settings-intact-and-default: its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "phase-4-hardening",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "fuzz-scheduler"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/phase-4/fuzz-scheduler.json",
   "id": "phase-4-hardening::fuzz-scheduler",
   "name": "fuzz-scheduler",
   "note": "fuzz-scheduler: fuzz-scheduler must exit zero; and artifacts/phase-4/fuzz-scheduler.json must exist. Requirements: UI-RUNTIME-001, UI-RUNTIME-003, UI-LIFE-002.",
   "phase": "phase-4-hardening",
   "releaseBlocking": false,
   "requirements": [
    "UI-RUNTIME-001",
    "UI-RUNTIME-003",
    "UI-LIFE-002"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "fuzz-layout"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/phase-4/fuzz-layout.json",
   "id": "phase-4-hardening::fuzz-layout",
   "name": "fuzz-layout",
   "note": "fuzz-layout: fuzz-layout must exit zero; and artifacts/phase-4/fuzz-layout.json must exist. Requirements: UI-LAYOUT-001, UI-LAYOUT-002.",
   "phase": "phase-4-hardening",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001",
    "UI-LAYOUT-002"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "fuzz-replication"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/phase-4/fuzz-replication.json",
   "id": "phase-4-hardening::fuzz-replication",
   "name": "fuzz-replication",
   "note": "fuzz-replication: fuzz-replication must exit zero; and artifacts/phase-4/fuzz-replication.json must exist. Requirements: UI-REPL-001, UI-REPL-002.",
   "phase": "phase-4-hardening",
   "releaseBlocking": false,
   "requirements": [
    "UI-REPL-001",
    "UI-REPL-002"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "faults"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/phase-4/faults.json",
   "id": "phase-4-hardening::fault-injection",
   "name": "fault-injection",
   "note": "fault-injection: faults must exit zero; and artifacts/phase-4/faults.json must exist. Requirements: UI-RUNTIME-003, UI-LIFE-001, UI-LIFE-002, UI-INPUT-002, UI-ENV-001.",
   "phase": "phase-4-hardening",
   "releaseBlocking": false,
   "requirements": [
    "UI-RUNTIME-003",
    "UI-LIFE-001",
    "UI-LIFE-002",
    "UI-INPUT-002",
    "UI-ENV-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "table_virtualized::Table virtualized: only the window is mounted::a 400-row table mounts a bounded window, not 400 rows",
     "virtualization::virtualization: fixed-height keyed rows (§10.5)::mounts a bounded window (visible + overscan), never the whole list",
     "virtualization::virtualization: fixed-height keyed rows (§10.5)::a same-window scroll delta is ZERO framework writes (the engine owns the offset)",
     "virtualization::virtualization: fixed-height keyed rows (§10.5)::surviving rows keep their mounted identity across a window slide",
     "virtualization::virtualization: fixed-height keyed rows (§10.5)::focuses an off-window item BY KEY: materializes and scrolls it into view",
     "focus::logical focus::removing the focused item falls to the nearest surviving neighbor",
     "virtualization::virtualization: fixed-height keyed rows (§10.5)::keeps focus on the nearest surviving neighbor when the focused row is removed",
     "virtualization::virtualization: fixed-height keyed rows (§10.5)::a re-entered row's dump equals a fresh row's dump (no state bleed)",
     "virtualization::virtualization: fixed-height keyed rows (§10.5)::cancels async cell resources on window exit; late completion is stale",
     "virtualization::virtualization: fixed-height keyed rows (§10.5)::scroll storms leave every registry at baseline after dispose"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "phase-4-hardening::virtualization-hardening",
   "name": "virtualization-hardening",
   "note": "virtualization-hardening: 10 named suite cases must pass; suite must exit zero. Requirements: UI-RUNTIME-002, UI-LIFE-002, UI-FOCUS-001.",
   "phase": "phase-4-hardening",
   "releaseBlocking": false,
   "requirements": [
    "UI-RUNTIME-002",
    "UI-LIFE-002",
    "UI-FOCUS-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "navigation_groups::navigation groups (§9.3)::moves along the group axis; the orthogonal axis does not move within the group",
     "navigation_groups::navigation groups (§9.3)::wrap=false stops at the edge; wrap=true wraps inside the group",
     "navigation_groups::navigation groups (§9.3)::a per-direction exit override crosses groups with the target's entry policy",
     "navigation_groups::navigation groups (§9.3)::entry='restore' returns to the group's last-focused item",
     "navigation_groups::navigation groups (§9.3)::containment blocks implicit exits; only declared exits leave the group",
     "navigation_groups::navigation groups (§9.3)::without containment, an axis edge falls through to the neighboring group in order",
     "navigation_groups::navigation groups (§9.3)::a modal trap keeps direction navigation inside the modal scope",
     "virtual_list_focus_policy::VirtualList focusPolicy \"key\" (the default: focus follows the ITEM)::removal still lands on the nearest survivor, preferring the follower",
     "navigation_groups::navigation groups (§9.3)::replacing a group's order keeps focus or lands on the nearest survivor",
     "navigation_groups::navigation groups (§9.3)::flat scopes keep the legacy ring semantics (navigate ±1, all wrap)"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "phase-4-hardening::navigation-groups",
   "name": "navigation-groups",
   "note": "navigation-groups: 10 named suite cases must pass; suite must exit zero. Requirements: UI-FOCUS-001.",
   "phase": "phase-4-hardening",
   "releaseBlocking": false,
   "requirements": [
    "UI-FOCUS-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "corpus_cli"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/phase-4/a11y-l10n-corpus.json",
   "id": "phase-4-hardening::a11y-l10n-corpus",
   "name": "a11y-l10n-corpus",
   "note": "a11y-l10n-corpus: corpus_cli must exit zero; and artifacts/phase-4/a11y-l10n-corpus.json must exist. Requirements: UI-ADAPT-001.",
   "phase": "phase-4-hardening",
   "releaseBlocking": false,
   "requirements": [
    "UI-ADAPT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "perf"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/phase-4/perf.json",
   "id": "phase-4-hardening::perf-runner-floor-suite",
   "name": "perf-runner-floor-suite",
   "note": "perf-runner-floor-suite: perf must exit zero; and artifacts/phase-4/perf.json must exist. Requirements: UI-PERF-001.",
   "phase": "phase-4-hardening",
   "releaseBlocking": false,
   "requirements": [
    "UI-PERF-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "api_surface::semantic versioning and deprecation::VERSION is a plain semver MAJOR.MINOR.PATCH (scaffold suffixes are gone)",
     "api_surface::semantic versioning and deprecation::the published policy exists and states the rules the ledger is built on",
     "api_surface::semantic versioning and deprecation::deprecations are a machine-readable ledger with the policy's required fields"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "phase-4-hardening::semver-and-deprecation",
   "name": "semver-and-deprecation",
   "note": "semver-and-deprecation: 3 named suite cases must pass; suite must exit zero. Requirements: UI-AGENT-001.",
   "phase": "phase-4-hardening",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "error_boundary::error boundaries (§17 Phase 4)::renders its view when healthy; the fallback is not mounted",
     "error_boundary::error boundaries (§17 Phase 4)::a throwing view factory at mount swaps to the fallback; siblings survive",
     "error_boundary::error boundaries (§17 Phase 4)::a structural rebuild that throws INSIDE the boundary trips it at runtime",
     "error_boundary::error boundaries (§17 Phase 4)::boundary trips are registry-clean: dispose returns to baseline",
     "error_boundary::error boundaries (§17 Phase 4)::critical screen: a throwing screen presents its declared fallback screen",
     "error_boundary::error boundaries (§17 Phase 4)::outside a boundary the mount-time hard-error contract is unchanged"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "phase-4-hardening::error-boundaries",
   "name": "error-boundaries",
   "note": "error-boundaries: 6 named suite cases must pass; suite must exit zero. Requirements: UI-RUNTIME-003.",
   "phase": "phase-4-hardening",
   "releaseBlocking": false,
   "requirements": [
    "UI-RUNTIME-003"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "receipt": "tools/lune/verify/evidence/phase-4-hardening--engine-styling-shadows-corners.json",
    "resultIds": [
     "styling::engine styling: shadows and per-corner rounding (§17)::UI.shadow on an element lands as engine-true style data via the style authority",
     "styling::engine styling: shadows and per-corner rounding (§17)::a token preset shadow resolves from the style's shadow tokens",
     "styling::engine styling: shadows and per-corner rounding (§17)::shadows never enter layout: identical rects with and without a shadow",
     "styling::engine styling: shadows and per-corner rounding (§17)::uniform corners emit the alias form; distinct corners emit the per-corner form",
     "styling::engine styling: shadows and per-corner rounding (§17)::corner radii resolve token names from the active style",
     "styling::engine styling: shadows and per-corner rounding (§17)::mixing the alias and per-corner radii in one spec is a build error",
     "styling::engine styling: shadows and per-corner rounding (§17)::a positive shadow zIndex is a build error (engine: negative only)",
     "styling::engine styling: shadows and per-corner rounding (§17)::UI.styleGroup applies the modifier set to EACH element (group semantics)",
     "styling::engine styling: shadows and per-corner rounding (§17)::styling a container styles the container's own surface, not its children",
     "styling::engine styling: shadows and per-corner rounding (§17)::style lint warns on the jagged shadow+large-corner caveat and the shadow budget"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/studio/phase4-styling-fidelity.json",
   "id": "phase-4-hardening::engine-styling-shadows-corners",
   "name": "engine-styling-shadows-corners",
   "note": "engine-styling-shadows-corners: 10 named suite cases must pass; suite must exit zero; and artifacts/studio/phase4-styling-fidelity.json must exist. Requirements: UI-STYLE-001, UI-FID-001.",
   "phase": "phase-4-hardening",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001",
    "UI-FID-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_registration_cli",
     "suite"
    ],
    "resultIds": [
     "extension_checker::registration checker (agentic maintainability)::the live repository passes every registration rule",
     "extension_checker::extension scaffold::plans a complete control skeleton: source, spec, runner registration, registry entry, docs stub",
     "extension_checker::extension scaffold::plans a render-target adapter skeleton against the target contract"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "phase-4-hardening::maintainability-playbooks-and-checker",
   "name": "maintainability-playbooks-and-checker",
   "note": "maintainability-playbooks-and-checker: 3 named suite cases must pass; check_registration_cli, suite must exit zero. Requirements: UI-AGENT-001.",
   "phase": "phase-4-hardening",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite",
     "check_registration_cli"
    ],
    "resultIds": [
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL PopupButton: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "theme_roles::migrated controls: byte-parity under Studio Neutral::PopupButton: 40px trigger, and 36/44/56 rows per presentation",
     "theme_roles::migrated controls: byte-parity under Studio Neutral::PopupButton inline rows are 44 and sheet rows are 56",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newPopupButton refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newPopupButton still accepts its legal call",
     "adaptive_defaults::ADAPT-1: the six adaptive controls take their facts from the surface::PopupButton: touch resolves the SHEET with nothing wired at all",
     "adaptive_defaults::ADAPT-1: a control that cannot get its facts refuses, naming the host::Picker, PopupButton, Menu and TextInput each name the fix, not the symptom"
    ],
    "shell": "test -f src/controls/popup_button.luau"
   },
   "class": "result-ids",
   "evidence": "artifacts/phase-4/popup-dryrun.json",
   "id": "phase-4-hardening::popup-button-dry-run",
   "name": "popup-button-dry-run",
   "note": "popup-button-dry-run: 7 named suite cases must pass; suite, check_registration_cli must exit zero; its file-existence and text assertions must hold; and artifacts/phase-4/popup-dryrun.json must exist. Requirements: UI-AGENT-001.",
   "phase": "phase-4-hardening",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite",
     "check_registration_cli"
    ],
    "resultIds": [
     "examples_gallery::examples/gallery 01: temperature converter (numeric TextInput)::COMMIT mode: Enter commits the conversion into the Result (onCommit path)",
     "examples_gallery::examples/gallery 01: temperature converter (numeric TextInput)::the numeric field is the primary affordance: no stepper or Convert buttons remain",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::filter-as-you-type narrows the rows REACTIVELY, and clearing restores them all",
     "examples_gallery::examples/gallery 04: settings sync::a valid toggle applies optimistically, goes pending, then confirms"
    ],
    "shell": "test -f docs/guide/README.md && test -f docs/guide/01-concepts.md && test -f docs/guide/02-architecture.md && test -f docs/guide/03-getting-started.md && test -f docs/guide/04-tutorial-examples.md && test -f docs/guide/05-styling.md && test -f docs/guide/06-client-server.md && test -f docs/reference/api.md && ls examples/gallery/examples/01_temperature_converter.luau examples/gallery/examples/02_playlist_table.luau examples/gallery/examples/03_settings_sync.luau examples/gallery/examples/04_confirm_dialog.luau examples/gallery/examples/05_word_game.luau examples/gallery/examples/06_tile_game.luau examples/gallery/examples/07_match3.luau >/dev/null && ! grep -q \"TODO\" docs/guide/04-tutorial-examples.md"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "phase-4-hardening::documentation-and-examples",
   "name": "documentation-and-examples",
   "note": "documentation-and-examples: 4 named suite cases must pass; suite, check_registration_cli must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "phase-4-hardening",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "shell": "grep -q 'Designing for Facet' ../../specialists/UI_DESIGNER.md && grep -q 'Building on Facet' ../../specialists/UI_ENGINEER.md && grep -q 'semantic' ../../specialists/UI_DESIGNER.md && grep -q 'docs/guide/README.md' ../../specialists/UI_ENGINEER.md"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "phase-4-hardening::specialist-docs-updated",
   "name": "specialist-docs-updated",
   "note": "specialist-docs-updated: its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "phase-4-hardening",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_verdicts-artifacts-phase-4-opus-verification-json-ALL-6c8acd"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/phase-4/opus-verification.json",
   "id": "phase-4-hardening::opus-verification",
   "name": "opus-verification",
   "note": "opus-verification: check_verdicts-artifacts-phase-4-opus-verification-json-ALL-6c8acd must exit zero; and artifacts/phase-4/opus-verification.json must exist. Requirements: UI-AGENT-001.",
   "phase": "phase-4-hardening",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "phase-4-hardening::physical-device-confirmation",
   "name": "physical-device-confirmation",
   "note": "physical-device-confirmation: declared evidence, recorded rather than executed. Requirements: UI-PERF-001, UI-FID-001.",
   "phase": "phase-4-hardening",
   "releaseBlocking": false,
   "requirements": [
    "UI-PERF-001",
    "UI-FID-001"
   ],
   "state": "FAIL_ENVIRONMENT"
  },
  {
   "check": {
    "receipt": "tools/lune/verify/evidence/input-adaptation-audit--audit-matrix.json"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "input-adaptation-audit::audit-matrix",
   "name": "audit-matrix",
   "note": "audit-matrix: and artifacts/input-adaptation-audit/matrix.md must exist. Requirements: UI-INPUT-001, UI-INPUT-002.",
   "phase": "input-adaptation-audit",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-001",
    "UI-INPUT-002"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/test.json",
   "id": "input-adaptation-audit::library-suite-green",
   "name": "library-suite-green",
   "note": "library-suite-green: suite must exit zero; and artifacts/test.json must exist. Requirements: UI-AGENT-001.",
   "phase": "input-adaptation-audit",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "cd ../../../games/RascalRally/code"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "input-adaptation-audit::game-suite-green",
   "name": "game-suite-green",
   "note": "game-suite-green: rascalrally-suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "input-adaptation-audit",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "auto_input::auto-input: Button props.onActivate fires on every input, no consumer wiring::Button touch: a finger tap activates through the auto dispatch",
     "auto_input::auto-input: Toggle auto-flips its bound value (contract §10.2)::Toggle gamepad: ButtonA on the focused Toggle flips the value",
     "virtual_list_input::VirtualList gamepad input (no consumer wiring)::VirtualList gamepad: ButtonA activates the focused row",
     "virtual_list_input::VirtualList touch input (no consumer wiring)::VirtualList touch: a native pan scroll slides the window (driveScroll)",
     "virtual_list_input::VirtualList keyboard input (no consumer wiring)::VirtualList keyboard: focus past the window edge scrolls the row into view",
     "table_input::table_input: header-sort via keyboard and gamepad (auto headers group)::gamepad ButtonA SELECTS the column; the stick then acts on it (sort on Up/Down)",
     "table_input::table_input: body scroll-into-view on keyboard/gamepad focus moves::gamepad DPadDown past the clip band scrolls the focused row into view",
     "table_input::table_input: touch column-resize through the grip pointer seam::a TOUCH drag on the grip previews and commits the new column width",
     "popup_button::PopupButton: pointer interaction (the real tap path)::a TOUCH-typed tap on the trigger opens the popup listing every option"
    ],
    "shell": "test -f src/input/contribution.luau && grep -q \"contribution.attach\" src/controls/table.luau && grep -q \"contribution.attach\" src/controls/text_input.luau && grep -q \"contribution.attach\" src/controls/popup_button.luau && grep -q \"contribution.attach\" src/controls/virtual_list.luau"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "input-adaptation-audit::framework-auto-input",
   "name": "framework-auto-input",
   "note": "framework-auto-input: 9 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-INPUT-001, UI-INPUT-002, UI-FOCUS-001.",
   "phase": "input-adaptation-audit",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-001",
    "UI-INPUT-002",
    "UI-FOCUS-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_registration_cli"
    ],
    "stdoutPins": [
     {
      "flags": "-q",
      "pattern": "\"interactive controls prove four-input\"",
      "producer": "check_registration_cli"
     }
    ]
   },
   "class": "exit0",
   "evidence": null,
   "id": "input-adaptation-audit::per-control-per-input-conformance",
   "name": "per-control-per-input-conformance",
   "note": "per-control-per-input-conformance: check_registration_cli must exit zero; 1 pin on a producer's own output must match. Requirements: UI-INPUT-001, UI-INPUT-002.",
   "phase": "input-adaptation-audit",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-001",
    "UI-INPUT-002"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "examples_gallery::examples/gallery 04: settings sync::✓ ex03 touch: a touch tap on the Music toggle applies optimistically and goes pending",
     "examples_gallery::examples/gallery 04: settings sync::✓ ex03 touch: the whole accept round trip is reachable by touch alone",
     "examples_gallery::examples/gallery 04: settings sync::✓ ex03 keyboard: Return on the focused Music toggle applies optimistically",
     "examples_gallery::examples/gallery 04: settings sync::✓ ex03 keyboard: the arrows reach Deliver, and Return there completes the round trip",
     "examples_gallery::examples/gallery 04: settings sync::✓ ex03 gamepad: ButtonA on the focused Music toggle applies optimistically",
     "examples_gallery::examples/gallery 04: settings sync::✓ ex03 gamepad: D-pad reaches the volume steppers and ButtonA nudges the volume",
     "examples_gallery::examples/gallery 05: confirmation dialog::✓ ex04 pointer: tapping Delete opens the modal; tapping Cancel records cancelled and dismisses",
     "examples_gallery::examples/gallery 05: confirmation dialog::✓ ex04 touch: a touch tap opens the modal and a touch tap on Confirm records confirmed",
     "examples_games::examples/05 word game (Wordle-style)::✓ ex05 pointer: tapping on-screen keys fills the row and Enter submits",
     "examples_games::examples/05 word game (Wordle-style)::✓ ex05 touch: a touch tap on an on-screen key types its letter",
     "examples_games::examples/05 word game (Wordle-style)::✓ ex05 keyboard: arrow keys navigate the on-screen keyboard grid (device path)",
     "examples_games::examples/05 word game (Wordle-style)::✓ ex05 gamepad: DPad navigates the keyboard and ButtonA types the focused key",
     "examples_games::examples/07 tile game (Scrabble-style)::✓ ex06 touch: touch taps select a rack tile and place it on a board cell",
     "examples_games::examples/07 tile game (Scrabble-style)::✓ ex06 keyboard: arrow/Return select-then-place from the rack onto the board",
     "examples_games::examples/07 tile game (Scrabble-style)::✓ ex06 gamepad: DPad grid navigation with ButtonA select-then-place",
     "examples_games::examples/08 match-3::✓ ex07 pointer: tapping two adjacent tiles swaps them (matches the swap API)",
     "examples_games::examples/08 match-3::✓ ex07 touch: touch taps on two adjacent tiles swap them",
     "examples_games::examples/08 match-3::✓ ex07 keyboard: arrow keys navigate and Return swaps adjacent tiles",
     "examples_games::examples/08 match-3::✓ ex07 gamepad: DPad navigation and ButtonA swaps adjacent tiles",
     "examples_gallery::examples/gallery 01: temperature converter (numeric TextInput)::per-input activation: pointer tap and touch tap each enter edit mode",
     "examples_gallery::examples/gallery 01: temperature converter (numeric TextInput)::per-input activation: gamepad A enters edit mode, B cancels reverting to the snapshot"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "input-adaptation-audit::examples-all-input-headless",
   "name": "examples-all-input-headless",
   "note": "examples-all-input-headless: 21 named suite cases must pass; suite must exit zero. Requirements: UI-INPUT-001, UI-AGENT-001.",
   "phase": "input-adaptation-audit",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-001",
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_example_drift_cli"
    ],
    "shell": "! grep -l \"navigationGroups\\|onNavigateIntercept\\|onFocusNav\\|keepVisibleOffset\\|onGeometry\" $(ls examples/gallery/examples/*.luau | grep -v 07_match3) && grep -q \"THE BOARD DECLARES ITS OWN 2D NAVIGATION\" examples/gallery/examples/07_match3.luau"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "input-adaptation-audit::examples-no-input-boilerplate",
   "name": "examples-no-input-boilerplate",
   "note": "examples-no-input-boilerplate: no tutorial example hand-wires input or navigation, with one recorded exception: the match-3 board anchors free-placed tiles, so no layout can derive its groups, and the example declares them with the stated reason the check pins. Requirements: UI-AGENT-001.",
   "phase": "input-adaptation-audit",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "receipt": "tools/lune/verify/evidence/input-adaptation-audit--studio-all-input-verification.json"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/input-adaptation-audit/studio-drive.json",
   "id": "input-adaptation-audit::studio-all-input-verification",
   "name": "studio-all-input-verification",
   "note": "studio-all-input-verification: and artifacts/input-adaptation-audit/studio-drive.json must exist. Requirements: UI-FID-001.",
   "phase": "input-adaptation-audit",
   "releaseBlocking": false,
   "requirements": [
    "UI-FID-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "gamepad_contention::gamepad_contention (client module, headless structural spec)::describeContention() returns a string carrying all three §3 truths and the resolution",
     "gamepad_contention::gamepad_contention (client module, headless structural spec)::legacyStackActive() is a guarded probe: headless it returns false, never throws"
    ],
    "shell": "test -f src/client/gamepad_contention.luau && grep -q \"legacyStackActive\" src/client/gamepad_contention.luau && grep -q \"disableLegacyControls\" examples/gallery/client/init.client.luau && test -f docs/guide/07-input.md && grep -q \"PlayerScriptsUseInputActionSystem\" docs/guide/07-input.md"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "input-adaptation-audit::engine-truths-encoded",
   "name": "engine-truths-encoded",
   "note": "engine-truths-encoded: 2 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-FID-001, UI-AGENT-001.",
   "phase": "input-adaptation-audit",
   "releaseBlocking": false,
   "requirements": [
    "UI-FID-001",
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "responder::responder (a): a passive HUD leaves avatar input untouched::Space/ButtonA/Up all reach gameplay and the HUD focus never moves",
     "responder::responder (d): a modal over gameplay suppresses avatar input; dismiss restores::Space/ButtonA/Up are suppressed while the modal is up and restored on dismiss",
     "responder::responder (b): a tap on the HUD's focusable engages it (first responder)::handle.engage() is the explicit public path to first responder",
     "responder::responder (f): present() default (no responder opt) is byte-identical::nav works immediately AND gameplay ALSO receives (documented UI-only-place semantics)",
     "responder::responder (g): engage/resign churn is leak-free and registry-neutral::50 engage/resign cycles leave a single working context and gameplay intact",
     "responder::responder: Sink changes go through the setSink adapter seam::engage() calls setSink(true) and resign() calls setSink(false) on the context"
    ],
    "shell": "test -f src/client/responder_effects.luau && grep -q \"setSink\" src/client/roblox_input.luau && grep -q \"Workspace.PlayerScriptsUseInputActionSystem\" docs/guide/07-input.md"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "input-adaptation-audit::first-responder-model",
   "name": "first-responder-model",
   "note": "first-responder-model: 6 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-INPUT-003, UI-INPUT-004, UI-FID-001.",
   "phase": "input-adaptation-audit",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-003",
    "UI-INPUT-004",
    "UI-FID-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_registration_cli"
    ],
    "shell": "grep -q \"contribution.attach\" docs/extending/new-control.md && grep -q \"inputProofs\" docs/extending/new-control.md && grep -q \"contribution.attach\" tools/lune/scaffold.luau && grep -q \"PlayerScriptsUseInputActionSystem\" docs/guide/07-input.md && grep -q \"responder\" docs/guide/07-input.md && grep -q \"07-input\" docs/guide/README.md && grep -q \"onActivate\" docs/reference/api.md && grep -q \"engage\" docs/reference/api.md"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "input-adaptation-audit::playbooks-and-docs-updated",
   "name": "playbooks-and-docs-updated",
   "note": "playbooks-and-docs-updated: check_registration_cli must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "input-adaptation-audit",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "input-adaptation-audit::physical-device-confirmation",
   "name": "physical-device-confirmation",
   "note": "physical-device-confirmation: declared evidence, recorded rather than executed. Requirements: UI-PERF-001, UI-FID-001.",
   "phase": "input-adaptation-audit",
   "releaseBlocking": false,
   "requirements": [
    "UI-PERF-001",
    "UI-FID-001"
   ],
   "state": "FAIL_ENVIRONMENT"
  },
  {
   "check": {
    "producers": [
     "suite"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/test.json",
   "id": "expansion-textinput::library-suite-green",
   "name": "library-suite-green",
   "note": "library-suite-green: suite must exit zero; and artifacts/test.json must exist. Requirements: UI-AGENT-001.",
   "phase": "expansion-textinput",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "cd ../../../games/RascalRally/code"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "expansion-textinput::game-suite-green",
   "name": "game-suite-green",
   "note": "game-suite-green: rascalrally-suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "expansion-textinput",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_spike-artifacts-studio-expansion-textinput-probe-j-378ee4"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/studio/expansion-textinput-probe.json",
   "id": "expansion-textinput::engine-facts-probed",
   "name": "engine-facts-probed",
   "note": "engine-facts-probed: check_spike-artifacts-studio-expansion-textinput-probe-j-378ee4 must exit zero; and artifacts/studio/expansion-textinput-probe.json must exist. Requirements: UI-FID-001.",
   "phase": "expansion-textinput",
   "releaseBlocking": false,
   "requirements": [
    "UI-FID-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "text_input::TextInput: pointer (tap to edit, type, commit)::tapping the field enters editing (the control-owned handshake)",
     "text_input::TextInput: touch (activation + keep-visible, end to end)::a touch tap enters editing and the occlusion keep-visible engages (touch story end to end)",
     "text_input::TextInput: keyboard (Activate enters, sink blocks navigation, Enter commits, nav resumes)::focused field + Activate (Return) enters editing",
     "text_input::TextInput: gamepad (D-pad focus, A enters, D-pad sunk, B cancels-revert, A re-enters)::ButtonA enters editing; D-pad does not navigate; ButtonB cancels reverting to the snapshot",
     "text_input::TextInput: pointer (tap to edit, type, commit)::typed text flows through the adapter seam into the owner value, firing onChange per accepted edit"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "expansion-textinput::textinput-per-input-interaction",
   "name": "textinput-per-input-interaction",
   "note": "textinput-per-input-interaction: 5 named suite cases must pass; suite must exit zero. Requirements: UI-INPUT-001, UI-INPUT-002, UI-FOCUS-001.",
   "phase": "expansion-textinput",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-001",
    "UI-INPUT-002",
    "UI-FOCUS-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "text_input::TextInput: keyboard (Activate enters, sink blocks navigation, Enter commits, nav resumes)::while editing, navigation keys are sunk: a lower Navigate probe never fires and focus stays",
     "text_input::TextInput: keyboard (Activate enters, sink blocks navigation, Enter commits, nav resumes)::Enter commits, editing ends, and navigation works again (probe fires post-edit)",
     "text_input::TextInput: keyboard (Activate enters, sink blocks navigation, Enter commits, nav resumes)::Activate does not re-fire while editing: a second Return is an idempotent no-op",
     "text_input::TextInput: gamepad (D-pad focus, A enters, D-pad sunk, B cancels-revert, A re-enters)::ButtonA enters editing; D-pad does not navigate; ButtonB cancels reverting to the snapshot",
     "text_input::TextInput: gamepad (D-pad focus, A enters, D-pad sunk, B cancels-revert, A re-enters)::ButtonA re-enters editing after a cancel",
     "text_input::TextInput: conformance (reactive discipline, dump, registry neutrality)::build/mount/interact/dispose returns every registry to its baseline (incl. a mid-edit dispose)"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "expansion-textinput::textinput-handshake-and-cancel",
   "name": "textinput-handshake-and-cancel",
   "note": "textinput-handshake-and-cancel: 6 named suite cases must pass; suite must exit zero. Requirements: UI-INPUT-003, UI-INPUT-004.",
   "phase": "expansion-textinput",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-003",
    "UI-INPUT-004"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "text_input::TextInput: keyboard-occlusion keep-visible::a mid-edit occlusion that overlaps the field produces an upward transform, restored on dismiss",
     "text_input::TextInput: keyboard-occlusion keep-visible::no occlusion offset when not editing, even if the keyboard rect is set"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "expansion-textinput::textinput-occlusion-keep-visible",
   "name": "textinput-occlusion-keep-visible",
   "note": "textinput-occlusion-keep-visible: 2 named suite cases must pass; suite must exit zero. Requirements: UI-ENV-001, UI-ADAPT-001.",
   "phase": "expansion-textinput",
   "releaseBlocking": false,
   "requirements": [
    "UI-ENV-001",
    "UI-ADAPT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_boundary"
    ],
    "receipt": "tools/lune/verify/evidence/expansion-textinput--adapter-capability-detection.json",
    "shell": "grep -q 'canSetTextInputType' src/client/screen_props.luau && grep -q 'ClearTextOnFocus' src/client/screen_target.luau && grep -q 'lastModelText' src/client/screen_target.luau && grep -q 'setTextInputHandlers' src/client/screen_target.luau"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/studio/expansion-textinput-adapter.json",
   "id": "expansion-textinput::adapter-capability-detection",
   "name": "adapter-capability-detection",
   "note": "adapter-capability-detection: check_boundary must exit zero; its file-existence and text assertions must hold; and artifacts/studio/expansion-textinput-adapter.json must exist. Requirements: UI-STYLE-001, UI-FID-001.",
   "phase": "expansion-textinput",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001",
    "UI-FID-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "examples_gallery::examples/gallery 01: temperature converter (numeric TextInput)::the numeric field is the primary affordance: no stepper or Convert buttons remain",
     "examples_gallery::examples/gallery 01: temperature converter (numeric TextInput)::LIVE mode: typing a valid number updates the Celsius preview WITHOUT committing the Result",
     "examples_gallery::examples/gallery 01: temperature converter (numeric TextInput)::COMMIT mode: Enter commits the conversion into the Result (onCommit path)",
     "examples_gallery::examples/gallery 01: temperature converter (numeric TextInput)::COMMIT mode: focus loss (tapping away) also commits",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::filter-as-you-type narrows the rows REACTIVELY, and clearing restores them all",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::the in-field clear (✕) exists ONLY while the filter is non-empty",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::the handshake: typing in the filter field never navigates the table or moves selection",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::reordering is DISABLED while a filter is active (the media-library convention): the base order is preserved",
     "examples_gallery::examples/gallery 01: temperature converter (numeric TextInput)::per-input activation: pointer tap and touch tap each enter edit mode",
     "examples_gallery::examples/gallery 01: temperature converter (numeric TextInput)::per-input activation: gamepad A enters edit mode, B cancels reverting to the snapshot"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "expansion-textinput::examples-upgraded",
   "name": "examples-upgraded",
   "note": "examples-upgraded: 10 named suite cases must pass; suite must exit zero. Requirements: UI-AGENT-001, UI-RUNTIME-001.",
   "phase": "expansion-textinput",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001",
    "UI-RUNTIME-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_registration_cli"
    ],
    "shell": "grep -q 'newTextInput' docs/reference/api.md && grep -q 'onFocusGained(path)' docs/reference/api.md && grep -q 'TextInput' docs/guide/04-tutorial-examples.md"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "expansion-textinput::registration-and-docs",
   "name": "registration-and-docs",
   "note": "registration-and-docs: check_registration_cli must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "expansion-textinput",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "receipt": "tools/lune/verify/evidence/expansion-textinput--studio-drive-and-screenshots.json"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/studio/expansion-textinput-drive.json",
   "id": "expansion-textinput::studio-drive-and-screenshots",
   "name": "studio-drive-and-screenshots",
   "note": "studio-drive-and-screenshots: and artifacts/studio/expansion-textinput-drive.json must exist. Requirements: UI-FID-001.",
   "phase": "expansion-textinput",
   "releaseBlocking": false,
   "requirements": [
    "UI-FID-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "bench"
    ],
    "shell": "grep -q 'textinput-typing-storm' artifacts/bench.json"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/bench.json",
   "id": "expansion-textinput::expansion-adr-bench-rollback",
   "name": "expansion-adr-bench-rollback",
   "note": "expansion-adr-bench-rollback: bench must exit zero; its file-existence and text assertions must hold; and artifacts/bench.json must exist. Requirements: UI-AGENT-001, UI-PERF-001.",
   "phase": "expansion-textinput",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001",
    "UI-PERF-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_verdicts-artifacts-expansion-textinput-platform-resea-b2e918"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/expansion-textinput/phase-gate-verification.json",
   "id": "expansion-textinput::opus-verification",
   "name": "opus-verification",
   "note": "opus-verification: check_verdicts-artifacts-expansion-textinput-platform-resea-b2e918 must exit zero; and artifacts/expansion-textinput/phase-gate-verification.json must exist. Requirements: UI-AGENT-001.",
   "phase": "expansion-textinput",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "expansion-textinput::physical-device-confirmation",
   "name": "physical-device-confirmation",
   "note": "physical-device-confirmation: declared evidence, recorded rather than executed. Requirements: UI-PERF-001, UI-FID-001.",
   "phase": "expansion-textinput",
   "releaseBlocking": false,
   "requirements": [
    "UI-PERF-001",
    "UI-FID-001"
   ],
   "state": "FAIL_ENVIRONMENT"
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "table::Table: touch scroll vs edit-mode reorder (director design 2026-07-20)::auto Edit/Done toggle: shown when a gamepad capability is live even though preferredInput stays KeyboardAndMouse",
     "table::Table: touch scroll vs edit-mode reorder (director design 2026-07-20)::auto Edit/Done toggle: a live touch capability shows it under a KeyboardAndMouse preferredInput (hybrid handheld)"
    ],
    "shell": "grep -q \"interactionClasses\" src/env/environment.luau && grep -q \"interactionClasses\" src/controls/table.luau && grep -q \"GamepadConnected\" src/client/roblox_env.luau && grep -q \"sawGamepadInput\" src/client/roblox_env.luau"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "input-paradigms::defect-example02-fix",
   "name": "defect-example02-fix",
   "note": "defect-example02-fix: 2 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold; and artifacts/input-paradigms/defect-example02.md must exist. Requirements: UI-INPUT-002, UI-ENV-001.",
   "phase": "input-paradigms",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-002",
    "UI-ENV-001"
   ],
   "state": null
  },
  {
   "check": {
    "shell": "grep -q 'UI-PARADIGM-001' requirements.json && grep -q 'UI-PARADIGM-003' requirements.json"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "input-paradigms::affordance-matrix",
   "name": "affordance-matrix",
   "note": "affordance-matrix: its file-existence and text assertions must hold; and artifacts/input-paradigms/affordance-matrix.md must exist. Requirements: UI-INPUT-002, UI-PARADIGM-001.",
   "phase": "input-paradigms",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-002",
    "UI-PARADIGM-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_registration_cli",
     "suite"
    ],
    "resultIds": [
     "table::Table: touch scroll vs edit-mode reorder (director design 2026-07-20)::auto Edit/Done toggle: shown when a gamepad capability is live even though preferredInput stays KeyboardAndMouse",
     "paradigm_hover::UI-PARADIGM-001: pointer hover layer::a pointer-live renderer enables hover on interactive nodes and synthetic hover flips their state",
     "paradigm_table::paradigm_table: reorderable + selection-none keeps rows reachable (live-drive regression)::DPadDown leaves the auto Edit/Done toggle and reaches a row",
     "paradigm_table::paradigm_table: reorderable + selection-none keeps rows reachable (live-drive regression)::gamepad grab-mode reorders end to end with selection none",
     "paradigm_popup::PopupButton parity: outside-tap dismiss::a tap on a rendered node OUTSIDE the open panel dismisses it (no selection change)",
     "paradigm_popup::PopupButton parity: gamepad Cancel::gamepad ButtonB while open closes the popup (no selection change)",
     "paradigm_input_axis::input-axis: bare-control Adjust auto-wiring (dynamic, focus-gated)::Adjust keys bind the moment focus lands on the declared target",
     "paradigm_input_axis::input-axis: presenter binds Thumbstick1 for Navigate (vertical, flat ring)::a Thumbstick1 y push moves focus down like DPadDown, with the latch"
    ],
    "stdoutPins": [
     {
      "flags": "-q",
      "pattern": "\"prove the paradigm axis\"",
      "producer": "check_registration_cli"
     }
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "input-paradigms::per-control-affordance-proofs",
   "name": "per-control-affordance-proofs",
   "note": "per-control-affordance-proofs: 8 named suite cases must pass; check_registration_cli, suite must exit zero; 1 pin on a producer's own output must match; and artifacts/input-paradigms/affordance-matrix.md must exist. Requirements: UI-PARADIGM-001.",
   "phase": "input-paradigms",
   "releaseBlocking": false,
   "requirements": [
    "UI-PARADIGM-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "paradigm_table::paradigm_table: hot-switch in-flight transitions (§C)::pointer drag mid-flight + pointer capability lost => CANCEL, revert, no reorder",
     "paradigm_carry::UI-PARADIGM-002: framework CARRY rows (matrix §C)::focus survives any class flip (ring stays put; every live class can keep driving it)",
     "paradigm_carry::UI-PARADIGM-002: framework CARRY rows (matrix §C)::an exclusive modal survives any class flip (never un-engages; dismissal paths intact)",
     "paradigm_textinput::UI-PARADIGM-002 text entry: CARRY when a device arrives mid-edit::a gamepad arriving mid-edit CARRIES the edit (editing stays, text intact)",
     "paradigm_textinput::UI-PARADIGM-002 text entry: CANCEL -> commit when the editing surface leaves::touch (the on-screen-keyboard surface) leaving mid-edit ends the edit via commit (no wedge)"
    ],
    "shell": "grep -q \"interactionClasses\" src/env/environment.luau && grep -q \"derived interactionClasses\" tests/renderer.spec.luau"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "input-paradigms::hybrid-hotswitch",
   "name": "hybrid-hotswitch",
   "note": "hybrid-hotswitch: 5 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold; and artifacts/input-paradigms/affordance-matrix.md must exist. Requirements: UI-PARADIGM-002, UI-ENV-001.",
   "phase": "input-paradigms",
   "releaseBlocking": false,
   "requirements": [
    "UI-PARADIGM-002",
    "UI-ENV-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "receipt": "tools/lune/verify/evidence/input-paradigms--console-10foot.json",
    "resultIds": [
     "renderer::environment: per-key isolation::displaySize fact defaults to Medium and distanceProfile derives ten-foot from Large",
     "preview::Preview: device profiles::ships a console profile whose facts flip distanceProfile and interactionClasses together",
     "preview::Preview: device profiles::applying the console profile insets the solved tree by the overscan margins",
     "paradigm_tenfoot::UI-PARADIGM-003: ten-foot text scaling at the paint seam (matrix D.1)::a ten-foot display scales rendered text sizes by 1.5x",
     "paradigm_tenfoot::environment ten-foot derivations (matrix D.1/D.3)::sizeClass caps at regular on a ten-foot Large display even on a wide viewport",
     "paradigm_tenfoot::UI-PARADIGM-003: ten-foot focus strengthening (matrix D.4)::focus visuals differ between a Large display and a Medium display"
    ],
    "shell": "grep -q \"ViewportDisplaySize\" src/client/roblox_env.luau && grep -q \"console\" src/preview/device_profiles.luau"
   },
   "class": "result-ids",
   "evidence": "artifacts/input-paradigms/studio-drive.json",
   "id": "input-paradigms::console-10foot",
   "name": "console-10foot",
   "note": "console-10foot: 6 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold; and artifacts/input-paradigms/studio-drive.json must exist. Requirements: UI-PARADIGM-003, UI-ADAPT-001, UI-ENV-001.",
   "phase": "input-paradigms",
   "releaseBlocking": false,
   "requirements": [
    "UI-PARADIGM-003",
    "UI-ADAPT-001",
    "UI-ENV-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_registration_cli",
     "suite"
    ],
    "resultIds": [
     "chip::Chip control::Chip gamepad affordance: the gamepad structural idiom (focus/grab/A-B/ten-foot)",
     "chip::Chip control::Chip keyboard affordance: the keyboard structural idiom (focus ring/Navigate/Activate/Adjust)"
    ],
    "shell": "grep -q \"affordanceProofs\" tools/lune/scaffold.luau && grep -q \"paradigm\" docs/guide/07-input.md && grep -q \"Three load-bearing facts\" docs/extending/new-control.md && test -f src/controls/chip.luau",
    "stdoutPins": [
     {
      "flags": "-q",
      "pattern": "\"prove the paradigm axis\"",
      "producer": "check_registration_cli"
     }
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "input-paradigms::axes-future-proofing",
   "name": "axes-future-proofing",
   "note": "axes-future-proofing: 2 named suite cases must pass; check_registration_cli, suite must exit zero; 1 pin on a producer's own output must match; its file-existence and text assertions must hold; and artifacts/input-paradigms/dryrun-chip.md must exist. Requirements: UI-AGENT-001, UI-PARADIGM-001.",
   "phase": "input-paradigms",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001",
    "UI-PARADIGM-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/test.json",
   "id": "input-paradigms::library-suite-green",
   "name": "library-suite-green",
   "note": "library-suite-green: suite must exit zero; and artifacts/test.json must exist. Requirements: UI-AGENT-001.",
   "phase": "input-paradigms",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "cd ../../../games/RascalRally/code"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "input-paradigms::game-suite-green",
   "name": "game-suite-green",
   "note": "game-suite-green: rascalrally-suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "input-paradigms",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "receipt": "tools/lune/verify/evidence/input-paradigms--studio-drive.json"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/input-paradigms/studio-drive.json",
   "id": "input-paradigms::studio-drive",
   "name": "studio-drive",
   "note": "studio-drive: and artifacts/input-paradigms/studio-drive.json must exist. Requirements: UI-FID-001.",
   "phase": "input-paradigms",
   "releaseBlocking": false,
   "requirements": [
    "UI-FID-001"
   ],
   "state": null
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "input-paradigms::physical-device-confirmation",
   "name": "physical-device-confirmation",
   "note": "physical-device-confirmation: declared evidence, recorded rather than executed. Requirements: UI-INPUT-002.",
   "phase": "input-paradigms",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-002"
   ],
   "state": "FAIL_ENVIRONMENT"
  },
  {
   "check": {
    "receipt": "tools/lune/verify/evidence/native-substrate--feasibility-matrix.json"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/native-substrate/feasibility/m1-scroll-host.json",
   "id": "native-substrate::feasibility-matrix",
   "name": "feasibility-matrix",
   "note": "feasibility-matrix: and artifacts/native-substrate/feasibility/m1-scroll-host.json must exist. Requirements: UI-NATIVE-001, UI-NATIVE-002, UI-NATIVE-003, UI-NATIVE-004, UI-NATIVE-005, UI-NATIVE-006, UI-NATIVE-007, UI-NATIVE-008.",
   "phase": "native-substrate",
   "releaseBlocking": false,
   "requirements": [
    "UI-NATIVE-001",
    "UI-NATIVE-002",
    "UI-NATIVE-003",
    "UI-NATIVE-004",
    "UI-NATIVE-005",
    "UI-NATIVE-006",
    "UI-NATIVE-007",
    "UI-NATIVE-008"
   ],
   "state": null
  },
  {
   "check": {
    "shell": "test -f examples/gallery/scenarios/init.luau && test -f examples/gallery/scenarios/runner.luau && test -f examples/gallery/scenarios/probe.luau && test -f tools/lune/studio_sync.luau && test -f tools/studio/inject.luau && grep -q \"Facet_Scenario\" examples/gallery/client/init.client.luau"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/native-substrate/verification-surface-probe.json",
   "id": "native-substrate::verification-surface",
   "name": "verification-surface",
   "note": "verification-surface: its file-existence and text assertions must hold; and artifacts/native-substrate/verification-surface-probe.json must exist. Requirements: UI-AGENT-001.",
   "phase": "native-substrate",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "renderer::safe areas: deviceSafeContent root policy (native-substrate NS-A1)::deviceSafeContent insets the content rect by the per-edge MAX of core and device insets",
     "preview::Preview: notched device-safe profile (native-substrate NS-A1)::phoneNotchLandscape pushes deviceSafeInsets and profile switch resets them"
    ],
    "shell": "grep -q \"GetInsetArea\" src/client/roblox_env.luau && grep -q \"deviceSafeInsets\" src/client/roblox_env.luau && grep -q \"deviceSafeContent\" src/render/renderer.luau && grep -q \"phoneNotchLandscape\" src/preview/device_profiles.luau && grep -q \"deviceSafeContent insets the content rect\" tests/renderer.spec.luau"
   },
   "class": "result-ids",
   "evidence": "artifacts/native-substrate/a1-safe-area.json",
   "id": "native-substrate::safe-area-adoption",
   "name": "safe-area-adoption",
   "note": "safe-area-adoption: 2 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold; and artifacts/native-substrate/a1-safe-area.json must exist. Requirements: UI-NATIVE-005, UI-ENV-001.",
   "phase": "native-substrate",
   "releaseBlocking": false,
   "requirements": [
    "UI-NATIVE-005",
    "UI-ENV-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "native_scroll::native scroll host (NS-A2)::a ScrollView declares itself a clip host and sends its solved content extent as the scroll region",
     "native_scroll::native scroll host (NS-A2)::FALLBACK: an adapter without the scroll seam mounts, clips, and lays out identically (no crash)"
    ],
    "shell": "grep -q \"ScrollView = \\\"ScrollingFrame\\\"\" src/client/screen_vocabulary.luau && grep -q \"setScrollRegion\" src/client/screen_target.luau && grep -q \"observeScroll\" src/client/screen_target.luau && grep -q \"forceScrollFallback\" src/client/billboard_target.luau && grep -q \"setScrollRegion\" src/render/target_contract.luau && grep -q \"scrollTo\" src/render/renderer.luau"
   },
   "class": "result-ids",
   "evidence": "artifacts/native-substrate/a2-scroll-host.json",
   "id": "native-substrate::scroll-host-adoption",
   "name": "scroll-host-adoption",
   "note": "scroll-host-adoption: 2 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold; and artifacts/native-substrate/a2-scroll-host.json must exist. Requirements: UI-NATIVE-001.",
   "phase": "native-substrate",
   "releaseBlocking": false,
   "requirements": [
    "UI-NATIVE-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "shell": "grep -q \"bindNativeScroll\" src/controls/virtual_list.luau && grep -q \"bindNativeScroll\" src/controls/table.luau && grep -q \"bindNativeScroll\" examples/gallery/client/init.client.luau && ! grep -q \"onScrollWheel\" src/controls/virtual_list.luau"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/native-substrate/a4-virtuallist.json",
   "id": "native-substrate::table-virtuallist-migration",
   "name": "table-virtuallist-migration",
   "note": "table-virtuallist-migration: suite must exit zero; its file-existence and text assertions must hold; and artifacts/native-substrate/a4-virtuallist.json must exist. Requirements: UI-NATIVE-001.",
   "phase": "native-substrate",
   "releaseBlocking": false,
   "requirements": [
    "UI-NATIVE-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "shell": "test -f src/input/drag_session.luau && test -f src/input/touch_gestures.luau && grep -q \"setDragDetector\" src/client/screen_target.luau && grep -q \"setTouchGestureHandlers\" src/client/screen_target.luau"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/native-substrate/a5-drag.json",
   "id": "native-substrate::drag-touch-adoption",
   "name": "drag-touch-adoption",
   "note": "drag-touch-adoption: suite must exit zero; its file-existence and text assertions must hold; and artifacts/native-substrate/a5-drag.json must exist. Requirements: UI-NATIVE-002, UI-NATIVE-003.",
   "phase": "native-substrate",
   "releaseBlocking": false,
   "requirements": [
    "UI-NATIVE-002",
    "UI-NATIVE-003"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "path::path_shapes: deterministic arc math (NS-A7)::a full ring closes: 9 points, endpoints coincide, all on the radius",
     "path::UI.Path: paint-only reactive stroke (NS-A7)::mounts as a leaf; a points change is one prop write and never a re-solve"
    ],
    "shell": "test -f src/controls/path_shapes.luau && grep -q \"class(\\\"Path\\\"\" src/blueprint_schema.luau && grep -q \"blueprint.Path\" src/blueprint.luau && grep -q \"applyPathPoints\" src/client/screen_target.luau && grep -q \"pathShapes\" docs/reference/api.md"
   },
   "class": "result-ids",
   "evidence": "artifacts/native-substrate/a7-path.json",
   "id": "native-substrate::path2d-pagelayout",
   "name": "path2d-pagelayout",
   "note": "path2d-pagelayout: 2 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold; and artifacts/native-substrate/a7-path.json must exist. Requirements: UI-NATIVE-004.",
   "phase": "native-substrate",
   "releaseBlocking": false,
   "requirements": [
    "UI-NATIVE-004"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "renderer::preferred text applied exactly once (native-substrate NS-A9)::the player preference scales MEASUREMENT (reserved bounds) but never the painted TextSize",
     "text_calibration::text_metrics calibration (NS-A10)::a calibrated fraction tightens the estimate and never widens it"
    ],
    "shell": "grep -q \"typographyPaintScale\" src/env/environment.luau && grep -q \"typographyPaintScale\" src/render/renderer.luau && grep -q \"calibrate\" src/layout/text_metrics.luau && test -f src/client/text_calibration.luau"
   },
   "class": "result-ids",
   "evidence": "artifacts/native-substrate/a9-preferred-text.json",
   "id": "native-substrate::preferred-text-single-application",
   "name": "preferred-text-single-application",
   "note": "preferred-text-single-application: 2 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold; and artifacts/native-substrate/a9-preferred-text.json must exist. Requirements: UI-NATIVE-006, UI-FID-001.",
   "phase": "native-substrate",
   "releaseBlocking": false,
   "requirements": [
    "UI-NATIVE-006",
    "UI-FID-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "selection_bridge::engine-selection bridge (NS-A12)::default surfaces never touch engine selection (NS-A11)",
     "selection_bridge::engine-selection bridge (NS-A12)::an opted-in modal mirrors logical focus and clears on dismiss",
     "selection_bridge::engine-selection bridge (NS-A12)::present() IGNORES the opt (bridge is modal-only)"
    ],
    "shell": "grep -q \"engineSelectionBridge\" src/present/presenter.luau && grep -q \"setEngineSelection\" src/client/screen_target.luau && grep -q \"setEngineSelection\" src/render/target_contract.luau"
   },
   "class": "result-ids",
   "evidence": "artifacts/native-substrate/a12-bridge.json",
   "id": "native-substrate::selection-policy",
   "name": "selection-policy",
   "note": "selection-policy: 3 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold; and artifacts/native-substrate/a12-bridge.json must exist. Requirements: UI-NATIVE-007.",
   "phase": "native-substrate",
   "releaseBlocking": false,
   "requirements": [
    "UI-NATIVE-007"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "async_image::AsyncImage (NS-A14)::release-then-stale-completion never resurrects (honest cancellation)"
    ],
    "shell": "test -f src/client/roblox_resources.luau && test -f src/controls/async_image.luau && grep -q \"PreloadAsync\" src/client/roblox_resources.luau && grep -q \"newAsyncImage\" docs/reference/api.md"
   },
   "class": "result-ids",
   "evidence": "artifacts/native-substrate/a13-resources.json",
   "id": "native-substrate::resource-transport",
   "name": "resource-transport",
   "note": "resource-transport: 1 named suite case must pass; suite must exit zero; its file-existence and text assertions must hold; and artifacts/native-substrate/a13-resources.json must exist. Requirements: UI-NATIVE-008.",
   "phase": "native-substrate",
   "releaseBlocking": false,
   "requirements": [
    "UI-NATIVE-008"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "native_scroll::A-SV2: the keep-visible clamp exists exactly once::all three consumers call it — no second implementation survives",
     "native_scroll::A-SV2: the keep-visible clamp exists exactly once::the renderer's substrate and the index path agree on the same geometry",
     "native_scroll::A-SV2: the keep-visible clamp exists exactly once::returns nil rather than a no-op scroll"
    ],
    "shell": "grep -q \"function solver.keepVisibleOffset\" src/layout/solver.luau && grep -q \"keepVisibleOffset\" src/render/renderer.luau && grep -q \"keepVisibleOffset\" src/controls/virtual_list.luau && grep -q \"keepVisibleOffset\" src/controls/table.luau"
   },
   "class": "result-ids",
   "evidence": "artifacts/authoring-adaptive-ui/a-sv2-substrate-consolidation.json",
   "id": "native-substrate::keep-visible-consolidation",
   "name": "keep-visible-consolidation",
   "note": "keep-visible-consolidation: 3 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold; and artifacts/authoring-adaptive-ui/a-sv2-substrate-consolidation.json must exist. Requirements: UI-LAYOUT-001.",
   "phase": "native-substrate",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "layout_vocabulary::A-LV4: aspectRatio against a fill sibling::derives the height from the RESOLVED fill width, not from zero",
     "layout_vocabulary::A-LV4: overlay does not collapse its base::the wrapper inherits the base's dimensions",
     "layout_vocabulary::A-LV4: aspectRatio against a fill sibling::a grid cell derives its aspect from the derived column width"
    ],
    "shell": "grep -q \"Toolbar\" examples/gallery/scenarios/adaptive_controls.luau && grep -q \"aspectRatio\" examples/gallery/scenarios/adaptive_controls.luau && grep -q \"badgedTile\" examples/gallery/scenarios/adaptive_controls.luau"
   },
   "class": "result-ids",
   "evidence": "artifacts/authoring-adaptive-ui/a-al4-a-lv4-writeonce.json",
   "id": "native-substrate::write-once-and-recipes",
   "name": "write-once-and-recipes",
   "note": "write-once-and-recipes: 3 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold; and artifacts/authoring-adaptive-ui/a-al4-a-lv4-writeonce.json must exist. Requirements: UI-LAYOUT-001.",
   "phase": "native-substrate",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/test.json",
   "id": "native-substrate::headless-suite-preserved",
   "name": "headless-suite-preserved",
   "note": "headless-suite-preserved: suite must exit zero; and artifacts/test.json must exist. Requirements: UI-RUNTIME-001.",
   "phase": "native-substrate",
   "releaseBlocking": false,
   "requirements": [
    "UI-RUNTIME-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "cd ../../../games/RascalRally/code"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "native-substrate::game-suite-unchanged",
   "name": "game-suite-unchanged",
   "note": "game-suite-unchanged: rascalrally-suite must exit zero; its file-existence and text assertions must hold; and artifacts/native-substrate/game-suite.txt must exist. Requirements: UI-NATIVE-001.",
   "phase": "native-substrate",
   "releaseBlocking": false,
   "requirements": [
    "UI-NATIVE-001"
   ],
   "state": null
  },
  {
   "check": {
    "priorPhases": true
   },
   "class": "prior-phases",
   "evidence": null,
   "id": "native-substrate::prior-gates-unregressed",
   "name": "prior-gates-unregressed",
   "note": "prior-gates-unregressed: every row of every earlier phase must pass in this same run; and artifacts/native-substrate/prior-gates.txt must exist. Requirements: UI-AGENT-001.",
   "phase": "native-substrate",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/native-substrate/verifier-architecture.json",
   "id": "native-substrate::fresh-context-verification",
   "name": "fresh-context-verification",
   "note": "fresh-context-verification: suite must exit zero; and artifacts/native-substrate/verifier-architecture.json must exist. Requirements: UI-AGENT-001.",
   "phase": "native-substrate",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "native-substrate::physical-device-confirmation",
   "name": "physical-device-confirmation",
   "note": "physical-device-confirmation: and artifacts/native-substrate/review-packet.md must exist. Requirements: UI-NATIVE-007, UI-NATIVE-003.",
   "phase": "native-substrate",
   "releaseBlocking": false,
   "requirements": [
    "UI-NATIVE-007",
    "UI-NATIVE-003"
   ],
   "state": "FAIL_ENVIRONMENT"
  },
  {
   "check": {
    "receipt": "tools/lune/verify/evidence/native-stylesheets--spike-matrix.json"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/native-stylesheets/feasibility/m1-state-selectors.json",
   "id": "native-stylesheets::spike-matrix",
   "name": "spike-matrix",
   "note": "spike-matrix: and artifacts/native-stylesheets/feasibility/m1-state-selectors.json must exist. Requirements: UI-STYLE-001.",
   "phase": "native-stylesheets",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "sheet_model::sheet_model.build::Dark theme tokens equal the default style values exactly",
     "sheet_model::sheet_model.build::every theme passes the contrast gate (and a failing theme is rejected)",
     "sheet_model::sheet_model.build::orders state rules per the measured cascade (insertion order wins)",
     "sheet_model::sheet_model.build::gates hover rules on the pointer-live tag (touch can never match)",
     "sheet_model::sheet_model.build::classifyTags: the pure hint->tag mapping the native adapter applies"
    ],
    "shell": "test -f src/tokens/sheet_model.luau && test -f src/tokens/default_light_style.luau"
   },
   "class": "result-ids",
   "evidence": "tests/sheet_model.spec.luau",
   "id": "native-stylesheets::sheet-model-headless",
   "name": "sheet-model-headless",
   "note": "sheet-model-headless: 5 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold; and tests/sheet_model.spec.luau must exist. Requirements: UI-STYLE-001, UI-RUNTIME-001.",
   "phase": "native-stylesheets",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001",
    "UI-RUNTIME-001"
   ],
   "state": null
  },
  {
   "check": {
    "shell": "test -f src/client/native_style.luau && grep -q \"SEED-ONCE\" src/client/native_style.luau && grep -q \"FacetStyle\" src/client/native_style.luau && grep -q \"IsA(\\\"StyleSheet\\\")\" src/client/native_style.luau && grep -q \"SetDerives\" src/client/native_style.luau && grep -q \"SetPropertyTransitions\" src/client/native_style.luau"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/native-stylesheets/a9-layout-token-sync.json",
   "id": "native-stylesheets::seed-once-materializer",
   "name": "seed-once-materializer",
   "note": "seed-once-materializer: its file-existence and text assertions must hold; and artifacts/native-stylesheets/a9-layout-token-sync.json must exist. Requirements: UI-STYLE-001.",
   "phase": "native-stylesheets",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001"
   ],
   "state": null
  },
  {
   "check": {
    "shell": "grep -q \"nativeStyle\" src/client/screen_target.luau && grep -q \"syncTags\" src/client/screen_target.luau && grep -q \"classifyTags\" src/client/screen_target.luau && grep -q \"wireNativePressDip\" src/client/screen_target.luau && grep -q \"nativeSheetOwned\" src/render/authority.luau && grep -q \"setNativeTransitionsEnabled\" src/render/renderer.luau"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/native-stylesheets/a2-native-paint.json",
   "id": "native-stylesheets::adapter-native-mode",
   "name": "adapter-native-mode",
   "note": "adapter-native-mode: its file-existence and text assertions must hold; and artifacts/native-stylesheets/a2-native-paint.json must exist. Requirements: UI-STYLE-001, UI-PARADIGM-001.",
   "phase": "native-stylesheets",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001",
    "UI-PARADIGM-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "native_style_scenario::native_style scenario (headless conformance)::mounts through the real presenter and emits every classification input"
    ],
    "shell": "test -f examples/gallery/scenarios/native_style.luau && grep -q \"native_style\" examples/gallery/scenarios/init.luau && grep -q \"STYLED_PROPS\" examples/gallery/scenarios/runner.luau && grep -q \"mountIdentity\" examples/gallery/scenarios/runner.luau && grep -q \"editRuleProperty\" examples/gallery/scenarios/runner.luau && grep -q \"Facet_NativeStyle\" examples/gallery/client/init.client.luau"
   },
   "class": "result-ids",
   "evidence": "examples/gallery/scenarios/native_style.luau",
   "id": "native-stylesheets::scenario-surface-extended",
   "name": "scenario-surface-extended",
   "note": "scenario-surface-extended: 1 named suite case must pass; suite must exit zero; its file-existence and text assertions must hold; and examples/gallery/scenarios/native_style.luau must exist. Requirements: UI-AGENT-001.",
   "phase": "native-stylesheets",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "shell": "grep -q \"Native stylesheets (the default)\" docs/guide/05-styling.md && grep -q \"reference only\" docs/guide/05-styling.md && grep -q \"sheet_model\" src/tokens/tokens.luau && ! grep -q \"generation is deferred\" src/tokens/tokens.luau"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "native-stylesheets::docs-and-adr",
   "name": "docs-and-adr",
   "note": "docs-and-adr: its file-existence and text assertions must hold. Requirements: UI-STYLE-001.",
   "phase": "native-stylesheets",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/test.json",
   "id": "native-stylesheets::headless-suite-preserved",
   "name": "headless-suite-preserved",
   "note": "headless-suite-preserved: suite must exit zero; and artifacts/test.json must exist. Requirements: UI-RUNTIME-001.",
   "phase": "native-stylesheets",
   "releaseBlocking": false,
   "requirements": [
    "UI-RUNTIME-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "cd ../../../games/RascalRally/code"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "native-stylesheets::game-suite-unchanged",
   "name": "game-suite-unchanged",
   "note": "game-suite-unchanged: rascalrally-suite must exit zero; its file-existence and text assertions must hold; and artifacts/native-stylesheets/game-suite.txt must exist. Requirements: UI-STYLE-001.",
   "phase": "native-stylesheets",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001"
   ],
   "state": null
  },
  {
   "check": {
    "priorPhases": true
   },
   "class": "prior-phases",
   "evidence": null,
   "id": "native-stylesheets::prior-gates-unregressed",
   "name": "prior-gates-unregressed",
   "note": "prior-gates-unregressed: every row of every earlier phase must pass in this same run; and artifacts/native-stylesheets/prior-gates.txt must exist. Requirements: UI-AGENT-001.",
   "phase": "native-stylesheets",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "native-stylesheets::designer-and-device-confirmation",
   "name": "designer-and-device-confirmation",
   "note": "designer-and-device-confirmation: and artifacts/native-stylesheets/review-packet.md must exist. Requirements: UI-STYLE-001.",
   "phase": "native-stylesheets",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001"
   ],
   "state": "FAIL_ENVIRONMENT"
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "authoring::M0-A1: invalid authoring fails at construction::an unknown property names the control, the property, and the valid alternatives",
     "authoring::M0-A1: invalid authoring fails at construction::a misspelled property suggests the intended one",
     "authoring::M0-A1: invalid authoring fails at construction::a bare number where a dimension belongs explains the dimension table",
     "authoring::M0-A1: invalid authoring fails at construction::an unrecognised enum value is rejected with the closed value set",
     "authoring::M0-A1: invalid authoring fails at construction::children on a leaf are rejected and the containers are listed"
    ],
    "shell": "test -f src/blueprint_schema.luau && grep -q \"unknownPropError\" src/blueprint.luau"
   },
   "class": "result-ids",
   "evidence": "artifacts/authoring-adaptive-ui/m0-a1-unknown-props.json",
   "id": "authoring-adaptive-ui::strict-authoring",
   "name": "strict-authoring",
   "note": "strict-authoring: 5 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold; and artifacts/authoring-adaptive-ui/m0-a1-unknown-props.json must exist. Requirements: UI-AGENT-001.",
   "phase": "authoring-adaptive-ui",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_prop_parity_cli",
     "suite"
    ],
    "resultIds": [
     "authoring::M0-A3: one reconciled property model::the live repository passes the property-parity check",
     "authoring::M0-A3: one reconciled property model::it detects a repainting property the refresh loop cannot re-apply",
     "authoring::M0-A4: diagnosed properties instead of silent drops::a reactive style hint is re-applied instead of being dropped by the refresh loop",
     "authoring::M0-A4: diagnosed properties instead of silent drops::a reactive `active` flag is re-applied instead of being dropped"
    ],
    "shell": "grep -q \"schema.propDirty()\" src/blueprint.luau && grep -q \"STYLE_PROPS\" src/render/renderer.luau"
   },
   "class": "result-ids",
   "evidence": "artifacts/authoring-adaptive-ui/m0-a4-dead-props.json",
   "id": "authoring-adaptive-ui::one-property-model",
   "name": "one-property-model",
   "note": "one-property-model: 4 named suite cases must pass; check_prop_parity_cli, suite must exit zero; its file-existence and text assertions must hold; and artifacts/authoring-adaptive-ui/m0-a4-dead-props.json must exist. Requirements: UI-AGENT-001, UI-STYLE-001.",
   "phase": "authoring-adaptive-ui",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001",
    "UI-STYLE-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "authoring::M0-A5: the extension seam is genuinely public::an attached bundle rides the internal meta channel, never the public prop bag",
     "authoring::M0-A2: useful exported public types::no public constructor takes a bare untyped spec"
    ],
    "shell": "grep -q \"export type ButtonSpec\" src/blueprint.luau && grep -q \"contribution = require\" src/init.luau && grep -q \"Facet.contribution.attach\" docs/extending/new-control.md && grep -q \"check_prop_parity_cli\" docs/extending/new-control.md && grep -q \"inputContribution\" src/input/contribution.luau"
   },
   "class": "result-ids",
   "evidence": "artifacts/authoring-adaptive-ui/m0-a2-a7.json",
   "id": "authoring-adaptive-ui::public-types-and-seams",
   "name": "public-types-and-seams",
   "note": "public-types-and-seams: 2 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold; and artifacts/authoring-adaptive-ui/m0-a2-a7.json must exist. Requirements: UI-AGENT-001.",
   "phase": "authoring-adaptive-ui",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_registration_cli",
     "suite"
    ],
    "resultIds": [
     "authoring::M0-A6: every public constructor is visible to conformance::each UI.* constructor has a control contract and a registry row",
     "authoring::M0-A6: every public constructor is visible to conformance::Grip is registered as an INTERACTIVE control with a hit floor"
    ],
    "shell": "grep -q \"Grip = {\" src/class_contract.luau && grep -q \"ErrorBoundary = {\" src/class_contract.luau && grep -q \"Grip is registered\" tests/authoring.spec.luau"
   },
   "class": "result-ids",
   "evidence": "artifacts/authoring-adaptive-ui/m0-a2-a7.json",
   "id": "authoring-adaptive-ui::complete-registration",
   "name": "complete-registration",
   "note": "complete-registration: 2 named suite cases must pass; check_registration_cli, suite must exit zero; its file-existence and text assertions must hold; and artifacts/authoring-adaptive-ui/m0-a2-a7.json must exist. Requirements: UI-AGENT-001.",
   "phase": "authoring-adaptive-ui",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "layout_vocabulary::A-LV1: Grid children with fill dimensions::a fill-width cell occupies its whole column instead of collapsing to content",
     "layout_vocabulary::A-LV1: Spacer expands along the stack's main axis by default::a bare Spacer pushes its siblings apart in a VStack",
     "layout_vocabulary::A-LV1: Spacer expands along the stack's main axis by default::two bare Spacers share the leftover space equally (centring idiom)",
     "layout_vocabulary::A-LV1: Spacer expands along the stack's main axis by default::a Spacer outside a stack axis stays inert (no unbounded growth)"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/authoring-adaptive-ui/a-lv1-grid-spacer.json",
   "id": "authoring-adaptive-ui::grid-fill-and-spacer",
   "name": "grid-fill-and-spacer",
   "note": "grid-fill-and-spacer: 4 named suite cases must pass; suite must exit zero; and artifacts/authoring-adaptive-ui/a-lv1-grid-spacer.json must exist. Requirements: UI-LAYOUT-001.",
   "phase": "authoring-adaptive-ui",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "native_scroll::A-SV1: horizontal ScrollView::lays children out along x and advances by gap",
     "native_scroll::A-SV1: horizontal ScrollView::reports a horizontal canvas extent, not a vertical one",
     "native_scroll::A-SV1: scrollToVisible::scrolls the minimum distance to bring a node below the viewport into view",
     "native_scroll::A-SV1: scrollToVisible::finds the nearest scroll ancestor, not just a direct parent",
     "native_scroll::A-SV1: focus keep-visible through the shared substrate::moving focus past the visible band scrolls the host without any control wiring"
    ],
    "shell": "grep -q \"scrollToVisible\" src/render/renderer.luau && grep -q \"getScrollPosition\" src/client/screen_target.luau && grep -q \"scrollToVisible\" src/present/presenter.luau"
   },
   "class": "result-ids",
   "evidence": "artifacts/authoring-adaptive-ui/a-sv1-scrollview.json",
   "id": "authoring-adaptive-ui::scrollview-axes-and-keep-visible-headless",
   "name": "scrollview-axes-and-keep-visible-headless",
   "note": "scrollview-axes-and-keep-visible-headless: 5 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold; and artifacts/authoring-adaptive-ui/a-sv1-scrollview.json must exist. Requirements: UI-LAYOUT-001, UI-PARADIGM-001.",
   "phase": "authoring-adaptive-ui",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001",
    "UI-PARADIGM-001"
   ],
   "state": null
  },
  {
   "check": {
    "receipt": "tools/lune/verify/evidence/authoring-adaptive-ui--scrollview-live-geometry.json"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/authoring-adaptive-ui/a-sv1-scrollview.json",
   "id": "authoring-adaptive-ui::scrollview-live-geometry",
   "name": "scrollview-live-geometry",
   "note": "scrollview-live-geometry: and artifacts/authoring-adaptive-ui/a-sv1-scrollview.json must exist. Requirements: UI-LAYOUT-001.",
   "phase": "authoring-adaptive-ui",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "adaptive::A-AL3: pure adaptive decisions (no device names)::a ten-foot display never resolves the densest class, however wide it is",
     "adaptive::A-AL2: AdaptiveStack changes axis without rebuilding anything::flipping the bound axis re-solves WITHOUT a factory rerun or a remount",
     "adaptive::A-AL1: ViewThatFits::measures against the CONTAINER's space, not the viewport's",
     "adaptive::A-AL1: ViewThatFits::an unchosen candidate's focusables are removed from focus order"
    ],
    "shell": "test -f src/layout/adaptive.luau && grep -q \"adaptive = require\" src/init.luau && grep -q \"AdaptiveStack\" src/blueprint_schema.luau && grep -q \"ViewThatFits\" src/blueprint_schema.luau && grep -q \"chosenCandidate\" src/layout/solver.luau && grep -q \"filterHidden\" src/present/presenter.luau"
   },
   "class": "result-ids",
   "evidence": "artifacts/authoring-adaptive-ui/a-al2-adaptive.json",
   "id": "authoring-adaptive-ui::adaptive-layout",
   "name": "adaptive-layout",
   "note": "adaptive-layout: 4 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold; and artifacts/authoring-adaptive-ui/a-al2-adaptive.json must exist. Requirements: UI-LAYOUT-001.",
   "phase": "authoring-adaptive-ui",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "layout_vocabulary::A-LV2: padding, offset, aspectRatio, alignment::an unknown alignment value fails at construction",
     "layout_vocabulary::A-LV2: padding, offset, aspectRatio, alignment::a modifier writing a property the class does not have is rejected",
     "layout_vocabulary::A-LV2: modifier order is explicit::the LAST writer of a property wins",
     "adaptive::A-LV3: Divider is axis-aware::draws a vertical hairline in a horizontal stack",
     "adaptive::A-LV3: Divider is axis-aware::follows an AdaptiveStack's axis flip with no rebuild"
    ],
    "shell": "grep -q \"function blueprint.frame\" src/blueprint.luau && grep -q \"function blueprint.overlay\" src/blueprint.luau && grep -q \"function blueprint.aspectRatio\" src/blueprint.luau && grep -q \"facet-divider\" src/tokens/sheet_model.luau && grep -q \"nativeDivider\" src/client/screen_target.luau"
   },
   "class": "result-ids",
   "evidence": "artifacts/authoring-adaptive-ui/a-lv2-modifiers.json",
   "id": "authoring-adaptive-ui::layout-modifiers-and-divider",
   "name": "layout-modifiers-and-divider",
   "note": "layout-modifiers-and-divider: 5 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold; and artifacts/authoring-adaptive-ui/a-lv2-modifiers.json must exist. Requirements: UI-LAYOUT-001, UI-STYLE-001.",
   "phase": "authoring-adaptive-ui",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001",
    "UI-STYLE-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "button_complete::B-BTN1: Button supports custom content behind one activation surface::a focusable inside the content is rejected: one activation surface, one focus site",
     "button_complete::B-BTN1: Button supports custom content behind one activation surface::a content button exposes exactly ONE focusable and activates once",
     "button_complete::B-BTN3: the hit-target floor is enforced in solved/hit geometry::a sub-floor button reports an expanded hit rect",
     "button_complete::B-BTN3: the hit-target floor is enforced in solved/hit geometry::a button already at or above the floor gets NO expander",
     "button_complete::B-BTN4: disabled is consistent across focus, activation, appearance and semantics::a disabled button is skipped by focus navigation",
     "button_complete::B-BTN4: disabled is consistent across focus, activation, appearance and semantics::a disabled button does not activate on the keyboard or gamepad path either",
     "button_complete::B-BTN2: semantic roles ride tags and style rules::every theme's danger pair passes the contrast gate"
    ],
    "shell": "grep -q \"assertNoFocusableContent\" src/blueprint.luau && grep -q \"effectiveHitFloor\" src/render/renderer.luau && grep -q \"setHitRect\" src/client/screen_target.luau && grep -q \"isDisabled\" src/present/presenter.luau && grep -q \"Destructive button\" src/tokens/sheet_model.luau && grep -q \"dangerPair\" src/tokens/sheet_model.luau"
   },
   "class": "result-ids",
   "evidence": "artifacts/authoring-adaptive-ui/b-btn-button.json",
   "id": "authoring-adaptive-ui::button-complete",
   "name": "button-complete",
   "note": "button-complete: 7 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold; and artifacts/authoring-adaptive-ui/b-btn-button.json must exist. Requirements: UI-PARADIGM-001, UI-STYLE-001.",
   "phase": "authoring-adaptive-ui",
   "releaseBlocking": false,
   "requirements": [
    "UI-PARADIGM-001",
    "UI-STYLE-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "value_controls::B-VAL1: the shared value model::quantizes onto a grid measured FROM min, so the bounds are reachable",
     "value_controls::B-VAL1: the shared value model::stepping always makes progress and never leaves the range",
     "value_controls::B-VAL3: Stepper reaches the same arithmetic from every input class::keyboard: Adjust on the focused control steps without touching a button",
     "value_controls::B-VAL3: Stepper reaches the same arithmetic from every input class::no Adjust key is bound while focus sits outside the Stepper (no gameplay bumper shadow)",
     "value_controls::B-VAL3: Stepper bounds, formatting, disabled and lifecycle::the affordance at a bound is DISABLED, not silently inert",
     "value_controls::B-VAL3: Stepper bounds, formatting, disabled and lifecycle::an explicitly disabled Stepper refuses every input class"
    ],
    "shell": "test -f src/controls/value_model.luau && test -f src/controls/stepper.luau && grep -q \"newStepper\" src/init.luau && grep -q \"isEnabled\" src/controls/stepper.luau"
   },
   "class": "result-ids",
   "evidence": "artifacts/authoring-adaptive-ui/b-val-value-controls.json",
   "id": "authoring-adaptive-ui::value-controls",
   "name": "value-controls",
   "note": "value-controls: 6 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold; and artifacts/authoring-adaptive-ui/b-val-value-controls.json must exist. Requirements: UI-PARADIGM-001.",
   "phase": "authoring-adaptive-ui",
   "releaseBlocking": false,
   "requirements": [
    "UI-PARADIGM-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "display_controls::B-DSP1: ProgressView is determinate and style-owned::paint is style-owned through the BAR FAMILY, never a borrowed surface",
     "display_controls::B-DSP1: Label keeps a semantic text fallback::iconOnly DEGRADES to the title when there is no icon (an empty square is worse than a word)",
     "display_controls::B-DSP2: Picker presentation adapts from space, not device names::a live size-class change flips the presentation WITHOUT remounting the options",
     "display_controls::B-DSP2: DisclosureGroup updates focus correctly::collapsing with focus INSIDE the content moves focus back to the header first",
     "display_controls::B-DSP3: PopupButton rows meet the enforced target floor::the trigger and every open row report a hit rect at or above the floor",
     "display_controls::B-DSP3: PopupButton adapts its presentation::the pure rule prefers a sheet on touch, inline when short, menu otherwise",
     "display_controls::B-DSP3: PopupButton adapts its presentation::a sheet anchors to the bottom edge with visually larger rows",
     "display_controls::B-DSP3: PopupButton adapts its presentation::every presentation keeps selection, Cancel, and the tap-away machinery",
     "display_controls::B-DSP3: PopupButton adapts its presentation::a LIVE touch class flips it to a sheet with no rebuild of the trigger"
    ],
    "shell": "test -f src/controls/progress_view.luau && test -f src/controls/label.luau && test -f src/controls/picker.luau && test -f src/controls/disclosure_group.luau && grep -q \"resolvePresentation\" src/controls/picker.luau && grep -q \"focusOn\" src/controls/disclosure_group.luau && grep -q \"resolvePresentation\" src/controls/popup_button.luau"
   },
   "class": "result-ids",
   "evidence": "artifacts/authoring-adaptive-ui/b-dsp-display-controls.json",
   "id": "authoring-adaptive-ui::display-and-selection-controls",
   "name": "display-and-selection-controls",
   "note": "display-and-selection-controls: 9 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold; and artifacts/authoring-adaptive-ui/b-dsp-display-controls.json must exist. Requirements: UI-PARADIGM-001, UI-STYLE-001.",
   "phase": "authoring-adaptive-ui",
   "releaseBlocking": false,
   "requirements": [
    "UI-PARADIGM-001",
    "UI-STYLE-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/test.json",
   "id": "authoring-adaptive-ui::headless-suite-preserved",
   "name": "headless-suite-preserved",
   "note": "headless-suite-preserved: suite must exit zero; and artifacts/test.json must exist. Requirements: UI-RUNTIME-001.",
   "phase": "authoring-adaptive-ui",
   "releaseBlocking": false,
   "requirements": [
    "UI-RUNTIME-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "cd ../../../games/RascalRally/code"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "authoring-adaptive-ui::game-suite-unchanged",
   "name": "game-suite-unchanged",
   "note": "game-suite-unchanged: rascalrally-suite must exit zero; its file-existence and text assertions must hold; and artifacts/authoring-adaptive-ui/game-suite.txt must exist. Requirements: UI-AGENT-001.",
   "phase": "authoring-adaptive-ui",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "priorPhases": true
   },
   "class": "prior-phases",
   "evidence": null,
   "id": "authoring-adaptive-ui::prior-gates-unregressed",
   "name": "prior-gates-unregressed",
   "note": "prior-gates-unregressed: every row of every earlier phase must pass in this same run; and artifacts/authoring-adaptive-ui/prior-gates.txt must exist. Requirements: UI-AGENT-001.",
   "phase": "authoring-adaptive-ui",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "receipt": "tools/lune/verify/evidence/authoring-adaptive-ui--live-studio-matrix.json",
    "resultIds": [
     "adaptive::D-6: the matrix assertions reject a broken fixture::the hit-floor assertion fails when a control is pinned below the floor",
     "adaptive::D-6: the matrix assertions reject a broken fixture::the hidden-candidate assertion fails when a losing candidate still paints",
     "adaptive::D-6: the matrix assertions reject a broken fixture::the adaptation assertion fails when a screen hard-codes one axis"
    ],
    "shell": "test -f examples/gallery/scenarios/adaptive_controls.luau && grep -q \"adaptive_controls\" examples/gallery/scenarios/init.luau && grep -q \"setEnv\" examples/gallery/scenarios/runner.luau"
   },
   "class": "result-ids",
   "evidence": "artifacts/authoring-adaptive-ui/matrix/five-view-matrix.json",
   "id": "authoring-adaptive-ui::live-studio-matrix",
   "name": "live-studio-matrix",
   "note": "live-studio-matrix: 3 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold; and artifacts/authoring-adaptive-ui/matrix/five-view-matrix.json must exist. Requirements: UI-AGENT-001, UI-PARADIGM-001.",
   "phase": "authoring-adaptive-ui",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001",
    "UI-PARADIGM-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "renderer::preferred text applied exactly once (native-substrate NS-A9)::a preference BELOW 1 never reserves less than the painted size",
     "renderer::preferred text applied exactly once (native-substrate NS-A9)::the reservation floor composes with the ten-foot paint scale",
     "layout::layout: ScrollView::a stack that cannot hold its content reports it instead of painting off-screen",
     "layout::layout: ScrollView::a ViewThatFits candidate that lost does not report an overflow",
     "layout::layout: ScrollView::a stack that exactly fits reports no overflow"
    ],
    "shell": "grep -q \"diagnostics\" examples/gallery/scenarios/runner.luau"
   },
   "class": "result-ids",
   "evidence": "artifacts/authoring-adaptive-ui/a-al4-preferred-text-sweep.json",
   "id": "authoring-adaptive-ui::remaining-milestone-a-b-scope",
   "name": "remaining-milestone-a-b-scope",
   "note": "remaining-milestone-a-b-scope: 5 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold; and artifacts/authoring-adaptive-ui/a-al4-preferred-text-sweep.json must exist. Requirements: UI-LAYOUT-001, UI-PARADIGM-001.",
   "phase": "authoring-adaptive-ui",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001",
    "UI-PARADIGM-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_matrix_rows",
     "suite"
    ],
    "receipt": "tools/lune/verify/evidence/authoring-adaptive-ui--adaptive-fixture-fits-one-screen.json",
    "resultIds": [
     "layout::layout: ScrollView::a scroll canvas covers content laid out in fill columns that wrap",
     "layout::layout: ScrollView::an aspect box paired with fill is measured at the extent it will receive"
    ],
    "shell": "grep -q \"BodyScroll\" examples/gallery/scenarios/adaptive_controls.luau"
   },
   "class": "result-ids",
   "evidence": "artifacts/authoring-adaptive-ui/matrix/five-view-matrix.json",
   "id": "authoring-adaptive-ui::adaptive-fixture-fits-one-screen",
   "name": "adaptive-fixture-fits-one-screen",
   "note": "adaptive-fixture-fits-one-screen: 2 named suite cases must pass; check_matrix_rows, suite must exit zero; its file-existence and text assertions must hold; and artifacts/authoring-adaptive-ui/matrix/five-view-matrix.json must exist. Requirements: UI-LAYOUT-001.",
   "phase": "authoring-adaptive-ui",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "button_complete::verifier V12: a disabled control refuses the CONSUMER OVERRIDE too::opts.onActivate does not fire for a disabled node",
     "button_complete::verifier V10: structural remount forgets the per-path caches::a path re-created while still hidden is written hidden again",
     "button_complete::verifier V10: structural remount forgets the per-path caches::a sub-floor control re-created at the same path keeps its hit expander",
     "value_controls::verifier V11: the hit expander is re-based by its clip host::a sub-floor control inside a ScrollView gets an expander in HOST space, not window space",
     "value_controls::verifier V13: a disabled Slider leaves focus order::navigation skips a disabled Slider's track instead of only refusing the verb",
     "button_complete::B-BTN2: semantic roles ride tags and style rules::verifier V14: a role carries its OWN hover and press rules"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/authoring-adaptive-ui/verifier-phase-gate.json",
   "id": "authoring-adaptive-ui::fresh-context-verification",
   "name": "fresh-context-verification",
   "note": "fresh-context-verification: 6 named suite cases must pass; suite must exit zero; and artifacts/authoring-adaptive-ui/verifier-phase-gate.json must exist. Requirements: UI-AGENT-001.",
   "phase": "authoring-adaptive-ui",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "receipt": "tools/lune/verify/evidence/theme-packages-and-skinning--feasibility-probes.json"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/theme-packages-and-skinning/feasibility/m1-per-target-sheets.json",
   "id": "theme-packages-and-skinning::feasibility-probes",
   "name": "feasibility-probes",
   "note": "feasibility-probes: and artifacts/theme-packages-and-skinning/feasibility/m1-per-target-sheets.json must exist. Requirements: UI-STYLE-001, UI-FID-001.",
   "phase": "theme-packages-and-skinning",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001",
    "UI-FID-001"
   ],
   "state": null
  },
  {
   "check": {
    "shell": "test -f src/themes/package.luau && grep -q \"themes = {\" src/init.luau && grep -q \"checkCoverage\" src/init.luau && grep -q \"onDanger on danger\" src/themes/package.luau && grep -q \"theme_package.spec\" tests/run.luau"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/theme-packages-and-skinning/b-a1-package-contract.json",
   "id": "theme-packages-and-skinning::theme-package-contract",
   "name": "theme-package-contract",
   "note": "theme-package-contract: its file-existence and text assertions must hold; and artifacts/theme-packages-and-skinning/b-a1-package-contract.json must exist. Requirements: UI-STYLE-001, UI-AGENT-001.",
   "phase": "theme-packages-and-skinning",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001",
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_flat_baseline"
    ],
    "shell": "test -f src/themes/snapshot.luau && grep -q \"themeMetrics\" src/env/environment.luau && grep -q \"themeMetrics\" src/render/renderer.luau"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/theme-packages-and-skinning/b-a2-metric-snapshot.json",
   "id": "theme-packages-and-skinning::metric-snapshot-single-source",
   "name": "metric-snapshot-single-source",
   "note": "metric-snapshot-single-source: check_flat_baseline must exit zero; its file-existence and text assertions must hold; and artifacts/theme-packages-and-skinning/b-a2-metric-snapshot.json must exist. Requirements: UI-STYLE-001, UI-LAYOUT-001.",
   "phase": "theme-packages-and-skinning",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001",
    "UI-LAYOUT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_theme_drift_cli"
    ],
    "shell": "grep -q \"theme_drift.spec\" tests/run.luau && grep -q \"theme_roles.spec\" tests/run.luau"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/theme-packages-and-skinning/b-a3-controls-roles.json",
   "id": "theme-packages-and-skinning::controls-semantic-roles",
   "name": "controls-semantic-roles",
   "note": "controls-semantic-roles: check_theme_drift_cli must exit zero; its file-existence and text assertions must hold; and artifacts/theme-packages-and-skinning/b-a3-controls-roles.json must exist. Requirements: UI-STYLE-001, UI-LAYOUT-001.",
   "phase": "theme-packages-and-skinning",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001",
    "UI-LAYOUT-001"
   ],
   "state": null
  },
  {
   "check": {
    "shell": "test -f src/client/theme_controller.luau && grep -q \"themeRootGui\" src/client/screen_target.luau && grep -q \"theme_controller.spec\" tests/run.luau"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/theme-packages-and-skinning/b-a4-atomic-swap.json",
   "id": "theme-packages-and-skinning::reactive-controller-atomic-swap",
   "name": "reactive-controller-atomic-swap",
   "note": "reactive-controller-atomic-swap: its file-existence and text assertions must hold; and artifacts/theme-packages-and-skinning/b-a4-atomic-swap.json must exist. Requirements: UI-STYLE-001, UI-RUNTIME-001.",
   "phase": "theme-packages-and-skinning",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001",
    "UI-RUNTIME-001"
   ],
   "state": null
  },
  {
   "check": {
    "shell": "grep -q \"fontKey\" src/layout/measure_facts.luau && grep -q 'require(\"./measure_facts\")' src/layout/solver.luau && grep -q \"buildPackage\" src/client/theme_controller.luau"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/theme-packages-and-skinning/b-a5-font-measurement.json",
   "id": "theme-packages-and-skinning::font-aware-measurement",
   "name": "font-aware-measurement",
   "note": "font-aware-measurement: its file-existence and text assertions must hold; and artifacts/theme-packages-and-skinning/b-a5-font-measurement.json must exist. Requirements: UI-FID-001, UI-LAYOUT-001.",
   "phase": "theme-packages-and-skinning",
   "releaseBlocking": false,
   "requirements": [
    "UI-FID-001",
    "UI-LAYOUT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "theme_sync_cli-dump-artifacts-theme-packages-and-skinning-t-6f81a3"
    ],
    "shell": "test -f tools/lune/theme_sync_cli.luau && test -f src/themes/token_sync.luau"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/theme-packages-and-skinning/b-a6-editor-sync.json",
   "id": "theme-packages-and-skinning::style-editor-sync",
   "name": "style-editor-sync",
   "note": "style-editor-sync: theme_sync_cli-dump-artifacts-theme-packages-and-skinning-t-6f81a3 must exit zero; its file-existence and text assertions must hold; and artifacts/theme-packages-and-skinning/b-a6-editor-sync.json must exist. Requirements: UI-STYLE-001, UI-AGENT-001.",
   "phase": "theme-packages-and-skinning",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001",
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "shell": "grep -q \"buildPackage\" src/tokens/sheet_model.luau && grep -q \"FacetChromeText\" src/client/screen_chrome.luau && grep -q \"chromeArtJudgement\" src/client/screen_chrome.luau && grep -q \"judgeArt\" src/tokens/chrome_slots.luau && grep -q \"theme_chrome.spec\" tests/run.luau"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/theme-packages-and-skinning/b-a7-chrome-slots.json",
   "id": "theme-packages-and-skinning::chrome-recipes-and-slots",
   "name": "chrome-recipes-and-slots",
   "note": "chrome-recipes-and-slots: its file-existence and text assertions must hold; and artifacts/theme-packages-and-skinning/b-a7-chrome-slots.json must exist. Requirements: UI-STYLE-001.",
   "phase": "theme-packages-and-skinning",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001"
   ],
   "state": null
  },
  {
   "check": {
    "shell": "test -f examples/themes/fantasy_parchment.luau && grep -q \"neutralPackage\" examples/themes/fantasy_parchment.luau && ! grep -q 'require(\"%.%./%.%./src' examples/themes/fantasy_parchment.luau && test -f assets/themes/fantasy-parchment/provenance.md && test -f assets/themes/fantasy-parchment/upload-manifest.json"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/theme-packages-and-skinning/b-a9-fantasy-parchment.json",
   "id": "theme-packages-and-skinning::fantasy-parchment-public-build",
   "name": "fantasy-parchment-public-build",
   "note": "fantasy-parchment-public-build: its file-existence and text assertions must hold; and artifacts/theme-packages-and-skinning/b-a9-fantasy-parchment.json must exist. Requirements: UI-STYLE-001, UI-AGENT-001.",
   "phase": "theme-packages-and-skinning",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001",
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "shell": "for f in classic_desktop glossy_mobile scifi_hud fantasy_parchment fantasy_parchment_stub custom_control; do test -f \"examples/themes/$f.luau\" || exit 1; done && grep -q \"checkCoverage\" src/themes/package.luau && grep -q \"theme_reference_packages.spec\" tests/run.luau"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/theme-packages-and-skinning/b-a10-reference-families.json",
   "id": "theme-packages-and-skinning::reference-theme-families",
   "name": "reference-theme-families",
   "note": "reference-theme-families: its file-existence and text assertions must hold; and artifacts/theme-packages-and-skinning/b-a10-reference-families.json must exist. Requirements: UI-STYLE-001.",
   "phase": "theme-packages-and-skinning",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_docs_cli"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/theme-packages-and-skinning/b-a12-docs-gate.json",
   "id": "theme-packages-and-skinning::docs-and-check-docs-cli",
   "name": "docs-and-check-docs-cli",
   "note": "docs-and-check-docs-cli: check_docs_cli must exit zero; and artifacts/theme-packages-and-skinning/b-a12-docs-gate.json must exist. Requirements: UI-AGENT-001.",
   "phase": "theme-packages-and-skinning",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/test.json",
   "id": "theme-packages-and-skinning::library-suite-green",
   "name": "library-suite-green",
   "note": "library-suite-green: suite must exit zero; and artifacts/test.json must exist. Requirements: UI-AGENT-001.",
   "phase": "theme-packages-and-skinning",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "cd ../../../games/RascalRally/code"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "theme-packages-and-skinning::game-suite-untouched",
   "name": "game-suite-untouched",
   "note": "game-suite-untouched: rascalrally-suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "theme-packages-and-skinning",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "priorPhases": true
   },
   "class": "prior-phases",
   "evidence": null,
   "id": "theme-packages-and-skinning::prior-gates-unregressed",
   "name": "prior-gates-unregressed",
   "note": "prior-gates-unregressed: every row of every earlier phase must pass in this same run; and artifacts/theme-packages-and-skinning/prior-gates.txt must exist. Requirements: UI-AGENT-001.",
   "phase": "theme-packages-and-skinning",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "theme-packages-and-skinning::physical-and-human-rows",
   "name": "physical-and-human-rows",
   "note": "physical-and-human-rows: and artifacts/theme-packages-and-skinning/review-packet.md must exist. Requirements: UI-STYLE-001.",
   "phase": "theme-packages-and-skinning",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001"
   ],
   "state": "FAIL_ENVIRONMENT"
  },
  {
   "check": {
    "receipt": "tools/lune/verify/evidence/rich-skinning-v2--feasibility-probes.json"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/rich-skinning-v2/feasibility/rs-m1-tilesize.json",
   "id": "rich-skinning-v2::feasibility-probes",
   "name": "feasibility-probes",
   "note": "feasibility-probes: and artifacts/rich-skinning-v2/feasibility/rs-m1-tilesize.json must exist. Requirements: UI-STYLE-001, UI-FID-001.",
   "phase": "rich-skinning-v2",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001",
    "UI-FID-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_flat_baseline"
    ],
    "shell": "grep -q 'theme_layers.spec' tests/run.luau"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/rich-skinning-v2/rs-a2-layered-slots.json",
   "id": "rich-skinning-v2::layered-slots-and-posture",
   "name": "layered-slots-and-posture",
   "note": "layered-slots-and-posture: check_flat_baseline must exit zero; its file-existence and text assertions must hold; and artifacts/rich-skinning-v2/rs-a2-layered-slots.json must exist. Requirements: UI-STYLE-001.",
   "phase": "rich-skinning-v2",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "theme_variants::themes.normalizeVariant: one grammar for both rungs::unstated states fall back to default, which is what lets an author ship less art",
     "theme_variants::themes.normalizeVariant: one grammar for both rungs::rejects an unknown state key, naming the legal set",
     "theme_variants::themes.define: a recipe takes the same grammar::rejects a state naming art the package never declared, naming the state",
     "theme_variants::themes.normalizeVariant: a state change may never RELAYOUT the tree::accepts insets that are identical across every declared state",
     "theme_variants::themes.normalizeVariant: a state change may never RELAYOUT the tree::rejects a per-axis difference, naming the slot, the state and the axis"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/rich-skinning-v2/rs-a3-state-variants-rung1.json",
   "id": "rich-skinning-v2::state-variant-assets",
   "name": "state-variant-assets",
   "note": "state-variant-assets: 5 named suite cases must pass; suite must exit zero; and artifacts/rich-skinning-v2/rs-a3-state-variants-rung1.json must exist. Requirements: UI-STYLE-001.",
   "phase": "rich-skinning-v2",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "theme_value_displays::chrome_slots: the window clip, expressed as numbers::the art inside the window is the WHOLE track on the driven axis",
     "theme_value_displays::chrome_slots: the window clip, expressed as numbers::direction decides the axis and the pinned edge, and nothing else",
     "theme_value_displays::chrome_slots: the window clip, expressed as numbers::the centrepiece is dead centre on both axes, direction-independently",
     "theme_value_displays::themes.define: the bar family's declared geometry::an ImageRect crop is REFUSED, citing the probe that rejected it",
     "theme_value_displays::the bar's ornaments reserve the space they paint in::chrome_slots.barReservation derives the overhang from the declared boxes"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/rich-skinning-v2/rs-a5-image-bars.json",
   "id": "rich-skinning-v2::image-value-displays",
   "name": "image-value-displays",
   "note": "image-value-displays: 5 named suite cases must pass; suite must exit zero; and artifacts/rich-skinning-v2/rs-a5-image-bars.json must exist. Requirements: UI-STYLE-001.",
   "phase": "rich-skinning-v2",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "theme_icons::themes.define: the icon map is validated like every other section::rejects a name that is neither a framework icon nor namespaced",
     "theme_icons::themes.define: the icon map is validated like every other section::rejects an unknown iconSizes role and a non-positive size",
     "theme_icons::the fallback glyph table can never be tofu::a private-use codepoint FAILS the check — the sci-fi caret defect",
     "theme_icons_applied::iconSizes is metric authority, not a package-only field::every snapshot answers all three roles, authored or not",
     "theme_icons_applied::iconSizes is metric authority, not a package-only field::a METRIC package resizes icons — the whole reason it rides the snapshot"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/rich-skinning-v2/rs-a7-semantic-icons.json",
   "id": "rich-skinning-v2::semantic-icons",
   "name": "semantic-icons",
   "note": "semantic-icons: 5 named suite cases must pass; suite must exit zero; and artifacts/rich-skinning-v2/rs-a7-semantic-icons.json must exist. Requirements: UI-STYLE-001.",
   "phase": "rich-skinning-v2",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "theme_pixel::themes.define: the pixel declaration::rejects an unknown rendering mode, naming the modes",
     "theme_pixel::themes.define: the pixel declaration::rejects a pixel package with no unit, and a fractional or zero one",
     "theme_pixel::themes.define: the pixel declaration::locks SliceScale to integers, naming the slot that uses the art",
     "theme_pixel::buildPackage: the Pixelated stamp is a counted claim::every image-bearing rule carries it, and the census proves it",
     "theme_pixel_content::a pixel package's snapshot is an integer coordinate system::a NON-pixel package never enters the path — no key, no movement"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/rich-skinning-v2/rs-a8-pixel-mode.json",
   "id": "rich-skinning-v2::pixel-mode",
   "name": "pixel-mode",
   "note": "pixel-mode: 5 named suite cases must pass; suite must exit zero; and artifacts/rich-skinning-v2/rs-a8-pixel-mode.json must exist. Requirements: UI-STYLE-001, UI-FID-001.",
   "phase": "rich-skinning-v2",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001",
    "UI-FID-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "theme_pixel_content::content adoption: `contentId` is an alias, never a second path::both spellings compile to the same bytes — same assets, same STAMP",
     "theme_pixel_content::content adoption: `contentId` is an alias, never a second path::both emit the same rules, carrying the same plain strings",
     "theme_pixel_content::content adoption: `contentId` is an alias, never a second path::the compiled value is a plain STRING on one field, both ways round"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/rich-skinning-v2/rs-a9-content-adoption.json",
   "id": "rich-skinning-v2::content-adoption",
   "name": "content-adoption",
   "note": "content-adoption: 3 named suite cases must pass; suite must exit zero; and artifacts/rich-skinning-v2/rs-a9-content-adoption.json must exist. Requirements: UI-STYLE-001.",
   "phase": "rich-skinning-v2",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "theme_selectby::selectBy resolves at INSTALL from the facts already published::a pointer profile takes the pointer package",
     "theme_selectby::selectBy resolves at INSTALL from the facts already published::an UNMAPPED profile falls back to the positional default package",
     "theme_selectby::selectBy resolves at INSTALL from the facts already published::the vocabulary is CLOSED — a name outside the classes is an authoring error",
     "theme_selectby::a profile change swaps ONCE, after it settles::dock: touch -> pointer swaps the package and the metrics together",
     "theme_selectby::a profile change swaps ONCE, after it settles::a hybrid FLAP produces one swap per settlement, not one per flap"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/rich-skinning-v2/rs-a10-selectby.json",
   "id": "rich-skinning-v2::selectby-profile-selection",
   "name": "selectby-profile-selection",
   "note": "selectby-profile-selection: 5 named suite cases must pass; suite must exit zero; and artifacts/rich-skinning-v2/rs-a10-selectby.json must exist. Requirements: UI-STYLE-001, UI-INPUT-002.",
   "phase": "rich-skinning-v2",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001",
    "UI-INPUT-002"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "receipt": "tools/lune/verify/evidence/rich-skinning-v2--tile-decision-honest.json",
    "resultIds": [
     "theme_layer_application::chrome_slots.layerBoxes: rs-m3's idiom, expressed as numbers::a fill, a frame and a tile are one full-bleed child each"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/rich-skinning-v2/rs-a11-tile-decision.json",
   "id": "rich-skinning-v2::tile-decision-honest",
   "name": "tile-decision-honest",
   "note": "tile-decision-honest: 1 named suite case must pass; suite must exit zero; and artifacts/rich-skinning-v2/rs-a11-tile-decision.json must exist. Requirements: UI-STYLE-001.",
   "phase": "rich-skinning-v2",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "theme_reference_packages::the corpus covers all four theme classes::palette-only, metric-changing, font-changing and asset-backed are all represented",
     "theme_reference_packages::the corpus covers all four theme classes::METRIC-CHANGING moves the numbers a control resolves on every solve",
     "theme_reference_packages::the corpus covers all four theme classes::ASSET-BACKED declares content IDs only inside package data",
     "theme_reference_packages::the failure fixtures fail the way the scenario needs them to::an incompatible schema is REJECTED with the offending field and the fix"
    ],
    "shell": "test -f examples/themes/fantasy_ornate.luau && test -f assets/themes/fantasy-ornate/provenance.md"
   },
   "class": "result-ids",
   "evidence": "artifacts/rich-skinning-v2/rs-a12-fantasy-ornate.json",
   "id": "rich-skinning-v2::reference-packages",
   "name": "reference-packages",
   "note": "reference-packages: 4 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold; and artifacts/rich-skinning-v2/rs-a12-fantasy-ornate.json must exist. Requirements: UI-STYLE-001, UI-AGENT-001.",
   "phase": "rich-skinning-v2",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001",
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "theme_selectby::a profile change swaps ONCE, after it settles::dock: touch -> pointer swaps the package and the metrics together",
     "theme_selectby::a profile change swaps ONCE, after it settles::a hybrid FLAP produces one swap per settlement, not one per flap",
     "theme_selectby::a manual swapPackage wins until the next profile change::the manual flag clears on a settlement even when the package does not move"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/rich-skinning-v2/rs-a14-platform-pair.json",
   "id": "rich-skinning-v2::platform-pair",
   "name": "platform-pair",
   "note": "platform-pair: 3 named suite cases must pass; suite must exit zero; and artifacts/rich-skinning-v2/rs-a14-platform-pair.json must exist. Requirements: UI-STYLE-001, UI-INPUT-002.",
   "phase": "rich-skinning-v2",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001",
    "UI-INPUT-002"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_docs_cli"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/rich-skinning-v2/rs-a15-ladder-docs.json",
   "id": "rich-skinning-v2::customization-ladder-docs",
   "name": "customization-ladder-docs",
   "note": "customization-ladder-docs: check_docs_cli must exit zero; and artifacts/rich-skinning-v2/rs-a15-ladder-docs.json must exist. Requirements: UI-AGENT-001.",
   "phase": "rich-skinning-v2",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "theme_layer_application::chrome_slots.layerBoxes: rs-m3's idiom, expressed as numbers::layers and INSTANCES are different numbers, and both are published",
     "theme_layers::themes.define: a layered recipe compiles to inspectable layer data::a stack with no plaque, and every flat package, publishes NOTHING"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/rich-skinning-v2/rs-a17-cost.json",
   "id": "rich-skinning-v2::cost-honesty",
   "name": "cost-honesty",
   "note": "cost-honesty: 2 named suite cases must pass; suite must exit zero; and artifacts/rich-skinning-v2/rs-a17-cost.json must exist. Requirements: UI-STYLE-001.",
   "phase": "rich-skinning-v2",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_flat_baseline"
    ],
    "receipt": "tools/lune/verify/evidence/rich-skinning-v2--circle-button.json",
    "shell": "grep -q \"shapePrefix\" src/tokens/sheet_model.luau && grep -q \"Circle button — corner\" src/tokens/sheet_model.luau && grep -q \"Circle button — hairline\" src/tokens/sheet_model.luau && grep -q \"circleDiameter\" src/themes/snapshot.luau && grep -q \"button_shape.spec\" tests/run.luau && grep -q \"presentCircleFixture\" examples/gallery/scenarios/theme_authoring.luau"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/rich-skinning-v2/rs-circle.json",
   "id": "rich-skinning-v2::circle-button",
   "name": "circle-button",
   "note": "circle-button: check_flat_baseline must exit zero; its file-existence and text assertions must hold; and artifacts/rich-skinning-v2/rs-circle.json must exist. Requirements: UI-STYLE-001, UI-AGENT-001.",
   "phase": "rich-skinning-v2",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001",
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/test.json",
   "id": "rich-skinning-v2::library-suite-green",
   "name": "library-suite-green",
   "note": "library-suite-green: suite must exit zero; and artifacts/test.json must exist. Requirements: UI-AGENT-001.",
   "phase": "rich-skinning-v2",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "cd ../../../games/RascalRally/code"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "rich-skinning-v2::game-suite-untouched",
   "name": "game-suite-untouched",
   "note": "game-suite-untouched: rascalrally-suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "rich-skinning-v2",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "priorPhases": true
   },
   "class": "prior-phases",
   "evidence": null,
   "id": "rich-skinning-v2::prior-gates-unregressed",
   "name": "prior-gates-unregressed",
   "note": "prior-gates-unregressed: every row of every earlier phase must pass in this same run; and artifacts/rich-skinning-v2/prior-gates.txt must exist. Requirements: UI-AGENT-001.",
   "phase": "rich-skinning-v2",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "rich-skinning-v2::physical-and-human-rows",
   "name": "physical-and-human-rows",
   "note": "physical-and-human-rows: and artifacts/rich-skinning-v2/review-packet.md must exist. Requirements: UI-STYLE-001.",
   "phase": "rich-skinning-v2",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001"
   ],
   "state": "FAIL_ENVIRONMENT"
  },
  {
   "check": {
    "receipt": "tools/lune/verify/evidence/cross-platform-proof--console-tenfoot-profile.json",
    "shell": "grep -q \"consoleTenFoot\" bench/perf_profiles.luau"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/cross-platform-proof/rows/xp-a1-console-profile.json",
   "id": "cross-platform-proof::console-tenfoot-profile",
   "name": "console-tenfoot-profile",
   "note": "console-tenfoot-profile: its file-existence and text assertions must hold; and artifacts/cross-platform-proof/rows/xp-a1-console-profile.json must exist. Requirements: UI-PERF-002, UI-PARADIGM-003.",
   "phase": "cross-platform-proof",
   "releaseBlocking": false,
   "requirements": [
    "UI-PERF-002",
    "UI-PARADIGM-003"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_perf_scenes"
    ],
    "receipt": "tools/lune/verify/evidence/cross-platform-proof--production-shaped-scenes.json"
   },
   "class": "exit0",
   "evidence": "artifacts/cross-platform-proof/rows/xp-a2-production-scenes.json",
   "id": "cross-platform-proof::production-shaped-scenes",
   "name": "production-shaped-scenes",
   "note": "production-shaped-scenes: check_perf_scenes must exit zero; and artifacts/cross-platform-proof/rows/xp-a2-production-scenes.json must exist. Requirements: UI-PERF-001, UI-PERF-002.",
   "phase": "cross-platform-proof",
   "releaseBlocking": false,
   "requirements": [
    "UI-PERF-001",
    "UI-PERF-002"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_perf_scenes-themes"
    ],
    "receipt": "tools/lune/verify/evidence/cross-platform-proof--theme-swap-invalidation-scenes.json"
   },
   "class": "exit0",
   "evidence": "artifacts/cross-platform-proof/rows/xp-a3-theme-swap-scenes.json",
   "id": "cross-platform-proof::theme-swap-invalidation-scenes",
   "name": "theme-swap-invalidation-scenes",
   "note": "theme-swap-invalidation-scenes: check_perf_scenes-themes must exit zero; and artifacts/cross-platform-proof/rows/xp-a3-theme-swap-scenes.json must exist. Requirements: UI-PERF-002, UI-STYLE-001.",
   "phase": "cross-platform-proof",
   "releaseBlocking": false,
   "requirements": [
    "UI-PERF-002",
    "UI-STYLE-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_perf_metrics"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/phase-4/perf.json",
   "id": "cross-platform-proof::metric-set-complete",
   "name": "metric-set-complete",
   "note": "metric-set-complete: check_perf_metrics must exit zero; and artifacts/phase-4/perf.json must exist. Requirements: UI-PERF-002.",
   "phase": "cross-platform-proof",
   "releaseBlocking": false,
   "requirements": [
    "UI-PERF-002"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_perf_budgets"
    ]
   },
   "class": "exit0",
   "evidence": "bench/perf_budgets.json",
   "id": "cross-platform-proof::budgets-baseline-and-frame-target",
   "name": "budgets-baseline-and-frame-target",
   "note": "budgets-baseline-and-frame-target: check_perf_budgets must exit zero; and bench/perf_budgets.json must exist. Requirements: UI-PERF-002.",
   "phase": "cross-platform-proof",
   "releaseBlocking": false,
   "requirements": [
    "UI-PERF-002"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "prove_perf_gate"
    ],
    "receipt": "tools/lune/verify/evidence/cross-platform-proof--regression-gate-falsified.json"
   },
   "class": "exit0",
   "evidence": "artifacts/cross-platform-proof/rows/xp-a6-regression-proof.json",
   "id": "cross-platform-proof::regression-gate-falsified",
   "name": "regression-gate-falsified",
   "note": "regression-gate-falsified: prove_perf_gate must exit zero; and artifacts/cross-platform-proof/rows/xp-a6-regression-proof.json must exist. Requirements: UI-PERF-002, UI-AGENT-001.",
   "phase": "cross-platform-proof",
   "releaseBlocking": false,
   "requirements": [
    "UI-PERF-002",
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "receipt": "tools/lune/verify/evidence/cross-platform-proof--evidence-classes-separated.json",
    "resultIds": [
     "perf_evidence::device budgets are unreachable from the wrong class (UI-PERF-002)::refuses to enforce a device budget from a non-device class",
     "perf_evidence::evidence classes (UI-PERF-002)::every record carries exactly one evidence class, and Lune may only emit the headless one"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/cross-platform-proof/rows/xp-a7-evidence-classes.json",
   "id": "cross-platform-proof::evidence-classes-separated",
   "name": "evidence-classes-separated",
   "note": "evidence-classes-separated: 2 named suite cases must pass; suite must exit zero; and artifacts/cross-platform-proof/rows/xp-a7-evidence-classes.json must exist. Requirements: UI-PERF-002.",
   "phase": "cross-platform-proof",
   "releaseBlocking": false,
   "requirements": [
    "UI-PERF-002"
   ],
   "state": null
  },
  {
   "check": {
    "receipt": "tools/lune/verify/evidence/cross-platform-proof--matrix-driver-reusable.json",
    "shell": "test -f src/preview/matrix_rows.luau && test -f tools/studio/device_matrix.luau && grep -q \"matrix_rows.spec\" tests/run.luau && ! grep -qE \"\\bsamsung_|\\biphone_|\\bipad_|\\bhd_1080\\b|\\bxbox\\b\" src/preview/matrix_rows.luau && grep -q \"probeMethod\" tools/studio/device_matrix.luau && grep -q \"StopSimulationAsync\" tools/studio/device_matrix.luau"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/cross-platform-proof/matrix.json",
   "id": "cross-platform-proof::matrix-driver-reusable",
   "name": "matrix-driver-reusable",
   "note": "matrix-driver-reusable: its file-existence and text assertions must hold; and artifacts/cross-platform-proof/matrix.json must exist. Requirements: UI-FID-001, UI-ADAPT-001.",
   "phase": "cross-platform-proof",
   "releaseBlocking": false,
   "requirements": [
    "UI-FID-001",
    "UI-ADAPT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_xp_matrix"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/cross-platform-proof/matrix.json",
   "id": "cross-platform-proof::matrix-five-views-driven",
   "name": "matrix-five-views-driven",
   "note": "matrix-five-views-driven: check_xp_matrix must exit zero; and artifacts/cross-platform-proof/matrix.json must exist. Requirements: UI-FID-001.",
   "phase": "cross-platform-proof",
   "releaseBlocking": false,
   "requirements": [
    "UI-FID-001"
   ],
   "state": null
  },
  {
   "check": {
    "receipt": "tools/lune/verify/evidence/cross-platform-proof--matrix-intentional-failure.json"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/cross-platform-proof/rows/xp-b5-intentional-failure.json",
   "id": "cross-platform-proof::matrix-intentional-failure",
   "name": "matrix-intentional-failure",
   "note": "matrix-intentional-failure: and artifacts/cross-platform-proof/rows/xp-b5-intentional-failure.json must exist. Requirements: UI-FID-001, UI-AGENT-001.",
   "phase": "cross-platform-proof",
   "releaseBlocking": false,
   "requirements": [
    "UI-FID-001",
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_device_captures"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/cross-platform-proof/device/studio-emulated.json",
   "id": "cross-platform-proof::device-capture-path",
   "name": "device-capture-path",
   "note": "device-capture-path: check_device_captures must exit zero; and artifacts/cross-platform-proof/device/studio-emulated.json must exist. Requirements: UI-PERF-002, UI-FID-001.",
   "phase": "cross-platform-proof",
   "releaseBlocking": false,
   "requirements": [
    "UI-PERF-002",
    "UI-FID-001"
   ],
   "state": null
  },
  {
   "check": {
    "receipt": "tools/lune/verify/evidence/cross-platform-proof--spatial-seam-contracts.json",
    "shell": "test -f src/input/spatial.luau && grep -q \"spatial.spec\" tests/run.luau && grep -q \"presentationSpace\" src/env/environment.luau && grep -q \"FUTURE\" src/render/target_contract.luau"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/cross-platform-proof/rows/xp-d2-spatial-events.json",
   "id": "cross-platform-proof::spatial-seam-contracts",
   "name": "spatial-seam-contracts",
   "note": "spatial-seam-contracts: its file-existence and text assertions must hold; and artifacts/cross-platform-proof/rows/xp-d2-spatial-events.json must exist. Requirements: UI-SPATIAL-001.",
   "phase": "cross-platform-proof",
   "releaseBlocking": false,
   "requirements": [
    "UI-SPATIAL-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_docs_cli",
     "suite"
    ],
    "receipt": "tools/lune/verify/evidence/cross-platform-proof--no-vr-claim.json",
    "resultIds": [
     "spatial::no VR support claim (XP-D5)::no shipped document claims VR support"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/cross-platform-proof/rows/xp-d5-no-vr-claim.json",
   "id": "cross-platform-proof::no-vr-claim",
   "name": "no-vr-claim",
   "note": "no-vr-claim: 1 named suite case must pass; check_docs_cli, suite must exit zero; and artifacts/cross-platform-proof/rows/xp-d5-no-vr-claim.json must exist. Requirements: UI-SPATIAL-001, UI-AGENT-001.",
   "phase": "cross-platform-proof",
   "releaseBlocking": false,
   "requirements": [
    "UI-SPATIAL-001",
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/test.json",
   "id": "cross-platform-proof::library-suite-green",
   "name": "library-suite-green",
   "note": "library-suite-green: suite must exit zero; and artifacts/test.json must exist. Requirements: UI-AGENT-001.",
   "phase": "cross-platform-proof",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "receipt": "tools/lune/verify/evidence/cross-platform-proof--showcase-place.json",
    "shell": "test -f examples/gallery/client/demo_picker.luau && test -f examples/places/Facet-Showcase.rbxl && grep -q 'Facet-Showcase' tools/build_places.sh && grep -q 'FacetShowcaseAPI' examples/gallery/client/init.client.luau && grep -q 'other.bottom' examples/gallery/client/init.client.luau && grep -q 'The hands-on place' docs/guide/11-device-verification.md && grep -q 'scopedCore' examples/gallery/client/init.client.luau"
   },
   "class": "evidence-pin",
   "evidence": "examples/places/Facet-Showcase.rbxl",
   "id": "cross-platform-proof::showcase-place",
   "name": "showcase-place",
   "note": "showcase-place: its file-existence and text assertions must hold; and examples/places/Facet-Showcase.rbxl must exist. Requirements: UI-AGENT-001.",
   "phase": "cross-platform-proof",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "cd ../../../games/RascalRally/code"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "cross-platform-proof::game-untouched",
   "name": "game-untouched",
   "note": "game-untouched: rascalrally-suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "cross-platform-proof",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "priorPhases": true
   },
   "class": "prior-phases",
   "evidence": null,
   "id": "cross-platform-proof::prior-gates-unregressed",
   "name": "prior-gates-unregressed",
   "note": "prior-gates-unregressed: every row of every earlier phase must pass in this same run; and artifacts/cross-platform-proof/prior-gates.txt must exist. Requirements: UI-AGENT-001.",
   "phase": "cross-platform-proof",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_docs_cli",
     "check_registration_cli"
    ],
    "shell": "test -f docs/guide/11-device-verification.md && grep -q '11-device-verification' docs/guide/README.md && grep -q 'StudioTestService' docs/guide/11-device-verification.md && grep -q 'do not claim per-client' docs/guide/11-device-verification.md && grep -q 'StopSimulationAsync' docs/guide/11-device-verification.md"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "cross-platform-proof::docs-and-registration",
   "name": "docs-and-registration",
   "note": "docs-and-registration: check_docs_cli, check_registration_cli must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "cross-platform-proof",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "cross-platform-proof::physical-and-human-rows",
   "name": "physical-and-human-rows",
   "note": "physical-and-human-rows: and artifacts/cross-platform-proof/review-packet.md must exist. Requirements: UI-PERF-002.",
   "phase": "cross-platform-proof",
   "releaseBlocking": false,
   "requirements": [
    "UI-PERF-002"
   ],
   "state": "FAIL_ENVIRONMENT"
  },
  {
   "check": {
    "producers": [
     "suite",
     "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-m1-c62ec7",
     "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-m2-5a0768",
     "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-m7-c31a70"
    ],
    "resultIds": [
     "motion_spring::spring solver (semi-implicit Euler)::setTarget touches neither value nor velocity (the interruptibility invariant)",
     "motion_spring::spring solver (semi-implicit Euler)::a seeded release velocity produces a continuous first frame (no seam)",
     "motion_chase::motion chase: perceptual arrival::chases a target that moves mid-flight and lands on the MOVED one",
     "motion_chase::motion chase: perceptual arrival::fires on the radius frame, well before the settle epsilon would",
     "motion_clock::motion clock: stepping and the active set::commits every value in ONE transaction per stepped frame"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/sponsor-framework-gaps/rows/sf-m1.json",
   "id": "sponsor-framework-gaps::motion-authority",
   "name": "motion-authority",
   "note": "motion-authority: 5 named suite cases must pass; suite, check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-m1-c62ec7, check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-m2-5a0768, check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-m7-c31a70 must exit zero; and artifacts/sponsor-framework-gaps/rows/sf-m1.json must exist. Requirements: UI-MOTION-001, UI-STYLE-001.",
   "phase": "sponsor-framework-gaps",
   "releaseBlocking": false,
   "requirements": [
    "UI-MOTION-001",
    "UI-STYLE-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite",
     "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-m4-9a0448",
     "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-m6-a9c408"
    ],
    "resultIds": [
     "motion_timeline::motion timeline: interrupt and skip::interrupt at EVERY beat boundary leaves the declared end state, in order",
     "sponsor_scenarios::sponsor_celebration (SF-M4)::an interrupt MID-beat lands every remaining terminal and nothing persists half-painted",
     "transitions::mount retire model: When::the branch scope disposes exactly once, at done — never at retire, never twice",
     "transitions::transition coordinator: lifetime::a re-entry mid-exit reverses from the value it had, without a jump"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/sponsor-framework-gaps/rows/sf-m4.json",
   "id": "sponsor-framework-gaps::choreography-and-transitions",
   "name": "choreography-and-transitions",
   "note": "choreography-and-transitions: 4 named suite cases must pass; suite, check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-m4-9a0448, check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-m6-a9c408 must exit zero; and artifacts/sponsor-framework-gaps/rows/sf-m4.json must exist. Requirements: UI-MOTION-002.",
   "phase": "sponsor-framework-gaps",
   "releaseBlocking": false,
   "requirements": [
    "UI-MOTION-002"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite",
     "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-m5-8cdae7",
     "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-t3-b16b62"
    ],
    "resultIds": [
     "motion_chase::motion chase: perceptual arrival::under reduced motion the flight is instant and the arrival still fires",
     "motion_clock::motion clock: reduced motion (information parity)::an informational value KEEPS RUNNING under RM, quantized to 250 ms writes",
     "sponsor_scenarios::sponsor_motion (SF-M1/M2/M3/M5/M7/M8)::under reduced motion the SAME events fire and the terminal states match (SF-M5)",
     "sponsor_scenarios::sponsor_toast (SF-T1/T2/T3/M6)::under reduced motion the same toasts appear for the same durations (SF-T3)"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/sponsor-framework-gaps/rows/sf-m5.json",
   "id": "sponsor-framework-gaps::reduced-motion-parity",
   "name": "reduced-motion-parity",
   "note": "reduced-motion-parity: 4 named suite cases must pass; suite, check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-m5-8cdae7, check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-t3-b16b62 must exit zero; and artifacts/sponsor-framework-gaps/rows/sf-m5.json must exist. Requirements: UI-MOTION-003.",
   "phase": "sponsor-framework-gaps",
   "releaseBlocking": false,
   "requirements": [
    "UI-MOTION-003"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite",
     "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-l1-edbdee",
     "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-l3-fed491"
    ],
    "resultIds": [
     "sponsor_scenarios::sponsor_list (SF-L1/L3/P1/P3)::selection is single and survives a live re-sort (SF-L1)",
     "sponsor_scenarios::sponsor_list (SF-L1/L3/P1/P3)::churn never remounts a row: one cell factory run per key (SF-L1's no-blink)",
     "sponsor_scenarios::sponsor_list (SF-L1/L3/P1/P3)::navigation SKIPS ineligible rows and never lands on one (SF-L3)",
     "sponsor_scenarios::sponsor_list (SF-L1/L3/P1/P3)::the armed row keeps its focusability while the interaction lives (SF-L3's exemption)",
     "collection_list::unified collection: rows as drop targets (SF-L1)::a foreign payload lands on the row the game asked for"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/sponsor-framework-gaps/rows/sf-l1.json",
   "id": "sponsor-framework-gaps::unified-collection",
   "name": "unified-collection",
   "note": "unified-collection: 5 named suite cases must pass; suite, check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-l1-edbdee, check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-l3-fed491 must exit zero; and artifacts/sponsor-framework-gaps/rows/sf-l1.json must exist. Requirements: UI-COLLECT-001.",
   "phase": "sponsor-framework-gaps",
   "releaseBlocking": false,
   "requirements": [
    "UI-COLLECT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite",
     "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-l2-dcc9ef"
    ],
    "resultIds": [
     "collection_list::unified collection: edge autoscroll wired to the host (SF-L2)::dwelling at the bottom edge arms, scrolls, and re-resolves the verdict IN THE SAME frame",
     "autoscroll::autoscroll: the ends of the world::short content is INERT: bands never arm when there is nothing to scroll",
     "autoscroll::autoscroll: the ends of the world::clamps at the canvas end and STAYS armed (no re-paying the dwell)",
     "sponsor_scenarios::sponsor_drop (SF-D1..D5, SF-L2, SF-P4, SF-M2)::edge autoscroll dwells, ramps, clamps, and keeps the verdict fresh EVERY frame (SF-L2)"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/sponsor-framework-gaps/rows/sf-l2.json",
   "id": "sponsor-framework-gaps::edge-autoscroll",
   "name": "edge-autoscroll",
   "note": "edge-autoscroll: 4 named suite cases must pass; suite, check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-l2-dcc9ef must exit zero; and artifacts/sponsor-framework-gaps/rows/sf-l2.json must exist. Requirements: UI-DRAG-003.",
   "phase": "sponsor-framework-gaps",
   "releaseBlocking": false,
   "requirements": [
    "UI-DRAG-003"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite",
     "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-d1-1c5ceb",
     "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-d4-9a25f3",
     "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-d5-329c3e"
    ],
    "resultIds": [
     "drag_public::public drag: acquisition and promotion (SF-D1, SF-D3)::mounts the declarations: the card acquires a detector, the slots register",
     "drag_public::public drag: the non-pointer paradigm (SF-D4)::arm -> navigate -> commit drives the SAME session and the SAME legality",
     "sponsor_scenarios::sponsor_drop (SF-D1..D5, SF-L2, SF-P4, SF-M2)::the non-pointer path drives ONE session to both terminals (SF-D4)",
     "sponsor_scenarios::sponsor_drop (SF-D1..D5, SF-L2, SF-P4, SF-M2)::an illegal drop rejects EXACTLY once, with the fixture's reason code (SF-D5)"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/sponsor-framework-gaps/rows/sf-d1.json",
   "id": "sponsor-framework-gaps::drag-public-contract",
   "name": "drag-public-contract",
   "note": "drag-public-contract: 4 named suite cases must pass; suite, check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-d1-1c5ceb, check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-d4-9a25f3, check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-d5-329c3e must exit zero; and artifacts/sponsor-framework-gaps/rows/sf-d1.json must exist. Requirements: UI-DRAG-001.",
   "phase": "sponsor-framework-gaps",
   "releaseBlocking": false,
   "requirements": [
    "UI-DRAG-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite",
     "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-d3-46e41d"
    ],
    "resultIds": [
     "interaction_tokens::drag velocity: the rolling release window (SF-D2)::answers zero until it has two samples",
     "interaction_tokens::drag velocity: the rolling release window (SF-D2)::estimates first-vs-last across the retained window",
     "interaction_tokens::drag velocity: the rolling release window (SF-D2)::drops samples older than the window: a flick after a pause reads as a FLICK",
     "interaction_tokens::interaction tokens: promotion thresholds (SF-D3)::promotion is a MAGNITUDE test, not two axis tests",
     "interaction_tokens::interaction tokens: promotion thresholds (SF-D3)::the same travel is a drag on a mouse and still a tap on a finger",
     "sponsor_scenarios::sponsor_drop (SF-D1..D5, SF-L2, SF-P4, SF-M2)::a release under the promotion gate starts NO session (SF-D3)"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/sponsor-framework-gaps/rows/sf-d3.json",
   "id": "sponsor-framework-gaps::velocity-and-promotion",
   "name": "velocity-and-promotion",
   "note": "velocity-and-promotion: 6 named suite cases must pass; suite, check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-d3-46e41d must exit zero; and artifacts/sponsor-framework-gaps/rows/sf-d3.json must exist. Requirements: UI-DRAG-002.",
   "phase": "sponsor-framework-gaps",
   "releaseBlocking": false,
   "requirements": [
    "UI-DRAG-002"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite",
     "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-p1-71afb8",
     "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-p2-a5c718",
     "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-p3-1a56dc",
     "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-p4-d3903b",
     "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-p5-f34f1a"
    ],
    "resultIds": [
     "sponsor_scenarios::sponsor_list (SF-L1/L3/P1/P3)::the wash is a role BLEND on its own node, and the card keeps its state fill (SF-P1)",
     "sponsor_scenarios::sponsor_avatars (SF-A1..A4, SF-P2)::the dim treatment rides the authored tint channel, both ends themable (SF-P2)",
     "sponsor_scenarios::sponsor_list (SF-L1/L3/P1/P3)::the stroke pulse rides ONE bound prop and leaves nothing behind (SF-P3)",
     "sponsor_scenarios::sponsor_markers (SF-P5)::marker #12 carries the DECLARED theming-exempt hue, and nothing else does"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/sponsor-framework-gaps/rows/sf-p1.json",
   "id": "sponsor-framework-gaps::paint-extensions",
   "name": "paint-extensions",
   "note": "paint-extensions: 4 named suite cases must pass; suite, check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-p1-71afb8, check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-p2-a5c718, check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-p3-1a56dc, check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-p4-d3903b, check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-p5-f34f1a must exit zero; and artifacts/sponsor-framework-gaps/rows/sf-p1.json must exist. Requirements: UI-PAINT-001, UI-STYLE-001.",
   "phase": "sponsor-framework-gaps",
   "releaseBlocking": false,
   "requirements": [
    "UI-PAINT-001",
    "UI-STYLE-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite",
     "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-t1-006c64",
     "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-t2-1fee50"
    ],
    "resultIds": [
     "sponsor_scenarios::sponsor_toast (SF-T1/T2/T3/M6)::a toast never joins the stack and never moves focus (SF-T1)",
     "sponsor_scenarios::sponsor_toast (SF-T1/T2/T3/M6)::the control beneath a live toast still activates (SF-T1's whole point)",
     "toast_presentation::toast presentation: the layer is input-transparent::no toast node is focusable, so no navigation can ever reach one",
     "sponsor_scenarios::sponsor_toast (SF-T1/T2/T3/M6)::a burst shows three, queues the rest, and drops at the cap WITH a reason (SF-T2)",
     "sponsor_scenarios::sponsor_toast (SF-T1/T2/T3/M6)::priority never truncates a showing toast's read floor (finding F2)"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/sponsor-framework-gaps/rows/sf-t1.json",
   "id": "sponsor-framework-gaps::toast-presentation",
   "name": "toast-presentation",
   "note": "toast-presentation: 5 named suite cases must pass; suite, check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-t1-006c64, check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-t2-1fee50 must exit zero; and artifacts/sponsor-framework-gaps/rows/sf-t1.json must exist. Requirements: UI-PRESENT-001.",
   "phase": "sponsor-framework-gaps",
   "releaseBlocking": false,
   "requirements": [
    "UI-PRESENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite",
     "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-a1-b7f5d0",
     "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-a4-478080"
    ],
    "resultIds": [
     "sponsor_scenarios::sponsor_avatars (SF-A1..A4, SF-P2)::a staggered load ends with every avatar ready",
     "sponsor_scenarios::sponsor_avatars (SF-A1..A4, SF-P2)::failure keeps the placeholder and reports `failed` for instrumentation only (SF-A1)",
     "sponsor_scenarios::sponsor_avatars (SF-A1..A4, SF-P2)::retry spends its budget a second apart and then gives up for the session (SF-A2)",
     "sponsor_scenarios::sponsor_avatars (SF-A1..A4, SF-P2)::preload warms a declared set so a debuting composite skips the placeholder (SF-A3)",
     "sponsor_scenarios::sponsor_avatars (SF-A1..A4, SF-P2)::the live-transport step degrades honestly with no engine seam (SF-A4 stays E3)"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/sponsor-framework-gaps/rows/sf-a4.json",
   "id": "sponsor-framework-gaps::async-avatars",
   "name": "async-avatars",
   "note": "async-avatars: 5 named suite cases must pass; suite, check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-a1-b7f5d0, check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-a4-478080 must exit zero; and artifacts/sponsor-framework-gaps/rows/sf-a4.json must exist. Requirements: UI-ASYNC-001, UI-NATIVE-008.",
   "phase": "sponsor-framework-gaps",
   "releaseBlocking": false,
   "requirements": [
    "UI-ASYNC-001",
    "UI-NATIVE-008"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite",
     "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-w1-e07115"
    ],
    "resultIds": [
     "sponsor_scenarios::sponsor_billboard (SF-W1)::the world-anchored steps degrade honestly when the engine seam is absent",
     "sponsor_scenarios::sponsor_billboard (SF-W1)::the depleting ring is an INFORMATIONAL timer that reaches empty on wall clock",
     "sponsor_scenarios::sponsor_billboard (SF-W1)::the omen unmounts and remounts with the scope, and the value keeps its terminus"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/sponsor-framework-gaps/rows/sf-w1.json",
   "id": "sponsor-framework-gaps::billboard-fixture",
   "name": "billboard-fixture",
   "note": "billboard-fixture: 3 named suite cases must pass; suite, check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-w1-e07115 must exit zero; and artifacts/sponsor-framework-gaps/rows/sf-w1.json must exist. Requirements: UI-WORLD-001.",
   "phase": "sponsor-framework-gaps",
   "releaseBlocking": false,
   "requirements": [
    "UI-WORLD-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite",
     "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-f1-d7b43f"
    ],
    "resultIds": [
     "feedback::feedback bus: the taxonomy is closed::an invented verb is an authoring error that lists the vocabulary",
     "feedback::presenter feedback: activate::fires exactly once per press, on the causal frame, after the effect",
     "feedback::presenter feedback: activate::a disabled control emits nothing — nothing happened",
     "feedback::presenter feedback: dismiss and the session registration point::presenter.emitFeedback is the seam a drag session commits through"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/sponsor-framework-gaps/rows/sf-f1.json",
   "id": "sponsor-framework-gaps::semantic-feedback",
   "name": "semantic-feedback",
   "note": "semantic-feedback: 4 named suite cases must pass; suite, check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-f1-d7b43f must exit zero; and artifacts/sponsor-framework-gaps/rows/sf-f1.json must exist. Requirements: UI-FEEDBACK-001.",
   "phase": "sponsor-framework-gaps",
   "releaseBlocking": false,
   "requirements": [
    "UI-FEEDBACK-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "sponsor_scenarios::sponsor fixtures: the module contract::every scenario is registered in the gallery registry, in order",
     "sponsor_scenarios::sponsor fixtures: the module contract::every scenario exports title, ledgerRows and build",
     "sponsor_scenarios::sponsor fixtures: the module contract::every scenario mounts its screen through the real presenter"
    ],
    "shell": "Q=$(printf \"\\042\"); for s in sponsor_motion sponsor_celebration sponsor_list sponsor_drop sponsor_toast sponsor_avatars sponsor_markers sponsor_billboard; do grep -q \"$Q$s$Q,\" examples/gallery/scenarios/init.luau || { echo \"scenario $s not registered in the shared registry\"; exit 1; }; done; test \"$(ls examples/gallery/scenarios/sponsor_*.luau | wc -l)\" -eq 8"
   },
   "class": "result-ids",
   "evidence": "examples/gallery/scenarios/init.luau",
   "id": "sponsor-framework-gaps::sponsor-gallery-registered",
   "name": "sponsor-gallery-registered",
   "note": "sponsor-gallery-registered: 3 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold; and examples/gallery/scenarios/init.luau must exist. Requirements: UI-AGENT-001, UI-DIAG-001.",
   "phase": "sponsor-framework-gaps",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001",
    "UI-DIAG-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite",
     "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-c1-73e76f"
    ],
    "resultIds": [
     "motion_clock::motion clock: lifetime::50 build/step/dispose cycles return the core counters to baseline",
     "transitions::mount retire model: ForEach::churn with exits in flight returns the core to baseline",
     "toast_presentation::toast presentation: the layer's lifetime::churning toasts through mount and retire returns the core to baseline",
     "collection_list::unified collection: teardown::build/mount/reorder/drop/dispose returns the core to baseline",
     "async_completeness::preload: warming a declared set::preload churn returns the provider's counters to baseline"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/sponsor-framework-gaps/rows/sf-c1.json",
   "id": "sponsor-framework-gaps::lifecycle-churn",
   "name": "lifecycle-churn",
   "note": "lifecycle-churn: 5 named suite cases must pass; suite, check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-c1-73e76f must exit zero; and artifacts/sponsor-framework-gaps/rows/sf-c1.json must exist. Requirements: UI-LIFE-002.",
   "phase": "sponsor-framework-gaps",
   "releaseBlocking": false,
   "requirements": [
    "UI-LIFE-002"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite",
     "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-c2-725d54"
    ],
    "resultIds": [
     "sponsor_scenarios::sponsor_list (SF-L1/L3/P1/P3)::the row height is DERIVED from the theme snapshot, never typed",
     "sponsor_scenarios::sponsor_list (SF-L1/L3/P1/P3)::the window RE-DERIVES on a live rotation, with no reset (R2-F7)"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/sponsor-framework-gaps/rows/sf-c2.json",
   "id": "sponsor-framework-gaps::preferred-text-axis",
   "name": "preferred-text-axis",
   "note": "preferred-text-axis: 2 named suite cases must pass; suite, check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-c2-725d54 must exit zero; and artifacts/sponsor-framework-gaps/rows/sf-c2.json must exist. Requirements: UI-NATIVE-006.",
   "phase": "sponsor-framework-gaps",
   "releaseBlocking": false,
   "requirements": [
    "UI-NATIVE-006"
   ],
   "state": null
  },
  {
   "check": {
    "receipt": "tools/lune/verify/evidence/sponsor-framework-gaps--five-view-matrix.json"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/sponsor-framework-gaps/rows/sf-c3.json",
   "id": "sponsor-framework-gaps::five-view-matrix",
   "name": "five-view-matrix",
   "note": "five-view-matrix: and artifacts/sponsor-framework-gaps/rows/sf-c3.json must exist. Requirements: UI-FID-001, UI-ADAPT-001.",
   "phase": "sponsor-framework-gaps",
   "releaseBlocking": false,
   "requirements": [
    "UI-FID-001",
    "UI-ADAPT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite",
     "check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-m8-45ab23"
    ],
    "resultIds": [
     "sponsor_scenarios::sponsor_motion (SF-M1/M2/M3/M5/M7/M8)::the stress scene runs 20 concurrent springs and RESTS at zero cost (SF-M8)"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/sponsor-framework-gaps/rows/sf-m8.json",
   "id": "sponsor-framework-gaps::perf-scene",
   "name": "perf-scene",
   "note": "perf-scene: 1 named suite case must pass; suite, check_sf_rows-artifacts-sponsor-framework-gaps-rows-sf-m8-45ab23 must exit zero; and artifacts/sponsor-framework-gaps/rows/sf-m8.json must exist. Requirements: UI-PERF-002.",
   "phase": "sponsor-framework-gaps",
   "releaseBlocking": false,
   "requirements": [
    "UI-PERF-002"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/test.json",
   "id": "sponsor-framework-gaps::library-suite-green",
   "name": "library-suite-green",
   "note": "library-suite-green: suite must exit zero; and artifacts/test.json must exist. Requirements: UI-AGENT-001.",
   "phase": "sponsor-framework-gaps",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "cd ../../../games/RascalRally/code"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "sponsor-framework-gaps::game-untouched",
   "name": "game-untouched",
   "note": "game-untouched: rascalrally-suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "sponsor-framework-gaps",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "priorPhases": true
   },
   "class": "prior-phases",
   "evidence": null,
   "id": "sponsor-framework-gaps::prior-gates-unregressed",
   "name": "prior-gates-unregressed",
   "note": "prior-gates-unregressed: every row of every earlier phase must pass in this same run; and artifacts/sponsor-framework-gaps/prior-gates.txt must exist. Requirements: UI-AGENT-001.",
   "phase": "sponsor-framework-gaps",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "sponsor-framework-gaps::physical-and-human-rows",
   "name": "physical-and-human-rows",
   "note": "physical-and-human-rows: and artifacts/sponsor-framework-gaps/review-packet.md must exist. Requirements: UI-MOTION-001, UI-DRAG-001.",
   "phase": "sponsor-framework-gaps",
   "releaseBlocking": false,
   "requirements": [
    "UI-MOTION-001",
    "UI-DRAG-001"
   ],
   "state": "FAIL_ENVIRONMENT"
  },
  {
   "check": {
    "producers": [
     "check_public_surface"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/verify/public-surface.txt",
   "id": "code-simplicity-cleanup::public-surface-unchanged",
   "name": "public-surface-unchanged",
   "note": "public-surface-unchanged: _probe_public_surface must exit zero; and artifacts/verify/public-surface.txt must exist. Requirements: UI-AGENT-001.",
   "phase": "code-simplicity-cleanup",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "runtime_quarantine::scope disposal: a throwing cleanup is quarantined (RR-4)::the double-dispose report fires for an ALIASED child and stays silent otherwise (RR-5-R1)",
     "runtime_quarantine::motion clock: the stats contract under a quarantined frame (RR-1-R2)::transactions counts frames that really committed, never the aborted ones",
     "presenter_drag_integration::mid-flight teardown and retiring surfaces (ARCH-F1 / ARCH-F2)::disposing the registry with several flights in the air is clean (RR-3-R1)",
     "conformance/conformance::core conformance (custom)::scope-cleanup-quarantined-and-early-child-is-not-a-double-dispose [UI-LIFE-001]"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "code-simplicity-cleanup::carry-over-pins",
   "name": "carry-over-pins",
   "note": "carry-over-pins: 4 named suite cases must pass; suite must exit zero. Requirements: UI-LIFE-001, UI-RUNTIME-003.",
   "phase": "code-simplicity-cleanup",
   "releaseBlocking": false,
   "requirements": [
    "UI-LIFE-001",
    "UI-RUNTIME-003"
   ],
   "state": null
  },
  {
   "check": {
    "shell": "for core in custom imperative; do lune run tests/conformance/cli $core >/dev/null 2>&1; python3 -c \"import json,sys; d=json.load(open('artifacts/conformance-$core.json')); r=[c for c in d['checks'] if c['name']=='scope-cleanup-quarantined-and-early-child-is-not-a-double-dispose']; sys.exit(0 if r and r[0]['status']=='PASS' else 1)\" || exit 1; done"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/conformance-custom.json",
   "id": "code-simplicity-cleanup::conformance-all-cores",
   "name": "conformance-all-cores",
   "note": "conformance-all-cores: its file-existence and text assertions must hold; and artifacts/conformance-custom.json must exist. Requirements: UI-LIFE-001.",
   "phase": "code-simplicity-cleanup",
   "releaseBlocking": false,
   "requirements": [
    "UI-LIFE-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/test.json",
   "id": "code-simplicity-cleanup::library-suite-green",
   "name": "library-suite-green",
   "note": "library-suite-green: suite must exit zero; and artifacts/test.json must exist. Requirements: UI-AGENT-001.",
   "phase": "code-simplicity-cleanup",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "cd ../../../games/RascalRally/code"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "code-simplicity-cleanup::game-untouched",
   "name": "game-untouched",
   "note": "game-untouched: rascalrally-suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "code-simplicity-cleanup",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_registration_cli",
     "check_boundary",
     "check_docs_cli",
     "check_prop_parity_cli",
     "check_manifest_integrity",
     "stylua-check-check-src-tests-tools-bench-examples"
    ]
   },
   "class": "exit0",
   "evidence": null,
   "id": "code-simplicity-cleanup::registration-and-drift",
   "name": "registration-and-drift",
   "note": "registration-and-drift: check_registration_cli, check_boundary, check_docs_cli, check_prop_parity_cli, check_manifest_integrity, stylua-check-check-src-tests-tools-bench-examples must exit zero. Requirements: UI-AGENT-001.",
   "phase": "code-simplicity-cleanup",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "priorPhases": true
   },
   "class": "prior-phases",
   "evidence": null,
   "id": "code-simplicity-cleanup::prior-gates-unregressed",
   "name": "prior-gates-unregressed",
   "note": "prior-gates-unregressed: every row of every earlier phase must pass in this same run; and artifacts/code-simplicity-cleanup/prior-gates.txt must exist. Requirements: UI-AGENT-001.",
   "phase": "code-simplicity-cleanup",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "bench"
    ],
    "shell": "python3 -c \"import json,sys; d=json.load(open('artifacts/bench.json')); sys.exit(0 if d['status']=='PASS' and not any(r['regression'] for r in d['results']) else 1)\""
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "code-simplicity-cleanup::performance-unregressed",
   "name": "performance-unregressed",
   "note": "performance-unregressed: bench must exit zero; its file-existence and text assertions must hold; and artifacts/code-simplicity-cleanup/perf-after.md must exist. Requirements: UI-PERF-001.",
   "phase": "code-simplicity-cleanup",
   "releaseBlocking": false,
   "requirements": [
    "UI-PERF-001"
   ],
   "state": null
  },
  {
   "check": {
    "receipt": "tools/lune/verify/evidence/code-simplicity-cleanup--studio-evidence.json"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/code-simplicity-cleanup/studio/five-view-matrix.json",
   "id": "code-simplicity-cleanup::studio-evidence",
   "name": "studio-evidence",
   "note": "studio-evidence: and artifacts/code-simplicity-cleanup/studio/five-view-matrix.json must exist. Requirements: UI-FID-001.",
   "phase": "code-simplicity-cleanup",
   "releaseBlocking": false,
   "requirements": [
    "UI-FID-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_surface_ledger"
    ]
   },
   "class": "exit0",
   "evidence": "tools/lune/verify/data/surface-ledger.md",
   "id": "api-architecture-consistency::surface-ledger-complete",
   "name": "surface-ledger-complete",
   "note": "surface-ledger-complete: check_surface_ledger must exit zero; and tools/lune/verify/data/surface-ledger.md must exist. Requirements: UI-AGENT-001.",
   "phase": "api-architecture-consistency",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "shell": "test -f docs/reference/constitution.md && grep -q \"16. Exceptions\" docs/reference/constitution.md && grep -q \"constitution.md\" docs/guide/README.md && grep -q \"constitution.md\" docs/guide/02-architecture.md && grep -qi \"kind ladder\" docs/reference/constitution.md"
   },
   "class": "evidence-pin",
   "evidence": "docs/reference/constitution.md",
   "id": "api-architecture-consistency::constitution-published",
   "name": "constitution-published",
   "note": "constitution-published: its file-existence and text assertions must hold; and docs/reference/constitution.md must exist. Requirements: UI-AGENT-001.",
   "phase": "api-architecture-consistency",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "replication::ReplicatedCollection::a resnapshot at the CURRENT revision re-bases while awaiting, instead of wedging",
     "replication::Mutation::a throwing optimistic apply still returns the envelope and leaves the mutation usable",
     "replication::ReplicatedCollection::a throwing requestResnapshot does not latch the gap — the next patch asks again",
     "replication::Mutation::reset() escapes a stuck pending request, restores, and the stale id stays ignored",
     "layout_vocabulary::F-3: a structural modifier carries the content's meta channel::an INFLATED backing layer keeps its drag declaration",
     "styling::F-4 (BP-F1): UI.styleGroup is the WHOLE style-modifier family, and closed::an unknown spec key is refused, naming the modifier and the legal set",
     "styling::F-5 (BP-F2): the shadow and gradient grammars are closed like the stroke one::a typo'd gradient field is refused instead of silently producing the default",
     "authoring::F-6 (BP-F5): the schema authority tables are frozen::a consumer of the tooling seam cannot disable framework validation",
     "popup_button::PopupButton: build-time contract::rejects a read-only Memo `value` at BUILD, naming the control and the field",
     "chip::Chip control::rejects a read-only Memo `selected` at BUILD",
     "table::Table: column-owned columns (director round 5)::the header title honours the column alignment, exactly like the column's cells",
     "focus::focus graph: the caller's tables are never written::a table.freeze'd grouped scope pushes and navigates, groups included",
     "api_surface::semantic versioning and deprecation::the ledger is the UNION of schema-generated and declared entries",
     "api_surface::semantic versioning and deprecation::every entry gives at least one MINOR of notice, and the ledger is frozen",
     "hint_no_remount::inputHint: scope ownership and a validated style (F-33)::opts.scope owns the memo, so a screen cannot leak it",
     "presenter::presenter: presentCritical carries the caller's opts to the fallback (F-16)::a critical fallback under rootPolicy=edgeToEdge resolves the edge-to-edge rect",
     "presenter::presenter: unknown string-enum opts fail at the call site (F-15)::an unknown responder names the opt and its legal set",
     "display_controls::B-DSP1: Label keeps a semantic text fallback::semanticText is a Readable, like every sibling that publishes one",
     "text_fit::text.measure — the spec-table form (F-31)::both forms answer identically for the same inputs",
     "value_controls::B-VAL1: the shared value model::a throwing formatter falls back to the default format instead of unwinding a solve",
     "touch_gestures::touch_gestures.newArbiter (corrections §2)::refuses a non-empty opts table: options are RESERVED, none exist yet (SEAM-4)",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::the sweep covers every boundary the constitution's §4 claim names",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newResourceProvider refuses 'definitelyNotAKey' and names itself",
     "paint_extensions::UI.stroke: the authored border::shadowData / gradientData / cornersData complete the family and pulse the same way",
     "authoring::M0-A3: one reconciled property model::it detects a restricted class the authority manifest does not back"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "api-architecture-consistency::compatible-fixes-proven",
   "name": "compatible-fixes-proven",
   "note": "compatible-fixes-proven: 25 named suite cases must pass; suite must exit zero. Requirements: UI-AGENT-001.",
   "phase": "api-architecture-consistency",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_registration_cli",
     "check_docs_cli",
     "check_boundary",
     "check_prop_parity_cli",
     "check_surface_ledger"
    ]
   },
   "class": "exit0",
   "evidence": null,
   "id": "api-architecture-consistency::enforcement-checks",
   "name": "enforcement-checks",
   "note": "enforcement-checks: check_registration_cli, check_docs_cli, check_boundary, check_prop_parity_cli, check_surface_ledger must exit zero. Requirements: UI-AGENT-001.",
   "phase": "api-architecture-consistency",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "shell": "test ! -f src/controls/tag_count.luau"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "api-architecture-consistency::fresh-author-exercise",
   "name": "fresh-author-exercise",
   "note": "fresh-author-exercise: its file-existence and text assertions must hold; and artifacts/api-architecture-consistency/fresh-author-exercise.md must exist. Requirements: UI-AGENT-001.",
   "phase": "api-architecture-consistency",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/test.json",
   "id": "api-architecture-consistency::library-suite-green",
   "name": "library-suite-green",
   "note": "library-suite-green: suite must exit zero; and artifacts/test.json must exist. Requirements: UI-AGENT-001.",
   "phase": "api-architecture-consistency",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "cd ../../../games/RascalRally/code"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "api-architecture-consistency::rascalrally-consumer",
   "name": "rascalrally-consumer",
   "note": "rascalrally-consumer: rascalrally-suite must exit zero; its file-existence and text assertions must hold; and artifacts/api-architecture-consistency/consumer-impact.md must exist. Requirements: UI-AGENT-001.",
   "phase": "api-architecture-consistency",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_registration_cli",
     "check_boundary",
     "check_docs_cli",
     "check_prop_parity_cli",
     "check_manifest_integrity",
     "stylua-check-check-src-tests-tools-bench-examples"
    ]
   },
   "class": "exit0",
   "evidence": null,
   "id": "api-architecture-consistency::registration-and-drift",
   "name": "registration-and-drift",
   "note": "registration-and-drift: check_registration_cli, check_boundary, check_docs_cli, check_prop_parity_cli, check_manifest_integrity, stylua-check-check-src-tests-tools-bench-examples must exit zero. Requirements: UI-AGENT-001.",
   "phase": "api-architecture-consistency",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "priorPhases": true
   },
   "class": "prior-phases",
   "evidence": null,
   "id": "api-architecture-consistency::prior-gates-unregressed",
   "name": "prior-gates-unregressed",
   "note": "prior-gates-unregressed: every row of every earlier phase must pass in this same run; and artifacts/api-architecture-consistency/prior-gates.txt must exist. Requirements: UI-AGENT-001.",
   "phase": "api-architecture-consistency",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "bench"
    ],
    "shell": "python3 -c \"import json,sys; d=json.load(open('artifacts/bench.json')); sys.exit(0 if d['status']=='PASS' and not any(r['regression'] for r in d['results']) else 1)\""
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "api-architecture-consistency::performance-unregressed",
   "name": "performance-unregressed",
   "note": "performance-unregressed: bench must exit zero; its file-existence and text assertions must hold. Requirements: UI-PERF-001.",
   "phase": "api-architecture-consistency",
   "releaseBlocking": false,
   "requirements": [
    "UI-PERF-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_flat_baseline"
    ],
    "receipt": "tools/lune/verify/evidence/api-architecture-consistency--studio-evidence.json"
   },
   "class": "exit0",
   "evidence": null,
   "id": "api-architecture-consistency::studio-evidence",
   "name": "studio-evidence",
   "note": "studio-evidence: check_flat_baseline must exit zero; and artifacts/api-architecture-consistency/studio-disposition.md must exist. Requirements: UI-FID-001.",
   "phase": "api-architecture-consistency",
   "releaseBlocking": false,
   "requirements": [
    "UI-FID-001"
   ],
   "state": null
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "api-architecture-consistency::physical-rows",
   "name": "physical-rows",
   "note": "physical-rows: and artifacts/api-architecture-consistency/studio-disposition.md must exist. Requirements: UI-INPUT-002.",
   "phase": "api-architecture-consistency",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-002"
   ],
   "state": "FAIL_ENVIRONMENT"
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "keyboard_navigation::keyboard traversal: the order comes from the mounted focus graph (DK-1)::traverse(+1) walks the flat scope forward and traverse(-1) walks it back",
     "keyboard_navigation::keyboard traversal crosses navigation groups in group order (DK-2)::Tab leaves a CONTAINED group that the arrows cannot leave sideways",
     "keyboard_navigation::keyboard traversal crosses navigation groups in group order (DK-2)::Shift+Tab crosses back into the previous group at its LAST node",
     "keyboard_navigation::the SCOPE declares the traversal wrap policy (DK-3)::wraps by default at both ends",
     "keyboard_navigation::the SCOPE declares the traversal wrap policy (DK-3)::traversalWrap = false stops at the ends and reports that it did not move",
     "keyboard_navigation::the SCOPE declares the traversal wrap policy (DK-3)::a GROUPED scope's wrap policy is the scope's, not any group's",
     "keyboard_navigation::traversal skips exactly what navigation skips (DK-4)::an ineligible focus-skip entry is stepped over, not landed on",
     "keyboard_navigation::traversal skips exactly what navigation skips (DK-4)::focus stays put when nothing else in the scope is eligible",
     "keyboard_navigation::traversal survives structural churn (DK-5)::a node removed between presses never becomes a traversal target",
     "keyboard_navigation::traversal survives structural churn (DK-5)::re-ordering the scope re-reads the order on the very next press",
     "keyboard_navigation::modal scopes trap traversal and restore focus (DK-6)::Tab cannot leave a trapped scope and the prior focus is restored on pop"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "desktop-keyboard-navigation::traversal-pure",
   "name": "traversal-pure",
   "note": "traversal-pure: 11 named suite cases must pass; suite must exit zero. Requirements: UI-FOCUS-001.",
   "phase": "desktop-keyboard-navigation",
   "releaseBlocking": false,
   "requirements": [
    "UI-FOCUS-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "keyboard_navigation::Tab traverses a presented screen with zero consumer wiring (DK-1/DK-16)::Tab and Shift+Tab move the focus ring through a plain form",
     "keyboard_navigation::Space activates alongside Return, once per press (DK-8)::Space activates the focused control exactly once",
     "keyboard_navigation::Space activates alongside Return, once per press (DK-8)::a disabled control consumes Space and does nothing",
     "keyboard_navigation::exactly one Activate per press across the IAS and native-tap paths (DK-9)::a native Activated echoing the same keyboard press does not activate twice",
     "keyboard_navigation::exactly one Activate per press across the IAS and native-tap paths (DK-9)::a real pointer tap beside a key press is two presses, never an echo",
     "keyboard_navigation::exactly one Activate per press across the IAS and native-tap paths (DK-9)::a spent echo does not roll the window forward onto the next real press",
     "keyboard_navigation::keyboard traversal: the order comes from the mounted focus graph (DK-1)::Shift+Tab into a scope with no focus enters at the LAST eligible node",
     "keyboard_navigation::exactly one Activate per press across the IAS and native-tap paths (DK-9)::a surface with no selection bridge never collapses a tap next to a key press",
     "keyboard_navigation::a focused value control consumes its declared axis (DK-10/DK-11)::Left/Right adjust a focused horizontal Slider on a GROUPED screen",
     "keyboard_navigation::a focused value control consumes its declared axis (DK-10/DK-11)::the OTHER axis still navigates while the value control holds focus",
     "keyboard_navigation::a focused value control consumes its declared axis (DK-10/DK-11)::the Adjust keys are UNBOUND while focus sits off a declared target",
     "keyboard_navigation::text editing owns Space and the arrows; Tab commits then advances (DK-12)::Tab commits through the field's own path and only THEN advances focus",
     "keyboard_navigation::text editing owns Space and the arrows; Tab commits then advances (DK-12)::the arrows stay with the field's own sinking context while editing",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::a PASSIVE HUD binds no Tab and no Space: gameplay is untouched",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::engage() binds them and resign() gives them back, with no residue",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::keyboard hot-plug adds and removes the bindings with no stuck sink",
     "keyboard_navigation::teardown removes every keyboard binding (DK-14)::a dismissed screen stops eating Tab and Space"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "desktop-keyboard-navigation::action-system-behavior",
   "name": "action-system-behavior",
   "note": "action-system-behavior: 17 named suite cases must pass; suite must exit zero. Requirements: UI-INPUT-003, UI-INPUT-004.",
   "phase": "desktop-keyboard-navigation",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-003",
    "UI-INPUT-004"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "keyboard_navigation::a value control claims Adjust only inside its own subtree (DK-11)::the Adjust keys unbind when focus leaves the control for a sibling button",
     "keyboard_navigation::a value control claims Adjust only inside its own subtree (DK-11)::a sibling button's arrows fire no dead Adjust"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "desktop-keyboard-navigation::adjust-claim-is-subtree-scoped",
   "name": "adjust-claim-is-subtree-scoped",
   "note": "adjust-claim-is-subtree-scoped: 2 named suite cases must pass; suite must exit zero. Requirements: UI-PARADIGM-001.",
   "phase": "desktop-keyboard-navigation",
   "releaseBlocking": false,
   "requirements": [
    "UI-PARADIGM-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "keyboard_navigation::traversal calls the SHARED keep-visible service (DK-7)::tabbing past the visible band scrolls the host, with no control wiring"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "desktop-keyboard-navigation::keep-visible-on-traverse",
   "name": "keep-visible-on-traverse",
   "note": "keep-visible-on-traverse: 1 named suite case must pass; suite must exit zero. Requirements: UI-FOCUS-001.",
   "phase": "desktop-keyboard-navigation",
   "releaseBlocking": false,
   "requirements": [
    "UI-FOCUS-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_no_screen_key_bindings-selftest",
     "check_no_screen_key_bindings",
     "suite"
    ],
    "resultIds": [
     "keyboard_navigation::Tab traverses a presented screen with zero consumer wiring (DK-1/DK-16)::Tab and Shift+Tab move the focus ring through a plain form"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "desktop-keyboard-navigation::no-screen-key-bindings",
   "name": "no-screen-key-bindings",
   "note": "no-screen-key-bindings: 1 named suite case must pass; check_no_screen_key_bindings-selftest, check_no_screen_key_bindings, suite must exit zero. Requirements: UI-INPUT-001.",
   "phase": "desktop-keyboard-navigation",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_registration_cli",
     "check_docs_cli",
     "check_boundary",
     "check_prop_parity_cli",
     "check_surface_ledger",
     "check_manifest_integrity",
     "stylua-check-check-src-tests-tools-bench-examples"
    ]
   },
   "class": "exit0",
   "evidence": null,
   "id": "desktop-keyboard-navigation::registration-and-docs",
   "name": "registration-and-docs",
   "note": "registration-and-docs: check_registration_cli, check_docs_cli, check_boundary, check_prop_parity_cli, check_surface_ledger, check_manifest_integrity, stylua-check-check-src-tests-tools-bench-examples must exit zero. Requirements: UI-AGENT-001.",
   "phase": "desktop-keyboard-navigation",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_registration_cli"
    ],
    "shell": "grep -q \"Tab and Shift+Tab move the focus ring through a plain form\" tests/conformance/controls_registry.luau && grep -q \"Space activates the focused control exactly once\" tests/conformance/controls_registry.luau && grep -q \"a PASSIVE HUD binds no Tab and no Space\" tests/conformance/controls_registry.luau && grep -q \"Left/Right adjust a focused horizontal Slider on a GROUPED screen\" tests/conformance/controls_registry.luau && grep -q \"Space on the focused button steps the value, in both directions\" tests/conformance/controls_registry.luau"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "desktop-keyboard-navigation::conformance-proofs",
   "name": "conformance-proofs",
   "note": "conformance-proofs: check_registration_cli must exit zero; its file-existence and text assertions must hold. Requirements: UI-PARADIGM-001.",
   "phase": "desktop-keyboard-navigation",
   "releaseBlocking": false,
   "requirements": [
    "UI-PARADIGM-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/test.json",
   "id": "desktop-keyboard-navigation::library-suite-green",
   "name": "library-suite-green",
   "note": "library-suite-green: suite must exit zero; and artifacts/test.json must exist. Requirements: UI-AGENT-001.",
   "phase": "desktop-keyboard-navigation",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "receipt": "tools/lune/verify/evidence/desktop-keyboard-navigation--studio-evidence.json"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/desktop-keyboard-navigation/studio/keyboard.json",
   "id": "desktop-keyboard-navigation::studio-evidence",
   "name": "studio-evidence",
   "note": "studio-evidence: and artifacts/desktop-keyboard-navigation/studio/keyboard.json must exist. Requirements: UI-FID-001.",
   "phase": "desktop-keyboard-navigation",
   "releaseBlocking": false,
   "requirements": [
    "UI-FID-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "cd ../../../games/RascalRally/code"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "desktop-keyboard-navigation::rascalrally-consumer",
   "name": "rascalrally-consumer",
   "note": "rascalrally-consumer: rascalrally-suite must exit zero; its file-existence and text assertions must hold; and artifacts/desktop-keyboard-navigation/consumer-impact.md must exist. Requirements: UI-AGENT-001.",
   "phase": "desktop-keyboard-navigation",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "priorPhases": true
   },
   "class": "prior-phases",
   "evidence": null,
   "id": "desktop-keyboard-navigation::prior-gates-unregressed",
   "name": "prior-gates-unregressed",
   "note": "prior-gates-unregressed: every row of every earlier phase must pass in this same run; and artifacts/desktop-keyboard-navigation/prior-gates.txt must exist. Requirements: UI-AGENT-001.",
   "phase": "desktop-keyboard-navigation",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "desktop-keyboard-navigation::physical-and-contended-rows",
   "name": "physical-and-contended-rows",
   "note": "physical-and-contended-rows: and artifacts/desktop-keyboard-navigation/review-packet.md must exist. Requirements: UI-INPUT-002.",
   "phase": "desktop-keyboard-navigation",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-002"
   ],
   "state": "FAIL_ENVIRONMENT"
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "traversal_order::a Grip-bearing control BETWEEN ordinary controls (TD-1, TD-2)::Tab visits the Slider's track in DOCUMENT position, not last",
     "traversal_order::a Grip-bearing control BETWEEN ordinary controls (TD-1, TD-2)::pressing Tab really moves focus in that order (onFocusNav, not the dump)",
     "traversal_order::every focusable Grip traverses in document position — no exceptions (TD-4)::a bare focusable Grip declared between buttons traverses between them",
     "traversal_order::every focusable Grip traverses in document position — no exceptions (TD-4)::...and pressing Tab really lands on it there",
     "traversal_order::a modal traverses in document order too, and still traps (TD-12)::a Grip inside a modal takes its document position within the modal",
     "traversal_order::structural churn re-ranks on the next press (TD-11)::a row mounted after present() takes its document position, not the end"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "traversal-document-order::document-order-traversal",
   "name": "document-order-traversal",
   "note": "document-order-traversal: 6 named suite cases must pass; suite must exit zero. Requirements: UI-FOCUS-001.",
   "phase": "traversal-document-order",
   "releaseBlocking": false,
   "requirements": [
    "UI-FOCUS-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "traversal_order::a Grip-bearing control BETWEEN ordinary controls (TD-1, TD-2)::...and the ARROWS still defer it to the end, exactly as before",
     "traversal_order::a Grip-bearing control BETWEEN ordinary controls (TD-1, TD-2)::the two readings cover the SAME SET — traversal reorders, never adds or drops",
     "traversal_order::traversalPriority is a sort TIER, not an index (TD-5, TD-6)::the tier does NOT reach the arrows",
     "traversal_order::graph.traverse sorts by rank without changing membership (TDN-2)::the arrows never read the rank",
     "traversal_order::graph.traverse sorts by rank without changing membership (TDN-2)::ENTRY focus still reads the unranked order, by design"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "traversal-document-order::arrows-unregressed",
   "name": "arrows-unregressed",
   "note": "arrows-unregressed: 5 named suite cases must pass; suite must exit zero. Requirements: UI-FOCUS-001.",
   "phase": "traversal-document-order",
   "releaseBlocking": false,
   "requirements": [
    "UI-FOCUS-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "traversal_order::traversalPriority is a sort TIER, not an index (TD-5, TD-6)::a positive tier pushes one control to the back without moving the others",
     "traversal_order::traversalPriority is a sort TIER, not an index (TD-5, TD-6)::a negative tier pulls one to the front",
     "traversal_order::traversalPriority is a sort TIER, not an index (TD-5, TD-6)::within one tier, DOCUMENT order always wins",
     "traversal_order::traversalPriority refuses what it cannot honour (TD-8)::a non-number is refused, naming the property",
     "traversal_order::traversalPriority refuses what it cannot honour (TD-8)::a BOUND value is refused: the prop is construction-only"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "traversal-document-order::traversal-priority",
   "name": "traversal-priority",
   "note": "traversal-priority: 5 named suite cases must pass; suite must exit zero. Requirements: UI-FOCUS-001.",
   "phase": "traversal-document-order",
   "releaseBlocking": false,
   "requirements": [
    "UI-FOCUS-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "traversal_order::an explicit navigationGroups map is NOT re-ranked (TD-7)::the declared order survives exactly, grips and all",
     "traversal_order::an explicit navigationGroups map is NOT re-ranked (TD-7)::navigationGroups = false is NOT a declared map: the flat ring is still ranked",
     "traversal_order::graph.traverse sorts by rank without changing membership (TDN-2)::no rank means the pre-stage reading, byte for byte",
     "traversal_order::graph.traverse sorts by rank without changing membership (TDN-2)::an INCOMPLETE rank is ignored entirely rather than applied to half the ring"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "traversal-document-order::consumer-map-untouched",
   "name": "consumer-map-untouched",
   "note": "consumer-map-untouched: 4 named suite cases must pass; suite must exit zero. Requirements: UI-FOCUS-001.",
   "phase": "traversal-document-order",
   "releaseBlocking": false,
   "requirements": [
    "UI-FOCUS-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "receipt": "tools/lune/verify/evidence/traversal-document-order--slider-focus-affordance.json",
    "resultIds": [
     "keyboard_navigation::a focused Slider draws its own treatment, on the thumb (DK-20)::the thumb takes a focus stroke while the track holds focus, and drops it after",
     "keyboard_navigation::a focused Slider draws its own treatment, on the thumb (DK-20)::the \"focusRing\" thickness alias resolves to the adapter's own ring weight"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/traversal-document-order/studio/td17-slider-focused.png",
   "id": "traversal-document-order::slider-focus-affordance",
   "name": "slider-focus-affordance",
   "note": "slider-focus-affordance: 2 named suite cases must pass; suite must exit zero; and artifacts/traversal-document-order/studio/td17-slider-focused.png must exist. Requirements: UI-FOCUS-001.",
   "phase": "traversal-document-order",
   "releaseBlocking": false,
   "requirements": [
    "UI-FOCUS-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "receipt": "tools/lune/verify/evidence/traversal-document-order--engage-and-resign.json",
    "resultIds": [
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::a tap on the UI takes the keyboard, and a tap OUTSIDE gives it back"
    ],
    "shell": "grep -q 'responder = \"passive\"' examples/gallery/scenarios/keyboard_navigation.luau"
   },
   "class": "result-ids",
   "evidence": "artifacts/traversal-document-order/studio/traversal.json",
   "id": "traversal-document-order::engage-and-resign",
   "name": "engage-and-resign",
   "note": "engage-and-resign: 1 named suite case must pass; suite must exit zero; its file-existence and text assertions must hold; and artifacts/traversal-document-order/studio/traversal.json must exist. Requirements: UI-INPUT-003.",
   "phase": "traversal-document-order",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-003"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "receipt": "tools/lune/verify/evidence/traversal-document-order--ring-follows-ownership.json",
    "resultIds": [
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::a PASSIVE surface paints no focus ring: the ring never claims input it lacks",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::an ordinary (engaged-open) screen still paints its ring exactly as before"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/traversal-document-order/studio/td19-no-ring-when-passive.png",
   "id": "traversal-document-order::ring-follows-ownership",
   "name": "ring-follows-ownership",
   "note": "ring-follows-ownership: 2 named suite cases must pass; suite must exit zero; and artifacts/traversal-document-order/studio/td19-no-ring-when-passive.png must exist. Requirements: UI-FOCUS-001.",
   "phase": "traversal-document-order",
   "releaseBlocking": false,
   "requirements": [
    "UI-FOCUS-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::initialFocus defaults to the first focusable — every existing caller unmoved",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::initialFocus = \"none\" mounts the surface with NO focus",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::...and Tab still ENTERS such a scope rather than no-opping",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::initialFocus names a control by id, without the author knowing its path",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::focus lands there ONCE — the ring never appears elsewhere first",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::an initialFocus id that names nothing is refused, listing what is available",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::a focusable actually named \"first\" makes the keyword ambiguous, and it says so",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::...and { id = … } is the escape hatch that means the CONTROL",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::a dismissed surface gives up its focus ring immediately, mid-exit-transition"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "traversal-document-order::initial-focus-and-release",
   "name": "initial-focus-and-release",
   "note": "initial-focus-and-release: 9 named suite cases must pass; suite must exit zero. Requirements: UI-FOCUS-001.",
   "phase": "traversal-document-order",
   "releaseBlocking": false,
   "requirements": [
    "UI-FOCUS-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::a self-painted focus ring clears when the surface resigns",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::a PASSIVE surface defaults to NO initial focus",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::...while an engaged-open screen still defaults to its first focusable",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::an explicit initialFocus still wins on a passive surface"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "traversal-document-order::self-painted-ring-releases",
   "name": "self-painted-ring-releases",
   "note": "self-painted-ring-releases: 4 named suite cases must pass; suite must exit zero. Requirements: UI-FOCUS-001.",
   "phase": "traversal-document-order",
   "releaseBlocking": false,
   "requirements": [
    "UI-FOCUS-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::an engaged HUD resigns on an outside tap even CLOSE to its own content",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::...while a MODAL keeps its dismissal forgiveness",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::a resigned surface unbinds the Adjust keys it had claimed",
     "modal_dismissal::modal dismissal: 24px forgiveness ring around the painted panel::a tap within 24px of the panel edge does NOT dismiss (near-miss forgiveness)"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "traversal-document-order::resign-has-no-forgiveness-zone",
   "name": "resign-has-no-forgiveness-zone",
   "note": "resign-has-no-forgiveness-zone: 4 named suite cases must pass; suite must exit zero. Requirements: UI-INPUT-003.",
   "phase": "traversal-document-order",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-003"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "traversal_order::handle.focusOrder() (TD-9, TD-10)::carries its schema and both readings",
     "traversal_order::handle.focusOrder() (TD-9, TD-10)::is deterministic: two calls with nothing changed are equal",
     "traversal_order::handle.focusOrder() (TD-9, TD-10)::is frozen — an inspector cannot mutate the map it is inspecting",
     "traversal_order::handle.focusOrder() (TD-9, TD-10)::survives dispose: an overlay outliving its surface gets an empty map, not a throw",
     "traversal_order::handle.focusOrder() (TD-9, TD-10)::reports the AUTHORED tier per entry"
    ],
    "shell": "echo \"✓ frozen\" >/dev/null"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "traversal-document-order::focus-order-dump",
   "name": "focus-order-dump",
   "note": "focus-order-dump: 5 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "traversal-document-order",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_registration_cli",
     "check_docs_cli",
     "check_boundary",
     "check_prop_parity_cli",
     "check_surface_ledger",
     "check_manifest_integrity",
     "stylua-check-check-src-tests-tools-bench-examples"
    ]
   },
   "class": "exit0",
   "evidence": null,
   "id": "traversal-document-order::registration-and-docs",
   "name": "registration-and-docs",
   "note": "registration-and-docs: check_registration_cli, check_docs_cli, check_boundary, check_prop_parity_cli, check_surface_ledger, check_manifest_integrity, stylua-check-check-src-tests-tools-bench-examples must exit zero. Requirements: UI-AGENT-001.",
   "phase": "traversal-document-order",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "shell": "grep -q 'keyboardNavigation' docs/reference/api.md && grep -q 'Camera 100' docs/guide/07-input.md"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "traversal-document-order::step8-debt-cleared",
   "name": "step8-debt-cleared",
   "note": "step8-debt-cleared: its file-existence and text assertions must hold; and artifacts/traversal-document-order/step8-debt.md must exist. Requirements: UI-AGENT-001.",
   "phase": "traversal-document-order",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/test.json",
   "id": "traversal-document-order::library-suite-green",
   "name": "library-suite-green",
   "note": "library-suite-green: suite must exit zero; and artifacts/test.json must exist. Requirements: UI-AGENT-001.",
   "phase": "traversal-document-order",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_traversal_evidence"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/traversal-document-order/studio/traversal.json",
   "id": "traversal-document-order::studio-evidence",
   "name": "studio-evidence",
   "note": "studio-evidence: check_traversal_evidence must exit zero; and artifacts/traversal-document-order/studio/traversal.json must exist. Requirements: UI-FID-001.",
   "phase": "traversal-document-order",
   "releaseBlocking": false,
   "requirements": [
    "UI-FID-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "test -f ../../../games/RascalRally/code/tests/facet_focus_order_contract.spec.luau && cd ../../../games/RascalRally/code"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "traversal-document-order::rascalrally-consumer",
   "name": "rascalrally-consumer",
   "note": "rascalrally-consumer: rascalrally-suite must exit zero; its file-existence and text assertions must hold; and artifacts/traversal-document-order/consumer-impact.md must exist. Requirements: UI-AGENT-001.",
   "phase": "traversal-document-order",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "preferred_text_seam::preferred_text offset seam (LT-1/LT-3/LT-4)::applies the measured fallback synchronously — first mount never waits",
     "preferred_text_seam::preferred_text offset seam (LT-1/LT-3/LT-4)::a confirmed answer equal to the fallback applies nothing more (no second reflow)",
     "preferred_text_seam::preferred_text offset seam (LT-1/LT-3/LT-4)::a confirmed answer that differs applies exactly one atomic correction",
     "preferred_text_seam::preferred_text offset seam (LT-1/LT-3/LT-4)::an answer that raced a preference step is DISCARDED — the A->B->A double-step cannot poison the cache",
     "preferred_text_seam::preferred_text offset seam (LT-1/LT-3/LT-4)::an answer resolved with NO step in flight is cached: the revisit asks nothing",
     "preferred_text_seam::preferred_text offset seam (LT-1/LT-3/LT-4)::a FAILED lookup keeps the fallback and does not retry in a loop",
     "preferred_text_seam::preferred_text offset seam (LT-1/LT-3/LT-4)::garbage answers are refused: NaN and negative offsets keep the fallback",
     "preferred_text_seam::preferred_text offset seam (LT-1/LT-3/LT-4)::an answer past the ceiling is CLAMPED and applied, never traded for a smaller fallback",
     "preferred_text_seam::preferred_text offset seam (LT-1/LT-3/LT-4)::an unknown future enum value starts from the LARGEST known fallback (over-reserve, never clip)",
     "preferred_text_seam::preferred_text offset seam (LT-1/LT-3/LT-4)::dispose drops late answers on the floor"
    ],
    "shell": "grep -q \"GetPropertyChangedSignal\" src/client/roblox_env.luau && ! grep -q \"Large = 6,\" src/client/preferred_text.luau"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "large-text-accessibility::exact-once-seam",
   "name": "exact-once-seam",
   "note": "exact-once-seam: 10 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-FID-001.",
   "phase": "large-text-accessibility",
   "releaseBlocking": false,
   "requirements": [
    "UI-FID-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "preferred_text_seam::live preference change reflows without remount (LT-2)::raising the offset re-solves in place: same node, taller text box, no creates/removes",
     "preferred_text_seam::live preference change reflows without remount (LT-2)::lowering it back restores the original geometry exactly (no residue)"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "large-text-accessibility::live-change-reflow",
   "name": "live-change-reflow",
   "note": "live-change-reflow: 2 named suite cases must pass; suite must exit zero. Requirements: UI-ENV-001.",
   "phase": "large-text-accessibility",
   "releaseBlocking": false,
   "requirements": [
    "UI-ENV-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "large_text_matrix::LT-6 policy: reflow before truncation on the controls that can reflow::LT6-ACTION a Button's action label wraps and grows WHILE wrapping can succeed, and never breaks a word",
     "large_text_layout::LT-6 order: a ViewThatFits CTA band steps down before anything is cut::LT6-BAND the chosen candidate never gets richer as the preference rises, and no action is ever cut",
     "large_text_layout::LT-6 order: a ViewThatFits CTA band steps down before anything is cut::LT6-BANDWITNESS the compact-portrait band is a row at Medium and a column at Largest (named witness)",
     "large_text_layout::LT-6 order: a ViewThatFits CTA band steps down before anything is cut::LT6-LADDER nothing is truncated while the band still has a cheaper candidate to take",
     "large_text_layout::LT-6 order: a ViewThatFits CTA band steps down before anything is cut::LT6-LADDERNEG the ladder rule FIRES on a band that cuts a label with a candidate still in hand",
     "large_text_matrix::LT-6 policy: reflow before truncation on the controls that can reflow::LT6-DISCLOSE an unbroken identity name truncates WITH a full-value path and files no finding",
     "large_text_layout::LT-6 order: a form in a ScrollView reflows, then scrolls, and never truncates::LT6-FORM the form's action pair reflows and the region scrolls — no clip at any of the 32 rows",
     "large_text_layout::LT-6 order: a Composition steps its arrangement down, never its CTA::LT6-COMPOSE the composition never picks a richer arrangement as the text grows, and cuts no action",
     "large_text_layout::LT-6 order: a Composition steps its arrangement down, never its CTA::LT6-COMPOSESCROLL the field region keeps its declared scroll path at every preference",
     "text_disclosure::LT-G1: a Toggle's one-line label discloses its full value::a truncated disclosing Toggle presents its LABEL through the same plate a Text does",
     "table::Table column disclosure (Step 8.5 LT-G2)::a disclosing column stamps disclose on its value cells AND its header title"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "large-text-accessibility::overflow-policy",
   "name": "overflow-policy",
   "note": "overflow-policy: 11 named suite cases must pass; suite must exit zero. Requirements: UI-LAYOUT-001.",
   "phase": "large-text-accessibility",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "text_audit::LTN-4 text facts: the solve reports what it truncated::the layout dump is facet-dump/2 and carries textFacts per text node",
     "text_audit::LTN-4 audit: overlaps::overlapping painted siblings are a finding naming both nodes and the fix",
     "text_audit::LTN-4 audit: overlaps::...and the finding is gone once the parent declares intentionalOverlap",
     "text_audit::LTN-4 audit: clippedEssential::truncated text with no disclose and no allowlist entry is a finding",
     "text_audit::LTN-4 audit: clippedEssential::...gone the moment the node declares disclose",
     "text_audit::LTN-4 audit: hitFloor::an interactive node painted under its declared floor is a finding",
     "text_audit::LTN-4 audit: movingText::a second moving text surface is a finding at the default maximum of one",
     "text_audit::LTN-4 audit: focusVisibility::a focused rect fully outside its clipping viewport is a finding",
     "text_audit::LTN-4 audit: focusVisibility::...gone once the viewport scrolls it back into view",
     "text_audit::LTN-4 audit: findings are ordered and frozen::two runs over equal input produce equal, deterministically ordered findings",
     "text_audit::LTN-4 audit: overlaps::a hidden subtree is audited by nobody: the losing candidate's rects are skipped",
     "text_audit::LTN-4 text facts: the solve reports what it truncated::UI.Text refuses a bound disclose, naming the prop and the rebuild idiom"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "large-text-accessibility::diagnostics-checks",
   "name": "diagnostics-checks",
   "note": "diagnostics-checks: 12 named suite cases must pass; suite must exit zero. Requirements: UI-AGENT-001.",
   "phase": "large-text-accessibility",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "large_text_matrix::LT-8 control matrix: the sweep's own coverage and mirrors::LT8-COVER every controls_registry row is swept or carries a declared reason",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Toggle: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Table: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL ViewThatFits: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: the sweep's own coverage and mirrors::LT8-MIRROR the audit's target-floor mirror equals the renderer's own hit expander",
     "large_text_matrix::LT-8 mixed scripts: CJK, emoji and latin at every preference::LT8-SCRIPTS a mixed CJK + emoji + latin line files no overlap or clip finding at any preference",
     "large_text_matrix::LT-8 mixed scripts: CJK, emoji and latin at every preference::LT8-MONOTONE every mixed-script line's box grows monotonically with the preference",
     "large_text_matrix::LT-8 mixed scripts: CJK, emoji and latin at every preference::LT8-GROWTH the Largest preference reserves a strictly taller box than Medium for the same string",
     "large_text_layout::LT-8 reduced motion: the same geometry, and a policy independent of the preference::LT8-REDUCEDSWEEP the reduced-motion form and band file no finding at any preference",
     "large_text_hot_swap::LT-2 hot swap: a live screen survives the preference swing 0 -> 14 -> 0::LTSWING-IDENTITY the swing writes rects and props only — not one create or remove",
     "large_text_hot_swap::LT-2 hot swap: a live screen survives the preference swing 0 -> 14 -> 0::LTSWING-FOCUS the focused path is the same node before, during and after the swing",
     "large_text_hot_swap::LT-2 hot swap: a live screen survives the preference swing 0 -> 14 -> 0::LTSWING-EDIT the field keeps its typed, uncommitted text and stays in edit mode",
     "large_text_hot_swap::LT-2 hot swap: a live screen survives the preference swing 0 -> 14 -> 0::LTSWING-SCROLL the list keeps its scroll offset, or is legally clamped to its new extent",
     "large_text_hot_swap::LT-2 hot swap: a live screen survives the preference swing 0 -> 14 -> 0::LTSWING-GEOMETRY every rect returns byte-identical when the preference comes back to Medium",
     "large_text_hot_swap::LT-5 composition: a theme swap and the preference compose on a live screen::LTSWAP-THEME installing Fantasy Parchment at Largest re-solves in place with no finding",
     "large_text_hot_swap::LT-5 composition: a theme swap and the preference compose on a live screen::LTSWAP-BORN a screen BORN at Largest under Parchment matches one swung there live"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "large-text-accessibility::headless-matrix",
   "name": "headless-matrix",
   "note": "headless-matrix: 16 named suite cases must pass; suite must exit zero. Requirements: UI-LAYOUT-001.",
   "phase": "large-text-accessibility",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001"
   ],
   "state": null
  },
  {
   "check": {
    "receipt": "tools/lune/verify/evidence/large-text-accessibility--studio-evidence.json"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/large-text-accessibility/studio/large-text.json",
   "id": "large-text-accessibility::studio-evidence",
   "name": "studio-evidence",
   "note": "studio-evidence: and artifacts/large-text-accessibility/studio/large-text.json must exist. Requirements: UI-FID-001.",
   "phase": "large-text-accessibility",
   "releaseBlocking": false,
   "requirements": [
    "UI-FID-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "cd ../../../games/RascalRally/code && out=\"$(cat \"$FACET_VERIFY_RR_TRANSCRIPT\")\" && echo \"$out\" | grep -q \"✓.*no painted siblings overlap ANYWHERE on the surface, on any view, at any offset\" && echo \"$out\" | grep -q \"✓.*nothing on the whole table surface is truncated without a way back to its full value\" && echo \"$out\" | grep -q \"✓.*the same surface with the disclosure withdrawn reports every name\" && echo \"$out\" | grep -q \"✓.*no row carries a position numeral — the list.s own order is the place\" && echo \"$out\" | grep -q \"✓.*both CTA labels PAINT inside their fixed boxes at every offset, on every view\" && echo \"$out\" | grep -q \"✓.*neither sub-line ellipsizes at any offset\" && echo \"$out\" | grep -q \"✓.*the longest shipped reason and a ~1.4x expansion of it read WHOLE at every offset\" && echo \"$out\" | grep -q \"✓.*whichever CTA form the composition chose reads WHOLE, both roles, every offset\" && echo \"$out\" | grep -q \"✓.*the coin TOTAL never truncates\" && echo \"$out\" | grep -q \"✓.*the standings and roll-call NAMES are the things that truncate, and they disclose\" && echo \"$out\" | grep -q \"✓.*the 667x375 fixture re-arranges rather than clipping a lane, for both roles\" && echo \"$out\" | grep -q \"✓.*NOTHING PAINTS OVER ANYTHING at any preference\" && echo \"$out\" | grep -q \"✓.*the mounted nodes, the live count, the geometry and the watched row all survive\" && echo \"$out\" | grep -q \"✓.*the race-start countdown numeral never truncates\""
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "large-text-accessibility::sponsor-fixtures",
   "name": "sponsor-fixtures",
   "note": "sponsor-fixtures: rascalrally-suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-LAYOUT-001.",
   "phase": "large-text-accessibility",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "f=../../../games/RascalRally/docs/ui/UI_SPEC_sponsor_facet.md && grep -q \"Corrected 2026-08-03, Step 8.5 / LTN-3\" \"$f\" && grep -q \"identity only, full value reachable\" \"$f\" && ! grep -q \"seat name, countdown\" \"$f\""
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "large-text-accessibility::sponsor-policy-reconciled",
   "name": "sponsor-policy-reconciled",
   "note": "sponsor-policy-reconciled: its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "large-text-accessibility",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "test -f ../../../games/RascalRally/code/tests/facet_large_text_contract.spec.luau && test -f ../../../games/RascalRally/code/tests/facet_large_text_sweep.spec.luau && cd ../../../games/RascalRally/code"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "large-text-accessibility::rascalrally-consumer",
   "name": "rascalrally-consumer",
   "note": "rascalrally-consumer: rascalrally-suite must exit zero; its file-existence and text assertions must hold; and artifacts/large-text-accessibility/consumer-impact.md must exist. Requirements: UI-AGENT-001.",
   "phase": "large-text-accessibility",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_registration_cli",
     "check_docs_cli",
     "check_boundary",
     "check_prop_parity_cli",
     "check_surface_ledger",
     "check_manifest_integrity",
     "stylua-check-check-src-tests-tools-bench-examples"
    ],
    "shell": "grep -q 'preferredTextOffset' docs/reference/api.md && grep -q 'disclose' docs/reference/api.md && grep -q 'preferred text size' docs/guide/05-styling.md"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "large-text-accessibility::registration-and-docs",
   "name": "registration-and-docs",
   "note": "registration-and-docs: check_registration_cli, check_docs_cli, check_boundary, check_prop_parity_cli, check_surface_ledger, check_manifest_integrity, stylua-check-check-src-tests-tools-bench-examples must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "large-text-accessibility",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/test.json",
   "id": "large-text-accessibility::library-suite-green",
   "name": "library-suite-green",
   "note": "library-suite-green: suite must exit zero; and artifacts/test.json must exist. Requirements: UI-AGENT-001.",
   "phase": "large-text-accessibility",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "preferred_text_seam::preferred_text offset seam (LT-1/LT-3/LT-4)::an answer that raced a preference step is DISCARDED — the A->B->A double-step cannot poison the cache",
     "text_disclosure::fresh-review closures: the plate answers for the label as it IS::a HIDDEN ViewThatFits candidate discloses nothing — its plate would show text that is not on screen",
     "text_disclosure::fresh-review closures: the plate answers for the label as it IS::the pointer dwell RE-ASKS at expiry: a re-solve that made room presents nothing",
     "text_disclosure::fresh-review closures: the plate answers for the label as it IS::a touch class arriving MID-SESSION retrofits the long-press half onto mounted labels",
     "preferred_text_seam::preferred_text offset seam (LT-1/LT-3/LT-4)::an answer past the ceiling is CLAMPED and applied, never traded for a smaller fallback",
     "table::Table column boundary is strict (architecture review F-4)::an unknown column key is refused, naming the column — a disclose typo cannot silently drop the guarantee",
     "render_target_contract::RenderTargetAdapter contract::the FakeTarget satisfies every required and optional method"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "large-text-accessibility::fresh-context-reviews",
   "name": "fresh-context-reviews",
   "note": "fresh-context-reviews: 7 named suite cases must pass; suite must exit zero. Requirements: UI-AGENT-001.",
   "phase": "large-text-accessibility",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_perf_place"
    ]
   },
   "class": "exit0",
   "evidence": null,
   "id": "performance-stress-places::place-builds-and-opens",
   "name": "place-builds-and-opens",
   "note": "place-builds-and-opens: check_perf_place must exit zero; and artifacts/performance-stress-places/place.json must exist. Requirements: UI-AGENT-001.",
   "phase": "performance-stress-places",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "perf_lab::perf lab: the dataset is deterministic (PL-2)::the same seed, count and content produce a byte-identical row set",
     "perf_lab::perf lab: the dataset is deterministic (PL-2)::a different seed produces a different row set, and the digest says so",
     "perf_lab::perf lab: the dataset is deterministic (PL-2)::each content variant is its own workload, not a relabelling of the same one",
     "perf_lab::perf lab: the dataset is deterministic (PL-2)::an unknown content variant is refused, naming it",
     "perf_lab::perf lab: selectors and workload identity (PL-2/PL-10)::every declared workload names only passes the scenario implements",
     "perf_lab::perf lab: selectors and workload identity (PL-2/PL-10)::selecting a scenario adopts its implementation and content",
     "perf_lab::perf lab: selectors and workload identity (PL-2/PL-10)::the row count is clamped to the declared ceiling rather than honoured blindly"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "performance-stress-places::deterministic-scenarios",
   "name": "deterministic-scenarios",
   "note": "deterministic-scenarios: 7 named suite cases must pass; suite must exit zero. Requirements: UI-RUNTIME-002.",
   "phase": "performance-stress-places",
   "releaseBlocking": false,
   "requirements": [
    "UI-RUNTIME-002"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "perf_lab::perf lab: the row is the shape the plan requires (PL-3)::a row carries an image, a two-line text stack, and three controls",
     "perf_lab::perf lab: the row is the shape the plan requires (PL-3)::the identity line keeps its full-value disclosure — the profile is not bought with accessibility",
     "perf_lab::perf lab: virtualization stays bounded (PL-3/PL-4)::2000 logical rows mount a window, not a collection",
     "perf_lab::perf lab: virtualization stays bounded (PL-3/PL-4)::the window stays bounded across a full programmatic scroll sweep",
     "perf_lab::perf lab: virtualization stays bounded (PL-3/PL-4)::the bound is not a bigger row count away from firing: 20 000 rows window the same",
     "perf_lab::perf lab: virtualization stays bounded (PL-3/PL-4)::NEGATIVE CONTROL: the declared window fault makes the mount FAIL, naming the defect",
     "perf_lab::perf lab: virtualization stays bounded (PL-3/PL-4)::collection churn keeps the window bounded through insert, remove and reorder",
     "perf_lab::perf lab: run, stop, reset and clean capture (PL-10/PL-11)::running a scenario executes its declared sequence and records each pass separately",
     "perf_lab::perf lab: virtualization stays bounded (PL-3/PL-4)::scrolling a virtualized list does NOT grow the reactive core (the row must own its controls)"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "performance-stress-places::dense-scroll-workload",
   "name": "dense-scroll-workload",
   "note": "dense-scroll-workload: 9 named suite cases must pass; suite must exit zero. Requirements: UI-PERF-001.",
   "phase": "performance-stress-places",
   "releaseBlocking": false,
   "requirements": [
    "UI-PERF-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite",
     "check_perf_gate_evidence-native-reference"
    ],
    "resultIds": [
     "perf_lab::perf lab: exactly one implementation mounts (PL-5)::a second mount is refused, naming the rule",
     "perf_lab::perf lab: exactly one implementation mounts (PL-5)::the native reference refuses to mount where the host seam is absent, rather than silently mounting nothing"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "performance-stress-places::native-reference-and-exclusivity",
   "name": "native-reference-and-exclusivity",
   "note": "native-reference-and-exclusivity: 2 named suite cases must pass; suite, check_perf_gate_evidence-native-reference must exit zero. Requirements: UI-PERF-002.",
   "phase": "performance-stress-places",
   "releaseBlocking": false,
   "requirements": [
    "UI-PERF-002"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_perf_gate_evidence-theme-cost"
    ]
   },
   "class": "exit0",
   "evidence": null,
   "id": "performance-stress-places::theme-cost-isolated",
   "name": "theme-cost-isolated",
   "note": "theme-cost-isolated: check_perf_gate_evidence-theme-cost must exit zero. Requirements: UI-PERF-002.",
   "phase": "performance-stress-places",
   "releaseBlocking": false,
   "requirements": [
    "UI-PERF-002"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_perf_gate_evidence-large-text",
     "suite"
    ],
    "resultIds": [
     "perf_lab::perf lab: the dataset is deterministic (PL-2)::each content variant is its own workload, not a relabelling of the same one"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "performance-stress-places::large-text-workload",
   "name": "large-text-workload",
   "note": "large-text-workload: 1 named suite case must pass; check_perf_gate_evidence-large-text, suite must exit zero. Requirements: UI-LAYOUT-001.",
   "phase": "performance-stress-places",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite",
     "check_perf_gate_evidence-scopes"
    ],
    "resultIds": [
     "profile_scopes::profile scopes are a closed, bounded set::every declared label is prefixed so LibMP can select exactly this set",
     "profile_scopes::profile scopes are a closed, bounded set::an undeclared scope name is refused at the call, naming the offender",
     "profile_scopes::profile scopes are a closed, bounded set::refusing an undeclared name opens no scope at all",
     "profile_scopes::a span is balanced on every exit path::an ERRORING body still closes the scope, and the error value is unchanged",
     "profile_scopes::a span is balanced on every exit path::a non-string error value survives the span unchanged",
     "profile_scopes::a span is balanced on every exit path::nested spans unwind in order when the INNER one throws",
     "profile_scopes::a real render pass emits the phase set and stays balanced::the number of scopes a frame opens does NOT scale with the row count",
     "profile_scopes::a real render pass emits the phase set and stays balanced::mount -> solve -> commit produces only declared labels, and closes them all"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "performance-stress-places::microprofiler-scopes",
   "name": "microprofiler-scopes",
   "note": "microprofiler-scopes: 8 named suite cases must pass; suite, check_perf_gate_evidence-scopes must exit zero. Requirements: UI-PERF-001.",
   "phase": "performance-stress-places",
   "releaseBlocking": false,
   "requirements": [
    "UI-PERF-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_perf_captures"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/performance-stress-places/captures.json",
   "id": "performance-stress-places::capture-metadata",
   "name": "capture-metadata",
   "note": "capture-metadata: check_perf_captures must exit zero; and artifacts/performance-stress-places/captures.json must exist. Requirements: UI-AGENT-001.",
   "phase": "performance-stress-places",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite",
     "check_perf_gate_evidence-studio"
    ],
    "resultIds": [
     "perf_lab::perf lab: run, stop, reset and clean capture (PL-10/PL-11)::the emergency stop halts a running sequence between passes",
     "perf_lab::perf lab: run, stop, reset and clean capture (PL-10/PL-11)::the ramp climbs its declared ladder and stops when told",
     "perf_lab::perf lab: the dataset is deterministic (PL-2)::the ramp ladder is bounded by a declared ceiling (PL-11)",
     "perf_lab::perf lab: run, stop, reset and clean capture (PL-10/PL-11)::reset returns to the idle baseline and PROVES the workload is unmounted",
     "perf_lab::perf lab: run, stop, reset and clean capture (PL-10/PL-11)::clean capture DISMISSES the overlay rather than hiding it"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "performance-stress-places::overlay-and-ramp",
   "name": "overlay-and-ramp",
   "note": "overlay-and-ramp: 5 named suite cases must pass; suite, check_perf_gate_evidence-studio must exit zero. Requirements: UI-AGENT-001.",
   "phase": "performance-stress-places",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_perf_gate_evidence-headless-linkage"
    ]
   },
   "class": "exit0",
   "evidence": null,
   "id": "performance-stress-places::headless-linkage",
   "name": "headless-linkage",
   "note": "headless-linkage: check_perf_gate_evidence-headless-linkage must exit zero. Requirements: UI-PERF-001.",
   "phase": "performance-stress-places",
   "releaseBlocking": false,
   "requirements": [
    "UI-PERF-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_perf_gate_evidence-studio"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/performance-stress-places/studio/perf-lab.json",
   "id": "performance-stress-places::studio-evidence",
   "name": "studio-evidence",
   "note": "studio-evidence: check_perf_gate_evidence-studio must exit zero; and artifacts/performance-stress-places/studio/perf-lab.json must exist. Requirements: UI-FID-001.",
   "phase": "performance-stress-places",
   "releaseBlocking": false,
   "requirements": [
    "UI-FID-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_perf_gate_evidence-device-matrix"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/performance-stress-places/studio/device-matrix.json",
   "id": "performance-stress-places::device-matrix-smoke",
   "name": "device-matrix-smoke",
   "note": "device-matrix-smoke: check_perf_gate_evidence-device-matrix must exit zero; and artifacts/performance-stress-places/studio/device-matrix.json must exist. Requirements: UI-LAYOUT-001.",
   "phase": "performance-stress-places",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_perf_gate_evidence-falsifiable"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/performance-stress-places/prove-perf-gate.json",
   "id": "performance-stress-places::perf-gate-falsifiable",
   "name": "perf-gate-falsifiable",
   "note": "perf-gate-falsifiable: check_perf_gate_evidence-falsifiable must exit zero; and artifacts/performance-stress-places/prove-perf-gate.json must exist. Requirements: UI-PERF-001.",
   "phase": "performance-stress-places",
   "releaseBlocking": false,
   "requirements": [
    "UI-PERF-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_perf_gate_evidence-budgets"
    ],
    "shell": "grep -q \"measureUncached\" src/layout/solver.luau && grep -q \"measureCache\" src/layout/solver.luau"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "performance-stress-places::optimization-loop-recorded",
   "name": "optimization-loop-recorded",
   "note": "optimization-loop-recorded: check_perf_gate_evidence-budgets must exit zero; its file-existence and text assertions must hold; and artifacts/performance-stress-places/optimization-log.md must exist. Requirements: UI-PERF-001.",
   "phase": "performance-stress-places",
   "releaseBlocking": false,
   "requirements": [
    "UI-PERF-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "cd ../../../games/RascalRally/code"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "performance-stress-places::rascalrally-consumer",
   "name": "rascalrally-consumer",
   "note": "rascalrally-consumer: rascalrally-suite must exit zero; its file-existence and text assertions must hold; and artifacts/performance-stress-places/consumer-impact.md must exist. Requirements: UI-AGENT-001.",
   "phase": "performance-stress-places",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_registration_cli",
     "check_docs_cli",
     "check_boundary",
     "check_prop_parity_cli",
     "check_surface_ledger",
     "check_manifest_integrity",
     "stylua-check-check-src-tests-tools-bench-examples"
    ],
    "shell": "test -f docs/guide/12-performance-lab.md && grep -q '12-performance-lab.md' docs/guide/README.md && grep -q 'Publish to Roblox' docs/guide/12-performance-lab.md && grep -q 'MicroProfiler' docs/guide/12-performance-lab.md && grep -q 'phone-physical' docs/guide/12-performance-lab.md"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "performance-stress-places::registration-and-docs",
   "name": "registration-and-docs",
   "note": "registration-and-docs: check_registration_cli, check_docs_cli, check_boundary, check_prop_parity_cli, check_surface_ledger, check_manifest_integrity, stylua-check-check-src-tests-tools-bench-examples must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "performance-stress-places",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/test.json",
   "id": "performance-stress-places::library-suite-green",
   "name": "library-suite-green",
   "note": "library-suite-green: suite must exit zero; and artifacts/test.json must exist. Requirements: UI-AGENT-001.",
   "phase": "performance-stress-places",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "perf",
     "check_perf_gate_evidence-perf-gate"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/phase-4/perf.json",
   "id": "performance-stress-places::existing-perf-gate",
   "name": "existing-perf-gate",
   "note": "existing-perf-gate: perf, check_perf_gate_evidence-perf-gate must exit zero; and artifacts/phase-4/perf.json must exist. Requirements: UI-PERF-001.",
   "phase": "performance-stress-places",
   "releaseBlocking": false,
   "requirements": [
    "UI-PERF-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_perf_gate_evidence-prior-gates"
    ]
   },
   "class": "exit0",
   "evidence": null,
   "id": "performance-stress-places::prior-gates-unregressed",
   "name": "prior-gates-unregressed",
   "note": "prior-gates-unregressed: check_perf_gate_evidence-prior-gates must exit zero; and artifacts/performance-stress-places/prior-gates.txt must exist. Requirements: UI-AGENT-001.",
   "phase": "performance-stress-places",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_perf_gate_evidence-budgets"
    ]
   },
   "class": "exit0",
   "evidence": null,
   "id": "performance-stress-places::physical-rows",
   "name": "physical-rows",
   "note": "physical-rows: check_perf_gate_evidence-budgets must exit zero; and artifacts/performance-stress-places/review-packet.md must exist. Requirements: UI-PERF-002.",
   "phase": "performance-stress-places",
   "releaseBlocking": false,
   "requirements": [
    "UI-PERF-002"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_example_drift_cli",
     "suite"
    ],
    "resultIds": [
     "example_drift::no tutorial example bypasses property authority::the seven examples are clean",
     "example_drift::no tutorial example bypasses property authority::it actually scanned the examples, and saw real semantic values there",
     "example_drift::the example drift lint bites::R2: an INVENTED surface token — the defect that made the word-game board invisible",
     "example_drift::the example drift lint bites::R2 does NOT fire on the legal vocabularies",
     "example_drift::the example drift lint bites::R3: a raw colour, in each of its three spellings",
     "example_drift::the example drift lint bites::R4: a platform branch is refused by name",
     "example_drift::the example drift lint bites::the allowlist suppresses ONLY its own line, in its own file"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/example-quality-pass/drift.json",
   "id": "example-quality-pass::style-authority-and-drift",
   "name": "style-authority-and-drift",
   "note": "style-authority-and-drift: 7 named suite cases must pass; check_example_drift_cli, suite must exit zero; and artifacts/example-quality-pass/drift.json must exist. Requirements: UI-THEME-001.",
   "phase": "example-quality-pass",
   "releaseBlocking": false,
   "requirements": [
    "UI-THEME-001"
   ],
   "state": null
  },
  {
   "check": {
    "receipt": "tools/lune/verify/evidence/example-quality-pass--theme-package-swap.json"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/example-quality-pass/captures.json",
   "id": "example-quality-pass::theme-package-swap",
   "name": "theme-package-swap",
   "note": "theme-package-swap: and artifacts/example-quality-pass/captures.json must exist. Requirements: UI-THEME-001.",
   "phase": "example-quality-pass",
   "releaseBlocking": false,
   "requirements": [
    "UI-THEME-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite",
     "check_eq6_evidence"
    ],
    "resultIds": [
     "table::Table: toolbar, header and body stack at every text preference::the solver files NO overflow anywhere in the table's chrome, at any preference",
     "table::Table: toolbar, header and body stack at every text preference::the toolbar, the header and the body never overlap — the reported defect",
     "table::Table: toolbar, header and body stack at every text preference::the header band GROWS with the preference instead of holding its token"
    ],
    "shell": "grep -qE \"minMax., min = .controls[.]table[.]headerHeight\" src/controls/table_header.luau && grep -qE \"reorderable = true\" tests/lib/large_text_fixtures.luau"
   },
   "class": "result-ids",
   "evidence": "artifacts/example-quality-pass/studio/large-text.json",
   "id": "example-quality-pass::large-text-overflow",
   "name": "large-text-overflow",
   "note": "large-text-overflow: 3 named suite cases must pass; suite, check_eq6_evidence must exit zero; its file-existence and text assertions must hold; and artifacts/example-quality-pass/studio/large-text.json must exist. Requirements: UI-LAYOUT-001.",
   "phase": "example-quality-pass",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "examples_gallery::examples/gallery 04: settings sync::the seven promised states are reachable through on-screen controls alone",
     "examples_gallery::examples/gallery 04: settings sync::a valid toggle applies optimistically, goes pending, then confirms",
     "examples_gallery::examples/gallery 04: settings sync::a rejected change restores the optimistic draft to server truth",
     "examples_gallery::examples/gallery 04: settings sync::reset recovers from a request the server never answered",
     "examples_gallery::examples/gallery 04: settings sync::the visible history reads newest first and is bounded"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/example-quality-pass/studio/ex03.json",
   "id": "example-quality-pass::example-03-optimistic-sync",
   "name": "example-03-optimistic-sync",
   "note": "example-03-optimistic-sync: 5 named suite cases must pass; suite must exit zero; and artifacts/example-quality-pass/studio/ex03.json must exist. Requirements: UI-REPL-001.",
   "phase": "example-quality-pass",
   "releaseBlocking": false,
   "requirements": [
    "UI-REPL-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "example_word_game::examples/05 word game — the pure scoring rules::a guess with a DOUBLED letter against a solution holding ONE spends the budget once",
     "example_word_game::examples/05 word game — the pure scoring rules::an EXACT match consumes the budget BEFORE an earlier misplaced copy of the same letter",
     "example_word_game::examples/05 word game — the pure scoring rules::three copies against two: the excess is absent",
     "example_word_game::examples/05 word game — validation and the two generated sets::refuses a guess shorter than five letters"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "example-quality-pass::example-05-word-game",
   "name": "example-05-word-game",
   "note": "example-05-word-game: 4 named suite cases must pass; suite must exit zero. Requirements: UI-AGENT-001.",
   "phase": "example-quality-pass",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "receipt": "tools/lune/verify/evidence/example-quality-pass--example-05-native-input.json"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/example-quality-pass/studio/ex05.json",
   "id": "example-quality-pass::example-05-native-input",
   "name": "example-05-native-input",
   "note": "example-05-native-input: and artifacts/example-quality-pass/studio/ex05.json must exist. Requirements: UI-INPUT-001.",
   "phase": "example-quality-pass",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "examples_games::examples/07 tile game (Scrabble-style)::placing with nothing picked up says so instead of doing nothing",
     "example_tile_game::examples/06 crossword — nine refusals, nine sentences, on screen::8 — placing on an occupied square",
     "example_tile_game::examples/06 crossword — committed versus uncommitted is not a colour::a spent rack slot is DISABLED, not a live-looking blank button",
     "example_tile_game::examples/06 crossword — both endings, and the way back::reaching the goal wins, and the screen says which ending it was",
     "example_tile_game::examples/06 crossword — both endings, and the way back::spending the budget without reaching the goal loses, and says so",
     "examples_games::examples/08 match-3::opens with no run already on the board",
     "examples_games::examples/08 match-3::opens with at least one legal move",
     "examples_games::examples/08 match-3::a swap that makes no run is refused WITH A REASON, and costs no move",
     "examples_games::examples/08 match-3::a FAILED load is visible and recoverable, which is the half that was missing"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/example-quality-pass/studio/games.json",
   "id": "example-quality-pass::example-06-07-gameplay",
   "name": "example-06-07-gameplay",
   "note": "example-06-07-gameplay: 9 named suite cases must pass; suite must exit zero; and artifacts/example-quality-pass/studio/games.json must exist. Requirements: UI-AGENT-001.",
   "phase": "example-quality-pass",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "example_tile_game::examples/06 crossword — robustness::rapid alternating input leaves no stale selection or held tile",
     "examples_games::examples/08 match-3::rapid tapping and resetting leaves no stale selection or error",
     "example_tile_game::examples/06 crossword — both endings, and the way back::restart returns EVERY observable to its seeded start",
     "examples_games::examples/05 word game (Wordle-style)::tears down to a registry baseline after dispose",
     "examples_games::examples/07 tile game (Scrabble-style)::tears down to a registry baseline after dispose",
     "examples_games::examples/08 match-3::tears down to a registry baseline after dispose",
     "example_word_game::examples/05 word game — the state machine::tears down to a registry baseline after dispose",
     "example_tile_game::examples/06 crossword — robustness::tears down to a registry baseline after dispose"
    ]
   },
   "class": "result-ids",
   "evidence": "artifacts/example-quality-pass/studio/lifecycle.json",
   "id": "example-quality-pass::lifecycle-and-rapid-input",
   "name": "lifecycle-and-rapid-input",
   "note": "lifecycle-and-rapid-input: 8 named suite cases must pass; suite must exit zero; and artifacts/example-quality-pass/studio/lifecycle.json must exist. Requirements: UI-RUNTIME-002.",
   "phase": "example-quality-pass",
   "releaseBlocking": false,
   "requirements": [
    "UI-RUNTIME-002"
   ],
   "state": null
  },
  {
   "check": {
    "receipt": "tools/lune/verify/evidence/example-quality-pass--device-matrix.json"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/example-quality-pass/studio/device-matrix.json",
   "id": "example-quality-pass::device-matrix",
   "name": "device-matrix",
   "note": "device-matrix: and artifacts/example-quality-pass/studio/device-matrix.json must exist. Requirements: UI-LAYOUT-001.",
   "phase": "example-quality-pass",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_docs_cli"
    ],
    "shell": "test \"$(ls examples/places/*.rbxl | wc -l | tr -d ' ')\" -ge 10 && grep -q \"Refusal is feedback\" docs/guide/04-tutorial-examples.md && grep -q \"Who drains the provider\" docs/guide/04-tutorial-examples.md && grep -q \"The answer has to be visible\" docs/guide/04-tutorial-examples.md && ! grep -qE \"sizeClass == .compact. then 40\" docs/guide/04-tutorial-examples.md"
   },
   "class": "evidence-pin",
   "evidence": "artifacts/example-quality-pass/places.json",
   "id": "example-quality-pass::places-and-docs",
   "name": "places-and-docs",
   "note": "places-and-docs: check_docs_cli must exit zero; its file-existence and text assertions must hold; and artifacts/example-quality-pass/places.json must exist. Requirements: UI-AGENT-001.",
   "phase": "example-quality-pass",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/test.json",
   "id": "example-quality-pass::library-suite-green",
   "name": "library-suite-green",
   "note": "library-suite-green: suite must exit zero; and artifacts/test.json must exist. Requirements: UI-AGENT-001.",
   "phase": "example-quality-pass",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "priorPhases": true
   },
   "class": "prior-phases",
   "evidence": null,
   "id": "example-quality-pass::prior-gates-unregressed",
   "name": "prior-gates-unregressed",
   "note": "prior-gates-unregressed: every row of every earlier phase must pass in this same run; and artifacts/example-quality-pass/prior-gates.txt must exist. Requirements: UI-AGENT-001.",
   "phase": "example-quality-pass",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "cd ../../../games/RascalRally/code"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "example-quality-pass::rascalrally-consumer",
   "name": "rascalrally-consumer",
   "note": "rascalrally-consumer: rascalrally-suite must exit zero; its file-existence and text assertions must hold; and artifacts/example-quality-pass/consumer-impact.md must exist. Requirements: UI-AGENT-001.",
   "phase": "example-quality-pass",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "example-quality-pass::physical-and-human-rows",
   "name": "physical-and-human-rows",
   "note": "physical-and-human-rows: and artifacts/example-quality-pass/review-packet.md must exist. Requirements: UI-INPUT-001.",
   "phase": "example-quality-pass",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {
    "shell": "! python3 -c \"import sys, importlib.util, pathlib; s = importlib.util.spec_from_file_location('g', 'tools/check_brand_drift.py'); m = importlib.util.module_from_spec(s); s.loader.exec_module(m); sys.exit(0 if any(m.VENDOR.search(l) and 'artifacts/' not in l for p in pathlib.Path('examples/reference').rglob('*.luau') for l in p.read_text().splitlines()) else 1)\""
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "reference-app-validation::source-inspection-and-ip-boundary",
   "name": "source-inspection-and-ip-boundary",
   "note": "source-inspection-and-ip-boundary: its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "reference-app-validation",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_example_drift_cli"
    ],
    "shell": "! { grep -rnE \"Instance[.]new|GetService[(]|UserInputService|os[.]clock|os[.]time|math[.]random\" examples/reference/ tests/fixtures/retired/ --include=\"*.luau\" | grep -vE \":[0-9]+:[[:space:]]*--\" | grep -q .; }"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "reference-app-validation::responsibility-ledger",
   "name": "responsibility-ledger",
   "note": "responsibility-ledger: check_example_drift_cli must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "reference-app-validation",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "reference/glade_spec::Glade: supply drain, low and empty (spec §1, §6)::a refill stamps the clock, restores the level, and the ring drains again",
     "reference/glade_spec::Glade S3/S4: the purchase command's four phases (spec §9)::idle → pending → confirmed: the button says Confirming…, then the count counts up",
     "reference/glade_spec::Glade S3/S4: the purchase command's four phases (spec §9)::the second attempt is the scripted `declined` rejection, with its copy under the card",
     "reference/glade_spec::Glade S3/S4: the purchase command's four phases (spec §9)::every declared rejection reason has player-facing copy, including the fallback",
     "reference/glade_spec::Glade S6/S7: wisp info, restore, the edit form and Fresh Start (spec §10)::restore runs the same three-phase command and toasts on confirm"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "reference-app-validation::proof-garden-loop",
   "name": "proof-garden-loop",
   "note": "proof-garden-loop: 5 named suite cases must pass; suite must exit zero. Requirements: UI-AGENT-001.",
   "phase": "reference-app-validation",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "reference/cartwheel_spec::Cartwheel — the status machine::THE COUNTDOWN SURVIVES NAVIGATION (the service owns the fact, not the view)",
     "reference/cartwheel_spec::Cartwheel — the shell::SELECTION RESETS PUSHED DETAIL: a sidebar change lands on the section root",
     "reference/cartwheel_spec::Cartwheel — entitlement, gate and lock::BOTH purchase outcomes are reachable: the first declines, the second confirms",
     "reference/cartwheel_spec::Cartwheel — scripted arrivals::arrive in id order at seeded intervals and then stop",
     "reference/cartwheel_spec::Cartwheel — the status machine::collecting an already-collected brew is REJECTED, with copy, and reverts"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "reference-app-validation::proof-dashboard-loop",
   "name": "proof-dashboard-loop",
   "note": "proof-dashboard-loop: 5 named suite cases must pass; suite must exit zero. Requirements: UI-AGENT-001.",
   "phase": "reference-app-validation",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "reference/sipworks_spec::Sipworks §8 — the order flow: guard, rejection, confirmation, ready::the CTA runs idle -> pending -> rejected, shows a toast, and returns to idle",
     "reference/sipworks_spec::Sipworks §8 — the order flow: guard, rejection, confirmation, ready::the second order confirms, presents Order-Placed, and the disc flips at t+4s",
     "reference/sipworks_spec::Sipworks §9 — Steam Stamps: accrual, the once-per-visit pop, redemption::at ten stamps the CTA is the redeem verb, and redeeming debits a flat ten",
     "reference/sipworks_spec::Sipworks §10 — the Blend Book unlock and the recipe view::locked rows are ABSENT until the purchase confirms, then they enter the list",
     "reference/sipworks_spec::Sipworks §13 — localization: expansion, plurals, lists, measures::the pseudo-locale expands every string by at least 1.4x and keeps placeholders",
     "reference/sipworks_spec::Sipworks §13 — localization: expansion, plurals, lists, measures::the locale step reflows every mounted surface with no remount"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "reference-app-validation::proof-catalog-loop",
   "name": "proof-catalog-loop",
   "note": "proof-catalog-loop: 6 named suite cases must pass; suite must exit zero. Requirements: UI-AGENT-001.",
   "phase": "reference-app-validation",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "reference/foyer_spec::Foyer proof — mount and structure (RA-P4)::a size-class flip swaps chrome but never remounts the home body",
     "reference/foyer_spec::Foyer proof — tabs, search, stubs::the charts tab swaps the feed to the approval-ranked section and back",
     "reference/foyer_spec::Foyer proof — refresh command lifecycle::refresh pends (button disabled), then confirms with re-seeded sections",
     "reference/foyer_spec::Foyer proof — tabs, search, stubs::a no-match query shows the honest empty state with a working clear button",
     "reference/glade_spec::Glade: the scenario surface::the five-view sweep is diagnostic-clean with a live scrollbar (matrix pin)",
     "reference/cartwheel_spec::Cartwheel — reduced motion::the five-view sweep is diagnostic-clean with a live scrollbar (matrix pin)",
     "reference/sipworks_spec::Sipworks — reduced motion, themes, and reachability::the five-view sweep is diagnostic-clean with a live scrollbar (matrix pin)",
     "reference/foyer_spec::Foyer proof — notices, locale, images, determinism::the five-view sweep is diagnostic-clean with a live scrollbar (matrix pin)"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "reference-app-validation::proof-discovery-home-loop",
   "name": "proof-discovery-home-loop",
   "note": "proof-discovery-home-loop: 8 named suite cases must pass; suite must exit zero. Requirements: UI-AGENT-001.",
   "phase": "reference-app-validation",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "reference/wardrobe_spec::Wardrobe proof — mount, stage seam, arrangement (RA-P5)::the stage host is live headlessly (recording stub): lighting + camera recorded, fallback closed",
     "reference/wardrobe_spec::Wardrobe proof — mount, stage seam, arrangement (RA-P5)::wide solves the split arrangement; a phone box solves stacked — and worn state survives the flip",
     "reference/wardrobe_spec::Wardrobe proof — try-on and history::undo/redo walk the equip history and disable at the stack ends",
     "reference/wardrobe_spec::Wardrobe proof — the purchase lifecycle::insufficient Sparks rejects with a visible reason; the wallet never moves; retry stays allowed",
     "reference/wardrobe_spec::Wardrobe proof — the purchase lifecycle::sold-out rejects once, then a retry confirms: balance debits, chip flips to Owned+Wearing, modal dismisses, BuyBar leaves"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "reference-app-validation::proof-avatar-editor-loop",
   "name": "proof-avatar-editor-loop",
   "note": "proof-avatar-editor-loop: 5 named suite cases must pass; suite must exit zero. Requirements: UI-AGENT-001.",
   "phase": "reference-app-validation",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "receipt": "tools/lune/verify/evidence/reference-app-validation--device-matrix.json"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "reference-app-validation::device-matrix",
   "name": "device-matrix",
   "note": "device-matrix: declared evidence, recorded rather than executed. Requirements: UI-LAYOUT-001.",
   "phase": "reference-app-validation",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001"
   ],
   "state": null
  },
  {
   "check": {
    "receipt": "tools/lune/verify/evidence/reference-app-validation--fixture-axes.json"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "reference-app-validation::fixture-axes",
   "name": "fixture-axes",
   "note": "fixture-axes: declared evidence, recorded rather than executed. Requirements: UI-THEME-001.",
   "phase": "reference-app-validation",
   "releaseBlocking": false,
   "requirements": [
    "UI-THEME-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "build_reference_places"
    ],
    "shell": "python3 -c \"import os; built={'Facet-Ref-Glade','Facet-Ref-Cartwheel','Facet-Ref-Sipworks','Facet-Ref-Foyer'}; assert all(os.path.getsize(f'examples/places/{n}.rbxl')>200000 for n in built); assert not os.path.exists('examples/places/Facet-Ref-Wardrobe.rbxl'), 'retired: a publishable Wardrobe place is back'\""
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "reference-app-validation::places-and-builds",
   "name": "places-and-builds",
   "note": "places-and-builds: build_reference_places must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "reference-app-validation",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "grid_measure_arrange::grid measure/arrange agreement::a grid of content buttons in a hug stack reports no overflow (fixed thumb)",
     "scroll_bar_measure::ScrollView measure includes the bar its arrange reserves::an overflowing hug x-shelf reports zero diagnostics with a live bar",
     "icon_ns_glyph::namespaced icon ASCII floor::derives the glyph from the name after the namespace",
     "instance_park_corpse::instance park corpse: the pool can never hold a destroyed instance::parkEligible refuses an unparented (destroyed) instance",
     "instance_park_corpse::instance park corpse: the pool can never hold a destroyed instance::adopt refuses (never throws) when the Parent write fails",
     "zstack_fill_diagnostic::zstack overflow diagnostic: fill axes are granted, not measured::a fill-size page scroller taller than the stack is NOT an overflow",
     "zstack_fill_diagnostic::zstack overflow diagnostic: fill axes are granted, not measured::NEGATIVE CONTROL: a fill-WIDTH child still reports its oversize HEIGHT",
     "stage::UI.Stage: the stageHost content seam (c)::hands back ONE cached handle per node",
     "foreign::UI.Foreign: the foreignHost content seam (c)::hands back ONE cached handle per node",
     "stage::UI.Stage: the stageHost content seam (c)::setCamera records position/lookAt/fov in the exact table shape"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "reference-app-validation::framework-fixes-tested",
   "name": "framework-fixes-tested",
   "note": "framework-fixes-tested: 10 named suite cases must pass; suite must exit zero. Requirements: UI-AGENT-001.",
   "phase": "reference-app-validation",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/test.json",
   "id": "reference-app-validation::library-suite-green",
   "name": "library-suite-green",
   "note": "library-suite-green: suite must exit zero; and artifacts/test.json must exist. Requirements: UI-AGENT-001.",
   "phase": "reference-app-validation",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "priorPhases": true
   },
   "class": "prior-phases",
   "evidence": null,
   "id": "reference-app-validation::prior-gates-unregressed",
   "name": "prior-gates-unregressed",
   "note": "prior-gates-unregressed: every row of every earlier phase must pass in this same run. Requirements: UI-AGENT-001.",
   "phase": "reference-app-validation",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "cd ../../../games/RascalRally/code"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "reference-app-validation::rascalrally-consumer",
   "name": "rascalrally-consumer",
   "note": "rascalrally-consumer: rascalrally-suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "reference-app-validation",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_docs_cli",
     "check_registration_cli"
    ],
    "shell": "grep -qF \"UI.Stage\" docs/reference/api.md"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "reference-app-validation::docs-updated",
   "name": "docs-updated",
   "note": "docs-updated: check_docs_cli, check_registration_cli must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "reference-app-validation",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "row_actions_input::RowActions pointer gesture (Task 5: axis lock, drag, flick, commit)::full swipe commits (destructive): row slides off, collapses, onAction fires exactly once",
     "row_actions_input::RowActions touch gesture (Task 6: touch accepted, same axis-lock dance as mouse)::TOUCH: full swipe commits (destructive): row slides off, collapses, onAction fires exactly once",
     "row_actions_input::RowActions Task 7: open-state coordinator + outside-tap/scroll dismissal::opening row B closes row A (one-open policy)",
     "row_actions_input::RowActions pointer gesture (Task 5: axis lock, drag, flick, commit)::vertical drag never moves the offset (axis lock)",
     "row_actions_input::RowActions redteam item 2: the menu contributes zero to row/list measure::opening row A's menu leaves row B's solved rect (y, x, height) byte-identical — VStack list",
     "row_actions_input::RowActions Task 8: keyboard Delete::Delete on a focused row fires the first destructive action",
     "table::Table: row actions (Task 10)::tap-to-close: a tap on a row's own content while ITS tray is open closes it — no select, no activate",
     "virtual_list_row_actions::VirtualList rowActions (Task 5): behavior parity::(P9) focus TRAVERSAL: the d-pad reaches the revealed tray, and the ring comes BACK to the row",
     "virtual_list_row_actions::VirtualList rowActions (Task 5): behavior parity::(P10) the ACTIVATE SEAM closes the open row: Return on ANOTHER row puts the tray away",
     "virtual_list_row_actions::VirtualList rowActions (Task 5): behavior parity::(P2b) REGRESSION: every later short swipe on the SAME row also snaps back (the reused spring)",
     "virtual_list_row_actions_identity::VirtualList rowActions: mount identity (the shipping differential)::a hosted list's mounted paths are the plain list's PLUS exactly the one overlay node",
     "virtual_list_row_actions_identity::VirtualList rowActions: mount identity (the shipping differential)::engaging and disengaging returns the tree to the closed shape, with zero instance leaks"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "row-actions::row-actions-suite",
   "name": "row-actions-suite",
   "note": "row-actions-suite: 12 named suite cases must pass; suite must exit zero. Requirements: ROW-ACTIONS-001.",
   "phase": "row-actions",
   "releaseBlocking": false,
   "requirements": [
    "ROW-ACTIONS-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_row_actions_matrix"
    ],
    "receipt": "tools/lune/verify/evidence/row-actions--row-actions-device-matrix.json"
   },
   "class": "exit0",
   "evidence": null,
   "id": "row-actions::row-actions-device-matrix",
   "name": "row-actions-device-matrix",
   "note": "row-actions-device-matrix: check_row_actions_matrix must exit zero; and artifacts/row-actions/device-matrix.md must exist. Requirements: ROW-ACTIONS-001.",
   "phase": "row-actions",
   "releaseBlocking": false,
   "requirements": [
    "ROW-ACTIONS-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/test.json",
   "id": "row-actions::library-suite-green",
   "name": "library-suite-green",
   "note": "library-suite-green: suite must exit zero; and artifacts/test.json must exist. Requirements: ROW-ACTIONS-001, UI-AGENT-001.",
   "phase": "row-actions",
   "releaseBlocking": false,
   "requirements": [
    "ROW-ACTIONS-001",
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "test -f ../../../games/RascalRally/code/tests/facet_sponsor_table.spec.luau && cd ../../../games/RascalRally/code"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "row-actions::rascalrally-consumer",
   "name": "rascalrally-consumer",
   "note": "rascalrally-consumer: rascalrally-suite must exit zero; its file-existence and text assertions must hold; and artifacts/row-actions/consumer-impact.md must exist. Requirements: ROW-ACTIONS-001, UI-AGENT-001.",
   "phase": "row-actions",
   "releaseBlocking": false,
   "requirements": [
    "ROW-ACTIONS-001",
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "priorPhases": true
   },
   "class": "prior-phases",
   "evidence": null,
   "id": "row-actions::prior-gates-unregressed",
   "name": "prior-gates-unregressed",
   "note": "prior-gates-unregressed: every row of every earlier phase must pass in this same run; and artifacts/row-actions/prior-gates-rerun.txt must exist. Requirements: UI-AGENT-001.",
   "phase": "row-actions",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/test.json",
   "id": "parity-round-2::library-suite-green",
   "name": "library-suite-green",
   "note": "library-suite-green: suite must exit zero; and artifacts/test.json must exist. Requirements: UI-AGENT-001.",
   "phase": "parity-round-2",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "with_animation::withAnimation: the solver stays exact and instant::the solved rect is the NEW one on the frame the call returns",
     "with_animation::withAnimation: the solver stays exact and instant::the PAINTED position starts at the old place and travels to the new one",
     "with_animation::withAnimation: records are relative, and only at animation roots::a child riding its moved parent installs NO record of its own",
     "with_animation::withAnimation: interruption carries velocity::a second call mid-flight re-targets instead of restarting from rest",
     "with_animation::withAnimation: a path that did not exist before::a node that APPEARS inside the call installs no record of its own",
     "with_animation::withAnimation: a path that did not exist before::the sibling it displaced DOES fly, so the case is not vacuous",
     "with_animation::withAnimation: reduced motion installs nothing at all::the layout is exact and there is no flight",
     "with_animation::withAnimation: refusals::a call inside an already-open transaction is refused, not silently inert"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-2::with-animation",
   "name": "with-animation",
   "note": "with-animation: 8 named suite cases must pass; suite must exit zero. Requirements: UI-MOTION-001, UI-AGENT-001.",
   "phase": "parity-round-2",
   "releaseBlocking": false,
   "requirements": [
    "UI-MOTION-001",
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "stack_distribution::§2.4 shrink — the outer sort (`layoutPriority`)::the lowest tier absorbs the whole deficit before the next tier gives a pixel",
     "stack_distribution::§2.4 shrink — the inner distribution (`shrinkWeight`)::the weight is MULTIPLIED BY THE BASIS, so the small child is not collapsed first",
     "stack_distribution::§2.4 shrink — the floor cascade: minMax.min, longest word, compact form, ellipsis, clip::the measure MEMO tells the two offers apart — the cache key already covers the pass",
     "stack_distribution::§2.6 distribute — the three flex distributions::a vertical stack distributes along Y, not X",
     "stack_distribution::§2.1 lineAlign — per-child cross-axis alignment::`center` centres just that child in the line",
     "grid_row::row mode: the columns come from every row at once::column n is as wide as the widest natural cell in column n",
     "grid_row::row mode: a span covers columns, and widens none of them::a spanning cell contributes to NO single column's maximum",
     "grid_row::the two authoring errors row mode can produce::a GridRow outside a Grid names the fix instead of inventing columns",
     "container_relative_frame::the container is the nearest viewport, not the parent::the NEAREST scroller wins, not the outermost",
     "container_relative_frame::the container is the nearest viewport, not the parent::a fraction ignores the wrappers between the view and the scroller",
     "container_relative_frame::the measure memo tells two containers apart::the same node at the same offer under two containers gets two answers"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-2::layout-vocabulary",
   "name": "layout-vocabulary",
   "note": "layout-vocabulary: 11 named suite cases must pass; suite must exit zero. Requirements: UI-LAYOUT-001.",
   "phase": "parity-round-2",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "display_controls::ProgressView: indeterminate is selected by value = nil (§3.1)::the DETERMINATE tree and dump are exactly what they were before indeterminate existed",
     "display_controls::ProgressView: indeterminate is selected by value = nil (§3.1)::REDUCED MOTION keeps the indicator advancing — quantized, never frozen (§3.1)",
     "display_controls::ProgressView: indeterminate is selected by value = nil (§3.1)::FULL motion writes far more often than reduced motion over the same wall clock",
     "display_controls::ProgressView: the refusals that keep the two modes from contradicting::refuses a spinner with a value — and names the shape that DOES draw one",
     "sensory_feedback::UI.sensoryFeedback: the declaration is ruled on at the call site::refuses an event outside the closed twelve, listing the vocabulary",
     "sensory_feedback::UI.sensoryFeedback: what reaches the bus::emits the declared verb with the mounted path when the trigger changes",
     "sensory_feedback::UI.sensoryFeedback: what reaches the bus::fires once per change, and not at all for a write of the same value",
     "sensory_feedback::UI.sensoryFeedback: what reaches the bus::stops emitting the moment the node unmounts",
     "sensory_feedback::UI.sensoryFeedback: what reaches the bus::observes NOTHING when no sink is wired, and exactly one thing when one is"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-2::indicators-and-sensory-feedback",
   "name": "indicators-and-sensory-feedback",
   "note": "indicators-and-sensory-feedback: 9 named suite cases must pass; suite must exit zero. Requirements: UI-FEEDBACK-001, UI-MOTION-001.",
   "phase": "parity-round-2",
   "releaseBlocking": false,
   "requirements": [
    "UI-FEEDBACK-001",
    "UI-MOTION-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "table_input::table_input: onPrimaryAction across pointer, touch, keyboard and gamepad::Table pointer: a double-click on a row fires onPrimaryAction once, and a single click never does",
     "table_input::table_input: onPrimaryAction across pointer, touch, keyboard and gamepad::Table pointer: two clicks SLOWER than the window are two single clicks, not an open",
     "table_input::table_input: onPrimaryAction across pointer, touch, keyboard and gamepad::Table touch: a plain single tap on a row activates it — the first tap, on any row",
     "table_input::table_input: onPrimaryAction across pointer, touch, keyboard and gamepad::Table keyboard: Return on the focused row fires onPrimaryAction for THAT row",
     "table_input::table_input: onPrimaryAction across pointer, touch, keyboard and gamepad::Table gamepad: ButtonA on the focused row fires onPrimaryAction for THAT row",
     "table_input::table_input: onPrimaryAction across pointer, touch, keyboard and gamepad::a table with NO onPrimaryAction behaves exactly as it does today on all four inputs",
     "row_actions_scenario::row_actions scenario: table surface (reorderable, editable, rowActions)::a horizontal drag on the same table row reveals its tray instead of reordering",
     "row_actions_scenario::row_actions scenario: table surface (reorderable, editable, rowActions)::RowActions touch: a swipe reveals the tray on a table NOT in edit mode and survives its release Activate (after)",
     "row_actions_scenario::row_actions scenario: table surface (reorderable, editable, rowActions)::RowActions touch: a swipe reveals the tray on a table NOT in edit mode and survives its release Activate (before)",
     "row_actions_scenario::row_actions scenario: table surface (reorderable, editable, rowActions)::RowActions mouse: a swipe reveals the tray on a table NOT in edit mode and survives its release Activate (after)",
     "row_actions_scenario::row_actions scenario: table surface (reorderable, editable, rowActions)::RowActions mouse: a swipe reveals the tray on a table NOT in edit mode and survives its release Activate (before)",
     "row_actions_scenario::row_actions scenario: list surface (hand-wrapped ScrollView>VStack)::keyboard Delete on a focused row fires the destructive action end to end",
     "row_actions_scenario::row_actions scenario: list surface (hand-wrapped ScrollView>VStack)::Shift+Return opens the action menu on a focused row",
     "virtual_list_row_actions::VirtualList rowActions (Task 5): behavior parity::(P9) focus TRAVERSAL: the d-pad reaches the revealed tray, and the ring comes BACK to the row"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-2::four-input-proofs",
   "name": "four-input-proofs",
   "note": "four-input-proofs: 14 named suite cases must pass; suite must exit zero. Requirements: UI-INPUT-003, UI-PARADIGM-001.",
   "phase": "parity-round-2",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-003",
    "UI-PARADIGM-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "virtual_list_row_actions::VirtualList rowActions (redteam NEW-1): a hosted swipe survives its own release Activate::RowActions hosted touch: a VirtualList swipe reveals the tray and fires no onActivate (silent release, control)",
     "virtual_list_row_actions::VirtualList rowActions (redteam NEW-1): a hosted swipe survives its own release Activate::RowActions hosted touch: a VirtualList swipe reveals the tray and survives its release Activate (after)",
     "virtual_list_row_actions::VirtualList rowActions (redteam NEW-1): a hosted swipe survives its own release Activate::RowActions hosted touch: a VirtualList swipe reveals the tray and survives its release Activate (before)",
     "virtual_list_row_actions::VirtualList rowActions (redteam NEW-1): a hosted swipe survives its own release Activate::RowActions hosted mouse: a VirtualList swipe reveals the tray and fires no onActivate (silent release, control)",
     "virtual_list_row_actions::VirtualList rowActions (redteam NEW-1): a hosted swipe survives its own release Activate::RowActions hosted mouse: a VirtualList swipe reveals the tray and survives its release Activate (after)",
     "virtual_list_row_actions::VirtualList rowActions (redteam NEW-1): a hosted swipe survives its own release Activate::RowActions hosted mouse: a VirtualList swipe reveals the tray and survives its release Activate (before)",
     "virtual_list_row_actions::VirtualList rowActions (redteam NEW-1): a hosted swipe survives its own release Activate::the arm is one activate wide: the tap AFTER a swipe's release Activate still closes the tray",
     "virtual_list_row_actions::VirtualList rowActions (redteam NEW-1): a hosted swipe survives its own release Activate::a hosted swipe CANCELLED mid-gesture does not swallow the next gamepad Activate",
     "virtual_list_row_actions::VirtualList rowActions (redteam NEW-1): a hosted swipe survives its own release Activate::a hosted swipe CANCELLED mid-gesture does not swallow the hit expander's Activate either",
     "virtual_list_row_actions::VirtualList rowActions (redteam NEW-1): a hosted swipe survives its own release Activate::a hosted swipe with a SILENT release still answers the next gamepad Activate with a close",
     "table_input::table_input: a swipe that ends with no Activated does not eat the next device press::a swipe CANCELLED mid-gesture does not swallow the next gamepad Activate on that row",
     "table_input::table_input: a swipe that ends with no Activated does not eat the next device press::a swipe CANCELLED mid-gesture does not swallow the hit expander's Activate either",
     "table_input::table_input: a swipe that ends with no Activated does not eat the next device press::a swipe with a SILENT release still answers the next gamepad Activate with a tray close",
     "table_input::table_input: a swipe that ends with no Activated does not eat the next device press::a swipe whose release DID Activate still swallows that artifact, and the tray survives it"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-2::hosted-swipe-survives-release-activate",
   "name": "hosted-swipe-survives-release-activate",
   "note": "hosted-swipe-survives-release-activate: 14 named suite cases must pass; suite must exit zero. Requirements: UI-INPUT-003, UI-PARADIGM-001.",
   "phase": "parity-round-2",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-003",
    "UI-PARADIGM-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "examples_gallery::examples/gallery fixture: all controls — the round-2 layout vocabulary::§2.2 a Grid of `UI.GridRow`s aligns its columns across rows, and `gridSpan` covers both plus the gap",
     "examples_gallery::examples/gallery fixture: all controls — the round-2 layout vocabulary::§2.4 when the axis is short the LOWEST tier gives way first, and a weightless child never gives at all",
     "examples_gallery::examples/gallery fixture: all controls — the round-2 layout vocabulary::§2.5 `containerRelativeFrame` measures against the SCROLLER's viewport, not the parent column",
     "examples_gallery::examples/gallery fixture: all controls — the round-2 layout vocabulary::§3.3 `UI.sensoryFeedback` emits the declared verb on the presenter's bus, stamped with the mounted path",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::onPrimaryAction: a DOUBLE-click plays the track and the page says so; a single click only selects",
     "examples_gallery::examples/gallery fixture: withAnimation + reduced motion::four inputs reach the demonstration: pointer, touch, keyboard and gamepad",
     "examples_gallery::examples/gallery fixture: withAnimation + reduced motion::carries the INDETERMINATE pair, and reduced motion steps them instead of stopping them"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-2::gallery-coverage",
   "name": "gallery-coverage",
   "note": "gallery-coverage: 7 named suite cases must pass; suite must exit zero. Requirements: UI-AGENT-001.",
   "phase": "parity-round-2",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'adaptive_controls': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'with_animation': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'card_rail': the solver reports NOTHING at any swept viewport, at any text size"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-2::overflow-sweep",
   "name": "overflow-sweep",
   "note": "overflow-sweep: 3 named suite cases must pass; suite must exit zero. Requirements: UI-LAYOUT-001.",
   "phase": "parity-round-2",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_registration_cli",
     "check_docs_cli",
     "check_boundary",
     "check_prop_parity_cli",
     "check_surface_ledger",
     "check_flat_baseline",
     "check_example_drift_cli",
     "check_manifest_integrity",
     "stylua-check-check-src-tests-tools-bench-examples"
    ]
   },
   "class": "exit0",
   "evidence": null,
   "id": "parity-round-2::checker-battery",
   "name": "checker-battery",
   "note": "checker-battery: check_registration_cli, check_docs_cli, check_boundary, check_prop_parity_cli, check_surface_ledger, check_flat_baseline, check_example_drift_cli, check_manifest_integrity, stylua-check-check-src-tests-tools-bench-examples must exit zero. Requirements: UI-AGENT-001.",
   "phase": "parity-round-2",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "theme_docs::documentation gate: a comparison document sources what it compares against::the live tree's comparison document passes, and an absent one is not an error",
     "theme_docs::documentation gate: a comparison document sources what it compares against::fails when a framework it compares against carries no source URL",
     "theme_docs::documentation gate: a comparison document sources what it compares against::fails when the comparison never marks a claim as an inference",
     "theme_docs::documentation gate: a comparison document sources what it compares against::passes on a minimal well-formed document, and reports that it read it"
    ],
    "shell": "grep -qF \"sensoryFeedback\" docs/reference/api.md"
   },
   "class": "result-ids",
   "evidence": "docs/guide/14-choosing-a-ui-library.md",
   "id": "parity-round-2::parity-doc-falsifiable",
   "name": "parity-doc-falsifiable",
   "note": "parity-doc-falsifiable: 4 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold; and docs/guide/14-choosing-a-ui-library.md must exist. Requirements: UI-AGENT-001.",
   "phase": "parity-round-2",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "for f in facet_grid_row_contract facet_sensory_feedback_contract facet_motion_and_scroll_contract; do test -f ../../../games/RascalRally/code/tests/$f.spec.luau || { echo \"missing RR round-2 contract spec: $f\"; exit 1; }; done && cd ../../../games/RascalRally/code && out=\"$(cat \"$FACET_VERIFY_RR_TRANSCRIPT\")\" && echo \"$out\" | grep -q \"✓.*carries .UI.GridRow. as a primitive with a contract and a TINY prop set\" && echo \"$out\" | grep -q \"✓.*carries .UI.containerRelativeFrame. with a CLOSED spec\" && echo \"$out\" | grep -q \"✓.*the two live flow-grid declarations solve to their frozen geometry\" && echo \"$out\" | grep -q \"✓.*carries .UI.sensoryFeedback. as a blueprint modifier\" && echo \"$out\" | grep -q \"✓.*carries the haptics adapter, and it is DEFAULT OFF\" && echo \"$out\" | grep -q \"✓.*a real game screen.s activate still emits exactly one .activate. and nothing else\" && echo \"$out\" | grep -q \"✓.*a production screen.s target learns the setting with no wiring at all\""
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "parity-round-2::rascalrally-consumer",
   "name": "rascalrally-consumer",
   "note": "rascalrally-consumer: rascalrally-suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "parity-round-2",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "shell": "test -f tools/check_traversal_evidence.py && grep -qF \"THE FIX IS A RE-RECORD, NOT AN EDIT\" tools/check_traversal_evidence.py && python3 -c \"import json,sys; g=json.load(open('tools/lune/verify/graph.json')); rows={r['id']: r for r in g['rows']}; r=rows.get('traversal-document-order::studio-evidence'); sys.exit(0 if r is not None and (r.get('state') or 'evaluated') != 'PASS' else 1)\""
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "parity-round-2::traversal-evidence-red-carried",
   "name": "traversal-evidence-red-carried",
   "note": "traversal-evidence-red-carried: its file-existence and text assertions must hold. Requirements: UI-FID-001, UI-AGENT-001.",
   "phase": "parity-round-2",
   "releaseBlocking": false,
   "requirements": [
    "UI-FID-001",
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "priorPhases": true
   },
   "class": "prior-phases",
   "evidence": null,
   "id": "parity-round-2::prior-gates-unregressed",
   "name": "prior-gates-unregressed",
   "note": "prior-gates-unregressed: every row of every earlier phase must pass in this same run. Requirements: UI-AGENT-001.",
   "phase": "parity-round-2",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_spike-artifacts-declarative-3d-architecture-studio-694f2a"
    ]
   },
   "class": "exit0",
   "evidence": null,
   "id": "declarative-3d-architecture::studio-topology-evidence",
   "name": "studio-topology-evidence",
   "note": "studio-topology-evidence: check_spike-artifacts-declarative-3d-architecture-studio-694f2a must exit zero; and artifacts/declarative-3d-architecture/studio-topology.json must exist. Requirements: UI-AGENT-001.",
   "phase": "declarative-3d-architecture",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_spike-artifacts-declarative-3d-architecture-costs-61165b"
    ]
   },
   "class": "exit0",
   "evidence": null,
   "id": "declarative-3d-architecture::spike-costs-measured",
   "name": "spike-costs-measured",
   "note": "spike-costs-measured: check_spike-artifacts-declarative-3d-architecture-costs-61165b must exit zero; and artifacts/declarative-3d-architecture/costs.json must exist. Requirements: UI-AGENT-001.",
   "phase": "declarative-3d-architecture",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/test.json",
   "id": "declarative-3d-architecture::library-suite-green",
   "name": "library-suite-green",
   "note": "library-suite-green: suite must exit zero; and artifacts/test.json must exist. Requirements: UI-AGENT-001.",
   "phase": "declarative-3d-architecture",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/test.json",
   "id": "parity-round-3::library-suite-green",
   "name": "library-suite-green",
   "note": "library-suite-green: suite must exit zero; and artifacts/test.json must exist. Requirements: UI-AGENT-001.",
   "phase": "parity-round-3",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "flow_wrap::flow-wrap: the line partition is pure, and greedy::fuzz: every child lands in exactly one line, in order, and only a lone item may overflow",
     "flow_wrap::flow-wrap: what a wrapping stack REPORTS::a line's cross extent is its TALLEST item, not the first one's",
     "flow_wrap::flow-wrap: the cross-axis rule Roblox actually has (probed 2026-08-13)::Center: the BLOCK is centred — (400-80)/2 = 160 exactly, not per line",
     "flow_wrap::flow-wrap: the cross-axis rule Roblox actually has (probed 2026-08-13)::ragged: line 2 starts at the TALLEST item of line 1 (90), never at 40",
     "flow_wrap::flow-wrap: measure and arrange cannot disagree about the lines::the plan is keyed by the BOX: a SHRUNK wrapping stack re-breaks its lines",
     "flow_wrap::flow-wrap: an item wider than its line::gets its own line, is CLAMPED to the line, and the solver says so",
     "flow_wrap::flow-wrap: the lines can overflow the CROSS axis, and that is now visible::more lines than fit reports on the cross axis, with the pixel count",
     "flow_wrap::flow-wrap: the lines can overflow the CROSS axis, and that is now visible::...and a stack whose lines DO fit says nothing at all",
     "flow_wrap::flow-wrap: `align = 'stretch'` is refused, not quietly reinterpreted::a LITERAL stretch on a wrapping stack is a construction error naming both readings",
     "flow_wrap::flow-wrap: `align = 'stretch'` is refused, not quietly reinterpreted::a stretch that only a SOLVE can see is refused there, and treated as start",
     "flow_wrap::flow-wrap: a `fill` child on the main axis::takes a whole line to itself, and is named rather than ignored",
     "flow_wrap::flow-wrap: the placement-prop audit polices a wrapping stack::an unknown kind polices NOTHING — which is why the row has to exist",
     "flow_wrap::flow-wrap: the placement-prop audit polices a wrapping stack::`lineAlign` IS read by a wrapping stack; `alignH` beside it is NOT",
     "flow_wrap::flow-wrap: the prop, through the authoring surface::`wrap` is declared on VStack and HStack, reactive, and dirties measure",
     "flow_wrap::flow-wrap: the prop, through the authoring surface::...and NOWHERE else: a `wrap` a solver branch would never read is refused",
     "flow_wrap::flow-wrap: the prop, through the authoring surface::a BOUND `wrap` flips in place: the same stack, re-solved, never remounted",
     "flow_wrap::flow-wrap: the placement-prop audit polices a wrapping stack::the SHRINK PAIR is refused: wrapping IS the answer to a main-axis deficit",
     "examples_gallery::examples/gallery fixture: flow wrap (UI.HStack wrap = true)::THE TOGGLE IS THE ARGUMENT: wrap off overflows, wrap on does not — same nodes",
     "examples_gallery::examples/gallery fixture: flow wrap (UI.HStack wrap = true)::the tags really WRAP: more than one distinct row of y at every viewport",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'flow_wrap': the solver reports NOTHING at any swept viewport, at any text size"
    ],
    "shell": "grep -q \"flowPartition\" src/layout/solver.luau"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-3::flow-wrap",
   "name": "flow-wrap",
   "note": "flow-wrap: 20 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-LAYOUT-001, UI-AGENT-001.",
   "phase": "parity-round-3",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001",
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "shell": "test -f tests/flow_wrap.spec.luau"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "parity-round-3::layout-protocol-verdict",
   "name": "layout-protocol-verdict",
   "note": "layout-protocol-verdict: its file-existence and text assertions must hold; and docs/plans/parity-round3.md must exist. Requirements: UI-LAYOUT-001, UI-AGENT-001.",
   "phase": "parity-round-3",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001",
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "row_capability_optouts::row capability opt-outs — VirtualList::rowSelectable = false STOPS the selection at the funnel, not just at the affordance",
     "row_capability_optouts::row capability opt-outs — VirtualList::rowMovable = false refuses the GRAB — a pinned row never arms, on any input",
     "row_capability_optouts::row capability opt-outs — VirtualList::an absent predicate changes nothing — every row participates, byte-identical to today",
     "row_capability_optouts::row capability opt-outs — VirtualList::a predicate that THROWS is quarantined and fails CLOSED (the row does not participate)",
     "row_capability_optouts::row capability opt-outs — construction refusals (a predicate may only subtract)::rowSelectable on a selection = 'none' list is refused, naming the missing capability",
     "row_capability_optouts::row capability opt-outs — construction refusals (a predicate may only subtract)::rowMovable without onReorder is refused, naming the missing capability",
     "row_capability_optouts::row capability opt-outs — construction refusals (a predicate may only subtract)::rowDeletable without rowActions is refused, naming the missing capability",
     "row_capability_optouts::row capability opt-outs — Table::rowSelectable = false refuses at api.select, in EVERY selection mode path",
     "row_capability_optouts::row capability opt-outs — Table::rowMovable = false refuses at api.moveRow — Table has FIVE reorder entry points",
     "row_capability_optouts::row capability opt-outs — Table::a pinned row is not a DESTINATION either: a movable row cannot displace it",
     "row_capability_optouts::flow-wrap x virtualization: refused, and the refusal names the conflict::newVirtualList refuses `wrap` and explains the index x itemExtent arithmetic"
    ],
    "shell": "test -f src/row_capability.luau && grep -qF \"rowSelectable(item)\" docs/reference/api.md"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-3::row-capability-optouts",
   "name": "row-capability-optouts",
   "note": "row-capability-optouts: 11 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-INPUT-003, UI-AGENT-001.",
   "phase": "parity-round-3",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-003",
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "progress_circular::ProgressView circular: the DETERMINATE ring is the value's own arc::draws an arc over a full backing ring, and no bar at all",
     "progress_circular::ProgressView circular: the DETERMINATE ring is the value's own arc::the SWEEP is the value: 25% ends at 3 o'clock, 100% closes the ring",
     "progress_circular::ProgressView circular: the DETERMINATE ring is the value's own arc::a value change is PAINT ONLY: one prop write on `points`, zero re-solves",
     "progress_circular::ProgressView circular: the INDETERMINATE ring rotates its own start angle::the HEAD travels while the sweep stays fixed — and there is no backing ring",
     "progress_circular::ProgressView circular: the INDETERMINATE ring rotates its own start angle::rotating is PAINT: 60 frames of travel write points and never re-solve",
     "progress_circular::ProgressView circular: the INDETERMINATE ring rotates its own start angle::takes the SAME single clock entry the dots take — no second one for the arc",
     "progress_circular::ProgressView circular: the INDETERMINATE ring rotates its own start angle::under REDUCED motion the ring keeps advancing, on the quantized tick",
     "progress_circular::ProgressView circular: the OFF-PATH cost is exactly zero::no circular indicator mounted = no Path instance and no `points` op, ever",
     "progress_circular::ProgressView circular: the stroke fits inside its own box, in every shipped theme::radius * size / 2 + thickness / 2 never exceeds size / 2",
     "display_controls::ProgressView: the refusals that keep the two modes from contradicting::refuses a spinner with a value — and names the shape that DOES draw one",
     "display_controls::ProgressView: the refusals that keep the two modes from contradicting::refuses `height` on a circular indicator, naming the metric that owns its size",
     "display_controls::ProgressView: the refusals that keep the two modes from contradicting::refuses `showValue` on a circular indicator rather than putting it beside the ring",
     "display_controls::ProgressView: the refusals that keep the two modes from contradicting::refuses an unknown presentation, listing the three that exist",
     "examples_gallery::examples/gallery fixture: circular progress, both forms::both forms are on screen: a filled ring, a rotating head, and the dot spinner",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'progress_ring': the solver reports NOTHING at any swept viewport, at any text size"
    ],
    "shell": "grep -qF \"newProgressView\" docs/reference/api.md && test -f tests/progress_circular.spec.luau"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-3::circular-progress",
   "name": "circular-progress",
   "note": "circular-progress: 15 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-FEEDBACK-001, UI-MOTION-001.",
   "phase": "parity-round-3",
   "releaseBlocking": false,
   "requirements": [
    "UI-FEEDBACK-001",
    "UI-MOTION-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "reference/cartwheel_spec::Cartwheel — the rows that WRAP (parity round 3)::THE PLAZA SUMMARY IS ONE ROW: each chip at ONE path, none at a zero rect",
     "reference/cartwheel_spec::Cartwheel — the rows that WRAP (parity round 3)::THE TAG CLOUD IS RAGGED AND MULTI-LINE, where a uniform grid gave one width",
     "reference/cartwheel_spec::Cartwheel — the rows that WRAP (parity round 3)::A POST'S TAGS AND THE CHART LEGEND KEEP THEIR OWN WIDTHS",
     "reference/foyer_spec::Foyer — the tile meta row WRAPS where it used to ladder (parity round 3)::THE LADDER'S TWIN NODES ARE GONE: no MetaColumn, no ApprovalB, no ChipB",
     "reference/foyer_spec::Foyer — the tile meta row WRAPS where it used to ladder (parity round 3)::AT NORMAL TEXT they share a line and `distribute` pushes the chip to the TRAILING edge",
     "reference/foyer_spec::Foyer — the tile meta row WRAPS where it used to ladder (parity round 3)::AT RAISED PREFERRED TEXT it REFLOWS to a second line instead of squeezing",
     "reference/foyer_spec::Foyer — the tile meta row WRAPS where it used to ladder (parity round 3)::NOT ONE `Spacer` INSTANCE IS MOUNTED: `distribute` says it on the container",
     "reference/wardrobe_spec::Wardrobe — the worn chips FLOW (parity round 3)::FIVE WORN PIECES SHARE ONE LINE, each at its own width — not five stacked lines",
     "reference/wardrobe_spec::Wardrobe — the worn chips FLOW (parity round 3)::IN A NARROW PANE THEY WRAP to a second line rather than paint past the plate",
     "reference/glade_spec::Glade: the scenario surface::the five-view sweep is diagnostic-clean with a live scrollbar (matrix pin)",
     "reference/cartwheel_spec::Cartwheel — reduced motion::the five-view sweep is diagnostic-clean with a live scrollbar (matrix pin)",
     "reference/sipworks_spec::Sipworks — reduced motion, themes, and reachability::the five-view sweep is diagnostic-clean with a live scrollbar (matrix pin)",
     "reference/foyer_spec::Foyer proof — notices, locale, images, determinism::the five-view sweep is diagnostic-clean with a live scrollbar (matrix pin)"
    ],
    "shell": "for p in Glade Cartwheel Sipworks Foyer; do test -f \"examples/places/Facet-Ref-$p.rbxl\" || { echo \"missing place: $p\"; exit 1; }; done && { test ! -f \"examples/places/Facet-Ref-Wardrobe.rbxl\" || { echo \"retired: a publishable Wardrobe place is back\"; exit 1; }; }"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-3::reference-apps-reproved",
   "name": "reference-apps-reproved",
   "note": "reference-apps-reproved: 13 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "parity-round-3",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "lifecycle_hooks::lifecycle hooks: the schema declaration::the hooks are NOT reactive — a lifetime is not a value that changes",
     "lifecycle_hooks::onAppear::fires AFTER the first layout solve — the callback can read its own rect",
     "lifecycle_hooks::onDisappear::fires AFTER the handle is released: the path is no longer mounted",
     "lifecycle_hooks::onAppear::does NOT fire again on a re-solve that changes no structure",
     "lifecycle_hooks::onDisappear::surface teardown fires every still-mounted hook, so a cleanup is never dropped",
     "lifecycle_hooks::hidden — the space-reserving hide::keeps its layout box: the sibling below does NOT move up",
     "lifecycle_hooks::hidden — the space-reserving hide::...which is the whole difference from `UI.When`, whose sibling DOES move up",
     "lifecycle_hooks::hidden — the space-reserving hide::leaves focus order — 'can't receive or respond to interactions'",
     "lifecycle_hooks::hidden — the space-reserving hide::takes no tap — the sentence is two halves and this is the second",
     "lifecycle_hooks::hidden — the space-reserving hide::...and so does a losing ViewThatFits candidate, which is the SAME verdict",
     "examples_gallery::examples/gallery fixture: lifecycle hooks and hidden::teardown fires the badge's own disappear, so the count is never left short"
    ],
    "shell": "grep -qF \"newAsyncImage\" docs/reference/api.md && grep -qF \"keyboardType\" docs/reference/api.md && grep -qF \"effectiveTransparency\" docs/reference/api.md"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-3::audit-gaps-and-free-parity",
   "name": "audit-gaps-and-free-parity",
   "note": "audit-gaps-and-free-parity: 11 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "parity-round-3",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "gallery_chrome::showcase chrome (0): a closed chrome leaves the demo's own input alone::Tab reaches the demo, not the chip strip",
     "gallery_chrome::showcase chrome (1): a keyboard alone can change demo and change theme::the toggle key opens the panel, engages it, and lands focus inside it with the ring showing",
     "gallery_chrome::showcase chrome (2): a gamepad alone can change demo and change theme::the pad's shoulder opens the panel and engages it — with no keyboard anywhere",
     "gallery_chrome::showcase chrome (2): a gamepad alone can change demo and change theme::the motion setting is reachable and changeable with a pad alone",
     "gallery_chrome::showcase chrome (2): a gamepad alone can change demo and change theme::ButtonB (Cancel) resigns the panel, which takes it down and returns the demo",
     "gallery_chrome::showcase chrome (3): mouse and touch still work exactly as before::a tap outside the open panel takes it down (the engaged-surface dismissal path)",
     "gallery_chrome::showcase chrome (4): exactly one engaged surface, and no stolen keys::one Tab press moves the ring one step, not two",
     "gallery_chrome::showcase chrome (4): exactly one engaged surface, and no stolen keys::the toggle context binds exactly the three documented keys, and nothing a demo uses",
     "gallery_chrome::showcase chrome (5): two targets forever, stepping down to icon-only::the 320px phone that killed the third chip steps down to icons rather than clipping",
     "gallery_chrome::showcase chrome (5): two targets forever, stepping down to icon-only::the losing rung is out of the focus order at every width, on both rungs",
     "gallery_chrome::settings model: Full | Reduced, never on/off::no player-visible string in this surface says on, off, disable or enable",
     "gallery_chrome::settings surface: the motion setting survives a demo swap::flipping it RE-SOLVES without rebuilding: mount identity and factory runs are untouched",
     "gallery_chrome::settings surface x with_animation: one fact, two editors::and the host's re-apply is what makes the SETTING win over that restore",
     "gallery_chrome::showcase chrome (9): the sweep::no overflow on either axis, at any swept viewport, under any shipped theme",
     "gallery_chrome::showcase chrome (9): the sweep::the panel never overlaps the chip strip, under any shipped theme at 320x640",
     "gallery_chrome::showcase chrome (9): the sweep::every player-visible string survives ~1.4x pseudo-localization"
    ],
    "shell": "test -f examples/gallery/client/showcase_chrome.luau && test -f examples/gallery/client/settings_panel.luau"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-3::chrome-four-inputs-and-settings",
   "name": "chrome-four-inputs-and-settings",
   "note": "chrome-four-inputs-and-settings: 16 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-INPUT-003, UI-PARADIGM-001, UI-MOTION-001.",
   "phase": "parity-round-3",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-003",
    "UI-PARADIGM-001",
    "UI-MOTION-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "overflow_sweep::overflow sweep: the text-size axis bites::AXIS CONTROL: a slot sized for Larger is SILENT at +0/+4/+10 and REPORTED at +14",
     "overflow_sweep::overflow sweep: the accessibility waiver list::every waived finding still fires — a stale waiver is a fixed defect, delete the line",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'adaptive_controls': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'with_animation': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'card_rail': the solver reports NOTHING at any swept viewport, at any text size"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-3::large-text-sweep-axis",
   "name": "large-text-sweep-axis",
   "note": "large-text-sweep-axis: 5 named suite cases must pass; suite must exit zero. Requirements: UI-A11Y-001, UI-LAYOUT-001.",
   "phase": "parity-round-3",
   "releaseBlocking": false,
   "requirements": [
    "UI-A11Y-001",
    "UI-LAYOUT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "virtual_list_slot_guard::newVirtualList: a lying itemExtent is named, with both numbers::FIRES on the pre-fix lab shape and names the declared extent AND the measure",
     "virtual_list_slot_guard::newVirtualList: a lying itemExtent is named, with both numbers::names the RIGHT numbers at every step of the preference (4 -> 60, 14 -> 84)",
     "virtual_list_slot_guard::newVirtualList: a lying itemExtent is named, with both numbers::is SILENT on the fixed shape — the same rows in a slot that tells the truth",
     "virtual_list_slot_guard::newVirtualList: a lying itemExtent is named, with both numbers::is SILENT on a row that is legitimately SHORTER than its slot",
     "virtual_list_slot_guard::newVirtualList: a lying itemExtent is named, with both numbers::SEES a cell that declares `height = fill` — filling a slot is not fitting it",
     "virtual_list_slot_guard::newVirtualList: a lying itemExtent is named, with both numbers::is SILENT on a cell that ABSORBS its own overflow (a scroller on the list's axis)",
     "virtual_list_slot_guard::newVirtualList: a lying itemExtent is named, with both numbers::is SILENT inside a hidden subtree (a losing ViewThatFits candidate)",
     "virtual_list_slot_guard::newVirtualList: a lying itemExtent is named, with both numbers::never accuses the list's OWN furniture: a 16px slot, and the row hit is 24px",
     "virtual_list_slot_guard::newVirtualList: a lying itemExtent is named, with both numbers::compares against the DECLARED extent, never the row's current box",
     "overflow_sweep::overflow sweep: the widened breadth bites::BREADTH CONTROL: a layered overlap and a lying itemExtent both reach the collector"
    ],
    "shell": "grep -qF \"virtualSlot\" docs/reference/api.md"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-3::virtual-slot-guard",
   "name": "virtual-slot-guard",
   "note": "virtual-slot-guard: 10 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-LAYOUT-001, UI-A11Y-001.",
   "phase": "parity-round-3",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001",
    "UI-A11Y-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "geometry_solve_coalescing::one geometry change is one solve::a batched geometry change is ONE solve",
     "geometry_solve_coalescing::one geometry change is one solve::every geometry key still re-solves",
     "geometry_solve_coalescing::one geometry change is one solve::a non-geometry write does not solve",
     "geometry_solve_coalescing::one geometry change is one solve::env:batch is the general mechanism"
    ],
    "shell": "grep -qF \"env:batch\" docs/reference/api.md"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-3::solve-count-coalescing",
   "name": "solve-count-coalescing",
   "note": "solve-count-coalescing: 4 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-PERF-001, UI-AGENT-001.",
   "phase": "parity-round-3",
   "releaseBlocking": false,
   "requirements": [
    "UI-PERF-001",
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "popup_catcher_paint::popup catcher paint: an open row-actions tray::swiping LEFT TO RIGHT to reveal the leading tray must not paint a plate over the viewport",
     "popup_catcher_paint::popup catcher paint: an open PopupButton::the same catcher, reached through the other outsideDismiss owner, is equally unpainted",
     "popup_catcher_paint::popup catcher paint: an open PopupButton::sits beneath its owner's DisplayOrder — which is why only the uncovered chrome band showed it"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-3::popup-catcher-paint",
   "name": "popup-catcher-paint",
   "note": "popup-catcher-paint: 3 named suite cases must pass; suite must exit zero. Requirements: UI-AGENT-001.",
   "phase": "parity-round-3",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_registration_cli",
     "check_docs_cli",
     "check_boundary",
     "check_prop_parity_cli",
     "check_surface_ledger",
     "check_flat_baseline",
     "check_example_drift_cli",
     "check_manifest_integrity",
     "stylua-check-check-src-tests-tools-bench-examples"
    ]
   },
   "class": "exit0",
   "evidence": null,
   "id": "parity-round-3::checker-battery",
   "name": "checker-battery",
   "note": "checker-battery: check_registration_cli, check_docs_cli, check_boundary, check_prop_parity_cli, check_surface_ledger, check_flat_baseline, check_example_drift_cli, check_manifest_integrity, stylua-check-check-src-tests-tools-bench-examples must exit zero. Requirements: UI-AGENT-001.",
   "phase": "parity-round-3",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "for f in facet_flow_wrap_contract facet_resize_solve_contract facet_sponsor_results; do test -f ../../../games/RascalRally/code/tests/$f.spec.luau || { echo \"missing RR round-3 contract spec: $f\"; exit 1; }; done && cd ../../../games/RascalRally/code && out=\"$(cat \"$FACET_VERIFY_RR_TRANSCRIPT\")\" && echo \"$out\" | grep -q \"✓.*carries .wrap. on VStack and HStack, boolean, reactive, measure-dirty\" && echo \"$out\" | grep -q \"✓.*the placement audit knows the wrapping kinds, so their children are policed\" && echo \"$out\" | grep -q \"✓.*every .wrap =. in this package is the FOCUS-navigation one, in a known file\" && echo \"$out\" | grep -q \"✓.*a real shipped screen still solves with zero layout diagnostics\" && echo \"$out\" | grep -q \"✓.*the new CROSS-axis overflow message exists, and a fitting stack never fires it\" && echo \"$out\" | grep -q \"✓.*env:batch is on the environment the game builds\" && echo \"$out\" | grep -q \"✓.*the surface still RESPONDS to each geometry fact\" && echo \"$out\" | grep -q \"✓.*a rotation re-solves the role-pick modal without per-key fan-out\" && echo \"$out\" | grep -q \"✓.*the capability registry still declares the BAR.s three cells the rally bar depends on\" && echo \"$out\" | grep -q \"✓.*A37b: the CTA form that LOST the ViewThatFits reaches nothing at all\""
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "parity-round-3::rascalrally-consumer",
   "name": "rascalrally-consumer",
   "note": "rascalrally-consumer: rascalrally-suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "parity-round-3",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "shell": "test -f tests/gallery_chrome.spec.luau && test -f examples/gallery/client/showcase_chrome.luau && test -f examples/gallery/client/settings_panel.luau && test -f examples/places/Facet-Showcase.rbxl && test -f docs/reference/api.md && test -f tests/overflow_sweep.spec.luau && test -f src/controls/table.luau && test -f src/controls/virtual_list.luau && test -f src/layout/shrink.luau && test -f tests/text_degrade_cascade.spec.luau && test -f tests/row_actions_input.spec.luau && test -f tests/virtual_list_row_actions.spec.luau && test -f src/controls/progress_view.luau && test -f tests/progress_circular.spec.luau && test -f tests/paradigm_table.spec.luau && test -f tools/check_source_size.py && test -f tools/lune/check_tier_costs.luau && test -f tools/lune/triage_overflow_waivers.luau && test -f tests/preferred_transparency.spec.luau && test -f examples/gallery/scenarios/preferred_transparency.luau && test -f src/controls/native_scroll_binding.luau && test -f tests/native_scroll_autobind.spec.luau"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "parity-round-3::owed-ledger-honest",
   "name": "owed-ledger-honest",
   "note": "owed-ledger-honest: its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "parity-round-3",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "shell": "test -f tools/check_traversal_evidence.py && grep -qF \"THE FIX IS A RE-RECORD, NOT AN EDIT\" tools/check_traversal_evidence.py && python3 -c \"import json,sys; g=json.load(open('tools/lune/verify/graph.json')); rows={r['id']: r for r in g['rows']}; r=rows.get('traversal-document-order::studio-evidence'); sys.exit(0 if r is not None and (r.get('state') or 'evaluated') != 'PASS' else 1)\""
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "parity-round-3::traversal-evidence-red-carried",
   "name": "traversal-evidence-red-carried",
   "note": "traversal-evidence-red-carried: its file-existence and text assertions must hold. Requirements: UI-FID-001, UI-AGENT-001.",
   "phase": "parity-round-3",
   "releaseBlocking": false,
   "requirements": [
    "UI-FID-001",
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "parity-round-3::studio-device-canary",
   "name": "studio-device-canary",
   "note": "studio-device-canary: declared evidence, recorded rather than executed. Requirements: UI-FID-001, UI-PARADIGM-001.",
   "phase": "parity-round-3",
   "releaseBlocking": false,
   "requirements": [
    "UI-FID-001",
    "UI-PARADIGM-001"
   ],
   "state": "FAIL_ENVIRONMENT"
  },
  {
   "check": {
    "priorPhases": true
   },
   "class": "prior-phases",
   "evidence": null,
   "id": "parity-round-3::prior-gates-unregressed",
   "name": "prior-gates-unregressed",
   "note": "prior-gates-unregressed: every row of every earlier phase must pass in this same run. Requirements: UI-AGENT-001.",
   "phase": "parity-round-3",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/test.json",
   "id": "parity-round-4::library-suite-green",
   "name": "library-suite-green",
   "note": "library-suite-green: suite must exit zero; and artifacts/test.json must exist. Requirements: UI-AGENT-001.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "motion_tween::clock:tween — a duration that means what it says::ARRIVES EXACTLY ON TIME, and not one frame early",
     "motion_tween::clock:tween — a duration that means what it says::advances by RAW dt: a frame spike does not stretch the duration",
     "motion_tween::clock:tween — a duration that means what it says::a zero-duration curve lands on the NEXT step, never on an intermediate value",
     "motion_tween::motion curves: the pure twin IS the engine's curve::matches the frozen engine corpus at every sampled alpha",
     "motion_tween::motion curves: the pure twin IS the engine's curve::every style is anchored at both ends, in all three directions",
     "motion_tween::motion curves: the pure twin IS the engine's curve::carries Roblox's eleven styles, not a private vocabulary",
     "motion_tween::motion curves: the pure twin IS the engine's curve::hands the adapter the engine's own enum spelling",
     "motion_tween::motion curves: the registry is the only dial::ships EMPTY — there is no built-in duration to inherit by accident",
     "motion_tween::motion curves: the registry is the only dial::refuses an inline spec table and names registerCurve instead",
     "motion_tween::clock:setEasing — the engine owns the curve::routes every evaluation through the installed evaluator",
     "motion_tween::clock:setEasing — the engine owns the curve::is read per evaluation, so a bind upgrades values built before it",
     "motion_tween::clock:tween — reduced motion is inherited, not re-decided::INFORMATIONAL keeps running to the same terminus, quantized — a frozen cooldown is a hung game",
     "motion_tween::clock:tween — the clock entry cannot leak::a scope owns the clock, and a tween dies with it",
     "with_animation::withAnimation: a registered CURVE drives the flight::ARRIVES ON THE CURVE'S DURATION — the thing a class cannot promise",
     "with_animation::withAnimation: a registered CURVE drives the flight::an unknown name is refused, and the message names BOTH vocabularies"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-4::time-based-easing",
   "name": "time-based-easing",
   "note": "time-based-easing: 15 named suite cases must pass; suite must exit zero. Requirements: UI-MOTION-001, UI-AGENT-001.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-MOTION-001",
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "foreign::UI.Foreign: the foreignHost content seam (c)::adopt makes exactly ONE claim on the caller's instance: its Parent",
     "foreign::UI.Foreign: property authority (d)::retains no reference to the adopted content, so it cannot write to it later",
     "foreign::UI.Foreign: property authority (d)::makes the reserved `host` authority LIVE, owning exactly Foreign.Parent",
     "foreign::UI.Foreign: property authority (d)::the gate catches a second writer for the one claimed property",
     "foreign::UI.Foreign: property authority (d)::claims NOTHING else: every other Foreign prop resolves through common",
     "foreign::UI.Foreign: the foreignHost content seam (c)::refuses nothing, a LayerCollector, a non-GuiObject and a destroyed instance — each by name",
     "stage::UI.Stage: the stageHost content seam (c)::a handle used after its node dies errors with a message naming the node",
     "foreign::UI.Foreign: the foreignHost content seam (c)::a handle used after its node dies errors with a message naming the node",
     "foreign::UI.Foreign: the foreignHost content seam (c)::returns nil for an unknown path, a non-Foreign node, and an adapter without the seam",
     "stage::UI.Stage: the stageHost content seam (c)::is a DECLARED optional target method, so an adapter without it degrades by name",
     "foreign::UI.Foreign: the foreignHost content seam (c)::is a DECLARED optional target method, so an adapter without it degrades by name",
     "stage::UI.Stage: the stageHost content seam (c)::hands back ONE cached handle per node",
     "foreign::UI.Foreign: the foreignHost content seam (c)::hands back ONE cached handle per node",
     "foreign::UI.Foreign: strict authoring (a)::refuses the UNBOUNDED form by name, with the argument and the alternative",
     "foreign::UI.Foreign: strict authoring (a)::refuses `tint`, because Facet never paints the caller's content",
     "foreign::UI.Foreign: mount, elision, dispose (b)::is NEVER elided, on either adapter, even bare",
     "foreign::UI.Foreign: layout (e)::clips its content to the reserved box, on both adapters",
     "foreign::UI.Foreign: containment (f)::is never parked into the recycling pool",
     "stage::UI.Stage: mount, dump, dispose (b)::mount/render/dispose returns the core registry to baseline",
     "foreign::UI.Foreign: mount, elision, dispose (b)::mount/render/dispose returns the core registry to baseline"
    ],
    "shell": "grep -q \"Foreign = { Parent = .host. }\" src/render/authority.luau"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-4::foreign-instance-seam",
   "name": "foreign-instance-seam",
   "note": "foreign-instance-seam: 20 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-EXT-001, UI-AGENT-001.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-EXT-001",
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "preferred_transparency::the preference composes with a theme's backdrop alpha, and nothing else::is base x preference — 1 is the identity and 0 is fully opaque",
     "preferred_transparency::the preference composes with a theme's backdrop alpha, and nothing else::a garbage preference is the IDENTITY, never a silent paint change",
     "preferred_transparency::the scope is a measurement: one partial background in the whole sheet::the ONLY rule painting a see-through background is the backdrop",
     "preferred_transparency::the scope is a measurement: one partial background in the whole sheet::...and in a compiled theme package too, whatever its own dim is",
     "preferred_transparency::the scope is a measurement: one partial background in the whole sheet::the declared patch scope is exactly the measured set of translucent backgrounds",
     "preferred_transparency::why the sweep is refused rather than merely unfashionable::scaling an INVISIBLE background does not dim it, it reveals it",
     "preferred_transparency::the refusals: what the preference deliberately does not touch::the DISABLED dim is untouched — the alpha there is the state, not a backdrop",
     "preferred_transparency::the refusals: what the preference deliberately does not touch::the HAIRLINE is untouched — a stroke is a border, and the setting names backgrounds",
     "preferred_transparency::an AUTHORED opacity is the author's number and the preference never scales it::opacity 0.5 stays 0.5 at every preference, including fully opaque",
     "preferred_transparency::the renderer pushes the preference at attach and on every change::arrives at attach with no consumer wiring at all",
     "preferred_transparency::the renderer pushes the preference at attach and on every change::...and again when the player moves the setting mid-session",
     "preferred_transparency::the renderer pushes the preference at attach and on every change::a garbage platform fact never reaches the adapter — the memo clamps first",
     "preferred_transparency::the two paint modes compose the SAME number::the bespoke scrim write goes through backdropTransparency, not a bare token read",
     "preferred_transparency::the two paint modes compose the SAME number::the native rule patch goes through it too, over the DECLARED scope"
    ],
    "shell": "grep -qF \"PREFERENCE_RULE_PROPS\" src/tokens/sheet_model.luau"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-4::preferred-transparency",
   "name": "preferred-transparency",
   "note": "preferred-transparency: 14 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-A11Y-001, UI-STYLE-001.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-A11Y-001",
    "UI-STYLE-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "mount::mount: When hands its branch the branch scope::thenView is handed the BRANCH scope — labelled with the branch path",
     "mount::mount: When hands its branch the branch scope::the initial-true mount hands the same scope the later flip does",
     "mount::mount: When hands its branch the branch scope::a resource owned on the handed scope is released when the branch closes",
     "mount::mount: When hands its branch the branch scope::a flip back gets a FRESH scope: nothing from the previous entry survives",
     "mount::mount: When hands its branch the branch scope::fifty open/close cycles leave no scope and no cleanup behind",
     "transitions::mount retire model: When::the scope handed to thenView survives the RETIRE and dies at done, once"
    ],
    "shell": "grep -q \"AND WHY THERE IS NO .elseView. TO HAND A SECOND SCOPE TO\" src/mount.luau && grep -qF \"Refused deliberately; see\" src/mount.luau && grep -q \"There is deliberately [*][*]no .elseView.[*][*]\" docs/reference/api.md && grep -qF \"the hoisted-state leak\" docs/reference/api.md"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-4::when-branch-scope",
   "name": "when-branch-scope",
   "note": "when-branch-scope: 6 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "sorted_entries::sortedEntries: the hazard it exists for::THE NEGATIVE CONTROL — the same twelve pairs iterate in DIFFERENT orders",
     "sorted_entries::sortedEntries: the hazard it exists for::the naive determinism check would PASS with pairs — repetition is the wrong axis",
     "sorted_entries::sortedEntries: the hazard it exists for::at SIX keys three of the four constructions agree — which is why this file uses twelve",
     "sorted_entries::sortedEntries: what it guarantees::every construction of the same dictionary yields ONE order",
     "sorted_entries::sortedEntries: the comparator::the comparator receives KEYS, which is what makes the guarantee unconditional",
     "sorted_entries::sortedEntries: what it guarantees::numeric keys order NUMERICALLY, not lexicographically",
     "sorted_entries::sortedEntries: the comparator::a key type with no natural order is REFUSED at construction, naming the comparator",
     "sorted_entries::sortedEntries: what it guarantees::each call returns a FRESH array — a caller may sort or splice it",
     "sorted_entries::sortedEntries: through UI.ForEach::a dictionary signal drives a keyed region in one order whatever built it"
    ],
    "shell": "grep -qF \"ORDERS KEYS, NOT ENTRIES\" src/blueprint.luau && grep -qF \"blueprint.sortedEntries\" src/blueprint.luau"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-4::sorted-entries-determinism",
   "name": "sorted-entries-determinism",
   "note": "sorted-entries-determinism: 9 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "focus_grid_axis::focus graph: a 2-D group answers the perpendicular direction itself::the perpendicular direction moves a whole LINE, not one cell",
     "focus_grid_axis::focus graph: a 2-D group answers the perpendicular direction itself::the LANE axis is untouched: ±1 in document order, crossing lines row-major",
     "focus_grid_axis::focus graph: a 2-D group answers the perpendicular direction itself::TRANSPOSE: on a `vertical` lane axis the LINE move is Right/Left",
     "focus_grid_axis::focus graph: a ragged last line has an answer in both directions::FORWARD the move CLAMPS into a short last line rather than falling out",
     "focus_grid_axis::focus graph: a ragged last line has an answer in both directions::...and clamping is NOT a wrap: from the last line itself the move exits",
     "focus_grid_axis::focus graph: 2-D edges, eligibility and wrap::`wrap` governs the group's DECLARED axis only; the perpendicular exits",
     "focus_grid_axis::focus graph: 2-D edges, eligibility and wrap::exiting perpendicular hands the LANE on, so the column survives the crossing",
     "focus_grid_axis::focus graph: perpendicular ENTRY into a 2-D group is the near LINE::entering DOWNWARD lands in the FIRST line, at the ordinal lane",
     "focus_grid_axis::focus graph: perpendicular ENTRY into a 2-D group is the near LINE::entering UPWARD lands in the LAST line, at the ordinal lane",
     "focus_grid_axis::focus graph: perpendicular ENTRY into a 2-D group is the near LINE::entering upward into a RAGGED last line clamps to the lane that exists",
     "focus_grid_axis::focus graph: the rectangle invariant::a SPLICE degrades a 2-D group to its 1-D reading rather than mis-navigating",
     "focus_grid_axis::focus graph: a group with no `columns` is unchanged::NULL HYPOTHESIS: 1-D groups walk byte-identically with the field absent",
     "focus_grid_axis::focus graph: a group with no `columns` is unchanged::`columns` is refused as a navigable claim unless it is an integer >= 1",
     "focus_grid_axis::focus graph: the 2-D ring matches a coordinate model over a generated corpus::agrees with the (line, lane) oracle over 300 seeded grids and walks",
     "focus_grid_axis::focus graph: the 2-D ring matches a coordinate model over a generated corpus::THE CORPUS BITES: the oracle rejects a one-cell perpendicular step",
     "virtual_grid_input::VirtualGrid keyboard input (no consumer wiring)::VirtualGrid keyboard: Down moves a whole LINE, not one cell",
     "virtual_grid_input::VirtualGrid keyboard input (no consumer wiring)::VirtualGrid keyboard: the focus dump REPORTS the second axis",
     "virtual_hgrid::VirtualHGrid keyboard and gamepad: the ring is the TRANSPOSE, not a copy::Right/Left moves a whole LINE, not one cell"
    ],
    "shell": "! grep -q \"bindFocusGraph\" src/controls/virtual_grid.luau && grep -q \"THERE IS NO .navigateIntercept. HERE ANY MORE, AND ITS DELETION IS THE POINT\" src/controls/virtual_grid.luau && grep -qF \"columns = lanesNow()\" src/controls/virtual_grid.luau && grep -qF \"laneCount\" src/focus/focus_graph.luau"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-4::two-dimensional-focus",
   "name": "two-dimensional-focus",
   "note": "two-dimensional-focus: 18 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-INPUT-003, UI-A11Y-001.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-003",
    "UI-A11Y-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "hit_expander_overhang::hit-expander overhang: the table divider's floor is reachable::the divider outranks the NEIGHBOUR column its 44px floor reaches into",
     "hit_expander_overhang::hit-expander overhang: the table divider's floor is reachable::nothing is left that can take the band: the whole 44px belongs to the divider",
     "hit_expander_overhang::hit-expander overhang: a NON-table expander carried the same defect::every sub-floor row outranks the row its floor reaches into",
     "hit_expander_overhang::hit-expander overhang: the qualifier and the stability it buys::two floors that overlap ONLY each other keep their declared order",
     "hit_expander_overhang::hit-expander overhang: the qualifier and the stability it buys::the lift is read from the base order, so a second solve moves nothing"
    ],
    "shell": "test -f src/render/hit_lift.luau && grep -q \"measured: .hud., host .R1., two 44x44 expanders overlapping by 5%\" src/render/hit_lift.luau && grep -qF \"The caller supplies that census\" src/render/hit_lift.luau"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-4::hit-expander-z-order",
   "name": "hit-expander-z-order",
   "note": "hit-expander-z-order: 5 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold; and artifacts/hit-expander-overhang/corpus-measurement.md must exist. Requirements: UI-INPUT-003, UI-PARADIGM-001.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-003",
    "UI-PARADIGM-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "paint_extensions::tint: the native-sheet claim mechanism (source contract)::a tinted fill rides a tag, because the claim on it could never be taken",
     "sheet_model::sheet_model.build::a tint's FILL rides a tag rule, because a default-valued write never claims",
     "sheet_model::buildPackage: a rule NAME is identity, so the vocabulary is shared (C16)::emits the tint fill in the same cascade position as the legacy builder",
     "paint_extensions::tint: emission and application::a Box tint paints at its declared transparency, and defaults to opaque",
     "paint_extensions::tint: emission and application::the live adapter writes the alpha onto the engine property, all three classes",
     "theme_paint_repaint::a theme commit re-resolves the tint channel::repaints a STATIC tint that nothing will ever re-write",
     "theme_paint_repaint::the live adapter carries the same commit (source contract)::refreshThemedPaint re-applies the path role AND the tint, tint last"
    ],
    "shell": "grep -qF \"facet-tint-fill\" src/tokens/sheet_model.luau && grep -qF \"tintFill\" src/client/screen_paint.luau"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-4::tint-fill-tag",
   "name": "tint-fill-tag",
   "note": "tint-fill-tag: 7 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-STYLE-001.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "layout::layout: a HUGGING ZStack child pays for its own margin too::an END-aligned child stops a FAR margin short of the edge",
     "layout::layout: a HUGGING ZStack child pays for its own margin too::...on the vertical axis too",
     "layout::layout: a HUGGING ZStack child pays for its own margin too::a NEAR margin still pushes an end-aligned child AWAY from its own edge",
     "layout::layout: a HUGGING ZStack child pays for its own margin too::a CENTRED child centres in the box it was GIVEN, not the box it was denied",
     "layout::layout: a HUGGING ZStack child pays for its own margin too::a START-aligned child is unmoved — it always sat at its near margin",
     "table_themed_header::Table header: the sort mark under every shipped theme package::the mark stops the theme's OWN reserve short of the column boundary",
     "table_themed_header::Table header: the sort mark under every shipped theme package::the mark clears the plate's carved border, so it paints INSIDE the box",
     "table_themed_header::Table header: the sort mark under every shipped theme package::the title never overlaps the mark, leading or trailing",
     "table_themed_header::Table header: the sort mark under every shipped theme package::a column resize carries the mark with the boundary, at any width",
     "table_themed_header::Table header: the sort mark under every shipped theme package::no package makes the header band report a layout finding",
     "table::Table: column-owned columns (director round 5)::a LEADING title on a sortable column reserves the mark's room too"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-4::zstack-hugging-margin",
   "name": "zstack-hugging-margin",
   "note": "zstack-hugging-margin: 11 named suite cases must pass; suite must exit zero. Requirements: UI-LAYOUT-001.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "native_scroll_autobind::O-31: a virtualised collection follows the engine with no consumer call::VirtualList: an engine scroll slides the window — NO bindNativeScroll call",
     "native_scroll_autobind::O-31: a virtualised collection follows the engine with no consumer call::VirtualGrid (vertical grid, axis y): an engine scroll slides the window — NO bindNativeScroll call",
     "native_scroll_autobind::O-31: a virtualised collection follows the engine with no consumer call::VirtualGrid (sideways grid, axis x): an engine scroll slides the window — NO bindNativeScroll call",
     "native_scroll_autobind::O-31: a virtualised collection follows the engine with no consumer call::Table (virtualized): an engine scroll slides the window — NO bindNativeScroll call",
     "native_scroll_autobind::O-31: a virtualised collection follows the engine with no consumer call::...and it still does when the CONSUMER declares navigationGroups (the focusGroups seam is dead here)",
     "native_scroll_autobind::O-31: a virtualised collection follows the engine with no consumer call::PROOF THE DISCRIMINATOR IS REAL: with a declared focus map the presenter calls focusGroups ZERO times",
     "native_scroll_autobind::O-31: a virtualised collection follows the engine with no consumer call::an explicit bindNativeScroll on top of the auto-bind adds NO second engine observer",
     "native_scroll_autobind::O-31: a virtualised collection follows the engine with no consumer call::...and a refresh after that unbind does NOT resurrect the mirror",
     "native_scroll_autobind::O-31: a virtualised collection follows the engine with no consumer call::a BLOCK table (scrolls = false) auto-binds NOTHING — its published scroll path is nil",
     "native_scroll_autobind::O-31: a virtualised collection follows the engine with no consumer call::NEGATIVE CONTROL: a bare mount with no presenter still learns no path, and says so",
     "gallery_demo_picker::showcase host: the fixture branch binds the native-scroll mirror::every catalogue fixture that rides the engine canvas gets its mirror bound",
     "gallery_demo_picker::showcase host: the fixture branch binds the native-scroll mirror::the lazy grid's window follows an engine scroll — the live defect, headless"
    ],
    "shell": "test -f src/controls/native_scroll_binding.luau && grep -qF \"You do not wire the mirror. The framework does\" docs/reference/api.md && grep -qF \"There is **no opt-out key**, because there is only one collection whose scrolling\" docs/reference/api.md"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-4::native-scroll-autobind",
   "name": "native-scroll-autobind",
   "note": "native-scroll-autobind: 12 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-INPUT-003, UI-AGENT-001.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-003",
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "hud_chrome_rotation::director item 4: the APP's chrome is reserved per column::a column the chrome does not cover starts LEVEL with it, not below it",
     "hud_chrome_rotation::director item 4: the APP's chrome is reserved per column::NOTHING declared is byte-for-byte the bounding box this HUD always spent",
     "hud_chrome_rotation::director item 4: the APP's chrome is reserved per column::the declared chrome is REPORTED as occupied, so a collision check can see it",
     "hud_chrome_rotation::director item 5: the URL bar is a box you can see, and the HUD re-flows around it::the drawn bar is EXACTLY the height the HUD lost",
     "hud_chrome_rotation::director 2026-08-15 round 3: the ladder may not buy the app's chrome::the reservation is never DROPPED while the chrome is declared — and the ladder was under real pressure",
     "hud_chrome_rotation::director 2026-08-15 round 2: anchored content never touches the physical edge::401x777: every anchored zone clears BOTH lateral edges by the theme's own step",
     "hud_chrome_rotation::director 2026-08-15 round 2: anchored content never touches the physical edge::749x380: every anchored zone clears BOTH lateral edges by the theme's own step",
     "hud_chrome_rotation::director 2026-08-15 round 2: anchored content never touches the physical edge::a REAL safe-area inset still wins when it is bigger than the gutter"
    ],
    "shell": "grep -qF \"appChromeRects\" src/env/environment.luau && grep -qF \"DECLARATION, never a measurement this module makes\" src/env/environment.luau && grep -qF \"THE RULE IS THE MEASUREMENT, NOT A FRACTION\" src/layout/composition.luau"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-4::app-chrome-and-screen-gutter",
   "name": "app-chrome-and-screen-gutter",
   "note": "app-chrome-and-screen-gutter: 8 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-LAYOUT-001, UI-PARADIGM-001.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001",
    "UI-PARADIGM-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "gallery_demo_picker::showcase host: advancing the picker actually mounts the demo it names::every catalogue demo names a root screen this sweep knows to look for",
     "gallery_demo_picker::showcase host: advancing the picker actually mounts the demo it names::one showNext per demo leaves EXACTLY that demo's own screen in the tree, all 34",
     "gallery_demo_picker::showcase host: advancing the picker actually mounts the demo it names::NEGATIVE CONTROL: a demo that cannot build is NOT reported as mounted",
     "gallery_demo_picker::showcase host: advancing the picker actually mounts the demo it names::the shipped scriptable API answers with what it DELIVERED, not what it was asked for"
    ],
    "shell": "grep -qF \"mountedId\" examples/gallery/client/demo_picker.luau && grep -q \"Read .mounted., not .current.\" docs/guide/11-device-verification.md"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-4::demo-picker-mounted",
   "name": "demo-picker-mounted",
   "note": "demo-picker-mounted: 4 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-FID-001, UI-AGENT-001.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-FID-001",
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {},
   "class": "retired",
   "evidence": null,
   "id": "parity-round-4::comparison-docs-honest",
   "name": "comparison-docs-honest",
   "note": "comparison-docs-honest: retired in this stage; see retiredReason.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "retiredReason": "the two framework-comparison documents this row pinned were archived out of the repository by workstream K; the public framework-choice guide is their living replacement",
   "state": "RETIRED"
  },
  {
   "check": {
    "shell": "sz=\"$(python3 tools/check_source_size.py)\" && echo \"$sz\" | grep -qF \"KNOWN_OVER is empty.\" && grep -qE \"^KNOWN_OVER[^=]*= *[{][}]$\" tools/check_source_size.py"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "parity-round-4::source-size-cap",
   "name": "source-size-cap",
   "note": "source-size-cap: its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_registration_cli",
     "check_docs_cli",
     "check_boundary",
     "check_prop_parity_cli",
     "check_surface_ledger",
     "check_flat_baseline",
     "check_example_drift_cli",
     "check_manifest_integrity",
     "stylua-check-check-src-tests-tools-bench-examples"
    ]
   },
   "class": "exit0",
   "evidence": null,
   "id": "parity-round-4::checker-battery",
   "name": "checker-battery",
   "note": "checker-battery: check_registration_cli, check_docs_cli, check_boundary, check_prop_parity_cli, check_surface_ledger, check_flat_baseline, check_example_drift_cli, check_manifest_integrity, stylua-check-check-src-tests-tools-bench-examples must exit zero. Requirements: UI-AGENT-001.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "shell": "test -f tools/lune/theme_sync_cli.luau && test -f src/themes/token_sync.luau && for t in circularSize circularThickness spinnerDotSize; do grep -qE \"^[[:space:]]*$t = \" examples/themes/fantasy_parchment.luau || { echo \"the theme metric $t was DELETED rather than re-recorded\"; exit 1; }; done && python3 -c \"import json,sys; g=json.load(open('tools/lune/verify/graph.json')); rows={r['id']: r for r in g['rows']}; r=rows.get('theme-packages-and-skinning::style-editor-sync'); sys.exit(0 if r is not None and (r.get('state') or 'evaluated') != 'PASS' else 1)\""
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "parity-round-4::theme-sync-red-carried",
   "name": "theme-sync-red-carried",
   "note": "theme-sync-red-carried: its file-existence and text assertions must hold. Requirements: UI-STYLE-001, UI-AGENT-001.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001",
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "for f in facet_foreign_seam_contract facet_branch_scope_contract facet_preferred_transparency_contract facet_focus_grid_axis_contract facet_hit_expander_overhang_contract facet_motion_and_scroll_contract facet_stack_arrange_contract; do test -f ../../../games/RascalRally/code/tests/$f.spec.luau || { echo \"missing RR round-4 contract spec: $f\"; exit 1; }; done && cd ../../../games/RascalRally/code && out=\"$(cat \"$FACET_VERIFY_RR_TRANSCRIPT\")\" && echo \"$out\" | grep -q \"✓.*half 1 — the framework this package requires hands thenView the branch scope\" && echo \"$out\" | grep -q \"✓.*half 4 — UI.sortedEntries is live here, and this package deliberately calls it nowhere\" && echo \"$out\" | grep -q \"✓.*half 3 — the shipped StartCountdown still opens and closes, twenty times, with no leak\" && echo \"$out\" | grep -q \"✓.*half 4 — .host. is live, owns exactly one property, and touches nothing this game writes\" && echo \"$out\" | grep -q \"✓.*half 2 — nothing in this package reaches for the escape hatch, measured off the source\" && echo \"$out\" | grep -q \"✓.*is the framework default 0.45, composed — not a number this repo restates\" && echo \"$out\" | grep -q \"✓.*nothing in src/ reads the preference or its derived key\" && echo \"$out\" | grep -q \"✓.*no navigation group on the shipped results screen declares a lane count\" && echo \"$out\" | grep -q \"✓.*POSITIVE CONTROL: this game.s focus dump does report a lane count when there is one\" && echo \"$out\" | grep -q \"✓.*...and the floor is DELIVERED there: the row outranks the row it overhangs\" && echo \"$out\" | grep -q \"✓.*the reorder is invisible: no two rows. PAINTED rects overlap\" && echo \"$out\" | grep -q \"✓.*grows no hit expander at all, so the rule cannot reach it\" && echo \"$out\" | grep -q \"✓.*this game.s sponsor list follows an engine scroll with NO bindNativeScroll call\" && echo \"$out\" | grep -q \"✓.*...and FacetSponsor.s OWN bind on top of it opens no second engine observer\" && echo \"$out\" | grep -q \"✓.*clock:tween is REACHABLE from this game.s own presenter clock, and arrives ON TIME\" && echo \"$out\" | grep -q \"✓.*an installed evaluator changes NOTHING about this game.s spring and timer\" && echo \"$out\" | grep -q \"✓.*the LIVE motion_driver fills the seam at bind — and this game binds its OWN frame source\" && echo \"$out\" | grep -q \"✓.*.lineAlign. aligns one child in its line and outranks the container.s .align.\" && echo \"$out\" | grep -q \"✓.*a real shipped screen solves with zero layout diagnostics\""
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "parity-round-4::rascalrally-consumer",
   "name": "rascalrally-consumer",
   "note": "rascalrally-consumer: rascalrally-suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "parity-round-4::studio-and-device-evidence",
   "name": "studio-and-device-evidence",
   "note": "studio-and-device-evidence: and artifacts/time-based-easing/engine-oracle.md must exist. Requirements: UI-FID-001, UI-PARADIGM-001.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-FID-001",
    "UI-PARADIGM-001"
   ],
   "state": "FAIL_ENVIRONMENT"
  },
  {
   "check": {
    "priorPhases": true
   },
   "class": "prior-phases",
   "evidence": null,
   "id": "parity-round-4::prior-gates-unregressed",
   "name": "prior-gates-unregressed",
   "note": "prior-gates-unregressed: every row of every earlier phase must pass in this same run. Requirements: UI-AGENT-001.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "authored_presentation::authored presentation B — the composition rule::OPACITY MULTIPLIES: authored 0.5 under a framework fade of 0.5 paints 0.25 opacity",
     "authored_presentation::authored presentation B — the composition rule::SCALE MULTIPLIES: authored 2 under a presentation scale of 1.5 paints 3",
     "authored_presentation::authored presentation B — the composition rule::ROTATION ADDS: authored 30 under a presentation rotation of 15 paints 45",
     "authored_presentation::authored presentation B — the composition rule::NESTED groups do NOT double-compose: the engine multiplies across nodes, the framework within one",
     "authored_presentation::authored presentation B — the composition rule::clearing the FRAMEWORK's contribution leaves the AUTHORED value standing",
     "authored_presentation::authored presentation B — the composition rule::the offset half is untouched by an authored scale: x/y still compose onto the rect",
     "authored_presentation::authored presentation D — off-path cost, and one write site::ONE SITE writes each presentation engine property — the manifest's promise, in the source",
     "authored_presentation::authored presentation E — an authored scale survives a press::NO press-dip target in screen_paint is an absolute 1 — every one derives from the resting scale",
     "authored_presentation::authored presentation E — an authored scale survives a press::...and all FOUR sites read it through one named helper, not four literals",
     "authored_presentation::authored presentation E — an authored scale survives a press::the presentation authority RECORDS the resting scale at the moment it writes it",
     "authored_presentation::authored presentation A — the props, and the refusals::a plain sibling is NOT promoted to a fade group",
     "authored_presentation::authored presentation C — withAnimation reaches all three::REDUCED MOTION deletes the flight and never the destination",
     "authored_presentation::authored presentation D — off-path cost, and one write site::a surface authoring NONE of the three writes no presentation property at all"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-4::authored-presentation-composition",
   "name": "authored-presentation-composition",
   "note": "authored-presentation-composition: 13 named suite cases must pass; suite must exit zero. Requirements: UI-MOTION-001, UI-STYLE-001.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-MOTION-001",
    "UI-STYLE-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "surface_overlap::cross-surface overlap: what counts as declaring an intent to cover::deviceSafeContent is NOT a declaration — it is an inset choice, not a layering one",
     "surface_overlap::cross-surface overlap: what counts as declaring an intent to cover::an UNSET display order declares nothing — a raw attach that never ordered itself is not layered",
     "surface_overlap::cross-surface overlap: what counts as declaring an intent to cover::edgeToEdge on EITHER side is a declaration — a scrim means to cover, and means to be covered",
     "surface_overlap::cross-surface overlap: it arrives through controller.diagnostics()::a MODAL over a base screen reports nothing — the band is the declaration",
     "surface_overlap::cross-surface overlap: what counts as declaring an intent to cover::the four SURFACE_LAYER bands land in four DISTINCT bands — the presenter's table and this scan agree",
     "surface_overlap::cross-surface overlap: the covering set::a surface at `opacity = 0` covers nothing — a geometry-only test would report it",
     "surface_overlap::cross-surface overlap: the covering set::...and at any opacity above zero it covers again — 0 is the edge, not `opacity` itself",
     "surface_overlap::cross-surface overlap: the covering set::a framework fade to nothing covers nothing — the same rule, from the other contributor",
     "surface_overlap::cross-surface overlap: the covering set::coverRect is the union of what a surface paints BELOW its root, not the root's own claim",
     "surface_overlap::cross-surface overlap: the pure pair scan::rects that only touch at an edge do not intersect — a 0px overlap is not an overlap",
     "surface_overlap::cross-surface overlap: the cost is zero off the inspection path::a frame NEVER scans — the whole mechanism runs only when diagnostics() is read",
     "surface_overlap::cross-surface overlap: the cost is zero off the inspection path::...and a single-surface target does not scan even when it IS read",
     "surface_overlap::cross-surface overlap: it arrives through controller.diagnostics()::a dismissed surface with a live EXIT TRANSITION is out immediately, tree and all",
     "surface_overlap::cross-surface overlap: it arrives through controller.diagnostics()::a disposed surface leaves the REGISTRY: the target's bucket does not grow with every mount",
     "surface_overlap::cross-surface overlap: the cost is zero off the inspection path::two surfaces on DIFFERENT targets never see each other",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'surface_overlap': the solver reports NOTHING at any swept viewport, at any text size"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-4::cross-surface-overlap",
   "name": "cross-surface-overlap",
   "note": "cross-surface-overlap: 16 named suite cases must pass; suite must exit zero. Requirements: UI-LAYOUT-001, UI-AGENT-001.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001",
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "authored_presentation::authored presentation A — the props, and the refusals::`opacity` on a LEAF is refused at construction, because a leaf fade would defeat the sheet",
     "authored_presentation::authored presentation A — the props, and the refusals::THE DISABLED-DIMMING CASE: the refusal names the defeat it exists to prevent",
     "authored_presentation::authored presentation A — the props, and the refusals::...and it names the WORKAROUND, spelled out, not a doc reference",
     "authored_presentation::authored presentation A — the props, and the refusals::`canvasGroup` on a leaf is refused with the SAME message — one fact, both spellings",
     "authored_presentation::authored presentation A — the props, and the refusals::THE ASYMMETRY: `opacity` is on exactly two classes and every other one refuses it BY NAME",
     "authored_presentation::authored presentation A — the props, and the refusals::a STRUCTURAL class is not given the fade refusal — it has no instance to fade",
     "authored_presentation::authored presentation A — the props, and the refusals::`scale` and `rotation` ARE offered on a leaf — neither touches a sheet-owned property",
     "authored_presentation::authored presentation A — the props, and the refusals::...and `scale`/`rotation` are offered on EVERY rendered class and refused on none",
     "authored_presentation::authored presentation A — the props, and the refusals::a genuinely unknown property is UNCHANGED — the refusal did not swallow the suggester",
     "authored_presentation::authored presentation A — the props, and the refusals::the wrap fades the GROUP and writes nothing at all on the control inside it",
     "authored_presentation::authored presentation D — off-path cost, and one write site::the refusal is on the ERROR PATH ONLY — one caller, and it is the failing constructor",
     "authored_presentation::authored presentation A — the props, and the refusals::the authored domain is refused rather than clamped: opacity outside 0..1, scale <= 0",
     "authored_presentation::authored presentation A — the props, and the refusals::a REACTIVE value out of domain is refused at the read site too, not silently painted"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-4::leaf-opacity-refusal",
   "name": "leaf-opacity-refusal",
   "note": "leaf-opacity-refusal: 13 named suite cases must pass; suite must exit zero. Requirements: UI-STYLE-001, UI-AGENT-001.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-STYLE-001",
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite",
     "check_elision_census"
    ],
    "resultIds": [
     "host_move_write_cost::a host that moves costs ONE engine write, not one per descendant::THE BILL: the whole list moves 40px and ONE Position is written",
     "host_move_write_cost::a host that moves costs ONE engine write, not one per descendant::A/A: re-applying the SAME rects writes nothing at all",
     "host_move_write_cost::a host that moves costs ONE engine write, not one per descendant::NEGATIVE CONTROL: with no host above them, moving nodes pays per node",
     "nested_tree_pins::a host's rect lands BEFORE its descendants', or they re-base against a ghost::on a first render, every host is placed before anything inside it",
     "nested_tree_pins::a host's rect lands BEFORE its descendants', or they re-base against a ghost::...and a NESTED host is placed before its own subtree too",
     "instance_hosts::WHICH NODES ARE REAL PARENTS, through the renderer that decides it::THE TRADE: a registered host is refused by the recycle pool",
     "instance_hosts::WHICH NODES ARE REAL PARENTS, through the renderer that decides it::CROPS IS NOT clipChildren: a fade group crops too, because its buffer does",
     "nested_tree_pins::the framework writes ONE node, the engine carries the subtree::STEP 4: the delivery mechanism — the container became a real parent",
     "nested_tree_pins::the framework writes ONE node, the engine carries the subtree::STEP 4 IS BOUNDED: a rotated LEAF earns nothing, and neither does an empty container",
     "instance_hosts::hostFor does not scan the registry, and that is what makes nesting affordable::thousands of unrelated hosts do not slow a lookup",
     "instance_hosts::hostFor: the deepest registered ancestor wins, and only a real ancestor::a shared STRING prefix is not a path prefix — /A/B never captures /A/BC/D",
     "nested_tree_pins::the depth-first z counter IS Sibling paint order::a parent is below its own subtree, and its subtree is below its next sibling",
     "nested_tree_pins::the depth-first z counter IS Sibling paint order::the counter is strictly increasing — no two nodes share a slot"
    ],
    "shell": "grep -q \"ZIndexBehavior.Sibling\" src/client/screen_target.luau && grep -q \"local function registerHost\" src/client/screen_presentation.luau && grep -q \"handle.lastWriteX ~= px\" src/client/screen_presentation.luau && grep -q \"function adapter.settleRects\" src/client/screen_target.luau && test -f src/render/instance_boundary.luau && test -f src/render/rect_pass.luau"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-4::nested-instance-tree",
   "name": "nested-instance-tree",
   "note": "nested-instance-tree: 13 named suite cases must pass; suite, check_elision_census must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "conformance/conformance::core conformance (custom)::settle-runs-after-propagation-and-converges-in-one-flush [UI-RUNTIME-003]",
     "conformance/conformance::core conformance (custom)::settle-callbacks-never-see-a-half-propagated-graph [UI-RUNTIME-003]",
     "conformance/conformance::core conformance (custom)::settle-that-never-converges-hits-the-iteration-cap [UI-RUNTIME-003]",
     "conformance/conformance::core conformance (custom)::settle-unsubscribe-is-idempotent-and-counter-neutral [UI-LIFE-002]",
     "measure_publish_settle::a measure→publish cycle settles inside one flush::the round trip costs NO extra reactive flush — the control is in the case",
     "measure_publish_settle::a measure→publish cycle settles inside one flush::a surface is FINAL when the geometry change returns — no correction a frame later",
     "measure_publish_settle::a measure→publish cycle settles inside one flush::...and the published reservation is REALLY applied — the fix is not 'never publish'",
     "measure_publish_settle::a measure→publish cycle settles inside one flush::a round trip that will not converge is a LOUD error, not a hang or a silent frame",
     "measure_publish_settle::a measure→publish cycle whose publication is a PROP settles in the same solve::an ENV change publishes and lands in the same write — the settle-pass arrival"
    ],
    "shell": "grep -qF \"settle\" src/core/contract.luau && grep -qF \"FEEDBACK_ROUND_CAP\" src/core/custom.luau"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-4::core-settle-phase",
   "name": "core-settle-phase",
   "note": "core-settle-phase: 9 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001, UI-PERF-001.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001",
    "UI-PERF-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "grid_column_flow::UI.Grid column flow — the transpose of the one arithmetic::every cell's rect is the row-flow rect with both axes exchanged",
     "grid_column_flow::UI.Grid column flow — the transpose of the one arithmetic::`flow` on a ROW grid is a diagnostic, not a second layout",
     "grid_measure_arrange::the grid's plan has ONE owner, and both passes read it::`grid.measure` and `grid.arrange` both call the SAME flow plan",
     "grid_measure_arrange::the grid's plan has ONE owner, and both passes read it::the solver keeps no grid arithmetic of its own — it only holds the cache's home",
     "grid_column_flow::UI.Grid column flow — the transpose of the one arithmetic::minColumnWidth = 'intrinsic' measures the LANE axis, not always the width",
     "grid_row::the two authoring errors row mode can produce::a MIX of rows and loose cells is refused, and the grid keeps the flow reading",
     "virtual_grid::VirtualGrid: the extent index is the shipped one, its unit is a LINE::the canvas extent IS virtual_extents.uniform over ceil(N / lanes) lines — a differential",
     "virtual_grid::VirtualGrid: only the windowed cells are ever built::the build count does not depend on N — 100 items and 10 000 build the same cells",
     "virtual_hgrid::VirtualHGrid: only the windowed cells are ever built::...and the build count is INDEPENDENT OF N — the property 'only as needed' names",
     "virtual_hgrid::VirtualHGrid: the mounted band's lanes are the flow grid's own arithmetic::the lane heights match a plain UI.Grid { flow = 'column' } of the same cross extent",
     "virtual_grid::VirtualGrid: the scroll does not jump::when the COLUMN COUNT changes, the item at the top of the viewport stays at the top",
     "virtual_hgrid::VirtualHGrid: the axis the control used to refuse::`width` is refused on the horizontal axis, and the message names the route",
     "virtual_hgrid::VirtualHGrid: the axis the control used to refuse::...and an axis that is neither is still refused, naming the legal pair",
     "virtual_grid::VirtualGrid: what the spec accepts::columns must be a positive INTEGER — 0, a fraction and a negative are all refused"
    ],
    "shell": "test -f tests/grid_column_flow.spec.luau"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-4::virtual-grid-family",
   "name": "virtual-grid-family",
   "note": "virtual-grid-family: 14 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-LAYOUT-001, UI-PERF-001.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001",
    "UI-PERF-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "virtual_extents::virtual_extents: the variable index CONTAINS the uniform one::every question agrees when every extent is equal",
     "virtual_list_variable_extents::VirtualList variable extents: the uniform path is untouched::a per-item function returning one constant is IDENTICAL to the uniform list",
     "virtual_list_measured_extents::VirtualList measured extents: the uniform path is untouched::a measured list of EQUAL rows converges to the uniform list declaring that number",
     "virtual_list_variable_extents::VirtualList variable extents: the geometry is the running offset::the PAINTED canvas spans the running total, not count x one extent",
     "virtual_extents::virtual_extents: the uniform index is today's arithmetic, unchanged::content extent carries no trailing gutter",
     "virtual_extents::virtual_extents: the search is bounded, which is the whole point::windowing a 10,000-item list reads a logarithmic number of extents",
     "virtual_extents::virtual_extents: the search is bounded, which is the whole point::the window never grows with the list",
     "virtual_extents::virtual_extents: the variable index CONTAINS the uniform one::mid-slot, the variable rule windows one MORE row — and that is deliberate",
     "virtual_list_variable_extents::VirtualList variable extents: the geometry is the running offset::keep-visible measures the row's OWN extent, not an average",
     "virtual_list_measured_extents::VirtualList measured extents: laziness survives::convergence is ONE step: a second refresh over the same content writes nothing",
     "virtual_list_measured_extents::VirtualList measured extents: what a measurement is allowed to be::a fractional measure is rounded to whole px, and a zero one is floored to 1",
     "virtual_list_measured_extents::VirtualList measured extents: a fill main axis is refused::a cell that declares fill on the SCROLL axis is refused, naming the row",
     "virtual_list_measured_extents::VirtualList measured extents: a fill main axis is refused::a DECLARED list still accepts a fill cell — the refusal is measured mode's",
     "virtual_list_measured_extents::VirtualList measured extents: what the spec accepts::refuses any other string: \"measured\" is the one word, not an open enum",
     "virtual_list_measured_extents::VirtualList measured extents: what the spec accepts::refuses an estimate on a list that does not measure — a field that drives nothing",
     "virtual_list_variable_extents::VirtualList variable extents: what the spec accepts::refuses a per-item extent that is not a positive number, naming the row",
     "virtual_list_measured_extents::VirtualList measured extents: the lying-extent class is structurally closed::the cell that makes the guard shout under a DECLARATION produces no finding here",
     "virtual_list_slot_guard::newVirtualList: a lying itemExtent is named, with both numbers::A FIXED ROW STOPS BEING REPORTED — the finding does not outlive the defect"
    ],
    "shell": "grep -qF \"IT IS AN OPT-IN FOURTH FORM AND NOT THE DEFAULT, on measured evidence\" src/controls/virtual_list.luau && grep -qF \"itemExtent\" docs/reference/api.md && test -f tests/virtual_list_measured_extents.spec.luau"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-4::variable-item-extents",
   "name": "variable-item-extents",
   "note": "variable-item-extents: 18 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-LAYOUT-001, UI-A11Y-001.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001",
    "UI-A11Y-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "text_line_box::composition.floorPx — the three defects a public function carried::DEFECT 1 — no `ceil`: the floor could be smaller than any box the solver can draw",
     "text_line_box::composition.floorPx — the three defects a public function carried::DEFECT 2 — no SCALE: the player's text preference and the ten-foot floor moved nothing",
     "text_line_box::composition.floorPx — the three defects a public function carried::DEFECT 3 — no TEXT OFFSET: the four preferences this project binds itself to were invisible",
     "text_line_box::text.lineBox — the one implementation of the line-box arithmetic::THE CEIL LANDS ONCE, on the whole product — not per line",
     "text_line_box::text.lineBox — the one implementation of the line-box arithmetic::the offset is ADDITIVE and lands AFTER the scale, never through it",
     "text_line_box::text.lineBox — the one implementation of the line-box arithmetic::the offset's marginal cost does NOT depend on the scale — the invariant behind `additive AFTER`",
     "text_line_box::text.facts — the live text facts, read once for a whole extent pass::a SUB-1 preference reserves at the PAINT scale, never at the smaller one",
     "text_line_box::text.lineBox — the one implementation of the line-box arithmetic::`role` overrides which line height applies, without moving the size",
     "text_line_box::text.lineBox — the one implementation of the line-box arithmetic::is the solver's own two seams, composed in the solver's order",
     "text_line_box::text.lineBox equals what the solver arranges — differentially, over a generated corpus::every row: the predicted line box IS the arranged rect, and the predicted reserved size IS the solve's",
     "text_line_box::text.facts — the live text facts, read once for a whole extent pass::{ env } WITHOUT `use` is refused — a fact read with :get() is right once and then stale",
     "text_line_box::text.lineBox — the one implementation of the line-box arithmetic::refuses a role this theme does not carry, a bad size, a bad line count and an unknown key"
    ],
    "shell": "grep -qF \"THIS FUNCTION WAS WRONG THREE WAYS UNTIL 2026-08-15\" src/layout/composition.luau && grep -qF \"When three consumers make an identical mistake the API is wrong, not\" src/layout/text_fit.luau"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-4::text-line-box",
   "name": "text-line-box",
   "note": "text-line-box: 12 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-A11Y-001, UI-LAYOUT-001.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-A11Y-001",
    "UI-LAYOUT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "measure_memo::the measure fan-out no longer grows with nesting depth::...and the fan-out does not RISE with depth, which is the whole defect",
     "measure_memo::the measure fan-out no longer grows with nesting depth::a 30-deep chain measures each node about twice, not sixteen times",
     "perf_lab::perf lab: the three named levers measure what they claim (device capture 6)::the measure memo serves the fan-out at depth — the L-37 regression pin",
     "measure_memo::the measure memo's key carries the offer only where the offer is read::a `hug` height READS the offer, so it keeps `maxH` in the key",
     "measure_memo::the measure memo's key carries the offer only where the offer is read::a `vwrap` breaks its lines ON the offered height, so it is never height-free",
     "measure_memo::the measure memo's key carries the offer only where the offer is read::a child that can be SQUEEZED makes its vertical stack read the offer too",
     "measure_memo::the measure memo replays the verdicts it cached::the memo does not move geometry — the verdicts are the only thing at risk",
     "measure_memo::the measure memo replays the verdicts it cached::seed 91: a cut label reports truncated = true, so its disclosure path survives",
     "measure_memo::the measure memo replays the verdicts it cached::seed 612: a compact label that does NOT fit keeps its compact verdict",
     "measure_memo::the memo is scoped to trees that can benefit from it::gating the memo on a scroll node changes no geometry on a scroll-free tree"
    ],
    "shell": "grep -qF \"MEMO_ARM_DEPTH\" src/layout/solver.luau && grep -qF \"16.33 distinct heights per node against 1.00 distinct width\" src/layout/solver.luau"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-4::measure-memo-key",
   "name": "measure-memo-key",
   "note": "measure-memo-key: 10 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-PERF-001, UI-LAYOUT-001.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-PERF-001",
    "UI-LAYOUT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "table_virtualized::Table virtualized: only the window is mounted::BOUNDED BY CONSTRUCTION: 5,000 rows build only the windowed cells",
     "table_virtualized::Table virtualized: only the window is mounted::FRAME ONE already fills the body: the seed is the screen, so it over-mounts, never under",
     "table_virtualized::Table virtualized: one arithmetic with newVirtualList::a FLOWING table's row tops agree with the same index (the paths cannot drift)",
     "table_virtualized::Table virtualized: the reorder slot is over the FULL list::a drag while scrolled names a slot two hundred rows down, not a window-relative one",
     "table_virtualized::Table virtualized: model state outlives the window::multi-selection ranges reach rows the window never held",
     "table_virtualized::Table virtualized: the scroll does not jump when the extents move::a text-preference change RE-SOLVES the rows, never rebuilds them",
     "table_virtualized::Table virtualized: the scroll does not jump when the extents move::...and WITHOUT the anchor the same change would move it — the guard is not decorative",
     "table_virtualized::Table virtualized: what is refused, and why::`virtualized` + `scrolls = false` is refused: a block table has no viewport to window against",
     "table_virtualized::Table virtualized: what is refused, and why::`virtualized` + `rowActions` is refused (v1), the way VirtualList refuses reorder + rowActions"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-4::table-virtualized",
   "name": "table-virtualized",
   "note": "table-virtualized: 9 named suite cases must pass; suite must exit zero. Requirements: UI-PERF-001, UI-INPUT-003.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-PERF-001",
    "UI-INPUT-003"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "hud_composition::holdsLane pins a column that has nothing in it::an empty centre column keeps its third and the right cluster does not move",
     "hud_composition::holdsLane pins a column that has nothing in it::without the flag the lane still releases — rule 9 is unchanged",
     "hud_composition::holdsLane pins a column that has nothing in it::holdsLane is refused on a spanning group — a span row has no lane to hold",
     "hud_composition::a zone painting over its neighbour is REPORTED::a well-behaved HUD reports NO collisions",
     "hud_composition::a zone painting over its neighbour is REPORTED::a zone wider than its column names the zone it is painting over",
     "hud_composition::a zone painting over its neighbour is REPORTED::a wide zone with nothing beside it is not a collision",
     "hud_composition::a zone painting over its neighbour is REPORTED::the SCROLL region is excluded — its content is meant to be bigger than its box",
     "hud_composition::a height shrink DEGRADES, in rank order::the least important zone in the failing column gives way first",
     "hud_composition::a height shrink DEGRADES, in rank order::a column that runs out of forms drops its droppable zone, and no other",
     "hud_composition::a height shrink DEGRADES, in rank order::a zone that was never in the failing column keeps its richest form",
     "hud_composition::the nine anchors PARTITION the screen::no two zones intersect, at any height a landscape phone can lose"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-4::hud-composition-collisions",
   "name": "hud-composition-collisions",
   "note": "hud-composition-collisions: 11 named suite cases must pass; suite must exit zero. Requirements: UI-LAYOUT-001, UI-A11Y-001.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001",
    "UI-A11Y-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "scroll_bar_measure::the bar gutter a host spent is published for its SIBLINGS to pay::an X shelf reports it on `bottom` — the side an X scroller reserves from",
     "scroll_bar_measure::the bar gutter a host spent is published for its SIBLINGS to pay::a Y list reports it on `right`",
     "scroll_bar_measure::the bar gutter a host spent is published for its SIBLINGS to pay::a host whose content FITS reserves nothing, and publishes nothing",
     "scroll_bar_measure::the bar gutter a host spent is published for its SIBLINGS to pay::...and a fallback adapter with no bar publishes nothing either",
     "scroll_bar_measure::the bar gutter a host spent is published for its SIBLINGS to pay::an unmounted host stops answering",
     "playlist_columns::newTable: the header's column boundaries are the body's::every column's header spans exactly its cells, with the bar reserved",
     "playlist_columns::newTable: the header's column boundaries are the body's::...and with no bar to reserve, the same grid, and no gutter paid",
     "playlist_columns::newTable: the header's column boundaries are the body's::PRESENT ALONE lands the grids together — the gutter costs no frame at all",
     "playlist_columns::playlist columns: the divider forwards a press it did not turn into a drag::a press and release with no travel sorts the column and commits NO width",
     "playlist_columns::playlist columns: the divider forwards a press it did not turn into a drag::travel UNDER the pointer promotion gate is still a tap; travel over it is a resize",
     "playlist_columns::playlist columns: the divider forwards a press it did not turn into a drag::the guide line does not appear for a press that never becomes a drag",
     "adaptive::RS-A16-D1: a hidden ViewThatFits candidate requests no hit geometry::the minimum-target expander begins the host's pointer capture, through the one shared body"
    ],
    "shell": "grep -qF \"WHY PADDING AND NOT A TRAILING SPACER CHILD\" src/controls/table.luau && grep -qF \"The drift IS the reserve\" src/controls/table.luau"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "parity-round-4::table-gutter-and-divider-press",
   "name": "table-gutter-and-divider-press",
   "note": "table-gutter-and-divider-press: 12 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-INPUT-003, UI-LAYOUT-001.",
   "phase": "parity-round-4",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-003",
    "UI-LAYOUT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_manifest_integrity",
     "suite",
     "rascalrally-suite"
    ],
    "resultIds": [
     "smoke::Facet public surface::loads headless under lune with the phase-1 API",
     "smoke::Facet public surface::the public API drives a full slice without touching internals",
     "conformance/conformance::core conformance (custom)::signal-read-write [UI-RUNTIME-003]",
     "conformance/conformance::core conformance (custom)::memo-derives-and-updates [UI-RUNTIME-003]",
     "conformance/conformance::core conformance (custom)::transaction-batches-observer-to-one-fire [UI-RUNTIME-003]",
     "conformance/conformance::core conformance (custom)::glitch-free-diamond [UI-RUNTIME-003]",
     "conformance/conformance::core conformance (custom)::equal-write-skipped [UI-RUNTIME-001]",
     "conformance/conformance::core conformance (custom)::custom-equality-respected [UI-RUNTIME-001]",
     "conformance/conformance::core conformance (custom)::dynamic-dependencies-swap-atomically [UI-RUNTIME-003]",
     "conformance/conformance::core conformance (custom)::cycle-reported-not-hung [UI-RUNTIME-003]",
     "conformance/conformance::core conformance (custom)::write-during-memo-is-error [UI-RUNTIME-003]",
     "conformance/conformance::core conformance (custom)::effect-runs-post-commit-and-writes-schedule-later-round [UI-RUNTIME-003]",
     "conformance/conformance::core conformance (custom)::feedback-loop-hits-iteration-cap [UI-RUNTIME-003]",
     "conformance/conformance::core conformance (custom)::settle-runs-after-propagation-and-converges-in-one-flush [UI-RUNTIME-003]",
     "conformance/conformance::core conformance (custom)::settle-callbacks-never-see-a-half-propagated-graph [UI-RUNTIME-003]",
     "conformance/conformance::core conformance (custom)::settle-that-never-converges-hits-the-iteration-cap [UI-RUNTIME-003]",
     "conformance/conformance::core conformance (custom)::settle-unsubscribe-is-idempotent-and-counter-neutral [UI-LIFE-002]",
     "conformance/conformance::core conformance (custom)::memo-error-quarantined [UI-RUNTIME-003]",
     "conformance/conformance::core conformance (custom)::scope-dispose-reverse-order-idempotent [UI-LIFE-001]",
     "conformance/conformance::core conformance (custom)::child-scope-disposed-with-parent-borrowed-not-disposed [UI-LIFE-001]",
     "conformance/conformance::core conformance (custom)::memory-neutral-churn [UI-LIFE-002]",
     "conformance/conformance::core conformance (custom)::no-spurious-fire-on-unchanged-recompute [UI-RUNTIME-001]",
     "conformance/conformance::core conformance (custom)::mid-transaction-derived-reads-fresh [UI-RUNTIME-003]",
     "conformance/conformance::core conformance (custom)::transaction-revert-produces-no-fire [UI-RUNTIME-003]",
     "conformance/conformance::core conformance (custom)::nan-equal-write-skipped [UI-RUNTIME-001]",
     "conformance/conformance::core conformance (custom)::scope-cleanup-quarantined-and-early-child-is-not-a-double-dispose [UI-LIFE-001]",
     "conformance/conformance::core conformance (custom)::double-dispose-detected [UI-LIFE-001]",
     "conformance/conformance::core conformance (custom)::observer-disposed-by-sibling-does-not-fire [UI-LIFE-001]",
     "conformance/conformance::core conformance (custom)::observer-added-mid-flush-fires-next-flush-only [UI-RUNTIME-003]",
     "conformance/conformance::core conformance (custom)::disposed-observer-never-fires [UI-LIFE-001]",
     "conformance/conformance::micro-screens (custom)::micro-settings-row [UI-RUNTIME-001]",
     "conformance/conformance::micro-screens (custom)::micro-live-hud-value [UI-RUNTIME-001]",
     "conformance/conformance::micro-screens (custom)::micro-keyed-collection [UI-RUNTIME-002]",
     "conformance/conformance::a11y/l10n corpus (custom)::l10n-pseudo-loc-expansion [UI-ADAPT-001]",
     "conformance/conformance::a11y/l10n corpus (custom)::l10n-tall-script-line-height [UI-ADAPT-001]",
     "conformance/conformance::a11y/l10n corpus (custom)::l10n-cjk-full-em [UI-ADAPT-001]",
     "conformance/conformance::a11y/l10n corpus (custom)::l10n-emoji-combining-over-reservation [UI-ADAPT-001]",
     "conformance/conformance::a11y/l10n corpus (custom)::l10n-bidi-rtl-declared-fallback [UI-ADAPT-001]",
     "conformance/conformance::a11y/l10n corpus (custom)::a11y-text-size-variants [UI-ADAPT-001]",
     "conformance/conformance::a11y/l10n corpus (custom)::a11y-effective-target-by-display-class [UI-ADAPT-001]",
     "conformance/conformance::a11y/l10n corpus (custom)::a11y-target-not-shrink-under-text-scale [UI-ADAPT-001]",
     "conformance/conformance::a11y/l10n corpus (custom)::a11y-preferred-transparency-readable [UI-ADAPT-001]",
     "conformance/conformance::a11y/l10n corpus (custom)::a11y-reduced-motion-substitution [UI-ADAPT-001]",
     "conformance/conformance::a11y/l10n corpus (custom)::a11y-contrast-token-pairs-and-focus [UI-ADAPT-001]",
     "conformance/conformance::a11y/l10n corpus (custom)::corpus-text-in-content-box-or-truncation-declared [UI-ADAPT-001]",
     "conformance/conformance::a11y/l10n corpus (custom)::corpus-content-within-clip-containers [UI-ADAPT-001]",
     "conformance/conformance::a11y/l10n corpus (custom)::corpus-valid-finite-rects [UI-ADAPT-001]",
     "conformance/conformance::a11y/l10n corpus (custom)::corpus-determinism-identical-dumps [UI-ADAPT-001]",
     "replication::ReplicatedSnapshot::applies newer, ignores stale and duplicate revisions",
     "replication::ReplicatedCollection::applies in-order patches, ignores stale/dup",
     "replication::ReplicatedCollection::gap requests one resnapshot, refuses patches until it lands, then converges",
     "replication::ReplicatedCollection::reconnect = resnapshot from any newer authoritative revision",
     "replication::ReplicatedCollection::a resnapshot at the CURRENT revision re-bases while awaiting, instead of wedging",
     "replication::ReplicatedCollection::an equal-revision resnapshot is still stale when no gap is outstanding",
     "replication::ReplicatedCollection::a throwing requestResnapshot does not latch the gap — the next patch asks again",
     "replication::Mutation::optimistic apply reconciles on confirm via authoritative ingest",
     "replication::Mutation::rejection restores optimistic state; duplicate responses are idempotent",
     "replication::Mutation::confirm racing ahead of the snapshot cannot leave permanent divergence",
     "replication::Mutation::send while pending is a structured error, not silent corruption",
     "replication::Mutation::the envelope carries requestId and expectedRevision for server validation",
     "replication::Mutation::a throwing optimistic apply still returns the envelope and leaves the mutation usable",
     "replication::Mutation::reset() escapes a stuck pending request, restores, and the stale id stays ignored",
     "replication::Mutation::a throwing restore cannot unwind reset() either",
     "replication::two clients, one semantic snapshot::independent environments and presentation state converge on the same semantics",
     "layout::layout: sizing algebra::vstack content height = children + gaps + padding",
     "layout::layout: sizing algebra::fill children share remaining space by weight with exact integer total",
     "layout::layout: sizing algebra::uneven fill rounding is deterministic and lossless",
     "layout::layout: wrapped text::narrower width wraps to more lines and taller height",
     "layout::layout: wrapped text::unknown font takes the pending path with a conservative (larger) fallback",
     "layout::layout: wrapped text::text node height flows into stack layout",
     "layout::layout: safe area::coreSafeContent insets the content rect; edgeToEdge does not",
     "layout::layout: ScrollView::reports content size and scroll overflow when content exceeds viewport",
     "layout::layout: ScrollView::percent height inside the unbounded scroll axis produces a diagnostic, not silence",
     "layout::layout: ScrollView::a stack that cannot hold its content reports it instead of painting off-screen",
     "layout::layout: ScrollView::a ViewThatFits candidate that lost does not report an overflow",
     "layout::layout: ScrollView::a scroll canvas covers content laid out in fill columns that wrap",
     "layout::layout: ScrollView::an aspect box paired with fill is measured at the extent it will receive",
     "layout::layout: ScrollView::a stack's own align is the default for its children",
     "layout::layout: ScrollView::a child's own align still beats the container's",
     "layout::layout: ScrollView::a container align of stretch equalises children that would be ragged",
     "layout::layout: ScrollView::a scrolling region lays content inside the scrollbar, not under it",
     "layout::layout: ScrollView::a scrolling region that does not overflow reserves nothing",
     "layout::layout: ScrollView::ViewThatFits chooses against its own width, not its parent's offer",
     "layout::layout: ScrollView::a stack that exactly fits reports no overflow",
     "layout::layout: purity and determinism::solves a frozen snapshot without mutation and double-solve dumps are identical",
     "layout::layout: ScrollView fill stretch (phase-3 regression guard)::cross-axis fill children stretch to the scroll viewport",
     "layout::RS-DIR9: a per-word sum may never reserve LESS than the engine draws::the join slack is added once per inter-word gap when the widths are ENGINE-exact",
     "layout::RS-DIR9: a per-word sum may never reserve LESS than the engine draws::three gaps carry three steps: the error accumulates and so does the reservation",
     "layout::RS-DIR9: a per-word sum may never reserve LESS than the engine draws::a single word carries NO slack: there is no junction to be wrong about",
     "layout::RS-DIR9: a per-word sum may never reserve LESS than the engine draws::an ESTIMATED line is untouched: the estimator already over-reserves",
     "layout::RS-DIR9: a per-word sum may never reserve LESS than the engine draws::the slack rides the WRAP decision too, so a line breaks before it clips",
     "layout::A-TXT: lineLimit caps the reserved box::an uncapped label reserves every line it wraps to",
     "layout::A-TXT: lineLimit caps the reserved box::a capped label stops at the cap and says it was cut",
     "layout::A-TXT: lineLimit caps the reserved box::a cap the text does not reach changes nothing",
     "layout::A-TXT: lineLimit caps the reserved box::a cap below one line still draws one — it is a limit, not an eraser",
     "layout::A-TXT: lineLimit caps the reserved box::the solver honours a node's lineLimit",
     "layout::layout: hug (content, capped by the offer)::SHORT content: the box stops at the last child instead of filling",
     "layout::layout: hug (content, capped by the offer)::TALL content: the box stops at the offer instead of running off the screen",
     "layout::layout: hug (content, capped by the offer)::a hug ScrollView hugs a short list AND scrolls a long one — one declaration",
     "layout::layout: hug (content, capped by the offer)::a hug child's ceiling is what its SIBLINGS left it, not the whole stack",
     "layout::layout: hug (content, capped by the offer)::two hug siblings each see what the one before it left",
     "layout::layout: hug (content, capped by the offer)::a hug child resolves BEFORE fill shares the remainder",
     "layout::layout: hug (content, capped by the offer)::min/max clamp the content BEFORE the offer does",
     "layout::layout: a fill anchor child pays for its own offset::a leading-anchored bar pushed down loses exactly that much height",
     "layout::layout: a fill anchor child pays for its own offset::a trailing-anchored bar pulled in loses exactly that much width",
     "layout::layout: a fill anchor child pays for its own offset::a CENTRED bar gives back twice its offset — it loses room at both ends",
     "layout::layout: a fill anchor child pays for its own offset::no offset is still the whole box — the common case does not move",
     "layout::layout: a fill anchor child pays for its own offset::pushed OUT past its own anchored edge is left alone — that is deliberate",
     "layout::layout: a fill ZStack child pays for its own MARGIN::a top margin is room the child does not get, not a displacement",
     "layout::layout: a fill ZStack child pays for its own MARGIN::margins on both sides of an axis both come out of the fill",
     "layout::layout: a fill ZStack child pays for its own MARGIN::a number margin applies to all four sides",
     "layout::layout: a fill ZStack child pays for its own MARGIN::a filled axis ignores alignment — there is nothing left to align",
     "layout::layout: a fill ZStack child pays for its own MARGIN::a NON-fill child keeps the alignment it always had (no silent re-flow)",
     "layout::layout: a fill ZStack child pays for its own MARGIN::margins wider than the stack clamp to zero rather than going negative",
     "layout::layout: a HUGGING ZStack child pays for its own margin too::an END-aligned child stops a FAR margin short of the edge",
     "layout::layout: a HUGGING ZStack child pays for its own margin too::...on the vertical axis too",
     "layout::layout: a HUGGING ZStack child pays for its own margin too::a NEAR margin still pushes an end-aligned child AWAY from its own edge",
     "layout::layout: a HUGGING ZStack child pays for its own margin too::a CENTRED child centres in the box it was GIVEN, not the box it was denied",
     "layout::layout: a HUGGING ZStack child pays for its own margin too::a START-aligned child is unmoved — it always sat at its near margin",
     "layout::solver — a ZStack child that overflows its box is reported::names the CHILD and states the overrun",
     "layout::solver — a ZStack child that overflows its box is reported::a child that FITS reports nothing — the check is not noise",
     "layout::solver — a ZStack child that overflows its box is reported::`intentionalOverlap` is the opt-out for layouts whose job IS to stack over",
     "layout::solver — a ZStack child that overflows its box is reported::it is a REPORT, not a failure: the layout still resolves",
     "transform_footprint::Facet.layout.transformFootprint (gap 33)::is reachable on the public Facet table",
     "transform_footprint::Facet.layout.transformFootprint (gap 33)::reproduces the exact device defect measurement (api.md:325)",
     "transform_footprint::Facet.layout.transformFootprint (gap 33)::an unscaled, unrotated box reserves exactly its own size",
     "transform_footprint::Facet.layout.transformFootprint (gap 33)::a 90-degree rotation swaps width and height",
     "transform_footprint::Facet.layout.transformFootprint (gap 33)::scale alone (no rotation) scales both axes directly",
     "transform_footprint::Facet.layout.transformFootprint (gap 33)::always rounds UP, never clipping a fraction of a pixel off an edge",
     "focus::logical focus::navigates the screen ring and wraps at the edges (director 2026-07-20)",
     "focus::logical focus::modal traps navigation with wrap and restores previous focus on pop",
     "focus::logical focus::nested modals restore in LIFO order",
     "focus::logical focus::removing the focused item falls to the nearest surviving neighbor",
     "focus::logical focus::focus changes are observable client-local state",
     "focus::focus graph: the caller's tables are never written::a table.freeze'd flat scope pushes, navigates and removes without erroring",
     "focus::focus graph: the caller's tables are never written::a table.freeze'd grouped scope pushes and navigates, groups included",
     "focus::focus graph: the caller's tables are never written::one NavigationGroup literal pushed onto two scopes does not share a predicate map",
     "focus::focus graph: the caller's tables are never written::replaceGroups copies too: the caller's array survives a later remove",
     "mount::mount: factory-once and dirty classes::mounts each node exactly once with stable paths",
     "mount::mount: factory-once and dirty classes::a non-structural prop change dirties ONLY its node with declared classes and reruns no factory",
     "mount::mount: factory-once and dirty classes::duplicate explicit sibling ids are a hard error",
     "mount::mount: When region::mounts/unmounts the branch scope and dirties only the region",
     "mount::mount: When hands its branch the branch scope::thenView is handed the BRANCH scope — labelled with the branch path",
     "mount::mount: When hands its branch the branch scope::the initial-true mount hands the same scope the later flip does",
     "mount::mount: When hands its branch the branch scope::a resource owned on the handed scope is released when the branch closes",
     "mount::mount: When hands its branch the branch scope::a flip back gets a FRESH scope: nothing from the previous entry survives",
     "mount::mount: When hands its branch the branch scope::fifty open/close cycles leave no scope and no cleanup behind",
     "mount::mount: ForEach region::applies add/remove/move with keyed scopes; moves never remount",
     "mount::mount: ForEach region::duplicate keys are a hard error",
     "mount::mount: churn and dumps::50 mount/dispose cycles return mount and core registries to baseline",
     "mount::mount: churn and dumps::dumps are deterministic and carry dirty flags",
     "node_facts::paths.stripThenSegments (gap 15)::removes a single /then segment a UI.When branch injects",
     "node_facts::paths.stripThenSegments (gap 15)::removes every /then segment across nested Whens",
     "node_facts::paths.stripThenSegments (gap 15)::a path with no /then segment is unchanged",
     "node_facts::paths.stripThenSegments (gap 15)::a root-level /then is removed too",
     "node_facts::controller.mountedPathOf (gap 15)::resolves a node's DECLARED path to its LIVE path across a UI.When prefix",
     "node_facts::controller.mountedPathOf (gap 15)::a path already live at the literal string resolves to itself",
     "node_facts::controller.mountedPathOf (gap 15)::an unmounted declared path answers nil, not a false substring match",
     "node_facts::controller.screenRectOf relativeTo (gap 37)::equals the manual root-relative-then-subtract arithmetic it replaces",
     "node_facts::controller.screenRectOf relativeTo (gap 37)::a node relative to itself is {0,0,w,h}",
     "node_facts::controller.screenRectOf relativeTo (gap 37)::answers nil when either side is not live, never a partial rect",
     "node_facts::controller.screenRectOf relativeTo (gap 37)::the one-argument form is unaffected (no regression)",
     "node_facts::VirtualList.positionOf (gap 40)::answers a key's current 1-based index in the data order",
     "node_facts::VirtualList.positionOf (gap 40)::tracks a live reorder of the data, not a construction-time snapshot",
     "profile_scopes::profile scopes are a closed, bounded set::every declared label is prefixed so LibMP can select exactly this set",
     "profile_scopes::profile scopes are a closed, bounded set::an undeclared scope name is refused at the call, naming the offender",
     "profile_scopes::profile scopes are a closed, bounded set::refusing an undeclared name opens no scope at all",
     "profile_scopes::a span is balanced on every exit path::a normal return closes the scope and returns the body's value",
     "profile_scopes::a span is balanced on every exit path::an ERRORING body still closes the scope, and the error value is unchanged",
     "profile_scopes::a span is balanced on every exit path::a non-string error value survives the span unchanged",
     "profile_scopes::a span is balanced on every exit path::nested spans unwind in order when the INNER one throws",
     "profile_scopes::a span is balanced on every exit path::a THROWING hook cannot strand the counters or the caller (RR-1/RR-2)",
     "profile_scopes::a span is balanced on every exit path::the counters see the same opens and closes the recorder does",
     "profile_scopes::scopes stay out of the way when the host cannot profile::with no engine hooks the body runs, unwrapped, and no counter moves",
     "profile_scopes::scopes stay out of the way when the host cannot profile::scopes are OFF by default: production pays no pcall and keeps its tracebacks",
     "profile_scopes::scopes stay out of the way when the host cannot profile::setEnabled(false) suppresses instrumentation even where the engine exists",
     "profile_scopes::scopes stay out of the way when the host cannot profile::an error raised while scopes are OFF keeps its ordinary position prefix",
     "profile_scopes::a real render pass emits the phase set and stays balanced::mount -> solve -> commit produces only declared labels, and closes them all",
     "profile_scopes::a real render pass emits the phase set and stays balanced::the number of scopes a frame opens does NOT scale with the row count",
     "perf_lab::perf lab: the dataset is deterministic (PL-2)::the same seed, count and content produce a byte-identical row set",
     "perf_lab::perf lab: the dataset is deterministic (PL-2)::a different seed produces a different row set, and the digest says so",
     "perf_lab::perf lab: the dataset is deterministic (PL-2)::each content variant is its own workload, not a relabelling of the same one",
     "perf_lab::perf lab: the dataset is deterministic (PL-2)::an unknown content variant is refused, naming it",
     "perf_lab::perf lab: the dataset is deterministic (PL-2)::the ramp ladder is bounded by a declared ceiling (PL-11)",
     "perf_lab::perf lab: a resize step is the resize a device performs::drives the adapter's whole fact-group, batched into one flush",
     "perf_lab::perf lab: a resize step is the resize a device performs::a resize step is TWO reactive flushes — the round trip no longer buys a third",
     "perf_lab::perf lab: the resize storm reports both costs separately::separates a resize from a single-value change, and keeps the ratio visible",
     "perf_lab::incremental layout Stage 1: boundary detection sizes the prize::reports how much of the real workload a single change would touch",
     "perf_lab::perf lab: the haptics counters (task 11)::is OFF by default, and the counter line says so rather than omitting itself",
     "perf_lab::perf lab: the haptics counters (task 11)::a whole dense scroll with haptics ON moves NO haptic counter — it is event-driven",
     "perf_lab::perf lab: the haptics counters (task 11)::...and the SAME adapter hears a real verb, so the zero above is not a dead binding",
     "perf_lab::perf lab: the haptics counters (task 11)::turning it back off disposes the adapter, and the line returns to `off`",
     "perf_lab::perf lab: the sensory-cascade pass measures something (director ask 2026-08-13)::runs four arms, and every one of them really pressed",
     "perf_lab::perf lab: the sensory-cascade pass measures something (director ask 2026-08-13)::the declared arms really carry the verb — the arms are not three copies",
     "perf_lab::perf lab: selectors and workload identity (PL-2/PL-10)::every declared workload names only passes the scenario implements",
     "perf_lab::perf lab: selectors and workload identity (PL-2/PL-10)::selecting a scenario adopts its implementation and content",
     "perf_lab::perf lab: selectors and workload identity (PL-2/PL-10)::a settings pair that is not `key=value` is REFUSED, not silently dropped",
     "perf_lab::perf lab: selectors and workload identity (PL-2/PL-10)::an unknown scenario and an unknown setting are both refused by name",
     "perf_lab::perf lab: selectors and workload identity (PL-2/PL-10)::the row count is clamped to the declared ceiling rather than honoured blindly",
     "perf_lab::perf lab: exactly one implementation mounts (PL-5)::a second mount is refused, naming the rule",
     "perf_lab::perf lab: exactly one implementation mounts (PL-5)::the native reference refuses to mount where the host seam is absent, rather than silently mounting nothing",
     "perf_lab::perf lab: virtualization stays bounded (PL-3/PL-4)::2000 logical rows mount a window, not a collection",
     "perf_lab::perf lab: virtualization stays bounded (PL-3/PL-4)::the window stays bounded across a full programmatic scroll sweep",
     "perf_lab::perf lab: virtualization stays bounded (PL-3/PL-4)::the bound is not a bigger row count away from firing: 20 000 rows window the same",
     "perf_lab::perf lab: virtualization stays bounded (PL-3/PL-4)::NEGATIVE CONTROL: the declared window fault makes the mount FAIL, naming the defect",
     "perf_lab::perf lab: virtualization stays bounded (PL-3/PL-4)::scrolling a virtualized list does NOT grow the reactive core (the row must own its controls)",
     "perf_lab::perf lab: virtualization stays bounded (PL-3/PL-4)::collection churn keeps the window bounded through insert, remove and reorder",
     "perf_lab::perf lab: the workload paints inside its own boxes at every viewport (PL-3)::mounts and windows cleanly at 320x640 (narrowest supported phone portrait)",
     "perf_lab::perf lab: the workload paints inside its own boxes at every viewport (PL-3)::mounts and windows cleanly at 359x718 (the device that exposed the defect)",
     "perf_lab::perf lab: the workload paints inside its own boxes at every viewport (PL-3)::mounts and windows cleanly at 390x844 (compact phone portrait)",
     "perf_lab::perf lab: the workload paints inside its own boxes at every viewport (PL-3)::mounts and windows cleanly at 667x375 (small phone landscape)",
     "perf_lab::perf lab: the workload paints inside its own boxes at every viewport (PL-3)::mounts and windows cleanly at 844x390 (compact phone landscape)",
     "perf_lab::perf lab: the workload paints inside its own boxes at every viewport (PL-3)::mounts and windows cleanly at 1080x810 (tablet landscape)",
     "perf_lab::perf lab: the workload paints inside its own boxes at every viewport (PL-3)::mounts and windows cleanly at 1280x720 (desktop)",
     "perf_lab::perf lab: the workload paints inside its own boxes at every viewport (PL-3)::mounts and windows cleanly at 1920x1080 (console / ten-foot)",
     "perf_lab::perf lab: the workload paints inside its own boxes at every viewport (PL-3)::survives ROTATION after mounting — the case a real device found",
     "perf_lab::perf lab: the workload paints inside its own boxes at every viewport (PL-3)::the scenario cycler steps through every workload and WRAPS at both ends",
     "perf_lab::perf lab: the workload paints inside its own boxes at every viewport (PL-3)::the cycler reports its position, so the panel can say which of the nine you are on",
     "perf_lab::perf lab: the workload paints inside its own boxes at every viewport (PL-3)::cycling actually REMOUNTS the workload, it does not just relabel it",
     "perf_lab::perf lab: the workload paints inside its own boxes at every viewport (PL-3)::a viewport change DROPS the overlay's stale reservation before the workload re-solves",
     "perf_lab::perf lab: the workload paints inside its own boxes at every viewport (PL-3)::the full resize pass leaves the workload MOUNTED, so the next pass has something to measure",
     "perf_lab::perf lab: the workload paints inside its own boxes at every viewport (PL-3)::selecting a scenario RESTORES the settings it does not declare",
     "perf_lab::perf lab: the workload paints inside its own boxes at every viewport (PL-3)::the identity workload walks its whole collection cleanly at every phone width",
     "perf_lab::perf lab: the workload paints inside its own boxes at every viewport (PL-3)::Run all sweeps every workload and ONE failure does not end the sweep",
     "perf_lab::perf lab: the workload paints inside its own boxes at every viewport (PL-3)::Run all ends by saying how to take the dump",
     "perf_lab::perf lab: the workload paints inside its own boxes at every viewport (PL-3)::the emergency stop halts Run all between workloads and says where it stopped",
     "perf_lab::perf lab: the workload paints inside its own boxes at every viewport (PL-3)::NEGATIVE CONTROL: a row that cannot fit its slot FAILS the mount",
     "perf_lab::perf lab: run, stop, reset and clean capture (PL-10/PL-11)::running a scenario executes its declared sequence and records each pass separately",
     "perf_lab::perf lab: run, stop, reset and clean capture (PL-10/PL-11)::the emergency stop halts a running sequence between passes",
     "perf_lab::perf lab: run, stop, reset and clean capture (PL-10/PL-11)::the ramp climbs its declared ladder and stops when told",
     "perf_lab::perf lab: run, stop, reset and clean capture (PL-10/PL-11)::reset returns to the idle baseline and PROVES the workload is unmounted",
     "perf_lab::perf lab: run, stop, reset and clean capture (PL-10/PL-11)::clean capture DISMISSES the overlay rather than hiding it",
     "perf_lab::perf lab: a capture row is admissible or it is refused (PL-9)::a fully conditioned row is admissible and carries the whole workload identity",
     "perf_lab::perf lab: a capture row is admissible or it is refused (PL-9)::a row missing its conditions is INADMISSIBLE and says which ones",
     "perf_lab::perf lab: a capture row is admissible or it is refused (PL-9)::a Studio row cannot become a device row by relabelling",
     "perf_lab::perf lab: a capture row is admissible or it is refused (PL-9)::two captures are the same workload only when every identity field agrees",
     "perf_lab::perf lab: the row is the shape the plan requires (PL-3)::a row carries an image, a two-line text stack, and three controls",
     "perf_lab::perf lab: the row is the shape the plan requires (PL-3)::the identity line keeps its full-value disclosure — the profile is not bought with accessibility",
     "perf_lab::perf lab: the row is the shape the plan requires (PL-3)::the mounted-window rule is arithmetic over the viewport, not a remembered constant",
     "perf_lab::perf lab: installing a theme installs BOTH halves (L-26)::selecting a theme publishes its METRICS to the environment, not only its art",
     "perf_lab::perf lab: installing a theme installs BOTH halves (L-26)::...so the SOLVER reserves the recipe's contentInsets a lifted label is painted into",
     "perf_lab::perf lab: installing a theme installs BOTH halves (L-26)::a theme the environment does not wear is REFUSED, not painted half-on",
     "perf_lab::perf lab: installing a theme installs BOTH halves (L-26)::the FLAT row geometry is byte-identical — which is why the workload version holds",
     "perf_lab::perf lab: installing a theme installs BOTH halves (L-26)::a themed row reserves the recipe's vertical inset per control line",
     "perf_lab::perf lab: profileWindow cannot be aimed at nothing (device captures 1 and 2)::REFUSES the raw-Roblox reference unless it is asked for by name",
     "perf_lab::perf lab: profileWindow cannot be aimed at nothing (device captures 1 and 2)::...and the refusal names the explicit spelling that overrides it",
     "perf_lab::perf lab: profileWindow cannot be aimed at nothing (device captures 1 and 2)::refuses a pass the workload does not declare, and lists the ones it does",
     "perf_lab::perf lab: profileWindow cannot be aimed at nothing (device captures 1 and 2)::refuses an unknown workload and lists the legal set",
     "perf_lab::perf lab: profileWindow cannot be aimed at nothing (device captures 1 and 2)::loops ONE pass — not the workload's whole declared sequence",
     "perf_lab::perf lab: profileWindow cannot be aimed at a pass that does nothing (device capture 5)::loops the workload's declared PROFILE pass, not blindly its first one",
     "perf_lab::perf lab: profileWindow cannot be aimed at a pass that does nothing (device capture 5)::...and an explicitly named pass still overrides it",
     "perf_lab::perf lab: profileWindow cannot be aimed at a pass that does nothing (device capture 5)::reports what ONE lap provoked, so an empty capture is visible before it is taken",
     "perf_lab::perf lab: profileWindow cannot be aimed at a pass that does nothing (device capture 5)::says the readout is UNMEASURED rather than zero when the profiler is off",
     "perf_lab::perf lab: the three named levers measure what they claim (device capture 6)::the declared arms ARE the installed arms — the two files cannot drift apart",
     "perf_lab::perf lab: the three named levers measure what they claim (device capture 6)::the nine shape arms are NINE TREES, not nine copies of the control",
     "perf_lab::perf lab: the three named levers measure what they claim (device capture 6)::the measure memo serves the fan-out at depth — the L-37 regression pin",
     "perf_lab::perf lab: the three named levers measure what they claim (device capture 6)::every rep provokes exactly ONE full solve — a rep that dirties nothing measures nothing",
     "perf_lab::perf lab: the three named levers measure what they claim (device capture 6)::the incremental A/B is TWO configurations: one skips nothing, the other skips almost everything",
     "perf_lab::perf lab: the three named levers measure what they claim (device capture 6)::an off-window edit provokes ONE solve and NO structural sync",
     "perf_lab::perf lab: the three named levers measure what they claim (device capture 6)::ONE LAP FITS A 60-FRAME WINDOW, and every rep yields a frame",
     "perf_lab::perf lab: the host-move lever measures a real host, not a label::hosted arms have a real ScrollView ancestor; unhosted arms do not — the structural oracle",
     "perf_lab::perf lab: the host-move lever measures a real host, not a label::N IS ACTUALLY SWEPT — Large is five times Small's rows and leaves, not a relabeled copy",
     "perf_lab::perf lab: the host-move lever measures a real host, not a label::every rep provokes exactly ONE full solve, and the SAME commit cadence hosted and unhosted",
     "perf_lab::perf lab overlay: the arm selector aims Profile 1 without a payload::advances through the declared arms, wraps, and starts from what Profile would pick",
     "perf_lab::perf lab overlay: the arm selector aims Profile 1 without a payload::the pointer is PER WORKLOAD — cycling away and back does not re-aim the session",
     "perf_lab::perf lab overlay: the arm selector aims Profile 1 without a payload::Profile 1 actually loops the arm the pointer names",
     "perf_lab::perf lab overlay: the arm selector aims Profile 1 without a payload::a workload that declares no passes says so instead of doing nothing",
     "perf_lab::perf lab overlay: the status line is readable, not one character wide::takes a real share of the panel at the reporter's device viewport, and wraps",
     "perf_lab::perf lab overlay: the status line is readable, not one character wide::...and on the narrowest supported phone, where it needs the most lines",
     "perf_lab::perf lab: the profiling loop is stoppable, and does not depend on the pass to yield::a pass that resets emergencyStopped CANNOT keep the loop alive",
     "perf_lab::perf lab: the profiling loop is stoppable, and does not depend on the pass to yield::stop() ends the loop even though the pass clears the run flag",
     "perf_lab::perf lab: the motion-flight pass profiles a withAnimation flight::takes four arms, and both flights seed the SAME record count",
     "perf_lab::perf lab: the motion-flight pass profiles a withAnimation flight::takes a PAINT arm on the same record count, plus an off-path control it cleans up",
     "perf_lab::perf lab: the motion-flight pass profiles a withAnimation flight::takes its own surface away again, and leaves no record behind",
     "perf_lab::perf lab: the guide's workload table matches the registry (T15)::every declared workload has a row in the chapter, and the chapter invents none",
     "perf_lab::perf lab: the guide's workload table matches the registry (T15)::the spelled-out count in the heading is the real one",
     "perf_lab::perf lab: the live counters and the capture row read ONE block (T15)::every counter the live read publishes is on the exported row, and vice versa",
     "perf_lab::perf lab: the live counters and the capture row read ONE block (T15)::the block carries the counters the release waves landed, not the ones they replaced",
     "perf_lab::perf lab: the live counters and the capture row read ONE block (T15)::the module-load marks come from the HOST, and a host with none says so",
     "perf_lab::the lab's registry satisfies the SHARED runner's contract::the runner asks for something, or every check below is vacuous",
     "perf_lab::the lab's registry satisfies the SHARED runner's contract::every registry field the runner reads is exported by the LAB",
     "perf_lab::the lab's registry satisfies the SHARED runner's contract::...and by the GALLERY, which is the control",
     "perf_lab::the lab's registry satisfies the SHARED runner's contract::the project MOUNTS what the lab exports, or the child is never there to find",
     "measure_memo::the measure memo replays the verdicts it cached::seed 132: a compact label that FITS is not marked compact by a stale measure",
     "measure_memo::the measure memo replays the verdicts it cached::seed 612: a compact label that does NOT fit keeps its compact verdict",
     "measure_memo::the measure memo replays the verdicts it cached::seed 91: a cut label reports truncated = true, so its disclosure path survives",
     "measure_memo::the measure memo replays the verdicts it cached::the memo does not move geometry — the verdicts are the only thing at risk",
     "measure_memo::the measure memo's key carries the offer only where the offer is read::a `hug` height READS the offer, so it keeps `maxH` in the key",
     "measure_memo::the measure memo's key carries the offer only where the offer is read::a `textSize = \"fit\"` node keeps `maxH` in its key — the box it fits IS the offer",
     "measure_memo::the measure memo's key carries the offer only where the offer is read::a content-terms `{ lines = n }` dim answers the SAME regardless of the offer it is asked at",
     "measure_memo::the measure memo's key carries the offer only where the offer is read::a `vwrap` breaks its lines ON the offered height, so it is never height-free",
     "measure_memo::the measure memo's key carries the offer only where the offer is read::a child that can be SQUEEZED makes its vertical stack read the offer too",
     "measure_memo::the measure fan-out no longer grows with nesting depth::a 30-deep chain measures each node about twice, not sixteen times",
     "measure_memo::the measure fan-out no longer grows with nesting depth::...and the fan-out does not RISE with depth, which is the whole defect",
     "measure_memo::the memo is scoped to trees that can benefit from it::gating the memo on a scroll node changes no geometry on a scroll-free tree",
     "content_terms_height::content-terms height: construction is a closed, cross-validated grammar::`{ type = \"content\", lines = n }` constructs",
     "content_terms_height::content-terms height: construction is a closed, cross-validated grammar::`{ type = \"content\", rows = n, of = metric }` constructs",
     "content_terms_height::content-terms height: construction is a closed, cross-validated grammar::`of` also accepts a literal px and an additive list, the documented escape hatch",
     "content_terms_height::content-terms height: construction is a closed, cross-validated grammar::`lines` and `rows` together is refused — two different units, one box",
     "content_terms_height::content-terms height: construction is a closed, cross-validated grammar::`rows` without `of` is refused",
     "content_terms_height::content-terms height: construction is a closed, cross-validated grammar::`of` without `rows` is refused",
     "content_terms_height::content-terms height: construction is a closed, cross-validated grammar::`role` without `lines` is refused",
     "content_terms_height::content-terms height: construction is a closed, cross-validated grammar::`role` on `rows` is refused — it names nothing there",
     "content_terms_height::content-terms height: construction is a closed, cross-validated grammar::an unknown `role` is refused, loud, at construction — not a silent fallback",
     "content_terms_height::content-terms height: construction is a closed, cross-validated grammar::`lines`/`rows`/`of`/`role` on any OTHER dim type are refused — not silently ignored",
     "content_terms_height::content-terms height: construction is a closed, cross-validated grammar::`lines`/`rows` must be finite counts >= 1",
     "content_terms_height::content-terms height: `of` resolves the same way px/min/max/preferred do::a metric name becomes a plain number",
     "content_terms_height::content-terms height: `of` resolves the same way px/min/max/preferred do::an additive list sums, exactly as it does for `px`",
     "content_terms_height::content-terms height: `of` resolves the same way px/min/max/preferred do::a literal number passes through untouched",
     "content_terms_height::content-terms height: `of` resolves the same way px/min/max/preferred do::`lines`/`role` are left alone — nothing to resolve against a metric name",
     "content_terms_height::content-terms height: the solver arithmetic::`rows * of` — pure arithmetic on the resolved dim, no theme facts needed at solve time",
     "content_terms_height::content-terms height: the solver arithmetic::`lines` — ceil(lines * (roleSize * scale + offset) * lineHeight), the SAME two calls `text.lineBox` makes",
     "content_terms_height::content-terms height: the solver arithmetic::a named `role` changes both the size AND the line height it multiplies",
     "content_terms_height::content-terms height: the solver arithmetic::`rows`+`of` tracks the TEN-FOOT ladder — `of` is a theme metric, and `metricScale` already scales it",
     "content_terms_height::content-terms height: the solver arithmetic::`lines` tracks the LIVE scale/offset a caller stamps on the dim (`layout_node.luau`'s job on the real pipeline)",
     "content_terms_height::content-terms height: the solver arithmetic::`rows`+`of` does NOT track the live accessibility offset — a theme metric never has, and that split is documented, not a gap",
     "content_terms_height::content-terms height: the solver arithmetic::a control tree over BOTH shapes still measures every other node exactly as before (no cross-talk)",
     "content_terms_height::content-terms height: a definite viewport, even nested in an unbounded scroll axis::a `rows` scroller nested in an outer y-scroller's unbounded offer reports NOTHING",
     "content_terms_height::content-terms height: a definite viewport, even nested in an unbounded scroll axis::a `lines` scroller nested the same way is ALSO definite",
     "content_terms_height::content-terms height: a definite viewport, even nested in an unbounded scroll axis::...and a BARE `content` scroller in the same position is STILL reported — the historic reading is unchanged",
     "content_terms_height::content-terms height: the four adaptation-slice sites::flow_wrap.AlignBox: 360px at Neutral (value-identical: 10 * 36), GROWS at ten-foot (was frozen)",
     "content_terms_height::content-terms height: the four adaptation-slice sites::keyboard_navigation.List: 144px at Neutral (measured, was 150 — a frozen ~4-row guess), GROWS at ten-foot",
     "content_terms_height::content-terms height: the four adaptation-slice sites::native_style.Scroll: 116px at Neutral (measured, was 120 — a frozen literal beside `body`-role rows), GROWS at ten-foot",
     "content_terms_height::content-terms height: the four adaptation-slice sites::preferred_text.Form: 180px at Neutral (value-identical: 5 * 36) — the text-growth fixture itself, GROWS at a raised preference",
     "content_terms_height::content-terms height: the four adaptation-slice sites::preferred_text.Form GROWS at the ten-foot ladder (was frozen at 180 regardless)",
     "content_terms_height::UI.fill / UI.hug: the shorthand item 14 named (dissolved with demonstration)::`UI.fill()` is exactly `{ type = \"fill\", weight = 1 }`",
     "content_terms_height::UI.fill / UI.hug: the shorthand item 14 named (dissolved with demonstration)::`UI.fill(2)` carries the weight through",
     "content_terms_height::UI.fill / UI.hug: the shorthand item 14 named (dissolved with demonstration)::`UI.fill` refuses a non-positive weight",
     "content_terms_height::UI.fill / UI.hug: the shorthand item 14 named (dissolved with demonstration)::`UI.hug()` is exactly `{ type = \"hug\" }`",
     "content_terms_height::UI.fill / UI.hug: the shorthand item 14 named (dissolved with demonstration)::`UI.hug({ max = 200 })` carries the bound through",
     "content_terms_height::UI.fill / UI.hug: the shorthand item 14 named (dissolved with demonstration)::`UI.hug` refuses an unknown field",
     "content_terms_height::UI.fill / UI.hug: the shorthand item 14 named (dissolved with demonstration)::the shorthand constructs through the real schema, exactly as the raw table did",
     "content_terms_height::UI.fill / UI.hug: the shorthand item 14 named (dissolved with demonstration)::migrated sites solve to the IDENTICAL geometry the raw table produced",
     "composition_resolve_seam::the composition-resolve seam: READ == DECLARED == HOSTED::every `ctx` field the module reads is declared in its own `SolveCtx`",
     "composition_resolve_seam::the composition-resolve seam: READ == DECLARED == HOSTED::every field `SolveCtx` declares is one the module actually reads",
     "composition_resolve_seam::the composition-resolve seam: READ == DECLARED == HOSTED::every field `SolveCtx` declares is still declared by the solver's own `Ctx`",
     "composition_resolve_seam::the composition-resolve seam: READ == DECLARED == HOSTED::NEGATIVE CONTROL: each scan reads what it claims to, and reports the pair out of step",
     "composition_resolve_seam::the composition-resolve seam: the shared WRITE surface is ONE name::the module writes through exactly one `ctx` field, and it is the per-box cache",
     "composition_resolve_seam::the composition-resolve seam: the shared WRITE surface is ONE name::NEGATIVE CONTROL: the write scan sees each spelling, and a read is not a write",
     "composition_resolve_seam::the composition-resolve seam: the Dep, one consumer, one-way require::`Deps` declares exactly `measure`, and that is the only host name the body calls",
     "composition_resolve_seam::the composition-resolve seam: the Dep, one consumer, one-way require::the solver PASSES the record at every call site — no call reaches the module bare",
     "composition_resolve_seam::the composition-resolve seam: the Dep, one consumer, one-way require::there is exactly ONE consumer of the module in src/",
     "composition_resolve_seam::the composition-resolve seam: the Dep, one consumer, one-way require::the module requires the solver NOWHERE — the require is one-way",
     "composition_resolve_seam::the composition-resolve seam: the Dep, one consumer, one-way require::the module names no type from the solver either",
     "composition_resolve_seam::the composition-resolve seam: no module-level mutable state::no module-level local is ever reassigned — two solves cannot see different helpers",
     "composition_resolve_seam::the composition-resolve seam: no module-level mutable state::NEGATIVE CONTROL: the reassignment scan sees a late assignment and ignores a local one",
     "measure_facts_seam::the measure-facts seam: READ == DECLARED == HOSTED::every `ctx` field the module reads is declared in its own `MeasureCtx`",
     "measure_facts_seam::the measure-facts seam: READ == DECLARED == HOSTED::every field `MeasureCtx` declares is one the module actually reads",
     "measure_facts_seam::the measure-facts seam: READ == DECLARED == HOSTED::every field `MeasureCtx` declares is still declared by the solver's own `Ctx`",
     "measure_facts_seam::the measure-facts seam: READ == DECLARED == HOSTED::NEGATIVE CONTROL: each scan reads what it claims to, and reports the pair out of step",
     "measure_facts_seam::the measure-facts seam: the shared WRITE surface::the module writes through exactly four `ctx` fields, and each is a published verdict",
     "measure_facts_seam::the measure-facts seam: the shared WRITE surface::NEGATIVE CONTROL: the write scan sees each spelling, and a read is not a write",
     "measure_facts_seam::the measure-facts seam: EXPORTED == BOUND, one consumer, one-way require::every name the module exports is bound by the solver, and every bound name is exported",
     "measure_facts_seam::the measure-facts seam: EXPORTED == BOUND, one consumer, one-way require::every bound name is actually USED by the solver, not just bound",
     "measure_facts_seam::the measure-facts seam: EXPORTED == BOUND, one consumer, one-way require::there is exactly ONE consumer of the module in src/",
     "measure_facts_seam::the measure-facts seam: EXPORTED == BOUND, one consumer, one-way require::the module requires the solver NOWHERE — the require is one-way",
     "measure_facts_seam::the measure-facts seam: EXPORTED == BOUND, one consumer, one-way require::the module names no type from the solver either",
     "measure_facts_seam::the measure-facts seam: `axisAbsorbs` carries ONE BIT to both its callers::every read of `limit`/`otherLimit` is a boundedness test and nothing else",
     "measure_facts_seam::the measure-facts seam: `axisAbsorbs` carries ONE BIT to both its callers::NEGATIVE CONTROL: a branch that reads the MAGNITUDE is reported",
     "measure_facts_seam::the measure-facts seam: no module-level mutable state::no module-level local is ever reassigned — two solves cannot see different helpers",
     "measure_facts_seam::the measure-facts seam: no module-level mutable state::NEGATIVE CONTROL: the reassignment scan sees a late assignment and ignores a local one",
     "src_tree::src_tree: the enumeration two seam specs were missing most of::reaches every `.luau` under src/, and the old nine-directory list did not",
     "src_tree::src_tree: the enumeration two seam specs were missing most of::the six directories the old list never named are all in it",
     "src_tree::src_tree: the enumeration two seam specs were missing most of::and the top-level src/*.luau files, which no directory in the old list covered",
     "src_tree::src_tree: the enumeration two seam specs were missing most of::descends into subdirectories rather than assuming a flat tree",
     "src_tree::src_tree: the enumeration two seam specs were missing most of::`pathsMatching` finds a require by its TAIL, from any directory",
     "surface_lifecycle_seam::the surface-lifecycle seam: HOSTED == DECLARED == READ::every field the presenter hands over is one the module unpacks",
     "surface_lifecycle_seam::the surface-lifecycle seam: HOSTED == DECLARED == READ::every field the module unpacks is one the presenter hands over",
     "surface_lifecycle_seam::the surface-lifecycle seam: HOSTED == DECLARED == READ::every unpacked name is USED by the module, not just unpacked",
     "surface_lifecycle_seam::the surface-lifecycle seam: HOSTED == DECLARED == READ::`ctx` is touched NOWHERE but the unpack block",
     "surface_lifecycle_seam::the surface-lifecycle seam: HOSTED == DECLARED == READ::NEGATIVE CONTROL: each scan reads what it claims to, and reports the pair out of step",
     "surface_lifecycle_seam::the surface-lifecycle seam: the shared WRITE surface::no name the presenter handed over is ever rebound here",
     "surface_lifecycle_seam::the surface-lifecycle seam: the shared WRITE surface::the module writes through exactly ONE of them, and it is the z record",
     "surface_lifecycle_seam::the surface-lifecycle seam: the shared WRITE surface::the presented stack is mutated by exactly one call, and never rebuilt",
     "surface_lifecycle_seam::the surface-lifecycle seam: the shared WRITE surface::NEGATIVE CONTROL: the write scans see each spelling, and a read is not a write",
     "surface_lifecycle_seam::the surface-lifecycle seam: EXPORTED == BOUND, one consumer, one-way require::every name the module exports is bound by the presenter, and every bound name is exported",
     "surface_lifecycle_seam::the surface-lifecycle seam: EXPORTED == BOUND, one consumer, one-way require::four of the five are bound straight onto the presenter under their original names",
     "surface_lifecycle_seam::the surface-lifecycle seam: EXPORTED == BOUND, one consumer, one-way require::there is exactly ONE consumer of the module in src/",
     "surface_lifecycle_seam::the surface-lifecycle seam: EXPORTED == BOUND, one consumer, one-way require::the module requires the presenter NOWHERE — the require is one-way",
     "surface_lifecycle_seam::the surface-lifecycle seam: EXPORTED == BOUND, one consumer, one-way require::the only way back to the presenter is the instance, and it carries exactly two names",
     "surface_lifecycle_seam::the surface-lifecycle seam: no module-level mutable state::no module-level local is ever reassigned — two presenters cannot share a helper",
     "surface_lifecycle_seam::the surface-lifecycle seam: no module-level mutable state::NEGATIVE CONTROL: the reassignment scan sees a late assignment and ignores a local one",
     "surface_lifecycle_seam::the surface-lifecycle seam: `displayLayer` is ONE record with TWO writers::the presenter declares it as a record, never as a number",
     "surface_lifecycle_seam::the surface-lifecycle seam: `displayLayer` is ONE record with TWO writers::both writers go through `.value` on that one object",
     "surface_lifecycle_seam::the surface-lifecycle seam: `displayLayer` is ONE record with TWO writers::neither file writes the bare name, on either side of the seam",
     "surface_lifecycle_seam::the surface-lifecycle seam: `displayLayer` is ONE record with TWO writers::NEGATIVE CONTROL: the bare-write scan reports each spelling and passes the three legal ones",
     "fine_grained_reactivity::fine-grained reactivity: one changed value costs the same at any scale::writing ONE visible row's own signal writes the same amount at 200, 2 000 and 20 000 rows",
     "fine_grained_reactivity::fine-grained reactivity: one changed value costs the same at any scale::the mounted window is the same size at 200 and at 20 000 rows",
     "fine_grained_reactivity::fine-grained reactivity: one changed value costs the same at any scale::writing an OFF-SCREEN row's signal writes nothing at all",
     "fine_grained_reactivity::fine-grained reactivity: one changed value costs the same at any scale::NEGATIVE CONTROL: a signal EVERY mounted row reads costs more than a signal one row reads",
     "fine_grained_reactivity::fine-grained reactivity: one changed value costs the same at any scale::a shared write scales with the WINDOW, not with the collection",
     "fine_grained_reactivity::fine-grained reactivity: one changed value costs the same at any scale::a burst of 50 single-row writes stays linear in the writes, not in the collection",
     "fine_grained_reactivity::fine-grained reactivity: one changed value costs the same at any scale::KNOWN GAP: one changed value re-lays-out the WHOLE tree, and this records it",
     "fine_grained_reactivity::fine-grained reactivity: one changed value costs the same at any scale::REGRESSION SHAPE: the per-row cost is recorded, so a change to it has to be deliberate",
     "reactive_recovery::a memo that fails on its FIRST evaluation can recover (RR-1/RR-4)::subscribes to the deps it reached before the throw, and retries when one changes",
     "reactive_recovery::a memo that fails on its FIRST evaluation can recover (RR-1/RR-4)::a throw before any use leaves the node stale, so the next pull retries",
     "reactive_recovery::a memo that fails on its FIRST evaluation can recover (RR-1/RR-4)::an illegal write during a first evaluation is the same recovery (RR-4)",
     "reactive_recovery::a memo that fails on its FIRST evaluation can recover (RR-1/RR-4)::a memo that already SUCCEEDED keeps its previous deps when a later run throws",
     "reactive_recovery::an effect that disposes itself is not re-subscribed (RR-2)::leaves zero records in dep.subs and the counters clean",
     "reactive_recovery::an effect that disposes itself is not re-subscribed (RR-2)::the same holds when the owning SCOPE disposes during the run",
     "reactive_recovery::diagnostics name the nodes they are about (RR-3/RR-7)::a forced cycle's path names BOTH memos, not 'memo -> memo'",
     "reactive_recovery::diagnostics name the nodes they are about (RR-3/RR-7)::reading a memo frozen by its dep's disposal says so, and changes nothing else",
     "reactive_recovery::diagnostics name the nodes they are about (RR-3/RR-7)::a memo with another live dep is never called frozen — READ BEFORE it recomputes",
     "reactive_recovery::diagnostics name the nodes they are about (RR-3/RR-7)::a real error on the channel is not overwritten by a benign read (NEW-4)",
     "reactive_recovery::diagnostics name the nodes they are about (RR-3/RR-7)::a memo frozen behind ANOTHER memo is reported on the transitive read (NEW-4)",
     "reactive_recovery::diagnostics name the nodes they are about (RR-3/RR-7)::a clean disposal is silent",
     "reactive_recovery::the flush's terminal contracts (RR-9/RR-10/RR-11)::observe()'s baseline pull routes a cycle error through lastError (RR-10)",
     "reactive_recovery::the flush's terminal contracts (RR-9/RR-10/RR-11)::a settle callback that trips the cap is quarantined, not re-run every flush (RR-9)",
     "reactive_recovery::the flush's terminal contracts (RR-9/RR-10/RR-11)::an INNOCENT settle survives a cap blown by an unrelated effect (NEW-2)",
     "reactive_recovery::the flush's terminal contracts (RR-9/RR-10/RR-11)::a quarantined settle comes back when it is RE-REGISTERED (RR-9's stated exit)",
     "reactive_recovery::the flush's terminal contracts (RR-9/RR-10/RR-11)::a healthy settle keeps running after an unrelated one is quarantined",
     "reactive_recovery::the flush's terminal contracts (RR-9/RR-10/RR-11)::the feedback cap permits exactly FEEDBACK_ROUND_CAP rounds (RR-11)",
     "reactive_recovery::the one-observer notify path keeps every snapshot guarantee (RR-12)::an observer that disposes ITSELF mid-callback is not called again",
     "reactive_recovery::the one-observer notify path keeps every snapshot guarantee (RR-12)::an observer that ADDS one mid-callback does not deliver to it in the same round",
     "reactive_recovery::the one-observer notify path keeps every snapshot guarantee (RR-12)::a memo that RECOMPUTED to an equal value delivers nothing to its lone observer",
     "reactive_recovery::the one-observer notify path keeps every snapshot guarantee (RR-12)::a THROWING lone observer is quarantined and still works on the next write",
     "reactive_recovery::the one-observer notify path keeps every snapshot guarantee (RR-12)::at fan-out two, an observer DISPOSED by the one before it is not called",
     "reactive_recovery::the one-observer notify path keeps every snapshot guarantee (RR-12)::THE CROSSOVER: at fan-out two the order is registration order, not hash order",
     "platform_env_binding::the platform adapter binds the connections it claims to (PLAT-1/PLAT-2)::the on-screen-keyboard connection is made even though the keyboard is down at bind",
     "platform_env_binding::the platform adapter binds the connections it claims to (PLAT-1/PLAT-2)::an engine without the member is cleanly skipped, with no nil hole",
     "platform_env_binding::the platform adapter binds the connections it claims to (PLAT-1/PLAT-2)::a replaced CurrentCamera re-binds ViewportSize and drops the old connection (PLAT-2)",
     "platform_env_binding::a sticky Gamepad preference does not outvote a pointer the player is using (DIR5)::nothing deliberate yet: the engine's preference stands, whatever it says",
     "platform_env_binding::a sticky Gamepad preference does not outvote a pointer the player is using (DIR5)::a mouse CLICK claims primacy from a pad nobody has pressed",
     "platform_env_binding::a sticky Gamepad preference does not outvote a pointer the player is using (DIR5)::...and a real pad PRESS takes it straight back",
     "platform_env_binding::a sticky Gamepad preference does not outvote a pointer the player is using (DIR5)::stick drift interleaved with mouse movement does not flap primacy, or publish at all",
     "platform_env_binding::a sticky Gamepad preference does not outvote a pointer the player is using (DIR5)::MOUSE MOVEMENT is not an act either — a pad player brushing a mouse keeps the pad",
     "platform_env_binding::a sticky Gamepad preference does not outvote a pointer the player is using (DIR5)::TOUCH is a deliberate act too, on a hybrid the engine still calls a pad",
     "platform_env_binding::a sticky Gamepad preference does not outvote a pointer the player is using (DIR5)::a device with NO pointer is a pad player whatever the player just did",
     "platform_env_binding::a sticky Gamepad preference does not outvote a pointer the player is using (DIR5)::an engine with no raw stream is taken at its word, and binds without a nil hole",
     "platform_env_binding::a sticky Gamepad preference does not outvote a pointer the player is using (DIR5)::...and `GetLastInputType` is never read, because this repository refused it",
     "platform_env_binding::a sticky Gamepad preference does not outvote a pointer the player is using (DIR5)::a pointer-primary desktop with no pad is exactly where it was",
     "platform_env_binding::safe-area insets are refused while the platform has not caught up (PLAT-3)::a stale area arriving AFTER the new viewportRect is refused, and the last good value stands",
     "platform_env_binding::safe-area insets are refused while the platform has not caught up (PLAT-3)::...and the same refusal holds when the stale area still FITS the new window",
     "platform_env_binding::safe-area insets are refused while the platform has not caught up (PLAT-3)::the belt refuses ONLY the disagreement: an area that moves with the viewport publishes",
     "platform_env_binding::safe-area insets are refused while the platform has not caught up (PLAT-3)::the belt is a REFUSAL, not a rewrite: a held pass changes no other fact",
     "platform_env_binding::safe-area insets are refused while the platform has not caught up (PLAT-3)::an area that cannot FIT the viewport it is published beside is refused on the FIRST pass",
     "platform_env_binding::safe-area insets are refused while the platform has not caught up (PLAT-3)::the belt holds an unmoved area beside a moved viewport, and releases when it moves",
     "platform_env_binding::safe-area insets are refused while the platform has not caught up (PLAT-3)::the three areas lag INDEPENDENTLY, so one holding never gags the others",
     "platform_env_binding::safe-area insets are refused while the platform has not caught up (PLAT-3)::the legacy gui inset is added back before edges are derived",
     "pointer_seam_ownership::the pointer seam borrows the host's cursor (PLAT-5)::writes nothing at all while it has no cursor art of its own",
     "pointer_seam_ownership::the pointer seam borrows the host's cursor (PLAT-5)::when it DOES have art, it captures the prior value and restores it",
     "pointer_seam_ownership::the pointer seam borrows the host's cursor (PLAT-5)::teardown restores the borrowed cursor even mid-hover",
     "pointer_seam_ownership::the menu connection is the adapter's, the cursor hint is the root's (PLAT-4)::releaseCursorHint leaves the menu connection alive",
     "pointer_seam_ownership::the menu connection is the adapter's, the cursor hint is the root's (PLAT-4)::the adapter calls the two apart: only a last-root teardown disposes globals",
     "pointer_seam_ownership::a touch capture belongs to the finger that opened it (PLAT-6)::the capture records the originating touch object",
     "pointer_seam_ownership::a touch capture belongs to the finger that opened it (PLAT-6)::the filter is touch-scoped, so a mouse drag is unaffected",
     "pointer_seam_ownership::a touch capture belongs to the finger that opened it (PLAT-6)::BOTH global connections apply it, and they are the first thing each does",
     "pointer_seam_ownership::task CONN: a recycled handle's connections do not accumulate (Finding 1)::pointerBeganConn: a second setPointerHandlers call leaves exactly one InputBegan listener",
     "pointer_seam_ownership::task CONN: a recycled handle's connections do not accumulate (Finding 1)::hoverEnterConn/hoverLeaveConn: two setPointerHandlers calls on a Grip leave exactly one listener each",
     "pointer_seam_ownership::task CONN: a recycled handle's connections do not accumulate (Finding 1)::scrollConn: a second setScrollHandler call leaves exactly one InputChanged listener",
     "pointer_seam_ownership::task CONN: hitExpanderPointerConn rewires on a genuine recycle, stays a no-op within one life (Finding 4)::a same-life re-entry (unchanged handle.path) adds no second connection",
     "pointer_seam_ownership::task CONN: hitExpanderPointerConn rewires on a genuine recycle, stays a no-op within one life (Finding 4)::rewires when handle.path changes -- the signal a park->adopt recycle actually produces",
     "platform_adapters_recovery::a lost resnapshot reply is a bounded retry, not a wedge (PLAT-7)::asks once for a gap, then retries on a backoff of refused patches",
     "platform_adapters_recovery::a lost resnapshot reply is a bounded retry, not a wedge (PLAT-7)::a resnapshot that finally arrives heals it completely",
     "platform_adapters_recovery::a lost resnapshot reply is a bounded retry, not a wedge (PLAT-7)::a throwing hook still un-latches immediately, as it always did",
     "platform_adapters_recovery::patch.remove is an array of keys, loudly (PLAT-25)::an array remove applies",
     "platform_adapters_recovery::patch.remove is an array of keys, loudly (PLAT-25)::a set-shaped remove is REFUSED by name instead of silently removing nothing",
     "platform_adapters_recovery::patch.remove is an array of keys, loudly (PLAT-25)::an empty remove and a missing remove are both fine",
     "platform_adapters_recovery::PreloadAsync is handed Instances (PLAT-26)::the transport builds one per key, hands it over, and destroys it",
     "platform_adapters_recovery::disableLegacyControls only claims what it freed (PLAT-21)::an action that was never bound was not freed by unbinding it",
     "platform_adapters_recovery::disableLegacyControls only claims what it freed (PLAT-21)::an action still bound afterwards was not freed either",
     "platform_adapters_recovery::disableLegacyControls only claims what it freed (PLAT-21)::gone from a table it was in IS freed",
     "platform_adapters_recovery::disableLegacyControls only claims what it freed (PLAT-21)::the live fallback consults the binding table before claiming success",
     "platform_adapters_recovery::disableLegacyControls only claims what it freed (PLAT-21)::the IAS inert check runs BEFORE either engine remedy is attempted",
     "platform_adapters_recovery::the touch-controls suppression is refcounted (PLAT-22)::two binders share one suppression, and the SECOND release restores the prior value",
     "platform_adapters_recovery::the touch-controls suppression is refcounted (PLAT-22)::unbinding while suppressed releases exactly one reference",
     "platform_adapters_recovery::the touch-controls suppression is refcounted (PLAT-22)::the release RE-READS: a host that took the property back keeps it (PLAT-22)",
     "platform_adapters_recovery::the touch-controls suppression is refcounted (PLAT-22)::...and it still restores when the value IS the one we wrote (PLAT-22)",
     "platform_adapters_recovery::the touch-controls suppression is refcounted (PLAT-22)::a write the engine accepts but does not APPLY is not counted as suppression",
     "platform_adapters_recovery::bounded waits and stranded sheets (PLAT-11, PLAT-9, PLAT-24)::the PlayerScripts wait is bounded and loud, like gamepad_contention's (PLAT-11)",
     "platform_adapters_recovery::bounded waits and stranded sheets (PLAT-11, PLAT-9, PLAT-24)::the theme-child ensure runs OUTSIDE the stamp-mismatch branch (PLAT-9)",
     "platform_adapters_recovery::bounded waits and stranded sheets (PLAT-11, PLAT-9, PLAT-24)::every wrong-schema sheet found is destroyed, in both lookup orders (PLAT-24)",
     "primitive_registration::primitive registration (agentic maintainability, MAINT-5)::the live repository passes every primitive-registration rule",
     "primitive_registration::primitive registration (agentic maintainability, MAINT-5)::it actually walked the surface it claims to cover",
     "primitive_registration::primitive registration (agentic maintainability, MAINT-5)::CLASS_TO_INSTANCE is READ, not assumed — an unreadable table is a loud failure",
     "primitive_registration::primitive registration (agentic maintainability, MAINT-5)::catches a STALE MAPPING: an engine instance for a class the schema dropped",
     "primitive_registration::primitive registration (agentic maintainability, MAINT-5)::catches an UNREGISTERED PRIMITIVE: a schema class with no contract row",
     "primitive_registration::primitive registration (agentic maintainability, MAINT-5)::catches a GHOST CONTRACT ROW: a contract for a class the schema does not declare",
     "primitive_registration::primitive registration (agentic maintainability, MAINT-5)::the playbook it points at exists and covers the whole path",
     "prefix_tests::a prefix test cannot disagree with the prefix it tests (rename fallout)::no comparison in src/, tools/ or examples/ counts differently from its own literal",
     "prefix_tests::a prefix test cannot disagree with the prefix it tests (rename fallout)::the scan BITES — every spelling, and prose in neither kind of comment",
     "prefix_tests::the adapter's facet-* tag ownership is one named fact (SCREEN-X)::the removal loop DELEGATES — the adapter hand-rolls no prefix test at all",
     "prefix_tests::the adapter's facet-* tag ownership is one named fact (SCREEN-X)::every tag the classifier can mint is one that predicate claims",
     "tag_sync::tag_sync: the pure ruling::owns a tag by the LENGTH OF ITS OWN PREFIX, not by a counted number",
     "tag_sync::tag_sync: the pure ruling::a state change produces a REMOVAL, and the plan is deterministic",
     "tag_sync::tag_sync: the pure ruling::never removes a tag the game owns",
     "tag_sync::tag_sync: mounted, state changed, tag removed::a Button that stops being selected LOSES facet-selected",
     "tag_sync::tag_sync: mounted, state changed, tag removed::a segment declared surface=plain ends up with NO tags — Bugs A and B",
     "tag_sync::tag_sync: mounted, state changed, tag removed::a surface change swaps the surface tag rather than accumulating one",
     "tag_sync::tag_sync: mounted, state changed, tag removed::a destructive Button returning to the default role LOSES the role tag",
     "theme_package_enumeration::the theme-package directory has ONE enumerator (MAINT-8c)::every module on disk has a declared role, and every role names a module",
     "theme_package_enumeration::the theme-package directory has ONE enumerator (MAINT-8c)::it really read the directory — the three kinds are all represented",
     "theme_package_enumeration::the theme-package directory has ONE enumerator (MAINT-8c)::no consumer names a theme module that does not exist",
     "theme_package_enumeration::the theme-package directory has ONE enumerator (MAINT-8c)::every SHIPPING reference package is named by at least one consumer",
     "theme_package_enumeration::the theme-package directory has ONE enumerator (MAINT-8c)::the shippable set is the eight player-facing packages, derived from the directory",
     "theme_package_enumeration::the theme-package directory has ONE enumerator (MAINT-8c)::the artifact name is derived from the module name, PascalCase",
     "theme_package_enumeration::the theme-package directory has ONE enumerator (MAINT-8c)::catches a NEW module nobody declared a role for",
     "theme_package_enumeration::the theme-package directory has ONE enumerator (MAINT-8c)::catches a role whose module was renamed away",
     "theme_package_enumeration::the theme-package directory has ONE enumerator (MAINT-8c)::catches an exclusion whose module was renamed away",
     "theme_package_enumeration::the theme-package directory has ONE enumerator (MAINT-8c)::catches an exclusion with no usable reason — silence is not a decision",
     "theme_package_enumeration::the theme-package directory has ONE enumerator (MAINT-8c)::catches a would-be shippable package that is not a reference package",
     "theme_package_enumeration::the theme-package directory has ONE enumerator (MAINT-8c)::catches a reference package left out of the declared order",
     "instance_recycling::instance recycling: a recycled node is indistinguishable from a fresh one::is ON by default — the whole point is that a list does not have to ask",
     "instance_recycling::instance recycling: a recycled node is indistinguishable from a fresh one::an explicit `false` opts out completely",
     "instance_recycling::instance recycling: a recycled node is indistinguishable from a fresh one::actually recycles when asked — otherwise every differential below is vacuous",
     "instance_recycling::instance recycling: a recycled node is indistinguishable from a fresh one::DIFFERENTIAL: the virtual state after a long scroll is byte-identical",
     "instance_recycling::instance recycling: a recycled node is indistinguishable from a fresh one::DIFFERENTIAL: state matches at every step of the scroll, not just the end",
     "instance_recycling::instance recycling: a recycled node is indistinguishable from a fresh one::DIFFERENTIAL: scrolling back to the top restores the opening state exactly",
     "instance_recycling::instance recycling: a recycled node is indistinguishable from a fresh one::DIFFERENTIAL: rows with DIFFERENT prop sets still match exactly",
     "instance_recycling::instance recycling: a recycled node is indistinguishable from a fresh one::the pool is BOUNDED — a long scroll cannot grow it without limit",
     "instance_recycling::recycling: an adopted node belongs to its OWN surface::a node recycled after ANOTHER surface created one is still parented under its own root",
     "instance_recycling::recycling: an adopted node belongs to its OWN surface::the adoption count is not zero, so the assertion above is not vacuous",
     "mount_unmount_soak::mount/unmount soak: the instance count returns to its starting level::five mount/unmount laps leave the outstanding-instance count flat",
     "mount_unmount_soak::mount/unmount soak: the instance count returns to its starting level::a SCROLLED surface — which actually fills the recycle pool — still returns to the floor, and the pool is drained",
     "mount_unmount_soak::mount/unmount soak: the instance count returns to its starting level::three full ramp laps (5 rungs each, up to the declared ceiling) leave the count flat",
     "instance_recycling_themed::themed recycling: hinted nodes reach the pool and come back correct::a HINT-carrying node is recycled across a window slide (bucket by content, not address)",
     "instance_recycling_themed::themed recycling: hinted nodes reach the pool and come back correct::DIFFERENTIAL: hinted rows land byte-identical with recycling on and off",
     "instance_recycling_themed::adopt refuses a decoration-hint mismatch (the fake mirrors the live rule)::refuses when the hint content differs",
     "instance_recycling_themed::adopt refuses a decoration-hint mismatch (the fake mirrors the live rule)::refuses across hinted and bare, in both directions",
     "instance_recycling_themed::adopt refuses a decoration-hint mismatch (the fake mirrors the live rule)::accepts a CONTENT-equal hint carried by a different table",
     "instance_recycling_themed::the LIVE adapter's themed park/adopt (its source is the contract)::adopt takes the incoming hint — the mismatch rule needs it",
     "instance_recycling_themed::the LIVE adapter's themed park/adopt (its source is the contract)::park no longer refuses a node for merely having child instances",
     "instance_recycling_themed::the LIVE adapter's themed park/adopt (its source is the contract)::ONE ruling enumerates the chrome's path-keyed children, used by park AND adopt",
     "instance_recycling_themed::the LIVE adapter's themed park/adopt (its source is the contract)::the enumeration covers every chrome family: decoration, lift, icon, layers+masks, bar, toggle",
     "instance_recycling_themed::the LIVE adapter's themed park/adopt (its source is the contract)::discardParked tears chrome down census-correctly, not just Destroy()",
     "instance_recycling_themed::the LIVE adapter's themed park/adopt (its source is the contract)::adopt refuses chrome facts that moved while the node was parked",
     "instance_recycling_themed::the LIVE adapter's themed park/adopt (its source is the contract)::the epoch moves on a package swap AND on an asset-ledger movement",
     "instance_park_corpse::instance park corpse: the pool can never hold a destroyed instance::parkEligible refuses an unparented (destroyed) instance",
     "instance_park_corpse::instance park corpse: the pool can never hold a destroyed instance::adopt refuses (never throws) when the Parent write fails",
     "instance_park_corpse::instance park corpse: the pool can never hold a destroyed instance::the instance-host refusal that makes the ordering hazard real is still in place",
     "incremental_layout::incremental layout: a partial solve answers exactly what a full solve does::an explicit `false` opts out completely",
     "incremental_layout::incremental layout: a partial solve answers exactly what a full solve does::is ON by default and ENGAGES — every differential below is vacuous otherwise",
     "incremental_layout::incremental layout: a partial solve answers exactly what a full solve does::DIFFERENTIAL: paint, text facts and diagnostics all match at every step",
     "incremental_layout::incremental layout: a partial solve answers exactly what a full solve does::does DRAMATICALLY less work — a ratio, not merely `less`",
     "incremental_layout::incremental layout: a partial solve answers exactly what a full solve does::the saving HOLDS as the collection grows — it is not an artifact of one size",
     "incremental_layout::incremental layout: a partial solve answers exactly what a full solve does::falls back to a full solve when the change is not contained",
     "diagnostic_replay::the placement audit does not duplicate under incremental layout (HIGH 2)::stays at exactly one finding across many layout-dirty frames",
     "diagnostic_replay::the placement audit does not duplicate under incremental layout (HIGH 2)::a finding whose FILING parent is skipped is still replayed",
     "diagnostic_replay::the placement audit does not duplicate under incremental layout (HIGH 2)::the incremental answer is identical to the full-solve oracle, finding for finding",
     "container_relative_incremental::containerRelative is re-resolved when the container moves under it (HIGH 3)::the bar narrows when the scrollbar reserve arms, and widens when it disarms",
     "container_relative_incremental::containerRelative is re-resolved when the container moves under it (HIGH 3)::matches the full-solve oracle at every step of a long flip sequence",
     "container_relative_incremental::containerRelative is re-resolved when the container moves under it (HIGH 3)::the incremental solve still SKIPS — the fix refuses one subtree, not the feature",
     "container_relative_incremental::containerRelative is re-resolved when the container moves under it (HIGH 3)::the refusal is SCOPED to the marked subtree — an unmarked twin still skips it",
     "container_relative_incremental::containerRelative is re-resolved when the container moves under it (HIGH 3)::an INNER scroller insulates what is below it — the mark stops there",
     "perf_principles::perf principle 1: work scales with WHAT CHANGED, not with what exists::LAYOUT — one bound value costs the same at 100, 800 and 3 200 rows",
     "perf_principles::perf principle 1: work scales with WHAT CHANGED, not with what exists::PRESENTATION — scrolling costs the same at 100, 800 and 3 200 rows",
     "perf_principles::perf principle 1: work scales with WHAT CHANGED, not with what exists::LAYOUT — a resize is allowed to cost the whole tree; ONE VALUE is not",
     "perf_principles::perf principle 3: nothing is built that cannot be seen::the adapter elides pure layout containers, and geometry writes do not resurrect them",
     "perf_principles::perf principle 4: an unchanged value is not written::PRESENTATION — re-committing identical state writes nothing at all",
     "perf_principles::perf principle 4: an unchanged value is not written::PRESENTATION — setting a bound value to the value it already holds is free",
     "perf_principles::perf principle 4: an unchanged value is not written::PRESENTATION — a toggle flip touches its own row and nothing else",
     "perf_principles::perf principle 5: the cheap path is the default::recycling, incremental layout and elision are all ON without being asked for",
     "layout_v1::layout v1: zstack::overlays children with per-child alignment",
     "layout_v1::layout v1: grid::fixed columns flow row-major with row heights from the tallest cell",
     "layout_v1::layout v1: grid::adaptive columns derive the count from minColumnWidth",
     "layout_v1::layout v1: anchor::anchors HUD children to parent corners/edges with offsets",
     "layout_v1::layout v1: dims::minMax clamps content into its band",
     "layout_v1::layout v1: dims::aspect derives one axis from the other",
     "layout_v1::layout v1: dims::aspect on both axes is a diagnostic, not silence",
     "layout_v1::layout v1: margins and alignment::margins add outer space in stacks; align centers on the cross axis",
     "layout_v1::UI.Grid blueprint (architecture verifier F3: §10.1 v1 control)::UI.Grid mounts and renders through the solver's grid kind",
     "renderer::renderer: minimal updates::initial render creates every mounted node with rects inside the safe content rect",
     "renderer::renderer: minimal updates::a paint/semantics-only change writes the prop and never re-solves or writes rects",
     "renderer::renderer: minimal updates::a viewport change re-solves and writes only changed rects",
     "renderer::renderer: minimal updates::When toggling creates/removes adapter nodes and re-solves",
     "renderer::renderer: minimal updates::dispose destroys the adapter root",
     "renderer::environment: per-key isolation::a keyboard-occlusion change never touches a color-only subscriber",
     "renderer::environment: per-key isolation::derived sizeClass recomputes only from viewportRect",
     "renderer::environment: per-key isolation::derived interactionClasses: defaults are a pointer/keyboard desktop",
     "renderer::environment: per-key isolation::derived interactionClasses: a live gamepad capability joins the set without moving primary",
     "renderer::environment: per-key isolation::derived interactionClasses: the preferred class is forced live even when its capability fact lags",
     "renderer::environment: per-key isolation::displaySize fact defaults to Medium and distanceProfile derives ten-foot from Large",
     "renderer::environment: per-key isolation::overscanInsets fact defaults to zero and is settable",
     "renderer::tokens and authority::compiles a valid schema and reports contrast",
     "renderer::tokens and authority::rejects an incomplete or low-contrast schema with a named report",
     "renderer::tokens and authority::authority conflicts are loud errors",
     "renderer::Button padding reaches the adapter (playlist stars defect 2026-07-20)::forwards the blueprint padding so the engine text inset matches the solver",
     "renderer::Toggle sizing honors its label (gallery place defect 2026-07-20)::a bare labeled Toggle measures wide enough for label text PLUS the track chrome",
     "renderer::safe areas: deviceSafeContent root policy (native-substrate NS-A1)::deviceSafeContent insets the content rect by the per-edge MAX of core and device insets",
     "renderer::safe areas: deviceSafeContent root policy (native-substrate NS-A1)::a deviceSafeInsets fact change re-solves and writes rects",
     "renderer::safe areas: deviceSafeContent root policy (native-substrate NS-A1)::coreSafeContent policy ignores deviceSafeInsets (default unchanged)",
     "renderer::preferred text applied exactly once (native-substrate NS-A9)::the player preference scales MEASUREMENT (reserved bounds) but never the painted TextSize",
     "renderer::preferred text applied exactly once (native-substrate NS-A9)::authored ten-foot scale still reaches BOTH seams (it is not engine-owned)",
     "renderer::preferred text applied exactly once (native-substrate NS-A9)::a preference BELOW 1 never reserves less than the painted size",
     "renderer::preferred text applied exactly once (native-substrate NS-A9)::the reservation floor composes with the ten-foot paint scale",
     "renderer::preferred text ADDITIVE reservation (platform verifier F1/F2, 2026-07-23)::the live additive offset grows reserved bounds for EXPLICIT and DEFAULT-size text, never paint",
     "renderer::Button padding is re-pushed on every solve, minimally::a theme commit moves the engine inset, not just the solver's reservation",
     "renderer::Button padding is re-pushed on every solve, minimally::carries an authored PER-SIDE padding through the same seam, resolved",
     "renderer::Button padding is re-pushed on every solve, minimally::the adapter spends the HORIZONTAL pair of that table, never the table itself",
     "renderer::Button padding is re-pushed on every solve, minimally::a steady theme costs zero padding writes (minimal-write contract)",
     "renderer::RS-DIR9: the Toggle's label column is ONE description, not two::the neutral answer is byte-identical to the constants the adapter used to carry",
     "renderer::RS-DIR9: the Toggle's label column is ONE description, not two::it follows the LIVE metrics — the classic-desktop and sci-fi numbers that clipped",
     "renderer::RS-DIR9: the Toggle's label column is ONE description, not two::a package that moves the track moves BOTH ends of the row",
     "renderer::RS-DIR9: the Toggle's label column is ONE description, not two::the renderer's measure seam reads it, and so does the adapter's paint seam",
     "renderer::RS-DIR13: a value flip is PAINT — it moves no solved geometry::is byte-stable across repeated flips, in every reference package",
     "renderer::RS-DIR13: a value flip is PAINT — it moves no solved geometry::says so at the boundary: `value` dirties paint and semantics, never measure",
     "renderer::RS-DIR13: a Toggle's label is ONE line — it never breaks mid-word::the adapter draws the toggle label on one line and ellipsizes rather than wrapping",
     "renderer::RS-DIR13: a Toggle's label is ONE line — it never breaks mid-word::...and the SOLVER reserves one line too, so measure and paint agree",
     "renderer::RS-DIR13: a Toggle's label is ONE line — it never breaks mid-word::a long label still GROWS the row where the row is free to grow",
     "renderer::renderer — root policy::insets a content root by the core safe area (the default)",
     "renderer::renderer — root policy::paints an edgeToEdge root under the insets — a backdrop is not content",
     "renderer::renderer — root policy::refuses an unknown policy instead of silently using the default",
     "renderer::renderer: diagnostic reads are copies, not the live tables (F-30)::mutating the hiddenRoots a caller was handed cannot corrupt the renderer",
     "renderer::renderer: diagnostic reads are copies, not the live tables (F-30)::diagnostics and the whole composition set are copies too",
     "edge_floor::renderer/presenter — edgeFloor knob (FIX-4 item 2)::THE PIN: explicit edgeToEdge with no knob stays ZERO padding, unchanged",
     "edge_floor::renderer/presenter — edgeFloor knob (FIX-4 item 2)::refuses edgeFloor together with rootPolicy = edgeToEdge instead of picking a winner",
     "edge_floor::renderer/presenter — edgeFloor knob (FIX-4 item 2)::the SAME refusal fires straight off renderer.attach, not just through the presenter",
     "edge_floor::renderer/presenter — edgeFloor knob (FIX-4 item 2)::omitted edgeFloor on coreSafeContent is unchanged behavior (no default flip)",
     "edge_floor::renderer/presenter — edgeFloor knob (FIX-4 item 2)::a literal-number edgeFloor floors bottom/left/right, never top",
     "edge_floor::renderer/presenter — edgeFloor knob (FIX-4 item 2)::an edgeFloor SMALLER than the existing inset is a no-op (max, not override)",
     "edge_floor::renderer/presenter — edgeFloor knob (FIX-4 item 2)::a metric-name edgeFloor resolves through the live theme snapshot",
     "edge_floor::renderer/presenter — edgeFloor knob (FIX-4 item 2)::a metric-name edgeFloor rides the ten-foot ladder, like its neighbors (space.gutter, cellPadding)",
     "edge_floor::renderer/presenter — edgeFloor knob (FIX-4 item 2)::a literal number does NOT ride the ladder — it is a pixel count at every distance",
     "edge_floor::renderer/presenter — edgeFloor knob (FIX-4 item 2)::composes with bandSafeContent's own gutter floor — the larger of the two wins",
     "edge_floor::renderer/presenter — edgeFloor knob (FIX-4 item 2)::bandSafeContent's own gutter floor still wins when it is the larger of the two",
     "edge_floor::renderer/presenter — edgeFloor knob (FIX-4 item 2)::an unresolvable metric name falls back to no floor (renderer falls back, per resolveNumber's own contract)",
     "commit_walks_seam::the commit-walk seam: READ == DECLARED == PASSED::every `ctx` field the module reads is declared in its own `CommitCtx`",
     "commit_walks_seam::the commit-walk seam: READ == DECLARED == PASSED::every field `CommitCtx` declares is one the module actually reads",
     "commit_walks_seam::the commit-walk seam: READ == DECLARED == PASSED::every field `CommitCtx` declares is one the renderer actually PASSES",
     "commit_walks_seam::the commit-walk seam: READ == DECLARED == PASSED::NEGATIVE CONTROL: each scan reads what it claims to, and reports the pair out of step",
     "commit_walks_seam::the commit-walk seam: the shared WRITE surface, and who owns each lifetime::the module writes through exactly twelve of the twenty-one records it is handed",
     "commit_walks_seam::the commit-walk seam: the shared WRITE surface, and who owns each lifetime::every per-path record the module writes is CLEARED by the renderer's removal sweep",
     "commit_walks_seam::the commit-walk seam: the shared WRITE surface, and who owns each lifetime::NEGATIVE CONTROL: the write scan sees each spelling, and a read is not a write",
     "commit_walks_seam::the commit-walk seam: the mutable-upvalue test itself::every name that crosses is declared ONCE by the renderer and never reassigned",
     "commit_walks_seam::the commit-walk seam: the mutable-upvalue test itself::the module never REBINDS one of them either — the same failure from this side",
     "commit_walks_seam::the commit-walk seam: the mutable-upvalue test itself::the module holds NO module-level mutable state",
     "commit_walks_seam::the commit-walk seam: the mutable-upvalue test itself::NEGATIVE CONTROL: a reassignment is reported and a table-constructor entry is not",
     "commit_walks_seam::the commit-walk seam: EXPORTED == CALLED, one consumer, one-way require::every walk the module exports is called by the renderer, and every call is exported",
     "commit_walks_seam::the commit-walk seam: EXPORTED == CALLED, one consumer, one-way require::there is exactly ONE consumer of the module in src/",
     "commit_walks_seam::the commit-walk seam: EXPORTED == CALLED, one consumer, one-way require::the module requires the renderer NOWHERE — the require is one-way",
     "commit_walks_seam::the commit-walk seam: EXPORTED == CALLED, one consumer, one-way require::the module names nothing off the renderer's own module table either",
     "commit_walks_seam::the commit-walk seam: the renderer's source-scanning pins still see the walks::`tests/lib/renderer_source` carries the new part",
     "commit_walks_seam::the commit-walk seam: the renderer's source-scanning pins still see the walks::...and every module its own header NAMES is a part",
     "native_scroll::native scroll host (NS-A2)::a ScrollView declares itself a clip host and sends its solved content extent as the scroll region",
     "native_scroll::native scroll host (NS-A2)::a content change updates the scroll region (and only when it changes)",
     "native_scroll::native scroll host (NS-A2)::non-overflowing content still declares its region honestly",
     "native_scroll::native scroll host (NS-A2)::controller.scrollTo writes a programmatic scroll position through the adapter",
     "native_scroll::native scroll host (NS-A2)::engine-driven scroll (driveScroll) reaches a framework observer",
     "native_scroll::native scroll host (NS-A2)::FALLBACK: an adapter without the scroll seam mounts, clips, and lays out identically (no crash)",
     "native_scroll::scroll contentSize includes gaps (migration-agent finding 2026-07-23)::a gapped scroll region's canvas covers rows plus gaps",
     "native_scroll::A-SV1: horizontal ScrollView::lays children out along x and advances by gap",
     "native_scroll::A-SV1: horizontal ScrollView::reports a horizontal canvas extent, not a vertical one",
     "native_scroll::A-SV1: horizontal ScrollView::a cross-axis fill child stretches to the viewport height",
     "native_scroll::A-SV1: horizontal ScrollView::the vertical axis is unchanged",
     "native_scroll::A-SV1: scrollToVisible::scrolls the minimum distance to bring a node below the viewport into view",
     "native_scroll::A-SV1: scrollToVisible::scrolls up for a node above the current offset",
     "native_scroll::A-SV1: scrollToVisible::does nothing when the node is already fully visible",
     "native_scroll::A-SV1: scrollToVisible::finds the nearest scroll ancestor, not just a direct parent",
     "native_scroll::A-SV1: scrollToVisible::returns false for a node with no scroll ancestor",
     "native_scroll::A-SV1: scrollToVisible::works on the horizontal axis",
     "native_scroll::A-SV1: scrollToVisible::degrades to false on an adapter without the scroll seam",
     "native_scroll::A-SV1: focus keep-visible through the shared substrate::moving focus past the visible band scrolls the host without any control wiring",
     "native_scroll::A-SV1: focus keep-visible through the shared substrate::focus stays on the first rows without scrolling",
     "native_scroll::A-SV2: the keep-visible clamp exists exactly once::scrolls the minimum distance in each direction, or not at all",
     "native_scroll::A-SV2: the keep-visible clamp exists exactly once::clamps to the scrollable extent when one is given",
     "native_scroll::A-SV2: the keep-visible clamp exists exactly once::returns nil rather than a no-op scroll",
     "native_scroll::A-SV2: the keep-visible clamp exists exactly once::a span taller than the band aligns to its top",
     "native_scroll::A-SV2: the keep-visible clamp exists exactly once::all three consumers call it — no second implementation survives",
     "native_scroll::A-SV2: the keep-visible clamp exists exactly once::the renderer's substrate and the index path agree on the same geometry",
     "native_scroll::fake target: observeScroll holds every observer, like the engine does::TWO observers on one node BOTH fire (the single-slot bug, pinned)",
     "native_scroll::fake target: observeScroll holds every observer, like the engine does::unsubscribing one leaves the other live, and is idempotent",
     "native_scroll::fake target: observeScroll holds every observer, like the engine does::an observer that unsubscribes itself mid-fire does not skip its sibling",
     "native_scroll::screen_target: the scroll seam tolerates an elided (instance-less) handle::every scroll-seam adapter function guards a nil instance before indexing it",
     "native_scroll::screen_target: the scroll seam tolerates an elided (instance-less) handle::...and so do the indicator helpers that share the same handle",
     "native_scroll_autobind::O-31: a virtualised collection follows the engine with no consumer call::VirtualList: an engine scroll slides the window — NO bindNativeScroll call",
     "native_scroll_autobind::O-31: a virtualised collection follows the engine with no consumer call::VirtualGrid (vertical grid, axis y): an engine scroll slides the window — NO bindNativeScroll call",
     "native_scroll_autobind::O-31: a virtualised collection follows the engine with no consumer call::VirtualGrid (sideways grid, axis x): an engine scroll slides the window — NO bindNativeScroll call",
     "native_scroll_autobind::O-31: a virtualised collection follows the engine with no consumer call::Table (virtualized): an engine scroll slides the window — NO bindNativeScroll call",
     "native_scroll_autobind::O-31: a virtualised collection follows the engine with no consumer call::...and it still does when the CONSUMER declares navigationGroups (the focusGroups seam is dead here)",
     "native_scroll_autobind::O-31: a virtualised collection follows the engine with no consumer call::...and when navigationGroups is a FUNCTION, and when it is `false` (the flat ring)",
     "native_scroll_autobind::O-31: a virtualised collection follows the engine with no consumer call::PROOF THE DISCRIMINATOR IS REAL: with a declared focus map the presenter calls focusGroups ZERO times",
     "native_scroll_autobind::O-31: a virtualised collection follows the engine with no consumer call::an explicit bindNativeScroll on top of the auto-bind adds NO second engine observer",
     "native_scroll_autobind::O-31: a virtualised collection follows the engine with no consumer call::...and a refresh after that unbind does NOT resurrect the mirror",
     "native_scroll_autobind::O-31: a virtualised collection follows the engine with no consumer call::the same on Table, whose body path can carry a SECOND mirror observer",
     "native_scroll_autobind::O-31: a virtualised collection follows the engine with no consumer call::a BLOCK table (scrolls = false) auto-binds NOTHING — its published scroll path is nil",
     "native_scroll_autobind::O-31: a virtualised collection follows the engine with no consumer call::NEGATIVE CONTROL: a bare mount with no presenter still learns no path, and says so",
     "keyboard_navigation::keyboard traversal: the order comes from the mounted focus graph (DK-1)::traverse(+1) walks the flat scope forward and traverse(-1) walks it back",
     "keyboard_navigation::keyboard traversal: the order comes from the mounted focus graph (DK-1)::Shift+Tab into a scope with no focus enters at the LAST eligible node",
     "keyboard_navigation::keyboard traversal: the order comes from the mounted focus graph (DK-1)::...and Tab into the same scope enters at the first",
     "keyboard_navigation::keyboard traversal: the order comes from the mounted focus graph (DK-1)::traversal with no focus enters the scope at its first eligible node",
     "keyboard_navigation::keyboard traversal crosses navigation groups in group order (DK-2)::Tab leaves a CONTAINED group that the arrows cannot leave sideways",
     "keyboard_navigation::keyboard traversal crosses navigation groups in group order (DK-2)::Shift+Tab crosses back into the previous group at its LAST node",
     "keyboard_navigation::the SCOPE declares the traversal wrap policy (DK-3)::wraps by default at both ends",
     "keyboard_navigation::the SCOPE declares the traversal wrap policy (DK-3)::traversalWrap = false stops at the ends and reports that it did not move",
     "keyboard_navigation::the SCOPE declares the traversal wrap policy (DK-3)::a GROUPED scope's wrap policy is the scope's, not any group's",
     "keyboard_navigation::traversal skips exactly what navigation skips (DK-4)::an ineligible focus-skip entry is stepped over, not landed on",
     "keyboard_navigation::traversal skips exactly what navigation skips (DK-4)::focus stays put when nothing else in the scope is eligible",
     "keyboard_navigation::traversal survives structural churn (DK-5)::a node removed between presses never becomes a traversal target",
     "keyboard_navigation::traversal survives structural churn (DK-5)::re-ordering the scope re-reads the order on the very next press",
     "keyboard_navigation::modal scopes trap traversal and restore focus (DK-6)::Tab cannot leave a trapped scope and the prior focus is restored on pop",
     "keyboard_navigation::Tab traverses a presented screen with zero consumer wiring (DK-1/DK-16)::Tab and Shift+Tab move the focus ring through a plain form",
     "keyboard_navigation::Tab traverses a presented screen with zero consumer wiring (DK-1/DK-16)::a traversal move restores the focus RING that a tap put away",
     "keyboard_navigation::traversal calls the SHARED keep-visible service (DK-7)::tabbing past the visible band scrolls the host, with no control wiring",
     "keyboard_navigation::Space activates alongside Return, once per press (DK-8)::Space activates the focused control exactly once",
     "keyboard_navigation::Space activates alongside Return, once per press (DK-8)::Return still activates, and the two keys do not double-count one press",
     "keyboard_navigation::Space activates alongside Return, once per press (DK-8)::a disabled control consumes Space and does nothing",
     "keyboard_navigation::exactly one Activate per press across the IAS and native-tap paths (DK-9)::a native Activated echoing the same keyboard press does not activate twice",
     "keyboard_navigation::exactly one Activate per press across the IAS and native-tap paths (DK-9)::two genuinely separate presses outside the echo window both count",
     "keyboard_navigation::exactly one Activate per press across the IAS and native-tap paths (DK-9)::a spent echo does not roll the window forward onto the next real press",
     "keyboard_navigation::exactly one Activate per press across the IAS and native-tap paths (DK-9)::a real pointer tap beside a key press is two presses, never an echo",
     "keyboard_navigation::exactly one Activate per press across the IAS and native-tap paths (DK-9)::a surface with no selection bridge never collapses a tap next to a key press",
     "keyboard_navigation::a focused value control consumes its declared axis (DK-10/DK-11)::Left/Right adjust a focused horizontal Slider on a GROUPED screen",
     "keyboard_navigation::a focused value control consumes its declared axis (DK-10/DK-11)::the OTHER axis still navigates while the value control holds focus",
     "keyboard_navigation::a focused value control consumes its declared axis (DK-10/DK-11)::the Adjust keys are UNBOUND while focus sits off a declared target",
     "keyboard_navigation::a value control claims Adjust only inside its own subtree (DK-11)::the Adjust keys unbind when focus leaves the control for a sibling button",
     "keyboard_navigation::a value control claims Adjust only inside its own subtree (DK-11)::a sibling button's arrows fire no dead Adjust",
     "keyboard_navigation::a value control claims Adjust only inside its own subtree (DK-11)::with keyboard navigation OFF the surface binds nothing and sinks nothing",
     "keyboard_navigation::a focused Slider draws its own treatment, on the thumb (DK-20)::the thumb takes a focus stroke while the track holds focus, and drops it after",
     "keyboard_navigation::a focused Slider draws its own treatment, on the thumb (DK-20)::the \"focusRing\" thickness alias resolves to the adapter's own ring weight",
     "keyboard_navigation::a focused Slider draws its own treatment, on the thumb (DK-20)::the track declares that the adapter must not paint a focused state",
     "keyboard_navigation::a Stepper is driven by Activate, not by the arrows (DK-10)::Space on the focused button steps the value, in both directions",
     "keyboard_navigation::a Stepper is driven by Activate, not by the arrows (DK-10)::the axis-independent Adjust keys still reach the same arithmetic",
     "keyboard_navigation::text editing owns Space and the arrows; Tab commits then advances (DK-12)::Space does not activate anything while the field is editing",
     "keyboard_navigation::text editing owns Space and the arrows; Tab commits then advances (DK-12)::Tab commits through the field's own path and only THEN advances focus",
     "keyboard_navigation::text editing owns Space and the arrows; Tab commits then advances (DK-12)::the arrows stay with the field's own sinking context while editing",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::a PASSIVE HUD binds no Tab and no Space: gameplay is untouched",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::engage() binds them and resign() gives them back, with no residue",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::a tap on the UI takes the keyboard, and a tap OUTSIDE gives it back",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::a PASSIVE surface paints no focus ring: the ring never claims input it lacks",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::an ordinary (engaged-open) screen still paints its ring exactly as before",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::initialFocus defaults to the first focusable — every existing caller unmoved",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::a PASSIVE surface defaults to NO initial focus",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::...while an engaged-open screen still defaults to its first focusable",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::an explicit initialFocus still wins on a passive surface",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::initialFocus = \"none\" mounts the surface with NO focus",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::...and Tab still ENTERS such a scope rather than no-opping",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::initialFocus names a control by id, without the author knowing its path",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::focus lands there ONCE — the ring never appears elsewhere first",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::an initialFocus id that names nothing is refused, listing what is available",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::a focusable actually named \"first\" makes the keyword ambiguous, and it says so",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::...and { id = … } is the escape hatch that means the CONTROL",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::a dismissed surface gives up its focus ring immediately, mid-exit-transition",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::a self-painted focus ring clears when the surface resigns",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::an engaged HUD resigns on an outside tap even CLOSE to its own content",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::...while a MODAL keeps its dismissal forgiveness",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::a resigned surface unbinds the Adjust keys it had claimed",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::keyboard hot-plug adds and removes the bindings with no stuck sink",
     "keyboard_navigation::keyboard bindings follow capability and responder ownership (DK-13/DK-15)::gameplayGuard = false leaves Space to the game even while engaged",
     "keyboard_navigation::keyboard navigation is opt-in, globally and per surface (DK-19)::defaults OFF at the presenter: no Tab, no Space, no sink",
     "keyboard_navigation::keyboard navigation is opt-in, globally and per surface (DK-19)::presenter opt turns it on for every surface, and the surface sinks",
     "keyboard_navigation::keyboard navigation is opt-in, globally and per surface (DK-19)::a surface can override the presenter default in both directions",
     "keyboard_navigation::keyboard navigation is opt-in, globally and per surface (DK-19)::an engaged surface EATS the contended keys instead of sharing them",
     "keyboard_navigation::keyboard navigation is opt-in, globally and per surface (DK-19)::a boolean is required, and the error names the inherit spelling",
     "keyboard_navigation::teardown removes every keyboard binding (DK-14)::a dismissed screen stops eating Tab and Space",
     "keyboard_navigation::a modal traps traversal and restores focus through the presenter (DK-6)::Tab stays inside the modal and the base screen's focus comes back",
     "keyboard_navigation::traversal wrap is declarable per presented surface (DK-3)::present({ traversalWrap = false }) stops Tab at the last control",
     "keyboard_navigation::traversal wrap is declarable per presented surface (DK-3)::an unknown present() opt is still refused by name",
     "traversal_order::a Grip-bearing control BETWEEN ordinary controls (TD-1, TD-2)::Tab visits the Slider's track in DOCUMENT position, not last",
     "traversal_order::a Grip-bearing control BETWEEN ordinary controls (TD-1, TD-2)::...and the ARROWS still defer it to the end, exactly as before",
     "traversal_order::a Grip-bearing control BETWEEN ordinary controls (TD-1, TD-2)::the two readings cover the SAME SET — traversal reorders, never adds or drops",
     "traversal_order::a Grip-bearing control BETWEEN ordinary controls (TD-1, TD-2)::pressing Tab really moves focus in that order (onFocusNav, not the dump)",
     "traversal_order::every focusable Grip traverses in document position — no exceptions (TD-4)::a bare focusable Grip declared between buttons traverses between them",
     "traversal_order::every focusable Grip traverses in document position — no exceptions (TD-4)::...and pressing Tab really lands on it there",
     "traversal_order::every focusable Grip traverses in document position — no exceptions (TD-4)::newRating's strip Grip traverses in document position too",
     "traversal_order::traversalPriority is a sort TIER, not an index (TD-5, TD-6)::a positive tier pushes one control to the back without moving the others",
     "traversal_order::traversalPriority is a sort TIER, not an index (TD-5, TD-6)::...and pressing Tab really reaches Submit last",
     "traversal_order::traversalPriority is a sort TIER, not an index (TD-5, TD-6)::a negative tier pulls one to the front",
     "traversal_order::traversalPriority is a sort TIER, not an index (TD-5, TD-6)::within one tier, DOCUMENT order always wins",
     "traversal_order::traversalPriority is a sort TIER, not an index (TD-5, TD-6)::the tier does NOT reach the arrows",
     "traversal_order::traversalPriority is a sort TIER, not an index (TD-5, TD-6)::a tier moves a GRIP relative to document order too",
     "traversal_order::traversalPriority refuses what it cannot honour (TD-8)::a non-number is refused, naming the property",
     "traversal_order::traversalPriority refuses what it cannot honour (TD-8)::a BOUND value is refused: the prop is construction-only",
     "traversal_order::traversalPriority refuses what it cannot honour (TD-8)::an unknown neighbour is still refused by name (the guard is live)",
     "traversal_order::an explicit navigationGroups map is NOT re-ranked (TD-7)::the declared order survives exactly, grips and all",
     "traversal_order::an explicit navigationGroups map is NOT re-ranked (TD-7)::...and pressing Tab really follows the DECLARED order, not document order",
     "traversal_order::an explicit navigationGroups map is NOT re-ranked (TD-7)::navigationGroups = false is NOT a declared map: the flat ring is still ranked",
     "traversal_order::handle.focusOrder() (TD-9, TD-10)::carries its schema and both readings",
     "traversal_order::handle.focusOrder() (TD-9, TD-10)::is deterministic: two calls with nothing changed are equal",
     "traversal_order::handle.focusOrder() (TD-9, TD-10)::is frozen — an inspector cannot mutate the map it is inspecting",
     "traversal_order::handle.focusOrder() (TD-9, TD-10)::reports the AUTHORED tier per entry",
     "traversal_order::handle.focusOrder() (TD-9, TD-10)::survives dispose: an overlay outliving its surface gets an empty map, not a throw",
     "traversal_order::traversal skips exactly what the arrows skip (TD-3)::a disabled control leaves BOTH readings",
     "traversal_order::traversal skips exactly what the arrows skip (TD-3)::focusable = false leaves both readings",
     "traversal_order::structural churn re-ranks on the next press (TD-11)::a row mounted after present() takes its document position, not the end",
     "traversal_order::a modal traverses in document order too, and still traps (TD-12)::a Grip inside a modal takes its document position within the modal",
     "traversal_order::graph.traverse sorts by rank without changing membership (TDN-2)::no rank means the pre-stage reading, byte for byte",
     "traversal_order::graph.traverse sorts by rank without changing membership (TDN-2)::a rank reorders the SAME members",
     "traversal_order::graph.traverse sorts by rank without changing membership (TDN-2)::ENTRY focus still reads the unranked order, by design",
     "traversal_order::graph.traverse sorts by rank without changing membership (TDN-2)::an INCOMPLETE rank is ignored entirely rather than applied to half the ring",
     "traversal_order::graph.traverse sorts by rank without changing membership (TDN-2)::the arrows never read the rank",
     "traversal_order::graph.traverse sorts by rank without changing membership (TDN-2)::the graph keeps its OWN copy of the rank (F-14)",
     "focus_chrome::adopted chrome (1): the focus map is composed, and the arrows cross into it::without the declaration the chrome is in NEITHER ring — the measured gap, kept",
     "focus_chrome::adopted chrome (1): the focus map is composed, and the arrows cross into it::with `focusChrome` the ACTIVE scope's map carries the chrome's paths",
     "focus_chrome::adopted chrome (1): the focus map is composed, and the arrows cross into it::a `top` chrome reads BEFORE the content, and its own group order survives",
     "focus_chrome::adopted chrome (1): the focus map is composed, and the arrows cross into it::a `bottom` chrome reads AFTER it",
     "focus_chrome::adopted chrome (1): the focus map is composed, and the arrows cross into it::`leading` and `trailing` are the horizontal spellings of the same two placements",
     "focus_chrome::adopted chrome (1): the focus map is composed, and the arrows cross into it::THE D-PAD CROSSES: up from the first content row lands on the strip",
     "focus_chrome::adopted chrome (1): the focus map is composed, and the arrows cross into it::...and the strip is a ROW, so left/right walk it and down returns to the content",
     "focus_chrome::adopted chrome (1): the focus map is composed, and the arrows cross into it::DOWN from the first of two chips LEAVES the strip; it does not step along it",
     "focus_chrome::adopted chrome (1): the focus map is composed, and the arrows cross into it::a `bottom` chrome is reached by pressing DOWN off the end instead",
     "focus_chrome::adopted chrome (1): the focus map is composed, and the arrows cross into it::Tab walks the SAME members (§9: one focus map, two readings)",
     "focus_chrome::adopted chrome (2): what adoption must NOT do::ENTRY is the active scope's, so the first press lands in the content",
     "focus_chrome::adopted chrome (2): what adoption must NOT do::a MODAL suspends adoption: its ring cannot leak into the strip",
     "focus_chrome::adopted chrome (2): what adoption must NOT do::dismissing the chrome takes its paths out of the composed map",
     "focus_chrome::adopted chrome (2): what adoption must NOT do::a chrome with nothing above it is simply the active scope, unchanged",
     "focus_chrome::adopted chrome (2): what adoption must NOT do::an edge nobody can place is refused, not silently ignored",
     "focus_chrome::adopted chrome (3): the press has to land on the chrome's OWN surface::Activate on an adopted chip runs the CHIP's handler, not nothing",
     "focus_chrome::adopted chrome (3): the press has to land on the chrome's OWN surface::...and a press on the content still runs the content's own handler",
     "focus_chrome::adopted chrome (3): the press has to land on the chrome's OWN surface::one press is one activation, not one per surface that saw the key",
     "path::path_shapes: deterministic arc math (NS-A7)::a full ring closes: 9 points, endpoints coincide, all on the radius",
     "path::path_shapes: deterministic arc math (NS-A7)::screen angle convention: 0deg is up, 90deg is right, y grows down",
     "path::path_shapes: deterministic arc math (NS-A7)::tangent handedness: out = forward along the path, in = backward, exact bezier handle length",
     "path::path_shapes: deterministic arc math (NS-A7)::progress sweep is monotonic: a bigger sweep ends farther along the circle",
     "path::path_shapes: deterministic arc math (NS-A7)::needle: two straight points from inner to outer radius at the angle",
     "path::path_shapes: deterministic arc math (NS-A7)::segment counts respect the engine's 100-point limit",
     "path::UI.Path: paint-only reactive stroke (NS-A7)::mounts as a leaf; a points change is one prop write and never a re-solve",
     "path::UI.Path: paint-only reactive stroke (NS-A7)::a metric-named Path thickness PAINTS to the theme's resolved number, and scales at ten-foot",
     "path::UI.Path clipping (RS-PATHCLIP)::a path whose row is outside the scroll viewport does not paint",
     "path::UI.Path clipping (RS-PATHCLIP)::the cull follows the LIVE scroll offset, with no re-solve",
     "path::UI.Path clipping (RS-PATHCLIP)::a partially-clipped row's path is culled: a stroke has no half-crop",
     "path::UI.Path clipping (RS-PATHCLIP)::a path with no clip host above it is never culled",
     "stage::UI.Stage: strict authoring (a)::constructs from a valid spec and is a non-container leaf",
     "stage::UI.Stage: strict authoring (a)::refuses an unknown property with a did-you-mean",
     "stage::UI.Stage: strict authoring (a)::enforces the eight-surface vocabulary",
     "stage::UI.Stage: mount, dump, dispose (b)::mounts as one node and dumps deterministically",
     "stage::UI.Stage: mount, dump, dispose (b)::mount/render/dispose returns the core registry to baseline",
     "stage::UI.Stage: the stageHost content seam (c)::is a DECLARED optional target method, so an adapter without it degrades by name",
     "stage::UI.Stage: the stageHost content seam (c)::returns nil for an unknown path, a non-Stage node, and an adapter without the seam",
     "stage::UI.Stage: the stageHost content seam (c)::hands back ONE cached handle per node",
     "stage::UI.Stage: the stageHost content seam (c)::setCamera records position/lookAt/fov in the exact table shape",
     "stage::UI.Stage: the stageHost content seam (c)::setLighting records ambient/lightColor/lightDirection as 0-1 rgb and a vector",
     "stage::UI.Stage: the stageHost content seam (c)::refuses a malformed camera/lighting spec instead of writing nonsense",
     "stage::UI.Stage: the stageHost content seam (c)::contentRoot answers the SAME content root every call (headless: a recording stub)",
     "stage::UI.Stage: the stageHost content seam (c)::a handle used after its node dies errors with a message naming the node",
     "stage::UI.Stage: property authority (d)::declares its authored props: surface is style, tint is binding",
     "stage::UI.Stage: property authority (d)::the four ViewportFrame properties the SEAM owns are declared seam-owned",
     "stage::UI.Stage: property authority (d)::the gate catches a write that is not the stage seam's, and an undeclared property",
     "stage::UI.Stage: property authority (d)::the live adapter makes NO bespoke write to a seam-owned property",
     "stage::UI.Stage: property authority (d)::is registered as a NON-INTERACTIVE control with a leaf row",
     "stage::UI.Stage: layout (e)::solves to exactly the box it declares",
     "stage::UI.Stage: layout (e)::measures 0x0 without explicit dims (it has no intrinsic content) — like Box",
     "stage::UI.Stage: layout (e)::paints through the ordinary style/binding channels (surface + tint)",
     "stage::UI.Stage: layout (e)::a Stage never classifies into a decoration slot (an ornate skin cannot cover the scene)",
     "foreign::UI.Foreign: strict authoring (a)::constructs from a valid spec and is a non-container leaf",
     "foreign::UI.Foreign: strict authoring (a)::refuses an unknown property with a did-you-mean",
     "foreign::UI.Foreign: strict authoring (a)::refuses the UNBOUNDED form by name, with the argument and the alternative",
     "foreign::UI.Foreign: strict authoring (a)::refuses `tint`, because Facet never paints the caller's content",
     "foreign::UI.Foreign: strict authoring (a)::enforces the eight-surface vocabulary on the plate BEHIND the content",
     "foreign::UI.Foreign: mount, elision, dispose (b)::mounts as one node and dumps deterministically",
     "foreign::UI.Foreign: mount, elision, dispose (b)::is NEVER elided, on either adapter, even bare",
     "foreign::UI.Foreign: mount, elision, dispose (b)::mount/render/dispose returns the core registry to baseline",
     "foreign::UI.Foreign: the foreignHost content seam (c)::is a DECLARED optional target method, so an adapter without it degrades by name",
     "foreign::UI.Foreign: the foreignHost content seam (c)::the renderer asserts the `host` authority at the seam, not just the engine adapter (ARCH-25)",
     "foreign::UI.Foreign: the foreignHost content seam (c)::returns nil for an unknown path, a non-Foreign node, and an adapter without the seam",
     "foreign::UI.Foreign: the foreignHost content seam (c)::hands back ONE cached handle per node",
     "foreign::UI.Foreign: the foreignHost content seam (c)::adopt makes exactly ONE claim on the caller's instance: its Parent",
     "foreign::UI.Foreign: the foreignHost content seam (c)::refuses nothing, a LayerCollector, a non-GuiObject and a destroyed instance — each by name",
     "foreign::UI.Foreign: the foreignHost content seam (c)::a handle used after its node dies errors with a message naming the node",
     "foreign::UI.Foreign: property authority (d)::makes the reserved `host` authority LIVE, owning exactly Foreign.Parent",
     "foreign::UI.Foreign: property authority (d)::the gate catches a second writer for the one claimed property",
     "foreign::UI.Foreign: property authority (d)::claims NOTHING else: every other Foreign prop resolves through common",
     "foreign::UI.Foreign: property authority (d)::the live adapter makes its ONE write through the gate",
     "foreign::UI.Foreign: property authority (d)::retains no reference to the adopted content, so it cannot write to it later",
     "foreign::UI.Foreign: property authority (d)::is registered as a NON-INTERACTIVE control with a leaf row",
     "foreign::UI.Foreign: layout (e)::solves to exactly the box it declares",
     "foreign::UI.Foreign: layout (e)::measures 0x0 without explicit dims — like Box",
     "foreign::UI.Foreign: layout (e)::clips its content to the reserved box, on both adapters",
     "foreign::UI.Foreign: layout (e)::the shipped materializer never leaves an adopted paint prop at its class default",
     "foreign::UI.Foreign: layout (e)::every SHIPPED foreign descriptor crosses the seam with the types the engine takes",
     "foreign::UI.Foreign: layout (e)::is never a decoration surface: the create path pins NO_SLOT for it",
     "foreign::UI.Foreign: containment (f)::is never parked into the recycling pool",
     "foreign::UI.Foreign: containment (f)::takes no focus stop: inserting one does not change traversal order",
     "foreign::UI.Foreign: containment (f)::contributes its box to the surface cover rect, like any other leaf",
     "async_image::AsyncImage (NS-A14)::pending shows the placeholder and an empty image",
     "async_image::AsyncImage (NS-A14)::transport completion swaps placeholder for the content",
     "async_image::AsyncImage (NS-A14)::transport failure is SILENT by default: the placeholder persists, no mark (SF-A1)",
     "async_image::AsyncImage (NS-A14)::release-then-stale-completion never resurrects (honest cancellation)",
     "selection_bridge::engine-selection bridge (NS-A12)::default surfaces never touch engine selection (NS-A11)",
     "selection_bridge::engine-selection bridge (NS-A12)::present() IGNORES the opt (bridge is modal-only)",
     "selection_bridge::engine-selection bridge (NS-A12)::an opted-in modal mirrors logical focus and clears on dismiss",
     "selection_bridge::engine-selection bridge (NS-A12)::a modal WITHOUT the opt stays engine-silent",
     "text_calibration::text_metrics calibration (NS-A10)::a calibrated fraction tightens the estimate and never widens it",
     "text_calibration::text_metrics calibration (NS-A10)::clamps garbage to the safe band [0.30, 0.62]",
     "text_calibration::text_metrics calibration (NS-A10)::calibration is per-font; unknown fonts keep the full-em pending fallback",
     "text_calibration::text_metrics calibration (NS-A10)::CJK glyphs stay full-em regardless of calibration (never under-reserve wide scripts)",
     "text_calibration::text_metrics: consecutive spaces are reserved, not collapsed::a doubled space is WIDER than a single one, by about one space",
     "text_calibration::text_metrics: consecutive spaces are reserved, not collapsed::the live legend string measures wider than its single-spaced form",
     "text_calibration::text_metrics: consecutive spaces are reserved, not collapsed::a single-spaced string is byte-identical to what it always reserved",
     "text_calibration::text_metrics: consecutive spaces are reserved, not collapsed::a gap made of a NEWLINE still counts as one, so embedded newlines are unchanged",
     "text_calibration::text_metrics: consecutive spaces are reserved, not collapsed::leading and trailing whitespace still reserve nothing",
     "text_degrade_cascade::ruling 1: the degrade cascade descends only as far as it must::a row that FITS is untouched — the cascade never opens",
     "text_degrade_cascade::ruling 1: the degrade cascade descends only as far as it must::rung 1: it gives up its WRAP first, and keeps every character",
     "text_degrade_cascade::ruling 1: the degrade cascade descends only as far as it must::rung 2: a declared COMPACT form is spent before any character is",
     "text_degrade_cascade::ruling 1: the degrade cascade descends only as far as it must::rung 3: with nothing better to say it TRUNCATES, on one line, with the box intact",
     "text_degrade_cascade::ruling 1: the degrade cascade descends only as far as it must::the rungs are ORDERED: the same deficit truncates without a compact form and does not with one",
     "text_degrade_cascade::ruling 1: the degrade cascade descends only as far as it must::the compact rung SPARES A SIBLING: the label with something shorter to say gives it up",
     "text_degrade_cascade::ruling 1: the degrade cascade descends only as far as it must::an authored `minMax.min` is a CONTRACT, not a rung — the cascade never descends below it",
     "text_degrade_cascade::ruling 1: clip is the floor of the cascade, never its first move::what the cascade cannot absorb is CROPPED at the box, not painted past it",
     "text_degrade_cascade::ruling 1: clip is the floor of the cascade, never its first move::...and it is still LOUD: the diagnostic names the deficit, the tiers, and the clip",
     "text_degrade_cascade::ruling 1: clip is the floor of the cascade, never its first move::the cascade absorbs the deficit ONCE, so a `fill` sibling is handed nothing extra",
     "text_degrade_cascade::ruling 1: clip is the floor of the cascade, never its first move::a stack that declared NO shrinkWeight is untouched — it still paints outside its box",
     "text_degrade_cascade::ruling 5: wrapping is attempted only where it can succeed::a phrase whose longest word FITS still wraps — the round-9 chips keep their two lines",
     "text_degrade_cascade::ruling 5: wrapping is attempted only where it can succeed::...and one whose longest word does NOT fit is measured on ONE line instead of breaking it",
     "text_degrade_cascade::ruling 5: wrapping is attempted only where it can succeed::the verdict is RE-DECIDED, never latched — a wider box wraps again",
     "text_degrade_cascade::ruling 5: wrapping is attempted only where it can succeed::a FIXED-width label is clamped to ITS OWN box, and says so when it is cut",
     "text_degrade_cascade::ruling 5: wrapping is attempted only where it can succeed::a ZERO-width box gives no verdict: a collapsed control is not a label being cut",
     "text_degrade_cascade::ruling 5: the two seams agree, because only one of them decides::the SOLVER's verdict reaches the adapter, which cannot compute it",
     "text_degrade_cascade::ruling 5: the two seams agree, because only one of them decides::...and a box that fits costs ZERO writes: `nil` IS the adapter's wrapping default",
     "text_degrade_cascade::ruling 5: the two seams agree, because only one of them decides::a steady re-solve writes the verdict once and never again",
     "text_degrade_cascade::the cascade holds under the studio's standing 1.4x pseudo-loc rule::every expanded label still lands INSIDE a box far too small for it",
     "text_degrade_cascade::the cascade holds under the studio's standing 1.4x pseudo-loc rule::...and the expansion is REAL, or the case above proves nothing",
     "text_degrade_cascade::min-content width: whitespace is not the only legal break::an ideographic run breaks between CHARACTERS, so it never claims the whole run",
     "text_degrade_cascade::min-content width: whitespace is not the only legal break::...and a CJK line therefore still WRAPS rather than ellipsizing",
     "text_degrade_cascade::min-content width: whitespace is not the only legal break::an EMOJI cluster is NOT split — the minimum may never be under-stated",
     "text_degrade_cascade::min-content width: whitespace is not the only legal break::a latin phrase is BYTE-IDENTICAL to the whitespace-only model it replaced",
     "text_fit::text_fit.fit — the largest size that actually fits::returns the cap untouched when the string already fits at it",
     "text_fit::text_fit.fit — the largest size that actually fits::the answer FITS and the next size up does NOT — verified against the measurer",
     "text_fit::text_fit.fit — the largest size that actually fits::a box that cannot hold the string at its floor says `fits = false`",
     "text_fit::text_fit.fit — the largest size that actually fits::honours a HEIGHT constraint as well as a width",
     "text_fit::text_fit.fit — the largest size that actually fits::is monotone: a wider box never returns a smaller size",
     "text_fit::text_fit.fit — the largest size that actually fits::an empty string is a fit at the cap, and a zero-width box never errors",
     "text_fit::text_fit.fit — the largest size that actually fits::reports whether the answer is EXACT or the conservative fallback",
     "text_fit::text_fit.fit — the largest size that actually fits::the paint-time offset shrinks the answer: a Largest player fits fewer authored px (LTN-1)",
     "text_fit::text_fit.fit — the largest size that actually fits::offset zero and absent are byte-identical (the pre-8.5 answer); NaN is refused at the boundary",
     "text_fit::text.measure — the spec-table form (F-31)::both forms answer identically for the same inputs",
     "text_fit::text.measure — the spec-table form (F-31)::a missing or mistyped key fails at the call site, naming the legal set",
     "text_fit::text_fit.fit — the state of the measure at the chosen size (F-32)::a calibrated font reports the same state its measure does",
     "text_fit::text_fit.fit — the state of the measure at the chosen size (F-32)::an uncalibrated font says `pending`, not merely `exact = false`",
     "text_fit::text_fit.fit — the state of the measure at the chosen size (F-32)::the cap-fits and empty-string paths carry it too",
     "text_fit::text.fit is strict at its boundary (architecture review F-6)::an unknown spec key is refused, naming it",
     "text_fit::text.fit is strict at its boundary (architecture review F-6)::a mistyped offset is refused, never silently measured at 0",
     "measure_entry_point::GAP 11: the measurement engine is reachable without src/init.luau::requires cleanly, standing alone, with zero @self/init dependency",
     "measure_entry_point::GAP 11: the measurement engine is reachable without src/init.luau::is the SAME module text_metrics.luau is — a republish, not a fork",
     "measure_entry_point::GAP 11: the measurement engine is reachable without src/init.luau::exports the fallback constant, so a consumer sources it instead of copying it",
     "measure_entry_point::GAP 11: the measurement engine is reachable without src/init.luau::measures for real: a headless caller gets the library's own metrics, not a guess",
     "measure_entry_point::GAP 11: the measurement engine is reachable without src/init.luau::minWidth answers the same fallback-fraction arithmetic a hand-rolled estimate reproduces",
     "measure_entry_point::Registry evidence — item 31 (pure viewport-fact simulator), NOT built here::DEMONSTRATION: adaptive.luau's pure half is already reachable with no @self/init dependency",
     "text_size_fit::the two constants, copied wrong — the class `textSize = \"fit\"` closes::a 1.25 line box is not the framework's line box, and the gap grows with the size",
     "text_size_fit::the two constants, copied wrong — the class `textSize = \"fit\"` closes::a reserve that ignores the type ladder under-reserves by the ladder's own factor",
     "text_size_fit::the two constants, copied wrong — the class `textSize = \"fit\"` closes::the safe-looking 1.25 does not cover the missing scale",
     "text_size_fit::textSize = \"fit\": the solver picks the size, and paint writes what it picked::paints the CAP when the label already fits it",
     "text_size_fit::textSize = \"fit\": the solver picks the size, and paint writes what it picked::steps the size down until the line fits the box, and the answer is the largest that does",
     "text_size_fit::textSize = \"fit\": the solver picks the size, and paint writes what it picked::the reserved box holds the painted line",
     "text_size_fit::textSize = \"fit\": the solver picks the size, and paint writes what it picked::re-derives on a ten-foot display, where every hand copy froze",
     "text_size_fit::textSize = \"fit\": the solver picks the size, and paint writes what it picked::a ten-foot cap that no longer fits the same box steps down instead of overflowing",
     "text_size_fit::textSize = \"fit\": the solver picks the size, and paint writes what it picked::pays the preferred-text offset, so a raised preference re-sizes instead of ellipsizing",
     "text_size_fit::textSize = \"fit\": the solver picks the size, and paint writes what it picked::honours an explicit cap and floor, and never paints below the floor",
     "text_size_fit::textSize = \"fit\": the solver picks the size, and paint writes what it picked::a cap that names a role uses that role's size as the ceiling",
     "text_size_fit::textSize = \"fit\": the solver picks the size, and paint writes what it picked::defaults its floor to the theme's caption role",
     "text_size_fit::textSize = \"fit\": the solver picks the size, and paint writes what it picked::wraps rather than shrinking when the box has the height and no lineLimit",
     "text_size_fit::textSize = \"fit\": every class that carries a textSize::a Toggle fits its label into the box the switch leaves it",
     "text_size_fit::textSize = \"fit\": the grammar is closed::reads both spellings and nothing else",
     "text_size_fit::textSize = \"fit\": the grammar is closed::refuses a misspelled option at construction",
     "text_size_fit::textSize = \"fit\": the grammar is closed::refuses a cap that is neither a px number nor a role name",
     "text_size_fit::textSize = \"fit\": the grammar is closed::still accepts every spelling it accepted before",
     "text_line_box::text.facts — the live text facts, read once for a whole extent pass::the { metrics } form derives the scales and the offset from the snapshot's own facts",
     "text_line_box::text.facts — the live text facts, read once for a whole extent pass::a SUB-1 preference reserves at the PAINT scale, never at the smaller one",
     "text_line_box::text.facts — the live text facts, read once for a whole extent pass::the offset is clamped to the same legal domain the renderer's measure seam clamps it to",
     "text_line_box::text.facts — the live text facts, read once for a whole extent pass::the { env, use } form and the { metrics } form agree on every measured preference",
     "text_line_box::text.facts — the live text facts, read once for a whole extent pass::{ env } WITHOUT `use` is refused — a fact read with :get() is right once and then stale",
     "text_line_box::text.facts — the live text facts, read once for a whole extent pass::neither form, or both, is refused; an unknown key is refused",
     "text_line_box::text.facts — the live text facts, read once for a whole extent pass::the extent function's contract: `use` is the memo's, so the facts re-derive when a fact moves",
     "text_line_box::text.lineBox — the one implementation of the line-box arithmetic::is the solver's own two seams, composed in the solver's order",
     "text_line_box::text.lineBox — the one implementation of the line-box arithmetic::THE CEIL LANDS ONCE, on the whole product — not per line",
     "text_line_box::text.lineBox — the one implementation of the line-box arithmetic::the offset is ADDITIVE and lands AFTER the scale, never through it",
     "text_line_box::text.lineBox — the one implementation of the line-box arithmetic::a numeric size takes `body`'s line height — which is what the solver resolves it to",
     "text_line_box::text.lineBox — the one implementation of the line-box arithmetic::the offset's marginal cost does NOT depend on the scale — the invariant behind `additive AFTER`",
     "text_line_box::text.lineBox — the one implementation of the line-box arithmetic::`role` overrides which line height applies, without moving the size",
     "text_line_box::text.lineBox — the one implementation of the line-box arithmetic::refuses a role this theme does not carry, a bad size, a bad line count and an unknown key",
     "text_line_box::composition.floorPx — the three defects a public function carried::DEFECT 1 — no `ceil`: the floor could be smaller than any box the solver can draw",
     "text_line_box::composition.floorPx — the three defects a public function carried::DEFECT 2 — no SCALE: the player's text preference and the ten-foot floor moved nothing",
     "text_line_box::composition.floorPx — the three defects a public function carried::DEFECT 3 — no TEXT OFFSET: the four preferences this project binds itself to were invisible",
     "text_line_box::composition.floorPx — the three defects a public function carried::the un-broken halves are untouched: `targets`, the max of both, nil, and the role fallback",
     "text_line_box::composition.floorPx — the three defects a public function carried::a named role still wins, and it scales with the facts like every other floor",
     "text_line_box::text.lineBox equals what the solver arranges — differentially, over a generated corpus::every row: the predicted line box IS the arranged rect, and the predicted reserved size IS the solve's",
     "value_reveal::value_reveal — rule 1: NOT YET KNOWN is not an abandon::a sync before the payload exists leaves the hold ALONE",
     "value_reveal::value_reveal — rule 2: SEED, then count::the animator holds the START value before anything reads it",
     "value_reveal::value_reveal — rule 3: every abandon path lands the truth::an explicit abandon releases, and cannot be restarted by a later cue",
     "value_reveal::value_reveal — rule 3: every abandon path lands the truth::a window already PAST releases instead of holding — the late-arrival path",
     "value_reveal::value_reveal — rule 3: every abandon path lands the truth::a window still COMING holds",
     "value_reveal::value_reveal — rule 3: every abandon path lands the truth::`rest` releases and rearms",
     "value_reveal::value_reveal — rule 4: once per epoch::runs once, and a repeated cue is a no-op",
     "value_reveal::value_reveal — rule 4: once per epoch::a NEW epoch rearms on the settled state of the last one",
     "value_reveal::value_reveal — rule 5: degrading is not withholding::with NO animator the fact is stated immediately, never held",
     "value_reveal::value_reveal — landing::`landed` stops the count without re-holding",
     "text_premeasure::text_metrics: the per-word exact store::an exact width replaces the estimate for that word and no other",
     "text_premeasure::text_metrics: the per-word exact store::the same word at a different size or font is a different measurement",
     "text_premeasure::text_metrics: the per-word exact store::an exact width WIDER than the estimate still wins — exactness is not a tightening-only seam",
     "text_premeasure::text_metrics: the per-word exact store::rejects a measurement that could cause a clip, keeping the safe estimate",
     "text_premeasure::text_metrics: the per-word exact store::an exact space width is used between words",
     "text_premeasure::text_metrics: the request set a solve emits::collects one request per unknown word plus the space, deduped and ordered",
     "text_premeasure::text_metrics: the request set a solve emits::keys on (font, size, word) — NOT maxWidth, so a resize costs no new measurement",
     "text_premeasure::text_metrics: the request set a solve emits::a word the engine could not measure is never asked for again",
     "text_premeasure::text_metrics: the request set a solve emits::nothing is collected outside a collect bracket (the solver stays request-free by default)",
     "text_premeasure::text_metrics: the request set a solve emits::reports whether a measurement was exact end to end",
     "text_premeasure::text_metrics: the request set a solve emits::exact word widths wrap greedily exactly where the engine would",
     "text_premeasure::renderer: engine premeasurement round::reserves the estimate on the first frame and the EXACT box after one re-solve",
     "text_premeasure::renderer: engine premeasurement round::publishes a MEASURE EPOCH, so a consumer memo can depend on exactness",
     "text_premeasure::renderer: engine premeasurement round::a known vocabulary asks for nothing on later solves",
     "text_premeasure::renderer: engine premeasurement round::an adapter with no premeasurement seam keeps today's conservative geometry",
     "text_premeasure::renderer: engine premeasurement round::a measurement arriving after dispose neither crashes nor re-solves",
     "text_premeasure::renderer: engine premeasurement round::a corrected width from a second answer to the same batch replaces the first",
     "text_premeasure::renderer: engine premeasurement round::a second answer that agrees with the first costs no re-solve",
     "text_premeasure::renderer: engine premeasurement round::an engine that cannot measure a word leaves the safe estimate and stops asking",
     "text_premeasure::presenter: revealWhenTextExact::keeps the surface hidden until the text round closes, then reveals it exact",
     "text_premeasure::presenter: revealWhenTextExact::keeps waiting through a NON-FINAL answer, so a boot-window correction cannot pop after the reveal",
     "text_premeasure::presenter: revealWhenTextExact::reveals anyway when the deadline passes: a hidden UI is worse than a jumpy one",
     "text_premeasure::presenter: revealWhenTextExact::does not gate a surface that never asked to be gated",
     "text_premeasure::presenter: revealWhenTextExact::never hides a surface on an adapter that cannot show it again",
     "text_premeasure::presenter: revealWhenTextExact::a cancelled measure never calls `done` again (PLAT-12)",
     "text_premeasure::presenter: revealWhenTextExact::a measured batch allocates ONE GetTextBoundsParams, not one per word (PLAT-20)",
     "text_premeasure::presenter: revealWhenTextExact::settles on the FLIP, not on the three-second floor",
     "text_premeasure::presenter: revealWhenTextExact::a move is CONFIRMED before it settles: a second move re-arms the gate",
     "text_premeasure::presenter: revealWhenTextExact::a session that never moves still settles on the floor, not the deadline",
     "text_premeasure::presenter: revealWhenTextExact::an engine that never answers settles at once rather than holding for the deadline",
     "text_premeasure::presenter: revealWhenTextExact::reveals immediately on an adapter with no premeasurement seam",
     "text_audit::LTN-4 text facts: the solve reports what it truncated::a wrapped label capped by lineLimit reports truncated with its natural line count",
     "text_audit::LTN-4 text facts: the solve reports what it truncated::a single-line label clamped narrower than its string reports truncated (the identity case)",
     "text_audit::LTN-4 text facts: the solve reports what it truncated::a label that fits reports policy wrap and truncated false",
     "text_audit::LTN-4 text facts: the solve reports what it truncated::the declared disclose permission rides the same channel as the truncation verdict",
     "text_audit::LTN-4 text facts: the solve reports what it truncated::the layout dump is facet-dump/2 and carries textFacts per text node",
     "text_audit::LTN-4 text facts: the solve reports what it truncated::UI.Text refuses a bound disclose, naming the prop and the rebuild idiom",
     "text_audit::LTN-4 text facts: the solve reports what it truncated::UI.Text refuses a non-boolean disclose",
     "text_audit::LTN-4 audit: overlaps::overlapping painted siblings are a finding naming both nodes and the fix",
     "text_audit::LTN-4 audit: overlaps::...and the finding is gone once the parent declares intentionalOverlap",
     "text_audit::LTN-4 audit: overlaps::...or once the caller declares the pair in opts.declaredOverlays",
     "text_audit::LTN-4 audit: overlaps::a ZStack's children never fire the overlap check: layering is the job",
     "text_audit::LTN-4 audit: overlaps::a hidden subtree is audited by nobody: the losing candidate's rects are skipped",
     "text_audit::LTN-4 audit: overlaps::a one-pixel seam is not an overlap at the default epsilon",
     "text_audit::LTN-4 audit: overlaps::text_audit.overlaps refuses an unknown opts key",
     "text_audit::LTN-4 audit: clippedEssential::truncated text with no disclose and no allowlist entry is a finding",
     "text_audit::LTN-4 audit: clippedEssential::...gone the moment the node declares disclose",
     "text_audit::LTN-4 audit: clippedEssential::...gone when the caller declares it non-essential",
     "text_audit::LTN-4 audit: clippedEssential::a real solve of a truncated undisclosed label produces exactly one clippedEssential finding",
     "text_audit::LTN-4 audit: hitFloor::an interactive node painted under its declared floor is a finding",
     "text_audit::LTN-4 audit: hitFloor::...gone once the box reflows back to the floor",
     "text_audit::LTN-4 audit: hitFloor::a decorative rect the same size is never a hitFloor finding",
     "text_audit::LTN-4 audit: hitFloor::an explicit floorPx audits every interactive node against one number",
     "text_audit::LTN-4 audit: movingText::a second moving text surface is a finding at the default maximum of one",
     "text_audit::LTN-4 audit: movingText::one moving surface (or none) is not",
     "text_audit::LTN-4 audit: focusVisibility::a focused rect fully outside its clipping viewport is a finding",
     "text_audit::LTN-4 audit: focusVisibility::...gone once the viewport scrolls it back into view",
     "text_audit::LTN-4 audit: focusVisibility::a viewport that does not contain the focused path is never consulted",
     "text_audit::LTN-4 audit: focusVisibility::nothing focused is nothing to report",
     "text_audit::LTN-4 audit: findings are ordered and frozen::two runs over equal input produce equal, deterministically ordered findings",
     "text_audit::LTN-4 audit: outOfBounds (the declared-bounds check the matrix build found missing)::a painted node past the declared bounds is a finding naming the node and the overshoot",
     "text_audit::LTN-4 audit: outOfBounds (the declared-bounds check the matrix build found missing)::...and is gone once a declared scroll host owns the escaping subtree",
     "text_audit::LTN-4 audit: outOfBounds (the declared-bounds check the matrix build found missing)::a one-pixel overshoot is antialiasing slop, not a finding",
     "text_audit::LTN-4 audit: outOfBounds (the declared-bounds check the matrix build found missing)::a hidden losing candidate escapes nobody's bounds",
     "text_audit::LTN-4 audit: outOfBounds (the declared-bounds check the matrix build found missing)::two runs over equal input produce equal, deterministically ordered out-of-bounds findings",
     "text_audit::LTN-4 audit: outOfBounds (the declared-bounds check the matrix build found missing)::text_audit.outOfBounds refuses an unknown opts key",
     "text_disclosure::LTN-4 disclosure: engagement by input class::focus entering the containing focusable presents the static full-value plate",
     "text_disclosure::LTN-4 disclosure: engagement by input class::pointer hover presents the plate only after the dwell elapses",
     "text_disclosure::LTN-4 disclosure: engagement by input class::touch long-press presents the plate immediately",
     "text_disclosure::LTN-4 disclosure: engagement by input class::the plate appears identically under reduced motion (nothing about it moves)",
     "text_disclosure::LTN-4 disclosure: engagement by input class::a plate never appears for text that is not truncated",
     "text_disclosure::LTN-4 disclosure: engagement by input class::a plate never appears for truncated text that did not declare disclose",
     "text_disclosure::LTN-4 disclosure: engagement by input class::ticking with nothing engaged never presents a plate",
     "text_disclosure::LTN-4 disclosure: at most one plate, and it leaves when engagement does::hovering a second disclosed label replaces the first plate rather than stacking",
     "text_disclosure::LTN-4 disclosure: at most one plate, and it leaves when engagement does::a refresh under a live pointer plate never swaps it for the focused row's",
     "text_disclosure::LTN-4 disclosure: at most one plate, and it leaves when engagement does::the same label engaged twice keeps one plate rather than remounting it",
     "text_disclosure::LTN-4 disclosure: at most one plate, and it leaves when engagement does::pointer leave removes the plate",
     "text_disclosure::LTN-4 disclosure: at most one plate, and it leaves when engagement does::long-press release removes the plate",
     "text_disclosure::LTN-4 disclosure: at most one plate, and it leaves when engagement does::focus moving off the label removes the plate",
     "text_disclosure::LTN-4 disclosure: at most one plate, and it leaves when engagement does::a tap anywhere removes the plate",
     "text_disclosure::LTN-4 disclosure: at most one plate, and it leaves when engagement does::dismissing the surface removes the plate",
     "text_disclosure::LTN-4 disclosure: at most one plate, and it leaves when engagement does::unmounting the source node removes the plate on the next refresh",
     "text_disclosure::LTN-4 disclosure: at most one plate, and it leaves when engagement does::a re-solve that makes room for the label removes the plate",
     "text_disclosure::LTN-4 disclosure: the plate is presentation chrome, never a surface::the plate is outside every focus order and mounts nothing focusable",
     "text_disclosure::LTN-4 disclosure: the plate is presentation chrome, never a surface::the plate paints above its owner and wears the theme's raised surface",
     "text_disclosure::LTN-4 disclosure: the plate is presentation chrome, never a surface::the plate is clamped into the safe viewport",
     "text_disclosure::LTN-4 disclosure: the plate is presentation chrome, never a surface::the clamp FOLLOWS a live plate — geometry that changes after mount re-places it",
     "text_disclosure::LTN-4 disclosure: the live engagement wiring (renderer -> adapter zones)::a disclose Text gets a zone; ordinary text and controls get none",
     "text_disclosure::LTN-4 disclosure: the live engagement wiring (renderer -> adapter zones)::a pointer world wires hover; a pure-touch world wires long-press instead",
     "text_disclosure::LTN-4 disclosure: the live engagement wiring (renderer -> adapter zones)::the zone's events drive the SAME plates the seams do, end to end",
     "text_disclosure::LT-G1: a Toggle's one-line label discloses its full value::a truncated disclosing Toggle presents its LABEL through the same plate a Text does",
     "text_disclosure::LT-G1: a Toggle's one-line label discloses its full value::an UNdisclosed Toggle stays plate-less — the declaration is the permission",
     "text_disclosure::fresh-review closures: the plate answers for the label as it IS::a HIDDEN ViewThatFits candidate discloses nothing — its plate would show text that is not on screen",
     "text_disclosure::fresh-review closures: the plate answers for the label as it IS::the pointer dwell RE-ASKS at expiry: a re-solve that made room presents nothing",
     "text_disclosure::fresh-review closures: the plate answers for the label as it IS::...and a value that changed during the dwell discloses the CURRENT string, not the snapshot",
     "text_disclosure::fresh-review closures: the plate answers for the label as it IS::a touch class arriving MID-SESSION retrofits the long-press half onto mounted labels",
     "anchored_surface::D1 placement solver: the preferred edge, and the gap off the source::places the surface on the preferred edge, gapped off the source",
     "anchored_surface::D1 placement solver: the preferred edge, and the gap off the source::alignment start, center and end place the surface along the edge",
     "anchored_surface::D1 placement solver: the preferred edge, and the gap off the source::leading and trailing resolve to left and right",
     "anchored_surface::D1 placement solver: the preferred edge, and the gap off the source::refuses an unknown edge, alignment or overflow policy",
     "anchored_surface::D1 placement solver: the flip, and the rule that it never lands somewhere worse::flips to the opposite edge when the preferred one crosses the safe inset",
     "anchored_surface::D1 placement solver: the flip, and the rule that it never lands somewhere worse::never flips into a worse place — no room either way keeps the preferred edge",
     "anchored_surface::D1 placement solver: the flip, and the rule that it never lands somewhere worse::overflow 'clamp' pulls a surface that fits nowhere back inside the safe box",
     "anchored_surface::D1 placement solver: the along-edge shift::shifts along the edge to stay inside the safe box, and reports how far",
     "anchored_surface::D1 placement solver: the along-edge shift::a surface TALLER than the safe box pins to the top inset rather than hanging off the bottom",
     "anchored_surface::D1 placement solver: the along-edge shift::a surface wider than the safe box pins to the leading inset rather than centring off-screen",
     "anchored_surface::D1 placement solver: the arrow tail::the tail centres on the source and rides the shared border",
     "anchored_surface::D1 placement solver: the arrow tail::the tail keeps clear of the surface's own corners",
     "anchored_surface::D1 placement solver: the arrow tail::a shift that carries the tail off the source SUPPRESSES it",
     "anchored_surface::D1 placement solver: the arrow tail::a surface the overflow clamp pulled OFF its edge suppresses the tail too",
     "anchored_surface::D1 placement solver: the arrow tail::a surface too narrow to host the tail inside its corners suppresses it",
     "anchored_surface::D1 placement solver: the arrow tail::a tail on a side edge rides the vertical border and centres on the source's own middle",
     "anchored_surface::D1 placement solver: the arrow tail::no tail requested means no tail decided",
     "anchored_surface::D1 anchored surface: presented against a source view's screen rect::presentAnchored places the panel against the source path's screen rect",
     "anchored_surface::D1 anchored surface: presented against a source view's screen rect::an anchored surface joins the presenter's own stack rather than forking one",
     "anchored_surface::D1 anchored surface: presented against a source view's screen rect::a modal anchored surface traps focus through the presenter's own modal path",
     "anchored_surface::D1 anchored surface: presented against a source view's screen rect::a moving source is followed on the existing refresh cadence",
     "anchored_surface::D1 anchored surface: presented against a source view's screen rect::a source that is not mounted leaves the surface where it is instead of erroring",
     "anchored_surface::D1 anchored surface: presented against a source view's screen rect::a raw source rect needs no mounted view at all",
     "anchored_surface::D1 anchored surface: presented against a source view's screen rect::an unknown anchor key is an authoring error, not a silent no-op",
     "anchored_surface::D1 anchored surface: presented against a source view's screen rect::an anchored surface refuses a root policy its window-space offsets cannot mean",
     "anchored_surface::D1 anchored surface: presented against a source view's screen rect::the dump is deterministic and names the resolved edge",
     "anchored_surface::D1 anchored surface: the arrow tail is a decoration, never a claim::the tail paints behind the panel it belongs to",
     "anchored_surface::D1 anchored surface: the arrow tail is a decoration, never a claim::the wedge carries a ZERO corner radius, so the panel's rounding cannot reach it",
     "anchored_surface::D1 anchored surface: the arrow tail is a decoration, never a claim::the seam band paints OVER the panel's border, spanning exactly the wedge's base",
     "anchored_surface::D1 anchored surface: the arrow tail is a decoration, never a claim::the seam band wears the panel's fill and neither a corner nor a border of its own",
     "anchored_surface::D1 anchored surface: the arrow tail is a decoration, never a claim::SEAM AUTHORITY: the band's tint role and the panel's own rule name the same colour, in every package",
     "anchored_surface::D1 anchored surface: the arrow tail is a decoration, never a claim::a suppressed tail takes its seam band with it",
     "anchored_surface::D1 anchored surface: the arrow tail is a decoration, never a claim::no tail requested means no seam band either",
     "anchored_surface::D1 anchored surface: the arrow tail is a decoration, never a claim::a safe-area flip re-seats the seam band on the plate's other border",
     "anchored_surface::D1 anchored surface: the arrow tail is a decoration, never a claim::a side-edge plate turns the seam band through ninety degrees",
     "anchored_surface::D1 anchored surface: the arrow tail is a decoration, never a claim::the tail is suppressed when the shift carries it off the source",
     "anchored_surface::D1 anchored surface: the arrow tail is a decoration, never a claim::the tail is a WEDGE under every package: no chrome slot on a rotated node",
     "anchored_surface::D1 anchored surface: the arrow tail is a decoration, never a claim::EVERY rotated node in src declares NO_SLOT — the rule, not just this tail",
     "anchored_surface::D1 anchored surface: the arrow tail is a decoration, never a claim::asking for a tail moves the plate, and changes NOTHING inside it",
     "anchored_surface::D1 anchored surface: the tap-away catcher is the presenter's own::an anchored panel that declares outsideDismiss gets the presenter's catcher, non-consuming",
     "anchored_surface::D1 fixture f1: the avatar-editor callout::f1: anchored under a top-right button, arrow up at it, body shifted left",
     "anchored_surface::D1: the hand-rolled precedents now share one solver::the disclosure plate places through the shared solver and never flips into a worse place",
     "anchored_surface::D1: the hand-rolled precedents now share one solver::row actions' menu flip is the shared solver's rule, not a second copy",
     "anchored_surface::framework-gaps-phase2 gap 39: Facet.layout.anchorPlacement is published::is reachable on the public library table, and is the SAME function as anchor_placement.solve",
     "anchored_surface::framework-gaps-phase2 gap 39: Facet.layout.anchorPlacement is published::lets a consumer DECLARE a staging spot instead of hand-computing one — RascalRally's own shape",
     "text_reveal::reveal: the declaration and its solve facts::`reveal = 'auto'` is construction-only and enum-checked",
     "text_reveal::reveal: the declaration and its solve facts::a truncated reveal text publishes reveal facts: policy, flag, natural width",
     "text_reveal::reveal: the declaration and its solve facts::clippedEssential accepts a reveal path as full-value access",
     "text_reveal::reveal: the auto cycle::rests in the engine ellipsis for the quiet delay: nothing moves at present",
     "text_reveal::reveal: the auto cycle::after the delay the strip mounts, the source paint yields, and travel advances left",
     "text_reveal::reveal: the auto cycle::pauses at the end with the tail fully shown, returns, and rests again as the ellipsis",
     "text_reveal::reveal: the auto cycle::never runs for text that fits, or for truncated text that did not declare it",
     "text_reveal::reveal: the auto cycle::a re-solve that makes room retires the reveal cleanly",
     "text_reveal::reveal: the auto cycle::a source that goes HIDDEN mid-flight retires the strip (found live, 2026-08-04)",
     "text_reveal::reveal: the auto cycle::dismissing the owning surface retires the reveal with it",
     "text_reveal::reveal: the constraint rails::reduced motion disables travel entirely — and the static plate remains reachable",
     "text_reveal::reveal: the constraint rails::a reduced-motion flip mid-flight retires the strip on the next tick",
     "text_reveal::reveal: the constraint rails::at most ONE strip runs: the first declared target in document order wins",
     "text_reveal::reveal: the constraint rails::a live disclosure plate for the same label outranks the strip: the reveal rests",
     "preferred_text_seam::preferred_text offset seam (LT-1/LT-3/LT-4)::applies the measured fallback synchronously — first mount never waits",
     "preferred_text_seam::preferred_text offset seam (LT-1/LT-3/LT-4)::a confirmed answer equal to the fallback applies nothing more (no second reflow)",
     "preferred_text_seam::preferred_text offset seam (LT-1/LT-3/LT-4)::a confirmed answer that differs applies exactly one atomic correction",
     "preferred_text_seam::preferred_text offset seam (LT-1/LT-3/LT-4)::an answer that raced a preference step is DISCARDED — the A->B->A double-step cannot poison the cache",
     "preferred_text_seam::preferred_text offset seam (LT-1/LT-3/LT-4)::an answer resolved with NO step in flight is cached: the revisit asks nothing",
     "preferred_text_seam::preferred_text offset seam (LT-1/LT-3/LT-4)::a FAILED lookup keeps the fallback and does not retry in a loop",
     "preferred_text_seam::preferred_text offset seam (LT-1/LT-3/LT-4)::garbage answers are refused: NaN and negative offsets keep the fallback",
     "preferred_text_seam::preferred_text offset seam (LT-1/LT-3/LT-4)::an answer past the ceiling is CLAMPED and applied, never traded for a smaller fallback",
     "preferred_text_seam::preferred_text offset seam (LT-1/LT-3/LT-4)::an unknown future enum value starts from the LARGEST known fallback (over-reserve, never clip)",
     "preferred_text_seam::preferred_text offset seam (LT-1/LT-3/LT-4)::dispose drops late answers on the floor",
     "preferred_text_seam::live preference change reflows without remount (LT-2)::raising the offset re-solves in place: same node, taller text box, no creates/removes",
     "preferred_text_seam::live preference change reflows without remount (LT-2)::lowering it back restores the original geometry exactly (no residue)",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Text: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Text-identity-disclosed: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Button-plain: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Button-circle: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Button-compactLabel: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Toggle: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL TextField: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Grip: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Slider: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Stepper: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Chip: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL RowActions: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL PopupButton: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Menu-panel: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Callout-plate: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Rating: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL LevelPicker: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Picker: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Picker-indicator: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL TabView: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL DisclosureGroup: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL ProgressView: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Label: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL TextInput: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Table: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL VirtualList: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL VirtualGrid: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL AsyncImage: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL PathShapes-ring: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Path: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Image: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Foreign: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Box: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Spacer: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Divider: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL VStack: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL HStack: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL ZStack: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Anchor: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Grid: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL GridRow: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL ScrollView: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL AdaptiveStack: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL ViewThatFits: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Composition: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Region: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL When: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL ForEach: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL ErrorBoundary: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "large_text_matrix::LT-8 control matrix: the sweep's own coverage and mirrors::LT8-COVER every controls_registry row is swept or carries a declared reason",
     "large_text_matrix::LT-8 control matrix: the sweep's own coverage and mirrors::LT8-ALIVE every declared clip allowlist entry is a REAL truncation at Largest",
     "large_text_matrix::LT-8 control matrix: the sweep's own coverage and mirrors::LT8-MIRROR the audit's target-floor mirror equals the renderer's own hit expander",
     "large_text_matrix::LT-6 policy: reflow before truncation on the controls that can reflow::LT6-ACTION a Button's action label wraps and grows WHILE wrapping can succeed, and never breaks a word",
     "large_text_matrix::LT-6 policy: reflow before truncation on the controls that can reflow::LT6-COMPACT a bounded Button draws its declared compact form rather than cutting the full label",
     "large_text_matrix::LT-6 policy: reflow before truncation on the controls that can reflow::LT6-FIXEDPX an authored px width that fits the compact form at Medium clips it at Larger (pinned)",
     "large_text_matrix::LT-6 policy: reflow before truncation on the controls that can reflow::LT6-DISCLOSE an unbroken identity name truncates WITH a full-value path and files no finding",
     "large_text_matrix::LT-8 mixed scripts: CJK, emoji and latin at every preference::LT8-SCRIPTS a mixed CJK + emoji + latin line files no overlap or clip finding at any preference",
     "large_text_matrix::LT-8 mixed scripts: CJK, emoji and latin at every preference::LT8-MONOTONE every mixed-script line's box grows monotonically with the preference",
     "large_text_matrix::LT-8 mixed scripts: CJK, emoji and latin at every preference::LT8-GROWTH the Largest preference reserves a strictly taller box than Medium for the same string",
     "large_text_layout::LT-6 order: a form in a ScrollView reflows, then scrolls, and never truncates::LT6-FORM the form's action pair reflows and the region scrolls — no clip at any of the 32 rows",
     "large_text_layout::LT-6 order: a form in a ScrollView reflows, then scrolls, and never truncates::LT6-FORMGROW the form's scrollable content grows with the preference while its viewport does not",
     "large_text_layout::LT-6 order: a ViewThatFits CTA band steps down before anything is cut::LT6-BAND the chosen candidate never gets richer as the preference rises, and no action is ever cut",
     "large_text_layout::LT-6 order: a ViewThatFits CTA band steps down before anything is cut::LT6-BANDWITNESS the compact-portrait band is a row at Medium and a column at Largest (named witness)",
     "large_text_layout::LT-6 order: a ViewThatFits CTA band steps down before anything is cut::LT6-LADDER nothing is truncated while the band still has a cheaper candidate to take",
     "large_text_layout::LT-6 order: a ViewThatFits CTA band steps down before anything is cut::LT6-LADDERNEG the ladder rule FIRES on a band that cuts a label with a candidate still in hand",
     "large_text_layout::LT-6 order: an AdaptiveStack split holds at every preference::LT6-SPLIT the split's axis is space-driven and its panes never overlap or cut essential text",
     "large_text_layout::LT-6 order: an AdaptiveStack split holds at every preference::LT6-SPLITSTABLE the split's axis is identical at Medium and Largest on both viewports",
     "large_text_layout::LT-6 order: a Composition steps its arrangement down, never its CTA::LT6-COMPOSE the composition never picks a richer arrangement as the text grows, and cuts no action",
     "large_text_layout::LT-6 order: a Composition steps its arrangement down, never its CTA::LT6-COMPOSEWITNESS the small-landscape composition is threeLane at Medium and steps down at Largest",
     "large_text_layout::LT-6 order: a Composition steps its arrangement down, never its CTA::LT6-COMPOSESCROLL the field region keeps its declared scroll path at every preference",
     "large_text_layout::LT-8 reduced motion: the same geometry, and a policy independent of the preference::LT8-REDUCED every layout row solves byte-identically with reducedMotion set",
     "large_text_layout::LT-8 reduced motion: the same geometry, and a policy independent of the preference::LT8-REDUCEDPOLICY motionPolicy answers from the motion fact alone, at every preference",
     "large_text_layout::LT-8 reduced motion: the same geometry, and a policy independent of the preference::LT8-REDUCEDSWEEP the reduced-motion form and band file no finding at any preference",
     "large_text_hot_swap::LT-2 hot swap: a live screen survives the preference swing 0 -> 14 -> 0::LTSWING-IDENTITY the swing writes rects and props only — not one create or remove",
     "large_text_hot_swap::LT-2 hot swap: a live screen survives the preference swing 0 -> 14 -> 0::LTSWING-FOCUS the focused path is the same node before, during and after the swing",
     "large_text_hot_swap::LT-2 hot swap: a live screen survives the preference swing 0 -> 14 -> 0::LTSWING-EDIT the field keeps its typed, uncommitted text and stays in edit mode",
     "large_text_hot_swap::LT-2 hot swap: a live screen survives the preference swing 0 -> 14 -> 0::LTSWING-SCROLL the list keeps its scroll offset, or is legally clamped to its new extent",
     "large_text_hot_swap::LT-2 hot swap: a live screen survives the preference swing 0 -> 14 -> 0::LTSWING-GEOMETRY every rect returns byte-identical when the preference comes back to Medium",
     "large_text_hot_swap::LT-2 hot swap: a live screen survives the preference swing 0 -> 14 -> 0::LTSWING-CLEAN the busy screen files no overlap or undisclosed-clip finding at either end",
     "large_text_hot_swap::LT-5 composition: a theme swap and the preference compose on a live screen::LTSWAP-THEME installing Fantasy Parchment at Largest re-solves in place with no finding",
     "large_text_hot_swap::LT-5 composition: a theme swap and the preference compose on a live screen::LTSWAP-ORDER the same pair applied in the other order lands on identical geometry",
     "large_text_hot_swap::LT-5 composition: a theme swap and the preference compose on a live screen::LTSWAP-BORN a screen BORN at Largest under Parchment matches one swung there live",
     "drag_session::drag_session (corrections §1)::update before any target hit returns nil and starts active with no hover",
     "drag_session::drag_session (corrections §1)::fires enter/leave exactly once per hover change",
     "drag_session::drag_session (corrections §1)::predicts the result on each hover change, including to no target",
     "drag_session::drag_session (corrections §1)::accepts-filter makes a target illegal: no hover, no enter",
     "drag_session::drag_session (corrections §1)::overlapping targets: the LAST in the array wins (paint z-order)",
     "drag_session::drag_session (corrections §1)::drop over a legal target returns dropped with payload; then inert",
     "drag_session::drag_session (corrections §1)::drop over no legal target rejects",
     "drag_session::drag_session (corrections §1)::cancel mid-hover fires leave for the hovered target, then goes inert",
     "drag_session::drag_session (corrections §1)::retarget re-evaluates hover at the last position: moved-away target leaves, new one enters",
     "drag_session::drag_session (corrections §1)::retarget before any position is a no-op (no phantom hover)",
     "drag_session::drag_session (corrections §1)::retarget after a terminal state stays inert",
     "touch_gestures::touch_gestures.normalize (corrections §2)::normalizes a tap: instantaneous, positions extracted, state 'none'",
     "touch_gestures::touch_gestures.normalize (corrections §2)::normalizes a longPress and maps UserInputState names to lifecycle states",
     "touch_gestures::touch_gestures.normalize (corrections §2)::accepts Enum-like { Name = ... } state values",
     "touch_gestures::touch_gestures.normalize (corrections §2)::normalizes a pan: Vector2 velocity + totalTranslation + state",
     "touch_gestures::touch_gestures.normalize (corrections §2)::normalizes a pinch: scalar velocity + scale",
     "touch_gestures::touch_gestures.normalize (corrections §2)::normalizes a rotate: rotation + scalar velocity",
     "touch_gestures::touch_gestures.normalize (corrections §2)::normalizes a swipe: direction name, no positions, state 'none'",
     "touch_gestures::touch_gestures.normalize (corrections §2)::tolerates missing optional fields without erroring",
     "touch_gestures::touch_gestures.normalize (corrections §2)::accepts short kind names too",
     "touch_gestures::touch_gestures.normalize (corrections §2)::reads ENGINE Vector2 userdata, not only {X=,Y=} tables (SEAM-3)",
     "touch_gestures::touch_gestures.normalize (corrections §2)::reads a real EnumItem's Name (userdata state / swipe direction)",
     "touch_gestures::touch_gestures.normalize (corrections §2)::a value object with no numeric X/Y is still not a position (no invention)",
     "touch_gestures::the live ScreenTarget calls normalize POSITIONALLY (SEAM-3)::passes the engine's argument list in order, for all six gesture kinds",
     "touch_gestures::the live ScreenTarget calls normalize POSITIONALLY (SEAM-3)::passes no NAMED-key table (a[1]..a[4] nil = every gesture normalizes empty)",
     "touch_gestures::touch_gestures.newArbiter (corrections §2)::refuses a non-empty opts table: options are RESERVED, none exist yet (SEAM-4)",
     "touch_gestures::touch_gestures.newArbiter (corrections §2)::nil and an EMPTY opts table stay legal (the reserved shape is the signature)",
     "touch_gestures::touch_gestures.newArbiter (corrections §2)::pinch preempts pan: two-finger intent takes ownership",
     "touch_gestures::touch_gestures.newArbiter (corrections §2)::rotate also preempts pan",
     "touch_gestures::touch_gestures.newArbiter (corrections §2)::a began longPress preempts tap",
     "touch_gestures::touch_gestures.newArbiter (corrections §2)::swipe and tap are instantaneous and never own on their own",
     "touch_gestures::touch_gestures.newArbiter (corrections §2)::ownership releases on the owning gesture's 'ended' frame",
     "input::input: engine-mirrored arbitration (spike parity)::T1: a higher-priority Sink context blocks the lower context on the same key",
     "input::input: engine-mirrored arbitration (spike parity)::T2: disabling the high context releases the key to the lower one",
     "input::input: engine-mirrored arbitration (spike parity)::T3: a scriptable fire bypasses arbitration even under an active sink",
     "input::input: engine-mirrored arbitration (spike parity)::T4/dedup: repeated identical states are deduplicated like the engine",
     "input::input: engine-mirrored arbitration (spike parity)::T6a: firing a binding of a destroyed context is a silent no-op",
     "input::input: engine-mirrored arbitration (spike parity)::disabling a context resets held Bool state",
     "input::input: hints::preferredBinding follows the preferred input kind and tolerates nil",
     "input::input: focus wiring::Navigate2D/Activate drive the logical focus graph through actions only",
     "input::input: modifier state (Phase B)::deviceKey tracks shift and ctrl/cmd modifier state",
     "input::input: modifier-gated bindings (Task 8b)::a modified binding fires only while its modifier is held",
     "input::input: modifier-gated bindings (Task 8b)::an unmodified sibling is unaffected when the modifier is NOT held (byte-identical)",
     "input::input: modifier-gated bindings (Task 8b)::a modified binding preempts an unmodified sibling on the same key when held (T1 arbitration, not a new rule)",
     "input::input: modifier-gated bindings (Task 8b)::plain Return keeps working normally after a shifted press (no stuck preemption)",
     "input::input: modifier-gated bindings (Task 8b)::gamepad bindings ignore modifier slots entirely: an unmodified ButtonX binding is unaffected by held shift",
     "input::input: modifier-gated bindings (Task 8b)::MAJOR-1 repro A1: tapping shift WHILE plain Return is held does not steal that Return's own key-up",
     "input::input: modifier-gated bindings (Task 8b)::MAJOR-1 repro A2: holding an unmodified sibling (ButtonX) on the SAME action does not steal a later plain Return's key-up",
     "input::input: modifier-gated bindings (Task 8b)::a chord's key-up reaches its binding even if the modifier released first (no stuck Bool state)",
     "input::input: modifier-gated bindings (Task 8b)::resetModifiers clears tracked held-modifier state (the documented stuck-modifier escape hatch)",
     "presenter::presenter: screens and modals::presents a screen with document-order focus and device navigation",
     "presenter::presenter: screens and modals::a modal sinks navigation from the base screen and traps focus",
     "presenter::presenter: screens and modals::Cancel dismisses the modal, restores focus, and disposes its context",
     "presenter::presenter: screens and modals::activate routes to the focused control of the top scope",
     "presenter::presenter: screens and modals::open/close churn is registry-neutral across the whole slice",
     "presenter::presenter: re-entrant dismiss (phase-2 verifier F-2)::dismissing a modal from inside its own onActivate is safe",
     "presenter::presenter: unknown string-enum opts fail at the call site (F-15)::an unknown responder names the opt and its legal set",
     "presenter::presenter: unknown string-enum opts fail at the call site (F-15)::an unknown scrim names the opt and its legal set",
     "presenter::presenter: unknown string-enum opts fail at the call site (F-15)::the legal values still present: responder passive, scrim scrim/none",
     "presenter::presenter: presentCritical carries the caller's opts to the fallback (F-16)::a critical fallback under rootPolicy=edgeToEdge resolves the edge-to-edge rect",
     "presenter::presenter: presentCritical carries the caller's opts to the fallback (F-16)::without the opt the fallback keeps the default content inset",
     "presenter::presenter: presentCritical carries the caller's opts to the fallback (F-16)::the fallback screen is not asked to present itself as its own fallback",
     "presenter::presenter: raise re-bands a live surface without dismiss+re-present::bumps a base screen into a fresh band slot, in place",
     "presenter::presenter: raise re-bands a live surface without dismiss+re-present::raises within the MODAL band, never the base band",
     "presenter::presenter: raise re-bands a live surface without dismiss+re-present::re-homes an owned scrim's displayOrder along with the surface it belongs to",
     "presenter::presenter: raise re-bands a live surface without dismiss+re-present::REFUSES a handle already dismissed — silently, matching `dismiss`'s own shape",
     "presenter::presenter: raise re-bands a live surface without dismiss+re-present::REFUSES nil without erroring — a defensive caller must not be punished",
     "presenter::presenter: raise re-bands a live surface without dismiss+re-present::never remounts: zero instance churn and focus never moves",
     "nav_bar::Facet.navBar: shape::defaults to id 'NavBar', gap 's', full width, and a bare Spacer with nothing declared",
     "nav_bar::Facet.navBar: shape::takes the caller's own id, gap and padding",
     "nav_bar::Facet.navBar: Back — refuse, don't guess::onBack = nil renders no Back button at all",
     "nav_bar::Facet.navBar: Back — refuse, don't guess::onBack given draws the framework's own chevron icon, never a hand-typed glyph",
     "nav_bar::Facet.navBar: Back — refuse, don't guess::Back is always first when present",
     "nav_bar::Facet.navBar: Title — bounded identity text, or none::title = nil renders no Title node (the site with none)",
     "nav_bar::Facet.navBar: Title — bounded identity text, or none::title truncates into its disclosure by default (\"title\" size)",
     "nav_bar::Facet.navBar: Title — bounded identity text, or none::titleSize and titleWidth pass through verbatim (the sipworks book site's own dim)",
     "nav_bar::Facet.navBar: trailing actions and the spacer::a Spacer always separates the leading segment from trailing content",
     "nav_bar::Facet.navBar: trailing actions and the spacer::trailing nodes are appended after the spacer, in order, unmodified",
     "nav_bar::Facet.navBar: trailing actions and the spacer::an empty trailing table behaves exactly like omitting it",
     "nav_bar::Facet.navBar: closed-key spec (constitution §4)::refuses an unknown key rather than silently ignoring it",
     "controls_conformance::control conformance kit::AdaptiveStack: contract + mount/dispose neutrality + deterministic dump",
     "controls_conformance::control conformance kit::Anchor: contract + mount/dispose neutrality + deterministic dump",
     "controls_conformance::control conformance kit::Box: contract + mount/dispose neutrality + deterministic dump",
     "controls_conformance::control conformance kit::Button: contract + mount/dispose neutrality + deterministic dump",
     "controls_conformance::control conformance kit::Composition: contract + mount/dispose neutrality + deterministic dump",
     "controls_conformance::control conformance kit::Divider: contract + mount/dispose neutrality + deterministic dump",
     "controls_conformance::control conformance kit::ErrorBoundary: contract + mount/dispose neutrality + deterministic dump",
     "controls_conformance::control conformance kit::ForEach: contract + mount/dispose neutrality + deterministic dump",
     "controls_conformance::control conformance kit::Foreign: contract + mount/dispose neutrality + deterministic dump",
     "controls_conformance::control conformance kit::Grid: contract + mount/dispose neutrality + deterministic dump",
     "controls_conformance::control conformance kit::GridRow: contract + mount/dispose neutrality + deterministic dump",
     "controls_conformance::control conformance kit::Grip: contract + mount/dispose neutrality + deterministic dump",
     "controls_conformance::control conformance kit::HStack: contract + mount/dispose neutrality + deterministic dump",
     "controls_conformance::control conformance kit::Image: contract + mount/dispose neutrality + deterministic dump",
     "controls_conformance::control conformance kit::Path: contract + mount/dispose neutrality + deterministic dump",
     "controls_conformance::control conformance kit::Region: contract + mount/dispose neutrality + deterministic dump",
     "controls_conformance::control conformance kit::Screen: contract + mount/dispose neutrality + deterministic dump",
     "controls_conformance::control conformance kit::ScrollView: contract + mount/dispose neutrality + deterministic dump",
     "controls_conformance::control conformance kit::Spacer: contract + mount/dispose neutrality + deterministic dump",
     "controls_conformance::control conformance kit::Stage: contract + mount/dispose neutrality + deterministic dump",
     "controls_conformance::control conformance kit::Text: contract + mount/dispose neutrality + deterministic dump",
     "controls_conformance::control conformance kit::TextField: contract + mount/dispose neutrality + deterministic dump",
     "controls_conformance::control conformance kit::Toggle: contract + mount/dispose neutrality + deterministic dump",
     "controls_conformance::control conformance kit::VStack: contract + mount/dispose neutrality + deterministic dump",
     "controls_conformance::control conformance kit::ViewThatFits: contract + mount/dispose neutrality + deterministic dump",
     "controls_conformance::control conformance kit::When: contract + mount/dispose neutrality + deterministic dump",
     "controls_conformance::control conformance kit::ZStack: contract + mount/dispose neutrality + deterministic dump",
     "controls_conformance::control conformance kit::bindable props of interactive controls have binding authority declared",
     "hint_no_remount::hint changes without remount::preferredInput flip updates the hint text prop only; zero factory reruns",
     "hint_no_remount::inputHint: scope ownership and a validated style (F-33)::opts.scope owns the memo, so a screen cannot leak it",
     "hint_no_remount::inputHint: scope ownership and a validated style (F-33)::omitting the scope leaves the memo to the caller, exactly as before",
     "hint_no_remount::inputHint: scope ownership and a validated style (F-33)::an unknown style fails at the call site, naming the legal set",
     "hint_no_remount::inputHint: scope ownership and a validated style (F-33)::both legal styles still resolve",
     "icon_box::the icon's box: a picture is reserved for, not painted over::an icon-hinted node is never smaller than the picture drawn on it",
     "icon_box::the icon's box: a picture is reserved for, not painted over::...at whichever `iconSizes` rung the hint names",
     "icon_box::the icon's box: a picture is reserved for, not painted over::a package that draws NO art reserves nothing extra — flat geometry is untouched",
     "icon_box::the icon's box: a picture is reserved for, not painted over::a node with an EXPLICIT size keeps it — the author still wins",
     "icon_box::the icon's box: a picture is reserved for, not painted over::the snapshot publishes WHICH names resolve to art, derived not authored",
     "icon_box::the icon's box: the paint seam clamps to what it was given::a picture never draws wider than the node's content box",
     "icon_box::the icon's box: the paint seam clamps to what it was given::a node with room draws the theme's rung untouched",
     "icon_box::the icon's box: the paint seam clamps to what it was given::no rect yet means no constraint — the re-fit happens when it lands",
     "icon_box::the icon's box: the paint seam clamps to what it was given::never zero — a tight picture beats an invisible one",
     "icon_box::the icon's box: the paint seam clamps to what it was given::the clamp is applied where the picture is sized",
     "chrome_inset_yield::the yield rule is pure arithmetic::an inset the box can afford is spent in full",
     "chrome_inset_yield::the yield rule is pure arithmetic::an inset that would leave nothing is not spent at all",
     "chrome_inset_yield::the yield rule is pure arithmetic::the boundary is CONTENT SURVIVES, not content is non-negative",
     "chrome_inset_yield::the yield rule is pure arithmetic::authored padding alone can still collapse a box, and is left alone",
     "chrome_inset_yield::the yield rule is pure arithmetic::an unbounded axis affords everything",
     "chrome_inset_yield::a control narrower than its own chrome still draws::no package leaves a fixed-width Button with a zero content box",
     "chrome_inset_yield::a control narrower than its own chrome still draws::...and says so, so the author learns the control is too small",
     "chrome_inset_yield::a control narrower than its own chrome still draws::a squeezed label wraps against a real width, never a zero one",
     "chrome_inset_yield::a control narrower than its own chrome still draws::a ceiling spelled `minMax{max}` affords exactly what the same `fixed` px affords",
     "chrome_inset_yield::a control narrower than its own chrome still draws::...and an axis with no ceiling at all is still free to grow and pay for it",
     "chrome_inset_yield::a control narrower than its own chrome still draws::a Button with room pays its chrome exactly as before — nothing that fit moves",
     "chrome_inset_yield::the lifted label is never smaller than the text it stands in for::a lift box that would collapse falls back to the host's own content box",
     "chrome_inset_yield::the lifted label is never smaller than the text it stands in for::a lift box with room keeps the recipe's insets untouched",
     "chrome_inset_yield::the lifted label is never smaller than the text it stands in for::with no host box known, the recipe is honoured as before",
     "chrome_inset_yield::the lifted label is never smaller than the text it stands in for::...and the SAME host box gives two different boxes at two text sizes",
     "chrome_inset_yield::the lifted label is never smaller than the text it stands in for::the textSize WRITE re-fits the lifted label — the TextSize-only drift case",
     "slice_mutation::slice: UI-driven authoritative mutation::toggle + save + delayed server confirm reconciles cleanly",
     "slice_mutation::slice: UI-driven authoritative mutation::rejection restores the draft; no permanent pending state",
     "slice_mutation::slice: UI-driven authoritative mutation::one action carries keyboard, touch, and gamepad bindings simultaneously",
     "focus_structural::focus ring follows structure (F1)::a focusable inside When joins the ring on mount and leaves on unmount",
     "focus_structural::confirm-before-ingest convergence (F2)::an optimistic view reconciled on snapshot ingest converges regardless of delivery order",
     "table::Table: layout::columns align across header and every row (fill + fixed)",
     "table::Table: layout::columns stay aligned in edit mode: the ≡ gutter",
     "table::Table: layout::columns stay aligned in edit mode: the selection gutter",
     "table::Table: layout::columns stay aligned in edit mode: the ≡ gutter AND the row-actions ⊖",
     "table::Table: layout::disposing a table with a destructive rowActions entry frees the edit-gutter observer",
     "table::Table: layout::variable row heights position rows correctly",
     "table::Table: layout::headerless mode renders no header row (the racer-list shape)",
     "table::Table: selection::single: activate selects exactly one; selection survives without remount",
     "table::Table: selection::multi: activates toggle additively (tap idiom)",
     "table::Table: selection::keyboard: focus walks row hits and Activate selects",
     "table::Table: custom cells and resize model::custom cells host live nested controls (director: results list w/ custom cell)",
     "table::Table: custom cells and resize model::setColumnWidth commits a new width to every row and the header",
     "table::Table: custom cells and resize model::committed overrides RESOLVE narrower when the viewport cannot hold them (DIR-4)",
     "table::Table: custom cells and resize model::a `fill` column's declared minWidth is a floor, and pinning it leaves the pool (ADAPT-19)",
     "table::Table: custom cells and resize model::the lowest-priority column collapses first, one at a time, until it fits (ADAPT-18)",
     "table::Table: custom cells and resize model::a declared `priority` overrides declaration order, and `always` is a refusal (ADAPT-18)",
     "table::Table: custom cells and resize model::collapse re-evaluates in BOTH directions, and the model never moved (ADAPT-18)",
     "table::Table: custom cells and resize model::J-3: a WIDE ALL-FIXED table at compact collapses instead of clipping",
     "table::Table: custom cells and resize model::a sum that equals `room` FITS — the collapse test is not an exact float compare (R1-H2)",
     "table::Table: custom cells and resize model::a table whose columns declare no floor never collapses anything (ADAPT-18)",
     "table::Table: custom cells and resize model::a COMMITTED override is clamped first and only then counted (ADAPT-18 x DIR-4)",
     "table::Table: custom cells and resize model::the mounted table gives a fill column its floor rather than a weighted share (ADAPT-19)",
     "table::Table: custom cells and resize model::a non-resizable column is never clamped below its declared width (DIR-4)",
     "table::Table: custom cells and resize model::the mounted table honours the clamp at a narrow viewport, and restores it (DIR-4)",
     "table::Table: custom cells and resize model::the clamp reserves the row's own GAP, so the last column still fits (NEW-3)",
     "table::Table: custom cells and resize model::the pure rule takes the reservation as a number, and zero is the old behaviour",
     "table::Table: custom cells and resize model::remounted rows keep hits painted beneath cell content (deterministic z-order)",
     "table::Table: custom cells and resize model::removed rows are pruned from the selection and anchor",
     "table::Table: custom cells and resize model::table ids containing Lua pattern magic still route rows and grips",
     "table::Table: custom cells and resize model::row churn + dispose are registry-neutral and dumps deterministic",
     "table::Table: custom cells and resize model::the CONTROL dump is deterministic and carries its schema string",
     "table::Table: custom cells and resize model::the control dump carries the live interaction state a bug report needs",
     "table::Table: custom cells and resize model::the dump's selectedKeys are SORTED, so a multi-selection stays deterministic",
     "table::Table: column-owned columns (director round 5)::per-column alignment positions cell content; vertical center is the default",
     "table::Table: column-owned columns (director round 5)::the header title honours the column alignment, exactly like the column's cells",
     "table::Table: column-owned columns (director round 5)::a trailing title on a SORTABLE column clears the sort mark",
     "table::Table: column-owned columns (director round 5)::a LEADING title on a sortable column reserves the mark's room too",
     "table::Table: column-owned columns (director round 5)::a centered column centers its header title too",
     "table::Table: column-owned columns (director round 5)::columns own their cells: `value` renders a default Text, `cell` renders custom",
     "table::Table: column-owned columns (director round 5)::width sugar: a number is fixed px; {min, ideal, max} is a minMax band",
     "table::Table: column-owned columns (director round 5)::partial width sugar defaults the band's preferred ({min} alone is valid)",
     "table::Table: column-owned columns (director round 5)::a content-less column asserts AT BUILD, even with zero rows; ids must be path-safe",
     "table::Table: column-owned columns (director round 5)::value() results are coerced to text (a raw number never reaches the Text prop)",
     "table::Table: column-owned columns (director round 5)::header taps cycle the owner-held sortOrder; the table never sorts the data",
     "table::Table: row gaps (director round 4 — cards must not touch)::rowGap spaces the cards and the drop indicator lands mid-gap",
     "table::Table: row drag-and-drop (Phase B)::a drag shows ghost + indicator with the row IN PLACE; drop converts to post-removal slots",
     "table::Table: row drag-and-drop (Phase B)::dragging a selected row drags the WHOLE selection as a block (director example)",
     "table::Table: row drag-and-drop (Phase B)::a five-row group still shows at most three chips",
     "table::Table: row drag-and-drop (Phase B)::dragging an UNSELECTED row drags only that row",
     "table::Table: row drag-and-drop (Phase B)::a group drop that reproduces the current order commits nothing",
     "table::Table: row drag-and-drop (Phase B)::dropping a row back onto itself commits nothing",
     "table::Table: row drag-and-drop (Phase B)::a system interrupt (Roblox menu opened) aborts the drag cleanly",
     "table::Table: row drag-and-drop (Phase B)::an invalid drop (outside the table) commits nothing",
     "table::Table: row drag-and-drop (Phase B)::one-step downward drag reports the post-removal insertion slot",
     "table::Table: row drag-and-drop (Phase B)::keyboard: shift+Navigate moves the focused row without moving focus",
     "table::Table: row drag-and-drop (Phase B)::an impossible shift+Navigate reorder moves NOTHING — not the row, not focus",
     "table::Table: row drag-and-drop (Phase B)::dismissing the screen mid-drag tears the capture down",
     "table::Table: row drag-and-drop (Phase B)::non-reorderable tables ignore row drags entirely",
     "table::Table: standard-model focus and selection (director round 2)::clicking a row moves keyboard focus there",
     "table::Table: standard-model focus and selection (director round 2)::arrow navigation moves the SELECTION with focus (the standard table model)",
     "table::Table: standard-model focus and selection (director round 2)::arrow-selection anchors the range: arrow to a later row, shift-click an earlier one",
     "table::Table: standard-model focus and selection (director round 2)::shift-clicking from a later row to a prior one range-selects (reversed)",
     "table::Table: modifier selection semantics (Phase B)::plain mouse click replaces a multi selection",
     "table::Table: modifier selection semantics (Phase B)::ctrl/cmd-click toggles rows in and out of a multi selection",
     "table::Table: modifier selection semantics (Phase B)::shift-click extends a contiguous range from the anchor",
     "table::Table: modifier selection semantics (Phase B)::keyboard Activate honors held modifiers from the action system",
     "table::Table: modifier selection semantics (Phase B)::touch taps keep the additive-toggle idiom (no modifier keys on device)",
     "table::Table: column resize interaction (Phase B)::resizable columns render a cursor-hinted grip; fixed columns do not",
     "table::Table: column resize interaction (Phase B)::grip drag previews a guide line and commits the width on release",
     "table::Table: column resize interaction (Phase B)::focus cycles through the column handle and the rows, and wraps around",
     "table::Table: column resize interaction (Phase B)::cells inset content from the left and center it vertically; headers read at full contrast",
     "table::Table: column resize interaction (Phase B)::gamepad/keyboard: a focused column handle resizes its column via the Adjust action",
     "table::Table: column resize interaction (Phase B)::grip drag clamps to the column minWidth",
     "table::Table: capture ownership (playlist place defect 2026-07-20)::a second pointer-down on ANOTHER row mid-drag cannot steal the capture; the drop still commits",
     "table::Table: touch scroll vs edit-mode reorder (director design 2026-07-20)::a touch drag on the row body defers to the native scroll and never reorders",
     "table::Table: touch scroll vs edit-mode reorder (director design 2026-07-20)::the body is a native scroll region: it declares the full content canvas and clips",
     "table::Table: touch scroll vs edit-mode reorder (director design 2026-07-20)::rows sit at absolute canvas offsets (the engine translates by CanvasPosition)",
     "table::Table: touch scroll vs edit-mode reorder (director design 2026-07-20)::edit mode shows a LEADING ≡ glyph and insets the cells into a gutter",
     "table::Table: touch scroll vs edit-mode reorder (director design 2026-07-20)::the ≡ handle clears the row CARD, not just the row rect (skinned themes)",
     "table::Table: touch scroll vs edit-mode reorder (director design 2026-07-20)::engine double-dispatch: the row body DECLINES gutter touches so the handle wins either order",
     "table::Table: touch scroll vs edit-mode reorder (director design 2026-07-20)::mobile multi-reorder: tap-select rows, then a handle drag moves the WHOLE selection",
     "table::Table: touch scroll vs edit-mode reorder (director design 2026-07-20)::edit mode grows a trailing handle per row; a TOUCH drag on the handle reorders",
     "table::Table: touch scroll vs edit-mode reorder (director design 2026-07-20)::a touch drag on the row body while editing defers to native scroll (handle-only reorder)",
     "table::Table: touch scroll vs edit-mode reorder (director design 2026-07-20)::a MOUSE drag on the row body still reorders directly (desktop idiom)",
     "table::Table: touch scroll vs edit-mode reorder (director design 2026-07-20)::auto Edit/Done toggle: shown for touch input, hidden for mouse, toggles the handles",
     "table::Table: touch scroll vs edit-mode reorder (director design 2026-07-20)::auto Edit/Done toggle: shown when a gamepad capability is live even though preferredInput stays KeyboardAndMouse",
     "table::Table: touch scroll vs edit-mode reorder (director design 2026-07-20)::auto Edit/Done toggle: a live touch capability shows it under a KeyboardAndMouse preferredInput (hybrid handheld)",
     "table::Table: gamepad support (director 2026-07-20 — every input, every control)::buildFocusGroups maps the toggle plus one horizontal group per row, in document order",
     "table::Table: gamepad support (director 2026-07-20 — every input, every control)::A-button (action) grab: grab, d-pad step-move, drop; focus path survives; visuals show grabbed",
     "table::Table: gamepad support (director 2026-07-20 — every input, every control)::focus moves anywhere INSIDE a row (stars included) re-select that row (standard model)",
     "table::Table: gamepad support (director 2026-07-20 — every input, every control)::a grabbed SELECTED row steps the whole selection block",
     "table::table rows sit inside the theme's own selection card::a flat package does not move a single pixel",
     "table::table rows sit inside the theme's own selection card::a skinning package insets the cells by what its card reserves PLUS its glow gutter",
     "table::table rows sit inside the theme's own selection card::the header tracks the rows, so the columns never come apart",
     "table::Table: `scrolls = false` makes it a block for a scrolling host::as a BLOCK there is exactly ONE scroller, and it is the host's page",
     "table::Table: `scrolls = false` makes it a block for a scrolling host::...and the page's canvas carries the WHOLE view, header included",
     "table::Table: `scrolls = false` makes it a block for a scrolling host::every row is laid out rather than clipped to a window",
     "table::Table: `scrolls = false` makes it a block for a scrolling host::the DEFAULT is unchanged: the table still owns its own viewport",
     "table::Table column disclosure (Step 8.5 LT-G2)::a disclosing column stamps disclose on its value cells AND its header title",
     "table::Table column disclosure (Step 8.5 LT-G2)::...and an ordinary column stamps nothing — the default is byte-identical",
     "table::Table column disclosure (Step 8.5 LT-G2)::engaging the truncated disclosing cell presents its FULL value through the presenter",
     "table::Table column boundary is strict (architecture review F-4)::an unknown column key is refused, naming the column — a disclose typo cannot silently drop the guarantee",
     "table::Table column boundary is strict (architecture review F-4)::a non-boolean disclose is refused with the type it got",
     "table::Table.rowGap: a number or a theme metric name::a number behaves exactly as it always has",
     "table::Table.rowGap: a number or a theme metric name::a SPACING STEP resolves to the theme's own step, not to a crash",
     "table::Table.rowGap: a number or a theme metric name::a DOTTED METRIC PATH resolves too — the spelling the playlist example uses",
     "table::Table.rowGap: a number or a theme metric name::the DROP SLOT arithmetic uses the resolved pixels, not the name",
     "table::Table.rowGap: a number or a theme metric name::a name that resolves to NOTHING is refused at the authoring boundary, naming it",
     "table::Table: toolbar, header and body stack at every text preference::the solver files NO overflow anywhere in the table's chrome, at any preference",
     "table::Table: toolbar, header and body stack at every text preference::the toolbar, the header and the body never overlap — the reported defect",
     "table::Table: toolbar, header and body stack at every text preference::the header band GROWS with the preference instead of holding its token",
     "table::Table: row actions (Task 10)::wraps only rows whose callback returns actions; nil rows get no RowActions node",
     "table::Table: row actions (Task 10)::unknown keys inside the returned action table error at build, naming the offending row",
     "table::Table: row actions (Task 10)::swiping row B open closes row A (one shared coordinator through Table)",
     "table::Table: row actions (Task 10)::mouse vertical drag on a reorderable + wrapped row still reorders",
     "table::Table: row actions (Task 10)::a reorderable + wrapped row mounts NO internal Content/Hit Grip at all (only Table's own composed Hit)",
     "table::Table: row actions (Task 10)::a wrapped, NON-reorderable row still mounts its own internal Content/Hit Grip",
     "table::Table: row actions (Task 10)::mouse horizontal drag on the same reorderable + wrapped row reveals actions instead of reordering",
     "table::Table: row actions (Task 10)::a tray-button TAP still fires onAction under Table's own opts.onActivate override",
     "table::Table: row actions (Task 10)::starting a reorder-handle drag on a row whose tray is open closes the tray FIRST, before reorder proceeds",
     "table::Table: row actions (Task 10)::mouse BODY-drag reorder (not the ≡ handle) on a row whose tray is open closes the tray first",
     "table::Table: row actions (Task 10)::gamepad/keyboard grab (A / action-source Activate) on a row whose tray is open closes the tray first",
     "table::Table: row actions (Task 10)::editing signal shows the minus on rows with a destructive action",
     "table::Table: row actions (Task 10)::rows removed from spec.rows dispose their wrapper (no leak)",
     "table::Table: row actions (Task 10)::a data refresh mid-gesture cancels: onAction never fires for a key removed mid-commit",
     "table::Table: row actions (Task 10)::Delete key on a focused row deletes via the row's destructive action",
     "table::Table: row actions (Task 10)::tap-to-close: a tap on a row's own content while ITS tray is open closes it — no select, no activate",
     "table::Table: row actions (Task 10)::a committed delete shrinks the row's own box and moves following rows up, THEN fires onAction and lets the owner remove it via keyed diff",
     "table::Table: row actions (Task 10)::edit mode shows BOTH the row_actions minus and Table's own reorder handle at once",
     "table::Table: row actions (Task 10)::opening a wrapped row's menu leaves every OTHER row's solved rect byte-identical — Table's own authored rowHeight survives",
     "table::Table: a BLOCK table publishes no scroll path of its own (ruling 3)::a VIEWPORT table publishes its own Body; a BLOCK table publishes nothing",
     "table::Table: a BLOCK table publishes no scroll path of its own (ruling 3)::the BLOCK table's published Body is not a scroller at all — which is why publishing it lied",
     "table::Table: a BLOCK table publishes no scroll path of its own (ruling 3)::THE SYMPTOM: a PAGE scroll closes an open tray in a BLOCK table",
     "table::Table: a BLOCK table publishes no scroll path of its own (ruling 3)::...and a VIEWPORT table still closes its tray on its OWN body scroll (unchanged)",
     "table::Table: a BLOCK table publishes no scroll path of its own (ruling 3)::binding a BLOCK table still reports the mount error when it was never presented",
     "table_virtualized::Table virtualized: only the window is mounted::a 400-row table mounts a bounded window, not 400 rows",
     "table_virtualized::Table virtualized: only the window is mounted::BOUNDED BY CONSTRUCTION: 5,000 rows build only the windowed cells",
     "table_virtualized::Table virtualized: only the window is mounted::the canvas carries the FULL content extent, so the engine can scroll all of it",
     "table_virtualized::Table virtualized: only the window is mounted::FRAME ONE already fills the body: the seed is the screen, so it over-mounts, never under",
     "table_virtualized::Table virtualized: only the window is mounted::the row just BELOW the viewport is mounted, so keyboard navigation never walks off the edge",
     "table_virtualized::Table virtualized: only the window is mounted::rowGap is spent by the OFFSETS exactly once, never by the canvas stack as well",
     "table_virtualized::Table virtualized: only the window is mounted::scrolling mounts the rows at the new offset and unmounts the ones left behind",
     "table_virtualized::Table virtualized: one arithmetic with newVirtualList::every windowed row lands at the index's own offset (ragged heights)",
     "table_virtualized::Table virtualized: one arithmetic with newVirtualList::a FLOWING table's row tops agree with the same index (the paths cannot drift)",
     "table_virtualized::Table virtualized: one arithmetic with newVirtualList::a FLOWING table's drop hairline is the index's own boundary (rowGap > 0)",
     "table_virtualized::Table virtualized: model state outlives the window::a selected row that scrolls away comes back selected",
     "table_virtualized::Table virtualized: model state outlives the window::multi-selection ranges reach rows the window never held",
     "table_virtualized::Table virtualized: model state outlives the window::a reorder addresses an unmounted row (the move funnel is model-level)",
     "table_virtualized::Table virtualized: model state outlives the window::revealRow reaches a row with no solved rect and materializes it",
     "table_virtualized::Table virtualized: the reorder slot is over the FULL list::a drag while scrolled names a slot two hundred rows down, not a window-relative one",
     "table_virtualized::Table virtualized: the scroll does not jump when the extents move::the row under the viewport's leading edge stays under it when every row grows",
     "table_virtualized::Table virtualized: the scroll does not jump when the extents move::...and WITHOUT the anchor the same change would move it — the guard is not decorative",
     "table_virtualized::Table virtualized: the scroll does not jump when the extents move::shrinking the rows at the BOTTOM leaves the list flush with its last row, not past it",
     "table_virtualized::Table virtualized: the scroll does not jump when the extents move::a text-preference change RE-SOLVES the rows, never rebuilds them",
     "table_virtualized::Table virtualized: a row's height is DERIVED, not content-sized (both modes)::the WINDOWED mode: both rows are the authored 40, whatever their cells say",
     "table_virtualized::Table virtualized: a row's height is DERIVED, not content-sized (both modes)::...and the FLOWING mode agrees, which is what makes the seam port to BOTH",
     "table_virtualized::Table virtualized: what is refused, and why::`virtualized` + `scrolls = false` is refused: a block table has no viewport to window against",
     "table_virtualized::Table virtualized: what is refused, and why::`virtualized` + `rowActions` is refused (v1), the way VirtualList refuses reorder + rowActions",
     "table_rows_seam::the row-builder seam: READ == DECLARED == PASSED::every fact the builder reads off `deps` is declared in `Deps`",
     "table_rows_seam::the row-builder seam: READ == DECLARED == PASSED::every key `Deps` declares is one the builder actually reads",
     "table_rows_seam::the row-builder seam: READ == DECLARED == PASSED::the one construction site passes exactly the declared set — no more, no less",
     "table_rows_seam::the row-builder seam: READ == DECLARED == PASSED::there is exactly ONE construction site",
     "table_rows_seam::the row-builder seam: READ == DECLARED == PASSED::NEGATIVE CONTROL: each set comparison reports the pair that is out of step",
     "table_rows_seam::the row-builder seam: the shared WRITE surface, and the one-way require::the builder writes through exactly two deps, and both are containers by design",
     "table_rows_seam::the row-builder seam: the shared WRITE surface, and the one-way require::NEGATIVE CONTROL: the write scan sees each of the three write shapes",
     "table_rows_seam::the row-builder seam: the shared WRITE surface, and the one-way require::the builder offers exactly the two entry points it declares",
     "table_rows_seam::the row-builder seam: the shared WRITE surface, and the one-way require::the builder requires the control NOWHERE — the require is one-way",
     "table_rows_seam::the row-builder seam: the shared WRITE surface, and the one-way require::the builder names no type from the control either",
     "playlist_columns::playlist columns: the header is on screen and legible::every column paints a title box with real area — presence is not visibility",
     "playlist_columns::playlist columns: the header is on screen and legible::a resizable column grows a divider and a locked one refuses — both invisibly, so the readout names them",
     "playlist_columns::playlist columns: the header is on screen and legible::pinning one column leaves a SIBLING to absorb the remainder — the row still fills its region",
     "playlist_columns::playlist columns: the three routes all move the same column::a pointer drag on the divider MOVES the column, and the live readout follows the commit",
     "playlist_columns::playlist columns: the three routes all move the same column::a KEYBOARD reaches it with no consumer wiring: the heading, Return, then the arrows",
     "playlist_columns::playlist columns: the three routes all move the same column::a GAMEPAD reaches it: the bumper steps the focused heading without selecting anything",
     "playlist_columns::playlist columns: the three routes all move the same column::SELECTING A COLUMN PAINTS NOTHING, so the hint line is the only thing that says the arrows moved house",
     "playlist_columns::playlist columns: the three routes all move the same column::a LOCKED column binds no Adjust key at all — the bumper stays the game's",
     "playlist_columns::playlist columns: the tutorial's contract::the device funnels each move the model, and the readouts follow",
     "playlist_columns::playlist columns: the tutorial's contract::the header IS reachable by a Tab walk from the page's first focus stop",
     "playlist_columns::playlist columns: the tutorial's contract::disposes clean: no node of this surface survives, and the core reports no error",
     "playlist_columns::playlist sort: the owner sorts, and the sort composes with the filter::no sort is the SHIPPED order, and the memo hands back the source array untouched",
     "playlist_columns::playlist sort: the owner sorts, and the sort composes with the filter::tapping the Name heading cycles the owner's signal, and the rows follow it",
     "playlist_columns::playlist sort: the owner sorts, and the sort composes with the filter::RATING opts in with `sortable = true` and sorts by the SIGNAL, live",
     "playlist_columns::playlist sort: the owner sorts, and the sort composes with the filter::the FILTER runs over the SORTED view, so both derivations are live at once",
     "playlist_columns::playlist sort: the owner sorts, and the sort composes with the filter::Artist sorts by `value` too, and duplicate artists tie-break on the SOURCE index",
     "playlist_columns::playlist sort: a manual reorder wins, and takes the sort with it::a drop BAKES the sorted order into the source and clears the sort",
     "playlist_columns::playlist sort: a manual reorder wins, and takes the sort with it::`Top` bakes too — a move a live sort would undo is a button that lies",
     "playlist_columns::playlist sort: a manual reorder wins, and takes the sort with it::REMOVING a row is not a reorder: the sort survives it",
     "playlist_columns::playlist sort: a manual reorder wins, and takes the sort with it::Restore puts the sort back to nil along with the order and the ratings",
     "playlist_columns::playlist sort: a manual reorder wins, and takes the sort with it::a reorder that does not happen does not clear the sort either",
     "playlist_columns::playlist sort: a manual reorder wins, and takes the sort with it::reordering is still REFUSED under a filter, sort or no sort",
     "playlist_columns::playlist columns: how big the divider actually is::the grip is the theme's small spacing step in every paradigm, touch included",
     "playlist_columns::the resize grip's hit floor lands on top of its own header button::asks for 44x44 centred on an 8x28 grip, which covers 26px of the header cell it sits in",
     "playlist_columns::column resize on a VIRTUALIZED table::a committed width remounts the WINDOWED rows at the new width, and the window stays bounded",
     "playlist_columns::column resize on a VIRTUALIZED table::...and it is still 2 000 rows the table never mounted",
     "playlist_columns::playlist columns: the divider forwards a press it did not turn into a drag::a press and release with no travel sorts the column and commits NO width",
     "playlist_columns::playlist columns: the divider forwards a press it did not turn into a drag::travel UNDER the pointer promotion gate is still a tap; travel over it is a resize",
     "playlist_columns::playlist columns: the divider forwards a press it did not turn into a drag::the guide line does not appear for a press that never becomes a drag",
     "playlist_columns::newTable: the header's column boundaries are the body's::every column's header spans exactly its cells, with the bar reserved",
     "playlist_columns::newTable: the header's column boundaries are the body's::...and with no bar to reserve, the same grid, and no gutter paid",
     "playlist_columns::newTable: the header's column boundaries are the body's::PRESENT ALONE lands the grids together — the gutter costs no frame at all",
     "playlist_columns::newTable: the header's column boundaries are the body's::converges in one refresh and then holds still",
     "playlist_columns::newTable: the header's column boundaries are the body's::a committed resize keeps the grids together",
     "playlist_columns::playlist columns: a selected column is released the moment focus leaves it::Cancel releases it, and a second Activate releases it",
     "playlist_columns::playlist columns: a selected column is released the moment focus leaves it::focus leaving the table entirely releases it too, however it left",
     "playlist_columns::playlist columns: the 320x640 edit-mode measurement that kept the third column::at the narrowest swept viewport, in edit mode, no cell collapses and the numbers are these",
     "playlist_columns::playlist columns: the 320x640 edit-mode measurement that kept the third column::...because the RATING column collapsed, and the table says so out loud (ADAPT-18)",
     "playlist_columns::playlist columns: the 320x640 edit-mode measurement that kept the third column::the solver files NO collapse diagnostic — which is the whole difference from the fixed column",
     "hit_expander_overhang::hit-expander overhang: the table divider's floor is reachable::the divider outranks the NEIGHBOUR column its 44px floor reaches into",
     "hit_expander_overhang::hit-expander overhang: the table divider's floor is reachable::nothing is left that can take the band: the whole 44px belongs to the divider",
     "hit_expander_overhang::hit-expander overhang: a NON-table expander carried the same defect::every sub-floor row outranks the row its floor reaches into",
     "hit_expander_overhang::hit-expander overhang: a NON-table expander carried the same defect::…and the rule holds against the ten-foot floor, on the same tree",
     "hit_expander_overhang::hit-expander overhang: the qualifier and the stability it buys::two floors that overlap ONLY each other keep their declared order",
     "hit_expander_overhang::hit-expander overhang: the qualifier and the stability it buys::the lift is read from the base order, so a second solve moves nothing",
     "table_themed_header::Table header: the sort mark under every shipped theme package::the mark stops the theme's OWN reserve short of the column boundary",
     "table_themed_header::Table header: the sort mark under every shipped theme package::the mark clears the plate's carved border, so it paints INSIDE the box",
     "table_themed_header::Table header: the sort mark under every shipped theme package::the title never overlaps the mark, leading or trailing",
     "table_themed_header::Table header: the sort mark under every shipped theme package::a column resize carries the mark with the boundary, at any width",
     "table_themed_header::Table header: the sort mark under every shipped theme package::no package makes the header band report a layout finding",
     "table_themed_header::Table header: the columns line up with the rows in every body shape::a header cell and its body cell occupy the same span, in every shape and package",
     "table_themed_header::Table header: the columns line up with the rows in every body shape::the Edit toolbar clears the header band and ends on the trailing column",
     "table_themed_header::Table header: the columns line up with the rows in every body shape::the Edit toolbar's clearance covers the chrome glow, not merely the zero floor",
     "table_themed_header::Table header: the columns line up with the rows in every body shape::the swept packages really do declare a glow gutter, or this axis proves nothing",
     "pointer::Pointer seam: grips and cursor hints::a Grip renders as a non-button node and hover requests its cursor hint",
     "pointer::Pointer seam: grips and cursor hints::pointer capture: down on a grip routes moves and up to it, with rects",
     "pointer::Pointer seam: grips and cursor hints::cursor hint persists across a captured drag even off the grip",
     "pointer::Pointer seam: capture lifecycle::destroying the root cancels an active pointer capture",
     "pointer::Pointer seam: capture lifecycle::a live capture on root B survives root A's destroyRoot, and cancels on root B's own destroyRoot",
     "pointer::Pointer seam: capture lifecycle::a capture whose node dies through an untracked path no longer blocks a fresh press",
     "pointer::Pointer seam: dynamic hints and opt-in bindings::a dynamic cursorHint re-applies through the adapter on change",
     "pointer::Pointer seam: dynamic hints and opt-in bindings::sinkNavigation stops nav keys from reaching lower contexts (the avatar)",
     "pointer::Pointer seam: dynamic hints and opt-in bindings::Adjust and reorder-nav bindings exist only when the screen opts in",
     "pointer::Pointer seam: rectOf and anchored overlays::controller.rectOf returns solved rects for live paths",
     "pointer::Pointer seam: rectOf and anchored overlays::Anchor overlay: a dynamic offset re-arranges with rect writes only (no remount)",
     "preview::Preview: device profiles::ships presets matching the measured device truths",
     "preview::Preview: device profiles::ships a console profile whose facts flip distanceProfile and interactionClasses together",
     "preview::Preview: device profiles::applying a profile re-renders an attached tree with rect writes only",
     "preview::Preview: device profiles::applying the console profile insets the solved tree by the overscan margins",
     "preview::Preview: notched device-safe profile (native-substrate NS-A1)::phoneNotchLandscape pushes deviceSafeInsets and profile switch resets them",
     "render_target_contract::RenderTargetAdapter contract::the FakeTarget satisfies every required and optional method",
     "render_target_contract::RenderTargetAdapter contract::the contract DECLARES all eleven methods the framework feature-detects",
     "render_target_contract::RenderTargetAdapter contract::setThemeStyle is a GROWTH seam: declared, feature-detected, never a theming blocker",
     "render_target_contract::RenderTargetAdapter contract::the FakeTarget satisfies the THEME group too (relink rides the opt-in)",
     "render_target_contract::RenderTargetAdapter contract::the LIVE ScreenTarget implements every declared name (its source is the contract)",
     "render_target_contract::RenderTargetAdapter contract::a billboard-sized pixel canvas renders a nameplate and updates paint-only",
     "render_target_contract::adapter prop parity: the fake and the live target handle the same props::every prop the renderer can emit is handled by the LIVE adapter",
     "render_target_contract::adapter prop parity: the fake and the live target handle the same props::every prop the renderer can emit is handled by the FAKE adapter",
     "render_target_contract::adapter prop parity: the fake and the live target handle the same props::the two adapters agree: nothing is handled by one and not the other",
     "render_target_contract::adapter prop parity: the fake and the live target handle the same props::the FakeTarget REFUSES an undeclared prop instead of recording it",
     "render_target_contract::adapter prop parity: the fake and the live target handle the same props::Text.role names BOTH ends of its closed set, and both adapters write both",
     "render_target_contract::adapter prop parity: the fake and the live target handle the same props::the renderer's declared emissions are exactly its two channels plus its own seams",
     "render_target_contract::the render-target playbook's OPTIONAL roster (MAINT-8b)::names every OPTIONAL method the contract declares, and nothing else",
     "render_target_contract::the render-target playbook's OPTIONAL roster (MAINT-8b)::BITES: a method added to the contract and not to the playbook fails",
     "render_target_contract::skinned-control asset roots are derived (MAINT-8d)::finds the shipped rung-3 control from its manifest, not from a path",
     "render_target_contract::skinned-control asset roots are derived (MAINT-8d)::a THEME package's art is not mistaken for a control's",
     "render_target_contract::skinned-control asset roots are derived (MAINT-8d)::BITES: check_docs requires every file the manifest names",
     "surface_target::surface_target: the canvas the solver is handed::canvasRect is the canvas, at the origin, and is pure",
     "surface_target::surface_target: the canvas the solver is handed::the canvas is the whole viewport, so a screen solves against it exactly",
     "surface_target::surface_target: every refusal says why::parent is required, and the message names what it should be",
     "surface_target::surface_target: every refusal says why::adornee is required, and the message says what it is",
     "surface_target::surface_target: every refusal says why::face is required, and the refusal explains the trap rather than just refusing",
     "surface_target::surface_target: every refusal says why::the queryable precondition is refused BEFORE any Instance exists",
     "surface_target::surface_target: every refusal says why::canvas is required, and the message says the solver is handed exactly it",
     "surface_target::surface_target: what it withholds is real::every capability it deletes is one the contract actually declares",
     "surface_target::surface_target: what it withholds is real::the module carries the measurement beside each choice it makes",
     "outpost_terminal::outpost rules: the ranges, walked rather than sampled::every value inside 0..capacity is legal, and capacity+1 and -1 are not",
     "outpost_terminal::outpost rules: the ranges, walked rather than sampled::the three declared ranges are the design's, and the goal is reachable inside them",
     "outpost_terminal::outpost rules: the ranges, walked rather than sampled::THE TRADE-OFF IS REAL: a fully-lit workshop can never leave all three running",
     "outpost_terminal::outpost rules: the ranges, walked rather than sampled::the total bound is exactly six, over the whole product",
     "outpost_terminal::outpost rules: the ranges, walked rather than sampled::WHAT RUNNING MEANS is `at least what it needs`, over the whole product",
     "outpost_terminal::outpost rules: the ranges, walked rather than sampled::the starting allocation is legal, and is a problem with an obvious first move",
     "outpost_terminal::outpost rules: every refusal, and the exact sentence it uses::a fraction is refused as `whole`, naming the consumer and the value",
     "outpost_terminal::outpost rules: every refusal, and the exact sentence it uses::a missing consumer is refused as `missing`, and an extra one as `unknown`",
     "outpost_terminal::outpost rules: every refusal, and the exact sentence it uses::THE ORDER IS PART OF THE CONTRACT: a fraction that is also over budget says `whole`",
     "outpost_terminal::outpost rules: every refusal, and the exact sentence it uses::every declared refusal code has a sentence, and an undeclared one is loud",
     "outpost_terminal::outpost rules: every refusal, and the exact sentence it uses::the three server-only sentences are the ones the design specifies",
     "outpost_terminal::outpost rules: what changed, which is what the terminal reports::only a CROSSING is news; a number that moved without crossing is not",
     "outpost_terminal::outpost rules: what changed, which is what the terminal reports::the outcome sentence has three cases and picks the player's one first",
     "outpost_terminal::outpost authority: every refusal the server can make::the happy path: the holder, a fresh token, in range, with a legal allocation",
     "outpost_terminal::outpost authority: every refusal the server can make::REFUSAL 1 — nobody holds the console, or somebody else does",
     "outpost_terminal::outpost authority: every refusal the server can make::REFUSAL 2 — a stale session token, or no token at all",
     "outpost_terminal::outpost authority: every refusal the server can make::REFUSAL 3 — faster than a hand can press",
     "outpost_terminal::outpost authority: every refusal the server can make::REFUSAL 4 — too far, MEASURED, and an unmeasurable distance is not a pass",
     "outpost_terminal::outpost authority: every refusal the server can make::REFUSAL 5 — the allocation itself, through the same rules the terminal ran",
     "outpost_terminal::outpost authority: every refusal the server can make::THE ORDER IS PART OF THE CONTRACT: who you are beats how far, beats what you asked",
     "outpost_terminal::outpost terminal: engage -> adjust -> apply -> success -> reset -> exit::a terminal nobody is standing at is idle, and says the objective",
     "outpost_terminal::outpost terminal: engage -> adjust -> apply -> success -> reset -> exit::ENGAGE seeds the draft from what the outpost is actually running",
     "outpost_terminal::outpost terminal: engage -> adjust -> apply -> success -> reset -> exit::ADJUST moves the draft, and the budget line and the live verdict follow it",
     "outpost_terminal::outpost terminal: engage -> adjust -> apply -> success -> reset -> exit::AN ILLEGAL DRAFT IS REFUSED HERE and never reaches the wire",
     "outpost_terminal::outpost terminal: engage -> adjust -> apply -> success -> reset -> exit::APPLY sends ONE intent carrying the draft, and `pending` is not success",
     "outpost_terminal::outpost terminal: engage -> adjust -> apply -> success -> reset -> exit::a SECOND apply while one is in flight is refused instead of throwing",
     "outpost_terminal::outpost terminal: engage -> adjust -> apply -> success -> reset -> exit::SUCCESS: the verdict lands, the outpost moves, and the terminal says what changed",
     "outpost_terminal::outpost terminal: engage -> adjust -> apply -> success -> reset -> exit::A REFUSAL IS SHOWN, NOT SWALLOWED — and the draft survives it",
     "outpost_terminal::outpost terminal: engage -> adjust -> apply -> success -> reset -> exit::every server refusal reaches the screen as its own sentence",
     "outpost_terminal::outpost terminal: engage -> adjust -> apply -> success -> reset -> exit::RESET IS A SERVER INTENT, not a local undo",
     "outpost_terminal::outpost terminal: engage -> adjust -> apply -> success -> reset -> exit::AN EDIT RETIRES THE LAST OUTCOME, so the line never stands over state it contradicts",
     "outpost_terminal::outpost terminal: engage -> adjust -> apply -> success -> reset -> exit::an allocation ANOTHER player applied lands without touching this draft",
     "outpost_terminal::outpost terminal: every exit lands in the same place::all eight reasons produce the same terminal state, and each is recorded by name",
     "outpost_terminal::outpost terminal: every exit lands in the same place::EXIT IS IDEMPOTENT, because the two directions have to be one door",
     "outpost_terminal::outpost terminal: every exit lands in the same place::an exited terminal sends nothing, and re-engaging is safe",
     "outpost_terminal::outpost terminal: every exit lands in the same place::NO TRANSPORT IS A STATE, NOT A CRASH",
     "outpost_terminal::outpost terminal: the screen itself::it holds its own canvas at every text preference the player can choose",
     "outpost_terminal::outpost terminal: the screen itself::it declares the walk-up contract rather than leaving each host to derive it",
     "outpost_terminal::outpost terminal: the screen itself::its canvas is declared once and the aspect is a real console's",
     "outpost_terminal::outpost terminal: the screen itself::EVERY CONTROL NEEDS ONLY ACTIVATION — there is not a drag on the terminal",
     "outpost_terminal::outpost terminal: the screen itself::THE CONTENT MODULE IS ENGINE-FREE, which is what lets two hosts share it",
     "presentation_channel::presentation transform: composition onto the solved rect::a transform on a leaf offsets its painted position and leaves the rect alone",
     "presentation_channel::presentation transform: composition onto the solved rect::nil clears the transform and the node returns to its solved position",
     "presentation_channel::presentation transform: composition onto the solved rect::a RECT WRITE re-applies the stored transform (a re-solve never drops a motion)",
     "presentation_channel::presentation transform: composition onto the solved rect::a settled motion re-asserting its value costs no adapter write (minimal writes)",
     "presentation_channel::presentation transform: composition onto the solved rect::scale materializes a named transient UIScale and removes it at rest",
     "presentation_channel::presentation transform: composition onto the solved rect::scale pivots on the node's CENTER: the visual rect inflates in place",
     "presentation_channel::presentation transform: composition onto the solved rect::rotation is recorded as paint and never as geometry",
     "presentation_channel::presentation transform: composition onto the solved rect::the write asserts the PRESENTATION authority for the node's own class",
     "presentation_channel::presentation transform: composition onto the solved rect::an unmounted path is a no-op, not an error (a motion may outlive its node)",
     "presentation_channel::presentation transform: the keep-visible root shift (SF-M9)::setPresentationOffset composes onto the ROOT's painted position",
     "presentation_channel::presentation transform: the keep-visible root shift (SF-M9)::...and onto every node in the surface, which is what makes it visible at all",
     "presentation_channel::presentation transform: the keep-visible root shift (SF-M9)::dy = 0 restores: the transform is written as zero, not cleared",
     "presentation_channel::presentation transform: the keep-visible root shift (SF-M9)::a node created UNDER a live shift starts composed (no one-frame jump)",
     "presentation_channel::presentation transform: the keep-visible root shift (SF-M9)::a removed node forgets its transform, so a re-created path starts at rest",
     "presentation_channel::presentation transparency: the declared fade group::a declared canvasGroup node becomes a fade group and takes GroupTransparency",
     "presentation_channel::presentation transparency: the declared fade group::fading an UNDECLARED node is refused, and the error names the fix",
     "presentation_channel::presentation transparency: the declared fade group::the declaration is not reactive and rejects a Signal at construction",
     "presentation_channel::presentation transparency: the declared fade group::GroupTransparency is NOT a native-sheet-owned property (that is the whole point)",
     "presentation_channel::screen_target: the presentation channel (source contract)::setProp handles BOTH presentation prop names (the SF-M9 branches)",
     "presentation_channel::screen_target: the presentation channel (source contract)::applyRect — the ONE Position write — composes the presentation offset",
     "presentation_channel::screen_target: the presentation channel (source contract)::applyRect — the ONE Size write — composes the presentation SIZE delta",
     "presentation_channel::screen_target: the presentation channel (source contract)::the SIZE half does NOT accumulate — one own-path lookup, never an ancestor walk",
     "presentation_channel::screen_target: the presentation channel (source contract)::the offset ACCUMULATES down the subtree, because the instance tree is flat",
     "presentation_channel::screen_target: the presentation channel (source contract)::a running motion re-places its SUBTREE, not the whole target",
     "presentation_channel::screen_target: the presentation channel (source contract)::the hit expander travels with the paint (a shifted control stays pressable)",
     "presentation_channel::screen_target: the presentation channel (source contract)::the motion scale SHARES the press dip's UIScale (the engine honours one)",
     "presentation_channel::screen_target: the presentation channel (source contract)::an invisible hairline is not built at all (modifier elision)",
     "presentation_channel::screen_target: the presentation channel (source contract)::a fade group is created as a CanvasGroup, capability-probed, and is a real parent",
     "presentation_channel::screen_target: the presentation channel (source contract)::teardown drops the transform record, the real-parent entry and both instances",
     "instance_hosts::instanceHosts: registration is create-or-reuse, through one function::registerHost builds the record the parenting rule reads",
     "instance_hosts::instanceHosts: registration is create-or-reuse, through one function::a second registration on the same path REUSES the entry, children intact",
     "instance_hosts::instanceHosts: registration is create-or-reuse, through one function::unregisterHost removes it, and hostFor stops matching",
     "instance_hosts::instanceHosts: registration is create-or-reuse, through one function::dropPath unregisters the host as well as the transform record",
     "instance_hosts::instanceHosts: registration is create-or-reuse, through one function::hostAt is an EXACT lookup and hostFor is an ANCESTOR lookup",
     "instance_hosts::hostFor: the deepest registered ancestor wins, and only a real ancestor::nested hosts resolve to the DEEPEST one",
     "instance_hosts::hostFor: the deepest registered ancestor wins, and only a real ancestor::registration order does not decide the winner — depth does",
     "instance_hosts::hostFor: the deepest registered ancestor wins, and only a real ancestor::a shared STRING prefix is not a path prefix — /A/B never captures /A/BC/D",
     "instance_hosts::hostFor: the deepest registered ancestor wins, and only a real ancestor::no hosts at all is nil, not an error",
     "instance_hosts::hostFor: the deepest registered ancestor wins, and only a real ancestor::a path with no separator has no ancestor to find",
     "instance_hosts::hostFor does not scan the registry, and that is what makes nesting affordable::thousands of unrelated hosts do not slow a lookup",
     "instance_hosts::WHICH NODES ARE REAL PARENTS, through the renderer that decides it::the three shipped triggers each produce a host, and an ordinary container does not",
     "instance_hosts::WHICH NODES ARE REAL PARENTS, through the renderer that decides it::a descendant parents under the NEAREST host, and a flat one under nothing",
     "instance_hosts::WHICH NODES ARE REAL PARENTS, through the renderer that decides it::CROPS IS NOT clipChildren: a fade group crops too, because its buffer does",
     "instance_hosts::WHICH NODES ARE REAL PARENTS, through the renderer that decides it::THE TRADE: a registered host is refused by the recycle pool",
     "instance_hosts::WHICH NODES ARE REAL PARENTS, through the renderer that decides it::a removed host stops being one, or a new node parents into a corpse",
     "host_move_write_cost::a host that moves costs ONE engine write, not one per descendant::the control: placing the surface writes every node exactly once",
     "host_move_write_cost::a host that moves costs ONE engine write, not one per descendant::A/A: re-applying the SAME rects writes nothing at all",
     "host_move_write_cost::a host that moves costs ONE engine write, not one per descendant::THE BILL: the whole list moves 40px and ONE Position is written",
     "host_move_write_cost::a host that moves costs ONE engine write, not one per descendant::NEGATIVE CONTROL: a child that really moves inside its host is still written",
     "host_move_write_cost::a host that moves costs ONE engine write, not one per descendant::NEGATIVE CONTROL: with no host above them, moving nodes pays per node",
     "host_move_write_cost::a host that moves costs ONE engine write, not one per descendant::a RESIZE still writes Size, and only Size",
     "nested_tree_pins::the depth-first z counter IS Sibling paint order::a parent is below its own subtree, and its subtree is below its next sibling",
     "nested_tree_pins::the depth-first z counter IS Sibling paint order::the counter is strictly increasing — no two nodes share a slot",
     "nested_tree_pins::the depth-first z counter IS Sibling paint order::a zIndex lift reorders siblings and still carries its own subtree with it",
     "nested_tree_pins::tapAt and the z counter cannot disagree::the tapped node is the one the counter puts on top",
     "nested_tree_pins::the framework writes ONE node, the engine carries the subtree::the container carries the scale and the rotation",
     "nested_tree_pins::the framework writes ONE node, the engine carries the subtree::UPDATED WHEN STEP 4 LANDED: the framework still writes ONE node — the engine carries it",
     "nested_tree_pins::the framework writes ONE node, the engine carries the subtree::STEP 4: the delivery mechanism — the container became a real parent",
     "nested_tree_pins::the framework writes ONE node, the engine carries the subtree::STEP 4 IS BOUNDED: a rotated LEAF earns nothing, and neither does an empty container",
     "nested_tree_pins::the framework writes ONE node, the engine carries the subtree::the POSITION half does accumulate, and that is the mechanism nesting replaces",
     "nested_tree_pins::a host's rect lands BEFORE its descendants', or they re-base against a ghost::on a first render, every host is placed before anything inside it",
     "nested_tree_pins::a host's rect lands BEFORE its descendants', or they re-base against a ghost::...and a NESTED host is placed before its own subtree too",
     "nested_tree_pins::a layout-only frame does not open a commit scope that commits nothing::a LAYOUT-only change opens commit once, and writes no props",
     "nested_tree_pins::a layout-only frame does not open a commit scope that commits nothing::NEGATIVE CONTROL: a PAINT change still opens the prop pass",
     "nested_tree_pins::a scale that went back to 1 must come back — the UIScale corpse::turn on, off, and on again: the scale is live and carries its value",
     "nested_tree_pins::a scale that went back to 1 must come back — the UIScale corpse::...and the fields do not keep a destroyed instance after the scale clears",
     "nested_compositing_scenario::nested_compositing showcase: registered everywhere a player or a sweep would look::is in scenarios/init.luau ORDER, in demo_picker.DEMOS, and in the overflow sweep's SCENARIOS",
     "nested_compositing_scenario::nested_compositing showcase: registered everywhere a player or a sweep would look::names a module that actually exists on disk",
     "nested_compositing_scenario::nested_compositing showcase: builds and paints with ZERO solver diagnostics::at the narrow/wide swept viewports, at rest",
     "nested_compositing_scenario::nested_compositing showcase: builds and paints with ZERO solver diagnostics::...and still zero after the fade is driven and the carry pair is turned",
     "nested_compositing_scenario::nested_compositing showcase: builds and paints with ZERO solver diagnostics::every node a player is meant to see paints a real, visible, positive-area rect",
     "nested_compositing_scenario::nested_compositing showcase: builds and paints with ZERO solver diagnostics::its chrome copy survives a ~1.4x pseudo-localization without clipping",
     "nested_compositing_scenario::nested_compositing showcase: group opacity composites once, or twice::the left panel is ONE engine CanvasGroup and the right panel is two",
     "nested_compositing_scenario::nested_compositing showcase: group opacity composites once, or twice::every one of them carries the fade the slider's own signal drives, live",
     "nested_compositing_scenario::nested_compositing showcase: a real parent, or not::the carrying panel's container is registered as a real instance host, and the alone panel's is not",
     "nested_compositing_scenario::nested_compositing showcase: a real parent, or not::the carried marker parents INSIDE its container; the alone marker's nearest host is the page scroller",
     "nested_compositing_scenario::nested_compositing showcase: a real parent, or not::turning the pair moves the SAME two driver signals both panels read",
     "paint_extensions::tint: the authoring boundary::accepts both declared value forms",
     "paint_extensions::tint: the authoring boundary::refuses a role outside the closed palette vocabulary",
     "paint_extensions::tint: the authoring boundary::refuses a blend outside 0..1, and a missing one",
     "paint_extensions::tint: the authoring boundary::refuses both forms at once, an unknown field, and a malformed hex",
     "paint_extensions::tint: the authoring boundary::is offered on FOUR classes only: a control paints from its role and states",
     "paint_extensions::tint: the authoring boundary::the direct form is the LOUD word: the docs call it theming-exempt",
     "paint_extensions::tint: resolution (the ONE pure resolver both adapters use)::blend = 0 is the class's identity base and blend = 1 is the role",
     "paint_extensions::tint: resolution (the ONE pure resolver both adapters use)::an Image's identity base is WHITE (the picture as authored)",
     "paint_extensions::tint: resolution (the ONE pure resolver both adapters use)::`from` names the base explicitly: control -> accent is the ratified shape",
     "paint_extensions::tint: resolution (the ONE pure resolver both adapters use)::the direct form is exactly the colour it names, in both spellings",
     "paint_extensions::tint: resolution (the ONE pure resolver both adapters use)::a REACTIVE value is validated at the write site, loudly",
     "paint_extensions::tint: resolution (the ONE pure resolver both adapters use)::the schema and the resolver share ONE role vocabulary",
     "paint_extensions::tint: emission and application::rides the BINDING channel with the authority declared per class",
     "paint_extensions::tint: emission and application::the adapter applies the RESOLVED colour and records the claimed properties",
     "paint_extensions::tint: emission and application::is the LAST paint write at creation, so a surface cannot overwrite it",
     "paint_extensions::tint: emission and application::a Box tint paints at its declared transparency, and defaults to opaque",
     "paint_extensions::tint: emission and application::Text and Image claim a transparency ONLY when the tint declares one",
     "paint_extensions::tint: emission and application::the live adapter writes the alpha onto the engine property, all three classes",
     "paint_extensions::tint: emission and application::a Path REFUSES an alpha rather than accepting and dropping it",
     "paint_extensions::tint: emission and application::the schema refuses an out-of-range alpha, at construction and at the write site",
     "paint_extensions::tint: emission and application::a reactive tint re-applies through the ordinary refresh loop",
     "paint_extensions::tint: the native-sheet claim mechanism (source contract)::the central gate CONSULTS the claims instead of erroring on them",
     "paint_extensions::tint: the native-sheet claim mechanism (source contract)::a claim records the pre-claim value through GetStyled and publishes it",
     "paint_extensions::tint: the native-sheet claim mechanism (source contract)::the claim does NOT strip the node's tags, and the file says why",
     "paint_extensions::tint: the native-sheet claim mechanism (source contract)::every claimed write goes claim-then-assert, per class",
     "paint_extensions::tint: the native-sheet claim mechanism (source contract)::a repaint of a claimed property re-applies the tint (declared as a set)",
     "paint_extensions::tint: the native-sheet claim mechanism (source contract)::the claim reaches the node dump, beside the GetStyled reading it explains",
     "paint_extensions::tint: the native-sheet claim mechanism (source contract)::a tinted fill rides a tag, because the claim on it could never be taken",
     "paint_extensions::tint: the native-sheet claim mechanism (source contract)::a bare circle node gets a real fallback UICorner the moment a tint paints it",
     "paint_extensions::tint: the native-sheet claim mechanism (source contract)::the four claimable properties are the sheet-owned ones (that is why they claim)",
     "paint_extensions::Image scaleMode::is a closed set, and an unknown value is a construction error",
     "paint_extensions::Image scaleMode::rides the STYLE channel and applies at creation and reactively",
     "paint_extensions::Image scaleMode::fill and crop are DELIBERATE synonyms for the engine's Crop, in both adapters",
     "paint_extensions::Image scaleMode::the live write claims ScaleType and restores it when the declaration goes",
     "paint_extensions::UI.stroke: the authored border::defaults to the theme's hairline and normalizes its three fields",
     "paint_extensions::UI.stroke: the authored border::rejects an unknown field and a Signal inside the spec, naming the pulse idiom",
     "paint_extensions::UI.stroke: the authored border::returns a NEW blueprint (blueprints are frozen) and rides the style authority",
     "paint_extensions::UI.stroke: the authored border::materializes ONE instance and REUSES it, so a pulse cannot stack borders",
     "paint_extensions::UI.stroke: the authored border::shadowData / gradientData / cornersData complete the family and pulse the same way",
     "paint_extensions::UI.stroke: the authored border::is a declared bespoke family, named from the manifest's own constant",
     "paint_extensions::UI.stroke: the authored border::COEXISTS with theme chrome: it neither asserts against the sheet nor removes a hairline",
     "paint_extensions::UI.stroke: the authored border::...and the theme's own chrome stroke is untouched: both mechanisms still exist",
     "paint_extensions::zIndex: paint order within the parent's stacking scope::without a zIndex the order is tree order (nothing moves)",
     "paint_extensions::zIndex: paint order within the parent's stacking scope::a lifted sibling paints above the siblings it was declared before",
     "paint_extensions::zIndex: paint order within the parent's stacking scope::a NEGATIVE zIndex sinks below its zero-index siblings",
     "paint_extensions::zIndex: paint order within the parent's stacking scope::a node's whole SUBTREE travels with it",
     "paint_extensions::zIndex: paint order within the parent's stacking scope::a child is ALWAYS above its own parent: lifting across scopes stays structural",
     "paint_extensions::zIndex: paint order within the parent's stacking scope::the order is DETERMINISTIC across re-solves and structural syncs",
     "paint_extensions::zIndex: paint order within the parent's stacking scope::is read once at mount and rejects a Signal, like every `what a node IS` prop",
     "paint_extensions::zIndex: paint order within the parent's stacking scope::ties are broken by declaration order rather than by an unstable sort",
     "fractional_offsets::Anchor: fractional offsets::places a child at a share of the anchor's inner width and height",
     "fractional_offsets::Anchor: fractional offsets::adds the px part on top of the fraction (a dot centred on its own point)",
     "fractional_offsets::Anchor: fractional offsets::the px part may itself be a THEME METRIC name",
     "fractional_offsets::Anchor: fractional offsets::respects the anchor's own PADDING: the fraction is of the INNER box",
     "fractional_offsets::Anchor: fractional offsets::composes with the anchor CORNER, which is what makes a centred marker layer work",
     "fractional_offsets::Anchor: fractional offsets::NESTS: an inner anchor's fraction is of ITS box, not the screen's",
     "fractional_offsets::Anchor: fractional offsets::re-resolves on a viewport change, like every other layout input",
     "fractional_offsets::Anchor: fractional offsets::a MOVING marker costs an arrange and a rect write — no remount, no re-measure",
     "fractional_offsets::Anchor: fractional offsets::a fill child still pays for its own fractional offset (the round-10 give-back)",
     "fractional_offsets::Anchor: fractional offsets — the authoring boundary::rejects a fraction with no scale, and an unknown field",
     "fractional_offsets::Anchor: fractional offsets — the authoring boundary::rejects a metric NAME that names no metric inside the px part",
     "fractional_offsets::Anchor: fractional offsets — the authoring boundary::keeps both older forms working untouched (a number and a metric name)",
     "presenter_drag_integration::presenter drag integration: the auto-wired collaborators::a presented surface's drag events reach the PRESENTER bus, surface attached",
     "presenter_drag_integration::presenter drag integration: the auto-wired collaborators::the auto-wired motion clock defers `land` to the ARRIVAL frame; ticking lands it",
     "presenter_drag_integration::presenter drag integration: the auto-wired collaborators::the HELD SOURCE is a framework guarantee: dragHeld rides pickup, clears on the RETURN'S ARRIVAL",
     "presenter_drag_integration::presenter drag integration: the auto-wired collaborators::the dragHeld write goes through the authority assert like every other (ARCH-3)",
     "presenter_drag_integration::presenter drag integration: the auto-wired collaborators::...and the RELEASE site pays the same assert (ARCH-3)",
     "presenter_drag_integration::presenter drag integration: the auto-wired collaborators::the ghost layer mounts on pickup and is GONE after the flight (registry-neutral)",
     "presenter_drag_integration::presenter drag integration: the auto-wired collaborators::presentModal mid-drag cancels the base surface's session (F7, end to end)",
     "presenter_drag_integration::detector taps are never eaten (SF-D3 live half)::an under-threshold detector release dispatches Activate exactly once",
     "presenter_drag_integration::detector taps are never eaten (SF-D3 live half)::armOnTap: a TAP IS the pickup — arms instead of activating; a tap while held flows through",
     "presenter_drag_integration::detector taps are never eaten (SF-D3 live half)::one press cannot activate twice when BOTH signals arrive (PLAT-1 echo guard)",
     "presenter_drag_integration::mid-flight teardown and retiring surfaces (ARCH-F1 / ARCH-F2)::dismissing a surface mid-commit-flight leaves the shared clock EMPTY",
     "presenter_drag_integration::mid-flight teardown and retiring surfaces (ARCH-F1 / ARCH-F2)::disposing the registry with several flights in the air is clean (RR-3-R1)",
     "presenter_drag_integration::mid-flight teardown and retiring surfaces (ARCH-F1 / ARCH-F2)::a RETIRING drop target leaves resolution and returns on re-entry (ARCH-F2)",
     "presenter_drag_integration::mid-flight teardown and retiring surfaces (ARCH-F1 / ARCH-F2)::a retiring '/S/Slot' never excludes its live SIBLING '/S/SlotA' (P2-2)",
     "presenter_drag_integration::mid-flight teardown and retiring surfaces (ARCH-F1 / ARCH-F2)::a DISABLED draggable acquires no session (P2-3), and re-enabling restores it",
     "presenter_drag_integration::presenter frame hook (onTick)::hooks run inside tick with the frame dt, and unsubscribe cleanly",
     "runtime_quarantine::motion clock: a throwing callback cannot wedge the clock (RR-1)::after a throwing onSettle, later motions still run and the error is recorded",
     "runtime_quarantine::motion clock: a throwing callback cannot wedge the clock (RR-1)::a throwing onSettle subscriber does not starve the ones after it (RR-12-R1)",
     "runtime_quarantine::motion clock: a throwing callback cannot wedge the clock (RR-1)::a throwing live-target function quarantines the frame, not the clock",
     "runtime_quarantine::motion clock: dispose during a step defers, never crashes (RR-2)::an onSettle handler disposing the clock is safe and completes at the step tail",
     "runtime_quarantine::scope disposal: a throwing cleanup is quarantined (RR-4)::siblings still release, counters stay exact, and no false double-dispose follows",
     "runtime_quarantine::scope disposal: a throwing cleanup is quarantined (RR-4)::the double-dispose report fires for an ALIASED child and stays silent otherwise (RR-5-R1)",
     "runtime_quarantine::scope ownership: an unreleasable resource is refused at own() (F-13)::a table with no dispose() is refused, naming the scope and the fix",
     "runtime_quarantine::scope ownership: an unreleasable resource is refused at own() (F-13)::functions, child scopes, and dispose-bearing objects all stay legal",
     "runtime_quarantine::scope ownership: an unreleasable resource is refused at own() (F-13)::`use` still borrows anything — only OWNERSHIP implies releasability",
     "runtime_quarantine::motion clock: the stats contract under a quarantined frame (RR-1-R2)::transactions counts frames that really committed, never the aborted ones",
     "fuzz_scheduler::scheduler property fuzz (seeded)::upholds invariants over 60 seeded cases and leaves registries at baseline",
     "fuzz_scheduler::scheduler property fuzz (seeded)::is replayable: the same base seed yields an identical run fingerprint",
     "fuzz_scheduler::scheduler property fuzz (seeded)::reports a replayable per-case seed on failure, and that seed reproduces",
     "fuzz_layout::layout property fuzz (seeded)::upholds solver invariants over 80 seeded random trees",
     "fuzz_layout::layout property fuzz (seeded)::is replayable: identical base seed, identical fingerprint",
     "fuzz_layout::layout property fuzz (seeded)::flags generated undefined combinations as diagnostics, not crashes",
     "fuzz_replication::replication property fuzz (seeded)::converges through delay/duplication/reorder/drop over 60 seeded cases",
     "fuzz_replication::replication property fuzz (seeded)::is replayable: identical base seed, identical fingerprint",
     "faults::fault injection (seeded, deterministic)::async-fixed: cap, retry generations, stale rejection, cancel, cache budget, failed re-request",
     "faults::fault injection (seeded, deterministic)::async-storm: seeded acquire/release/complete/fail/stale storms stay legal and registry-neutral",
     "faults::fault injection (seeded, deterministic)::core-eq-quarantine: throwing user eq callbacks quarantine (no wedged flush, no raised set); node dispose reclaims live observers",
     "faults::fault injection (seeded, deterministic)::scheduler-feedback: effect writes converge next round; true feedback loops hit the cap loudly and the scheduler survives",
     "faults::fault injection (seeded, deterministic)::presenter-dismiss-non-top: dismissing a covered screen removes ITS focus scope; the modal stays active and navigable",
     "faults::fault injection (seeded, deterministic)::teardown: dirty-queue dispose, detected double disposal, dispose-from-observer, quarantined runtime duplicate key",
     "faults::fault injection (seeded, deterministic)::locale: locale/text storms (pseudo-loc, CJK, emoji, bidi) queue measure dirties and stay registry-neutral",
     "faults::fault injection (seeded, deterministic)::accessibility: garbage platform facts clamp into legal derived domains (typography/transparency/motion/sizeClass)",
     "faults::fault injection (seeded, deterministic)::input-switch: preferred-input storms (incl. mid-press and unknown values) never remount; bindings survive",
     "virtualization::virtualization: fixed-height keyed rows (§10.5)::mounts a bounded window (visible + overscan), never the whole list",
     "virtualization::virtualization: fixed-height keyed rows (§10.5)::a same-window scroll delta is ZERO framework writes (the engine owns the offset)",
     "virtualization::virtualization: fixed-height keyed rows (§10.5)::an off-window edit that changes no key and no extent dirties NOTHING (RR-5)",
     "virtualization::virtualization: fixed-height keyed rows (§10.5)::requiring Facet loads the fifteen typed controls and defers exactly four (T15)",
     "virtualization::virtualization: fixed-height keyed rows (§10.5)::the deferred require fires at the construction seam and never in a frame (T15)",
     "virtualization::virtualization: fixed-height keyed rows (§10.5)::Facet.preload() force-loads exactly the deferred set, and twice is a cache hit (T15)",
     "virtualization::virtualization: fixed-height keyed rows (§10.5)::surviving rows keep their mounted identity across a window slide",
     "virtualization::virtualization: fixed-height keyed rows (§10.5)::focuses an off-window item BY KEY: materializes and scrolls it into view",
     "virtualization::virtualization: fixed-height keyed rows (§10.5)::keeps focus on the nearest surviving neighbor when the focused row is removed",
     "virtualization::virtualization: fixed-height keyed rows (§10.5)::a re-entered row's dump equals a fresh row's dump (no state bleed)",
     "virtualization::virtualization: fixed-height keyed rows (§10.5)::cancels async cell resources on window exit; late completion is stale",
     "virtualization::virtualization: fixed-height keyed rows (§10.5)::scroll storms leave every registry at baseline after dispose",
     "virtual_extents::virtual_extents: the uniform index is today's arithmetic, unchanged::content extent carries no trailing gutter",
     "virtual_extents::virtual_extents: the uniform index is today's arithmetic, unchanged::offsetOf is (index-1)*pitch and extentOf is the one number",
     "virtual_extents::virtual_extents: the uniform index is today's arithmetic, unchanged::window reproduces floor/ceil + overscan, the pre-feature formula",
     "virtual_extents::virtual_extents: the uniform index is today's arithmetic, unchanged::window clamps to the data at both ends",
     "virtual_extents::virtual_extents: the variable index is a prefix sum::offsetOf sums every preceding extent plus one gap each",
     "virtual_extents::virtual_extents: the variable index is a prefix sum::extentOf answers each item's own size",
     "virtual_extents::virtual_extents: the variable index is a prefix sum::window covers exactly the slots the viewport touches, plus overscan",
     "virtual_extents::virtual_extents: the variable index is a prefix sum::a viewport inside ONE tall item windows only that item",
     "virtual_extents::virtual_extents: the variable index is a prefix sum::degenerate inputs answer rather than throw",
     "virtual_extents::virtual_extents: the variable index is a prefix sum::an offset past the content clamps to the last slot",
     "virtual_extents::virtual_extents: the variable index CONTAINS the uniform one::every question agrees when every extent is equal",
     "virtual_extents::virtual_extents: the variable index CONTAINS the uniform one::mid-slot, the variable rule windows one MORE row — and that is deliberate",
     "virtual_extents::virtual_extents: insertion boundaries snap to the nearer edge::uniform boundaries sit in the middle of the gutter, clamped to the canvas",
     "virtual_extents::virtual_extents: insertion boundaries snap to the nearer edge::slotAt counts the whole slots whose midpoint the offset has passed",
     "virtual_extents::virtual_extents: insertion boundaries snap to the nearer edge::variable boundaries land on the item edge, gutter split",
     "virtual_extents::virtual_extents: the search is bounded, which is the whole point::windowing a 10,000-item list reads a logarithmic number of extents",
     "virtual_extents::virtual_extents: the search is bounded, which is the whole point::the window never grows with the list",
     "virtual_list_variable_extents::VirtualList variable extents: what the spec accepts::accepts a per-item function, and the dump says which arithmetic is running",
     "virtual_list_variable_extents::VirtualList variable extents: what the spec accepts::a uniform list still reports its one number, and says so",
     "virtual_list_variable_extents::VirtualList variable extents: what the spec accepts::refuses a per-item extent that is not a positive number, naming the row",
     "virtual_list_variable_extents::VirtualList variable extents: the geometry is the running offset::each row's box is its OWN extent, at the prefix-sum offset",
     "virtual_list_variable_extents::VirtualList variable extents: the geometry is the running offset::the gutter is dead space between ragged slots, exactly as it is uniform ones",
     "virtual_list_variable_extents::VirtualList variable extents: the geometry is the running offset::the content extent is the sum plus the gutters, with none trailing",
     "virtual_list_variable_extents::VirtualList variable extents: the geometry is the running offset::the PAINTED canvas spans the running total, not count x one extent",
     "virtual_list_variable_extents::VirtualList variable extents: the geometry is the running offset::keep-visible measures the row's OWN extent, not an average",
     "virtual_list_variable_extents::VirtualList variable extents: virtualization stays bounded::a viewport inside ONE tall row windows that row alone",
     "virtual_list_variable_extents::VirtualList variable extents: virtualization stays bounded::only the WINDOWED items are ever built — 10,000 ragged rows, bounded",
     "virtual_list_variable_extents::VirtualList variable extents: the scroll does not jump when extents change::the item at the top of the viewport stays at the top",
     "virtual_list_variable_extents::VirtualList variable extents: the scroll does not jump when extents change::shrinking the items at the END of the list still corrects the ENGINE, not just the memo",
     "virtual_list_variable_extents::VirtualList variable extents: the scroll does not jump when extents change::the extents re-derive through `use`, and the canvas follows",
     "virtual_list_variable_extents::VirtualList variable extents: the rest of the control agrees with the index::the insertion slot snaps on ragged midpoints",
     "virtual_list_variable_extents::VirtualList variable extents: the rest of the control agrees with the index::the declared slot is PER ROW: the guard names that row's own number",
     "virtual_list_variable_extents::VirtualList variable extents: an edit that moves no geometry rebuilds nothing::a uniform list rebuilds its index only when the geometry moves",
     "virtual_list_variable_extents::VirtualList variable extents: an edit that moves no geometry rebuilds nothing::a variable list rebuilds only when an EXTENT moves, not when an item does",
     "virtual_list_variable_extents::VirtualList variable extents: the uniform path is untouched::a per-item function returning one constant is IDENTICAL to the uniform list",
     "virtual_list_measured_extents::VirtualList measured extents: what the spec accepts::accepts itemExtent = \"measured\" with an estimate, and the dump says so",
     "virtual_list_measured_extents::VirtualList measured extents: what the spec accepts::refuses measured mode with no estimate, and an estimate that is not a positive number",
     "virtual_list_measured_extents::VirtualList measured extents: what the spec accepts::refuses an estimate on a list that does not measure — a field that drives nothing",
     "virtual_list_measured_extents::VirtualList measured extents: what the spec accepts::refuses any other string: \"measured\" is the one word, not an open enum",
     "virtual_list_measured_extents::VirtualList measured extents: the row is windowed at what it measures::a cell taller than the estimate moves the row's box AND the next row's offset",
     "virtual_list_measured_extents::VirtualList measured extents: the row is windowed at what it measures::rows of DIFFERENT measured heights each get their own slot",
     "virtual_list_measured_extents::VirtualList measured extents: the row is windowed at what it measures::the gutter is still dead space between measured slots",
     "virtual_list_measured_extents::VirtualList measured extents: the row is windowed at what it measures::axis = \"x\" measures the item's WIDTH",
     "virtual_list_measured_extents::VirtualList measured extents: the row is windowed at what it measures::a row that SHRINKS is re-measured down, not ratcheted",
     "virtual_list_measured_extents::VirtualList measured extents: what a measurement is allowed to be::a fractional measure is rounded to whole px, and a zero one is floored to 1",
     "virtual_list_measured_extents::VirtualList measured extents: laziness survives::only the WINDOWED rows are ever BUILT — 10,000 rows, counted",
     "virtual_list_measured_extents::VirtualList measured extents: laziness survives::rows the player has never reached keep the ESTIMATE, and the canvas says so",
     "virtual_list_measured_extents::VirtualList measured extents: laziness survives::convergence is ONE step: a second refresh over the same content writes nothing",
     "virtual_list_measured_extents::VirtualList measured extents: the scroll does not jump as measurements land::the item at the top of the viewport stays at the top when every row grows",
     "virtual_list_measured_extents::VirtualList measured extents: a fill main axis is refused::a cell that declares fill on the SCROLL axis is refused, naming the row",
     "virtual_list_measured_extents::VirtualList measured extents: a fill main axis is refused::fill on the CROSS axis is fine — that is how a row spans the list",
     "virtual_list_measured_extents::VirtualList measured extents: a fill main axis is refused::a DECLARED list still accepts a fill cell — the refusal is measured mode's",
     "virtual_list_measured_extents::VirtualList measured extents: the lying-extent class is structurally closed::the cell that makes the guard shout under a DECLARATION produces no finding here",
     "virtual_list_measured_extents::VirtualList measured extents: the data is the cache's boundary::a removed row's measurement is dropped with it",
     "virtual_list_measured_extents::VirtualList measured extents: the uniform path is untouched::a measured list of EQUAL rows converges to the uniform list declaring that number",
     "virtual_list_row_gap::VirtualList.rowGap: the gutter is in the PITCH, never in the row::is refused unless it is a non-negative number or a Readable<number>",
     "virtual_list_row_gap::VirtualList.rowGap: the gutter is in the PITCH, never in the row::carries the resolved gutter in the dump",
     "virtual_list_row_gap::VirtualList.rowGap: the gutter is in the PITCH, never in the row::the CONTENT EXTENT is N*rowHeight + (N-1)*rowGap — no trailing gutter",
     "virtual_list_row_gap::VirtualList.rowGap: the gutter is in the PITCH, never in the row::window membership steps on the PITCH, not the row height",
     "virtual_list_row_gap::VirtualList.rowGap: the gutter is in the PITCH, never in the row::keep-visible brings the ROW into the band, measured on the pitch",
     "virtual_list_row_gap::VirtualList.rowGap: the gutter is in the PITCH, never in the row::the row's own box stays rowHeight tall — the gutter is DEAD SPACE",
     "virtual_list_row_gap::VirtualList.rowGap: the gutter is in the PITCH, never in the row::rowGap = 0 reproduces the pre-field arithmetic exactly",
     "virtual_list_row_gap::VirtualList.rowGap: the gutter is in the PITCH, never in the row::the drop hit-test reads the same pitch: a pointer in the gutter is a slot boundary",
     "virtual_list_row_gap::VirtualList.rowGap: the gutter is in the PITCH, never in the row::a bounded window is still bounded with a gutter (the canvas never mounts the list)",
     "collection_gap_tokens::an AMBIGUOUS core is refused, not resolved against neutral::a NAMED gutter on an ambiguous core refuses, and the message names the way out",
     "collection_gap_tokens::an AMBIGUOUS core is refused, not resolved against neutral::...and so does `viewportExtent = \"auto\"`, whose seed is a screen it cannot name",
     "collection_gap_tokens::an AMBIGUOUS core is refused, not resolved against neutral::a build that needs NO environment is still built on an ambiguous core",
     "collection_gap_tokens::an AMBIGUOUS core is refused, not resolved against neutral::NOTHING published is still the headless case, and still resolves against neutral",
     "collection_gap_tokens::a virtualized gutter takes a theme metric name::VirtualList.rowGap resolves a space step, and the arithmetic is the number's",
     "collection_gap_tokens::a virtualized gutter takes a theme metric name::...and a DOTTED PATH into the metrics, exactly as newTable spells it",
     "collection_gap_tokens::a virtualized gutter takes a theme metric name::VirtualGrid.rowGap and VirtualGrid.gap both take one",
     "collection_gap_tokens::a virtualized gutter takes a theme metric name::VirtualGrid.gap takes a Readable — the field that refused even that",
     "collection_gap_tokens::a virtualized gutter takes a theme metric name::a name that resolves NOWHERE is refused, and names itself",
     "collection_gap_tokens::a virtualized gutter takes a theme metric name::a negative number is still refused — widening the type widened nothing else",
     "collection_gap_tokens::a virtualized gutter takes a theme metric name::the gutter is LIVE: a theme swap moves it with no rebuild",
     "collection_self_measure::viewportExtent = \"auto\": the list measures the box it was given::windows against the SOLVED host box, with no number handed in",
     "collection_self_measure::viewportExtent = \"auto\": the list measures the box it was given::SEEDS FROM THE SCREEN, so the frame before a measurement over-fills",
     "collection_self_measure::viewportExtent = \"auto\": the list measures the box it was given::a RESIZE re-measures in place, and a settled tree writes nothing",
     "collection_self_measure::viewportExtent = \"auto\": the list measures the box it was given::newVirtualGrid takes it too, on its own scroll axis",
     "collection_self_measure::viewportExtent = \"auto\": the list measures the box it was given::a number and a Readable still work, and are still refused when they are neither",
     "collection_self_measure::viewportExtent = \"auto\": the measurement that is NOT a viewport::a list nested in another scroller's SCROLL axis measures its whole canvas, and says so",
     "collection_self_measure::viewportExtent = \"auto\": the measurement that is NOT a viewport::NEGATIVE CONTROL: a list in a real pane reports NOTHING, however many rows it has",
     "collection_self_measure::viewportExtent = \"auto\": the measurement that is NOT a viewport::a grid says the same thing about the same shape",
     "collection_self_measure::viewportExtent = \"auto\" + itemExtent = \"cards\": the seed is not an arrangement::a card rail paints NOTHING before it has been measured, then the right arrangement",
     "collection_self_measure::viewportExtent = \"auto\" + itemExtent = \"cards\": the seed is not an arrangement::NEGATIVE CONTROL: a plain list still seeds from the screen and over-fills",
     "collection_self_measure::crossExtent: the axis the list does not scroll::absent, the cross axis FILLS — every shipped list is untouched",
     "collection_self_measure::crossExtent: the axis the list does not scroll::\"hug\" sizes to the content instead — the rail is as tall as its card",
     "collection_self_measure::crossExtent: the axis the list does not scroll::\"measured\" keeps filling and REPORTS the box it was given",
     "collection_self_measure::crossExtent: the axis the list does not scroll::\"hug\" and an authored `width` are two answers to one dim, and are refused together",
     "collection_self_measure::crossExtent: the axis the list does not scroll::\"hug\" works on a VERTICAL list too, where the cross axis is the WIDTH",
     "collection_self_measure::crossExtent: the axis the list does not scroll::an unknown word is refused, naming the two that are legal",
     "virtual_list_axis::VirtualList.axis — the contract (\"y\" | \"x\", construction-only)::defaults to \"y\", accepts \"y\" explicitly, and refuses anything else naming BOTH values",
     "virtual_list_axis::VirtualList.axis — the contract (\"y\" | \"x\", construction-only)::itemExtent / viewportExtent are the canonical names and land in the dump",
     "virtual_list_axis::VirtualList.axis — the contract (\"y\" | \"x\", construction-only)::rowHeight / viewportHeight still work UNCHANGED — the deprecated aliases",
     "virtual_list_axis::VirtualList.axis — the contract (\"y\" | \"x\", construction-only)::the deprecated names are ABSENT from a horizontal dump — a rowHeight there is a lie",
     "virtual_list_axis::VirtualList.axis — the contract (\"y\" | \"x\", construction-only)::a name and its alias TOGETHER are refused, naming both and which is deprecated",
     "virtual_list_axis::VirtualList.axis — the contract (\"y\" | \"x\", construction-only)::both deprecations are in Facet.DEPRECATIONS with a replacement and a minor of notice",
     "virtual_list_axis::VirtualList.axis — the contract (\"y\" | \"x\", construction-only)::rowActions is refused on axis = \"x\", and the refusal says WHY (the gesture collides)",
     "virtual_list_axis::VirtualList.axis — the contract (\"y\" | \"x\", construction-only)::`width` is refused on axis = \"x\" — there, the width IS viewportExtent",
     "virtual_list_axis::VirtualList.axis — the windowing arithmetic reads the x canvas::the content extent is N*itemExtent + (N-1)*rowGap along x, with no trailing gutter",
     "virtual_list_axis::VirtualList.axis — the windowing arithmetic reads the x canvas::window membership steps on the PITCH along x",
     "virtual_list_axis::VirtualList.axis — the windowing arithmetic reads the x canvas::keep-visible writes the target on X, and leaves Y at zero (the discriminator)",
     "virtual_list_axis::VirtualList.axis — the windowing arithmetic reads the x canvas::the CanvasPosition mirror reads .X on a horizontal list (and .Y on a vertical one)",
     "virtual_list_axis::VirtualList.axis — the windowing arithmetic reads the x canvas::scrollTo clamps and writes along the list's own axis",
     "virtual_list_axis::VirtualList.axis — the presented canvas is a full-WIDTH one::declares an x scroll region whose contentW is the full virtual extent",
     "virtual_list_axis::VirtualList.axis — the presented canvas is a full-WIDTH one::items sit at absolute canvas X offsets and are itemExtent WIDE, filling the height",
     "virtual_list_axis::VirtualList.axis — the presented canvas is a full-WIDTH one::rowGap is the gutter ALONG the axis: dead space between items, never inside one",
     "virtual_list_axis::VirtualList.axis — the presented canvas is a full-WIDTH one::the canvas grows sideways with the data",
     "virtual_list_axis::VirtualList.axis — the presented canvas is a full-WIDTH one::only the window mounts: a 1000-item strip is still a bounded window",
     "virtual_list_axis::VirtualList horizontal pointer input (no consumer wiring)::VirtualList pointer: a horizontal engine scroll slides the window along x",
     "virtual_list_axis::VirtualList horizontal pointer input (no consumer wiring)::VirtualList pointer: a tap on a horizontal item activates through auto handleActivate",
     "virtual_list_axis::VirtualList horizontal touch input (no consumer wiring)::VirtualList touch: a native sideways pan slides the window and never activates",
     "virtual_list_axis::VirtualList horizontal touch input (no consumer wiring)::VirtualList touch: a tap on a horizontal item activates",
     "virtual_list_axis::VirtualList horizontal keyboard input (no consumer wiring)::VirtualList keyboard: Left/Right steps a horizontal list item-by-item",
     "virtual_list_axis::VirtualList horizontal keyboard input (no consumer wiring)::VirtualList keyboard: Up/Down do NOT step a horizontal list — ONE map, read one way",
     "virtual_list_axis::VirtualList horizontal keyboard input (no consumer wiring)::VirtualList keyboard: focus past the horizontal window edge scrolls it into view",
     "virtual_list_axis::VirtualList horizontal keyboard input (no consumer wiring)::VirtualList keyboard: Return activates the focused horizontal item",
     "virtual_list_axis::VirtualList horizontal gamepad input (no consumer wiring)::VirtualList gamepad: DPadLeft/DPadRight move focus identically to arrows",
     "virtual_list_axis::VirtualList horizontal gamepad input (no consumer wiring)::VirtualList gamepad: ButtonA activates the focused horizontal item",
     "virtual_list_axis::VirtualList.axis — drag, drop line and edge autoscroll follow the axis::the predicted insertion slot is read from the pointer's X",
     "virtual_list_axis::VirtualList.axis — drag, drop line and edge autoscroll follow the axis::the ARMED cursor steps on Right/Left, and Up/Down are inert (the same axis focus walks)",
     "virtual_list_axis::VirtualList.axis — drag, drop line and edge autoscroll follow the axis::the COMMITTED index is read from the release point's X, not its Y",
     "virtual_list_axis::VirtualList.axis — drag, drop line and edge autoscroll follow the axis::edge autoscroll drives X and reports LEFT/RIGHT bands, never top/bottom",
     "virtual_list_axis::VirtualList.axis — drag, drop line and edge autoscroll follow the axis::a VERTICAL list still reports top/bottom — the band names are not globally renamed",
     "virtual_list_axis::VirtualList.axis — a list declaring no axis builds what it always did::vertical remains the default end to end: region, offsets, focus order, keep-visible",
     "virtual_list_axis::VirtualList.axis — a list declaring no axis builds what it always did::the DEPRECATED spelling builds the identical vertical geometry",
     "virtual_list_focus_policy::VirtualList focusPolicy \"key\" (the default: focus follows the ITEM)::the field is ABSENT by default and the dump says so, with no pin held",
     "virtual_list_focus_policy::VirtualList focusPolicy \"key\" (the default: focus follows the ITEM)::a re-sort under the cursor carries focus WITH the item — both halves",
     "virtual_list_focus_policy::VirtualList focusPolicy \"key\" (the default: focus follows the ITEM)::an EXPLICIT \"key\" is the same answer as an absent field",
     "virtual_list_focus_policy::VirtualList focusPolicy \"key\" (the default: focus follows the ITEM)::removal still lands on the nearest survivor, preferring the follower",
     "virtual_list_focus_policy::VirtualList focusPolicy \"index\" (focus stays on the SLOT)::a re-sort under the cursor retargets to the slot's new occupant — BOTH halves",
     "virtual_list_focus_policy::VirtualList focusPolicy \"index\" (focus stays on the SLOT)::the pin SURVIVES repeated re-sorts (the 250 ms standings case)",
     "virtual_list_focus_policy::VirtualList focusPolicy \"index\" (focus stays on the SLOT)::a data update that does NOT move the focused row leaves focus alone",
     "virtual_list_focus_policy::VirtualList focusPolicy \"index\" (focus stays on the SLOT)::SHRINK clamps the pin to the last slot",
     "virtual_list_focus_policy::VirtualList focusPolicy \"index\" (focus stays on the SLOT)::REMOVAL of the focused row is the same slot, clamped (the shared survivor rule)",
     "virtual_list_focus_policy::VirtualList focusPolicy \"index\" (focus stays on the SLOT)::it falls THROUGH to the item when the slot's new occupant is focus-INELIGIBLE",
     "virtual_list_focus_policy::VirtualList focusPolicy \"index\" (focus stays on the SLOT)::it never fights a LIVE DRAG: a re-sort mid-session leaves the aim alone",
     "virtual_list_focus_policy::VirtualList focusPolicy \"index\" (focus stays on the SLOT)::the retarget is an ORDINARY focus move: keep-visible applies",
     "virtual_list_focus_policy::VirtualList focusPolicy \"index\" (focus stays on the SLOT)::an EXPLICIT focus move answering the same update WINS over the pin",
     "virtual_list_focus_policy::VirtualList focusPolicy \"index\" (focus stays on the SLOT)::the policy never SUMMONS a focus ring the player put away",
     "virtual_list_focus_policy::VirtualList focusPolicy \"index\" (focus stays on the SLOT)::the RING is only moved when it was on the retargeted row (live canary 2026-08-02)",
     "virtual_list_focus_policy::VirtualList focusPolicy \"index\" (focus stays on the SLOT)::with nothing focused, a re-sort focuses nothing",
     "virtual_list_focus_policy::VirtualList focusPolicy validation (constitution §4)::an illegal value is refused at construction, naming BOTH legal ones",
     "virtual_list_focus_policy::VirtualList focusPolicy validation (constitution §4)::the closed spec set knows the field: a near-miss is refused by name",
     "virtual_list_slot_guard::newVirtualList: a lying itemExtent is named, with both numbers::FIRES on the pre-fix lab shape and names the declared extent AND the measure",
     "virtual_list_slot_guard::newVirtualList: a lying itemExtent is named, with both numbers::names the RIGHT numbers at every step of the preference (4 -> 60, 14 -> 84)",
     "virtual_list_slot_guard::newVirtualList: a lying itemExtent is named, with both numbers::is SILENT on the fixed shape — the same rows in a slot that tells the truth",
     "virtual_list_slot_guard::newVirtualList: a lying itemExtent is named, with both numbers::is SILENT on a row that is legitimately SHORTER than its slot",
     "virtual_list_slot_guard::newVirtualList: a lying itemExtent is named, with both numbers::SEES a cell that declares `height = fill` — filling a slot is not fitting it",
     "virtual_list_slot_guard::newVirtualList: a lying itemExtent is named, with both numbers::is SILENT on a cell that ABSORBS its own overflow (a scroller on the list's axis)",
     "virtual_list_slot_guard::newVirtualList: a lying itemExtent is named, with both numbers::is SILENT inside a hidden subtree (a losing ViewThatFits candidate)",
     "virtual_list_slot_guard::newVirtualList: a lying itemExtent is named, with both numbers::still reports a CROSS-axis overflow through the generic overlap finding",
     "virtual_list_slot_guard::newVirtualList: a lying itemExtent is named, with both numbers::never accuses the list's OWN furniture: a 16px slot, and the row hit is 24px",
     "virtual_list_slot_guard::newVirtualList: a lying itemExtent is named, with both numbers::compares against the DECLARED extent, never the row's current box",
     "virtual_list_slot_guard::newVirtualList: a lying itemExtent is named, with both numbers::refuses a garbled slot declaration at construction",
     "virtual_list_slot_guard::newVirtualList: a lying itemExtent is named, with both numbers::A FIXED ROW STOPS BEING REPORTED — the finding does not outlive the defect",
     "virtual_list_slot_guard::newVirtualList: a lying itemExtent is named, with both numbers::speaks the AXIS: a horizontal strip's item is too WIDE for its itemExtent",
     "virtual_grid::VirtualGrid: what the spec accepts::an unknown spec key is refused and the message names it",
     "virtual_grid::VirtualGrid: what the spec accepts::columns must be a positive INTEGER — 0, a fraction and a negative are all refused",
     "virtual_grid::VirtualGrid: what the spec accepts::itemExtent must be a positive number, a Readable, or a per-LINE function",
     "virtual_grid::VirtualGrid: what the spec accepts::axis = \"x\" is accepted; an axis that is not an axis is refused",
     "virtual_grid::VirtualGrid: what the spec accepts::a per-line itemExtent that returns garbage fails at CONSTRUCTION, naming the line",
     "virtual_grid::VirtualGrid: the extent index is the shipped one, its unit is a LINE::the canvas extent IS virtual_extents.uniform over ceil(N / lanes) lines — a differential",
     "virtual_grid::VirtualGrid: the extent index is the shipped one, its unit is a LINE::the WINDOW is the index's window in line units, and the items follow by division",
     "virtual_grid::VirtualGrid: the extent index is the shipped one, its unit is a LINE::a viewport inside ONE tall line windows that line alone (the prefix sum, searched)",
     "virtual_grid::VirtualGrid: the extent index is the shipped one, its unit is a LINE::the LAST line is short, and the index still spans exactly the lines that exist",
     "virtual_grid::VirtualGrid: only the windowed cells are ever built::10 000 items, 4 lanes: the EXACT cells of the windowed lines are built and no others",
     "virtual_grid::VirtualGrid: only the windowed cells are ever built::the build count does not depend on N — 100 items and 10 000 build the same cells",
     "virtual_grid::VirtualGrid: only the windowed cells are ever built::a per-line extent function is ARITHMETIC over every line and builds nothing",
     "virtual_grid::VirtualGrid: the scroll does not jump::when the line extents re-derive, the item at the top of the viewport stays at the top",
     "virtual_grid::VirtualGrid: the scroll does not jump::shrinking the lines at the END still corrects the ENGINE, not just the memo",
     "virtual_grid::VirtualGrid: the scroll does not jump::when the COLUMN COUNT changes, the item at the top of the viewport stays at the top",
     "virtual_grid::VirtualGrid: the column arithmetic is the shipped one, not a copy::cell width equals a plain UI.Grid's column at the same width, gap and count",
     "virtual_grid::VirtualGrid: the column arithmetic is the shipped one, not a copy::a SHORT last line keeps its column width and stays left-aligned",
     "virtual_grid::VirtualGrid: the column arithmetic is the shipped one, not a copy::each mounted cell is exactly its own LINE's extent tall, and the lines stack at the index's offsets",
     "virtual_grid::VirtualGrid: the column arithmetic is the shipped one, not a copy::the canvas is the FULL virtual extent and the band sits at its line's offset",
     "virtual_grid::VirtualGrid: dump() reports the cross extent and the per-lane width::the mounted band's own cross extent and its solved column width, not a re-derivation",
     "virtual_grid::VirtualGrid: dump() reports the cross extent and the per-lane width::a cross extent too small for the gaps CLAMPS at zero, never goes negative",
     "virtual_grid::VirtualGrid: the engine owns the offset, the framework owns the window::the engine's reported offset moves the window",
     "virtual_grid::VirtualGrid: the engine owns the offset, the framework owns the window::revealItem reaches an item that has no path at all, and returns the path it gains",
     "virtual_grid::VirtualGrid: the canvas Dim is compared by VALUE (review RR-5)::an off-window edit that changes no key and no extent dirties NOTHING",
     "virtual_grid_input::VirtualGrid pointer input (no consumer wiring)::VirtualGrid pointer: an engine scroll (driveScroll) slides the window",
     "virtual_grid_input::VirtualGrid pointer input (no consumer wiring)::VirtualGrid pointer: a cell tap activates through auto handleActivate",
     "virtual_grid_input::VirtualGrid pointer input (no consumer wiring)::VirtualGrid pointer: partial lines crop via the engine clip host",
     "virtual_grid_input::VirtualGrid touch input (no consumer wiring)::VirtualGrid touch: a native pan scroll slides the window and never activates a cell",
     "virtual_grid_input::VirtualGrid touch input (no consumer wiring)::VirtualGrid touch: a cell tap activates",
     "virtual_grid_input::VirtualGrid touch input (no consumer wiring)::VirtualGrid touch: a garbage mirror value cannot mount a window above the top",
     "virtual_grid_input::VirtualGrid keyboard input (no consumer wiring)::VirtualGrid keyboard: Left/Right steps cell-by-cell ACROSS a line",
     "virtual_grid_input::VirtualGrid keyboard input (no consumer wiring)::VirtualGrid keyboard: Down moves a whole LINE, not one cell",
     "virtual_grid_input::VirtualGrid keyboard input (no consumer wiring)::VirtualGrid keyboard: focus past the window edge scrolls the line into view",
     "virtual_grid_input::VirtualGrid keyboard input (no consumer wiring)::VirtualGrid keyboard: Right off the END of a line scrolls the next line into view",
     "virtual_grid_input::VirtualGrid keyboard input (no consumer wiring)::VirtualGrid keyboard: the focus dump REPORTS the second axis",
     "virtual_grid_input::VirtualGrid keyboard input (no consumer wiring)::VirtualGrid keyboard: Return activates the focused cell",
     "virtual_grid_input::VirtualGrid gamepad input (no consumer wiring)::VirtualGrid gamepad: DPadRight steps a cell and DPadDown steps a whole line",
     "virtual_grid_input::VirtualGrid gamepad input (no consumer wiring)::VirtualGrid gamepad: ButtonA activates the focused cell",
     "virtual_hgrid::VirtualHGrid: the axis the control used to refuse::axis = \"x\" builds, and the dump says which way it runs",
     "virtual_hgrid::VirtualHGrid: the axis the control used to refuse::...and an axis that is neither is still refused, naming the legal pair",
     "virtual_hgrid::VirtualHGrid: the axis the control used to refuse::`width` is refused on the horizontal axis, and the message names the route",
     "virtual_hgrid::VirtualHGrid: only the windowed cells are ever built::10 000 items, 4 lanes: the EXACT cells of the windowed lines are built and no others",
     "virtual_hgrid::VirtualHGrid: only the windowed cells are ever built::...and the build count is INDEPENDENT OF N — the property 'only as needed' names",
     "virtual_hgrid::VirtualHGrid: the mounted band's lanes are the flow grid's own arithmetic::the lane heights match a plain UI.Grid { flow = 'column' } of the same cross extent",
     "virtual_hgrid::VirtualHGrid: the mounted band's lanes are the flow grid's own arithmetic::each mounted cell is exactly its own LINE's extent wide, and lines stack rightward",
     "virtual_hgrid::VirtualHGrid: the mounted band's lanes are the flow grid's own arithmetic::the canvas is the FULL virtual extent along x, so the engine's scroll range is the collection",
     "virtual_hgrid::VirtualHGrid: the mounted band's lanes are the flow grid's own arithmetic::dump() reports the cross extent on ITS OWN axis (the height), and the solved lane width (framework-gaps-phase2 item 26)",
     "virtual_hgrid::VirtualHGrid pointer and touch (no consumer wiring)::a native scroll moves along X and slides the window; it never activates a cell",
     "virtual_hgrid::VirtualHGrid pointer and touch (no consumer wiring)::a garbage mirror value cannot mount a window before the leading edge",
     "virtual_hgrid::VirtualHGrid pointer and touch (no consumer wiring)::a cell tap activates through the auto-composed handleActivate",
     "virtual_hgrid::VirtualHGrid keyboard and gamepad: the ring is the TRANSPOSE, not a copy::Down/Up steps ONE CELL — the group's axis is the LANE axis, which is vertical here",
     "virtual_hgrid::VirtualHGrid keyboard and gamepad: the ring is the TRANSPOSE, not a copy::Right/Left moves a whole LINE, not one cell",
     "virtual_hgrid::VirtualHGrid keyboard and gamepad: the ring is the TRANSPOSE, not a copy::focus past the window edge scrolls the line into view",
     "virtual_hgrid::VirtualHGrid keyboard and gamepad: the ring is the TRANSPOSE, not a copy::Down off the END of a line crosses into the next one, and it is scrolled in",
     "virtual_hgrid::VirtualHGrid keyboard and gamepad: the ring is the TRANSPOSE, not a copy::Return and ButtonA activate the focused cell",
     "navigation_groups::navigation groups (§9.3)::moves along the group axis; the orthogonal axis does not move within the group",
     "navigation_groups::navigation groups (§9.3)::wrap=false stops at the edge; wrap=true wraps inside the group",
     "navigation_groups::navigation groups (§9.3)::a per-direction exit override crosses groups with the target's entry policy",
     "navigation_groups::navigation groups (§9.3)::entry='restore' returns to the group's last-focused item",
     "navigation_groups::navigation groups (§9.3)::containment blocks implicit exits; only declared exits leave the group",
     "navigation_groups::navigation groups (§9.3)::without containment, an axis edge falls through to the neighboring group in order",
     "navigation_groups::navigation groups (§9.3)::a modal trap keeps direction navigation inside the modal scope",
     "navigation_groups::navigation groups (§9.3)::replacing a group's order keeps focus or lands on the nearest survivor",
     "navigation_groups::navigation groups (§9.3)::presenter: navigationGroups opt drives grouped, direction-aware navigation end to end",
     "navigation_groups::navigation groups (§9.3)::replaceGroups swaps a scope's group map, keeping surviving focus",
     "navigation_groups::navigation groups (§9.3)::presenter: D-PAD navigates (grouped scopes) and a groups FUNCTION re-derives on refresh",
     "navigation_groups::navigation groups (§9.3)::the INITIAL focus paints its ring (gamepad users must see where focus starts)",
     "navigation_groups::navigation groups (§9.3)::ADAPT-24: a horizontal ScrollView is a HORIZONTAL run, and a vertical one is not",
     "navigation_groups::navigation groups (§9.3)::ADAPT-24: ...but a scroller holding ONE stack of focusables is not a run",
     "navigation_groups::navigation groups (§9.3)::flat scopes keep the legacy ring semantics (navigate ±1, all wrap)",
     "focus_grid_axis::focus graph: a 2-D group answers the perpendicular direction itself::the perpendicular direction moves a whole LINE, not one cell",
     "focus_grid_axis::focus graph: a 2-D group answers the perpendicular direction itself::the LANE axis is untouched: ±1 in document order, crossing lines row-major",
     "focus_grid_axis::focus graph: a 2-D group answers the perpendicular direction itself::TRANSPOSE: on a `vertical` lane axis the LINE move is Right/Left",
     "focus_grid_axis::focus graph: a 2-D group answers the perpendicular direction itself::a 2-D group answers all FOUR directions, so `navigate(±1)` means down/up",
     "focus_grid_axis::focus graph: a ragged last line has an answer in both directions::FORWARD the move CLAMPS into a short last line rather than falling out",
     "focus_grid_axis::focus graph: a ragged last line has an answer in both directions::...and clamping is NOT a wrap: from the last line itself the move exits",
     "focus_grid_axis::focus graph: a ragged last line has an answer in both directions::BACKWARD needs no clamp — only the last line can be short",
     "focus_grid_axis::focus graph: a ragged last line has an answer in both directions::a single short line is still a line (fewer cells than lanes)",
     "focus_grid_axis::focus graph: 2-D edges, eligibility and wrap::the perpendicular step SKIPS ineligible cells down the same lane",
     "focus_grid_axis::focus graph: 2-D edges, eligibility and wrap::...and the ragged clamp still fires when the walk REACHED the last line by skipping",
     "focus_grid_axis::focus graph: 2-D edges, eligibility and wrap::`wrap` governs the group's DECLARED axis only; the perpendicular exits",
     "focus_grid_axis::focus graph: 2-D edges, eligibility and wrap::exiting perpendicular hands the LANE on, so the column survives the crossing",
     "focus_grid_axis::focus graph: perpendicular ENTRY into a 2-D group is the near LINE::entering DOWNWARD lands in the FIRST line, at the ordinal lane",
     "focus_grid_axis::focus graph: perpendicular ENTRY into a 2-D group is the near LINE::entering UPWARD lands in the LAST line, at the ordinal lane",
     "focus_grid_axis::focus graph: perpendicular ENTRY into a 2-D group is the near LINE::entering upward into a RAGGED last line clamps to the lane that exists",
     "focus_grid_axis::focus graph: perpendicular ENTRY into a 2-D group is the near LINE::entry ALONG the lane axis is unchanged: the near END of the order",
     "focus_grid_axis::focus graph: the rectangle invariant::a SPLICE degrades a 2-D group to its 1-D reading rather than mis-navigating",
     "focus_grid_axis::focus graph: the rectangle invariant::the HIDDEN filter punches the same hole, and drops the field the same way",
     "focus_grid_axis::focus graph: the rectangle invariant::a whole-order replacement re-declares `columns` and the arithmetic returns",
     "focus_grid_axis::focus graph: the rectangle invariant::`setGroupOrder` carries the lane count with the order, or clears it",
     "focus_grid_axis::focus graph: a group with no `columns` is unchanged::NULL HYPOTHESIS: 1-D groups walk byte-identically with the field absent",
     "focus_grid_axis::focus graph: a group with no `columns` is unchanged::`columns` is refused as a navigable claim unless it is an integer >= 1",
     "focus_grid_axis::focus graph: the 2-D ring matches a coordinate model over a generated corpus::agrees with the (line, lane) oracle over 300 seeded grids and walks",
     "focus_grid_axis::focus graph: the 2-D ring matches a coordinate model over a generated corpus::is replayable: identical base seed, identical fingerprint",
     "focus_grid_axis::focus graph: the 2-D ring matches a coordinate model over a generated corpus::THE CORPUS BITES: the oracle rejects a one-cell perpendicular step",
     "perf_runner::perf runner — record schema (§14.3)::produces a schema-complete record for a tiny scene",
     "perf_runner::perf runner — budget enforcement (§16.1)::flags an over-budget reference-device scene",
     "perf_runner::perf runner — budget enforcement (§16.1)::passes an in-budget scene (and ignores non-reference devices)",
     "perf_runner::perf runner — shape determinism (§15.2)::records the same field set and phase set across two runs",
     "perf_runner::perf runner — shape determinism (§15.2)::run() covers the full scene×profile matrix with a stable key set",
     "perf_evidence::evidence classes (UI-PERF-002)::every record carries exactly one evidence class, and Lune may only emit the headless one",
     "perf_evidence::evidence classes (UI-PERF-002)::declares every class in the inventory, including the ones with zero rows",
     "perf_evidence::evidence classes (UI-PERF-002)::a metric the instrument cannot see is a blind marker with a reason, not a zero",
     "perf_evidence::evidence classes (UI-PERF-002)::lifts a scene-declared inputToVisible phase into the M6 slot",
     "perf_evidence::device budgets are unreachable from the wrong class (UI-PERF-002)::refuses to enforce a device budget from a non-device class",
     "perf_evidence::device budgets are unreachable from the wrong class (UI-PERF-002)::skips an unmeasured device budget LOUDLY rather than silently passing it",
     "perf_evidence::device budgets are unreachable from the wrong class (UI-PERF-002)::a device-class row with no measured frame work does not satisfy its budget",
     "perf_evidence::device budgets are unreachable from the wrong class (UI-PERF-002)::a device row whose frame work is blind does not error the budget check",
     "perf_evidence::device budgets are unreachable from the wrong class (UI-PERF-002)::a NaN or negative frame time is not a measurement",
     "perf_evidence::device budgets are unreachable from the wrong class (UI-PERF-002)::refuses without throwing on a hand-authored report that has no environment block",
     "perf_evidence::device budgets are unreachable from the wrong class (UI-PERF-002)::enforces a device budget only against rows of its own class",
     "perf_evidence::the frame ceiling is one-directional (UI-PERF-002)::derives the ceiling from the frame target and the declared UI share",
     "perf_evidence::the frame ceiling is one-directional (UI-PERF-002)::flags a scene above the ceiling separately from a trend regression",
     "perf_evidence::console / ten-foot profile (UI-PARADIGM-003, XP-A1)::is in the headless matrix",
     "perf_evidence::console / ten-foot profile (UI-PARADIGM-003, XP-A1)::applies the ten-foot facts, not just a large viewport",
     "perf_evidence::console / ten-foot profile (UI-PARADIGM-003, XP-A1)::declares a frame target for every profile, so a ceiling can be derived",
     "perf_evidence::physical-row provenance::accepts a row that carries everything its class implies",
     "perf_evidence::physical-row provenance::says nothing about a row that is not of a device class",
     "perf_evidence::physical-row provenance::refuses a physical row that carries a Studio version",
     "perf_evidence::physical-row provenance::refuses a physical row that names a device-simulator preset",
     "perf_evidence::physical-row provenance::refuses a physical row whose live facts came from the Studio simulator",
     "perf_evidence::physical-row provenance::refuses the Studio evidence level under a physical class",
     "perf_evidence::physical-row provenance::refuses a hand-written row with one number in it",
     "perf_evidence::physical-row provenance::refuses a row that carries no attestation at all",
     "perf_evidence::physical-row provenance::refuses an attestation with a field renamed rather than written",
     "perf_evidence::physical-row provenance::refuses an unmeasured or nonsense frame-work number",
     "perf_evidence::physical-row provenance::refuses the simulator scaling mode, the fingerprint that was left unread",
     "perf_evidence::physical-row provenance::refuses an attestation that disagrees with the row it sits on",
     "perf_evidence::physical-row provenance::catches the whole consistent relabel at once",
     "spatial::presentation facts (XP-D1)::defaults to a flat screen and never names a device",
     "spatial::presentation facts (XP-D1)::a billboard is still flat; only a world surface is not",
     "spatial::presentation facts (XP-D1)::clamps a malformed platform fact to screen rather than propagating it",
     "spatial::presentation facts (XP-D1)::spatial pointing is a capability among others, never a replacement mode",
     "spatial::normalized spatial event data (XP-D2)::normalizes a full platform event and derives the distance from the geometry",
     "spatial::normalized spatial event data (XP-D2)::clamps hostile platform data instead of erroring or propagating it",
     "spatial::normalized spatial event data (XP-D2)::an event with no spatial content is not a spatial event",
     "spatial::normalized spatial event data (XP-D2)::extending a 2D position leaves every 2D field exactly as it was",
     "spatial::normalized spatial event data (XP-D2)::every event Facet produces today is flat",
     "spatial::normalized spatial event data (XP-D2)::a 2D pointer handler written before spatial existed still receives its drag",
     "spatial::the world render target is declared AND shipped (XP-D3)::declares SurfaceGui, names its adapter, and still carries both halves",
     "spatial::the world render target is declared AND shipped (XP-D3)::the adapter exists and is required directly, never exported from the table",
     "spatial::the world render target is declared AND shipped (XP-D3)::every still-open question has a capability the adapter withholds",
     "spatial::the world render target is declared AND shipped (XP-D3)::AlwaysOnTop is pinned false and is not an option",
     "spatial::no VR support claim (XP-D5)::no shipped document claims VR support",
     "spatial::no VR support claim (XP-D5)::the platform playbook names the gates a claim would first have to pass",
     "spatial::no VR support claim (XP-D5)::the spatial module says so in its own header, where an implementer reads it",
     "matrix_rows::the canonical five rows (XP-B1)::declares exactly the five views the plan names, in order",
     "matrix_rows::the canonical five rows (XP-B1)::every row states what it cannot prove, where its result is read",
     "matrix_rows::the canonical five rows (XP-B1)::hard-codes no catalog id",
     "matrix_rows::phone rows: narrowest, and the SAME device twice (XP-B1)::ranks phones narrowest-then-smallest-canvas",
     "matrix_rows::phone rows: narrowest, and the SAME device twice (XP-B1)::selects by CANVAS, and says so — the weakest-hardware question is a physical row",
     "matrix_rows::phone rows: narrowest, and the SAME device twice (XP-B1)::the landscape row cannot resolve on its own — it must reuse the portrait device",
     "matrix_rows::tablet row: representative, and not a television (XP-B1)::excludes the low-density panel the catalog calls a Tablet",
     "matrix_rows::tablet row: representative, and not a television (XP-B1)::picks the median tablet by canvas area, not an extreme",
     "matrix_rows::desktop row: the project's standard viewport (XP-B1)::prefers the preset that is exactly the standard development viewport",
     "matrix_rows::console row: a television, not a handheld (XP-B1)::excludes the high-density handheld the catalog calls a Console",
     "matrix_rows::console row: a television, not a handheld (XP-B1)::offers every 1920x1080 television-density console, deterministically ordered",
     "matrix_rows::requirements are checked against the RUNNING session (XP-B1)::passes a healthy console observation",
     "matrix_rows::requirements are checked against the RUNNING session (XP-B1)::fails a console preset that never entered the ten-foot presentation",
     "matrix_rows::requirements are checked against the RUNNING session (XP-B1)::accepts the emulator's fitted viewport, but not a different one",
     "matrix_rows::requirements are checked against the RUNNING session (XP-B1)::fails a blind 1x1 viewport rather than reporting a layout",
     "matrix_rows::requirements are checked against the RUNNING session (XP-B1)::fails when the compact phone row is not compact",
     "error_boundary::error boundaries (§17 Phase 4)::renders its view when healthy; the fallback is not mounted",
     "error_boundary::error boundaries (§17 Phase 4)::a throwing view factory at mount swaps to the fallback; siblings survive",
     "error_boundary::error boundaries (§17 Phase 4)::a structural rebuild that throws INSIDE the boundary trips it at runtime",
     "error_boundary::error boundaries (§17 Phase 4)::boundary trips are registry-clean: dispose returns to baseline",
     "error_boundary::error boundaries (§17 Phase 4)::critical screen: a throwing screen presents its declared fallback screen",
     "error_boundary::error boundaries (§17 Phase 4)::a throwing row factory outside a boundary leaves the surviving tree consistent (ARCH-1)",
     "error_boundary::error boundaries (§17 Phase 4)::the row that threw is retried on the next update (ARCH-1)",
     "error_boundary::error boundaries (§17 Phase 4)::with a boundary the fallback path is unchanged (ARCH-1)",
     "error_boundary::error boundaries (§17 Phase 4)::outside a boundary the mount-time hard-error contract is unchanged",
     "sorted_entries::sortedEntries: the hazard it exists for::THE NEGATIVE CONTROL — the same twelve pairs iterate in DIFFERENT orders",
     "sorted_entries::sortedEntries: the hazard it exists for::the naive determinism check would PASS with pairs — repetition is the wrong axis",
     "sorted_entries::sortedEntries: the hazard it exists for::at SIX keys three of the four constructions agree — which is why this file uses twelve",
     "sorted_entries::sortedEntries: what it guarantees::every construction of the same dictionary yields ONE order",
     "sorted_entries::sortedEntries: what it guarantees::the values ride along, unchanged and not copied",
     "sorted_entries::sortedEntries: what it guarantees::numeric keys order NUMERICALLY, not lexicographically",
     "sorted_entries::sortedEntries: what it guarantees::mixed number and string keys get one total order, deterministically",
     "sorted_entries::sortedEntries: what it guarantees::an empty dictionary is an empty array, and the input is never mutated",
     "sorted_entries::sortedEntries: what it guarantees::each call returns a FRESH array — a caller may sort or splice it",
     "sorted_entries::sortedEntries: the comparator::a caller-supplied key comparator is honoured, and stays deterministic",
     "sorted_entries::sortedEntries: the comparator::the comparator receives KEYS, which is what makes the guarantee unconditional",
     "sorted_entries::sortedEntries: the comparator::a key type with no natural order is REFUSED at construction, naming the comparator",
     "sorted_entries::sortedEntries: the comparator::...and is accepted once the caller supplies one",
     "sorted_entries::sortedEntries: the comparator::a non-table dictionary and a non-function comparator are refused BY NAME",
     "sorted_entries::sortedEntries: through UI.ForEach::a dictionary signal drives a keyed region in one order whatever built it",
     "styling::engine styling: shadows and per-corner rounding (§17)::UI.shadow on an element lands as engine-true style data via the style authority",
     "styling::engine styling: shadows and per-corner rounding (§17)::a token preset shadow resolves from the style's shadow tokens",
     "styling::engine styling: shadows and per-corner rounding (§17)::shadows never enter layout: identical rects with and without a shadow",
     "styling::engine styling: shadows and per-corner rounding (§17)::uniform corners emit the alias form; distinct corners emit the per-corner form",
     "styling::engine styling: shadows and per-corner rounding (§17)::corner radii resolve token names from the active style",
     "styling::engine styling: shadows and per-corner rounding (§17)::mixing the alias and per-corner radii in one spec is a build error",
     "styling::engine styling: shadows and per-corner rounding (§17)::a positive shadow zIndex is a build error (engine: negative only)",
     "styling::engine styling: shadows and per-corner rounding (§17)::UI.styleGroup applies the modifier set to EACH element (group semantics)",
     "styling::engine styling: shadows and per-corner rounding (§17)::styling a container styles the container's own surface, not its children",
     "styling::engine styling: shadows and per-corner rounding (§17)::style lint warns on the jagged shadow+large-corner caveat and the shadow budget",
     "styling::engine styling: the per-view gradient modifier (rung 2)::normalizes to engine-true data: ordered stops, pinned endpoints, resolved tokens",
     "styling::engine styling: the per-view gradient modifier (rung 2)::is BOUNDED: one stop, four stops, an unordered ramp and an unknown token all refuse",
     "styling::engine styling: the per-view gradient modifier (rung 2)::keeps the SAME alpha ceiling the theme path enforces, for the same reason",
     "styling::engine styling: the per-view gradient modifier (rung 2)::UI.gradient lands on a Box as style data via the style authority",
     "styling::engine styling: the per-view gradient modifier (rung 2)::refuses a TEXT-BEARING node, because a wash multiplies its own glyphs",
     "styling::engine styling: the per-view gradient modifier (rung 2)::refuses a VALUE CONTROL's own chrome, the same wall themes.define puts up",
     "styling::engine styling: the per-view gradient modifier (rung 2)::never enters layout: identical rects with and without a gradient",
     "styling::engine styling: the per-view gradient modifier (rung 2)::UI.styleGroup enforces the SAME two gradient walls as UI.gradient",
     "styling::engine styling: the per-view gradient modifier (rung 2)::UI.styleGroup carries the gradient to every element of a collection",
     "styling::F-4 (BP-F1): UI.styleGroup is the WHOLE style-modifier family, and closed::carries `stroke`, the fourth family member, like the singular UI.stroke",
     "styling::F-4 (BP-F1): UI.styleGroup is the WHOLE style-modifier family, and closed::a stroked collection reaches the adapter under the style authority",
     "styling::F-4 (BP-F1): UI.styleGroup is the WHOLE style-modifier family, and closed::an unknown spec key is refused, naming the modifier and the legal set",
     "styling::F-4 (BP-F1): UI.styleGroup is the WHOLE style-modifier family, and closed::the four legal keys all still pass",
     "styling::F-5 (BP-F2): the shadow and gradient grammars are closed like the stroke one::a typo'd gradient field is refused instead of silently producing the default",
     "styling::F-5 (BP-F2): the shadow and gradient grammars are closed like the stroke one::a typo'd shadow field is refused",
     "styling::F-5 (BP-F2): the shadow and gradient grammars are closed like the stroke one::every LEGAL field of both grammars still passes",
     "styling::F-5 (BP-F2): the shadow and gradient grammars are closed like the stroke one::the modifier family the normalizers define is the family the manifest and the factories carry",
     "styling::F-5 (BP-F2): the shadow and gradient grammars are closed like the stroke one::EVERY derived modifier survives UI.styleGroup and lands on the node",
     "styling::F-5 (BP-F2): the shadow and gradient grammars are closed like the stroke one::a key outside the derived family is refused by name",
     "styling::F-5 (BP-F2): the shadow and gradient grammars are closed like the stroke one::the preset-NAME form of a shadow still works on both shipped styles",
     "sheet_model::sheet_model.build::is deterministic (same stamp for same inputs)",
     "sheet_model::sheet_model.build::Dark theme tokens equal the default style values exactly",
     "sheet_model::sheet_model.build::precomputes the accent interaction tokens with the edge's exact lerp",
     "sheet_model::sheet_model.build::every theme passes the contrast gate (and a failing theme is rejected)",
     "sheet_model::sheet_model.build::orders state rules per the measured cascade (insertion order wins)",
     "sheet_model::sheet_model.build::gates hover rules on the pointer-live tag (touch can never match)",
     "sheet_model::sheet_model.build::resolves every $token reference in every theme",
     "sheet_model::sheet_model.build::names rules and tokens for humans (Style Editor surface)",
     "sheet_model::sheet_model.build::mirrors the layout-affecting tokens completely (read-only reference)",
     "sheet_model::sheet_model.build::declares transitions only on state rules, with known motion tokens",
     "sheet_model::sheet_model.build::classifyTags: the pure hint->tag mapping the native adapter applies",
     "sheet_model::sheet_model.build::a tint's FILL rides a tag rule, because a default-valued write never claims",
     "sheet_model::sheet_model.build::builds a single-theme sheet from a custom game style (default-flip path)",
     "sheet_model::sheet_model.build::exports the frozen tag vocabulary the adapter classifies with",
     "sheet_model::build: the value-control own-paint family (RS-A16-D4)::selects every OWN_PAINT slot with a SOLID fill, in the base seed",
     "sheet_model::build: the value-control own-paint family (RS-A16-D4)::paints the token the BESPOKE path writes for the same slot",
     "sheet_model::build: the value-control own-paint family (RS-A16-D4)::gives each slot the phantom corner and hairline the package path gives it",
     "sheet_model::build: the value-control own-paint family (RS-A16-D4)::pre-arms the skinned suppression, so a rung-2 override has a rule with NO package",
     "sheet_model::build: the value-control own-paint family (RS-A16-D4)::orders paint, then suppression, inside the same cascade window as the package path",
     "sheet_model::build: the value-control own-paint family (RS-A16-D4)::emits each of the twelve names exactly ONCE (a rule name is identity)",
     "sheet_model::build: the value-control own-paint family (RS-A16-D4)::stays a LEGACY-shaped rule: no group, no priority",
     "sheet_model::buildPackage: a rule NAME is identity, so the vocabulary is shared (C16)::emits the legacy class-default names, never the engine class names",
     "sheet_model::buildPackage: a rule NAME is identity, so the vocabulary is shared (C16)::every rule the legacy builder names is named identically by buildPackage",
     "sheet_model::buildPackage: a rule NAME is identity, so the vocabulary is shared (C16)::emits the tint fill in the same cascade position as the legacy builder",
     "sheet_model::buildPackage: a rule NAME is identity, so the vocabulary is shared (C16)::ONE emitter: the value-slot family is byte-identical in both builders (RS-A16-D4)",
     "sheet_model::buildPackage: a rule NAME is identity, so the vocabulary is shared (C16)::the class-default vocabulary is exported as one table",
     "sheet_model::buildPackage: the chrome text lift paints on every skin that lifts (C17)::a SELECTION-only skin still emits the lift rules (it lifts TextButton labels)",
     "sheet_model::buildPackage: the chrome text lift paints on every skin that lifts (C17)::a flat theme emits none of them",
     "sheet_model::buildPackage: the chrome text lift paints on every skin that lifts (C17)::the parent-role variants reach the lifted label through a child combinator",
     "sheet_model::buildPackage: the chrome text lift paints on every skin that lifts (C17)::the editing yield goes FULLY transparent so the caret and text show",
     "sheet_model::buildPackage: an author rule earns chrome scope by SELECTOR (C20)::rejects an image property on a rule that names no chrome slot",
     "sheet_model::buildPackage: an author rule earns chrome scope by SELECTOR (C20)::accepts the same property on a rule that does name one",
     "sheet_model::buildPackage: an author rule earns chrome scope by SELECTOR (C20)::an author rule with a plain paint property is always fine",
     "sheet_model::RS-DIR10: a phantom ::UIStroke strokes the TEXT unless the rule says Border::every ::UIStroke rule the base seed emits carries ApplyStrokeMode = Border",
     "sheet_model::RS-DIR10: a phantom ::UIStroke strokes the TEXT unless the rule says Border::every ::UIStroke rule a package emits carries ApplyStrokeMode = Border",
     "sheet_model::RS-DIR10: a phantom ::UIStroke strokes the TEXT unless the rule says Border::ApplyStrokeMode is a legal phantom property, so the rule cannot be inert",
     "sheet_model::RS-DIR11: the accent surface keeps the corner every other surface has::the base seed emits Primary button — corner at radii.control",
     "sheet_model::RS-DIR11: the accent surface keeps the corner every other surface has::a package emits it too, from ITS radii, and a live metric edit repaints it",
     "sheet_model::RS-DIR11: the accent surface keeps the corner every other surface has::it sits with the other surface corners, so the skinned suppression still wins",
     "sheet_model::RS-DIR8: a selected surface pairs with a content colour that can be read on it::selectedContentColor prefers content and switches only when content fails the floor",
     "sheet_model::RS-DIR8: a selected surface pairs with a content colour that can be read on it::$OnSelected is a real token on every theme of both builders",
     "sheet_model::RS-DIR8: a selected surface pairs with a content colour that can be read on it::the lifted label of a SELECTED skinned node resolves it",
     "sheet_model::RS-DIR8: a selected surface pairs with a content colour that can be read on it::the UNSKINNED selected row resolves the same token in both builders",
     "sheet_model::RS-DIR8 sweep: every reference package's selected state is readable::a readable content role exists on every theme of every reference package",
     "sheet_model::RS-DIR8 sweep: every reference package's selected state is readable::the flat default is untouched: Studio Neutral still reads $Content",
     "native_style_scenario::native_style scenario (headless conformance)::mounts through the real presenter and emits every classification input",
     "native_style_scenario::native_style scenario (headless conformance)::state steps flip the bindings the adapter classifies from",
     "native_style_scenario::native_style scenario (headless conformance)::designer-edit and theme steps route through the runner-owned seams",
     "native_style_scenario::native_style: an enum can never be a theme-sheet attribute::refuses the enum kind by name instead of throwing from inside SetAttribute",
     "native_style_scenario::native_style: an enum can never be a theme-sheet attribute::no theme token the model builder emits is an enum (enums are rule props only)",
     "native_style_scenario::screen_target's chrome text lift: the source contract::adapter.remove destroys the lifted label (B13)",
     "native_style_scenario::screen_target's chrome text lift: the source contract::root teardown destroys every lifted label too (B13)",
     "native_style_scenario::screen_target's chrome text lift: the source contract::the fallback editing yield goes FULLY transparent (B14)",
     "native_style_scenario::screen_target's chrome text lift: the source contract::text SHAPE is mirrored onto the lifted label, and TextScaled is documented out (B15)",
     "native_style_scenario::screen_target's chrome text lift: the source contract::the theme sheet is relinked across EVERY live root, pruning destroyed ones (B12)",
     "native_style_scenario::screen_target: the image IS the button (source contract)::the skinned-slot tag rides the SAME desired set syncTags diffs against",
     "native_style_scenario::screen_target: the image IS the button (source contract)::fallback mode explicit-writes the same three suppressions the rules make",
     "native_style_scenario::screen_target: the image IS the button (source contract)::it re-suppresses after applySurface, which repaints a plate on every branch",
     "native_style_scenario::screen_target: the image IS the button (source contract)::bespoke interaction fills tint the DECORATION instead of the suppressed plate",
     "native_style_scenario::screen_target: the image IS the button (source contract)::the slot shadow names its authority and rides the capability probe",
     "native_style_scenario::screen_target: the image IS the button (source contract)::the decoration HINT arrives through create, not through a later setter",
     "native_style_scenario::screen_target: the image IS the button (source contract)::a per-view override is explicit-written and never tagged",
     "native_style_scenario::screen_target: the focus treatment (source contract)::the ring is no longer painted from a captured constant",
     "native_style_scenario::screen_target: the focus treatment (source contract)::a live theme swap repaints whatever is focused RIGHT NOW",
     "native_style_scenario::screen_target: the focus treatment (source contract)::the glow is a named UIShadow, capability-gated, and degrades to the ring",
     "native_style_scenario::screen_target: the focus treatment (source contract)::blur removes the glow and forgets the handle, so nothing can stick",
     "native_style_scenario::screen_target: the focus treatment (source contract)::the census exposes the live glow count independently of the ledger",
     "native_style_scenario::screen_target: a value control's own paint (source contract)::the slot identity tag rides the desired set, independent of skinning",
     "native_style_scenario::screen_target: a value control's own paint (source contract)::fallback mode writes the same SOLID fill the rules paint",
     "native_style_scenario::screen_target: a value control's own paint (source contract)::the tag sync is NOT behind the decoration branch (the invisible-thumb bug)",
     "native_style_scenario::screen_target: a value control's own paint (source contract)::it paints BEFORE the skinned suppression, never after",
     "native_style_scenario::screen_target: the toggle reads the live palette (source contract)::BOTH write sites go through the resolver, and neither keeps a constant",
     "native_style_scenario::screen_target: the toggle reads the live palette (source contract)::the resolver falls back to today's constants when no package is installed",
     "native_style_scenario::screen_target: the toggle reads the live palette (source contract)::a live swap repaints a mounted toggle without touching its value",
     "native_style_scenario::screen_target: the toggle reads the live palette (source contract)::a recolour cancels the in-flight tween and snaps, so no stale target lands",
     "native_style_default::the library default paint path::the default IS sheet paint — the flag itself, read off the module",
     "native_style_default::the library default paint path::NO opt resolves to the library default, rather than to `false`",
     "native_style_default::the library default paint path::an explicit `false` STILL refuses sheet paint — the per-target escape hatch",
     "native_style_default::the library default paint path::an explicit `true` or a config table passes through UNTOUCHED",
     "native_style_default::the library default paint path::the resolver NEVER answers `nil`, so one comparison decides the path",
     "native_style_default::the library default paint path::`screen_target` OBEYS the plan: it never touches the opt at all",
     "native_style_default::the library default paint path::the two exemptions really pass an explicit choice, so the default cannot sweep them",
     "native_style_default::the library default paint path::the flip is RECORDED in the changelog, which is what makes it legal",
     "native_style_default::the plan the adapter obeys::NO opt plans SHEET PAINT — the default, decided here and nowhere else",
     "native_style_default::the plan the adapter obeys::an explicit `false` plans the EXPLICIT-WRITE path",
     "native_style_default::the plan the adapter obeys::no engine capability refuses even an explicit opt-in",
     "native_style_default::the plan the adapter obeys::the plan carries the RESOLVED opt, and it cannot say whether one was passed",
     "native_style_default::the plan the adapter obeys::the sheet's HOST and THEME pass through by identity",
     "native_style_default::the plan the adapter obeys::a bare `true` names no host or theme, so `ensure` takes its own defaults",
     "native_style_default::the plan the adapter obeys::the plan carries no `handle` and no `model` — those are the caller's objects",
     "native_style_default::the plan the adapter obeys::transitions are OPT-IN, and reduced motion wins over the opt",
     "native_style_default::the plan the adapter obeys::the plan is NEVER nil and always answers `native`, so one test decides the branch",
     "native_style_default::the plan the adapter obeys::the engine probe is consulted when the caller states no capability",
     "native_style_default::the plan the adapter obeys::an explicit `false` refuses BEFORE the engine is probed",
     "native_style_default::the gallery A/B driver's precedence survives the flip::NEITHER SET -> no opt, so the place takes the LIBRARY default",
     "native_style_default::the gallery A/B driver's precedence survives the flip::FORCED FALLBACK WINS OVER EVERYTHING — including an explicit opt-in",
     "native_style_default::the gallery A/B driver's precedence survives the flip::an explicit opt-in is REDUNDANT-BUT-HARMLESS: it still carries transitions",
     "native_style_default::the gallery A/B driver's precedence survives the flip::the transitions attribute alone opts in, because it cannot mean anything else",
     "native_style_default::the gallery A/B driver's precedence survives the flip::the bootstrap reads each attribute once, into the decision",
     "api_surface::semantic versioning and deprecation::VERSION is a plain semver MAJOR.MINOR.PATCH (scaffold suffixes are gone)",
     "api_surface::semantic versioning and deprecation::the published policy exists and states the rules the ledger is built on",
     "api_surface::semantic versioning and deprecation::deprecations are a machine-readable ledger with the policy's required fields",
     "api_surface::semantic versioning and deprecation::the ledger is the UNION of schema-generated and declared entries",
     "api_surface::semantic versioning and deprecation::every entry gives at least one MINOR of notice, and the ledger is frozen",
     "api_surface::the reference marks what the ledger deprecates (ENF-3)::api.md mentions every deprecated surface, and says 'deprecated' beside it",
     "api_surface::public type re-exports (F-28)::init.luau names the core contract types it re-exports",
     "api_surface::public type re-exports (F-28)::requiring the contract keeps the entry point safe for shared/server graphs",
     "api_surface::the public SHAPE a breaking change moves::no prop has flipped to REQUIRED without a record",
     "api_surface::the public SHAPE a breaking change moves::no documented default has changed VALUE without a record",
     "api_surface::the public SHAPE a breaking change moves::every surface the register records as breaking is really in the pinned shape",
     "extension_checker::registration checker (agentic maintainability)::the live repository passes every registration rule",
     "extension_checker::registration checker (agentic maintainability)::reports a control file missing from the registry",
     "extension_checker::registration checker (agentic maintainability)::reports an export missing from the API reference AND a documented ghost",
     "extension_checker::registration checker (agentic maintainability)::reports a nested namespace member that api.md does not document QUALIFIED",
     "extension_checker::registration checker (agentic maintainability)::an allowlist entry needs a REASON — an empty one is itself a problem",
     "extension_checker::registration checker (agentic maintainability)::the live repository's namespace members are actually WALKED (not an empty set)",
     "extension_checker::registration checker (agentic maintainability)::reports a dumpFunction that is not defined in the module",
     "extension_checker::registration checker (agentic maintainability)::reports a dumpFunction that is not a dump DEFINITION at all (the old substring hole)",
     "extension_checker::registration checker (agentic maintainability)::reports a row that declares neither a dumpFunction nor dump = false",
     "extension_checker::registration checker (agentic maintainability)::reports dump = false with no reason, and accepts one with a reason",
     "extension_checker::registration checker (agentic maintainability)::reports a spec file that is not registered in the runner (silent zero)",
     "extension_checker::registration checker (agentic maintainability)::reports a missing extension playbook",
     "extension_checker::registration checker (agentic maintainability)::reports a row that declares no inputProofs at all (silent omission = failure)",
     "extension_checker::registration checker (agentic maintainability)::reports an interactive control that is mouse-only (missing an input class)",
     "extension_checker::registration checker (agentic maintainability)::reports an interactive control that declared inputProofs = false",
     "extension_checker::registration checker (agentic maintainability)::reports a declared proof case that no registered spec contains (rule b)",
     "extension_checker::registration checker (agentic maintainability)::the live repository proves the four-input story for every interactive control",
     "extension_checker::registration checker (agentic maintainability)::reports an interactive control that proves inputs but is MISSING an affordance class",
     "extension_checker::registration checker (agentic maintainability)::reports an interactive control that declares no affordanceProofs at all (silent omission)",
     "extension_checker::registration checker (agentic maintainability)::reports an interactive control that declared affordanceProofs = false",
     "extension_checker::registration checker (agentic maintainability)::reports an affordance proof case that no registered spec contains (rule b, paradigm)",
     "extension_checker::registration checker (agentic maintainability)::reports an interactive control whose affordanceProofs omits the hotSwitch decision",
     "extension_checker::registration checker (agentic maintainability)::the live repository proves the PARADIGM axis for every interactive control",
     "extension_checker::extension scaffold::plans a complete control skeleton: source, spec, runner registration, registry entry, docs stub",
     "extension_checker::extension scaffold::stamps the PARADIGM axis: an affordance-declaration block, four affordance cases, a hot-switch stub, and affordanceProofs citing them",
     "extension_checker::extension scaffold::plans a render-target adapter skeleton against the target contract",
     "extension_checker::extension scaffold::stamps the adapter's conformance spec AND registers it in the runner (SEAM-25)",
     "extension_checker::extension scaffold::applying the registry edit lands the row INSIDE the registry array (dry-run defect 1)",
     "extension_checker::extension scaffold::stamps EVERY registration a twentieth control needs, from the one declared name",
     "extension_checker::extension scaffold::every late-registration anchor is REAL: each edit applies to the shipped file, twice safely",
     "extension_checker::extension scaffold::the scenario, the demo, and the pinned root all name the SAME surface",
     "extension_checker::extension scaffold::applyBump moves every occurrence of a count pin, exactly once",
     "extension_checker::extension scaffold::the stamped output is formatter-clean: no line over 120 columns, no double blank line",
     "extension_checker::extension scaffold::rejects unknown kinds and unsafe names",
     "examples_gallery::examples/gallery 01: temperature converter (numeric TextInput)::mounts headlessly with no core error",
     "examples_gallery::examples/gallery 01: temperature converter (numeric TextInput)::the numeric field is the primary affordance: no stepper or Convert buttons remain",
     "examples_gallery::examples/gallery 01: temperature converter (numeric TextInput)::LIVE mode: typing a valid number updates the Celsius preview WITHOUT committing the Result",
     "examples_gallery::examples/gallery 01: temperature converter (numeric TextInput)::COMMIT mode: Enter commits the conversion into the Result (onCommit path)",
     "examples_gallery::examples/gallery 01: temperature converter (numeric TextInput)::COMMIT mode: focus loss (tapping away) also commits",
     "examples_gallery::examples/gallery 01: temperature converter (numeric TextInput)::the validate hook rejects non-numeric characters (value unchanged, no live update)",
     "examples_gallery::examples/gallery 01: temperature converter (numeric TextInput)::per-input activation: pointer tap and touch tap each enter edit mode",
     "examples_gallery::examples/gallery 01: temperature converter (numeric TextInput)::per-input activation: keyboard Activate (Return) on the focused field enters edit mode",
     "examples_gallery::examples/gallery 01: temperature converter (numeric TextInput)::per-input activation: gamepad A enters edit mode, B cancels reverting to the snapshot",
     "examples_gallery::examples/gallery 01: temperature converter (numeric TextInput)::Clear returns the field, the live preview and the committed result to the start",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::filter-as-you-type narrows the rows REACTIVELY, and clearing restores them all",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::case-insensitive match; surviving rows keep their mounted identity (no remount)",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::the in-field clear (✕) exists ONLY while the filter is non-empty",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::the handshake: typing in the filter field never navigates the table or moves selection",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::reordering is DISABLED while a filter is active (the media-library convention): the base order is preserved",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::per-input activation: keyboard/gamepad Activate on the focused filter field enters edit mode",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::mounts an iTunes-style table: name and rating columns aligned under their headers",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::the bootstrap auto-bind reaches a returned Table's api.bindNativeScroll",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::RULING 3: scrolling the PAGE closes a swiped-open tray on the shipped playlist",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::the bootstrap's auto-bind probes BOTH spellings, in both loops (the source is the contract)",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::tapping the rating INSIDE a table cell sets that track's rating and repaints without remount",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::the stars are glyphs, so NO theme can paint a plate behind one",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::multi-select on touch: EDIT MODE is where taps select, and both rows stay selected (additive toggle)",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::onPrimaryAction: a DOUBLE-click plays the track and the page says so; a single click only selects",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::onPrimaryAction: two clicks SLOWER than the double-click window play nothing",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::onPrimaryAction: on TOUCH the FIRST plain tap plays the track (the compact-touch rule)",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::Restore puts the playlist back INCLUDING what was playing",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::scrubbing left/right across the stars sets the rating live (pointer drag)",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::touch input auto-shows the Edit toggle; edit mode + handle drag reorders on touch",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::GAMEPAD end to end: d-pad navigation, A to rate, grab-move-drop to reorder",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::GAMEPAD: selection follows focus across rows, even when focus sits on a star",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::KEYBOARD: a swiped-open tray is REACHABLE on the shipped playlist — Tab lands on Remove and fires it",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::GAMEPAD: the d-pad walks PAST the rating and into the swiped-open tray (ruling 9)",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::drag & drop rearranges the playlist rows (post-removal reorder contract)",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::Restore puts the original order and every original rating back",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::PHONE (no mouse capability): Edit mounts the ≡ gutter, a handle drag reorders, and a tap selects",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::TRAILING swipe reveals Remove OUTSIDE edit mode, and firing it drops the track",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::LEADING swipe reveals Top OUTSIDE edit mode, and firing it moves the track to the front",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::a swipe never plays the track, whichever side of InputEnded its Activate lands (touch)",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::a swipe never plays the track, whichever side of InputEnded its Activate lands (mouse)",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::a plain TAP still plays the track and opens no tray — the two gestures do not fight",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::edit mode ADDS the ⊖, and tapping it REMOVES the row (the director's device scenario)",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::GAMEPAD in edit mode: Left selects THIS row's ⊖, and A removes THIS track",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::edit mode is diagnostic-clean at the NARROWEST swept viewport, in both orientations",
     "examples_gallery::examples/gallery 02: playlist table (director revision: rating + browser combined)::Restore brings a REMOVED track back (the way out of the destructive action)",
     "examples_gallery::examples/gallery 04: settings sync::mounts headlessly with no core error",
     "examples_gallery::examples/gallery 04: settings sync::every state the tutorial promises has an on-screen control",
     "examples_gallery::examples/gallery 04: settings sync::the seven promised states are reachable through on-screen controls alone",
     "examples_gallery::examples/gallery 04: settings sync::the status line names the next action at every step",
     "examples_gallery::examples/gallery 04: settings sync::a valid toggle applies optimistically, goes pending, then confirms",
     "examples_gallery::examples/gallery 04: settings sync::a rejected change restores the optimistic draft to server truth",
     "examples_gallery::examples/gallery 04: settings sync::reset recovers from a request the server never answered",
     "examples_gallery::examples/gallery 04: settings sync::a second change while one is in flight is refused visibly, and never throws",
     "examples_gallery::examples/gallery 04: settings sync::Deliver with nothing in flight explains itself instead of doing nothing",
     "examples_gallery::examples/gallery 04: settings sync::the visible history reads newest first and is bounded",
     "examples_gallery::examples/gallery 04: settings sync::stays laid out and operable on a compact phone at every text size",
     "examples_gallery::examples/gallery 04: settings sync::✓ ex03 touch: a touch tap on the Music toggle applies optimistically and goes pending",
     "examples_gallery::examples/gallery 04: settings sync::✓ ex03 touch: the whole accept round trip is reachable by touch alone",
     "examples_gallery::examples/gallery 04: settings sync::✓ ex03 keyboard: Return on the focused Music toggle applies optimistically",
     "examples_gallery::examples/gallery 04: settings sync::✓ ex03 keyboard: the arrows reach Deliver, and Return there completes the round trip",
     "examples_gallery::examples/gallery 04: settings sync::✓ ex03 gamepad: ButtonA on the focused Music toggle applies optimistically",
     "examples_gallery::examples/gallery 04: settings sync::✓ ex03 gamepad: D-pad reaches the volume steppers and ButtonA nudges the volume",
     "examples_gallery::examples/gallery 05: confirmation dialog::mounts headlessly with no core error",
     "examples_gallery::examples/gallery 05: confirmation dialog::opens a modal that traps focus, and ButtonB dismisses it and restores focus",
     "examples_gallery::examples/gallery 05: confirmation dialog::gamepad navigation to Cancel + activate reports a cancel and dismisses",
     "examples_gallery::examples/gallery 05: confirmation dialog::✓ ex04 pointer: tapping Delete opens the modal; tapping Cancel records cancelled and dismisses",
     "examples_gallery::examples/gallery 05: confirmation dialog::✓ ex04 touch: a touch tap opens the modal and a touch tap on Confirm records confirmed",
     "examples_gallery::examples/gallery 05: confirmation dialog::confirming CHANGES THE SCREEN the player is looking at",
     "examples_gallery::examples/gallery 05: confirmation dialog::cancelling says the save was KEPT, which is a different visible answer",
     "examples_gallery::examples/gallery 05: confirmation dialog::Restore puts the save back, so the demonstration is repeatable in place",
     "examples_gallery::examples/gallery 05: confirmation dialog::the card is CENTRED, not parked in the top-left corner",
     "examples_gallery::examples/gallery 05: confirmation dialog::tapping Delete twice does not stack two identical modals",
     "examples_gallery::examples/gallery fixture: card rail (newVirtualList axis = \"x\")::mounts under the showcase's own chrome with ZERO solver diagnostics",
     "examples_gallery::examples/gallery fixture: card rail (newVirtualList axis = \"x\")::windows a full-WIDTH canvas: 400 cards, a handful mounted, the rest not built",
     "examples_gallery::examples/gallery fixture: card rail (newVirtualList axis = \"x\")::the rail's width is re-derived from the viewport, never a constant",
     "examples_gallery::examples/gallery fixture: card rail (newVirtualList axis = \"x\")::keyboard: Left/Right step the rail — and Up/Down do nothing at all",
     "examples_gallery::examples/gallery fixture: card rail (newVirtualList axis = \"x\")::gamepad: the D-pad steps it sideways and A picks the focused card",
     "examples_gallery::examples/gallery fixture: card rail (newVirtualList axis = \"x\")::keyboard: Return picks the focused card, and the surface SAYS which one",
     "examples_gallery::examples/gallery fixture: card rail (newVirtualList axis = \"x\")::pointer and touch: a tap picks the card it landed on",
     "examples_gallery::examples/gallery fixture: card rail (newVirtualList axis = \"x\")::binds its own engine-scroll mirror with no scripted step (the showcase path)",
     "examples_gallery::examples/gallery fixture: card rail (newVirtualList axis = \"x\")::a sideways engine scroll slides the window along X",
     "examples_gallery::examples/gallery fixture: card rail (newVirtualList axis = \"x\")::keep-visible scrolls a far-off card into the band on the X axis",
     "examples_gallery::examples/gallery fixture: card rail (newVirtualList axis = \"x\")::its chrome copy survives a 1.4x pseudo-localization without clipping",
     "examples_gallery::examples/gallery fixture: withAnimation + reduced motion::mounts under the showcase's own chrome with ZERO solver diagnostics",
     "examples_gallery::examples/gallery fixture: withAnimation + reduced motion::one press, four consequences: something moves, something GROWS, something appears, something re-orders",
     "examples_gallery::examples/gallery fixture: withAnimation + reduced motion::the BADGE fades, shrinks and turns — on the puck's own spring, and its BOX never moves",
     "examples_gallery::examples/gallery fixture: withAnimation + reduced motion::REDUCED MOTION lands the badge's three values instantly, and never deletes one",
     "examples_gallery::examples/gallery fixture: withAnimation + reduced motion::the CARD's painted height travels — and the pip inside it does not move or stretch",
     "examples_gallery::examples/gallery fixture: withAnimation + reduced motion::the card rides the SAME spring as the puck — one call, one curve",
     "examples_gallery::examples/gallery fixture: withAnimation + reduced motion::the solver stays exact and instant; only the PAINT travels",
     "examples_gallery::examples/gallery fixture: withAnimation + reduced motion::ONE shared spring: the flights start together, run together and land together",
     "examples_gallery::examples/gallery fixture: withAnimation + reduced motion::the appearing panel installs no record of its own — that is the transition system's job",
     "examples_gallery::examples/gallery fixture: withAnimation + reduced motion::the control is a SEGMENTED Full | Reduced, and the words 'On'/'Off' appear nowhere",
     "examples_gallery::examples/gallery fixture: withAnimation + reduced motion::Reduced removes the DECORATIVE travel entirely — layout exact, no records, nothing in flight",
     "examples_gallery::examples/gallery fixture: withAnimation + reduced motion::...and the INFORMATIONAL half keeps running under Reduced, quantized rather than deleted",
     "examples_gallery::examples/gallery fixture: withAnimation + reduced motion::four inputs reach the demonstration: pointer, touch, keyboard and gamepad",
     "examples_gallery::examples/gallery fixture: withAnimation + reduced motion::carries the INDETERMINATE pair, and reduced motion steps them instead of stopping them",
     "examples_gallery::examples/gallery fixture: withAnimation + reduced motion::puts the environment back when it is dismissed",
     "examples_gallery::examples/gallery fixture: withAnimation + reduced motion::its copy survives a 1.4x pseudo-localization without clipping",
     "examples_gallery::examples/gallery fixture: circular progress, both forms::mounts under the showcase's own chrome with ZERO solver diagnostics",
     "examples_gallery::examples/gallery fixture: circular progress, both forms::both forms are on screen: a filled ring, a rotating head, and the dot spinner",
     "examples_gallery::examples/gallery fixture: circular progress, both forms::the steps drive the ring's sweep, and the arc follows the value",
     "examples_gallery::examples/gallery fixture: circular progress, both forms::the indeterminate head TRAVELS when the surface's clock advances",
     "examples_gallery::examples/gallery fixture: circular progress, both forms::its copy survives a 1.4x pseudo-localization without clipping",
     "examples_gallery::examples/gallery fixture: all controls — the round-2 layout vocabulary::mounts under the showcase's own chrome with ZERO solver diagnostics",
     "examples_gallery::examples/gallery fixture: all controls — the round-2 layout vocabulary::§2.6 `distribute = spaceBetween` puts the leftover in the GAPS: first at the start edge, last flush to the end",
     "examples_gallery::examples/gallery fixture: all controls — the round-2 layout vocabulary::§2.4 with room, nothing shrinks: all three bars are at their natural 140",
     "examples_gallery::examples/gallery fixture: all controls — the round-2 layout vocabulary::§2.4 when the axis is short the LOWEST tier gives way first, and a weightless child never gives at all",
     "examples_gallery::examples/gallery fixture: all controls — the round-2 layout vocabulary::§2.1 `lineAlign = end` sits the short bar on the BOTTOM of a line its siblings set",
     "examples_gallery::examples/gallery fixture: all controls — the round-2 layout vocabulary::§2.5 `containerRelativeFrame` measures against the SCROLLER's viewport, not the parent column",
     "examples_gallery::examples/gallery fixture: all controls — the round-2 layout vocabulary::§2.5 ...and on a desktop that answer DIFFERS from a third of the parent column",
     "examples_gallery::examples/gallery fixture: all controls — the round-2 layout vocabulary::§2.2 a Grid of `UI.GridRow`s aligns its columns across rows, and `gridSpan` covers both plus the gap",
     "examples_gallery::examples/gallery fixture: all controls — the round-2 layout vocabulary::§3.3 `UI.sensoryFeedback` emits the declared verb on the presenter's bus, stamped with the mounted path",
     "examples_gallery::examples/gallery fixture: variable item extents::mounts under the showcase's own chrome with ZERO solver diagnostics",
     "examples_gallery::examples/gallery fixture: variable item extents::the posts really are DIFFERENT heights, and none overlaps its neighbour",
     "examples_gallery::examples/gallery fixture: variable item extents::THE TOGGLE IS THE ARGUMENT: one declared height for all of them is a LIE, and the solver says so",
     "examples_gallery::examples/gallery fixture: variable item extents::the four measured text offsets grow every row, and the list stays virtualized",
     "examples_gallery::examples/gallery fixture: variable item extents::growing the text while scrolled keeps the SAME post under the top edge",
     "examples_gallery::examples/gallery fixture: flow wrap (UI.HStack wrap = true)::mounts under the showcase's own chrome with ZERO solver diagnostics",
     "examples_gallery::examples/gallery fixture: flow wrap (UI.HStack wrap = true)::the tags really WRAP: more than one distinct row of y at every viewport",
     "examples_gallery::examples/gallery fixture: flow wrap (UI.HStack wrap = true)::...and NOTHING in a line overlaps its neighbour, at every viewport",
     "examples_gallery::examples/gallery fixture: flow wrap (UI.HStack wrap = true)::THE TOGGLE IS THE ARGUMENT: wrap off overflows, wrap on does not — same nodes",
     "examples_gallery::examples/gallery fixture: flow wrap (UI.HStack wrap = true)::`align` moves the BLOCK of lines, and only the block",
     "examples_gallery::examples/gallery fixture: flow wrap (UI.HStack wrap = true)::pseudo-localized (~1.4x), it still wraps, still fits, and still clips nothing",
     "examples_gallery::examples/gallery fixture: lifecycle hooks and hidden::mounts under the showcase's own chrome with ZERO solver diagnostics",
     "examples_gallery::examples/gallery fixture: lifecycle hooks and hidden::THE TWO PANELS ARE THE ARGUMENT: one caption moves, the other does not",
     "examples_gallery::examples/gallery fixture: lifecycle hooks and hidden::the kept slot stops painting and stops taking taps, and comes back",
     "examples_gallery::examples/gallery fixture: lifecycle hooks and hidden::the log and the counters are the only evidence a lifetime hook ran",
     "examples_gallery::examples/gallery fixture: lifecycle hooks and hidden::teardown fires the badge's own disappear, so the count is never left short",
     "examples_gallery::examples/gallery fixture: lifecycle hooks and hidden::pseudo-localized (~1.4x), every line still reads whole and clips nothing",
     "examples_gallery::examples/gallery fixture: what a label gives up first::mounts under the showcase's own chrome, and only the LAST stop files a finding",
     "examples_gallery::examples/gallery fixture: what a label gives up first::THE ORDER IS THE ARGUMENT: it says it shorter BEFORE anything is cut",
     "examples_gallery::examples/gallery fixture: what a label gives up first::the widest stop gives nothing up at all, so the sequence has a top",
     "examples_gallery::examples/gallery fixture: what a label gives up first::nothing ever leaves the row, at any stop, at any viewport",
     "examples_gallery::examples/gallery fixture: what a label gives up first::the lower panel is ruling 5 on its own: it wraps until it cannot, then cuts",
     "examples_gallery::examples/gallery fixture: what a label gives up first::pseudo-localized (~1.4x), the row still keeps every label inside its box",
     "gallery_theme_picker::theme picker: the listing a player reads::puts Studio Neutral first, always, and sorts the rest by display name",
     "gallery_theme_picker::theme picker: the listing a player reads::carries each package's own themes as the second level",
     "gallery_theme_picker::theme picker: the listing a player reads::keeps the first module that claims an id — two rows would fight over one sheet",
     "gallery_theme_picker::theme picker: the listing a player reads::ignores anything under the folder that is not a compiled package",
     "gallery_theme_picker::theme picker: the listing a player reads::starts on what the gallery is already showing, so it never lies about state",
     "gallery_theme_picker::theme picker: what a click asks the controller to do::selecting a different package swaps to THAT package's own default theme",
     "gallery_theme_picker::theme picker: what a click asks the controller to do::selecting the package already installed is a NO-OP, not a redundant swap",
     "gallery_theme_picker::theme picker: what a click asks the controller to do::an unknown package id changes nothing",
     "gallery_theme_picker::theme picker: what a click asks the controller to do::selecting a theme of the active package swaps within it",
     "gallery_theme_picker::theme picker: what a click asks the controller to do::a theme the active package never declared is refused HERE, not thrown there",
     "gallery_theme_picker::theme picker: what a click asks the controller to do::the second level follows the selection",
     "gallery_theme_picker::theme picker: which packages a director can actually reach::lists every shipping reference package, and none of the fixtures",
     "gallery_theme_picker::theme picker: which packages a director can actually reach::isPickable reads the MODULE, because testOnly is a property of the file",
     "gallery_theme_picker::theme picker: which packages a director can actually reach::the ornate package carries two themes into the second level",
     "gallery_theme_picker::theme picker: it stays out of the library::is an EXAMPLE and reaches the framework only through public surface",
     "gallery_theme_picker::theme picker: composed pieces carry their own handlers::the COMPOSED chip hands the ladder the whole name, exactly as the demo chip does",
     "gallery_theme_picker::theme picker: composed pieces carry their own handlers::hands back a chip whose activation is on the chip itself",
     "gallery_theme_picker::theme picker: composed pieces carry their own handlers::puts a handler on every package row, so no path router is needed",
     "gallery_theme_picker::theme picker: composed pieces carry their own handlers::changes the selection when a package row's own handler runs",
     "gallery_theme_picker::theme picker: the chip is its package's control metric, not a padded guess::solves every chip to the ACTIVE package's compact control height",
     "gallery_theme_picker::theme picker: the chip is its package's control metric, not a padded guess::is SHORTER than the 12px-padded box it replaced, at the same width",
     "gallery_theme_picker::theme picker: the chip is its package's control metric, not a padded guess::holds the package's own line box in every reference package, skinned ones included",
     "gallery_theme_picker::theme picker: the chip is its package's control metric, not a padded guess::stays a FLOOR: a chip whose label has to wrap still grows",
     "gallery_theme_picker::theme picker: the two questions are separated, not stacked::puts the package section and the theme row a SECTION gap apart",
     "gallery_theme_picker::theme picker: the two questions are separated, not stacked::keeps the caption attached to the grid it labels",
     "gallery_theme_picker::theme picker: the two questions are separated, not stacked::spends METRIC NAMES, so a swap moves the spacing with the package",
     "gallery_theme_picker::RS-DIR14: the picker's grid rows breathe with the installed package::spaces the rows by the ACTIVE package's `s` step, in every reference package",
     "gallery_theme_picker::RS-DIR14: the picker's grid rows breathe with the installed package::never narrows the vertical air the literal used to give",
     "gallery_theme_picker::RS-DIR14: the picker's grid rows breathe with the installed package::leaves every chip BOX and the column count exactly where the director approved them",
     "gallery_theme_picker::RS-DIR14: the picker's grid rows breathe with the installed package::spends a METRIC NAME for the row gap and a LITERAL for the column gap",
     "gallery_theme_picker::RS-DIR14: the picker's grid rows breathe with the installed package::keeps the grid's own solved height honest about the taller rows",
     "gallery_theme_picker::theme picker: the composed panel hugs, and scrolls when it must::the panel declares `hug` on BOTH axes, not fill",
     "gallery_theme_picker::theme picker: the composed panel hugs, and scrolls when it must::with room to spare the card stops at its last row instead of running to the floor",
     "gallery_theme_picker::theme picker: the composed panel hugs, and scrolls when it must::the card is as WIDE as its chips, not as wide as the phone",
     "gallery_theme_picker::theme picker: the composed panel hugs, and scrolls when it must::...and inside the SHOWCASE'S OWN chrome column, under the chip row",
     "gallery_theme_picker::theme picker: the composed panel hugs, and scrolls when it must::on a short screen it stops at the viewport and SCROLLS instead of running off it",
     "gallery_theme_picker::theme picker: the standalone collapsed shell stays out of the example's way::starts COLLAPSED: the panel is not mounted at all",
     "gallery_theme_picker::theme picker: the standalone collapsed shell stays out of the example's way::docks top-RIGHT, so it cannot sit on a top-left example title",
     "gallery_theme_picker::theme picker: the standalone collapsed shell stays out of the example's way::collapsed, it paints nothing over the band an example lays its content into",
     "gallery_theme_picker::theme picker: the standalone collapsed shell stays out of the example's way::opening the panel is a `When`, not a rebuild of the shell",
     "gallery_theme_picker::theme picker: the standalone collapsed shell stays out of the example's way::exposes the same scriptable seams the composed handle has, so a capture can be driven",
     "gallery_theme_picker::theme picker: the standalone collapsed shell stays out of the example's way::on a compact phone the collapsed chip still fits inside the viewport",
     "gallery_demo_picker::showcase catalogue::lists every tutorial example plus the twenty-seven fixtures",
     "gallery_demo_picker::showcase catalogue::no blurb is longer than the longest one a device pass has already read",
     "gallery_demo_picker::showcase catalogue::names modules that actually exist",
     "gallery_demo_picker::showcase catalogue::every fixture the picker offers is registered in the scenario registry's ORDER",
     "gallery_demo_picker::showcase catalogue::has unique ids",
     "gallery_demo_picker::chip label::passes a title that already fits through untouched",
     "gallery_demo_picker::chip label::clips a longer title to the budget, ellipsis included",
     "gallery_demo_picker::chip label::never clips every title in the catalogue to the same thing",
     "gallery_demo_picker::selection::starts on the first demo",
     "gallery_demo_picker::selection::choosing the demo already shown is a no-op, not a remount",
     "gallery_demo_picker::selection::choosing another demo asks for a swap",
     "gallery_demo_picker::selection::an unknown id changes nothing",
     "gallery_demo_picker::stepping (the couch path)::advances and wraps in both directions",
     "gallery_demo_picker::stepping (the couch path)::a full cycle returns to where it started",
     "gallery_demo_picker::stepping (the couch path)::an unknown current id still yields a real demo",
     "gallery_demo_picker::the showcase demo list scrolls rather than running off the screen::the list is a scroll host, not a plain stack",
     "gallery_demo_picker::the showcase demo list scrolls rather than running off the screen::its box never exceeds the room it was given, in the package that showed the defect",
     "gallery_demo_picker::the showcase demo list scrolls rather than running off the screen::...and when the list does not fit, the canvas EXCEEDS the box, so it can actually be scrolled",
     "gallery_demo_picker::the showcase strip reserves what its chips MEASURE, not a literal::the reservation clears the chip row under every reference package",
     "gallery_demo_picker::the showcase strip reserves what its chips MEASURE, not a literal::...and a chip TALLER than the literal grows the strip, which is the defect",
     "gallery_demo_picker::the showcase strip reserves what its chips MEASURE, not a literal::with no geometry yet, the literal is the floor and nothing else",
     "gallery_demo_picker::reservationSlack: the gap covers the chip's own chrome glow::a flat package (zero chromeBleed) leaves the plain rhythm step untouched",
     "gallery_demo_picker::reservationSlack: the gap covers the chip's own chrome glow::a glow deeper than the rhythm step WINS — math.max, never the sum",
     "gallery_demo_picker::reservationSlack: the gap covers the chip's own chrome glow::a rhythm step already deeper than the glow is untouched",
     "gallery_demo_picker::reservationSlack: the gap covers the chip's own chrome glow::the reservation actually clears the chip's own glow, under every shadowed package",
     "gallery_demo_picker::showcase host: the fixture branch delivers `present` opts::every catalogue fixture's declared opts arrive at the presenter, by identity",
     "gallery_demo_picker::showcase host: the fixture branch delivers `present` opts::no `present` call in the shipped host drops its opts",
     "gallery_demo_picker::showcase host: the delivered opts CHANGE the surface (row_actions)::the hosted VirtualList's window tracks its solved pane, not the pre-geometry literal",
     "gallery_demo_picker::showcase host: the fixture branch binds the native-scroll mirror::every catalogue fixture that rides the engine canvas gets its mirror bound",
     "gallery_demo_picker::showcase host: the fixture branch binds the native-scroll mirror::the lazy grid's window follows an engine scroll — the live defect, headless",
     "gallery_demo_picker::showcase host: the fixture branch binds the native-scroll mirror::...and the virtualized Table's, which publishes the seam under `api`",
     "gallery_demo_picker::showcase host: the fixture branch binds the native-scroll mirror::the shipped host's minimal ctx carries the seam a catalogue fixture calls at build",
     "gallery_demo_picker::showcase host: the fixture branch binds the native-scroll mirror::the shipped host runs the auto-bind loop in every demo branch",
     "gallery_demo_picker::showcase fixture: one fade group, or three (canvas_group)::the left panel is ONE engine CanvasGroup and the right panel is three",
     "gallery_demo_picker::showcase fixture: one fade group, or three (canvas_group)::...and every one of them carries the opacity the chips set",
     "gallery_demo_picker::showcase fixture: one fade group, or three (canvas_group)::the transition panel declares the group and writes no opacity of its own",
     "gallery_demo_picker::showcase fixture: one fade group, or three (canvas_group)::the refusal on screen is the framework's own sentence, captured at build",
     "gallery_demo_picker::showcase host: advancing the picker actually mounts the demo it names::every catalogue demo names a root screen this sweep knows to look for",
     "gallery_demo_picker::showcase host: advancing the picker actually mounts the demo it names::one showNext per demo leaves EXACTLY that demo's own screen in the tree, all 34",
     "gallery_demo_picker::showcase host: advancing the picker actually mounts the demo it names::NEGATIVE CONTROL: a demo that cannot build is NOT reported as mounted",
     "gallery_demo_picker::showcase host: advancing the picker actually mounts the demo it names::the shipped scriptable API answers with what it DELIVERED, not what it was asked for",
     "gallery_demo_host::the showcase host path: a demo core is the core it delegates to::card-rail mounts through the host path, and arranges itself from the host's facts",
     "gallery_demo_host::the showcase host path: a demo core is the core it delegates to::sorted-entries mounts through the host path, and its picker chooses a presentation",
     "gallery_demo_host::the showcase host path: a demo core is the core it delegates to::every demo in the catalogue mounts through the host path",
     "gallery_demo_host::the showcase host path: a demo core is the core it delegates to::the proxy answers with the host's environment, and publishes none of its own",
     "gallery_demo_host::the showcase host path: a demo core is the core it delegates to::a core with no environment at all still REFUSES, naming the environment",
     "gallery_demo_host::the showcase host path: a demo core is the core it delegates to::the shipped host uses this module rather than its own proxy",
     "gallery_chrome::showcase chrome (0): a closed chrome leaves the demo's own input alone::Tab reaches the demo, not the chip strip",
     "gallery_chrome::showcase chrome (0): a closed chrome leaves the demo's own input alone::the d-pad reaches the demo too, and the strip is not the active focus scope",
     "gallery_chrome::showcase chrome (0): a closed chrome leaves the demo's own input alone::the strip is presented, visible, and never above the demo in scope order",
     "gallery_chrome::showcase chrome (0b): the d-pad alone reaches both chips::the demo's own focus map carries the chips — one map, not two",
     "gallery_chrome::showcase chrome (0b): the d-pad alone reaches both chips::up from the demo lands on the demo-list chip, and right walks to Settings",
     "gallery_chrome::showcase chrome (0b): the d-pad alone reaches both chips::...and down goes back to the demo, so the strip is somewhere you can leave",
     "gallery_chrome::showcase chrome (0b): the d-pad alone reaches both chips::ButtonA on the reached demo chip OPENS THE DEMO LIST — a pad, and nothing else",
     "gallery_chrome::showcase chrome (0b): the d-pad alone reaches both chips::ButtonA on the reached settings chip OPENS SETTINGS",
     "gallery_chrome::showcase chrome (0b): the d-pad alone reaches both chips::the ring PAINTS when the d-pad puts it on a chip (a pad must see where it is)",
     "gallery_chrome::showcase chrome (0b): the d-pad alone reaches both chips::a tap still moves focus onto a chip, and still does not paint the ring",
     "gallery_chrome::showcase chrome (1): a keyboard alone can change demo and change theme::the toggle key opens the panel, engages it, and lands focus inside it with the ring showing",
     "gallery_chrome::showcase chrome (1): a keyboard alone can change demo and change theme::Tab walks the panel and Return changes the demo",
     "gallery_chrome::showcase chrome (1): a keyboard alone can change demo and change theme::Space activates too (the desktop convention), and only while the panel is engaged",
     "gallery_chrome::showcase chrome (1): a keyboard alone can change demo and change theme::the theme rows are reachable from the keyboard, through settings",
     "gallery_chrome::showcase chrome (1): a keyboard alone can change demo and change theme::the toggle key closes again, and focus goes back to the demo",
     "gallery_chrome::showcase chrome (2): a gamepad alone can change demo and change theme::the pad's shoulder opens the panel and engages it — with no keyboard anywhere",
     "gallery_chrome::showcase chrome (2): a gamepad alone can change demo and change theme::the d-pad walks the panel and ButtonA changes the demo",
     "gallery_chrome::showcase chrome (2): a gamepad alone can change demo and change theme::ButtonB (Cancel) resigns the panel, which takes it down and returns the demo",
     "gallery_chrome::showcase chrome (2): a gamepad alone can change demo and change theme::the motion setting is reachable and changeable with a pad alone",
     "gallery_chrome::showcase chrome (3): mouse and touch still work exactly as before::a tap on the demo chip opens the panel; a tap on a row chooses and closes",
     "gallery_chrome::showcase chrome (3): mouse and touch still work exactly as before::a tap on the settings chip opens settings",
     "gallery_chrome::showcase chrome (3): mouse and touch still work exactly as before::a tap outside the open panel takes it down (the engaged-surface dismissal path)",
     "gallery_chrome::showcase chrome (4): exactly one engaged surface, and no stolen keys::the strip never holds first responder while the panel is up",
     "gallery_chrome::showcase chrome (4): exactly one engaged surface, and no stolen keys::one Tab press moves the ring one step, not two",
     "gallery_chrome::showcase chrome (4): exactly one engaged surface, and no stolen keys::the toggle context binds exactly the three documented keys, and nothing a demo uses",
     "gallery_chrome::showcase chrome (4): exactly one engaged surface, and no stolen keys::the toggle still works while the panel it opened is engaged and sinking",
     "gallery_chrome::showcase chrome (5): two targets forever, stepping down to icon-only::the strip carries exactly two chips",
     "gallery_chrome::showcase chrome (5): two targets forever, stepping down to icon-only::a desktop shows the labels",
     "gallery_chrome::showcase chrome (5): two targets forever, stepping down to icon-only::the 320px phone that killed the third chip steps down to icons rather than clipping",
     "gallery_chrome::showcase chrome (5): two targets forever, stepping down to icon-only::the losing rung is out of the focus order at every width, on both rungs",
     "gallery_chrome::settings model: Full | Reduced, never on/off::the vocabulary is the two words, in that order",
     "gallery_chrome::settings model: Full | Reduced, never on/off::no player-visible string in this surface says on, off, disable or enable",
     "gallery_chrome::settings model: Full | Reduced, never on/off::maps onto the one environment fact both ways, and refuses to reduce on a typo",
     "gallery_chrome::settings model: Full | Reduced, never on/off::the toggle state machine is a pure function of open x section",
     "gallery_chrome::settings surface: the motion setting survives a demo swap::flipping it writes the real environment fact",
     "gallery_chrome::settings surface: the motion setting survives a demo swap::...and it is still set after switching demos, which is what 'place-wide' means",
     "gallery_chrome::settings surface: the motion setting survives a demo swap::a demo that drives the fact itself is a LOCAL override the setting outlives",
     "gallery_chrome::settings surface: the motion setting survives a demo swap::flipping it RE-SOLVES without rebuilding: mount identity and factory runs are untouched",
     "gallery_chrome::settings surface x with_animation: one fact, two editors::the fixture still inherits whatever the place-wide setting left behind",
     "gallery_chrome::settings surface x with_animation: one fact, two editors::a settings flip moves the demo's own segmented control in the same frame",
     "gallery_chrome::settings surface x with_animation: one fact, two editors::the demo's own flip still restores on dispose — the trap that rule exists for",
     "gallery_chrome::settings surface x with_animation: one fact, two editors::and the host's re-apply is what makes the SETTING win over that restore",
     "gallery_chrome::showcase chrome (9): the sweep::no overflow on either axis, at any swept viewport, under any shipped theme",
     "gallery_chrome::showcase chrome (9): the sweep::the panel never overlaps the chip strip, under any shipped theme at 320x640",
     "gallery_chrome::showcase chrome (9): the sweep::every player-visible string survives ~1.4x pseudo-localization",
     "gallery_chrome::the showcase chip strip keeps a left gutter at phone width (DIR-1)::the strip stands off the screen edge under EVERY shipped package",
     "gallery_chrome::the showcase chip strip keeps a left gutter at phone width (DIR-1)::...and no package's screen-edge step is under the 8px floor, in the metric OR on the glass",
     "gallery_chrome::the showcase chip strip keeps a left gutter at phone width (DIR-1)::with no package at all it is the default spacing step, never zero",
     "gallery_chrome::showcase chrome (10): the open panel stays over the demo::a demo swap under an open panel leaves the panel on top",
     "gallery_chrome::showcase chrome (10): the open panel stays over the demo::...and the panel it re-presents is still the panel: focus and section survive",
     "gallery_chrome::showcase chrome (11): the two shoulder buttons open the two sections::ButtonL1 opens the demo list, with focus inside it",
     "gallery_chrome::showcase chrome (11): the two shoulder buttons open the two sections::ButtonR1 opens settings, from closed AND from the other section",
     "gallery_chrome::showcase chrome (11): the two shoulder buttons open the two sections::the same shoulder closes what it opened, and B closes it too",
     "gallery_chrome::showcase chrome (12): the chip strip sits inside the television's overscan::the console row starts at or inside the overscan inset, on both axes",
     "gallery_chrome::showcase chrome (12): the chip strip sits inside the television's overscan::...and the demo body does not move down to pay for it",
     "gallery_chrome::showcase chrome (12): the chip strip sits inside the television's overscan::every near row is byte-identical: the strip still starts at the screen gutter",
     "gallery_chrome::showcase chrome (12): the chip strip sits inside the television's overscan::a Large display that AUTHORS no overscan still gets the ten-foot default, and 'none' opts out",
     "gallery_chrome::showcase chrome (13): the chips carry their shoulder mark on a pad::a gamepad sees LB and RB, and they are the bindings' own display names",
     "gallery_chrome::showcase chrome (13): the chips carry their shoulder mark on a pad::...and they are still there with the panel OPEN, which is when the pair matters most",
     "gallery_chrome::showcase chrome (13): the chips carry their shoulder mark on a pad::a keyboard and a touchscreen are told nothing, and the row does not reserve for it",
     "gallery_chrome::showcase chrome (13): the chips carry their shoulder mark on a pad::the mark survives ~1.4x pseudo-localization inside the strip, at phone width",
     "gallery_chrome::showcase chrome (14): a shoulder press resolves to ONE context::the demo's Adjust really is live — the negative control this case needs",
     "gallery_chrome::showcase chrome (14): a shoulder press resolves to ONE context::the framework's Adjust does not also fire — L1 and R1, both",
     "gallery_chrome::showcase chrome (14): a shoulder press resolves to ONE context::all three transitions, on both shoulders, with the demo contending for the key",
     "gallery_chrome::showcase chrome (14): a shoulder press resolves to ONE context::the toggle key still reaches the chrome through a demo that sinks the keyboard",
     "gallery_chrome::showcase chrome (15): the raise spends a layer only when it has to::a FAILED MOUNT raises for free — the one path on which nothing climbed",
     "gallery_chrome::showcase chrome (15): the raise spends a layer only when it has to::...and a SUCCESSFUL swap still spends one, because the demo really did climb",
     "gallery_chrome::showcase chrome (15): the raise spends a layer only when it has to::a demo swap under an open panel leaves the ring where the player put it",
     "gallery_chrome::showcase chrome (16): ButtonY belongs to the menu verb, not the chrome::a context bound exactly as menu.luau binds it still receives ButtonY",
     "gallery_chrome::showcase chrome (16): ButtonY belongs to the menu verb, not the chrome::...and the real `menu` scenario opens its card, with the chrome untouched",
     "gallery_chrome::showcase chrome (16): ButtonY belongs to the menu verb, not the chrome::the pad still has its two chrome doors with the menu demo on screen",
     "gallery_chrome::showcase chrome (16): ButtonY belongs to the menu verb, not the chrome::the export says so: there is no gamepad toggle key to name",
     "gallery_chrome::showcase chrome (17): every edge of the strip clears the overscan::the bar's box is inside the safe rectangle on all four edges",
     "gallery_chrome::showcase chrome (17): every edge of the strip clears the overscan::...and the ladder is offered the VISIBLE width, not the panel's",
     "gallery_chrome::showcase chrome (17): every edge of the strip clears the overscan::the near rows are untouched: no edge moved on a display that crops nothing",
     "gallery_chrome::showcase chrome (18): a broken raise listener is reported, not swallowed::one throwing listener produces one report line, and the chrome survives",
     "transient_over_live::the standing rule: a transient opens OVER the live screen::an anchored panel adds no opaque full-viewport node",
     "transient_over_live::the standing rule: a transient opens OVER the live screen::...and neither does a MODAL anchored panel, whose scrim is the catcher",
     "transient_over_live::the standing rule: a transient opens OVER the live screen::...and neither does a region's EXPAND plate",
     "transient_over_live::the standing rule: a transient opens OVER the live screen::a MODAL's declared scrim is the one full-viewport node, and it is the scrim role",
     "transient_over_live::the standing rule: a transient opens OVER the live screen::the theme's scrim is translucent — every shipped package, not just the neutral one",
     "transient_over_live::the standing rule, on the surface that commissioned it::the picker panel stays over the live screen across demo swaps",
     "transient_over_live::the standing rule, on the surface that commissioned it::the demo's own opaque base IS visible to the predicate — the negative control",
     "gallery_boot_mode::gallery boot mode: §5 verification outranks the demo shell::a scenario selector wins in the showcase place",
     "gallery_boot_mode::gallery boot mode: §5 verification outranks the demo shell::...and so does a tutorial-example selector",
     "gallery_boot_mode::gallery boot mode: §5 verification outranks the demo shell::the reported scenario names are registered",
     "gallery_boot_mode::gallery boot mode: §5 verification outranks the demo shell::the showcase still wins when no verification selector is set",
     "gallery_boot_mode::gallery boot mode: §5 verification outranks the demo shell::a place with no attributes at all boots the plain demo",
     "gallery_boot_mode::gallery boot mode: §5 verification outranks the demo shell::a scenario outranks an example",
     "gallery_boot_mode::gallery boot mode: a selector that cannot be honoured is never silent::an empty scenario name refuses, and names the attribute",
     "gallery_boot_mode::gallery boot mode: a selector that cannot be honoured is never silent::a scenario attribute of the wrong type refuses, and names the type",
     "gallery_boot_mode::gallery boot mode: a selector that cannot be honoured is never silent::an example outside the shipped range refuses, and names the range",
     "gallery_boot_mode::gallery boot mode: a selector that cannot be honoured is never silent::two bad selectors report two reasons",
     "gallery_boot_mode::gallery boot mode: a selector that cannot be honoured is never silent::every refusal names the selector, the reason, and what the selector is for",
     "gallery_boot_mode::gallery boot mode: a selector that cannot be honoured is never silent::a runtime refusal shares the voice, and stamps a state a driver can read",
     "gallery_boot_mode::gallery boot mode: the shipped bootstrap decides here and nowhere else::each selector attribute is read exactly once, into the decision",
     "gallery_boot_mode::gallery boot mode: the shipped bootstrap decides here and nowhere else::the bootstrap branches on the decision",
     "gallery_boot_mode::gallery boot mode: the shipped bootstrap decides here and nowhere else::the bootstrap warns every refusal the decision reports",
     "gallery_boot_mode::gallery boot mode: the shipped bootstrap decides here and nowhere else::a scenario that cannot be mounted is stamped AND spoken",
     "gallery_boot_mode::gallery boot mode: the shipped bootstrap decides here and nowhere else::a runner that throws is stamped, spoken, and re-raised",
     "gallery_boot_mode::gallery boot mode: the shipped bootstrap decides here and nowhere else::the example module names live only with the selector that validates them",
     "examples_games::examples/05 word game (Wordle-style)::mounts and renders headlessly with no runtime error",
     "examples_games::examples/05 word game (Wordle-style)::hardware keys fill the row and a wrong guess colors the tiles",
     "examples_games::examples/05 word game (Wordle-style)::backspace deletes, and the winning word opens the results modal",
     "examples_games::examples/05 word game (Wordle-style)::the on-screen keyboard is three navigation-group rows a gamepad can cross",
     "examples_games::examples/05 word game (Wordle-style)::tears down to a registry baseline after dispose",
     "examples_games::examples/05 word game (Wordle-style)::✓ ex05 pointer: tapping on-screen keys fills the row and Enter submits",
     "examples_games::examples/05 word game (Wordle-style)::✓ ex05 touch: a touch tap on an on-screen key types its letter",
     "examples_games::examples/05 word game (Wordle-style)::✓ ex05 keyboard: arrow keys navigate the on-screen keyboard grid (device path)",
     "examples_games::examples/05 word game (Wordle-style)::✓ ex05 gamepad: DPad navigates the keyboard and ButtonA types the focused key",
     "examples_games::examples/07 tile game (Scrabble-style)::mounts and renders headlessly with no runtime error",
     "examples_games::examples/07 tile game (Scrabble-style)::select-then-place moves a rack tile onto a board square",
     "examples_games::examples/07 tile game (Scrabble-style)::tears down to a registry baseline after dispose",
     "examples_games::examples/07 tile game (Scrabble-style)::✓ ex06 touch: touch taps select a rack tile and place it on a board cell",
     "examples_games::examples/07 tile game (Scrabble-style)::✓ ex06 keyboard: arrow/Return select-then-place from the rack onto the board",
     "examples_games::examples/07 tile game (Scrabble-style)::✓ ex06 gamepad: DPad grid navigation with ButtonA select-then-place",
     "examples_games::examples/07 tile game (Scrabble-style)::placing with nothing picked up says so instead of doing nothing",
     "examples_games::examples/07 tile game (Scrabble-style)::progress and the goal are visible, and the readouts are ARRANGED to something",
     "examples_games::examples/08 match-3::mounts and renders headlessly with no runtime error",
     "examples_games::examples/08 match-3::a swap that forms a run clears and refills, and replays deterministically",
     "examples_games::examples/08 match-3::the board reflows across viewports without remounting, and the example names no device",
     "examples_games::examples/08 match-3::the board is PLAYABLE from the first frame: art is delivered at build",
     "examples_games::examples/08 match-3::re-request -> pending -> deliver is the async lesson, driven from the screen",
     "examples_games::examples/08 match-3::a FAILED load is visible and recoverable, which is the half that was missing",
     "examples_games::examples/08 match-3::the five tile kinds carry five DISTINCT shapes, and the ids are the framework's own art",
     "examples_games::examples/08 match-3::opens with no run already on the board",
     "examples_games::examples/08 match-3::opens with at least one legal move",
     "examples_games::examples/08 match-3::...and so does every board a reset deals",
     "examples_games::examples/08 match-3::a swap that makes no run is refused WITH A REASON, and costs no move",
     "examples_games::examples/08 match-3::tapping a NON-adjacent tile says tiles have to touch",
     "examples_games::examples/08 match-3::tapping the selected tile again puts it down",
     "examples_games::examples/08 match-3::a swap SCORES what it clears, counts the move, and chains resolve in one go",
     "examples_games::examples/08 match-3::reset returns score, moves, selection and the message to their start values",
     "examples_games::examples/08 match-3::rapid tapping and resetting leaves no stale selection or error",
     "examples_games::examples/08 match-3::supports gamepad grid navigation across navigation-group rows",
     "examples_games::examples/08 match-3::tears down to a registry baseline after dispose",
     "examples_games::examples/08 match-3::✓ ex07 pointer: tapping two adjacent tiles swaps them (matches the swap API)",
     "examples_games::examples/08 match-3::✓ ex07 touch: touch taps on two adjacent tiles swap them",
     "examples_games::examples/08 match-3::✓ ex07 keyboard: arrow keys navigate and Return swaps adjacent tiles",
     "examples_games::examples/08 match-3::✓ ex07 gamepad: DPad navigation and ButtonA swaps adjacent tiles",
     "example_word_game::examples/05 word game — the pure scoring rules::scores a plain guess with no repeated letters",
     "example_word_game::examples/05 word game — the pure scoring rules::a guess with a DOUBLED letter against a solution holding ONE spends the budget once",
     "example_word_game::examples/05 word game — the pure scoring rules::a SOLUTION with a doubled letter pays a misplaced guess letter from what the exact match left",
     "example_word_game::examples/05 word game — the pure scoring rules::BOTH doubled: two exact matches and one misplaced, all from a budget of two",
     "example_word_game::examples/05 word game — the pure scoring rules::an EXACT match consumes the budget BEFORE an earlier misplaced copy of the same letter",
     "example_word_game::examples/05 word game — the pure scoring rules::three copies against two: the excess is absent",
     "example_word_game::examples/05 word game — the pure scoring rules::the whole solution scores correct",
     "example_word_game::examples/05 word game — validation and the two generated sets::refuses a guess shorter than five letters",
     "example_word_game::examples/05 word game — validation and the two generated sets::refuses a five-letter non-word",
     "example_word_game::examples/05 word game — validation and the two generated sets::accepts every solution AND the wider guess list",
     "example_word_game::examples/05 word game — validation and the two generated sets::the answer set is a SUBSET of the accepted set, and strictly smaller",
     "example_word_game::examples/05 word game — validation and the two generated sets::accepts ordinary words the old hand-typed list refused",
     "example_word_game::examples/05 word game — validation and the two generated sets::takes a guess in whatever case it arrives in",
     "example_word_game::examples/05 word game — the key ladder is monotonic::ranks correct above present above absent",
     "example_word_game::examples/05 word game — the key ladder is monotonic::never downgrades on later, weaker evidence",
     "example_word_game::examples/05 word game — the key ladder is monotonic::an unknown key takes whatever the first evidence says",
     "example_word_game::examples/05 word game — the key ladder is monotonic::mergeKeys returns a NEW table and keeps the strongest per letter",
     "example_word_game::examples/05 word game — the key ladder is monotonic::a later guess that re-tests a solved letter cannot downgrade it",
     "example_word_game::examples/05 word game — the deterministic seed::the same seed always yields the same word",
     "example_word_game::examples/05 word game — the deterministic seed::every seed lands on a real answer, and a seed past the end wraps",
     "example_word_game::examples/05 word game — the deterministic seed::the game plays in UPPERCASE even though the data is lowercase",
     "example_word_game::examples/05 word game — the deterministic seed::REACT is reachable, and it is the seed the rest of this file plays",
     "example_word_game::examples/05 word game — the state machine::starts on a visible six-by-five board with a caret in the first cell",
     "example_word_game::examples/05 word game — the state machine::every tile paints a REAL tint — the plate is never invisible",
     "example_word_game::examples/05 word game — the state machine::letters land in the ACTIVE row only",
     "example_word_game::examples/05 word game — the state machine::a SHORT guess is refused with a message and does not consume a row",
     "example_word_game::examples/05 word game — the state machine::an INVALID guess is refused with a different message and does not consume a row",
     "example_word_game::examples/05 word game — the state machine::a rejected guess consumes NO row, and an ordinary word the old list refused plays on",
     "example_word_game::examples/05 word game — the state machine::four words the old thirty-four-word list rejected now play end to end",
     "example_word_game::examples/05 word game — the state machine::backspace deletes from the active row and stops at empty",
     "example_word_game::examples/05 word game — the state machine::a scored row keeps its verdict AND its marks while the next row becomes active",
     "example_word_game::examples/05 word game — the state machine::the duplicate-letter rule reaches the board, not just the scorer",
     "example_word_game::examples/05 word game — the state machine::key state accumulates monotonically and never downgrades",
     "example_word_game::examples/05 word game — the state machine::wins when the solution is submitted, and opens the results card",
     "example_word_game::examples/05 word game — the state machine::loses after SIX accepted wrong guesses and reveals the word",
     "example_word_game::examples/05 word game — the state machine::a refused guess does NOT count toward the six",
     "example_word_game::examples/05 word game — the state machine::input is dead after the game ends, except restart",
     "example_word_game::examples/05 word game — the state machine::restart is deterministic: the same seed replays the same word",
     "example_word_game::examples/05 word game — the state machine::restart after a loss closes the results card and re-enables the keyboard",
     "example_word_game::examples/05 word game — the state machine::the results card's Close button actually closes it",
     "example_word_game::examples/05 word game — the state machine::the results card's New game button restarts from inside the card",
     "example_word_game::examples/05 word game — the state machine::tears down to a registry baseline after dispose",
     "example_word_game::examples/05 word game — it fits and stays operable on a phone::the solver reports NO layout finding on a compact phone",
     "example_word_game::examples/05 word game — it fits and stays operable on a phone::the board and every key stay inside the phone's width",
     "example_word_game::examples/05 word game — it fits and stays operable on a phone::a tile is square and the board is six rows of five",
     "example_word_game::examples/05 word game — it fits and stays operable on a phone::the page is a scroll host, so the largest text size cannot strand the keyboard",
     "example_word_game::examples/05 word game — thirty cells that are actually there, in every shipped theme::sweeps a real set of themes, not an empty one",
     "example_word_game::examples/05 word game — thirty cells that are actually there, in every shipped theme::every one of the thirty cells is present AND painted at startup, under every theme",
     "example_word_game::examples/05 word game — thirty cells that are actually there, in every shipped theme::...and the floor BITES: the plate this example used to paint cannot clear it anywhere",
     "example_word_game::examples/05 word game — the active row's cue is not a colour::the caret's PAINT is the empty cell's paint — so colour cannot be the cue",
     "example_word_game::examples/05 word game — the active row's cue is not a colour::every cell of the ACTIVE row wears the outlined plate, and no other row does",
     "example_word_game::examples/05 word game — the active row's cue is not a colour::the outline FOLLOWS the active row, and leaves when nothing is taking letters",
     "example_word_game::examples/05 word game — the active row's cue is not a colour::the NEXT LETTER's cell carries a mark that no other cell carries",
     "example_word_game::examples/05 word game — the board holds its letters at every text size::every letter and mark on the board still has a box that holds it",
     "example_word_data::example word data: provenance is recorded, not asserted::names a specific licensed archive, not just 'a dictionary'",
     "example_word_data::example word data: provenance is recorded, not asserted::carries the licence notices the source requires",
     "example_word_data::example word data: provenance is recorded, not asserted::writes down the policy that produced the two sets",
     "example_word_data::example word data: the files on disk are the files that were generated::every generated file still hashes to what the manifest recorded",
     "example_word_data::example word data: the files on disk are the files that were generated::the manifest's counts are the counts the banks actually hold",
     "example_word_data::example word data: the banks are searchable::every bank is strictly ascending and exactly divisible by its width",
     "example_word_data::example word data: the banks are searchable::the solution bank is ascending too, and every answer is an accepted guess",
     "example_word_data::example word data: it accepts what a player would type::is broad — thousands of five-letter guesses, and a smaller reviewed answer set",
     "example_word_data::example word data: it accepts what a player would type::accepts an ordinary word starting with every letter of the alphabet",
     "example_word_data::example word data: it accepts what a player would type::accepts a guess in any dialect, while an answer keeps one spelling",
     "example_word_data::example word data: it accepts what a player would type::accepts the words the old hand-written list rejected",
     "example_word_data::example word data: it accepts what a player would type::accepts the inflections the source policy keeps",
     "example_word_data::example word data: it accepts what a player would type::refuses obvious nonwords, wrong lengths, and anything that is not plain letters",
     "example_word_data::example word data: it accepts what a player would type::does not care what case the letters arrived in",
     "example_word_data::example word data: a seed is a puzzle, and always the same puzzle::the same seed always chooses the same answer",
     "example_word_data::example word data: a seed is a puzzle, and always the same puzzle::consecutive seeds are not consecutive words",
     "example_word_data::example word data: a seed is a puzzle, and always the same puzzle::every answer is reachable, and the mapping wraps rather than erroring",
     "example_word_data::example word data: a seed is a puzzle, and always the same puzzle::an out-of-range index is refused rather than silently clamped",
     "example_word_data::example word data: the lookup is cheap enough to sit under a keystroke::a hit and a miss both cost a bisection, not a scan",
     "example_tile_game::examples/06 crossword — the rules are pure::the board, the rack, the budget and the goal are the design's numbers",
     "example_tile_game::examples/06 crossword — the rules are pure::the letter values are the familiar English ones",
     "example_tile_game::examples/06 crossword — the rules are pure::the bag is the 98-tile familiar multiset, with no blanks",
     "example_tile_game::examples/06 crossword — axis, run and connection::a turn's tiles lie in one row or one column, and scattered tiles are refused",
     "example_tile_game::examples/06 crossword — axis, run and connection::ONE tile does not fix an axis — the board does",
     "example_tile_game::examples/06 crossword — axis, run and connection::a gap in the run is refused, and a gap CLOSED by a committed letter is not",
     "example_tile_game::examples/06 crossword — axis, run and connection::the main word is read THROUGH committed letters at both ends",
     "example_tile_game::examples/06 crossword — axis, run and connection::the first turn must cover the starred centre, and later turns must not",
     "example_tile_game::examples/06 crossword — axis, run and connection::connection is ORTHOGONAL: touching diagonally is not touching",
     "example_tile_game::examples/06 crossword — every word a turn creates::every perpendicular word a placed tile now sits inside is extracted and validated",
     "example_tile_game::examples/06 crossword — every word a turn creates::a crossing word the dictionary does not know refuses the whole turn, BY NAME",
     "example_tile_game::examples/06 crossword — every word a turn creates::the score counts every letter in every word, once per word it appears in",
     "example_tile_game::examples/06 crossword — every word a turn creates::using five or more rack tiles in one turn adds ten",
     "example_tile_game::examples/06 crossword — the dictionary is the shared module::accepts and rejects through `words`, not through a list this game keeps",
     "example_tile_game::examples/06 crossword — the dictionary is the shared module::a run the dictionary CANNOT judge is refused by name, not called unknown",
     "example_tile_game::examples/06 crossword — the deal guarantees an opening::the shipped seed deals a rack that spells something across the centre",
     "example_tile_game::examples/06 crossword — the deal guarantees an opening::...and so does every seed in a wide sweep, inside the attempt bound",
     "example_tile_game::examples/06 crossword — the deal guarantees an opening::openingWord finds a word from a SUBSET in any order, and nil when there is none",
     "example_tile_game::examples/06 crossword — nine refusals, nine sentences, on screen::1 — the first turn misses the centre",
     "example_tile_game::examples/06 crossword — nine refusals, nine sentences, on screen::2 — the tiles are not in one line",
     "example_tile_game::examples/06 crossword — nine refusals, nine sentences, on screen::3 — a gap in the run",
     "example_tile_game::examples/06 crossword — nine refusals, nine sentences, on screen::4 — a later turn touches nothing",
     "example_tile_game::examples/06 crossword — nine refusals, nine sentences, on screen::5 — the main word is unknown, and the sentence names it",
     "example_tile_game::examples/06 crossword — nine refusals, nine sentences, on screen::6 — a crossing word is unknown, and the sentence names it AND its direction",
     "example_tile_game::examples/06 crossword — nine refusals, nine sentences, on screen::7 — submit with nothing placed",
     "example_tile_game::examples/06 crossword — nine refusals, nine sentences, on screen::8 — placing on an occupied square",
     "example_tile_game::examples/06 crossword — nine refusals, nine sentences, on screen::9 — picking up a letter from an earlier turn",
     "example_tile_game::examples/06 crossword — nine refusals, nine sentences, on screen::...and an uncommitted tile CAN be picked back up, which is what makes 9 a rule",
     "example_tile_game::examples/06 crossword — nine refusals, nine sentences, on screen::...and tapping an empty square with nothing held explains itself too",
     "example_tile_game::examples/06 crossword — nine refusals, nine sentences, on screen::the instruction line names the held tile, and clears on a placement",
     "example_tile_game::examples/06 crossword — commit, refill, undo, budget::a committed turn scores, banks the letters, and refills the rack to seven",
     "example_tile_game::examples/06 crossword — commit, refill, undo, budget::undo returns exactly the uncommitted tiles, to the slots they came from",
     "example_tile_game::examples/06 crossword — commit, refill, undo, budget::undo with nothing placed says so instead of doing nothing",
     "example_tile_game::examples/06 crossword — commit, refill, undo, budget::A REFUSED SUBMIT CHANGES NOTHING — every tile stays where the player put it",
     "example_tile_game::examples/06 crossword — commit, refill, undo, budget::the turn budget decrements only on a commit",
     "example_tile_game::examples/06 crossword — commit, refill, undo, budget::the legal-next-cell cue follows the rules, and locks with the axis",
     "example_tile_game::examples/06 crossword — both endings, and the way back::reaching the goal wins, and the screen says which ending it was",
     "example_tile_game::examples/06 crossword — both endings, and the way back::spending the budget without reaching the goal loses, and says so",
     "example_tile_game::examples/06 crossword — both endings, and the way back::restart returns EVERY observable to its seeded start",
     "example_tile_game::examples/06 crossword — determinism::the same seed deals the same rack and the same bag",
     "example_tile_game::examples/06 crossword — determinism::a fixed script on a fixed seed lands on the same board, score and dump",
     "example_tile_game::examples/06 crossword — robustness::rapid alternating input leaves no stale selection or held tile",
     "example_tile_game::examples/06 crossword — robustness::tears down to a registry baseline after dispose",
     "example_tile_game::examples/06 crossword — forty-nine cells that are actually there, in every theme::sweeps a real set of themes, not an empty one",
     "example_tile_game::examples/06 crossword — forty-nine cells that are actually there, in every theme::every cell state paints a plate the player can see, under every theme",
     "example_tile_game::examples/06 crossword — forty-nine cells that are actually there, in every theme::...and the floor BITES: the plate the word game used to paint clears it nowhere",
     "example_tile_game::examples/06 crossword — committed versus uncommitted is not a colour::a committed letter wears a solid plate and an uncommitted one wears an outline",
     "example_tile_game::examples/06 crossword — committed versus uncommitted is not a colour::...and the same fact a third time, in ASCII, for a screen with no colour at all",
     "example_tile_game::examples/06 crossword — committed versus uncommitted is not a colour::a spent rack slot is DISABLED, not a live-looking blank button",
     "example_match3_motion::match-3 motion: tile identity survives a move::the mounted node for a tile id is the same node it was before the swap",
     "example_match3_motion::match-3 motion: tile identity survives a move::every id on the board is unique, and a refill never reuses one",
     "example_match3_motion::match-3 motion: the two swapped tiles travel::halfway through the swap phase both tiles are painted at NEITHER endpoint",
     "example_match3_motion::match-3 motion: the two swapped tiles travel::a refused swap travels OUT and travels BACK, and says why",
     "example_match3_motion::match-3 motion: the resolve sequence is visible, in order::removal precedes gravity precedes refill, and input unlocks after the last cascade",
     "example_match3_motion::match-3 motion: the resolve sequence is visible, in order::a matched tile leaves the model and stays mounted-but-retiring while it fades",
     "example_match3_motion::match-3 motion: the resolve sequence is visible, in order::a refilled tile's FIRST painted position is above the board, not its resting one",
     "example_match3_motion::match-3 motion: the resolve sequence is visible, in order::a survivor DROPS: its painted position is between its old row and its new one",
     "example_match3_motion::match-3 motion: the resolve sequence is visible, in order::a tap while the board resolves is REFUSED in words, and costs nothing",
     "example_match3_motion::match-3 motion: the anchored board still navigates in two dimensions::left/right walks the lane and up/down keeps it, before AND after a cascade",
     "example_match3_motion::match-3 motion: the anchored board still navigates in two dimensions::...and everything below the board is still reachable without a pointer",
     "example_match3_motion::match-3 motion: a reset mid-cascade leaves nothing behind::stops the machine, drops its tick hook, and leaves no retiring row or animation record",
     "example_match3_motion::match-3 motion: a reset mid-cascade leaves nothing behind::dispose mid-cascade unregisters the hook and leaves the registry at baseline",
     "example_match3_motion::match-3 motion: reduced motion changes the paint and nothing else::the same board, score, moves, phase log and replay under both policies",
     "example_match3_motion::match-3 motion: reduced motion changes the paint and nothing else::...and reduced motion installs NO animation records rather than substituting another effect",
     "example_match3_motion::match-3 motion: reduced motion changes the paint and nothing else::the example contains no reduced-motion branch at all",
     "example_match3_motion::match-3 motion: the example builds no animation system of its own::finds none of them in a single line of its code",
     "example_match3_motion::match-3 motion: the example builds no animation system of its own::...and it really did scan the example, which uses the four public mechanisms by name",
     "example_match3_motion::match-3 motion: thirty-six tiles that are actually painted, in every shipped theme::sweeps a real set of themes, not an empty one",
     "example_match3_motion::match-3 motion: thirty-six tiles that are actually painted, in every shipped theme::every tile picture clears the visibility floor against the page it sits on",
     "example_match3_motion::match-3 motion: thirty-six tiles that are actually painted, in every shipped theme::...and the floor BITES: the role this example carried until this round drops under it",
     "popup_button::PopupButton: build + render::mounts and renders headlessly with no runtime error",
     "popup_button::PopupButton: build + render::the closed button shows the currently-selected option's label",
     "popup_button::PopupButton: build + render::the closed control is a SINGLE focusable (only the trigger)",
     "popup_button::PopupButton: pointer interaction (the real tap path)::tapping the trigger opens the popup listing every option",
     "popup_button::PopupButton: pointer interaction (the real tap path)::a TOUCH-typed tap on the trigger opens the popup listing every option",
     "popup_button::PopupButton: pointer interaction (the real tap path)::activating an option selects it, fires onChange, closes, and relabels",
     "popup_button::PopupButton: pointer interaction (the real tap path)::re-tapping the trigger while open closes it WITHOUT changing the selection",
     "popup_button::PopupButton: pointer interaction (the real tap path)::the Cancel row closes the popup without changing the selection",
     "popup_button::PopupButton: pointer interaction (the real tap path)::re-selecting the ALREADY-selected option closes without a redundant onChange",
     "popup_button::PopupButton: keyboard / gamepad::keyboard opens, navigates the options, and Activate selects",
     "popup_button::PopupButton: keyboard / gamepad::gamepad ButtonA drives the same open+select path",
     "popup_button::PopupButton: reactive discipline::opening/closing is the only structural change; a selection does not rerun the factory",
     "popup_button::PopupButton: reactive discipline::dump() is deterministic and reflects open state, value, and options",
     "popup_button::PopupButton: reactive discipline::build/mount/interact/dispose returns every registry to its baseline",
     "popup_button::PopupButton: build-time contract::option ids must be path-safe (a '/' would corrupt path routing)",
     "popup_button::PopupButton: build-time contract::rejects a read-only Memo `value` at BUILD, naming the control and the field",
     "popup_button::PopupButton: build-time contract::rejects a non-signal `value` at BUILD",
     "text_input::TextInput: build + render (TextField primitive wired headless)::mounts and renders headlessly with no runtime error",
     "text_input::TextInput: build + render (TextField primitive wired headless)::the field shows the owner value as its text binding and the placeholder as data",
     "text_input::TextInput: build + render (TextField primitive wired headless)::a bare TextField primitive mounts and renders text/placeholder through the renderer",
     "text_input::TextInput: build + render (TextField primitive wired headless)::a TextInput with NO env builds, edits, types and commits (the documented optional)",
     "text_input::TextInput: pointer (tap to edit, type, commit)::tapping the field enters editing (the control-owned handshake)",
     "text_input::TextInput: pointer (tap to edit, type, commit)::typed text flows through the adapter seam into the owner value, firing onChange per accepted edit",
     "text_input::TextInput: pointer (tap to edit, type, commit)::commit (engine Enter/focus-lost) fires onCommit with the reason and ends editing",
     "text_input::TextInput: pointer (tap to edit, type, commit)::blur (focus lost without Enter) commits with reason focusLost",
     "text_input::TextInput: keyboard (Activate enters, sink blocks navigation, Enter commits, nav resumes)::focused field + Activate (Return) enters editing",
     "text_input::TextInput: keyboard (Activate enters, sink blocks navigation, Enter commits, nav resumes)::while editing, navigation keys are sunk: a lower Navigate probe never fires and focus stays",
     "text_input::TextInput: keyboard (Activate enters, sink blocks navigation, Enter commits, nav resumes)::Enter commits, editing ends, and navigation works again (probe fires post-edit)",
     "text_input::TextInput: keyboard (Activate enters, sink blocks navigation, Enter commits, nav resumes)::Activate does not re-fire while editing: a second Return is an idempotent no-op",
     "text_input::TextInput: gamepad (D-pad focus, A enters, D-pad sunk, B cancels-revert, A re-enters)::ButtonA enters editing; D-pad does not navigate; ButtonB cancels reverting to the snapshot",
     "text_input::TextInput: gamepad (D-pad focus, A enters, D-pad sunk, B cancels-revert, A re-enters)::ButtonA re-enters editing after a cancel",
     "text_input::TextInput: value model (onChange live, onCommit modes coexist, maxLength, validate)::onChange and onCommit are distinct and both fire",
     "text_input::TextInput: value model (onChange live, onCommit modes coexist, maxLength, validate)::maxLength clamps typed text at the value-model boundary",
     "text_input::TextInput: value model (onChange live, onCommit modes coexist, maxLength, validate)::maxLength clamps a paste-like multi-char append to the boundary",
     "text_input::TextInput: value model (onChange live, onCommit modes coexist, maxLength, validate)::engine reports over-long text -> model clamps -> commit reconciles to the clamped value",
     "text_input::TextInput: value model (onChange live, onCommit modes coexist, maxLength, validate)::rejects an edit containing invalid UTF-8 (value unchanged, no onChange) with maxLength set",
     "text_input::TextInput: value model (onChange live, onCommit modes coexist, maxLength, validate)::rejects invalid UTF-8 even without maxLength (an applyProposed-level guard, not a truncate quirk)",
     "text_input::TextInput: value model (onChange live, onCommit modes coexist, maxLength, validate)::validate normalizes accepted text and can reject an edit (value unchanged, no onChange)",
     "text_input::TextInput: clear affordance::the clear button appears only while value is non-empty; activating it empties the value (a user edit)",
     "text_input::TextInput: clear affordance::no clear button when the value is empty",
     "text_input::TextInput: clear affordance::the field fills full width and the clear ✕ sits inset INSIDE the field's right edge (WP7 drive defect 3)",
     "text_input::TextInput: clear affordance::the clear button sits at the field's trailing (right) edge, vertically centered (WP7 rider)",
     "text_input::TextInput: disabled::a disabled field is not focusable, tap does nothing, and shows no clear button",
     "text_input::TextInput: keyboardType::a known keyboardType lands on the field as data for the engine adapter",
     "text_input::TextInput: keyboardType::numeric, email, and phone keyboardType hints are accepted and land on the field as data",
     "text_input::TextInput: keyboardType::an unknown keyboardType is a build error",
     "text_input::TextInput: keyboardType engine-application contract (capability-gated)::nothing is applied to the engine on a shipping (not-capable) adapter",
     "text_input::TextInput: keyboardType engine-application contract (capability-gated)::the MAPPED engine text-input-type reaches the adapter once it is capable",
     "text_input::TextInput: keyboardType engine-application contract (capability-gated)::maps numeric -> Number and phone -> Phone when capable",
     "text_input::TextInput: submitLabel (declared Return-key hint; engine-absent clean degrade)::a known submitLabel is accepted and carried as declared data (dump)",
     "text_input::TextInput: submitLabel (declared Return-key hint; engine-absent clean degrade)::defaults to 'default' when unset",
     "text_input::TextInput: submitLabel (declared Return-key hint; engine-absent clean degrade)::an unknown submitLabel is a build error",
     "text_input::TextInput: keyboard-occlusion keep-visible::a mid-edit occlusion that overlaps the field produces an upward transform, restored on dismiss",
     "text_input::TextInput: keyboard-occlusion keep-visible::no occlusion offset when not editing, even if the keyboard rect is set",
     "text_input::TextInput: keyboard-occlusion keep-visible::disposing the control mid-occlusion restores the screen's presentation transform to 0 (ARCH-TI-2)",
     "text_input::TextInput: keyboard-occlusion keep-visible::engine-initiated focus (native TextBox tap, NO prior activate) is occlusion-capable",
     "text_input::TextInput: touch (activation + keep-visible, end to end)::a touch tap enters editing and the occlusion keep-visible engages (touch story end to end)",
     "text_input::TextInput: conformance (reactive discipline, dump, registry neutrality)::typing does not rerun the factory or remount (paint/semantic writes only)",
     "text_input::TextInput: conformance (reactive discipline, dump, registry neutrality)::dump() is deterministic and reflects value, editing, placeholder-visible, clearVisible, offset",
     "text_input::TextInput: conformance (reactive discipline, dump, registry neutrality)::build/mount/interact/dispose returns every registry to its baseline (incl. a mid-edit dispose)",
     "text_input::TextInput: build-time contract::value is required and must be a Signal/Memo (owner-held)",
     "text_input::TextInput: build-time contract::value must be a SETTABLE signal — a read-only Memo is a build error (ARCH-TI-1)",
     "text_input::TextInput: activate after dispose is a harmless no-op::handleActivate on a disposed control neither throws nor enters editing",
     "text_input::TextInput: the clear chip sizes for its glyph, in every theme::a flat theme keeps the historic 28px chip exactly",
     "text_input::TextInput: the clear chip sizes for its glyph, in every theme::but it never grows TALLER than the field it lives inside",
     "text_input::TextInput: the clear chip sizes for its glyph, in every theme::a skinning theme GROWS the chip for its own frame instead of eating the glyph",
     "text_input::TextInput: clearButtonMode::never: no affordance, whatever the text or the focus",
     "text_input::TextInput: clearButtonMode::always: offered whenever there is something to clear, editing or not",
     "text_input::TextInput: clearButtonMode::whileEditing: appears with the caret and leaves with it",
     "text_input::TextInput: clearButtonMode::unlessEditing: the mirror image — it gets out of the way while you type",
     "text_input::TextInput: clearButtonMode::every mode still needs SOMETHING to clear — an empty field offers nothing",
     "text_input::TextInput: clearButtonMode::`clearButton = true` still means `always`, so nothing published breaks",
     "text_input::TextInput: clearButtonMode::an unrecognised mode is a build error, not a field that never offers a ✕",
     "auto_input::auto-input: Button props.onActivate fires on every input, no consumer wiring::pointer tap activates the Button through its onActivate prop",
     "auto_input::auto-input: Button props.onActivate fires on every input, no consumer wiring::keyboard Return activates the focused Button",
     "auto_input::auto-input: Button props.onActivate fires on every input, no consumer wiring::gamepad ButtonA activates the focused Button",
     "auto_input::auto-input: Button props.onActivate fires on every input, no consumer wiring::Button touch: a finger tap activates through the auto dispatch",
     "auto_input::auto-input: Toggle auto-flips its bound value (contract §10.2)::pointer tap flips the value",
     "auto_input::auto-input: Toggle auto-flips its bound value (contract §10.2)::focused Activate (Return) flips the value",
     "auto_input::auto-input: Toggle auto-flips its bound value (contract §10.2)::Toggle touch: a finger tap flips the value",
     "auto_input::auto-input: Toggle auto-flips its bound value (contract §10.2)::Toggle gamepad: ButtonA on the focused Toggle flips the value",
     "auto_input::auto-input: Table + TextInput on one screen, zero present opts::initial focus lands on the field (auto-merged groups, field first)",
     "auto_input::auto-input: Table + TextInput on one screen, zero present opts::D-pad navigates field -> rows and back (auto-merged focus groups)",
     "auto_input::auto-input: Table + TextInput on one screen, zero present opts::gamepad A selects the focused row (auto contribution dispatch)",
     "auto_input::auto-input: Table + TextInput on one screen, zero present opts::grab mode moves a row via auto navigateIntercept (A grabs, D-pad steps)",
     "auto_input::auto-input: Table + TextInput on one screen, zero present opts::typing in the field does not navigate (auto bindActionSystem sink)",
     "auto_input::auto-input: Table + TextInput on one screen, zero present opts::field geometry + keep-visible engage automatically (auto syncGeometry + keepVisibleOffset)",
     "auto_input::auto-input: per-opt override (consumer opt beats its auto counterpart)::an explicit opts.onActivate suppresses auto activate dispatch entirely",
     "auto_input::auto-input: per-opt override (consumer opt beats its auto counterpart)::navigationGroups=false forces the legacy flat ring (auto grouping suppressed)",
     "auto_input::auto-input: refresh re-derives auto groups after structural change::a row added after present becomes reachable through the auto groups",
     "auto_input::auto-input: registry neutrality of the contribution seam::build/mount/present/interact/dispose returns every registry to baseline",
     "table_input::table_input: header-sort via keyboard and gamepad (auto headers group)::initial focus lands on the leading headers group (first sortable column)",
     "table_input::table_input: header-sort via keyboard and gamepad (auto headers group)::gamepad ButtonA SELECTS the column; the stick then acts on it (sort on Up/Down)",
     "table_input::table_input: header-sort via keyboard and gamepad (auto headers group)::gamepad DPadLeft/DPadRight walk between sortable headers (horizontal group)",
     "table_input::table_input: header-sort via keyboard and gamepad (auto headers group)::gamepad DPadDown exits headers to the rows; DPadUp re-enters the headers",
     "table_input::table_input: header-sort via keyboard and gamepad (auto headers group)::keyboard Right/Return drive the header sort identically to the gamepad",
     "table_input::table_input: body scroll-into-view on keyboard/gamepad focus moves::gamepad DPadDown past the clip band scrolls the focused row into view",
     "table_input::table_input: body scroll-into-view on keyboard/gamepad focus moves::keyboard Down/Up scrolls both directions; scrolling back up reveals r1 again",
     "table_input::table_input: body scroll-into-view on keyboard/gamepad focus moves::navigating within the visible band never scrolls (minimal scroll)",
     "table_input::table_input: touch column-resize through the grip pointer seam::a TOUCH drag on the grip previews and commits the new column width",
     "table_input::table_input: touch column-resize through the grip pointer seam::a TOUCH grip drag clamps to the column minWidth",
     "table_input::table_input: device-true gamepad/keyboard row-select and grab-reorder::gamepad ButtonA selects the focused row (through presenter Activate dispatch)",
     "table_input::table_input: device-true gamepad/keyboard row-select and grab-reorder::gamepad DPadDown/DPadUp navigate rows and selection follows focus",
     "table_input::table_input: device-true gamepad/keyboard row-select and grab-reorder::keyboard Down + Return select the focused row",
     "table_input::table_input: device-true gamepad/keyboard row-select and grab-reorder::gamepad edit-mode: ButtonA grabs, DPadDown steps the row, ButtonA drops",
     "table_input::table_input: device-true gamepad/keyboard row-select and grab-reorder::keyboard edit-mode grab is input-agnostic: Return grabs, Down steps, Return drops",
     "table_input::table_input: reorder drag inside a scrolling PAGE (block table)::the ghost chip stays under the pointer while the PAGE scrolls mid-drag",
     "table_input::table_input: reorder drag inside a scrolling PAGE (block table)::the drop line snaps to the nearest PAINTED row boundary after a PAGE scroll",
     "table_input::table_input: reorder drag inside a scrolling PAGE (block table)::the drag overlay does not change the PAGE's scroll canvas (device report: tiny scrollbar)",
     "table_input::table_input: reorder drag inside a scrolling PAGE (block table)::holding the drag in the PAGE's bottom edge band autoscrolls the page",
     "table_input::table_input: reorder drag inside a scrolling PAGE (block table)::no drag, no autoscroll: a finger resting in the band with nothing picked up never scrolls",
     "table_input::table_input: reorder drag inside a scrolling PAGE (block table)::autoscroll releases with the drop (no runaway scroll after the finger lifts)",
     "table_input::table_input: reorder drag inside a scrolling PAGE (block table)::a drag at a NESTED list's bottom edge scrolls the INNER list, not the page",
     "table_input::table_input: reorder drag inside a scrolling PAGE (block table)::an inner list already at its end hands the drag OUT to the page (bands overlap)",
     "table_input::table_input: reorder drag inside a scrolling PAGE (block table)::autoscroll=false opts a scroller out for good: the chain skips it entirely",
     "table_input::table_input: reorder drag inside a scrolling PAGE (block table)::a drop after a PAGE scroll commits the slot the pointer is actually over",
     "table_input::table_input: onPrimaryAction across pointer, touch, keyboard and gamepad::Table pointer: a double-click on a row fires onPrimaryAction once, and a single click never does",
     "table_input::table_input: onPrimaryAction across pointer, touch, keyboard and gamepad::Table pointer: two clicks SLOWER than the window are two single clicks, not an open",
     "table_input::table_input: onPrimaryAction across pointer, touch, keyboard and gamepad::Table pointer: two fast clicks on DIFFERENT rows are not a double-click",
     "table_input::table_input: onPrimaryAction across pointer, touch, keyboard and gamepad::Table pointer: a modified click is a SELECTION gesture and never opens",
     "table_input::table_input: onPrimaryAction across pointer, touch, keyboard and gamepad::Table touch: a plain single tap on a row activates it — the first tap, on any row",
     "table_input::table_input: onPrimaryAction across pointer, touch, keyboard and gamepad::Table touch: THE COST — a normal-mode tap opens WITHOUT selecting, and leaves an existing selection exactly alone",
     "table_input::table_input: onPrimaryAction across pointer, touch, keyboard and gamepad::Table touch: EDIT MODE is the selection mode — tap toggles selection (multi's DESELECT is back) and never opens",
     "table_input::table_input: onPrimaryAction across pointer, touch, keyboard and gamepad::Table touch: with selection = \"none\" a single tap opens, exactly as it does in every other mode",
     "table_input::table_input: onPrimaryAction across pointer, touch, keyboard and gamepad::Table touch: a selectable NON-reorderable table with a primary action can still be put into edit mode, so touch can select at all",
     "table_input::table_input: onPrimaryAction across pointer, touch, keyboard and gamepad::the auto toggle stays OFF a mouse session: a click already selects, so there is nothing edit mode is the only route to",
     "table_input::table_input: onPrimaryAction across pointer, touch, keyboard and gamepad::the auto toggle stays off a touch table with selection = \"none\": edit mode is the only route to NOTHING there",
     "table_input::table_input: onPrimaryAction across pointer, touch, keyboard and gamepad::the auto toggle stays off a touch table with NO primary action — the shipped RascalRally racer-list shape",
     "table_input::table_input: onPrimaryAction across pointer, touch, keyboard and gamepad::the auto toggle stays off when the CONSUMER owns the editing signal — their affordance, their business",
     "table_input::table_input: onPrimaryAction across pointer, touch, keyboard and gamepad::control-owned edit mode CANCELS when touch leaves the session, exactly as it does for a reorderable table",
     "table_input::table_input: onPrimaryAction across pointer, touch, keyboard and gamepad::Table keyboard: Return on the focused row fires onPrimaryAction for THAT row",
     "table_input::table_input: onPrimaryAction across pointer, touch, keyboard and gamepad::Table gamepad: ButtonA on the focused row fires onPrimaryAction for THAT row",
     "table_input::table_input: onPrimaryAction across pointer, touch, keyboard and gamepad::Table gamepad: in edit mode a NON-reorderable table still opens rows — ButtonA is not swallowed by a grab it cannot do",
     "table_input::table_input: onPrimaryAction across pointer, touch, keyboard and gamepad::Table keyboard: in edit mode a NON-reorderable table still opens rows — Return is not swallowed by a grab it cannot do",
     "table_input::table_input: onPrimaryAction across pointer, touch, keyboard and gamepad::Table touch: a selection = \"none\" reorderable table still OPENS a row while editing",
     "table_input::table_input: onPrimaryAction across pointer, touch, keyboard and gamepad::a meta-less Activate is the TOUCH verb: it opens a primary-action table",
     "table_input::table_input: onPrimaryAction across pointer, touch, keyboard and gamepad::a table with NO onPrimaryAction behaves exactly as it does today on all four inputs",
     "table_input::table_input: a swipe that ends with no Activated does not eat the next device press::a swipe CANCELLED mid-gesture does not swallow the next gamepad Activate on that row",
     "table_input::table_input: a swipe that ends with no Activated does not eat the next device press::a swipe CANCELLED mid-gesture does not swallow the hit expander's Activate either",
     "table_input::table_input: a swipe that ends with no Activated does not eat the next device press::a swipe with a SILENT release still answers the next gamepad Activate with a tray close",
     "table_input::table_input: a swipe that ends with no Activated does not eat the next device press::a swipe whose release DID Activate still swallows that artifact, and the tray survives it",
     "table_input::table_input: a revealed tray and the edit minus are reachable by keyboard and pad::(R8-1) a revealed TRAILING tray joins the focused row's own group, and the pad walks into it",
     "table_input::table_input: a revealed tray and the edit minus are reachable by keyboard and pad::(R8-2) a LEADING tray is reachable too, and the ROW keeps the primary stop",
     "table_input::table_input: a revealed tray and the edit minus are reachable by keyboard and pad::(R8-3) the EDIT-MODE MINUS is a focus stop, and Activate on it deletes",
     "table_input::table_input: a revealed tray and the edit minus are reachable by keyboard and pad::(R8-5) LEFT off a focused row reaches THIS row's minus, never the row above's",
     "table_input::table_input: a revealed tray and the edit minus are reachable by keyboard and pad::(R8-6) a horizontal press NEVER changes rows — on the pad and on the keyboard alike",
     "table_input::table_input: a revealed tray and the edit minus are reachable by keyboard and pad::(R8-4) a CLOSED row is byte-identical to today: no wrapper path is a focus stop",
     "table_input::table_input: column priority-collapse and its disclosure (ADAPT-18)::THE DIRECTOR'S CASE: landscape holds three columns; portrait collapses Rating and says so",
     "table_input::table_input: column priority-collapse and its disclosure (ADAPT-18)::the collapse costs no selection and no focus — it is a hide, not a remount",
     "table_input::table_input: column priority-collapse and its disclosure (ADAPT-18)::a collapsed column leaves the focus ring and stops taking taps, subtree and all",
     "table_input::table_input: column priority-collapse and its disclosure (ADAPT-18)::the disclosure chip is a focus stop of its own, and a PAD opens the plate",
     "table_input::table_input: column priority-collapse and its disclosure (ADAPT-18)::a TOUCH tap opens it, and a second tap closes it — the trap a disclosure must not become",
     "table_input::table_input: column priority-collapse and its disclosure (ADAPT-18)::a MOUSE click opens it too, with the pointer meta a click actually carries",
     "table_input::table_input: column priority-collapse and its disclosure (ADAPT-18)::a KEYBOARD Return opens it, and the plate's own Close is the way out",
     "table_input::table_input: column priority-collapse and its disclosure (ADAPT-18)::a collapsed column's SORT is still selectable, from the plate",
     "table_input::table_input: column priority-collapse and its disclosure (ADAPT-18)::the plate closes by itself when the columns come back",
     "table_input::table_input: a rowActions-only table is reachable on keyboard and gamepad (ADAPT-13)::its rows are focus stops at all, which is the whole mechanism",
     "table_input::table_input: a rowActions-only table is reachable on keyboard and gamepad (ADAPT-13)::...so Delete on a focused row fires its destructive action (keyboard)",
     "table_input::table_input: a rowActions-only table is reachable on keyboard and gamepad (ADAPT-13)::...and a PAD reaches the same row and the same action",
     "table_input::table_input: a rowActions-only table is reachable on keyboard and gamepad (ADAPT-13)::NULL HYPOTHESIS: a table with no selection, no reorder AND no rowActions still has none",
     "table_input::table_input: multi-selection survives focus movement (ADAPT-14/15)::Ctrl+Down moves the ring and selects NOTHING — the focus cursor (ADAPT-14)",
     "table_input::table_input: multi-selection survives focus movement (ADAPT-14/15)::Shift+Down EXTENDS from the anchor instead of replacing (ADAPT-14)",
     "table_input::table_input: multi-selection survives focus movement (ADAPT-14/15)::a PLAIN arrow still replaces — the standard model, unchanged",
     "table_input::table_input: multi-selection survives focus movement (ADAPT-14/15)::a PAD holds two rows in edit mode: A toggles, and the ring does not undo it (ADAPT-15)",
     "table_input::table_input: multi-selection survives focus movement (ADAPT-14/15)::a KEYBOARD Return does the identical thing in edit mode",
     "table_input::table_input: multi-selection survives focus movement (ADAPT-14/15)::OUTSIDE edit mode the pad is unchanged: A on a row replaces",
     "table_input::table_input: edit mode says so, and a keyboard can reach it (ADAPT-27, M-4)::ADAPT-27: a selectable table paints a leading selection mark in edit mode, and it follows the selection",
     "table_input::table_input: edit mode says so, and a keyboard can reach it (ADAPT-27, M-4)::ADAPT-27: a table with NO selection grows no mark — the gutter is the handle's",
     "table_input::table_input: edit mode says so, and a keyboard can reach it (ADAPT-27, M-4)::ADAPT-27: a table with NO route into edit mode mounts no `When` at all — one node per ROW",
     "table_input::table_input: edit mode says so, and a keyboard can reach it (ADAPT-27, M-4)::R1-I5: the gutter and the mark are ONE gate — a routeless table insets nothing",
     "table_input::table_input: edit mode says so, and a keyboard can reach it (ADAPT-27, M-4)::M-4: a KEYBOARD-only session gets the Edit toggle, and can reorder a row end to end",
     "table_input::table_input: edit mode says so, and a keyboard can reach it (ADAPT-27, M-4)::M-4: a MOUSE session still shows nothing — it drags rows directly",
     "table_input::table_input: edit mode says so, and a keyboard can reach it (ADAPT-27, M-4)::M-4: a keyboard-only table that cannot reorder still shows nothing",
     "table_input::table_input: a focused row discloses its own truncated cell (ADAPT-20)::the premise: the row's cell really is truncated and really is disclosed",
     "table_input::table_input: a focused row discloses its own truncated cell (ADAPT-20)::a KEYBOARD focus on the row raises the plate with the whole value",
     "table_input::table_input: a focused row discloses its own truncated cell (ADAPT-20)::...and moving the ring to the next row re-answers for THAT row",
     "table_input::table_input: a focused row discloses its own truncated cell (ADAPT-20)::a focused HEADING discloses its own title, which is the other half of J-4",
     "table_input::table_input: a focused row discloses its own truncated cell (ADAPT-20)::NEGATIVE CONTROL: a row whose value FITS raises nothing",
     "table_input::table_input: whatever can collapse can be disclosed (R1-H1)::THE REVIEWER'S SWEEP: every collapsed width carries the chip and the dump record",
     "table_input::table_input: whatever can collapse can be disclosed (R1-H1)::...and the dump reports the collapse on a table that declared no floor",
     "table_input::table_input: whatever can collapse can be disclosed (R1-H1)::...and the plate OPENS on that table, which is the route the chip promises",
     "table_input::table_input: whatever can collapse can be disclosed (R1-H1)::NULL HYPOTHESIS: an unresized table with no declared floor still collapses nothing",
     "virtual_list_input::VirtualList pointer input (no consumer wiring)::VirtualList pointer: an engine scroll (driveScroll) slides the window",
     "virtual_list_input::VirtualList pointer input (no consumer wiring)::VirtualList pointer: a row tap activates through auto handleActivate",
     "virtual_list_input::VirtualList pointer input (no consumer wiring)::VirtualList pointer: partial rows crop via the engine clip host",
     "virtual_list_input::VirtualList touch input (no consumer wiring)::VirtualList touch: a native pan scroll slides the window (driveScroll)",
     "virtual_list_input::VirtualList touch input (no consumer wiring)::VirtualList touch: a garbage/negative mirror value cannot scroll above the top",
     "virtual_list_input::VirtualList touch input (no consumer wiring)::VirtualList touch: a row tap activates",
     "virtual_list_input::VirtualList keyboard input (no consumer wiring)::VirtualList keyboard: Up/Down moves focus row-by-row through the window",
     "virtual_list_input::VirtualList keyboard input (no consumer wiring)::VirtualList keyboard: focus past the window edge scrolls the row into view",
     "virtual_list_input::VirtualList keyboard input (no consumer wiring)::VirtualList keyboard: Return activates the focused row",
     "virtual_list_input::VirtualList gamepad input (no consumer wiring)::VirtualList gamepad: D-pad moves focus identically to arrows",
     "virtual_list_input::VirtualList gamepad input (no consumer wiring)::VirtualList gamepad: ButtonA activates the focused row",
     "virtual_list_input::VirtualList focus coherence + virtualization neutrality::VirtualList window-slide keeps focus coherent (focus follows the KEY, nearest-survivor on eviction)",
     "virtual_list_input::VirtualList focus coherence + virtualization neutrality::VirtualList scroll: window slide re-runs factories only for entering rows (survivors kept)",
     "virtual_list_input::VirtualList focus coherence + virtualization neutrality::VirtualList registry neutrality: build/mount/present/interact/dispose returns to baseline",
     "virtual_list_input::VirtualList native canvas (native scroll substrate)::declares the FULL virtual height as the scroll canvas and clips",
     "virtual_list_input::VirtualList native canvas (native scroll substrate)::the scroll canvas grows with the data (contentH tracks totalRows)",
     "virtual_list_input::VirtualList native canvas (native scroll substrate)::keep-visible writes a programmatic scrollTo target (scrollpos op) for an off-band focus",
     "virtual_list_input::VirtualList native canvas (native scroll substrate)::unbindNativeScroll stops the mirror (teardown cleanliness)",
     "virtual_list_input::VirtualList navigation-group overrides (SF-L4)::the group NAME is the list's own by default and is published for a sibling to reference",
     "virtual_list_input::VirtualList navigation-group overrides (SF-L4)::`exit` crosses to the named group where array-order fallthrough cannot",
     "virtual_list_input::VirtualList navigation-group overrides (SF-L4)::`wrap` wraps the row ring at both ends, and absent it still stops",
     "virtual_list_input::VirtualList navigation-group overrides (SF-L4)::the overrides never replace the group: the focus-skip predicates survive",
     "virtual_list_input::VirtualList navigation-group overrides (SF-L4)::an unknown navigation field is refused at construction, naming the valid set",
     "virtual_list_row_actions::VirtualList rowActions (Task 3): lazy hosted engines::(a) a mounted, scrolled, untouched list holds ZERO engines",
     "virtual_list_row_actions::VirtualList rowActions (Task 3): lazy hosted engines::(b) the first horizontal gesture on a row builds exactly ONE engine and engages that row",
     "virtual_list_row_actions::VirtualList rowActions (Task 3): lazy hosted engines::(b2) a gesture on a SECOND row builds a second engine; the first row is closed by the coordinator",
     "virtual_list_row_actions::VirtualList rowActions (Task 3): lazy hosted engines::(b3) a VERTICAL drag builds NO engine at all and never engages",
     "virtual_list_row_actions::VirtualList rowActions (Task 3): lazy hosted engines::(b3b) a touch FLING across many rows builds zero engines",
     "virtual_list_row_actions::VirtualList rowActions (Task 3): lazy hosted engines::(b4) a row whose rowActions() returns nil never builds an engine and declines the capture",
     "virtual_list_row_actions::VirtualList rowActions (Task 3): lazy hosted engines::(b5) a plain TAP activates the row, builds NO engine, and never engages",
     "virtual_list_row_actions::VirtualList rowActions (Task 3): lazy hosted engines::(c) rowActions + reorderable is refused at build (v1)",
     "virtual_list_row_actions::VirtualList rowActions (Task 3): lazy hosted engines::(d) MOUNT IDENTITY: a hosted list adds exactly ONE node, and no pointer surface per row",
     "virtual_list_row_actions::VirtualList rowActions (Task 3): lazy hosted engines::(d2) engaging and disengaging a row returns the mounted tree to the closed shape",
     "virtual_list_row_actions::VirtualList rowActions (Task 3): lazy hosted engines::(e) a scroll closes the engaged row (the shared coordinator's bindScroll)",
     "virtual_list_row_actions::VirtualList rowActions (Task 3): lazy hosted engines::(f) a plain list exposes the same debug shape with nothing hosted",
     "virtual_list_row_actions::VirtualList rowActions (Task 3): lazy hosted engines::(h) a row scrolled out from under a held finger still works on the next swipe",
     "virtual_list_row_actions::VirtualList rowActions (Task 3): lazy hosted engines::(i) a row REMOVED from the data mid-drag releases the engagement",
     "virtual_list_row_actions::VirtualList rowActions (Task 3): lazy hosted engines::(j) a RE-PRESENT drops engines built against the previous mount",
     "virtual_list_row_actions::VirtualList rowActions (Task 3): lazy hosted engines::(k) rowActions COEXISTS with dropSurface: the row is both a swipe hit and a drop target",
     "virtual_list_row_actions::VirtualList rowActions (Task 3): lazy hosted engines::(l) a destructive full swipe fires exactly ONCE, even if re-swiped during the commit",
     "virtual_list_row_actions::VirtualList rowActions (Task 3): lazy hosted engines::(m) N1: a row that GAINS actions when its item changes stops being declined",
     "virtual_list_row_actions::VirtualList rowActions (Task 3): lazy hosted engines::(n) N1: a row that LOSES its actions when its item changes drops its engine",
     "virtual_list_row_actions::VirtualList rowActions (Task 3): lazy hosted engines::(o) N2: a pen press is declined at the down — no capture, no engine",
     "virtual_list_row_actions::VirtualList rowActions (Task 3): lazy hosted engines::(g) engines survive their row leaving the window, and dispose with the list",
     "virtual_list_row_actions::VirtualList rowActions (Task 4): the shared tray overlay::(p) the overlay mounts at the engaged row's offset; the row slides by presentation",
     "virtual_list_row_actions::VirtualList rowActions (Task 4): the shared tray overlay::(w) a released gesture settles at the MEASURED tray width (Task 3 carried item 3)",
     "virtual_list_row_actions::VirtualList rowActions (Task 4): the shared tray overlay::(u) the tray is measured against the LIST'S OWN env, not the neutral theme (Task 3 carried item 1)",
     "virtual_list_row_actions::VirtualList rowActions (Task 4): the shared tray overlay::(q) a tap on a tray button fires its action exactly ONCE and closes the row",
     "virtual_list_row_actions::VirtualList rowActions (Task 4): the shared tray overlay::(r) disengaging unmounts the overlay, clears the slide, and returns the tree to the closed shape",
     "virtual_list_row_actions::VirtualList rowActions (Task 4): the shared tray overlay::(x) engagement moving straight from one row to another repaints the overlay for the NEW row",
     "virtual_list_row_actions::VirtualList rowActions (Task 4): the shared tray overlay::(x2) the row that loses engagement animates closed under the new one, monotonically",
     "virtual_list_row_actions::VirtualList rowActions (Task 4): the shared tray overlay::(v1) a tap OUTSIDE the list closes the open row, and does not swallow that tap",
     "virtual_list_row_actions::VirtualList rowActions (Task 4): the shared tray overlay::(v2) a press on ANOTHER row closes the open one, and still activates the row pressed",
     "virtual_list_row_actions::VirtualList rowActions (Task 4): the shared tray overlay::(v2b) F1: pressing an ACTIONLESS row closes the open one",
     "virtual_list_row_actions::VirtualList rowActions (Task 4): the shared tray overlay::(v2c) F1: a PEN press closes the open row too, and is still declined",
     "virtual_list_row_actions::VirtualList rowActions (Task 4): the shared tray overlay::(v3) a tap on the OPEN row's own slid content closes it, and fires no activate",
     "virtual_list_row_actions::VirtualList rowActions (Task 4): the shared tray overlay::(s) a destructive commit collapses THAT row's height, and only that row's",
     "virtual_list_row_actions::VirtualList rowActions (Task 4): the shared tray overlay::(y) the PHANTOM ROW: a fired commit leaves nothing standing, and the row is reusable",
     "virtual_list_row_actions::VirtualList rowActions (Task 4): the shared tray overlay::(z) an open row that unmounts and comes back is still slid",
     "virtual_list_row_actions::VirtualList rowActions (Task 4): the shared tray overlay::(t) a FULL SWIPE commits end-to-end on the mounted tree",
     "virtual_list_row_actions::VirtualList rowActions (Task 5): behavior parity::(P1) both edges reveal: a leftward swipe opens the TRAILING tray, a rightward one the LEADING",
     "virtual_list_row_actions::VirtualList rowActions (Task 5): behavior parity::(P2) the OPEN THRESHOLD: a release past half the tray width settles open, under it snaps back",
     "virtual_list_row_actions::VirtualList rowActions (Task 5): behavior parity::(P2b) REGRESSION: every later short swipe on the SAME row also snaps back (the reused spring)",
     "virtual_list_row_actions::VirtualList rowActions (Task 5): behavior parity::(P2c) REGRESSION: an INTERRUPTED drag on a re-used row springs back too",
     "virtual_list_row_actions::VirtualList rowActions (Task 5): behavior parity::(P3) fullSwipe = false: a full swipe past the commit threshold refuses to commit and settles OPEN",
     "virtual_list_row_actions::VirtualList rowActions (Task 5): behavior parity::(P4) a sibling row starting a gesture during a commit: the commit still fires exactly once",
     "virtual_list_row_actions::VirtualList rowActions (Task 5): behavior parity::(P5) a scroll during a commit: the commit still fires exactly once",
     "virtual_list_row_actions::VirtualList rowActions (Task 5): behavior parity::(P6) an outside tap during a commit: the commit fires once and the tapped control still activates",
     "virtual_list_row_actions::VirtualList rowActions (Task 5): behavior parity::(P7) one open at a time: a SETTLED open row closes when another row settles open",
     "virtual_list_row_actions::VirtualList rowActions (Task 5): behavior parity::(P8) keyboard Delete on the focused row hit fires its first destructive action",
     "virtual_list_row_actions::VirtualList rowActions (Task 5): behavior parity::(P8b) Delete on a row NOBODY has swiped fires its destructive action, building ONE engine",
     "virtual_list_row_actions::VirtualList rowActions (Task 5): behavior parity::(P8c) ButtonX on an unswiped row opens its action MENU — the pad's route to a non-destructive action",
     "virtual_list_row_actions::VirtualList rowActions (Task 5): behavior parity::(P8d) a d-pad WALK builds nothing: the press is what materialises an engine",
     "virtual_list_row_actions::VirtualList rowActions (Task 5): behavior parity::(P8e) a row that declares NO actions is untouched: Delete builds nothing and fires nothing",
     "virtual_list_row_actions::VirtualList rowActions (Task 5): behavior parity::(P8g) BOTH spellings of both verbs reach an unswiped row (the one shared key table)",
     "virtual_list_row_actions::VirtualList rowActions (Task 5): behavior parity::(P8h) the list owns Delete only while the ring is on an ACTIONABLE row",
     "virtual_list_row_actions::VirtualList rowActions (Task 5): behavior parity::(P8f) once the engine EXISTS it is the one that answers: Delete still fires exactly once",
     "virtual_list_row_actions::VirtualList rowActions (Task 5): behavior parity::(P9) focus TRAVERSAL: the d-pad reaches the revealed tray, and the ring comes BACK to the row",
     "virtual_list_row_actions::VirtualList rowActions (Task 5): behavior parity::(P9a) R1: EVERY close returns the ring to the row — scroll, Cancel and an outside tap too",
     "virtual_list_row_actions::VirtualList rowActions (Task 5): behavior parity::(P9c) a MULTI-ACTION tray navigates HORIZONTALLY, and is contained",
     "virtual_list_row_actions::VirtualList rowActions (Task 5): behavior parity::(P9d) a LEADING tray is entered from the other side (the edge decides the direction)",
     "virtual_list_row_actions::VirtualList rowActions (Task 5): behavior parity::(P9b) Tab reaches the revealed tray button too (the presenter's document-order rank)",
     "virtual_list_row_actions::VirtualList rowActions (Task 5): behavior parity::(P10) the ACTIVATE SEAM closes the open row: Return on ANOTHER row puts the tray away",
     "virtual_list_row_actions::VirtualList rowActions (Task 5): behavior parity::(P12) X1: an AUTHOR'S OWN exit does not lock the player out of a revealed tray",
     "virtual_list_row_actions::VirtualList rowActions (Task 5): behavior parity::(P13) X2: with EVERY row focus-skipped, the revealed tray is still reachable",
     "virtual_list_row_actions::VirtualList rowActions (Task 5): behavior parity::(P11) closing another row takes its action MENU with it, not just its tray",
     "virtual_list_row_actions::VirtualList rowActions (Task 5): behavior parity::(P10b) R3: a NON-POINTER Activate on the OPEN row's own hit closes it, exactly as a tap does",
     "virtual_list_row_actions::VirtualList rowActions (redteam NEW-1): a hosted swipe survives its own release Activate::RowActions hosted touch: a VirtualList swipe reveals the tray and fires no onActivate (silent release, control)",
     "virtual_list_row_actions::VirtualList rowActions (redteam NEW-1): a hosted swipe survives its own release Activate::RowActions hosted touch: a VirtualList swipe reveals the tray and survives its release Activate (after)",
     "virtual_list_row_actions::VirtualList rowActions (redteam NEW-1): a hosted swipe survives its own release Activate::RowActions hosted touch: a VirtualList swipe reveals the tray and survives its release Activate (before)",
     "virtual_list_row_actions::VirtualList rowActions (redteam NEW-1): a hosted swipe survives its own release Activate::RowActions hosted mouse: a VirtualList swipe reveals the tray and fires no onActivate (silent release, control)",
     "virtual_list_row_actions::VirtualList rowActions (redteam NEW-1): a hosted swipe survives its own release Activate::RowActions hosted mouse: a VirtualList swipe reveals the tray and survives its release Activate (after)",
     "virtual_list_row_actions::VirtualList rowActions (redteam NEW-1): a hosted swipe survives its own release Activate::RowActions hosted mouse: a VirtualList swipe reveals the tray and survives its release Activate (before)",
     "virtual_list_row_actions::VirtualList rowActions (redteam NEW-1): a hosted swipe survives its own release Activate::the arm is one activate wide: the tap AFTER a swipe's release Activate still closes the tray",
     "virtual_list_row_actions::VirtualList rowActions (redteam NEW-1): a hosted swipe survives its own release Activate::a hosted swipe CANCELLED mid-gesture does not swallow the next gamepad Activate",
     "virtual_list_row_actions::VirtualList rowActions (redteam NEW-1): a hosted swipe survives its own release Activate::a hosted swipe CANCELLED mid-gesture does not swallow the hit expander's Activate either",
     "virtual_list_row_actions::VirtualList rowActions (redteam NEW-1): a hosted swipe survives its own release Activate::a hosted swipe with a SILENT release still answers the next gamepad Activate with a close",
     "virtual_list_row_actions::VirtualList rowActions (redteam NEW-1): a hosted swipe survives its own release Activate::(defect 3) a vertical pan begun on a row does NOT fire that row's onActivate",
     "virtual_list_row_actions::VirtualList rowActions (redteam NEW-1): a hosted swipe survives its own release Activate::(defect 3) ...on BOTH sides of InputEnded, and upward too",
     "virtual_list_row_actions::VirtualList rowActions (redteam NEW-1): a hosted swipe survives its own release Activate::(defect 3) the pan's arm is spent by its own release and nothing more",
     "virtual_list_row_actions_identity::VirtualList rowActions: mount identity (the shipping differential)::a hosted list's mounted paths are the plain list's PLUS exactly the one overlay node",
     "virtual_list_row_actions_identity::VirtualList rowActions: mount identity (the shipping differential)::every shared path is the same node: class, rect, PROPS, elision and handler slots",
     "virtual_list_row_actions_identity::VirtualList rowActions: mount identity (the shipping differential)::engaging and disengaging returns the tree to the closed shape, with zero instance leaks",
     "virtual_list_row_actions_identity::VirtualList rowActions: mount identity (the shipping differential)::the plain list is untouched by the feature existing (the control's own control)",
     "virtual_list_hosted_seam::the hosted seam: READ == DECLARED == PASSED::every fact the hosted half reads off `deps` is declared in `Deps`",
     "virtual_list_hosted_seam::the hosted seam: READ == DECLARED == PASSED::every key `Deps` declares is one the hosted half actually reads",
     "virtual_list_hosted_seam::the hosted seam: READ == DECLARED == PASSED::the one construction site passes exactly the declared set — no more, no less",
     "virtual_list_hosted_seam::the hosted seam: READ == DECLARED == PASSED::there is exactly ONE construction site",
     "virtual_list_hosted_seam::the hosted seam: READ == DECLARED == PASSED::NEGATIVE CONTROL: each set comparison reports the pair that is out of step",
     "virtual_list_hosted_seam::the hosted seam: the shared record, and no stale twin of any cell::the hosted half writes through exactly ONE dep, and it is the record",
     "virtual_list_hosted_seam::the hosted seam: the shared record, and no stale twin of any cell::...and inside the record it writes exactly ONE field",
     "virtual_list_hosted_seam::the hosted seam: the shared record, and no stale twin of any cell::every cell of the record is read by the hosted half, and every one is written by the host",
     "virtual_list_hosted_seam::the hosted seam: the shared record, and no stale twin of any cell::NO STALE TWIN: neither file re-declares a cell of the record as a local",
     "virtual_list_hosted_seam::the hosted seam: the shared record, and no stale twin of any cell::NEGATIVE CONTROL: the write scans see each shape they claim to",
     "virtual_list_hosted_seam::the hosted seam: the surface, and the one-way require::the hosted half offers exactly twelve names, and they are the ones the host rebinds",
     "virtual_list_hosted_seam::the hosted seam: the surface, and the one-way require::a list with no `rowActions` builds NOTHING reactive — the construction site may be unconditional",
     "virtual_list_hosted_seam::the hosted seam: the surface, and the one-way require::the hosted half requires the control NOWHERE — the require is one-way",
     "virtual_list_hosted_seam::the hosted seam: the surface, and the one-way require::the two mounted-path ids have ONE spelling, published here and read there",
     "virtual_list_hosted_seam::the hosted seam: the surface, and the one-way require::the hosted half holds no module-level mutable state",
     "auto_input_screens::auto-screens WP-1: a Grid of Buttons gets 2D D-pad navigation, zero opts::initial focus lands on the first grid cell",
     "auto_input_screens::auto-screens WP-1: a Grid of Buttons gets 2D D-pad navigation, zero opts::Left/Right move within a row (DPad); Up/Down cross rows keeping the column",
     "auto_input_screens::auto-screens WP-1: a Grid of Buttons gets 2D D-pad navigation, zero opts::Left at the start of a row is contained (grid 2D nav, no cross-row wrap)",
     "auto_input_screens::auto-screens WP-1: a Grid is linked into the page around it::Down from the LAST row leaves the grid and reaches what follows it",
     "auto_input_screens::auto-screens WP-1: a Grid is linked into the page around it::Up from the FIRST row leaves the grid the other way",
     "auto_input_screens::auto-screens WP-1: a Grid is linked into the page around it::the inner rows still navigate as a grid, and left/right are still contained",
     "auto_input_screens::auto-screens WP-1: a Grid is linked into the page around it::two adjacent grids hand off to each other at the seam",
     "auto_input_screens::auto-screens WP-1: a Grid is linked into the page around it::a grid alone on a screen is unchanged: its boundary rows stay contained",
     "auto_input_screens::auto-screens WP-1: an HStack row navigates horizontally, zero opts::Right/Left (keyboard) walk the row; the row is the horizontal axis",
     "auto_input_screens::auto-screens WP-1: an HStack row navigates horizontally, zero opts::DPadLeft/DPadRight drive the same horizontal row (gamepad)",
     "auto_input_screens::auto-screens WP-1: a plain vertical screen keeps the byte-identical flat ring::Down advances in document order and WRAPS at the end (unchanged flat behavior)",
     "auto_input_screens::auto-screens WP-1: refresh re-derives auto layout groups after a structural change::a grid row added after present becomes reachable through the derived groups",
     "auto_input_screens::auto-screens WP-2: an outside tap dismisses the top modal, never clicks through::a stack of two modals pops exactly one per outside tap, base is never activated",
     "auto_input_screens::auto-screens WP-2: an outside tap dismisses the top modal, never clicks through::a tap INSIDE the top modal routes normally (dispatches, does not dismiss)",
     "auto_input_screens::auto-screens WP-2: an outside tap dismisses the top modal, never clicks through::outsideTapCancel=false swallows the outside tap (barrier; no dismiss, no clickthrough)",
     "auto_input_screens::auto-screens WP-2: an outside tap dismisses the top modal, never clicks through::with NO modal, base taps route normally (unchanged)",
     "auto_input_screens::auto-screens WP-3: a modal's close button is keyboard-reachable and Activate-dismisses::Navigate reaches the close Button; Return activates it and dismisses the modal end-to-end",
     "auto_input_screens::auto-screens WP-4: inputHint flips with preferredInput, no remount::the hint text prop follows env preferredInput via preferredBinding; zero factory reruns",
     "auto_input_screens::auto-screens WP-4: inputHint flips with preferredInput, no remount::style=\"phrase\" answers the whole affordance, so a consumer never branches on input class",
     "auto_input_screens::auto-screens WP-4: inputHint flips with preferredInput, no remount::displayName-less bindings fall back to the key/button name, and nil is tolerated",
     "late_contributions::DB-4: a contribution mounted AFTER present is discovered and wired::the region is closed at present time, so nothing of it is wired yet",
     "late_contributions::DB-4: a contribution mounted AFTER present is discovered and wired::opening the region wires handleActivate — a REAL finger tap dispatches",
     "late_contributions::DB-4: a contribution mounted AFTER present is discovered and wired::...and its one-time bindings run exactly once, on the frame it appears",
     "late_contributions::DB-4: a contribution mounted AFTER present is discovered and wired::a LATE focusGroups upgrades the scope's navigation model (flat -> grouped)",
     "late_contributions::DB-4: a contribution mounted AFTER present is discovered and wired::closing the region drops it again: a dispatch can never reach a dead control",
     "late_contributions::DB-3(b): the focus RING answers navigation, never a finger::a touch tap MOVES focus and paints NO ring",
     "late_contributions::DB-3(b): the focus RING answers navigation, never a finger::a NAVIGATION verb brings the ring back, on the node the finger left it on",
     "late_contributions::DB-3(b): the focus RING answers navigation, never a finger::a tap AFTER navigating puts the ring away again (no stale ring behind a finger)",
     "late_contributions::DB-3(b): the focus RING answers navigation, never a finger::keyboard/gamepad-only sessions are untouched: the ring paints from the first move",
     "gamepad_contention::gamepad_contention (client module, headless structural spec)::requires cleanly under Lune — no engine global touched at module scope",
     "gamepad_contention::gamepad_contention (client module, headless structural spec)::describeContention() returns a string carrying all three §3 truths and the resolution",
     "gamepad_contention::gamepad_contention (client module, headless structural spec)::disableLegacyControls() is guarded: headless it returns false, never throws",
     "gamepad_contention::gamepad_contention (client module, headless structural spec)::legacyStackActive() is a guarded probe: headless it returns false, never throws",
     "gamepad_contention::gamepad_contention (client module, headless structural spec)::cameraKeysContended() sees the camera holding the arrows in a live reading",
     "gamepad_contention::gamepad_contention (client module, headless structural spec)::cameraKeysContended() is false once the camera's binding leaves CAS",
     "gamepad_contention::gamepad_contention (client module, headless structural spec)::cameraKeysContended() answers each of the four arrows, not just Left/Right",
     "gamepad_contention::gamepad_contention (client module, headless structural spec)::cameraKeysContended() is a guarded probe: headless it returns false, never throws",
     "gamepad_contention::gamepad_contention (client module, headless structural spec)::cameraKeysContended() tolerates a bound action with no inputTypes",
     "gamepad_contention::gamepad_contention (client module, headless structural spec)::iasPlayerScriptsActive() sees the engine's default IAS contexts",
     "gamepad_contention::gamepad_contention (client module, headless structural spec)::iasPlayerScriptsActive() is false under the legacy stack, and headless",
     "gamepad_contention::gamepad_contention (client module, headless structural spec)::disableLegacyControls() is INERT when IAS owns the player scripts (DF-9)",
     "gamepad_contention::gamepad_contention (client module, headless structural spec)::disableLegacyControls() is UNCHANGED where the legacy stack still runs",
     "gamepad_contention::gamepad_contention (client module, headless structural spec)::disableLegacyControls(injectedParent) also stays guarded headless",
     "responder::responder (a): a passive HUD leaves avatar input untouched::Space/ButtonA/Up all reach gameplay and the HUD focus never moves",
     "responder::responder (b): a tap on the HUD's focusable engages it (first responder)::engaged: Up/Down navigate the HUD and NOT gameplay; Space/ButtonA guarded; Activate works",
     "responder::responder (b): a tap on the HUD's focusable engages it (first responder)::handle.engage() is the explicit public path to first responder",
     "responder::responder (c): resign restores gameplay delivery exactly::Cancel (ButtonB) resigns an engaged-from-passive HUD; gameplay restored",
     "responder::responder (c): resign restores gameplay delivery exactly::an outside tap resigns an engaged-from-passive HUD; gameplay restored, no clickthrough",
     "responder::responder (d): a modal over gameplay suppresses avatar input; dismiss restores::Space/ButtonA/Up are suppressed while the modal is up and restored on dismiss",
     "responder::responder (e): gameplayGuard=false lets Space reach gameplay while engaged::Space reaches gameplay; Up is still sunk (nav owns it)",
     "responder::responder (f): present() default (no responder opt) is byte-identical::nav works immediately AND gameplay ALSO receives (documented UI-only-place semantics)",
     "responder::responder (g): engage/resign churn is leak-free and registry-neutral::50 engage/resign cycles leave a single working context and gameplay intact",
     "responder::responder: the engaged band sits strictly above the gameplay sink (2000)::a modal's context priority is >= 3000 (mirrors CAS High) and > 2000",
     "responder::responder: the engaged band sits strictly above the gameplay sink (2000)::an engaged-from-passive context sits above 2000",
     "responder::responder: Sink changes go through the setSink adapter seam::engage() calls setSink(true) and resign() calls setSink(false) on the context",
     "modal_dismissal::modal dismissal: empty-space tap dismisses (Drive-F1 scrim catcher)::tapping the scrim far outside the panel dismisses the top modal",
     "modal_dismissal::modal dismissal: the synthesized catcher is never a skin target::declares chrome_slots.NO_SLOT, so no package can paint over the screen it guards",
     "modal_dismissal::modal dismissal: 24px forgiveness ring around the painted panel::a tap within 24px of the panel edge does NOT dismiss (near-miss forgiveness)",
     "modal_dismissal::modal dismissal: 24px forgiveness ring around the painted panel::a tap beyond the 24px ring dismisses",
     "modal_dismissal::modal dismissal: an invisible fullscreen root contributes nothing to Zone A::a tap outside the small painted panel still dismisses, despite the fill root",
     "modal_dismissal::modal dismissal: a visible fullscreen takeover has no outside::a tap anywhere lands in Zone A (the painted fullscreen panel) — no dismiss",
     "modal_dismissal::modal dismissal: outsideTapCancel=false is a true barrier (swallow)::a Zone-B scrim tap neither dismisses nor clicks through",
     "modal_dismissal::modal dismissal: outsideTapCancel=false is a true barrier (swallow)::a direct tap on a lower node (path-outside) is also swallowed",
     "modal_dismissal::modal dismissal: stacked modals pop only the top::an empty-space scrim tap pops M2, then M1, one per tap",
     "modal_dismissal::modal dismissal: the scrim is non-focusable and absent from navigation::focus stays inside the modal and never lands on the scrim",
     "modal_dismissal::modal dismissal: the scrim is non-focusable and absent from navigation::the scrim root's DisplayOrder is beneath the top modal",
     "modal_dismissal::modal dismissal: scrim=\"none\" is transparent but still catches::a Zone-B tap still dismisses, and the catcher renders without a scrim surface",
     "modal_dismissal::modal dismissal: scrim open/dismiss churn is registry-neutral::live node count returns to baseline after churn (scrim disposed with the modal)",
     "modal_dismissal::modal dismissal: cancelPolicy::the DEFAULT is unchanged — Cancel dismisses a modal and resigns an engaged HUD",
     "modal_dismissal::modal dismissal: cancelPolicy::cancelPolicy='none' makes Cancel a no-op on the modal, however many times it is pressed",
     "modal_dismissal::modal dismissal: cancelPolicy::cancelPolicy='none' does NOT imply a pointer barrier — the two opts are orthogonal",
     "modal_dismissal::modal dismissal: cancelPolicy::cancelPolicy='none' still offers Cancel to the focused contribution first",
     "modal_dismissal::modal dismissal: cancelPolicy::cancelPolicy='none' on a passive HUD keeps it engaged (Cancel no longer resigns)",
     "modal_dismissal::modal dismissal: cancelPolicy::an unknown cancelPolicy is refused at present time, naming the set",
     "paradigm_popup::PopupButton parity: outside-tap dismiss::a tap on a rendered node OUTSIDE the open panel dismisses it (no selection change)",
     "paradigm_popup::PopupButton parity: outside-tap dismiss::the synthesized catcher dismisses on a tap in EMPTY space (Zone B geometry)",
     "paradigm_popup::PopupButton parity: outside-tap dismiss::a tap on the trigger itself (inside the subtree) toggles closed, never routes to dismiss",
     "paradigm_popup::PopupButton parity: gamepad Cancel::gamepad ButtonB while open closes the popup (no selection change)",
     "paradigm_popup::PopupButton parity: gamepad Cancel::gamepad ButtonB with the popup CLOSED does not consume Cancel (falls through)",
     "paradigm_popup::PopupButton parity: focus is trapped while open::REGRESSION: closing always lands focus on a LIVE focusable (never nil/dangling)",
     "paradigm_popup::PopupButton parity: focus is trapped while open::REGRESSION: re-activating the trigger to close keeps focus on the trigger",
     "paradigm_popup::PopupButton parity: focus is trapped while open::gamepad Navigate cannot ESCAPE the open popup into a sibling (focus trap)",
     "paradigm_popup::PopupButton parity: close restores focus to the trigger from any path::row-focused close (api.close) restores focus to the trigger",
     "paradigm_popup::PopupButton parity: close restores focus to the trigger from any path::Cancel-row Activate closes without selecting and restores focus to the trigger",
     "paradigm_popup::PopupButton parity: close restores focus to the trigger from any path::gamepad ButtonB close restores focus to the trigger from a focused row",
     "paradigm_popup::PopupButton parity: close restores focus to the trigger from any path::outside-tap close restores focus to the trigger from a focused row",
     "paradigm_popup::PopupButton parity: dismiss-while-open is registry-neutral::open popup -> pres.dismiss + dispose returns every core counter to baseline",
     "popup_catcher_paint::popup catcher paint: an open row-actions tray::swiping LEFT TO RIGHT to reveal the leading tray must not paint a plate over the viewport",
     "popup_catcher_paint::popup catcher paint: an open PopupButton::the same catcher, reached through the other outsideDismiss owner, is equally unpainted",
     "popup_catcher_paint::popup catcher paint: an open PopupButton::sits beneath its owner's DisplayOrder — which is why only the uncovered chrome band showed it",
     "popup_catcher_paint::every synthesized catcher resolves to a number somebody chose::the oracle can see the difference between a role and a class default",
     "popup_catcher_paint::every synthesized catcher resolves to a number somebody chose::...and skipping the STATE rules is a fact about the sheet, not an assumption",
     "popup_catcher_paint::every synthesized catcher resolves to a number somebody chose::a region's EXPAND plate mounts a barrier that wears the scrim ROLE",
     "popup_catcher_paint::every synthesized catcher resolves to a number somebody chose::...and the player's PreferredTransparency composes against it, uncapped",
     "popup_catcher_paint::every synthesized catcher resolves to a number somebody chose::every catcher role resolves to a number somebody chose, and the two invisible ones to 1",
     "popup_catcher_paint::every synthesized catcher resolves to a number somebody chose::...and the two live `plain` owners really do mount a catcher that resolves to 1",
     "popup_catcher_paint::every synthesized catcher resolves to a number somebody chose::the synthesized scrim ROOT is linked to the same sheet as the screen it covers",
     "paradigm_input_axis::input-axis: Thumbstick1 latch (deadzone + re-center, no autorepeat)::crossing +threshold fires exactly one step; holding past it does NOT repeat",
     "paradigm_input_axis::input-axis: Thumbstick1 latch (deadzone + re-center, no autorepeat)::must re-center below the release threshold before the next step re-arms",
     "paradigm_input_axis::input-axis: Thumbstick1 latch (deadzone + re-center, no autorepeat)::the opposite direction steps once re-centered (negative crossing)",
     "paradigm_input_axis::input-axis: Thumbstick1 latch (deadzone + re-center, no autorepeat)::deadzone: a sub-threshold push never steps",
     "paradigm_input_axis::input-axis: Thumbstick1 latch (deadzone + re-center, no autorepeat)::the bound component is respected: an x-only push does NOT drive a y binding",
     "paradigm_input_axis::input-axis: Thumbstick1 latch (deadzone + re-center, no autorepeat)::a higher-priority Sink context consumes the axis (arbitration parity)",
     "paradigm_input_axis::input-axis: Thumbstick1 latch (deadzone + re-center, no autorepeat)::axis bindings never leak into the keyCode (deviceKey) path",
     "paradigm_input_axis::input-axis: presenter binds Thumbstick1 for Navigate (vertical, flat ring)::a Thumbstick1 y push moves focus down like DPadDown, with the latch",
     "paradigm_input_axis::input-axis: presenter binds Thumbstick1 for NavigateH (horizontal, grouped)::a Thumbstick1 x push walks an HStack row like DPadRight",
     "paradigm_input_axis::input-axis: bare-control Adjust auto-wiring (dynamic, focus-gated)::no Adjust key is bound while focus sits on a NON-target (no gameplay shadow)",
     "paradigm_input_axis::input-axis: bare-control Adjust auto-wiring (dynamic, focus-gated)::Adjust keys bind the moment focus lands on the declared target",
     "paradigm_input_axis::input-axis: bare-control Adjust auto-wiring (dynamic, focus-gated)::keyboard Adjust (Left/Comma) on the focused target routes to handleAdjust",
     "paradigm_input_axis::input-axis: bare-control Adjust auto-wiring (dynamic, focus-gated)::gamepad Adjust (ButtonR1) on the focused target routes to handleAdjust",
     "paradigm_input_axis::input-axis: bare-control Adjust auto-wiring (dynamic, focus-gated)::moving focus back OFF the target unbinds the Adjust keys again",
     "paradigm_input_axis::input-axis: bare-control Adjust auto-wiring (dynamic, focus-gated)::consumer opts.onAdjust wins per-opt and binds unconditionally (back-compat)",
     "dpad_repeat::actions: gamepad-source classification (director item 4 plumbing)::DPad/Button keyCodes classify as gamepad",
     "dpad_repeat::actions: gamepad-source classification (director item 4 plumbing)::keyboard keyCodes (Up/Down/Left/Right) classify as NOT gamepad",
     "dpad_repeat::actions: gamepad-source classification (director item 4 plumbing)::an axis binding (Thumbstick*) is always gamepad-classed",
     "dpad_repeat::actions: gamepad-source classification (director item 4 plumbing)::a scriptable binding (no keyCode) is never gamepad",
     "dpad_repeat::actions: gamepad-source classification (director item 4 plumbing)::dedup: a same-value re-fire from a DIFFERENT source class never overwrites lastGamepad",
     "dpad_repeat::d-pad auto-repeat: presenter cadence and gating::holding DPadDown steps once immediately, then repeats after 0.4s, then every 0.1s",
     "dpad_repeat::d-pad auto-repeat: presenter cadence and gating::a slow first frame catches up: a dt spanning several intervals fires every owed step in one tick",
     "dpad_repeat::d-pad auto-repeat: presenter cadence and gating::holding a keyboard arrow (Down) does NOT auto-repeat",
     "dpad_repeat::d-pad auto-repeat: presenter cadence and gating::releasing before the initial delay elapses cancels the repeat entirely",
     "dpad_repeat::d-pad auto-repeat: presenter cadence and gating::repeat honors the ring's OWN wrap policy exactly like a single press would",
     "dpad_repeat::d-pad auto-repeat: presenter cadence and gating::a held Thumbstick1 push repeats through the SAME glue, at the SAME cadence — no double-fire",
     "dpad_repeat::d-pad auto-repeat: presenter cadence and gating::dismissing the surface mid-repeat stops it (scope-owned, no leaked timer)",
     "dpad_repeat::d-pad auto-repeat + the axis-suspend Adjust seam (found while building item 4)::a repeat that lands focus on a vertical-adjust target stops cleanly, with no stuck Direction1D",
     "dpad_repeat::d-pad auto-repeat + RED-TEAM C1: ownership can be lost mid-hold without a dismiss::Variant A — resign() mid-hold stops the repeat and zeroes navigate.state, not just heldDir",
     "dpad_repeat::d-pad auto-repeat + RED-TEAM C1: ownership can be lost mid-hold without a dismiss::Variant B — a modal opening mid-hold stops the BACKGROUNDED repeat instead of hijacking the modal's own ring",
     "paradigm_table::paradigm_table: bare-Table column resize via keyboard/gamepad Adjust::the headers group walks ONE stop per column (the column handle)",
     "paradigm_table::paradigm_table: bare-Table column resize via keyboard/gamepad Adjust::no Adjust key is bound while focus sits off a RESIZABLE column (no bumper shadow)",
     "paradigm_table::paradigm_table: bare-Table column resize via keyboard/gamepad Adjust::keyboard Comma/Period on the focused grip resize the column by the step",
     "paradigm_table::paradigm_table: bare-Table column resize via keyboard/gamepad Adjust::gamepad ButtonL1/ButtonR1 on the focused grip resize the column",
     "paradigm_table::paradigm_table: bare-Table column resize via keyboard/gamepad Adjust::A selects the focused column, then Left/Right resizes it",
     "paradigm_table::paradigm_table: bare-Table column resize via keyboard/gamepad Adjust::keyboard drives the same model: Return selects, arrows resize, Cancel releases",
     "paradigm_table::paradigm_table: bare-Table column resize via keyboard/gamepad Adjust::a selected column cannot be left behind: moving focus off it releases it",
     "paradigm_table::paradigm_table: bare-Table column resize via keyboard/gamepad Adjust::resize clamps to the column minWidth",
     "paradigm_table::paradigm_table: hot-switch in-flight transitions (§C)::pointer drag mid-flight + pointer capability lost => CANCEL, revert, no reorder",
     "paradigm_table::paradigm_table: hot-switch in-flight transitions (§C)::gamepad grab mid-grab + gamepad capability lost => CANCEL, order reverts, grab cleared",
     "paradigm_table::paradigm_table: hot-switch in-flight transitions (§C)::control-owned edit-mode + all of touch/gamepad leave (pure pointer) => toggle unmounts AND editing resets (no wedge)",
     "paradigm_table::paradigm_table: hot-switch in-flight transitions (§C)::an OWNER-held editing signal is NOT reset by the control on a pure-pointer flip",
     "paradigm_table::paradigm_table: hot-switch in-flight transitions (§C)::CARRY: edit-mode persists while gamepad stays live after touch leaves",
     "paradigm_table::paradigm_table: hot-switch in-flight transitions (§C)::CARRY: a grabbed row keeps following Navigate when a pointer arrives (gamepad stays live)",
     "paradigm_table::paradigm_table: hot-switch in-flight transitions (§C)::CARRY: native scroll position survives a class flip",
     "paradigm_table::paradigm_table: reorderable + selection-none keeps rows reachable (live-drive regression)::DPadDown leaves the auto Edit/Done toggle and reaches a row",
     "paradigm_table::paradigm_table: reorderable + selection-none keeps rows reachable (live-drive regression)::gamepad grab-mode reorders end to end with selection none",
     "paradigm_table::paradigm_table: reorderable + selection-none keeps rows reachable (live-drive regression)::a selection-none NON-reorderable table keeps rows out of the ring (unchanged)",
     "paradigm_table::paradigm_table: class-flip-then-dispose is registry-neutral::grab, rip the classes away (cancel fires), dispose: counters at baseline",
     "paradigm_table::paradigm_table: the row box adapts, and cells can never outgrow it::a touch session gets a taller row than a pointer session, with no screen asking",
     "paradigm_table::paradigm_table: the row box adapts, and cells can never outgrow it::a raised text preference GROWS the row instead of spilling out of it",
     "paradigm_table::paradigm_table: the row box adapts, and cells can never outgrow it::a cell's text never exceeds its row — the device defect, at every text size",
     "paradigm_table::paradigm_table: the row box adapts, and cells can never outgrow it::a PINNED rowHeight still wins — and its cells are capped to it, not to the derived row",
     "paradigm_table::paradigm_table: the row box adapts, and cells can never outgrow it::a package that carves a border GROWS the row — the cell never pays for the frame",
     "paradigm_table::paradigm_table: the row box adapts, and cells can never outgrow it::...and a FLAT package's rows are byte-identical to the untethered ones",
     "paradigm_table::paradigm_table: the row box adapts, and cells can never outgrow it::a pin below ONE LINE of its own text is raised to it, never drawn through",
     "paradigm_table::paradigm_table: a surface onGeometry is additive, not an override::a contribution is fed with no surface hook (today's behaviour, unchanged)",
     "paradigm_table::paradigm_table: a surface onGeometry is additive, not an override::...and is STILL fed when the surface asks for geometry of its own",
     "paradigm_table::paradigm_table: a metrics snapshot missing the derived row keys falls back::still solves a real row, at neutral's own derived height",
     "paradigm_table::paradigm_table: a metrics snapshot missing the derived row keys falls back::a NaN typography scale or text offset does not produce a NaN row",
     "paradigm_hover::UI-PARADIGM-001: pointer hover layer::the default style defines a hover role distinct from the control rest fill",
     "paradigm_hover::UI-PARADIGM-001: pointer hover layer::a pointer-live renderer enables hover on interactive nodes and synthetic hover flips their state",
     "paradigm_hover::UI-PARADIGM-001: pointer hover layer::a pure-touch device never allocates hover wiring, and nothing essential lives behind hover",
     "paradigm_hover::UI-PARADIGM-001: pointer hover layer::non-interactive nodes never get hover wiring even on pointer",
     "paradigm_hover::verifier F3: a pointer arriving mid-session enables hover on existing nodes::a pure-touch tree gains hover wiring on its mounted Buttons when a mouse joins",
     "paradigm_tenfoot::environment ten-foot derivations (matrix D.1/D.3)::typographyScale composes the ten-foot 1.5x multiplier over the preference",
     "paradigm_tenfoot::environment ten-foot derivations (matrix D.1/D.3)::sizeClass caps at regular on a ten-foot Large display even on a wide viewport",
     "paradigm_tenfoot::UI-PARADIGM-003: ten-foot text scaling at the paint seam (matrix D.1)::a near display leaves rendered text sizes unscaled (typographyScale 1)",
     "paradigm_tenfoot::UI-PARADIGM-003: ten-foot text scaling at the paint seam (matrix D.1)::a ten-foot display scales rendered text sizes by 1.5x",
     "paradigm_tenfoot::UI-PARADIGM-003: ten-foot text scaling at the paint seam (matrix D.1)::layout re-solves and rendered text grows when displaySize flips to Large",
     "paradigm_tenfoot::ADAPT-7: the ten-foot type floor reaches UNAUTHORED text (matrix D.1)::an unsized Text rides the body role: 16 near, 24 at ten-foot",
     "paradigm_tenfoot::ADAPT-7: the ten-foot type floor reaches UNAUTHORED text (matrix D.1)::an unsized Button label rides the control role, and a Toggle the body role",
     "paradigm_tenfoot::ADAPT-7: the ten-foot type floor reaches UNAUTHORED text (matrix D.1)::measure agrees: the unsized box grows on the flip, exactly as the sized one does",
     "paradigm_tenfoot::ADAPT-7: the ten-foot type floor reaches UNAUTHORED text (matrix D.1)::a near display is byte-identical: the floor is a ten-foot rung, not a rescale",
     "paradigm_tenfoot::UI-PARADIGM-003: ten-foot focus strengthening (matrix D.4)::focus visuals differ between a Large display and a Medium display",
     "paradigm_tenfoot::UI-PARADIGM-003: ten-foot focus strengthening (matrix D.4)::a near display keeps the focus profile at near (2px-ring semantics unchanged)",
     "paradigm_tenfoot::verifier F1: Toggle honors an explicit textSize at the MEASURE seam::a Toggle with an explicit textSize measures taller than the 16pt default (measure/paint agree)",
     "paradigm_tenfoot::verifier F2: a displaySize flip re-strengthens the RESTING focus visual::focus already on a control adopts the ten-foot profile when the display docks to Large",
     "paradigm_tenfoot::ten-foot focus lift is capped to bounds that fit the screen (director live find)::a small control lifts; a full-width control keeps the strong ring with NO lift",
     "paradigm_tenfoot::ten-foot focus lift is capped to bounds that fit the screen (director live find)::a near display never lifts regardless of size",
     "paradigm_tenfoot::ten-foot default overscan margins (director live find: edge-flush UI on a TV-class display)::effectiveOverscanInsets defaults to the ten-foot 60/90 under Large when none are authored",
     "paradigm_tenfoot::ten-foot default overscan margins (director live find: edge-flush UI on a TV-class display)::an unauthored Large display insets the solved tree by the default margins",
     "paradigm_tenfoot::ADAPT-10: the table row box has a ten-foot rung, and distance selects it::the snapshot publishes THREE rungs, and the ten-foot floor is the largest",
     "paradigm_tenfoot::ADAPT-10: the table row box has a ten-foot rung, and distance selects it::a table on a Large display gets a TALLER row than the same table near",
     "paradigm_tenfoot::ADAPT-10: the table row box has a ten-foot rung, and distance selects it::a 41px control fits inside a ten-foot row — the defect that measured it",
     "paradigm_tenfoot::director item 5: a ROG-Ally-shaped device does NOT get console/ten-foot treatment::1920x1080 + gamepad + touch + engine-reported Large reads NEAR, with handheld-appropriate (1x) type scale",
     "paradigm_tenfoot::director item 5: a ROG-Ally-shaped device does NOT get console/ten-foot treatment::a PS5-shaped device (1920x1080, gamepad, NO touch) stays ten-foot — unchanged",
     "paradigm_tenfoot::director item 5: a ROG-Ally-shaped device does NOT get console/ten-foot treatment::gamepad input alone never changes the classing (the director's suspicion, measured false)",
     "paradigm_tenfoot::director item 5: a ROG-Ally-shaped device does NOT get console/ten-foot treatment::an honestly-reported Small handheld is untouched by the correction",
     "paradigm_tenfoot::director item 5: a ROG-Ally-shaped device does NOT get console/ten-foot treatment::navPlacement's gamepad branch is unchanged by this fix (audited, not altered)",
     "paradigm_textinput::UI-PARADIGM-002 text entry: CARRY when a device arrives mid-edit::a gamepad arriving mid-edit CARRIES the edit (editing stays, text intact)",
     "paradigm_textinput::UI-PARADIGM-002 text entry: CARRY when a device arrives mid-edit::touch leaving mid-edit CARRIES while a hardware keyboard stays live (keyboard supersedes the OSK)",
     "paradigm_textinput::UI-PARADIGM-002 text entry: CANCEL -> commit when the editing surface leaves::touch (the on-screen-keyboard surface) leaving mid-edit ends the edit via commit (no wedge)",
     "paradigm_textinput::UI-PARADIGM-002 text entry: CANCEL -> commit when the editing surface leaves::after the editing-surface loss the sink context comes down and navigation resumes",
     "paradigm_textinput::UI-PARADIGM-002 text entry: CANCEL -> commit when the editing surface leaves::the keep-visible offset resets when the on-screen keyboard clears on dock (mid-edit)",
     "paradigm_carry::UI-PARADIGM-002: framework CARRY rows (matrix §C)::focus survives any class flip (ring stays put; every live class can keep driving it)",
     "paradigm_carry::UI-PARADIGM-002: framework CARRY rows (matrix §C)::an exclusive modal survives any class flip (never un-engages; dismissal paths intact)",
     "chip::Chip control::builds, mounts, and renders headlessly with no error",
     "chip::Chip control::Chip pointer: a tap activates through the presenter (device-true)",
     "chip::Chip control::Chip touch: a touch tap/pan drives the control (device-true)",
     "chip::Chip control::Chip keyboard: focus + Return drives the control",
     "chip::Chip control::Chip gamepad: focus + ButtonA drives the control",
     "chip::Chip control::Chip pointer affordance: the pointer structural idiom (hover/direct-drag/wheel)",
     "chip::Chip control::Chip touch affordance: the touch structural idiom (44px/naked-pan-scroll/edit-grip)",
     "chip::Chip control::Chip keyboard affordance: the keyboard structural idiom (focus ring/Navigate/Activate/Adjust)",
     "chip::Chip control::Chip gamepad affordance: the gamepad structural idiom (focus/grab/A-B/ten-foot)",
     "chip::Chip control::flipping selected updates the surface with no factory rerun",
     "chip::Chip control::dump() is deterministic and reflects the live state",
     "chip::Chip control::build/mount/present/interact/dispose returns every registry to baseline",
     "chip::Chip control::rejects a missing `selected` at BUILD, naming the control and the field",
     "chip::Chip control::rejects a read-only Memo `selected` at BUILD",
     "authoring::M0-A1: invalid authoring fails at construction::an unknown property names the control, the property, and the valid alternatives",
     "authoring::M0-A1: invalid authoring fails at construction::a misspelled property suggests the intended one",
     "authoring::M0-A1: invalid authoring fails at construction::a property borrowed from another control is rejected, not silently frozen in",
     "authoring::M0-A1: invalid authoring fails at construction::a wrongly typed value names the expected type and the property's purpose",
     "authoring::M0-A1: invalid authoring fails at construction::a bare number where a dimension belongs explains the dimension table",
     "authoring::M0-A1: invalid authoring fails at construction::an unknown dimension kind is rejected",
     "authoring::M0-A1: invalid authoring fails at construction::a bad padding side is rejected",
     "authoring::M0-A1: invalid authoring fails at construction::a Signal on a prop that is read once at mount is rejected",
     "authoring::M0-A1: invalid authoring fails at construction::a missing required property names it and says what it is for",
     "authoring::M0-A1: invalid authoring fails at construction::children on a leaf are rejected and the containers are listed",
     "authoring::M0-A1: invalid authoring fails at construction::When.condition must be a Signal/Memo, not a plain boolean",
     "authoring::M0-A1: invalid authoring fails at construction::an unrecognised enum value is rejected with the closed value set",
     "authoring::M0-A1: invalid authoring fails at construction::valid authoring is unaffected",
     "authoring::M0-A4: diagnosed properties instead of silent drops::Text.color is diagnosed with its replacement instead of scheduling dropped paint work",
     "authoring::M0-A4: diagnosed properties instead of silent drops::Text.font is diagnosed because it reached measure but never paint",
     "authoring::M0-A4: diagnosed properties instead of silent drops::every diagnosed property is in the deprecation ledger with all required fields",
     "authoring::M0-A4: diagnosed properties instead of silent drops::a reactive style hint is re-applied instead of being dropped by the refresh loop",
     "authoring::M0-A4: diagnosed properties instead of silent drops::a BOUND enum value outside the closed set is refused at the write site, not silently ignored",
     "authoring::M0-A4: diagnosed properties instead of silent drops::...and a LATER set into an illegal value is refused too, so a valid first frame proves nothing",
     "authoring::M0-A4: diagnosed properties instead of silent drops::a legal bound enum value on a NON-surface prop is unaffected",
     "authoring::M0-A4: diagnosed properties instead of silent drops::a reactive `active` flag is re-applied instead of being dropped",
     "authoring::M0-A5: the extension seam is genuinely public::the input-contribution seam is exported from the library entry point",
     "authoring::M0-A5: the extension seam is genuinely public::an attached bundle rides the internal meta channel, never the public prop bag",
     "authoring::M0-A5: the extension seam is genuinely public::mount carries meta through so the presenter still discovers the bundle",
     "authoring::M0-A5: the extension seam is genuinely public::a non-table value under the reserved key reads as no contribution",
     "authoring::M0-A6: every public constructor is visible to conformance::each UI.* constructor has a control contract and a registry row",
     "authoring::M0-A6: every public constructor is visible to conformance::the previously invisible primitives are registered",
     "authoring::M0-A6: every public constructor is visible to conformance::Grip is registered as an INTERACTIVE control with a hit floor",
     "authoring::M0-A6: every public constructor is visible to conformance::every constructor's class exists in the schema and vice versa",
     "authoring::M0-A3: one reconciled property model::the live repository passes the property-parity check",
     "authoring::M0-A3: one reconciled property model::a plain-language comment inside a record type is not read as a field",
     "authoring::M0-A3: one reconciled property model::it detects an undocumented property",
     "authoring::M0-A3: one reconciled property model::it detects a repainting property the refresh loop cannot re-apply",
     "authoring::M0-A3: one reconciled property model::it detects a restricted class the authority manifest does not back",
     "authoring::M0-A3: one reconciled property model::it detects a class left OUTSIDE a restriction that still claims the binding",
     "authoring::M0-A3: one reconciled property model::the dirty map is derived from the schema for every class",
     "authoring::M0-A3: one reconciled property model::every reactive property declares dirty classes and vice versa",
     "authoring::M0-A2: useful exported public types::every class exports a spec type carrying its schema fields",
     "authoring::M0-A2: useful exported public types::no public constructor takes a bare untyped spec",
     "authoring::F-6 (BP-F5): the schema authority tables are frozen::a consumer of the tooling seam cannot disable framework validation",
     "authoring::F-6 (BP-F5): the schema authority tables are frozen::forClass hands out the same deeply frozen spec",
     "authoring::F-6 (BP-F5): the schema authority tables are frozen::the derived accessors still hand out FRESH tables the caller may keep",
     "authoring::F-34 (BP-F17/BP-F19): UI.PROP_DIRTY is frozen::the published dirty map cannot be rewritten",
     "authoring::F-34 (BP-F17/BP-F19): UI.PROP_DIRTY is frozen::mount.luau only READS it",
     "authoring::F-22 (BP-F18): a blueprint is immutable all the way down::props cannot be rewritten after construction",
     "authoring::F-22 (BP-F18): a blueprint is immutable all the way down::children cannot be inserted into after construction",
     "authoring::F-22 (BP-F18): a blueprint is immutable all the way down::a modifier's copy is frozen too — a shared template cannot be mutated through it",
     "authoring::F-22 (BP-F18): a blueprint is immutable all the way down::UI.background's inflated backing layer is frozen too",
     "authoring::F-7 (BP-F4): a modifier error names the modifier the author typed::UI.alignment on a UI.When names UI.alignment AND UI.When",
     "authoring::F-7 (BP-F4): a modifier error names the modifier the author typed::UI.frame on a UI.When names UI.frame",
     "authoring::F-7 (BP-F4): a modifier error names the modifier the author typed::a WRONG-TYPE write through a modifier names the modifier too",
     "authoring::F-7 (BP-F4): a modifier error names the modifier the author typed::a DIRECT constructor error is unchanged — no modifier is named",
     "authoring::F-19 (BP-F11): overflow = \"clip\" implies clipChildren::a VStack that declares clip becomes a clip host",
     "authoring::F-19 (BP-F11): overflow = \"clip\" implies clipChildren::an explicitly authored clipChildren always wins",
     "authoring::F-19 (BP-F11): overflow = \"clip\" implies clipChildren::every other overflow value stays declared intent and drives nothing",
     "authoring::F-19 (BP-F11): overflow = \"clip\" implies clipChildren::a BOUND overflow implies nothing — clipChildren is construction-only",
     "authoring::F-20 (BP-F12): Divider/Path thickness take the metric-name grammar::UI.Divider takes a theme metric name",
     "authoring::F-20 (BP-F12): Divider/Path thickness take the metric-name grammar::a bogus name errors naming the metric vocabulary",
     "authoring::F-20 (BP-F12): Divider/Path thickness take the metric-name grammar::a metric-named Divider thickness SOLVES to the theme's number",
     "authoring::F-20 (BP-F12): Divider/Path thickness take the metric-name grammar::an authored Divider thickness reaches the solve at all",
     "authoring::F-20 (BP-F12): Divider/Path thickness take the metric-name grammar::the schema doc states the TRUTHFUL default, not '1 px'",
     "authoring::gap 19 (W3-B): `surface = \"badge\"` carries an intrinsic minimum::an undimensioned badge floors to the theme's minimum, on both axes",
     "authoring::gap 19 (W3-B): `surface = \"badge\"` carries an intrinsic minimum::an explicit dim still wins — the census sites' own shipped content does not move",
     "authoring::gap 19 (W3-B): `surface = \"badge\"` carries an intrinsic minimum::the floor reaches Image and ZStack too — RascalRally's avatar/initial seal shape",
     "authoring::gap 19 (W3-B): `surface = \"badge\"` carries an intrinsic minimum::a non-badge surface is untouched — the floor is surface-scoped, not class-scoped",
     "authoring::F-21 (BP-F14): the suggester knows the two reserved keys::a typo'd `id` suggests it",
     "authoring::F-21 (BP-F14): the suggester knows the two reserved keys::a typo'd `children` suggests it",
     "layout_vocabulary::A-LV1: Grid children with fill dimensions::a fill-width cell occupies its whole column instead of collapsing to content",
     "layout_vocabulary::A-LV1: Grid children with fill dimensions::a fill-height cell occupies the row height set by its tallest sibling",
     "layout_vocabulary::A-LV1: Grid children with fill dimensions::a minColumnWidth grid derives its column count and still fills",
     "layout_vocabulary::A-LV1: Grid children with fill dimensions::a non-fill cell keeps content sizing and aligns inside its column",
     "layout_vocabulary::A-LV1: Grid children with fill dimensions::a cell aligns vertically inside a taller row",
     "layout_vocabulary::A-LV1: Grid children with fill dimensions::the grid never produces a negative cell width when gaps exceed the container",
     "layout_vocabulary::A-LV1: Spacer expands along the stack's main axis by default::a bare Spacer pushes its siblings apart in a VStack",
     "layout_vocabulary::A-LV1: Spacer expands along the stack's main axis by default::a bare Spacer expands horizontally inside an HStack",
     "layout_vocabulary::A-LV1: Spacer expands along the stack's main axis by default::two bare Spacers share the leftover space equally (centring idiom)",
     "layout_vocabulary::A-LV1: Spacer expands along the stack's main axis by default::an explicit dimension on a Spacer still wins",
     "layout_vocabulary::A-LV1: Spacer expands along the stack's main axis by default::a Spacer outside a stack axis stays inert (no unbounded growth)",
     "layout_vocabulary::A-LV2: frame::sets fixed dimensions",
     "layout_vocabulary::A-LV2: frame::expresses a min/max band",
     "layout_vocabulary::A-LV2: frame::`maxWidth = \"infinity\"` fills the available space, like a declarative fill",
     "layout_vocabulary::A-LV2: frame::rejects an unknown frame field instead of ignoring it",
     "layout_vocabulary::A-LV2: padding, offset, aspectRatio, alignment::padding insets a container's children",
     "layout_vocabulary::A-LV2: padding, offset, aspectRatio, alignment::padding works on a label without wrapping it",
     "layout_vocabulary::A-LV2: padding, offset, aspectRatio, alignment::padding accepts per-side values",
     "layout_vocabulary::A-LV2: padding, offset, aspectRatio, alignment::padding on a class that cannot inset says so",
     "layout_vocabulary::A-LV2: padding, offset, aspectRatio, alignment::offset moves a node inside an Anchor without affecting siblings",
     "layout_vocabulary::A-LV2: padding, offset, aspectRatio, alignment::aspectRatio derives the height from the width",
     "layout_vocabulary::A-LV2: padding, offset, aspectRatio, alignment::aspectRatio rejects a non-positive ratio",
     "layout_vocabulary::A-LV2: padding, offset, aspectRatio, alignment::alignment sets the ZStack placement props",
     "layout_vocabulary::A-LV2: padding, offset, aspectRatio, alignment::an unknown alignment value fails at construction",
     "layout_vocabulary::A-LV2: padding, offset, aspectRatio, alignment::a modifier writing a property the class does not have is rejected",
     "layout_vocabulary::A-LV2: overlay and background::overlay layers content ABOVE the node in a ZStack",
     "layout_vocabulary::A-LV2: overlay and background::background layers content BEHIND the node",
     "layout_vocabulary::A-LV2: overlay and background::the background layer FILLS what it is backing",
     "layout_vocabulary::A-LV2: overlay and background::an OVERLAY is still an ornament: it keeps its own size",
     "layout_vocabulary::A-LV2: overlay and background::the base node keeps its own id inside the wrapper, so it stays addressable",
     "layout_vocabulary::A-LV2: overlay and background::both modifiers require the base to carry an id",
     "layout_vocabulary::A-LV2: modifier order is explicit::the LAST writer of a property wins",
     "layout_vocabulary::A-LV2: modifier order is explicit::a modifier never mutates the blueprint it was given",
     "layout_vocabulary::A-LV4: aspectRatio against a fill sibling::derives the height from the RESOLVED fill width, not from zero",
     "layout_vocabulary::A-LV4: aspectRatio against a fill sibling::works in an HStack's cross axis too",
     "layout_vocabulary::A-LV4: aspectRatio against a fill sibling::a grid cell derives its aspect from the derived column width",
     "layout_vocabulary::A-LV4: aspectRatio against a fill sibling::an explicit height still wins over the aspect derivation",
     "layout_vocabulary::A-LV4: overlay does not collapse its base::the wrapper inherits the base's dimensions",
     "layout_vocabulary::A-LV4: overlay does not collapse its base::background inherits them too",
     "layout_vocabulary::A-LV4: overlay does not collapse its base::a base with no explicit dims still content-sizes as before",
     "layout_vocabulary::RS-DIR9: Grid minColumnWidth = \"intrinsic\"::columns are derived from the WIDEST child, not from a literal",
     "layout_vocabulary::RS-DIR9: Grid minColumnWidth = \"intrinsic\"::a narrower font packs MORE columns from the same declaration",
     "layout_vocabulary::RS-DIR9: Grid minColumnWidth = \"intrinsic\"::a numeric minColumnWidth is untouched",
     "layout_vocabulary::RS-DIR9: Grid minColumnWidth = \"intrinsic\"::the schema accepts the one string and refuses every other",
     "layout_vocabulary::RS-DIR9: a container measures in the box it will OCCUPY::a percent-capped panel contains the grid it arranges",
     "layout_vocabulary::RS-DIR9: a container measures in the box it will OCCUPY::a FIXED-width container is clamped the same way",
     "layout_vocabulary::RS-DIR9: Grid itemSizing = \"uniform\"::every cell takes the MAX measured size, on both axes",
     "layout_vocabulary::RS-DIR9: Grid itemSizing = \"uniform\"::the default is byte-unchanged: `natural` and an absent option agree",
     "layout_vocabulary::RS-DIR9: Grid itemSizing = \"uniform\"::a bigger font grows every cell together (the theme-swap property)",
     "layout_vocabulary::RS-DIR9: Grid itemSizing = \"uniform\"::the grid's own measured height counts uniform rows",
     "layout_vocabulary::RS-DIR9: Grid itemSizing = \"uniform\"::the schema accepts the two modes and refuses anything else",
     "layout_vocabulary::RS-DIR14: a Grid's ROW gap is its own axis::opens the ROWS without moving a single column",
     "layout_vocabulary::RS-DIR14: a Grid's ROW gap is its own axis::counts in the grid's own measured height, so a taller grid reports taller",
     "layout_vocabulary::RS-DIR14: a Grid's ROW gap is its own axis::defaults to `gap`: an absent rowGap is byte-identical to today",
     "layout_vocabulary::RS-DIR14: a Grid's ROW gap is its own axis::never touches the COLUMN COUNT — the property the director's ruling needs",
     "layout_vocabulary::RS-DIR14: a Grid's ROW gap is its own axis::is a spacing METRIC at the boundary, so a theme swap moves it",
     "layout_vocabulary::F-3: a structural modifier carries the content's meta channel::an INFLATED backing layer keeps its drag declaration",
     "layout_vocabulary::F-3: a structural modifier carries the content's meta channel::an input-contribution bundle survives the same inflate",
     "layout_vocabulary::F-3: a structural modifier carries the content's meta channel::a backing layer with its OWN dimensions is passed through untouched, meta included",
     "layout_vocabulary::F-3: a structural modifier carries the content's meta channel::UI.overlay never rebuilt its content, and still does not",
     "layout_vocabulary::shrink pass: the two edges the RED-TEAM named, pinned as they actually behave::(a) THE CASCADE: three shrinkable labels in a 120px box all land INSIDE it",
     "layout_vocabulary::shrink pass: the two edges the RED-TEAM named, pinned as they actually behave::(b) shrinkWeight DOES NOT change which ViewThatFits candidate wins, at ANY width",
     "layout_vocabulary::shrink pass: the two edges the RED-TEAM named, pinned as they actually behave::(c) ...and the candidate that WINS is still shrunk to the offer it is given",
     "layout_vocabulary::shrink pass: the two edges the RED-TEAM named, pinned as they actually behave::(d) the fit probe is part of the MEASURE MEMO's key, or a row reserves the wrong height",
     "stack_distribution::§2.6 distribute — the default is today, byte for byte::an absent `distribute` packs from the start of the axis",
     "stack_distribution::§2.6 distribute — the default is today, byte for byte::an explicit `distribute = \"start\"` is the same arrangement",
     "stack_distribution::§2.6 distribute — the three flex distributions::spaceBetween puts the leftover in the gaps only (0 / 200 / 400)",
     "stack_distribution::§2.6 distribute — the three flex distributions::spaceEvenly gives every gap AND both ends the same share (50 / 200 / 350)",
     "stack_distribution::§2.6 distribute — the three flex distributions::spaceAround gives the ends a HALF share (33 / 199 / 366)",
     "stack_distribution::§2.6 distribute — the three flex distributions::center and end move the whole group without changing the gaps",
     "stack_distribution::§2.6 distribute — the three flex distributions::a declared `gap` is kept and the distribution rides on top of it",
     "stack_distribution::§2.6 distribute — the three flex distributions::a single child: spaceBetween pins it at the start, spaceAround centres it",
     "stack_distribution::§2.6 distribute — the three flex distributions::a vertical stack distributes along Y, not X",
     "stack_distribution::§2.6 distribute — the three flex distributions::no leftover means no movement: distribute cannot invent space",
     "stack_distribution::§2.6 distribute — the composition rule with `fill`::a fill child takes the whole leftover, so distribute has nothing left",
     "stack_distribution::§2.6 distribute — the composition rule with `fill`::...and says so: the conflict is a diagnostic, never a silent no-op",
     "stack_distribution::§2.6 distribute — the composition rule with `fill`::a `fill` child under the default \"start\" is NOT a conflict and files nothing",
     "stack_distribution::§2.4 shrink — the default preserves today's geometry exactly::without `shrinkWeight` an overflowing row still paints outside its box",
     "stack_distribution::§2.4 shrink — the default preserves today's geometry exactly::...and the overflow diagnostic keeps its exact historic wording",
     "stack_distribution::§2.4 shrink — the inner distribution (`shrinkWeight`)::equal weights and equal bases split the deficit evenly",
     "stack_distribution::§2.4 shrink — the inner distribution (`shrinkWeight`)::the weight is MULTIPLIED BY THE BASIS, so the small child is not collapsed first",
     "stack_distribution::§2.4 shrink — the inner distribution (`shrinkWeight`)::`shrinkWeight = 0` (the default) never shrinks, whatever its siblings do",
     "stack_distribution::§2.4 shrink — the inner distribution (`shrinkWeight`)::a bigger weight gives up proportionally more",
     "stack_distribution::§2.4 shrink — the outer sort (`layoutPriority`)::the lowest tier absorbs the whole deficit before the next tier gives a pixel",
     "stack_distribution::§2.4 shrink — the outer sort (`layoutPriority`)::a tier that runs out of room passes the rest UP to the next tier",
     "stack_distribution::§2.4 shrink — the outer sort (`layoutPriority`)::a NEGATIVE priority is the first to give way",
     "stack_distribution::§2.4 shrink — the floor cascade: minMax.min, longest word, compact form, ellipsis, clip::`minMax.min` is a hard floor inside its own tier",
     "stack_distribution::§2.4 shrink — the floor cascade: minMax.min, longest word, compact form, ellipsis, clip::a text node stops at the width of its longest word while that is enough",
     "stack_distribution::§2.4 shrink — the floor cascade: minMax.min, longest word, compact form, ellipsis, clip::...and DESCENDS past it, to an ellipsis, when the longest word is not enough",
     "stack_distribution::§2.4 shrink — the floor cascade: minMax.min, longest word, compact form, ellipsis, clip::...and a declared COMPACT form is spent BEFORE any of the string is",
     "stack_distribution::§2.4 shrink — the floor cascade: minMax.min, longest word, compact form, ellipsis, clip::THE CLOSED SEAM: the row RESERVES the cross extent a squeezed text will need",
     "stack_distribution::§2.4 shrink — the floor cascade: minMax.min, longest word, compact form, ellipsis, clip::...and the measure-side pass is INERT: the same row without a weight is unmoved",
     "stack_distribution::§2.4 shrink — the floor cascade: minMax.min, longest word, compact form, ellipsis, clip::...and the REPORT moves with the RESERVATION: a `content` row fits what it shrank into",
     "stack_distribution::§2.4 shrink — the floor cascade: minMax.min, longest word, compact form, ellipsis, clip::...and the shrunk text is RE-MEASURED at the reduced offer, so its box covers its wraps",
     "stack_distribution::§2.4 shrink — the floor cascade: minMax.min, longest word, compact form, ellipsis, clip::the measure MEMO tells the two offers apart — the cache key already covers the pass",
     "stack_distribution::§2.4 shrink — the floor cascade: minMax.min, longest word, compact form, ellipsis, clip::...and the MEASURE-side pass is keyed by the memo too, at the same key",
     "stack_distribution::§2.4 shrink — the floor cascade: minMax.min, longest word, compact form, ellipsis, clip::with no floor left the row fits exactly and nothing overflows",
     "stack_distribution::§2.4 shrink — when everything is at its floor the diagnostic still fires::the overflow diagnostic names the priority order the pass tried",
     "stack_distribution::§2.4 shrink — when everything is at its floor the diagnostic still fires::a row that never opted in gets NO priority suffix (today's message, unchanged)",
     "stack_distribution::§2.4 shrink — composition with the rest of the algebra::a CONTENT-sized nested stack shrinks like any other once it declares a weight",
     "stack_distribution::§2.4 shrink — composition with the rest of the algebra::freed space goes to a `fill` sibling rather than becoming slack",
     "stack_distribution::§2.4 shrink — composition with the rest of the algebra::margins are not shrinkable: the deficit comes out of the child's own extent",
     "stack_distribution::§2.1 lineAlign — per-child cross-axis alignment::without it a short child sits at the start of the line (today)",
     "stack_distribution::§2.1 lineAlign — per-child cross-axis alignment::`center` centres just that child in the line",
     "stack_distribution::§2.1 lineAlign — per-child cross-axis alignment::`end` pins it to the far edge of the line",
     "stack_distribution::§2.1 lineAlign — per-child cross-axis alignment::`stretch` fills the line, exactly as the container word does",
     "stack_distribution::§2.1 lineAlign — per-child cross-axis alignment::it beats the container's own `align`",
     "stack_distribution::§2.1 lineAlign — per-child cross-axis alignment::it beats a nested stack's own `align`, which now means ONLY 'align my children'",
     "stack_distribution::§2.1 lineAlign — per-child cross-axis alignment::it works on a LEAF, which `align` never could — the ItemLineAlignment gap",
     "stack_distribution::§2.1 lineAlign — per-child cross-axis alignment::a VERTICAL stack aligns on X",
     "stack_distribution::the props are public, strict, and reach the solver::`distribute` on a real HStack moves the real rects",
     "stack_distribution::the props are public, strict, and reach the solver::`distribute` is accepted on Screen, VStack, HStack and AdaptiveStack — and nowhere else",
     "stack_distribution::the props are public, strict, and reach the solver::a `distribute` outside the closed set is refused at construction",
     "stack_distribution::the props are public, strict, and reach the solver::`shrinkWeight`, `layoutPriority` and `lineAlign` are shared box props",
     "stack_distribution::the props are public, strict, and reach the solver::a real overflowing row shrinks and the solver stops complaining",
     "stack_distribution::the props are public, strict, and reach the solver::`lineAlign` on a real Text child aligns it in its row",
     "stack_distribution::§2.4 shrink — C5/C6: the pass costs nothing until it has a member::STRUCTURAL C5: arrange scans for a member BEFORE allocating `shrunk` and `basisMain`",
     "stack_distribution::§2.4 shrink — C5/C6: the pass costs nothing until it has a member::STRUCTURAL C6: measure's PASS 1 allocates a basis only for a weight ABOVE zero",
     "stack_distribution::§2.4 shrink — C5/C6: the pass costs nothing until it has a member::BEHAVIOURAL: an explicit `shrinkWeight = 0` is byte-identical to omitting it",
     "stack_distribution::§2.4 shrink — C5/C6: the pass costs nothing until it has a member::BEHAVIOURAL: the hoisted guard does NOT disable the pass — a real member still shrinks",
     "stack_distribution::§2.4 shrink — C5/C6: the pass costs nothing until it has a member::BEHAVIOURAL: an overflow with no member still diagnoses, and WITHOUT the floor suffix",
     "grid_row::the flow grid is exactly what it was::a three-column flow grid places and sizes its cells exactly as before",
     "grid_row::the flow grid is exactly what it was::...and an `intrinsic` + `uniform` flow grid — the Rascal Rally shape — is unmoved",
     "grid_row::row mode: the columns come from every row at once::column n is as wide as the widest natural cell in column n",
     "grid_row::row mode: the columns come from every row at once::the grid REPORTS the columns it derived, so a hug parent reserves the right box",
     "grid_row::row mode: the columns come from every row at once::`rowGap` is the row pitch here too, and a row is as tall as its tallest cell",
     "grid_row::row mode: the columns come from every row at once::a `fill` cell takes its whole column; a content cell aligns inside it",
     "grid_row::row mode: a span covers columns, and widens none of them::a spanning cell contributes to NO single column's maximum",
     "grid_row::row mode: a span covers columns, and widens none of them::a span covers the gaps between the columns it crosses, not just their widths",
     "grid_row::row mode: a span covers columns, and widens none of them::a partial span still leaves the columns it did not cover to their own cells",
     "grid_row::row mode never paints outside the box it was given::columns wider than the grid are reduced proportionally — and the grid SAYS so",
     "grid_row::row mode never paints outside the box it was given::...and a row of CONTENT-sized cells narrows in silence, because that is honest",
     "grid_row::the two authoring errors row mode can produce::a MIX of rows and loose cells is refused, and the grid keeps the flow reading",
     "grid_row::the two authoring errors row mode can produce::a GridRow outside a Grid names the fix instead of inventing columns",
     "grid_row::GridRow through the public authoring surface::`UI.Grid` of `UI.GridRow`s lays out through mount + render with no diagnostics",
     "grid_row::GridRow through the public authoring surface::`gridSpan` reaches the solver from a blueprint prop",
     "grid_row::GridRow through the public authoring surface::a GridRow refuses the box properties its grid owns, and names the fix",
     "grid_row::GridRow through the public authoring surface::a row grid navigates as a GRID on a D-pad, from its declared rows",
     "grid_row::GridRow through the public authoring surface::`gridSpan` is a SHARED box prop, so any cell can carry it",
     "grid_column_flow::UI.Grid column flow — the transpose of the one arithmetic::every cell's rect is the row-flow rect with both axes exchanged",
     "grid_column_flow::UI.Grid column flow — the transpose of the one arithmetic::...and neither mode files a diagnostic the other does not",
     "grid_column_flow::UI.Grid column flow — the transpose of the one arithmetic::lanes run DOWN and lines advance RIGHTWARD, at frozen offsets",
     "grid_column_flow::UI.Grid column flow — the transpose of the one arithmetic::a `fill` cell takes its LANE, which under column flow is its height",
     "grid_column_flow::UI.Grid column flow — the transpose of the one arithmetic::minColumnWidth counts lanes down the height under column flow",
     "grid_column_flow::UI.Grid column flow — the transpose of the one arithmetic::ADAPT-23: a ten-foot grid gets FEWER lanes than the same grid at near distance",
     "grid_column_flow::UI.Grid column flow — the transpose of the one arithmetic::minColumnWidth = 'intrinsic' measures the LANE axis, not always the width",
     "grid_column_flow::UI.Grid column flow — the transpose of the one arithmetic::uniform item sizing equalises cells in both modes alike",
     "grid_column_flow::UI.Grid column flow — the transpose of the one arithmetic::`flow` on a ROW grid is a diagnostic, not a second layout",
     "grid_column_flow::UI.Grid column flow — the transpose of the one arithmetic::alignH/alignV turn with the mode: a cell aligns in its LANE and in its LINE",
     "grid_column_flow::UI.Grid column flow — the transpose of the one arithmetic::an aspect cell derives its FLOW extent from the lane, the right way round",
     "grid_column_flow::UI.Grid column flow — the transpose of the one arithmetic::an unbounded lane axis yields one lane, in either mode",
     "flow_wrap::flow-wrap: the line partition is pure, and greedy::packs greedily against the offer, gaps counted only BETWEEN items",
     "flow_wrap::flow-wrap: the line partition is pure, and greedy::an item WIDER than the line still gets a line — of its own",
     "flow_wrap::flow-wrap: the line partition is pure, and greedy::an UNBOUNDED offer is one line: a partition of infinity is not a wrap",
     "flow_wrap::flow-wrap: the line partition is pure, and greedy::no children is no lines",
     "flow_wrap::flow-wrap: the line partition is pure, and greedy::fuzz: every child lands in exactly one line, in order, and only a lone item may overflow",
     "flow_wrap::flow-wrap: what a wrapping stack REPORTS::content = (widest line) x (sum of line extents + a gap between lines)",
     "flow_wrap::flow-wrap: what a wrapping stack REPORTS::a line's cross extent is its TALLEST item, not the first one's",
     "flow_wrap::flow-wrap: the cross-axis rule Roblox actually has (probed 2026-08-13)::Top (the default): the block of lines packs at the start",
     "flow_wrap::flow-wrap: the cross-axis rule Roblox actually has (probed 2026-08-13)::Center: the BLOCK is centred — (400-80)/2 = 160 exactly, not per line",
     "flow_wrap::flow-wrap: the cross-axis rule Roblox actually has (probed 2026-08-13)::End: the block sits at 400-80 = 320, again exactly",
     "flow_wrap::flow-wrap: the cross-axis rule Roblox actually has (probed 2026-08-13)::ragged: line 2 starts at the TALLEST item of line 1 (90), never at 40",
     "flow_wrap::flow-wrap: the cross-axis rule Roblox actually has (probed 2026-08-13)::a vertical wrap wraps into COLUMNS: same rule, axes swapped",
     "flow_wrap::flow-wrap: the vocabulary it INHERITS rather than invents::`lineAlign` places one item inside its own line (Roblox ItemLineAlignment)",
     "flow_wrap::flow-wrap: the vocabulary it INHERITS rather than invents::`lineAlign = 'stretch'` fills the LINE, and only the line",
     "flow_wrap::flow-wrap: the vocabulary it INHERITS rather than invents::`distribute` spreads the leftover WITHIN each line, per line",
     "flow_wrap::flow-wrap: the vocabulary it INHERITS rather than invents::...and `distribute = 'center'` centres each line's own content",
     "flow_wrap::flow-wrap: measure and arrange cannot disagree about the lines::the hug parent's box is exactly the box the children were placed in",
     "flow_wrap::flow-wrap: measure and arrange cannot disagree about the lines::the plan is keyed by the BOX: a SHRUNK wrapping stack re-breaks its lines",
     "flow_wrap::flow-wrap: an item wider than its line::gets its own line, is CLAMPED to the line, and the solver says so",
     "flow_wrap::flow-wrap: the lines can overflow the CROSS axis, and that is now visible::more lines than fit reports on the cross axis, with the pixel count",
     "flow_wrap::flow-wrap: the lines can overflow the CROSS axis, and that is now visible::...and a stack whose lines DO fit says nothing at all",
     "flow_wrap::flow-wrap: `align = 'stretch'` is refused, not quietly reinterpreted::a LITERAL stretch on a wrapping stack is a construction error naming both readings",
     "flow_wrap::flow-wrap: `align = 'stretch'` is refused, not quietly reinterpreted::...and a non-wrapping stack keeps it, exactly as it always had",
     "flow_wrap::flow-wrap: `align = 'stretch'` is refused, not quietly reinterpreted::a stretch that only a SOLVE can see is refused there, and treated as start",
     "flow_wrap::flow-wrap: a `fill` child on the main axis::takes a whole line to itself, and is named rather than ignored",
     "flow_wrap::flow-wrap: the placement-prop audit polices a wrapping stack::an unknown kind polices NOTHING — which is why the row has to exist",
     "flow_wrap::flow-wrap: the placement-prop audit polices a wrapping stack::`lineAlign` IS read by a wrapping stack; `alignH` beside it is NOT",
     "flow_wrap::flow-wrap: the placement-prop audit polices a wrapping stack::the SHRINK PAIR is refused: wrapping IS the answer to a main-axis deficit",
     "flow_wrap::flow-wrap: the placement-prop audit polices a wrapping stack::...and a NON-wrapping stack still reads the shrink pair, unmoved",
     "flow_wrap::flow-wrap: the prop, through the authoring surface::`wrap` is declared on VStack and HStack, reactive, and dirties measure",
     "flow_wrap::flow-wrap: the prop, through the authoring surface::...and NOWHERE else: a `wrap` a solver branch would never read is refused",
     "flow_wrap::flow-wrap: the prop, through the authoring surface::a non-boolean `wrap` is a construction error",
     "flow_wrap::flow-wrap: the prop, through the authoring surface::`UI.HStack{ wrap = true }` wraps for real, through mount + render",
     "flow_wrap::flow-wrap: the prop, through the authoring surface::...and the SAME stack without `wrap` still runs off its own box, unmoved",
     "flow_wrap::flow-wrap: the prop, through the authoring surface::a BOUND `wrap` flips in place: the same stack, re-solved, never remounted",
     "flow_wrap::flow-wrap: it does NOT compose with newVirtualList, and that is refused::the same list WITHOUT `wrap` builds, so the refusal below is about the word",
     "flow_wrap::flow-wrap: it does NOT compose with newVirtualList, and that is refused::`newVirtualList{ wrap = true }` is refused: the spec is CLOSED",
     "flow_wrap::flow-wrap: the gallery surface holds under every shipped package::no diagnostic, in any package, at any of the three viewports",
     "flow_wrap::flow-wrap: the gallery surface holds under every shipped package::...and no two tags ever overlap, in any package, at any viewport",
     "flow_wrap::flow-wrap: the gallery surface holds under every shipped package::a WIDER package really does re-break the lines — the sweep is not vacuous",
     "geometry_solve_coalescing::one geometry change is one solve::a batched geometry change is ONE solve",
     "geometry_solve_coalescing::one geometry change is one solve::every geometry key still re-solves",
     "geometry_solve_coalescing::one geometry change is one solve::typographyScale is covered — the DERIVED key nothing could drive directly",
     "geometry_solve_coalescing::one geometry change is one solve::a nested geometry fact is REFUSED, not half-compared",
     "geometry_solve_coalescing::one geometry change is one solve::an edgeToEdge surface does not re-solve for a safe-area inset — proved by a differential",
     "geometry_solve_coalescing::one geometry change is one solve::a non-geometry write does not solve",
     "geometry_solve_coalescing::one geometry change is one solve::a set-and-revert inside one batch costs NOTHING",
     "geometry_solve_coalescing::one geometry change is one solve::a write that moves NO geometry value costs nothing",
     "geometry_solve_coalescing::one geometry change is one solve::re-publishing the SAME numbers in a fresh table costs nothing",
     "geometry_solve_coalescing::one geometry change is one solve::consumers on SEPARATE memos are notified in creation order",
     "geometry_solve_coalescing::one geometry change is one solve::env:batch is the general mechanism",
     "geometry_solve_coalescing::a geometry change that also moves a bound layout prop::is ONE solve, and stays one over repeated changes",
     "geometry_solve_coalescing::a geometry change that also moves a bound layout prop::...and the prop is still LIVE: a px that really moves moves the rect",
     "geometry_solve_coalescing::a geometry change that also moves a bound layout prop::a solve a listener really did feed back from still re-solves",
     "measure_publish_settle::a measure→publish cycle settles inside one flush::the round trip costs NO extra reactive flush — the control is in the case",
     "measure_publish_settle::a measure→publish cycle settles inside one flush::the round trip costs ONE extra solve, not one per surface per round trip",
     "measure_publish_settle::a measure→publish cycle settles inside one flush::a surface is FINAL when the geometry change returns — no correction a frame later",
     "measure_publish_settle::a measure→publish cycle settles inside one flush::...and the published reservation is REALLY applied — the fix is not 'never publish'",
     "measure_publish_settle::a measure→publish cycle settles inside one flush::a geometry fact written by a LATE observer costs ONE solve, not two",
     "measure_publish_settle::a measure→publish cycle settles inside one flush::a round trip that will not converge is a LOUD error, not a hang or a silent frame",
     "measure_publish_settle::a measure→publish cycle whose publication is a PROP settles in the same solve::the probe is already at the published height when `present` returns — no refresh",
     "measure_publish_settle::a measure→publish cycle whose publication is a PROP settles in the same solve::...and the control publishes nothing, so the check cannot pass on an empty scene",
     "measure_publish_settle::a measure→publish cycle whose publication is a PROP settles in the same solve::it converges: two more refreshes move nothing and cost no extra solve",
     "measure_publish_settle::a measure→publish cycle whose publication is a PROP settles in the same solve::the feedback solve is COUNTED, and the control counts none",
     "measure_publish_settle::a measure→publish cycle whose publication is a PROP settles in the same solve::an ENV change publishes and lands in the same write — the settle-pass arrival",
     "measure_publish_settle::a measure→publish cycle whose publication is a PROP settles in the same solve::...and so does an ordinary reactive prop change, through refresh's incremental set",
     "measure_publish_settle::a measure→publish cycle whose publication is a PROP settles in the same solve::a prop round trip that will not converge is a LOUD error, not a hang",
     "lifecycle_hooks::lifecycle hooks: the schema declaration::onAppear/onDisappear/hidden are SHARED box props — every rendered class has them",
     "lifecycle_hooks::lifecycle hooks: the schema declaration::the hooks are NOT reactive — a lifetime is not a value that changes",
     "lifecycle_hooks::lifecycle hooks: the schema declaration::a non-function hook and a non-boolean hidden are construction errors",
     "lifecycle_hooks::onAppear::fires once, with the node's path, for a node present at the first render",
     "lifecycle_hooks::onAppear::fires AFTER the first layout solve — the callback can read its own rect",
     "lifecycle_hooks::onAppear::does NOT fire again on a re-solve that changes no structure",
     "lifecycle_hooks::onAppear::fires for a node that arrives later, and again after it leaves and returns",
     "lifecycle_hooks::onDisappear::fires AFTER the handle is released: the path is no longer mounted",
     "lifecycle_hooks::onDisappear::fires exactly once even when the surface is torn down right after",
     "lifecycle_hooks::onDisappear::fires for a node whose Instance was PARKED for recycling, not destroyed",
     "lifecycle_hooks::onDisappear::surface teardown fires every still-mounted hook, so a cleanup is never dropped",
     "lifecycle_hooks::hidden — the space-reserving hide::keeps its layout box: the sibling below does NOT move up",
     "lifecycle_hooks::hidden — the space-reserving hide::...which is the whole difference from `UI.When`, whose sibling DOES move up",
     "lifecycle_hooks::hidden — the space-reserving hide::is not painted, and neither is its subtree",
     "lifecycle_hooks::hidden — the space-reserving hide::flips live off a signal, with no remount",
     "lifecycle_hooks::hidden — the space-reserving hide::does not survive its own node's removal: a remount is born visible",
     "lifecycle_hooks::hidden — the space-reserving hide::leaves focus order — 'can't receive or respond to interactions'",
     "lifecycle_hooks::hidden — the space-reserving hide::takes no tap — the sentence is two halves and this is the second",
     "lifecycle_hooks::hidden — the space-reserving hide::...and so does a losing ViewThatFits candidate, which is the SAME verdict",
     "lifecycle_hooks::hidden — the space-reserving hide::retracts the sub-floor hit expander it would otherwise leave live",
     "placement_audit::§2.1 the inert cell reports, and nothing moved::`anchor` under a ZStack: reported, and the child is not placed by corner",
     "placement_audit::§2.1 the inert cell reports, and nothing moved::`offsetX`/`offsetY` under an HStack: reported as a pair, and the child does not shift",
     "placement_audit::§2.1 the inert cell reports, and nothing moved::`alignH`/`alignV` under a VStack: reported, and `lineAlign` is the spelling that DOES move it",
     "placement_audit::§2.1 the inert cell reports, and nothing moved::`alignH` under a ViewThatFits candidate: reported (the live Rascal Rally shape)",
     "placement_audit::§2.1 the inert cell reports, and nothing moved::`anchor`/`offsetX`/`offsetY` on an Anchor under a Screen: reported (the live row-actions menu shape)",
     "placement_audit::§2.1 the inert cell reports, and nothing moved::`lineAlign` under a ZStack, and `gridSpan` outside a GridRow: both reported",
     "placement_audit::§2.1 the inert cell reports, and nothing moved::a UI.GridRow refuses every placement prop at CONSTRUCTION — the schema closes that cell",
     "placement_audit::§2.1 the honoured cell is SILENT — a false positive here un-ships a screen::a ZStack's own alignH/alignV are the CONTAINER's default, never a placement request",
     "placement_audit::§2.1 the honoured cell is SILENT — a false positive here un-ships a screen::every honoured cell of the §2.1 table is silent",
     "placement_audit::§2.1 the message names the REASON and the way out::`alignH` under an HStack points at lineAlign and says why nothing moved",
     "placement_audit::§2.1 the message names the REASON and the way out::`offsetX` under a ZStack points at UI.Anchor",
     "placement_audit::§2.1 the message names the REASON and the way out::`anchor` under a ZStack points at alignH/alignV, not at UI.Anchor",
     "placement_audit::§2.1 tier 2: the children a construction-time check cannot see::a UI.When child is audited against the parent it SPLICES into, not against the When",
     "placement_audit::§2.1 tier 2: the children a construction-time check cannot see::a UI.ForEach row is audited against the real parent kind",
     "placement_audit::§2.1 the hiddenDepth gate::a losing ViewThatFits candidate does not shout about its own children",
     "placement_audit::§2.1 the blank cells: every prop is authorable everywhere, so every parent is policed::under an Anchor: the two aligns and `lineAlign` and `gridSpan` are inert; the three it reads are not",
     "placement_audit::§2.1 the blank cells: every prop is authorable everywhere, so every parent is policed::under a ScrollView: `anchor`, both aligns, `lineAlign` and `gridSpan` are inert",
     "placement_audit::§2.1 the blank cells: every prop is authorable everywhere, so every parent is policed::under a flow Grid: `anchor`, both offsets, `lineAlign` and `gridSpan` are inert",
     "placement_audit::§2.1 the blank cells: every prop is authorable everywhere, so every parent is policed::under a stack: everything but `lineAlign` and the shrink pair is inert, on BOTH axes",
     "placement_audit::§2.1 the blank cells: every prop is authorable everywhere, so every parent is policed::under a ZStack: everything but the two aligns is inert",
     "placement_audit::§2.1 the blank cells: every prop is authorable everywhere, so every parent is policed::under a ViewThatFits candidate: ALL NINE are inert — a candidate is placed by rank",
     "placement_audit::§2.1 the blank cells: every prop is authorable everywhere, so every parent is policed::a UI.Region refuses every placement prop at CONSTRUCTION — the schema closes that cell too",
     "placement_audit::§2.1 the blank cells: every prop is authorable everywhere, so every parent is policed::under a Region (its chosen form): ALL NINE are inert",
     "placement_audit::§2.1 the FOUR props this same phase shipped are in the watched set::`lineAlign` is reported under every parent that is not a stack",
     "placement_audit::§2.1 the FOUR props this same phase shipped are in the watched set::`gridSpan` is reported under every parent that is not a GridRow",
     "placement_audit::§2.1 the FOUR props this same phase shipped are in the watched set::`shrinkWeight` is reported under every parent that is not a stack, and still shrinks under one",
     "placement_audit::§2.1 the FOUR props this same phase shipped are in the watched set::`layoutPriority` is reported under every parent that is not a stack, and still tiers under one",
     "placement_audit::§2.1 the FOUR props this same phase shipped are in the watched set::the shrink pair's message names the reason AND the way out",
     "container_relative_frame::the container is the nearest viewport, not the parent::a fraction ignores the wrappers between the view and the scroller",
     "container_relative_frame::the container is the nearest viewport, not the parent::the NEAREST scroller wins, not the outermost",
     "container_relative_frame::the container is the nearest viewport, not the parent::with no scroller anywhere, the container is the surface root",
     "container_relative_frame::the container is the nearest viewport, not the parent::...and it follows the scrollbar reservation, because that is the viewport too",
     "container_relative_frame::the paging form is the standard arithmetic::count + spacing carves the viewport into pages",
     "container_relative_frame::the paging form is the standard arithmetic::count with no spacing and no span is a clean division",
     "container_relative_frame::the paging form is the standard arithmetic::the SCROLL AXIS is where this matters, and the offer there is unbounded",
     "container_relative_frame::the paging form is the standard arithmetic::a scroller with no viewport on its own axis says so",
     "container_relative_frame::the paging form is the standard arithmetic::...but a DEFINITE inner extent is exempt — that scroller has a real viewport",
     "container_relative_frame::the paging form is the standard arithmetic::...and a scroller on the CROSS axis of another is exempt — that axis is bounded",
     "container_relative_frame::the paging form is the standard arithmetic::...and an ordinary `fill` child of a scroller says nothing — it is not a scroller",
     "container_relative_frame::the paging form is the standard arithmetic::an unbounded CONTAINER is refused the same way `percent` refuses one",
     "container_relative_frame::the authoring surface: UI.containerRelativeFrame::writes the dimension the solver reads, on the axis the spec named",
     "container_relative_frame::the authoring surface: UI.containerRelativeFrame::the vertical axis writes `height`",
     "container_relative_frame::the authoring surface: UI.containerRelativeFrame::the spec is CLOSED: unknown keys, a bad axis, both forms and a missing form all refuse",
     "container_relative_frame::the measure memo tells two containers apart::the same node at the same offer under two containers gets two answers",
     "container_relative_frame::the measure memo tells two containers apart::...and the fixture really is running through the memo, not around it",
     "container_relative_frame::§2.5 the scope key has exactly two assignment sites, and the header says so::`ctx.scopeKey` is assigned only inside `rebuildScopeKey` and `restoreScope`",
     "container_relative_frame::§2.5 the scope key has exactly two assignment sites, and the header says so::the header counts the writers it actually has",
     "grid_measure_arrange::grid measure/arrange agreement::a grid of content buttons in a hug stack reports no overflow (fixed thumb)",
     "grid_measure_arrange::grid measure/arrange agreement::...and with an aspect cell, whose height follows the column width",
     "grid_measure_arrange::grid measure/arrange agreement::...and under natural item sizing, both cell shapes",
     "grid_measure_arrange::grid measure/arrange agreement::the hug stack is as tall as the grid it holds",
     "grid_measure_arrange::grid measure/arrange agreement::one row of four cells stays one row of four cells",
     "grid_measure_arrange::grid measure/arrange agreement::a grid of plain boxes and texts keeps byte-identical rects",
     "grid_measure_arrange::grid measure/arrange agreement::a uniform minColumnWidth grid stays in the columns it measured",
     "grid_measure_arrange::grid measure/arrange agreement — column flow::a column-flow grid reproduces its own reported box (explicit lanes)",
     "grid_measure_arrange::grid measure/arrange agreement — column flow::...with uniform cells, where the report is the CELL and not the lane",
     "grid_measure_arrange::grid measure/arrange agreement — column flow::...and with the lane count derived from minColumnWidth against the HEIGHT",
     "grid_measure_arrange::grid measure/arrange agreement — column flow::...and the row-flow control does the same, on the same corpus",
     "grid_measure_arrange::grid measure/arrange agreement — column flow::neither flow direction files an overflow on a hug parent",
     "grid_measure_arrange::a grid line takes the grid's own flow extent when a cell fills it::three fill cells in a 60px-tall grid paint 60px tall, not 0",
     "grid_measure_arrange::a grid line takes the grid's own flow extent when a cell fills it::...which is exactly what the same declaration does in an HStack",
     "grid_measure_arrange::a grid line takes the grid's own flow extent when a cell fills it::a line with no fill cell keeps its content height, and content cells keep theirs",
     "grid_measure_arrange::a grid line takes the grid's own flow extent when a cell fills it::a fill cell beside a content one takes the whole line, and the content one does not grow",
     "grid_measure_arrange::a grid line takes the grid's own flow extent when a cell fills it::the REPORT is unchanged: a content-sized parent still hugs the cells",
     "grid_measure_arrange::the grid's plan has ONE owner, and both passes read it::each plan is defined exactly once, and in `src/layout/grid.luau`",
     "grid_measure_arrange::the grid's plan has ONE owner, and both passes read it::the solver keeps no grid arithmetic of its own — it only holds the cache's home",
     "grid_measure_arrange::the grid's plan has ONE owner, and both passes read it::`grid.measure` and `grid.arrange` both call the SAME flow plan",
     "grid_measure_arrange::the grid's plan has ONE owner, and both passes read it::the grid module holds no state: no mutable module-level local at all",
     "scroll_bar_measure::ScrollView measure includes the bar its arrange reserves::an overflowing hug x-shelf reports zero diagnostics with a live bar",
     "scroll_bar_measure::ScrollView measure includes the bar its arrange reserves::the shelf's box is tall enough for a full control PLUS the bar",
     "scroll_bar_measure::ScrollView measure includes the bar its arrange reserves::without a bar (fallback adapters) nothing changes",
     "scroll_bar_measure::ScrollView measure includes the bar its arrange reserves::a shelf whose content FITS reserves nothing",
     "scroll_bar_measure::ScrollView measure includes the bar its arrange reserves::the vertical mirror: an overflowing hug-width y-scroller widens by the bar",
     "scroll_bar_measure::the bar gutter a host spent is published for its SIBLINGS to pay::an X shelf reports it on `bottom` — the side an X scroller reserves from",
     "scroll_bar_measure::the bar gutter a host spent is published for its SIBLINGS to pay::a Y list reports it on `right`",
     "scroll_bar_measure::the bar gutter a host spent is published for its SIBLINGS to pay::a host whose content FITS reserves nothing, and publishes nothing",
     "scroll_bar_measure::the bar gutter a host spent is published for its SIBLINGS to pay::...and a fallback adapter with no bar publishes nothing either",
     "scroll_bar_measure::the bar gutter a host spent is published for its SIBLINGS to pay::an unmounted host stops answering",
     "scroll_bar_measure::item 3: a persistent scrollbar never overlaps a full-width card list::desktop/pointer (the default profile — a persistent bar): no card overlaps it, overflowing",
     "scroll_bar_measure::item 3: a persistent scrollbar never overlaps a full-width card list::desktop/pointer at rest (content fits — nothing to reserve, nothing to overlap)",
     "scroll_bar_measure::item 3: a persistent scrollbar never overlaps a full-width card list::touch/auto profile: ZERO reserve (director ruling 2026-08-28 — persistent holds space, auto overlaps)",
     "scroll_bar_measure::director ruling 2026-08-28: persistent holds space, auto lets it overlap — every construction::Table (headerless, RascalRally's racer-list shape): always holds space, auto does not",
     "scroll_bar_measure::director ruling 2026-08-28: persistent holds space, auto lets it overlap — every construction::the All-controls scenario, windowed desktop: persistent (pointer default) holds space",
     "scroll_bar_measure::director ruling 2026-08-28: persistent holds space, auto lets it overlap — every construction::the All-controls scenario, windowed desktop: auto lets it overlap (zero reserve)",
     "scroll_bar_measure::fix round 1 (RED-TEAM finding 1): the widen lands on the SAME paint that computes it::an already-overflowing 'auto' host gets the widen on its FIRST setRect call, not a later one",
     "scroll_bar_measure::fix round 1 (RED-TEAM finding 1): the widen lands on the SAME paint that computes it::...and under 'always' policy the same first solve applies no widen at all (sanity control)",
     "scroll_bar_measure::fix round 2 (RED-TEAM re-review): setScrollRegion receives the REAL solved-rect host box, never nil::hostW/hostH reaching setScrollRegion are non-nil and equal the solved rect",
     "scroll_bar_measure::fix round 2 (RED-TEAM re-review): setScrollRegion receives the REAL solved-rect host box, never nil::...and a hug-width X shelf gets the same fact on its own cross axis (hostH)",
     "scroll_indicators::scroll indicator policy: derivation is input facts, never a device::pointer sessions derive 'always'; touch and gamepad derive 'auto'",
     "scroll_indicators::scroll indicator policy: the presenter drives the adapter seam::boot applies the session's policy before anything mounts",
     "scroll_indicators::scroll indicator policy: the presenter drives the adapter seam::a pointer session keeps the configured thickness (the reserve survives)",
     "scroll_indicators::scroll indicator policy: the presenter drives the adapter seam::an input-class flip mid-session re-applies the policy and MOVES the reserve",
     "scroll_indicators::scroll indicator policy: the presenter drives the adapter seam::reduced motion rides the same seam",
     "scroll_indicators::scroll indicator policy: the presenter drives the adapter seam::an adapter WITHOUT the seam degrades by name: nothing is called, nothing breaks",
     "scroll_indicators::scroll indicator policy: a Y-axis hug height is stable across a session flip::a hug shelf does NOT change HEIGHT when the session flips to touch",
     "scroll_indicators::a single-axis scroller's canvas never exceeds its window on the other axis::a wide x-carousel inside a y-page does not inflate the page's canvas width",
     "scroll_indicators::indicators = \"none\": the carousel opt-out::a peeking carousel declares its indicator off; its parent page keeps one",
     "scroll_indicators::the LIVE ScreenTarget publishes the reserve through TWO sanctioned writes::its one constant is the same 8px the fake mirrors",
     "scroll_indicators::the LIVE ScreenTarget publishes the reserve through TWO sanctioned writes::the reserve is the bar PLUS a one-pixel gutter, and the PAINT never grew",
     "scroll_indicators::the LIVE ScreenTarget publishes the reserve through TWO sanctioned writes::TWO writes, not one: the construction default and the policy-aware seam",
     "scroll_indicators::the LIVE ScreenTarget publishes the reserve through TWO sanctioned writes::NOTHING BUT those two recognised lines assigns the reserve",
     "zstack_fill_diagnostic::zstack overflow diagnostic: fill axes are granted, not measured::a fill-size page scroller taller than the stack is NOT an overflow",
     "zstack_fill_diagnostic::zstack overflow diagnostic: fill axes are granted, not measured::NEGATIVE CONTROL: a hug child genuinely bigger than the stack still reports",
     "zstack_fill_diagnostic::zstack overflow diagnostic: fill axes are granted, not measured::NEGATIVE CONTROL: a fill-WIDTH child still reports its oversize HEIGHT",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'probe': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'safe_area': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'scroll_host': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'path_ring': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'async_images': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'selection_bridge': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'preferred_text': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'drag_session': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'virtual_list_native': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'native_style': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'authoring': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'adaptive_controls': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'theme_authoring': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'perf_capture': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'sponsor_motion': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'sponsor_celebration': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'sponsor_list': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'sponsor_drop': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'sponsor_toast': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'sponsor_avatars': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'sponsor_markers': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'sponsor_billboard': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'composition': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'keyboard_navigation': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'row_actions': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'card_rail': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'with_animation': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'level_picker': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'progress_ring': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'flow_wrap': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'lifecycle_hidden': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'row_capabilities': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'sensory_feedback': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'text_degrade': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'variable_extents': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'measured_extents': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'table_virtualized': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'virtual_grid': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'virtual_hgrid': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'hud': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'foreign_content': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'surface_overlap': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'branch_scope': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'sorted_entries': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'time_curves': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'canvas_group': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'preferred_transparency': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'callout': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'menu': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'tab_view': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'nested_compositing': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'outpost_terminal': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: tutorial examples::example '01_temperature_converter': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: tutorial examples::example '02_playlist_table': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: tutorial examples::example '03_settings_sync': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: tutorial examples::example '04_confirm_dialog': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: tutorial examples::example '05_word_game': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: tutorial examples::example '06_tile_game': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: tutorial examples::example '07_match3': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: reference proofs::proof 'p1_glade': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: reference proofs::proof 'p2_cartwheel': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: reference proofs::proof 'p3_sipworks': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: reference proofs::proof 'p4_foyer': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: the text-size axis bites::AXIS CONTROL: a slot sized for Larger is SILENT at +0/+4/+10 and REPORTED at +14",
     "overflow_sweep::overflow sweep: the widened breadth bites::BREADTH CONTROL: a layered overlap and a lying itemExtent both reach the collector",
     "overflow_sweep::overflow sweep: the accessibility waiver list::every waived finding still fires — a stale waiver is a fixed defect, delete the line",
     "overflow_sweep::overflow sweep: the locale ledger::every locale row still fires — a stale row is a fixed defect, delete the line",
     "overflow_sweep::overflow sweep: the locale ledger::REACH: the pass moved the copy it claims to, and names every surface it did not",
     "overflow_sweep::overflow sweep: every registered showcase surface is swept::RULE 4: the sweep list and the scenario registry are the same set",
     "overflow_sweep::overflow sweep: the theme axis bites::THEME CONTROL: a row that fits under Studio Neutral is REPORTED under a package",
     "overflow_sweep::overflow sweep: the theme ledger::every ledger row still fires — a stale row is a fixed defect, delete the line",
     "overflow_sweep::overflow sweep: the theme ledger::COVERAGE: the themed tier is what the header says it is, and says what it skips",
     "overflow_sweep::overflow sweep: the HUD under the showcase's OWN chrome::compact-phone-portrait (359x718): the HUD clears the showcase's chip row and every box holds its own text",
     "overflow_sweep::overflow sweep: the HUD under the showcase's OWN chrome::compact-phone-landscape (705x338): the HUD clears the showcase's chip row and every box holds its own text",
     "overflow_sweep::overflow sweep: the HUD under the showcase's OWN chrome::tablet-landscape (1079x809): the HUD clears the showcase's chip row and every box holds its own text",
     "overflow_sweep::overflow sweep: the HUD under the showcase's OWN chrome::desktop-standard (1232x1067): the HUD clears the showcase's chip row and every box holds its own text",
     "overflow_sweep::overflow sweep: the HUD under the showcase's OWN chrome::console-ten-foot (1920x1078): the HUD clears the showcase's chip row and every box holds its own text",
     "overflow_sweep::overflow sweep: the HUD under the showcase's OWN chrome::narrow-portrait (320x640): the HUD clears the showcase's chip row and every box holds its own text",
     "overflow_sweep::overflow sweep: the HUD under the showcase's OWN chrome::narrow-landscape (640x320): the HUD clears the showcase's chip row and every box holds its own text",
     "overflow_sweep::overflow sweep: the HUD under the showcase's OWN chrome::desktop-studio-1320 (1320x742): the HUD clears the showcase's chip row and every box holds its own text",
     "overflow_sweep::overflow sweep: the HUD under the showcase's OWN chrome::phone-390x844 (390x844): the HUD clears the showcase's chip row and every box holds its own text",
     "overflow_sweep::overflow sweep: the HUD under the showcase's OWN chrome::R23: no route falls below the touch floor except where an AUTHOR node is in the way",
     "overflow_sweep::overflow sweep: the HUD under the showcase's OWN chrome::compact-phone-portrait (359x718): every state the HUD opens into holds its own boxes",
     "overflow_sweep::overflow sweep: the HUD under the showcase's OWN chrome::compact-phone-landscape (705x338): every state the HUD opens into holds its own boxes",
     "overflow_sweep::overflow sweep: the HUD under the showcase's OWN chrome::tablet-landscape (1079x809): every state the HUD opens into holds its own boxes",
     "overflow_sweep::overflow sweep: the HUD under the showcase's OWN chrome::desktop-standard (1232x1067): every state the HUD opens into holds its own boxes",
     "overflow_sweep::overflow sweep: the HUD under the showcase's OWN chrome::console-ten-foot (1920x1078): every state the HUD opens into holds its own boxes",
     "overflow_sweep::overflow sweep: the HUD under the showcase's OWN chrome::narrow-portrait (320x640): every state the HUD opens into holds its own boxes",
     "overflow_sweep::overflow sweep: the HUD under the showcase's OWN chrome::narrow-landscape (640x320): every state the HUD opens into holds its own boxes",
     "overflow_sweep::overflow sweep: the HUD under the showcase's OWN chrome::desktop-studio-1320 (1320x742): every state the HUD opens into holds its own boxes",
     "overflow_sweep::overflow sweep: the HUD under the showcase's OWN chrome::phone-390x844 (390x844): every state the HUD opens into holds its own boxes",
     "overflow_sweep::overflow sweep: the HUD under the showcase's OWN chrome::DEAD-END CONTROL: a suppressed route is reported, and the AUTHORED escape is not",
     "overflow_sweep::overflow sweep: the HUD under the showcase's OWN chrome::VALUE CONTROL: the R9 classification reaches real nodes, and a cut value is reported",
     "overflow_sweep::overflow sweep: the HUD under the showcase's OWN chrome::GIVE-WAY CONTROL: a DROPPED region that paints a character is reported, and the settled tree is not",
     "overflow_sweep::overflow sweep: the HUD under the showcase's OWN chrome::LANE-CONTAINMENT CONTROL: a zone below its own lane is reported, and the settled tree is not",
     "overflow_sweep::overflow sweep: the HUD under the showcase's OWN chrome::ORACLE CONTROL: a plate squeezed under its own styled line is reported, and a plate that holds it is not",
     "example_readouts::readout sweep: every tutorial example renders the content it declares::example '01_temperature_converter': every declared readout has a real rect at every swept viewport, at every text size",
     "example_readouts::readout sweep: every tutorial example renders the content it declares::example '02_playlist_table': every declared readout has a real rect at every swept viewport, at every text size",
     "example_readouts::readout sweep: every tutorial example renders the content it declares::example '03_settings_sync': every declared readout has a real rect at every swept viewport, at every text size",
     "example_readouts::readout sweep: every tutorial example renders the content it declares::example '04_confirm_dialog': every declared readout has a real rect at every swept viewport, at every text size",
     "example_readouts::readout sweep: every tutorial example renders the content it declares::example '05_word_game': every declared readout has a real rect at every swept viewport, at every text size",
     "example_readouts::readout sweep: every tutorial example renders the content it declares::example '06_tile_game': every declared readout has a real rect at every swept viewport, at every text size",
     "example_readouts::readout sweep: every tutorial example renders the content it declares::example '07_match3': every declared readout has a real rect at every swept viewport, at every text size",
     "text_padding_paint::padded Text: the inset the solver measured reaches paint::✓ a padded Text raises the padding write and materializes a four-sided inset",
     "text_padding_paint::padded Text: the inset the solver measured reaches paint::✓ the glyph box is the box the solver measured, minus the padding it measured with",
     "text_padding_paint::padded Text: the inset the solver measured reaches paint::✓ an unpadded Text raises no padding write and materializes no modifier",
     "text_padding_paint::padded Text: the inset the solver measured reaches paint::✓ a per-side metric NAME resolves through the snapshot, like every other side",
     "text_padding_paint::padded Text: the inset the solver measured reaches paint::✓ minimal-write: a steady padded label costs one padding write, not one per refresh",
     "text_padding_paint::padded Text: the inset the solver measured reaches paint::✓ the inset FOLLOWS its value: a padding that goes to zero un-insets the label",
     "text_padding_paint::padded Text: the inset the solver measured reaches paint::✓ the fixture the device found it on: every adaptive_controls stat tile is inset",
     "scroll_window_clip::scroll host: the fake target models the engine's window::✓ a host whose content fits keeps its whole frame as its window",
     "scroll_window_clip::scroll host: the fake target models the engine's window::✓ under 'always', a Y host that overflows loses the bar's thickness off its WIDTH, never its height",
     "scroll_window_clip::scroll host: the fake target models the engine's window::✓ under 'auto', the SAME overflowing Y host loses NOTHING off its window (director ruling 2026-08-28)",
     "scroll_window_clip::scroll host: the fake target models the engine's window::✓ a host that declared `indicators = 'none'` has no bar and loses nothing",
     "scroll_window_clip::scroll host: the fake target models the engine's window::✓ the window FOLLOWS a host that moved, rather than describing where it used to be",
     "scroll_window_clip::scroll host: the fake target models the engine's window::✓ under 'auto' the RESERVE drops to zero while the BAR INSTANCE stays 8 (director ruling 2026-08-28)",
     "scroll_window_clip::scroll host: every policy lays content inside the window it can see::✓ a touch scroller's fill-width row lands INSIDE its window (director ruling 2026-08-28)",
     "scroll_window_clip::scroll host: every policy lays content inside the window it can see::✓ a POINTER session reserves the bar, so its rows land inside the window",
     "scroll_window_clip::scroll host: every policy lays content inside the window it can see::✓ ...one pixel clear of it, on the RIGHT of a Y host",
     "scroll_window_clip::scroll host: every policy lays content inside the window it can see::✓ ...and on the BOTTOM of an X host — the director's 'horizontal too'",
     "icon_ns_glyph::namespaced icon ASCII floor::derives the glyph from the name after the namespace",
     "icon_ns_glyph::namespaced icon ASCII floor::the derived glyph passes the safe-glyph rule",
     "icon_ns_glyph::namespaced icon ASCII floor::table names keep their curated glyphs (the derivation never shadows them)",
     "icon_ns_glyph::namespaced icon ASCII floor::a plain unknown name still resolves to nil (schema keeps it a construction error)",
     "icon_ns_glyph::namespaced icon ASCII floor::a namespace with no letter after the colon resolves to nil, never a broken char",
     "adaptive::A-AL3: pure adaptive decisions (no device names)::classifies width into compact / regular / wide at the documented breakpoints",
     "adaptive::A-AL3: pure adaptive decisions (no device names)::a ten-foot display never resolves the densest class, however wide it is",
     "adaptive::A-AL3: pure adaptive decisions (no device names)::chooses a stack axis from available width, not a platform name",
     "adaptive::A-AL3: pure adaptive decisions (no device names)::derives a column count from a minimum cell width and gap",
     "adaptive::A-AL3: pure adaptive decisions (no device names)::ADAPT-23: the ten-foot cap reaches the column count too",
     "adaptive::A-AL3: pure adaptive decisions (no device names)::tolerates a nonsense width instead of producing a nonsense class",
     "adaptive::A-AL3: pure adaptive decisions (no device names)::GAP7b: sizeClassAtLeast ranks compact < regular < wide",
     "adaptive::A-AL3: reactive conditions over the environment::exposes the class and its boolean tests as Readables",
     "adaptive::A-AL3: reactive conditions over the environment::re-resolves live when the viewport changes, with no remount",
     "adaptive::A-AL3: reactive conditions over the environment::isTenFoot follows the distance profile, not the input class",
     "adaptive::A-AL3: reactive conditions over the environment::axis is a Readable an AdaptiveStack can bind directly",
     "adaptive::A-AL3: reactive conditions over the environment::REFUSES the engine's placeholder viewport: a degenerate write keeps the last real fact",
     "adaptive::A-AL3: reactive conditions over the environment::GAP7a: navSidebar/navTopBar/navBottomBar/navBottomBarCompact are the four homes, promoted",
     "adaptive::A-AL3: reactive conditions over the environment::GAP7a: navSidebar re-resolves live, exactly as navPlacement does",
     "adaptive::A-AL3: reactive conditions over the environment::GAP7b: isRegularOrWider/isCompactOnly name the two-way split the trap invites",
     "adaptive::A-AL3: reactive conditions over the environment::GAP7b: isCompactOnly is isCompact under a name that pairs with isRegularOrWider, not a second fact",
     "adaptive::A-AL3: reactive conditions over the environment::GAP7b: conditions.atLeast is the general form isRegularOrWider names",
     "adaptive::A-AL2: AdaptiveStack changes axis without rebuilding anything::lays out vertically for axis y and horizontally for axis x",
     "adaptive::A-AL2: AdaptiveStack changes axis without rebuilding anything::flipping the bound axis re-solves WITHOUT a factory rerun or a remount",
     "adaptive::A-AL2: AdaptiveStack changes axis without rebuilding anything::an axis flip driven by a viewport change preserves node identity",
     "adaptive::A-AL2: AdaptiveStack changes axis without rebuilding anything::gap is reactive too, so spacing adapts without a rebuild",
     "adaptive::A-AL2: AdaptiveStack changes axis without rebuilding anything::rejects an axis outside the closed set at construction",
     "adaptive::A-LV3: Divider is axis-aware::draws a horizontal hairline in a vertical stack",
     "adaptive::A-LV3: Divider is axis-aware::draws a vertical hairline in a horizontal stack",
     "adaptive::A-LV3: Divider is axis-aware::an explicit orientation overrides the inferred one",
     "adaptive::A-LV3: Divider is axis-aware::follows an AdaptiveStack's axis flip with no rebuild",
     "adaptive::A-LV3: Divider is axis-aware::is non-interactive and takes no children",
     "adaptive::A-AL1: ViewThatFits::picks the first candidate that fits the available space",
     "adaptive::A-AL1: ViewThatFits::falls through to a later candidate when the first does not fit",
     "adaptive::A-AL1: ViewThatFits::uses the LAST candidate as the fallback when none fits",
     "adaptive::A-AL1: ViewThatFits::re-chooses on a live resize WITHOUT remounting any candidate",
     "adaptive::A-AL1: ViewThatFits::measures against the CONTAINER's space, not the viewport's",
     "adaptive::A-AL1: ViewThatFits::an unchosen candidate's focusables are removed from focus order",
     "adaptive::A-AL1: ViewThatFits::focus order follows the choice when the space changes",
     "adaptive::A-AL1: ViewThatFits::the exclusion holds when the candidates are STACKS, so nav is grouped",
     "adaptive::A-AL1: ViewThatFits::a losing candidate is HIDDEN, not merely zero-sized",
     "adaptive::A-AL1: ViewThatFits::visibility follows the choice on a resize",
     "adaptive::A-AL1: ViewThatFits::a hidden candidate's DESCENDANTS are hidden too",
     "adaptive::A-AL1: ViewThatFits::a single candidate is always chosen",
     "adaptive::A-AL1: ViewThatFits::requires at least one candidate",
     "adaptive::A-AL1: ViewThatFits::DIR-7: a candidate whose single-line label would be CUT does not count as fitting",
     "adaptive::A-AL1: ViewThatFits::M1a: a nested ladder that resolves internally costs the outer ladder nothing",
     "adaptive::A-AL1: ViewThatFits::M1b: an author-pinned label's own width does not lose its rung",
     "adaptive::A-AL1: ViewThatFits::DIR-7 control: a candidate that really does fit is still the one chosen",
     "adaptive::RS-A16-D1: a hidden ViewThatFits candidate requests no hit geometry::a hidden candidate's sub-floor control asks for NO hit rect",
     "adaptive::RS-A16-D1: a hidden ViewThatFits candidate requests no hit geometry::the VISIBLE winner's hit geometry is unchanged by the fix",
     "adaptive::RS-A16-D1: a hidden ViewThatFits candidate requests no hit geometry::a node hidden AFTER expansion has its expander RETRACTED",
     "adaptive::RS-A16-D1: a hidden ViewThatFits candidate requests no hit geometry::re-choosing BACK re-expands the control that became visible again",
     "adaptive::RS-A16-D1: a hidden ViewThatFits candidate requests no hit geometry::hiddenness reaches a DEEP descendant, not just the candidate root",
     "adaptive::RS-A16-D1: a hidden ViewThatFits candidate requests no hit geometry::the adapter mirrors its host's visibility onto the expander (structural half)",
     "adaptive::RS-A16-D1: a hidden ViewThatFits candidate requests no hit geometry::both activate producers build their meta through the one shared builder",
     "adaptive::RS-A16-D1: a hidden ViewThatFits candidate requests no hit geometry::the minimum-target expander begins the host's pointer capture, through the one shared body",
     "adaptive::RS-A16-D1: a hidden ViewThatFits candidate requests no hit geometry::RS-A16-D1b: the expander is placed by the SAME path that places its control",
     "adaptive::D-6: the matrix assertions reject a broken fixture::the hit-floor assertion fails when a control is pinned below the floor",
     "adaptive::D-6: the matrix assertions reject a broken fixture::the hidden-candidate assertion fails when a losing candidate still paints",
     "adaptive::D-6: the matrix assertions reject a broken fixture::the adaptation assertion fails when a screen hard-codes one axis",
     "adaptive::interactionClasses: the primary class before any input has happened::a touch-only device is TOUCH-primary even while PreferredInput still says KeyboardAndMouse",
     "adaptive::interactionClasses: the primary class before any input has happened::a gamepad-only device is GAMEPAD-primary on the same argument",
     "adaptive::interactionClasses: the primary class before any input has happened::a real mouse still wins: a desktop is pointer-primary",
     "adaptive::interactionClasses: the primary class before any input has happened::a HYBRID (mouse AND touch) stays pointer until the player says otherwise",
     "adaptive::interactionClasses: the primary class before any input has happened::effectiveInput speaks the platform fact's vocabulary, resolved",
     "adaptive::interactionClasses: the primary class before any input has happened::an EXPRESSED preference still wins over capability",
     "adaptive::DV3-1: the topbar band is addressable from facts alone::defaults to the desktop/no-notch answer rather than to a guess",
     "adaptive::DV3-1: the topbar band is addressable from facts alone::a NOTCHED phone puts the band right of the cluster, not at the window origin",
     "adaptive::DV3-1: the topbar band is addressable from facts alone::the band is reachable from an edgeToEdge surface and NOT from a safe one",
     "adaptive::platformChrome states the L, not its bounding box::with NO platform facts there is no band and no chrome — zeroes read as absence",
     "adaptive::platformChrome states the L, not its bounding box::the LIVE DESKTOP measurement: chrome is x 0..164 of a 58px band, and the rest is free",
     "adaptive::platformChrome states the L, not its bounding box::a platform that reports NO strip keeps the blunt full-width band",
     "adaptive::platformChrome states the L, not its bounding box::a strip reported with NO topbar-safe area still rides: the rect fact is enough",
     "adaptive::platformChrome states the L, not its bounding box::a NOTCHED landscape phone: chrome on BOTH sides of the band, and the notch is still spent",
     "adaptive::platformChrome states the L, not its bounding box::garbage platform facts clamp to 'no band' rather than to a band at a wrong origin",
     "adaptive::platformChrome states the L, not its bounding box::`insets` IS what deviceSafeContent applies — one authority, pinned to the live policy",
     "adaptive::regression pin: the width facts are byte-unchanged::sizeClass answers exactly what it answered before the height half landed",
     "adaptive::regression pin: the width facts are byte-unchanged::axisFor answers exactly what it answered before",
     "adaptive::regression pin: the width facts are byte-unchanged::BREAKPOINTS and DEFAULT_STACK_ABOVE are the same numbers",
     "adaptive::regression pin: the width facts are byte-unchanged::every conditions key that existed before still resolves the same, height facts present",
     "adaptive::height and orientation are facts, not device names::classifies height at the same breakpoints as width",
     "adaptive::height and orientation are facts, not device names::a ten-foot display never resolves the densest VERTICAL arrangement either",
     "adaptive::height and orientation are facts, not device names::degrades a nonsense height rather than producing a nonsense class",
     "adaptive::height and orientation are facts, not device names::orientation is a SHAPE fact: a windowed pane on a desktop is portrait",
     "adaptive::height and orientation are facts, not device names::a rotation maps a class pair onto its mirror",
     "adaptive::height and orientation are facts, not device names::exposes the height half as Readables that re-resolve in place",
     "adaptive::height and orientation are facts, not device names::opts.scope owns ALL of them, so the height half cannot leak",
     "adaptive::navPlacement: the tab/sidebar policy is shape and input facts, never a device::pure: the five canonical shapes land where the ruling puts them",
     "adaptive::navPlacement: the tab/sidebar policy is shape and input facts, never a device::precedence: compact beats short beats ten-foot beats input class",
     "adaptive::navPlacement: the tab/sidebar policy is shape and input facts, never a device::ADAPT-2: a tablet takes the tablet paradigm in BOTH orientations, a phone in neither",
     "adaptive::navPlacement: the tab/sidebar policy is shape and input facts, never a device::reactive: the conditions memo re-resolves in place when facts move",
     "adaptive::NM-H4a: a topbar-safe inset that cannot describe this viewport is discarded::the two encodings agreeing is an identity — the healthy platform",
     "adaptive::NM-H4a: a topbar-safe inset that cannot describe this viewport is discarded::an inset from the OTHER orientation no longer erases the band",
     "adaptive::NM-H4a: a topbar-safe inset that cannot describe this viewport is discarded::...and a HORIZONTALLY impossible one is discarded on the same rule",
     "adaptive::NM-H4a: a topbar-safe inset that cannot describe this viewport is discarded::a TRUTHFUL inset still narrows the band, because the belt is not switched off",
     "adaptive::NM-H4a: a topbar-safe inset that cannot describe this viewport is discarded::no topbar rect at all is still NO BAND — the discard is not a manufacture",
     "adaptive::director item 5: adaptive.effectiveDisplaySize::downgrades Large to Medium only when touch is ALSO live",
     "adaptive::director item 5: adaptive.effectiveDisplaySize::never touches Small or Medium, touch or not — only Large is ever in question",
     "adaptive::director item 5: adaptive.effectiveDisplaySize::passes nil/garbage through untouched — only a genuine Large+touch pair corrects",
     "fits_ideal_size::ViewThatFits: a candidate is judged at its IDEAL size, `hug` included::a `content` rung reports its true width and the ladder steps down",
     "fits_ideal_size::ViewThatFits: a candidate is judged at its IDEAL size, `hug` included::a `hug` rung ALSO reports its true width, so the same ladder steps",
     "fits_ideal_size::ViewThatFits: a candidate is judged at its IDEAL size, `hug` included::`hug` and `content` choose the same rung across a width sweep",
     "fits_ideal_size::ViewThatFits: a candidate is judged at its IDEAL size, `hug` included::the sweep really does contain both verdicts",
     "fits_ideal_size::ViewThatFits: a candidate is judged at its IDEAL size, `hug` included::a `hug` FALLBACK rung — the one never probed — is capped by the offer it is shown in",
     "fits_ideal_size::ViewThatFits: a candidate is judged at its IDEAL size, `hug` included::an author's own `max` on a `hug` rung is honoured by the probe",
     "fits_ideal_size::ViewThatFits: a candidate is judged at its IDEAL size, `hug` included::a `hug` node outside a ladder still caps at the offer",
     "fits_ideal_size::adaptive.fits — the container-relative decision as a value::agrees with the ViewThatFits ladder at every width in a sweep",
     "fits_ideal_size::adaptive.fits — the container-relative decision as a value::re-resolves when a part moves, which a pre-summed constant cannot",
     "fits_ideal_size::adaptive.fits — the container-relative decision as a value::clearance is subtracted from the container, not from the content",
     "fits_ideal_size::adaptive.fits — the container-relative decision as a value::an unbounded container fits everything, exactly as the ladder's own test does",
     "fits_ideal_size::adaptive.fits — the container-relative decision as a value::a live part that resolves to nil reaches the core's error channel",
     "fits_ideal_size::adaptive.fits — the container-relative decision as a value::refuses a static part that is not a number, at construction",
     "fits_ideal_size::adaptive.fits — the container-relative decision as a value::refuses a list with a hole in it, at construction",
     "fits_ideal_size::adaptive.fits — the container-relative decision as a value::refuses an unknown key, so a misspelled `clearence` cannot be silently ignored",
     "button_complete::B-BTN1: Button supports custom content behind one activation surface::renders its children inside the button",
     "button_complete::B-BTN1: Button supports custom content behind one activation surface::keeps the label as SEMANTICS only when content is present (the button draws no text of its own)",
     "button_complete::B-BTN1: Button supports custom content behind one activation surface::a plain Button still paints its label",
     "button_complete::B-BTN1: Button supports custom content behind one activation surface::the label stays REQUIRED even for an icon-only button",
     "button_complete::B-BTN1: Button supports custom content behind one activation surface::a focusable inside the content is rejected: one activation surface, one focus site",
     "button_complete::B-BTN1: Button supports custom content behind one activation surface::a focusable nested DEEPER in the content is also rejected",
     "button_complete::B-BTN1: Button supports custom content behind one activation surface::a content button exposes exactly ONE focusable and activates once",
     "button_complete::B-BTN2: semantic roles ride tags and style rules::accepts the three documented roles and rejects anything else",
     "button_complete::B-BTN2: semantic roles ride tags and style rules::the role reaches the adapter as a style write, not a paint write",
     "button_complete::B-BTN2: semantic roles ride tags and style rules::a reactive role re-applies (it is a style prop, not a create-time write)",
     "button_complete::B-BTN2: semantic roles ride tags and style rules::REGRESSION: a role rule is ordered AFTER the base fills so it owns the resting fill",
     "button_complete::B-BTN2: semantic roles ride tags and style rules::verifier V14: a role carries its OWN hover and press rules",
     "button_complete::B-BTN2: semantic roles ride tags and style rules::verifier V14: a role + selected combination is a KNOWN limitation, recorded not hidden",
     "button_complete::B-BTN2: semantic roles ride tags and style rules::the sheet carries a rule per role, so paint stays sheet-owned",
     "button_complete::B-BTN2: semantic roles ride tags and style rules::the classifier maps roles to tags purely",
     "button_complete::B-BTN2: semantic roles ride tags and style rules::every theme's danger pair passes the contrast gate",
     "button_complete::B-BTN3: the hit-target floor is enforced in solved/hit geometry::a sub-floor button reports an expanded hit rect",
     "button_complete::B-BTN3: the hit-target floor is enforced in solved/hit geometry::a button already at or above the floor gets NO expander",
     "button_complete::B-BTN3: the hit-target floor is enforced in solved/hit geometry::only ONE axis is expanded when only one is short",
     "button_complete::B-BTN3: the hit-target floor is enforced in solved/hit geometry::the floor applies to Toggle and TextField too (every focusable control)",
     "button_complete::B-BTN3: the hit-target floor is enforced in solved/hit geometry::a NON-interactive node never gets a hit rect",
     "button_complete::B-BTN3: the hit-target floor is enforced in solved/hit geometry::the expansion follows a re-solve",
     "button_complete::B-BTN3: the hit-target floor is enforced in solved/hit geometry::a disabled control still gets no smaller than the floor (the target does not move when it re-enables)",
     "button_complete::B-BTN4: disabled is consistent across focus, activation, appearance and semantics::a disabled button is skipped by focus navigation",
     "button_complete::B-BTN4: disabled is consistent across focus, activation, appearance and semantics::a disabled button does not activate on a pointer tap",
     "button_complete::B-BTN4: disabled is consistent across focus, activation, appearance and semantics::a disabled button does not activate on the keyboard or gamepad path either",
     "button_complete::B-BTN4: disabled is consistent across focus, activation, appearance and semantics::re-enabling restores focus reachability with no remount",
     "button_complete::B-BTN4: disabled is consistent across focus, activation, appearance and semantics::a disabled destructive button is still disabled (role does not re-enable it)",
     "button_complete::B-BTN4: disabled is consistent across focus, activation, appearance and semantics::teardown returns the registry to its baseline",
     "button_complete::verifier V12: a disabled control refuses the CONSUMER OVERRIDE too::opts.onActivate does not fire for a disabled node",
     "button_complete::verifier V10: structural remount forgets the per-path caches::a path re-created while still hidden is written hidden again",
     "button_complete::verifier V10: structural remount forgets the per-path caches::a sub-floor control re-created at the same path keeps its hit expander",
     "button_shape::circle Button: the accept classes::takes shape = circle with a short label and constructs",
     "button_shape::circle Button: the accept classes::takes shape = rect explicitly, which is what every Button already was",
     "button_shape::circle Button: the accept classes::leaves an ordinary Button untouched: no shape prop at all",
     "button_shape::circle Button: the accept classes::takes a semantic icon and keeps a LONG label, because the label is then only the name",
     "button_shape::circle Button: the accept classes::takes a long label on a CONTENT circle for the same reason",
     "button_shape::circle Button: the accept classes::takes a namespaced icon name a package contributes",
     "button_shape::circle Button: the accept classes::takes ONE authored axis on either side",
     "button_shape::circle Button: the accept classes::takes a BOUND label: a reactive value is checked when it is read, like every reactive prop",
     "button_shape::circle Button: the reject classes name the field, the rule and the fix::refuses a multi-word drawn label",
     "button_shape::circle Button: the reject classes name the field, the rule and the fix::refuses a drawn label longer than three characters even without a space",
     "button_shape::circle Button: the reject classes name the field, the rule and the fix::accepts exactly three characters and refuses four",
     "button_shape::circle Button: the reject classes name the field, the rule and the fix::refuses BOTH authored axes — that is the arithmetic the solver exists to do",
     "button_shape::circle Button: the reject classes name the field, the rule and the fix::refuses an icon on a rectangle and points at the control that owns that layout",
     "button_shape::circle Button: the reject classes name the field, the rule and the fix::refuses an icon NAME that means nothing, listing the framework vocabulary",
     "button_shape::circle Button: the reject classes name the field, the rule and the fix::refuses an asset id in `icon` — a control never names an asset",
     "button_shape::circle Button: the reject classes name the field, the rule and the fix::refuses an unknown shape with the closed set",
     "button_shape::circle Button: the reject classes name the field, the rule and the fix::refuses a REACTIVE shape: a shape is what the control is, not a state it passes through",
     "button_shape::circle Button: the schema declares it the way the parity checker requires::shape is a closed style-authority set that is not reactive",
     "button_shape::circle Button: the schema declares it the way the parity checker requires::icon is style authority too, and both are creation-applied style props",
     "button_shape::circle Button: the schema declares it the way the parity checker requires::shape is applied FIRST, so every later chrome write sees it",
     "button_shape::circle Button geometry: a true 1:1 box from the control metric::with neither axis authored, both come from controls.button.height",
     "button_shape::circle Button geometry: a true 1:1 box from the control metric::the diameter IS the metric: a metric package with a bigger button height makes a bigger disc",
     "button_shape::circle Button geometry: a true 1:1 box from the control metric::every package answers controls.button.height even when it authored none",
     "button_shape::circle Button geometry: a true 1:1 box from the control metric::a PIXEL package snaps the diameter up onto its grid",
     "button_shape::circle Button geometry: a true 1:1 box from the control metric::an authored WIDTH drives the height, and an authored HEIGHT drives the width",
     "button_shape::circle Button geometry: a true 1:1 box from the control metric::a circle's authored diameter FLOORS at the picture it has to hold",
     "button_shape::circle Button geometry: a true 1:1 box from the control metric::...and a disc roomier than its picture keeps the diameter the author asked for",
     "button_shape::circle Button geometry: a true 1:1 box from the control metric::a FILL width still squares: the paired axis takes the extent the parent offered",
     "button_shape::circle Button geometry: a true 1:1 box from the control metric::a rectangular Button is byte-unchanged: content width, theme button padding",
     "button_shape::circle Button geometry: a true 1:1 box from the control metric::the disc's default padding is ZERO — the diameter IS the box — and an authored one still wins",
     "button_shape::circle Button hit geometry is the FULL SQUARE::a disc at the floor is honestly a 44px target on both axes",
     "button_shape::circle Button hit geometry is the FULL SQUARE::a SMALLER disc still gets the 44 floor on both axes, squarely",
     "button_shape::circle Button paint: the pill and the rim are phantom modifier rules::the base seed emits a 0.5-scale corner on the shape tag — the slider thumb's proven path",
     "button_shape::circle Button paint: the pill and the rim are phantom modifier rules::the rim carries ApplyStrokeMode = Border, or it would halo the glyph instead",
     "button_shape::circle Button paint: the pill and the rim are phantom modifier rules::a PACKAGE emits both in the roles group, after the surfaces whose corners they replace",
     "button_shape::circle Button paint: the pill and the rim are phantom modifier rules::the disc's rim is a METRIC rule prop, so a live hairline edit re-strokes it",
     "button_shape::circle Button paint: the pill and the rim are phantom modifier rules::a FLAT theme pays ZERO new instances for the disc: rules create the modifiers",
     "button_shape::circle Button paint: ONE rule is enough, and that is an invariant, not luck::beats every surface corner in BOTH builders",
     "button_shape::circle Button paint: ONE rule is enough, and that is an invariant, not luck::no LATER rule writes CornerRadius on a selector a circle Button matches — except the skinned suppression",
     "button_shape::circle Button paint: the tag reaches the live adapter's desired set::classifyTags earns facet-shape-circle, and rect earns nothing",
     "button_shape::circle Button paint: the tag reaches the live adapter's desired set::the shape composes with every surface, role and state a rectangle takes",
     "button_shape::circle Button paint: the tag reaches the live adapter's desired set::the adapter puts the shape in the DESIRED SET, so a later pass cannot square it off",
     "button_shape::circle Button paint: the tag reaches the live adapter's desired set::the adapter records the shape and re-radiuses the REAL UICorner on the fallback path",
     "button_shape::circle Button paint: the tag reaches the live adapter's desired set::every bespoke Button corner site consults the handle, or a disc would go square",
     "button_shape::circle Button content: one semantic icon OR up to three characters::an icon button DRAWS the framework's ASCII glyph and keeps the label as the name",
     "button_shape::circle Button content: one semantic icon OR up to three characters::the glyph is what reaches the adapter, not the semantic label",
     "button_shape::circle Button content: one semantic icon OR up to three characters::the glyph is also what the SOLVER measured — one decision, two seams",
     "button_shape::circle Button content: one semantic icon OR up to three characters::a TEXT circle draws its own short label",
     "button_shape::circle Button content: one semantic icon OR up to three characters::the icon request reaches the chrome subsystem through the ONE reconcile entry point",
     "button_shape::circle Button content: one semantic icon OR up to three characters::every framework icon name resolves to an ASCII-safe glyph, so a disc is legible under any font",
     "button_shape::circle Button skinning needs no new slot::a disc classifies to the ordinary `control` decoration slot",
     "button_shape::a Button's label wraps only where a break is legal::one word in a box too small for it stays ONE line — it ellipsizes, never breaks",
     "button_shape::a Button's label wraps only where a break is legal::...and the same is true of the short words that showed the defect",
     "button_shape::a Button's label wraps only where a break is legal::a PHRASE in a box too small for it still wraps — that break is legal",
     "button_shape::a Button's label wraps only where a break is legal::the ADAPTER makes the same call, from the same string AND the solver's width verdict",
     "button_shape::a Button's label wraps only where a break is legal::BEHAVIOURAL: the two seams agree on a phrase whose longest word does not fit",
     "zstack_shape::circle ZStack: the accept classes::takes shape = circle and constructs",
     "zstack_shape::circle ZStack: the accept classes::takes shape = rect explicitly, which is what every ZStack already was",
     "zstack_shape::circle ZStack: the accept classes::leaves an ordinary ZStack untouched: no shape prop at all",
     "zstack_shape::circle ZStack: the accept classes::takes ONE authored axis on either side",
     "zstack_shape::circle ZStack: the reject classes name the field, the rule and the fix::refuses BOTH authored axes — the same rule the circle Button refuses by",
     "zstack_shape::circle ZStack: the reject classes name the field, the rule and the fix::refuses an unknown shape with the closed set",
     "zstack_shape::circle ZStack: the reject classes name the field, the rule and the fix::refuses a REACTIVE shape: a shape is what the container is, not a state it passes through",
     "zstack_shape::circle ZStack: the schema declares it the way the parity checker requires, sharing Button's spec::shape is a closed style-authority set that is not reactive, and it is the SAME spec object as Button's",
     "zstack_shape::circle ZStack: the schema declares it the way the parity checker requires, sharing Button's spec::promoting shape to `common` did not move Button's own authority",
     "zstack_shape::circle ZStack geometry: a true 1:1 box from the control metric::with neither axis authored, both come from controls.button.height",
     "zstack_shape::circle ZStack geometry: a true 1:1 box from the control metric::an authored WIDTH drives the height, and an authored HEIGHT drives the width",
     "zstack_shape::circle ZStack geometry: a true 1:1 box from the control metric::a FILL width still squares: the paired axis takes the extent the parent offered (the aspect+fill pairing)",
     "zstack_shape::circle ZStack geometry: a true 1:1 box from the control metric::a rectangular ZStack is byte-unchanged: no shape prop, no square enforcement",
     "zstack_shape::circle ZStack geometry: a true 1:1 box from the control metric::a metric package with a bigger button height makes a bigger circle ZStack too — one metric, two consumers",
     "zstack_shape::circle ZStack paint: the SAME phantom rule a circle Button uses, no new sheet code::classifyTags earns facet-shape-circle from the prop alone — no class field even accepted",
     "zstack_shape::circle ZStack paint: the SAME phantom rule a circle Button uses, no new sheet code::the render tree's ZStack node carries the shape prop through to the adapter, same as Button's",
     "compact_label::compactLabel: a node with BOTH dimensions fixed still runs the ladder::a 44px square button draws its GLYPH, not its ellipsized label",
     "compact_label::compactLabel: a node with BOTH dimensions fixed still runs the ladder::…and a square button with ROOM still draws the full label",
     "compact_label::compactLabel: a node with BOTH dimensions fixed still runs the ladder::a square button with NO compact form is unchanged (no extra measure, no swap)",
     "compact_label::compactLabel: the ladder::a label that FITS is drawn in full — nothing that fits today moves",
     "compact_label::compactLabel: the ladder::a label that does NOT fit is replaced by the compact form",
     "compact_label::compactLabel: the ladder::...and the compact form is ONE LINE, which is the whole point",
     "compact_label::compactLabel: the ladder::a PHRASE takes the compact form instead of wrapping",
     "compact_label::compactLabel: the ladder::with NO compact form, round 6's word/phrase rule is untouched",
     "compact_label::compactLabel: the ladder::an UNBOUNDED button never goes compact — a button free to grow says everything",
     "compact_label::compactLabel: one decision, two seams::the box the solver RESERVED is the box the compact string needs, not the full label's",
     "compact_label::compactLabel: one decision, two seams::the SOLVER reports the verdict on the same channel as the rect it produced",
     "compact_label::compactLabel: one decision, two seams::the verdict is RE-DECIDED, never latched — a re-solve that makes room restores the label",
     "compact_label::compactLabel: one decision, two seams::a REMOUNTED button gets its verdict re-applied — the cache dies with the node",
     "compact_label::compactLabel: one decision, two seams::a steady layout costs ZERO extra prop writes",
     "compact_label::compactLabel: the closed grammar::accepts the four legal spellings",
     "compact_label::compactLabel: the closed grammar::refuses an unknown form — a value nothing validates is a UI that silently draws nothing",
     "compact_label::compactLabel: the closed grammar::refuses TWO forms — that is two answers to one question",
     "compact_label::compactLabel: the closed grammar::refuses an empty table, an empty string and a non-string value",
     "compact_label::compactLabel: the closed grammar::refuses whitespace in a compact string, naming the reason",
     "compact_label::compactLabel: the closed grammar::refuses an icon name the framework cannot resolve, listing the ones it knows",
     "compact_label::compactLabel: the closed grammar::refuses a compact form on a CONTENT button, which draws no text to shorten",
     "compact_label::compactLabel: the closed grammar::is NOT reactive: a compact representation is what the control HAS",
     "compact_label::compactLabel: the icon form degrades and the image form does not::an icon form draws the framework's ASCII glyph when no art resolves",
     "compact_label::compactLabel: the icon form degrades and the image form does not::...and that glyph is ASCII-safe, so it cannot be tofu under any font",
     "compact_label::compactLabel: the icon form degrades and the image form does not::an IMAGE form draws nothing textual — the picture IS the content",
     "compact_label::compactLabel: the icon form degrades and the image form does not::every framework icon name has BOTH an ASCII glyph and a standard-art entry",
     "compact_label::compactLabel: the icon form degrades and the image form does not::standard_icons.resolve() answers nil for unknown names, art for uploaded ones",
     "compact_label::compactLabel: measured under every reference package::the ladder holds under the flat theme and all eight shipped packages",
     "compact_label::compactLabel: measured under every reference package::the compact form never costs a second line, under any package",
     "compact_label::compactLabel: the table's Edit/Done toggle, its first consumer::the toggle still sits at the path both of the table's own matchers anchor on",
     "compact_label::compactLabel: the table's Edit/Done toggle, its first consumer::its ACTIVATE router still matches — the button is not merely rendering",
     "compact_label::compactLabel: the table's Edit/Done toggle, its first consumer::with room, it still says its word — the shipped look does not move",
     "compact_label::compactLabel: the table's Edit/Done toggle, its first consumer::and it declares a compact form, so it can never fall back to an ellipsis",
     "value_controls::B-VAL1: the shared value model::clamps to its bounds",
     "value_controls::B-VAL1: the shared value model::quantizes onto a grid measured FROM min, so the bounds are reachable",
     "value_controls::B-VAL1: the shared value model::a non-integer grid still lands exactly on the bounds",
     "value_controls::B-VAL1: the shared value model::stepping always makes progress and never leaves the range",
     "value_controls::B-VAL1: the shared value model::a continuous model steps by a fraction of the range",
     "value_controls::B-VAL1: the shared value model::fraction and fromFraction round-trip through the grid",
     "value_controls::B-VAL1: the shared value model::formats without noise and never prints negative zero",
     "value_controls::B-VAL1: the shared value model::a custom formatter is honoured everywhere the value is shown",
     "value_controls::B-VAL1: the shared value model::a throwing formatter falls back to the default format instead of unwinding a solve",
     "value_controls::B-VAL1: the shared value model::a formatter that throws for ONE value still formats the others",
     "value_controls::B-VAL1: the shared value model::semantic value text states the value IN ITS RANGE",
     "value_controls::B-VAL1: the shared value model::a NaN or infinite value resolves to the low bound instead of propagating",
     "value_controls::B-VAL1: the shared value model::an impossible range or step is a build error, not a silent degradation",
     "value_controls::B-VAL3: Stepper reaches the same arithmetic from every input class::pointer: tapping plus and minus steps the owner's value",
     "value_controls::B-VAL3: Stepper reaches the same arithmetic from every input class::touch: a finger tap on + steps it identically",
     "value_controls::B-VAL3: Stepper reaches the same arithmetic from every input class::keyboard: Adjust on the focused control steps without touching a button",
     "value_controls::B-VAL3: Stepper reaches the same arithmetic from every input class::gamepad: ButtonA on the focused + activates, and the bumpers Adjust",
     "value_controls::B-VAL3: Stepper reaches the same arithmetic from every input class::no Adjust key is bound while focus sits outside the Stepper (no gameplay bumper shadow)",
     "value_controls::B-VAL3: Stepper bounds, formatting, disabled and lifecycle::stops at both bounds instead of running past them",
     "value_controls::B-VAL3: Stepper bounds, formatting, disabled and lifecycle::the affordance at a bound is DISABLED, not silently inert",
     "value_controls::B-VAL3: Stepper bounds, formatting, disabled and lifecycle::the readout follows the value with no factory rerun",
     "value_controls::B-VAL3: Stepper bounds, formatting, disabled and lifecycle::a custom format reaches the visible readout",
     "value_controls::B-VAL3: Stepper bounds, formatting, disabled and lifecycle::an explicitly disabled Stepper refuses every input class",
     "value_controls::B-VAL3: Stepper bounds, formatting, disabled and lifecycle::rejects a non-settable value instead of silently owning one",
     "value_controls::B-VAL3: Stepper bounds, formatting, disabled and lifecycle::dump is deterministic",
     "value_controls::B-VAL3: Stepper bounds, formatting, disabled and lifecycle::build/interact/dispose returns the registry to its baseline",
     "value_controls::slider: the fill and thumb show the value AT REST (director review 2026-07-24)::paints the fill and positions the thumb on the first solve, with no interaction",
     "value_controls::slider: the fill and thumb show the value AT REST (director review 2026-07-24)::an idle slider is idle: re-feeding the same track geometry writes nothing",
     "value_controls::slider: the fill and thumb show the value AT REST (director review 2026-07-24)::follows the track when a resize changes it, without the value moving",
     "value_controls::slider: the label clears the thumb (director review 2026-07-24)::leaves real space between the label's glyphs and the circle that can sit at the track's left edge",
     "value_controls::slider: the label clears the thumb (director review 2026-07-24)::the clearance survives a label whose measured box is exactly its glyphs",
     "value_controls::B-VAL2: the Slider's live drag is the NATIVE detector, with a proven fallback::attaches a UIDragDetector to the track through the controller seam",
     "value_controls::B-VAL2: the Slider's live drag is the NATIVE detector, with a proven fallback::a native drag moves the value and commits on end",
     "value_controls::B-VAL2: the Slider's live drag is the NATIVE detector, with a proven fallback::FALLBACK: with no detector, the Grip capture path drives the SAME arithmetic",
     "value_controls::B-VAL2: the Slider's live drag is the NATIVE detector, with a proven fallback::pointer: tap-to-position jumps to the tapped fraction",
     "value_controls::B-VAL2: the Slider's live drag is the NATIVE detector, with a proven fallback::tap-to-position can be switched off",
     "value_controls::B-VAL2: the Slider's live drag is the NATIVE detector, with a proven fallback::touch: a touch-typed drag drives it identically",
     "value_controls::B-VAL2: the Slider's live drag is the NATIVE detector, with a proven fallback::the track meets the effective-target floor",
     "value_controls::B-VAL3: Slider keyboard, gamepad, bounds and hot-switch::keyboard: Adjust steps predictably rather than requiring a drag",
     "value_controls::B-VAL3: Slider keyboard, gamepad, bounds and hot-switch::gamepad: the bumpers Adjust the focused slider",
     "value_controls::B-VAL3: Slider keyboard, gamepad, bounds and hot-switch::stays inside its bounds from every route",
     "value_controls::B-VAL3: Slider keyboard, gamepad, bounds and hot-switch::Slider hot-switch: losing the pointer class mid-drag CANCELS back to the snapshot",
     "value_controls::B-VAL3: Slider keyboard, gamepad, bounds and hot-switch::a cancelled capture also reverts (the adapter's own cancel path)",
     "value_controls::B-VAL3: Slider keyboard, gamepad, bounds and hot-switch::a disabled Slider refuses drag AND Adjust",
     "value_controls::B-VAL3: Slider keyboard, gamepad, bounds and hot-switch::shares the value model with Stepper: identical bounds arithmetic",
     "value_controls::B-VAL3: Slider keyboard, gamepad, bounds and hot-switch::dump is deterministic and build/dispose is registry-neutral",
     "value_controls::verifier V13: a disabled Slider leaves focus order::navigation skips a disabled Slider's track instead of only refusing the verb",
     "value_controls::verifier V11: the hit expander is re-based by its clip host::a sub-floor control inside a ScrollView gets an expander in HOST space, not window space",
     "value_controls::verifier V11: the hit expander is re-based by its clip host::the framework still asks for the expansion in solver space",
     "value_controls::stepper: target geometry (director review 2026-07-24)::the -/+ buttons carry the 44px floor on BOTH axes, not just height",
     "value_controls::stepper: target geometry (director review 2026-07-24)::the readout is centred and keeps a stable width, so the buttons never shift",
     "value_controls::Slider: the decoration slot hint and the per-view override::declares its rail and thumb slots on the INTERNAL meta channel",
     "value_controls::Slider: the decoration slot hint and the per-view override::a per-view thumbImage/trackImage rides the same channel",
     "value_controls::Slider: the decoration slot hint and the per-view override::without an override the control stays on the theme's rung",
     "value_controls::Slider: the decoration slot hint and the per-view override::the override still works, and still mounts, on a flat theme",
     "value_controls::Slider: the decoration hint reaches the render target::the adapter receives each hinted node's slot through create()",
     "value_controls::Slider: the decoration hint reaches the render target::a per-view override rides the same argument",
     "value_controls::Slider: the decoration hint reaches the render target::the hint yields the tag the sheet's rules actually select on",
     "rating::Rating: the value model::a read-only Memo is refused at BUILD, not on the first tap",
     "rating::Rating: the value model::a count that is not a whole number >= 1 is a build error",
     "rating::Rating: the value model::glyphs are filled up to the value and hollow after it, and repaint in place",
     "rating::Rating: the value model::the value is clamped to the run, and to zero only when zero is allowed",
     "rating::Rating: the value model::semantic text reports the value IN ITS RANGE, never a bare number",
     "rating::Rating: the value model::dump is deterministic and carries its schema string",
     "rating::Rating: the value model::build/interact/dispose returns the registry to its baseline",
     "rating::Rating: one control, not N buttons::no star is a Button, so no theme can paint a plate behind one",
     "rating::Rating: one control, not N buttons::the whole strip is ONE focus stop, not one per star",
     "rating::Rating: one control, not N buttons::a read-only rating paints but is not focusable and refuses every input",
     "rating::Rating: one control, not N buttons::a disabled rating refuses every input class, not just the pointer",
     "rating::Rating reaches the same arithmetic from every input class::pointer: clicking a star sets the rating to that star",
     "rating::Rating reaches the same arithmetic from every input class::touch: a finger drag across the strip scrubs the value live",
     "rating::Rating reaches the same arithmetic from every input class::keyboard: Adjust on the focused strip rates without touching a star",
     "rating::Rating reaches the same arithmetic from every input class::gamepad: the bumpers Adjust the focused rating",
     "rating::Rating reaches the same arithmetic from every input class::no Adjust key is bound while focus sits outside the Rating (no gameplay bumper shadow)",
     "rating::Rating reaches the same arithmetic from every input class::hotSwitch: losing the pointer mid-scrub CANCELS back, it never commits",
     "rating::Rating: at its limit the arrow goes back to navigation::at the MAXIMUM, one more Right moves the ring instead of the rating",
     "rating::Rating: at its limit the arrow goes back to navigation::at the MINIMUM, one more Left moves the ring the other way",
     "rating::Rating: at its limit the arrow goes back to navigation::the yield RE-ARMS: walking back onto the strip adjusts again",
     "rating::Rating: at its limit the arrow goes back to navigation::the yield RE-ARMS: the NEXT value control in the row is not dead on arrival",
     "rating::Rating: at its limit the arrow goes back to navigation::the yield RE-ARMS: the rating in the NEXT ROW down still takes the arrow",
     "rating::Rating: at its limit the arrow goes back to navigation::BELOW the limit the arrow is still the control's — the ring never moves",
     "rating::Rating: at its limit the arrow goes back to navigation::NEGATIVE CONTROL: the BUMPERS never yield — they took nothing from navigation",
     "rating::Rating: the strip fits the cell a table gives it, under every package::five stars fit a 132px cell in the flat theme and in all eight shipped packages",
     "rating::Rating: the strip fits the cell a table gives it, under every package::...at the rung a table row actually uses, in every package",
     "rating::Rating: the strip fits the cell a table gives it, under every package::the SPACING follows the glyph, not the cell — no package spreads its stars",
     "rating::Rating: the strip fits the cell a table gives it, under every package::...and the leftover is CENTRED in the column, not dumped after the last star",
     "rating::Rating: the strip fits the cell a table gives it, under every package::an unknown starSize is a build error, not a silently-default star",
     "rating::Rating: the star run spaces itself for the input paradigm::touch spaces the run WIDER than a pointer, in every package",
     "rating::Rating: the star run spaces itself for the input paradigm::the package that showed the defect is no longer 2px between 12px stars",
     "rating::Rating: the star run spaces itself for the input paradigm::...and neither paradigm overruns the cell, in any package",
     "rating::Rating: the star run spaces itself for the input paradigm::a rating with NO env takes the pointer spacing, which is the one that always fits",
     "display_controls::B-DSP1: ProgressView is determinate and style-owned::maps the value onto a percent fill",
     "display_controls::B-DSP1: ProgressView is determinate and style-owned::a reactive value re-renders the fill with no rebuild",
     "display_controls::B-DSP1: ProgressView is determinate and style-owned::clamps out-of-range values instead of overflowing the bar",
     "display_controls::B-DSP1: ProgressView is determinate and style-owned::paint is style-owned through the BAR FAMILY, never a borrowed surface",
     "display_controls::B-DSP1: ProgressView is determinate and style-owned::carries semantic value-in-range text and a deterministic dump",
     "display_controls::B-DSP1: ProgressView is determinate and style-owned::is registry-neutral after dispose",
     "display_controls::B-DSP1: Label keeps a semantic text fallback::renders icon and title together",
     "display_controls::B-DSP1: Label keeps a semantic text fallback::an icon-only presentation still reports the title as its semantic text",
     "display_controls::B-DSP1: Label keeps a semantic text fallback::semanticText is a Readable, like every sibling that publishes one",
     "display_controls::B-DSP1: Label keeps a semantic text fallback::dump is deterministic and dispose is registry-neutral",
     "display_controls::B-DSP1: Label keeps a semantic text fallback::iconOnly DEGRADES to the title when there is no icon (an empty square is worse than a word)",
     "display_controls::B-DSP1: Label keeps a semantic text fallback::titleOnly renders no empty icon slot",
     "display_controls::B-DSP1: Label keeps a semantic text fallback::requires a title, because an icon-only label without one has no accessible name",
     "display_controls::B-DSP1: Label keeps a semantic text fallback::rejects an unknown presentation",
     "display_controls::B-DSP2: Picker presentation adapts from space, not device names::the adaptive rule stacks when options cannot sit side by side",
     "display_controls::B-DSP2: Picker presentation adapts from space, not device names::segmented lays options along x; inline stacks them along y",
     "display_controls::B-DSP2: Picker presentation adapts from space, not device names::a live size-class change flips the presentation WITHOUT remounting the options",
     "display_controls::B-DSP2: Picker presentation adapts from space, not device names::pointer: tapping an option selects it and reports through onChange",
     "display_controls::B-DSP2: Picker presentation adapts from space, not device names::keyboard and gamepad select through the same path",
     "display_controls::B-DSP2: Picker presentation adapts from space, not device names::touch: a finger tap selects identically, and every row meets the floor",
     "display_controls::B-DSP2: Picker presentation adapts from space, not device names::selection rides the `selected` style binding, never a bespoke fill",
     "display_controls::B-DSP2: Picker presentation adapts from space, not device names::a disabled Picker refuses selection",
     "display_controls::B-DSP2: Picker presentation adapts from space, not device names::rejects an empty option set, a non-settable selection, and an unknown presentation",
     "display_controls::B-DSP2: Picker presentation adapts from space, not device names::dump is deterministic and dispose is registry-neutral",
     "display_controls::B-DSP2: DisclosureGroup updates focus correctly::expanding mounts the content; collapsing unmounts it",
     "display_controls::B-DSP2: DisclosureGroup updates focus correctly::collapsing with focus INSIDE the content moves focus back to the header first",
     "display_controls::B-DSP2: DisclosureGroup updates focus correctly::expanding leaves focus on the header (the player asked to SEE the content)",
     "display_controls::B-DSP2: DisclosureGroup updates focus correctly::keyboard and gamepad toggle it through the same path",
     "display_controls::B-DSP2: DisclosureGroup updates focus correctly::touch: a finger tap toggles it, and the header meets the floor",
     "display_controls::B-DSP2: DisclosureGroup updates focus correctly::a disabled group does not toggle",
     "display_controls::B-DSP2: DisclosureGroup updates focus correctly::requires a label, a settable expanded signal, and a content function",
     "display_controls::B-DSP2: DisclosureGroup updates focus correctly::dump is deterministic and dispose is registry-neutral",
     "display_controls::B-DSP3: PopupButton rows meet the enforced target floor::the trigger and every open row report a hit rect at or above the floor",
     "display_controls::B-DSP3: PopupButton adapts its presentation::the pure rule prefers a sheet on touch, inline when short, menu otherwise",
     "display_controls::B-DSP3: PopupButton adapts its presentation::a sheet anchors to the bottom edge with visually larger rows",
     "display_controls::B-DSP3: PopupButton adapts its presentation::a menu sits just below the trigger with compact rows",
     "display_controls::B-DSP3: PopupButton adapts its presentation::an inline list takes the CONTROL surface, not a floating-panel one",
     "display_controls::B-DSP3: PopupButton adapts its presentation::automatic resolves from LIVE facts, and re-resolves when they change",
     "display_controls::B-DSP3: PopupButton adapts its presentation::a LIVE touch class flips it to a sheet with no rebuild of the trigger",
     "display_controls::B-DSP3: PopupButton adapts its presentation::every presentation keeps selection, Cancel, and the tap-away machinery",
     "display_controls::B-DSP3: PopupButton adapts its presentation::rejects an unknown presentation at build",
     "display_controls::progress: exact vertical centring (director review 2026-07-24)::the track centres on the row's midline with no half-pixel bias",
     "display_controls::label: the title reads centred in its own box (director review 2026-07-24)::centres its text so the measurer's width over-reserve does not pile up on one side",
     "display_controls::ProgressView: indeterminate is selected by value = nil (§3.1)::the DETERMINATE tree and dump are exactly what they were before indeterminate existed",
     "display_controls::ProgressView: indeterminate is selected by value = nil (§3.1)::value = nil builds the sweeping segment instead of a value-tracking fill",
     "display_controls::ProgressView: indeterminate is selected by value = nil (§3.1)::the segment PEAKS AT MID-CYCLE and folds back, and never leaves the track",
     "display_controls::ProgressView: indeterminate is selected by value = nil (§3.1)::the cycle REPEATS: the indicator is still travelling in its third second",
     "display_controls::ProgressView: indeterminate is selected by value = nil (§3.1)::REDUCED MOTION keeps the indicator advancing — quantized, never frozen (§3.1)",
     "display_controls::ProgressView: indeterminate is selected by value = nil (§3.1)::FULL motion writes far more often than reduced motion over the same wall clock",
     "display_controls::ProgressView: indeterminate is selected by value = nil (§3.1)::with NO clock the indeterminate view holds its rest pose and says so",
     "display_controls::ProgressView: indeterminate is selected by value = nil (§3.1)::is registry-neutral after dispose, clock and all",
     "display_controls::ProgressView: an indeterminate view is OWNED, and stops with its owner::keeps writing while it is alive, and stops DEAD when its owner is disposed",
     "display_controls::ProgressView: an indeterminate view is OWNED, and stops with its owner::a view built and NEVER presented stops with its owner too",
     "display_controls::ProgressView: an indeterminate view is OWNED, and stops with its owner::`value = nil` without an owner is an authoring error that names the seam",
     "display_controls::ProgressView: an indeterminate view is OWNED, and stops with its owner::dispose() and the owner's disposal are safe in EITHER order, and in both",
     "display_controls::ProgressView: an indeterminate view is OWNED, and stops with its owner::the DETERMINATE path takes no clock entry at all, and needs no owner",
     "display_controls::ProgressView: the spinner presentation (§3.1)::is five dots on the one new theme slot, and no bar at all",
     "display_controls::ProgressView: the spinner presentation (§3.1)::THE PULSE IS PAINT, NOT GEOMETRY: every dot keeps a fixed square box",
     "display_controls::ProgressView: the spinner presentation (§3.1)::the pulse TRAVELS: the brightest dot moves along the ring as the phase sweeps",
     "display_controls::ProgressView: the spinner presentation (§3.1)::under REDUCED motion the pulse still travels, on the quantized tick",
     "display_controls::ProgressView: the refusals that keep the two modes from contradicting::refuses a spinner with a value — and names the shape that DOES draw one",
     "display_controls::ProgressView: the refusals that keep the two modes from contradicting::refuses an unknown presentation, listing the three that exist",
     "display_controls::ProgressView: the refusals that keep the two modes from contradicting::every shape x mode cell is exactly as the capability registry declares",
     "display_controls::ProgressView: the refusals that keep the two modes from contradicting::refuses `height` on a circular indicator, naming the metric that owns its size",
     "display_controls::ProgressView: the refusals that keep the two modes from contradicting::refuses `showValue` on a circular indicator rather than putting it beside the ring",
     "display_controls::ProgressView: the refusals that keep the two modes from contradicting::...and `showValue` still renders beside the BAR, which is the shape that has room",
     "display_controls::ProgressView: the refusals that keep the two modes from contradicting::refuses `height` on a spinner rather than silently making it the DOT size",
     "display_controls::ProgressView: the refusals that keep the two modes from contradicting::...and `height` keeps its ONE meaning on the bar: the track's thickness",
     "display_controls::ProgressView: the refusals that keep the two modes from contradicting::refuses min/max/format/showValue on a view with no value, rather than dropping them",
     "progress_circular::ProgressView circular: the DETERMINATE ring is the value's own arc::draws an arc over a full backing ring, and no bar at all",
     "progress_circular::ProgressView circular: the DETERMINATE ring is the value's own arc::the SWEEP is the value: 25% ends at 3 o'clock, 100% closes the ring",
     "progress_circular::ProgressView circular: the DETERMINATE ring is the value's own arc::a value change is PAINT ONLY: one prop write on `points`, zero re-solves",
     "progress_circular::ProgressView circular: the INDETERMINATE ring rotates its own start angle::the HEAD travels while the sweep stays fixed — and there is no backing ring",
     "progress_circular::ProgressView circular: the INDETERMINATE ring rotates its own start angle::rotating is PAINT: 60 frames of travel write points and never re-solve",
     "progress_circular::ProgressView circular: the INDETERMINATE ring rotates its own start angle::takes the SAME single clock entry the dots take — no second one for the arc",
     "progress_circular::ProgressView circular: the INDETERMINATE ring rotates its own start angle::under REDUCED motion the ring keeps advancing, on the quantized tick",
     "progress_circular::ProgressView circular: the INDETERMINATE ring rotates its own start angle::with no clock it holds its rest pose and says so",
     "progress_circular::ProgressView circular: the OFF-PATH cost is exactly zero::no circular indicator mounted = no Path instance and no `points` op, ever",
     "progress_circular::the two theme-sized shapes are AUTHORED by every shipped package::classic_desktop authors all three, rather than inheriting the space scale",
     "progress_circular::the two theme-sized shapes are AUTHORED by every shipped package::compact_pointer authors all three, rather than inheriting the space scale",
     "progress_circular::the two theme-sized shapes are AUTHORED by every shipped package::fantasy_ornate authors all three, rather than inheriting the space scale",
     "progress_circular::the two theme-sized shapes are AUTHORED by every shipped package::fantasy_parchment authors all three, rather than inheriting the space scale",
     "progress_circular::the two theme-sized shapes are AUTHORED by every shipped package::glossy_mobile authors all three, rather than inheriting the space scale",
     "progress_circular::the two theme-sized shapes are AUTHORED by every shipped package::glossy_touch authors all three, rather than inheriting the space scale",
     "progress_circular::the two theme-sized shapes are AUTHORED by every shipped package::pixel_quest authors all three, rather than inheriting the space scale",
     "progress_circular::the two theme-sized shapes are AUTHORED by every shipped package::scifi_hud authors all three, rather than inheriting the space scale",
     "progress_circular::the two theme-sized shapes are AUTHORED by every shipped package::every package's shapes are its OWN, not Studio Neutral's numbers",
     "progress_circular::the two theme-sized shapes are AUTHORED by every shipped package::the corpus SPANS the two shapes: the largest ring is at least twice the smallest",
     "progress_circular::the two theme-sized shapes are AUTHORED by every shipped package::a package that authors NOTHING still answers all three (the fill-in stays)",
     "progress_circular::ProgressView circular: the stroke fits inside its own box, in every shipped theme::radius * size / 2 + thickness / 2 never exceeds size / 2",
     "progress_circular::ProgressView circular: the stroke fits inside its own box, in every shipped theme::the ARC'S COST is a function of its sweep, not of the theme's size",
     "progress_circular::ProgressView circular: it lays out in every shipped theme, at every swept viewport::no solver diagnostic, and the label never overlaps the ring",
     "progress_circular::ProgressView circular: it lays out in every shipped theme, at every swept viewport::a LONG label on a NARROW screen still fits — every presentation, every package",
     "theme_package::themes.define: a valid package compiles, freezes and stamps::compiles the reference definition and freezes it deeply",
     "theme_package::themes.define: a valid package compiles, freezes and stamps::the content stamp is deterministic and moves with the content",
     "theme_package::themes.define: a valid package compiles, freezes and stamps::Studio Neutral compiles through the same public gate",
     "theme_package::themes.define: derivation from a base package::inherits every value the derived package does not restate",
     "theme_package::themes.define: derivation from a base package::a derived package is gated exactly as strictly as a standalone one",
     "theme_package::themes.define: derivation from a base package::rejects a `base` that is not a compiled package",
     "theme_package::themes.define: rejection classes (each names the field and the fix)::missing roles: a typography role, a space step, a control family metric",
     "theme_package::themes.define: rejection classes (each names the field and the fix)::unknown fields in any section, with a 'did you mean' where it is cheap",
     "theme_package::themes.define: rejection classes (each names the field and the fix)::unknown fields are caught in typography, controls, insets, chrome and assets",
     "theme_package::themes.define: rejection classes (each names the field and the fix)::contrast failures name the pair and its measured ratio",
     "theme_package::themes.define: rejection classes (each names the field and the fix)::invalid insets: negative and NaN",
     "theme_package::themes.define: rejection classes (each names the field and the fix)::a target size below the accessibility floor",
     "theme_package::themes.define: rejection classes (each names the field and the fix)::a nineSlice recipe without a fallback, or naming an undeclared asset",
     "theme_package::themes.define: rejection classes (each names the field and the fix)::a nineSlice recipe with a declared asset, insets and fallback COMPILES",
     "theme_package::themes.define: rejection classes (each names the field and the fix)::negative or NaN nineSlice contentInsets",
     "theme_package::themes.define: rejection classes (each names the field and the fix)::an incompatible schema version, declared either way round",
     "theme_package::themes.define: rejection classes (each names the field and the fix)::callbacks are rejected explicitly, wherever they hide",
     "theme_package::themes.define: rejection classes (each names the field and the fix)::a package definition that is not a table fails without throwing",
     "theme_package::themes.lintProperty: the §4 legal-property boundary::accepts the native paint set the Step 2 sheet already owns",
     "theme_package::themes.lintProperty: the §4 legal-property boundary::rejects every solver-, binding-, presentation- and host-owned property BY NAME",
     "theme_package::themes.lintProperty: the §4 legal-property boundary::names the OWNER so the author knows why, not merely that",
     "theme_package::themes.lintProperty: the §4 legal-property boundary::image chrome is legal only inside a nineSlice recipe",
     "theme_package::themes.lintProperty: the §4 legal-property boundary::an unknown property is rejected rather than quietly allowed",
     "theme_package::theme package: danger contrast parity (integration find, 2026-07-25)::rejects a theme whose danger/onDanger pair fails 4.5:1 at DEFINE time",
     "theme_package::theme package: danger contrast parity (integration find, 2026-07-25)::a theme that omits the pair rides the library fallbacks and passes",
     "theme_package::theme package: namespaced contribution coverage::a covering package passes with every need listed",
     "theme_package::theme package: namespaced contribution coverage::an uncovering package gets a useful pre-play error naming role and fix",
     "theme_package::theme package: namespaced contribution coverage::a partial control size names exactly the missing fields",
     "theme_package::theme package: namespaced contribution coverage::an unnamespaced or unknown-kind declaration is itself rejected",
     "theme_package::themes.define: the caller's own definition tables stay theirs (D24)::define deep-copies every section before freezing — def.metrics stays mutable",
     "theme_package::themes.define: the caller's own definition tables stay theirs (D24)::the compiled package is still deeply frozen",
     "theme_package::themes.define: the callback scan reaches real authoring depth (D25)::finds a function nested 15 levels deep (the old cap was 12)",
     "theme_package::themes.define: the callback scan reaches real authoring depth (D25)::a self-referential definition terminates instead of recursing forever",
     "theme_package::themes.define: a font family must be resolvable (D23)::rejects a bare unknown family with a fix line",
     "theme_package::themes.define: a font family must be resolvable (D23)::accepts an rbxasset:// / rbxassetid:// URL and the registered aliases",
     "theme_package::themes.define: a font family must be resolvable (D23)::the alias list matches what the client materializer can actually resolve",
     "theme_package::themes.define: gradient alpha is engine Transparency (D18a)::rejects a stop above 0.9 and explains that it fades the text too",
     "theme_package::themes.define: gradient alpha is engine Transparency (D18a)::accepts a subtle ramp",
     "theme_package::themes.lintProperty: image chrome is exactly the MEASURED props (D22)::accepts the six that feasibility m2 proved rule-ownable",
     "theme_package::themes.lintProperty: image chrome is exactly the MEASURED props (D22)::accepts the three rich-skinning-v2 probes proved, and not one more",
     "theme_package::themes.lintProperty: image chrome is exactly the MEASURED props (D22)::still rejects rect cropping, which rs-m4 measured as WRONG for sliced art",
     "theme_package::themes.define: the scrollbar slot cannot be nine-sliced (D19)::rejects the recipe, explaining that a canvas-space child scrolls away",
     "theme_package::themes.define: the scrollbar slot cannot be nine-sliced (D19)::still accepts a nine-sliced panel — only the scrollbar is impossible",
     "theme_snapshot::themes.neutral: Studio Neutral reproduces the pre-theme literals::reads its shared values from default_style rather than restating them",
     "theme_snapshot::themes.neutral: Studio Neutral reproduces the pre-theme literals::carries the semantic roles this stage added, at the values they replaced",
     "theme_snapshot::themes.neutral: Studio Neutral reproduces the pre-theme literals::every role carries the canonical font descriptor and the 1.2 line height",
     "theme_snapshot::themes.neutral: Studio Neutral reproduces the pre-theme literals::is frozen, shared and identity-stable (the env default must be immutable)",
     "theme_snapshot::themes.neutral: Studio Neutral reproduces the pre-theme literals::carries its package identity and stamp for inspection",
     "theme_snapshot::themes.resolve: composition happens exactly once, in the declared order::1. authored metrics are the base",
     "theme_snapshot::themes.resolve: composition happens exactly once, in the declared order::2. the ten-foot floor is RECORDED, never multiplied into the authored sizes",
     "theme_snapshot::themes.resolve: composition happens exactly once, in the declared order::3. preferred-text reservation inputs are carried through, not re-applied",
     "theme_snapshot::themes.resolve: composition happens exactly once, in the declared order::4. the accessibility floor clamps UP and never down",
     "theme_snapshot::themes.resolve: composition happens exactly once, in the declared order::5. explicit overrides apply last and are recorded as theme-independent",
     "theme_snapshot::themes.resolve: composition happens exactly once, in the declared order::an override cannot defeat the accessibility floor",
     "theme_snapshot::themes.resolve: composition happens exactly once, in the declared order::an override naming no metric is an error, not a silent no-op",
     "theme_snapshot::themes.resolve: composition happens exactly once, in the declared order::resolution is deterministic: the same inputs give equal values",
     "theme_snapshot::themes.resolve: composition happens exactly once, in the declared order::selects a named theme and rejects one the package does not declare",
     "theme_snapshot::themes.resolve: composition happens exactly once, in the declared order::a nineSlice recipe's contentInsets reach the snapshot's slot inset",
     "theme_snapshot::metric-name resolution::resolves spacing steps, dotted paths, and their negation",
     "theme_snapshot::metric-name resolution::resolveSize takes a typography role, a metric path, or a literal",
     "theme_snapshot::metric-name resolution::resolveSides and resolveDim keep their shape and pass numbers through",
     "theme_snapshot::metric-name resolution::names the canonical vocabulary for the schema's closed-set validation",
     "theme_snapshot::the themeMetrics environment fact::defaults to Studio Neutral and is a plain per-key signal",
     "theme_snapshot::the themeMetrics environment fact::a themeMetrics change notifies only its own subscribers",
     "theme_roles::role resolution: renderer layout-node construction::a typography role resolves to the same box a literal size does",
     "theme_roles::role resolution: renderer layout-node construction::a role also PAINTS its resolved size, so measure and paint agree",
     "theme_roles::role resolution: renderer layout-node construction::an ABSENT textSize paints the class's INTRINSIC role, so measure and paint agree",
     "theme_roles::role resolution: renderer layout-node construction::a package that moves the control role moves the DRAWN button label",
     "theme_roles::role resolution: renderer layout-node construction::spacing steps resolve for gap and padding",
     "theme_roles::role resolution: renderer layout-node construction::a metric path resolves inside a dimension, and its negation inside an offset",
     "theme_roles::role resolution: renderer layout-node construction::the solver's measure path resolves a role handed straight to it",
     "theme_roles::role resolution: renderer layout-node construction::the solver measures against the snapshot it is HANDED, not a global",
     "theme_roles::a themeMetrics commit RE-SOLVES a mounted screen — never a rebuild::new metrics move the geometry with zero factory reruns, creates or removes",
     "theme_roles::a themeMetrics commit RE-SOLVES a mounted screen — never a rebuild::the paint seam follows the same commit, so painted size tracks measured size",
     "theme_roles::a themeMetrics commit RE-SOLVES a mounted screen — never a rebuild::a themed target floor raises the enforced HIT rect (floors clamp up)",
     "theme_roles::closed-set validation for the semantic prop forms::an unknown typography role is a construction error naming the vocabulary",
     "theme_roles::closed-set validation for the semantic prop forms::an unknown spacing step on gap or padding is a construction error",
     "theme_roles::closed-set validation for the semantic prop forms::an unknown metric path inside a dimension or an offset is a construction error",
     "theme_roles::closed-set validation for the semantic prop forms::literal numbers stay legal everywhere — the documented theme-independent escape",
     "theme_roles::text_metrics: canonical font-descriptor keys::a short family name and its full descriptor are ONE key",
     "theme_roles::text_metrics: canonical font-descriptor keys::measured word widths are shared between the alias and the descriptor",
     "theme_roles::text_metrics: canonical font-descriptor keys::another WEIGHT of a known family is a different, uncalibrated descriptor",
     "theme_roles::text_metrics: canonical font-descriptor keys::lineHeight is per call and defaults to the historic 1.2",
     "theme_roles::text_metrics: canonical font-descriptor keys::a role's line height reaches the measurer through the layout node",
     "theme_roles::migrated controls: byte-parity under Studio Neutral::Slider: 44px track, 20px thumb, 6px rail, 16px label gap, 40px readout",
     "theme_roles::migrated controls: byte-parity under Studio Neutral::ProgressView: 6px track, 20px row floor, 14px label text, 8px row gap",
     "theme_roles::migrated controls: byte-parity under Studio Neutral::PopupButton: 40px trigger, and 36/44/56 rows per presentation",
     "theme_roles::migrated controls: byte-parity under Studio Neutral::PopupButton inline rows are 44 and sheet rows are 56",
     "theme_roles::migrated controls: byte-parity under Studio Neutral::TextInput: 44px field, 28px clear button inset 8px from the right edge",
     "theme_roles::migrated controls: byte-parity under Studio Neutral::Toggle: 28px minimum height and the 44+12+16 track reservation",
     "theme_roles::migrated controls: byte-parity under Studio Neutral::Stepper: square 44px buttons, 32px readout, 8px row gap, 16px label",
     "theme_roles::migrated controls: byte-parity under Studio Neutral::Chip, DisclosureGroup and Picker rows all sit on the 44px target floor",
     "theme_roles::migrated controls: byte-parity under Studio Neutral::Label: 20px icon, 6px gap, 16px title",
     "theme_roles::migrated controls: byte-parity under Studio Neutral::renderer defaults: Text 16, Button 18 with 12px padding, TextField 18",
     "theme_roles::migrated controls: byte-parity under Studio Neutral::AsyncImage: the default 48px square",
     "theme_roles::migrated controls: byte-parity under Studio Neutral::Table: 28px header, 15px cell text, 2px row gap, 8px cell inset",
     "theme_drift::no reusable framework control hardcodes a theme-owned metric::the framework surface is clean",
     "theme_drift::no reusable framework control hardcodes a theme-owned metric::it actually scanned the surface it claims to cover",
     "theme_drift::no reusable framework control hardcodes a theme-owned metric::catches a hardcoded dimension in a control",
     "theme_drift::no reusable framework control hardcodes a theme-owned metric::catches a hardcoded text size, gap, padding and named metric constant",
     "theme_drift::no reusable framework control hardcodes a theme-owned metric::catches drift in the renderer and the presenter too, not only in controls",
     "theme_drift::no reusable framework control hardcodes a theme-owned metric::zero is not a metric: 'this container adds no spacing' stays legal",
     "theme_drift::no reusable framework control hardcodes a theme-owned metric::a comment describing a literal is not a violation",
     "theme_drift::no reusable framework control hardcodes a theme-owned metric::every allowlist entry carries a real justification",
     "theme_drift::the teaching corpus spells the values the theme system already names::the corpus is clean",
     "theme_drift::the teaching corpus spells the values the theme system already names::it actually scanned the corpus it claims to cover",
     "theme_drift::the teaching corpus spells the values the theme system already names::catches a spacing literal a space step already names",
     "theme_drift::the teaching corpus spells the values the theme system already names::catches a text size a typography role already names, and names the role",
     "theme_drift::the teaching corpus spells the values the theme system already names::a value no name resolves to is NOT reported — there is nothing to rewrite it to",
     "theme_drift::the teaching corpus spells the values the theme system already names::zero stays legal in the corpus too: 'this container adds no spacing'",
     "theme_drift::the teaching corpus spells the values the theme system already names::a prop the theme system does not own is not this scope's business",
     "theme_drift::the teaching corpus spells the values the theme system already names::a THEME-OPT-OUT marker with a reason keeps the literal; one without does not",
     "theme_drift::the teaching corpus spells the values the theme system already names::a marker in the comment ABOVE excuses the next literal, and only that one",
     "theme_drift::the teaching corpus spells the values the theme system already names::a PROBE-EXEMPT region excuses what is inside it, and an unclosed one is itself a violation",
     "theme_drift::the teaching corpus spells the values the theme system already names::a PROBE_EXEMPT_FILES listing silences the file — and is refused when its header does not say so",
     "theme_drift::the teaching corpus spells the values the theme system already names::the shipped listing is guarded by real headers: every listed file says so in its own",
     "theme_drift::the teaching corpus spells the values the theme system already names::a finding in a file that predicts geometry from raw constants says so, and names them",
     "theme_drift::the teaching corpus spells the values the theme system already names::...and an UNCOUPLED file gets the plain message, so the distinction is real",
     "theme_drift::the teaching corpus spells the values the theme system already names::the coupled set is exactly these files — a new one is a decision, not a diff",
     "theme_drift::the teaching corpus spells the values the theme system already names::a metric assignment inside a LONG comment is not a violation",
     "theme_drift::the teaching corpus spells the values the theme system already names::the naming vocabulary is READ FROM THE FRAMEWORK, not restated here",
     "example_drift::no tutorial example bypasses property authority::the seven examples are clean",
     "example_drift::no tutorial example bypasses property authority::it actually scanned the examples, and saw real semantic values there",
     "example_drift::no tutorial example bypasses property authority::the allowlist is not a mute: every entry carries a reason",
     "example_drift::the example drift lint bites::R1: a px text size",
     "example_drift::the example drift lint bites::R1: a numeric padding and a numeric gap",
     "example_drift::the example drift lint bites::R1: a fixed-pixel dimension",
     "example_drift::the example drift lint bites::R1 does NOT fire on zero spacing, which is structure rather than style",
     "example_drift::the example drift lint bites::R2: an INVENTED surface token — the defect that made the word-game board invisible",
     "example_drift::the example drift lint bites::R2: a typography role that does not exist",
     "example_drift::the example drift lint bites::R2: a tint naming a palette role the framework does not resolve",
     "example_drift::the example drift lint bites::R2 does NOT fire on the legal vocabularies",
     "example_drift::the example drift lint bites::R3: a raw colour, in each of its three spellings",
     "example_drift::the example drift lint bites::R4: an engine or adapter reach-around",
     "example_drift::the example drift lint bites::R4: a platform branch is refused by name",
     "example_drift::the example drift lint bites::the allowlist suppresses ONLY its own line, in its own file",
     "scenario_require_paths::scenario_require_paths: every gallery/scenario/reference require resolves live::the shipped tree is clean",
     "scenario_require_paths::scenario_require_paths: every gallery/scenario/reference require resolves live::it actually scanned the real tree, not an empty one",
     "scenario_require_paths::scenario_require_paths: every gallery/scenario/reference require resolves live::catches the exact historical defect: a module-scope 3-level escape into src",
     "scenario_require_paths::scenario_require_paths: every gallery/scenario/reference require resolves live::does NOT fire on the same literal string used inside the SAFE dual-route idiom",
     "scenario_require_paths::scenario_require_paths: every gallery/scenario/reference require resolves live::a require with no relative path component is never flagged",
     "theme_controller::theme_controller.install: per-target, all-or-nothing::installs a package on a native target: sheet, one link, one metric commit",
     "theme_controller::theme_controller.install: per-target, all-or-nothing::the sheet name is per package and NEVER the library's own name",
     "theme_controller::theme_controller.install: per-target, all-or-nothing::package sheets carry NO reference mirrors — their attributes are the source",
     "theme_controller::theme_controller.install: per-target, all-or-nothing::a fallback target still installs: the whole metric half, no sheet",
     "theme_controller::theme_controller.install: capability errors leave everything untouched::a package requiring native paint on a fallback target names the capability",
     "theme_controller::theme_controller.install: capability errors leave everything untouched::a target that cannot expose its root fails, naming rootGui and themeRootGui",
     "theme_controller::theme_controller.install: capability errors leave everything untouched::an unknown required capability names the set this build knows",
     "theme_controller::theme_controller.install: capability errors leave everything untouched::a package built against another schema is refused before anything is touched",
     "theme_controller::theme_controller.install: capability errors leave everything untouched::install without an environment is refused: the snapshot has nowhere to ride",
     "theme_controller::the swap transaction: one solve, zero rebuilds::swap costs exactly one solve pass and zero factory reruns, creates or removes",
     "theme_controller::the swap transaction: one solve, zero rebuilds::swapPackage moves geometry in one solve, still without a rebuild",
     "theme_controller::the swap transaction: one solve, zero rebuilds::swapPackage lands on the NEW package's default when its theme names differ",
     "theme_controller::the swap transaction: one solve, zero rebuilds::swapping to an unknown theme names the declared themes and changes nothing",
     "theme_controller::the swap transaction: one solve, zero rebuilds::onChange reports every commit with its reason, and unsubscribes",
     "theme_controller::the swap transaction: one solve, zero rebuilds::uninstall restores the pre-install snapshot and link, and drops every connection",
     "theme_controller::live authoring: a supported token edit re-solves, an unknown one warns once::editing a metric token on the ACTIVE theme sheet re-resolves and re-solves",
     "theme_controller::live authoring: a supported token edit re-solves, an unknown one warns once::editing a FONT token re-keys measurement so the descriptor follows the theme",
     "theme_controller::live authoring: a supported token edit re-solves, an unknown one warns once::an unknown attribute is ignored with ONE warn naming the supported set",
     "theme_controller::live authoring: a supported token edit re-solves, an unknown one warns once::a Step 2 COLOUR token edit is silent — native paint owns it, not the solver",
     "theme_controller::live authoring: a supported token edit re-solves, an unknown one warns once::the watch follows the ACTIVE theme and never leaks a connection",
     "theme_controller::live authoring: a supported token edit re-solves, an unknown one warns once::a swapPackage moves the watch to the new package's sheet without leaking",
     "theme_controller::token_sync: typed records round-trip and drift is detectable::every supported typed kind survives canonicalization exactly (m6)",
     "theme_controller::token_sync: typed records round-trip and drift is detectable::a package's metrics round-trip through the generated Luau, value-exact",
     "theme_controller::token_sync: typed records round-trip and drift is detectable::the freshness gate fails on drift and names the token that drifted",
     "theme_controller::token_sync: typed records round-trip and drift is detectable::a marker-delimited package file keeps its hand-authored sections",
     "theme_controller::token_sync: typed records round-trip and drift is detectable::attribute names are engine-legal and reverse uniquely to their paths",
     "theme_controller::token_sync: typed records round-trip and drift is detectable::the client dump reads the ACTIVE theme sheet and is the shape the CLI consumes",
     "theme_controller::font preflight: conservative until calibrated, then ONE relayout::every declared descriptor is preflighted, pending until it measures",
     "theme_controller::font preflight: conservative until calibrated, then ONE relayout::an uncalibrated descriptor reserves MORE, never less — it can never clip",
     "theme_controller::font preflight: conservative until calibrated, then ONE relayout::a successful calibration drives EXACTLY ONE bounded relayout",
     "theme_controller::font preflight: conservative until calibrated, then ONE relayout::the solver measures with the role's DESCRIPTOR, so a theme weight is its own cache entry",
     "theme_controller::the decoration half rides the SAME transaction (A1)::install and each swapPackage push exactly one setThemePackage with the active package",
     "theme_controller::sheet overrides belong to the theme being resolved TO (A2)::swapping to Light does not apply Dark's authored edits",
     "theme_controller::swapPackage is all-or-nothing (A3)::an unknown theme on the new package leaves a fully working controller",
     "theme_controller::one live controller per environment (A4)::a second install on the same environment is refused, naming the conflict",
     "theme_controller::one live controller per environment (A4)::a legitimate uninstall frees the environment for the next controller",
     "theme_controller::one live controller per environment (A4)::uninstall does not clobber a themeMetrics value it no longer owns",
     "theme_controller::change listeners are observers, never drivers (A5)::a listener that unsubscribes itself does not skip the next listener",
     "theme_controller::change listeners are observers, never drivers (A5)::a throwing listener neither aborts the swap nor robs its siblings",
     "theme_controller::change listeners are observers, never drivers (A5)::a listener that re-enters swap hits a named error instead of a stack overflow",
     "theme_controller::install validates before it mutates (A6)::an opts.theme the package does not declare materializes nothing",
     "theme_controller::install validates before it mutates (A6)::a throwing calibrator leaves a live, working controller",
     "theme_controller::a disposed controller says so (A7)::inspect() reports disposal and dumpTokens/onChange refuse",
     "theme_controller::font preflight has a lifecycle (A8)::two rapid swapPackage calls launch ONE flight per descriptor",
     "theme_controller::font preflight has a lifecycle (A8)::a descriptor that came back unavailable is retried on the next swapPackage",
     "theme_controller::font preflight has a lifecycle (A8)::uninstall mid-flight writes nothing through the dead controller",
     "theme_controller::metric rule pushes happen on a token EDIT and nowhere else (A9)::a designer's rule edit survives install and every ordinary commit",
     "theme_controller::metric rule pushes happen on a token EDIT and nowhere else (A9)::a metric token edit DOES push, so paint follows the geometry",
     "theme_controller::metric rule pushes happen on a token EDIT and nowhere else (A9)::a FONT token edit pushes FontFace on every recorded rule and preflights the new face",
     "theme_controller::metric rule pushes happen on a token EDIT and nowhere else (A9)::a rule the designer renamed away warns ONCE instead of failing silently",
     "theme_controller::a sheet edit is sanitized before it reaches the resolver (A10)::a negative inset and a zero type size are DROPPED, naming attribute and reason",
     "theme_controller::a sheet edit is sanitized before it reaches the resolver (A10)::a NaN is refused and a good edit beside it still applies",
     "theme_controller::native transitions are provided only when opted into (A11)::a package requiring styleTransitions fails install without opts.transitions",
     "theme_controller::native transitions are provided only when opted into (A11)::...and installs when the target opts in",
     "theme_controller::the theme sheet follows EVERY root of the target (B12)::roots before install, after install and after a swap all point at the package sheet",
     "theme_controller::rung 2: a per-view gradient survives a theme-package swap::the child is materialized once, keeps the VIEW's values, and is not duplicated by a swap",
     "theme_controller::rung 2: a per-view gradient survives a theme-package swap::a package's own chromeGradient still paints everywhere ELSE — the view wins on ONE node",
     "theme_controller::the install-policy preload list is requested through the provider (A12)::requests each distinct content id exactly once, and never a lazy one",
     "theme_controller::the install-policy preload list is requested through the provider (A12)::a swap releases the old package's requests and takes the new one's",
     "theme_controller::the install-policy preload list is requested through the provider (A12)::with NO provider the step is lazily skipped and claims nothing",
     "theme_paint_repaint::a theme commit re-resolves the tint channel::repaints a STATIC tint that nothing will ever re-write",
     "theme_paint_repaint::a theme commit re-resolves the tint channel::repaints a tint that BLENDS from another role, base and target both",
     "theme_paint_repaint::a theme commit re-resolves the tint channel::leaves a `direct` tint alone — it named a colour, not a role",
     "theme_paint_repaint::a theme commit re-resolves a UI.Path's stroke role::paints the ring's accent arc and secondary track in the INSTALLED package",
     "theme_paint_repaint::a theme commit re-resolves a UI.Path's stroke role::paints a path that declares NO role in the installed package's content colour",
     "theme_paint_repaint::the live adapter carries the same commit (source contract)::setActiveTheme calls refreshThemedPaint beside the focus and toggle refreshes",
     "theme_paint_repaint::the live adapter carries the same commit (source contract)::refreshThemedPaint re-applies the path role AND the tint, tint last",
     "theme_paint_repaint::the live adapter carries the same commit (source contract)::a Path's stroke colour no longer comes from the construction-time palette",
     "theme_chrome::buildPackage: nine-slice chrome rules carry the package's own values::emits all six image properties on the slot tag, value-exact",
     "theme_chrome::buildPackage: nine-slice chrome rules carry the package's own values::modulates ONE base image per state instead of naming a second asset",
     "theme_chrome::buildPackage: nine-slice chrome rules carry the package's own values::the declared native fallback paints the slot when the asset fails",
     "theme_chrome::buildPackage: nine-slice chrome rules carry the package's own values::a flat package emits no chrome rules BUT the framework icon tints",
     "theme_chrome::buildPackage: gradients are phantom rules, not decoration children::emits a ::UIGradient rule per declared slot with per-theme tokens",
     "theme_chrome::buildPackage: gradients are phantom rules, not decoration children::every theme carries a token for every gradient slot, so a swap cannot blank a rule",
     "theme_chrome::buildPackage: the pseudo-selector allowlist::accepts the three proven phantom modifiers",
     "theme_chrome::buildPackage: the pseudo-selector allowlist::rejects ::UIShadow with a message naming the m7 evidence",
     "theme_chrome::buildPackage: the pseudo-selector allowlist::rejects any other pseudo selector, naming it",
     "theme_chrome::buildPackage: the pseudo-selector allowlist::an author-supplied rule goes through the same gate",
     "theme_chrome::buildPackage: cascade discipline::priority is index * 10 and the groups run base -> ... -> disabled",
     "theme_chrome::buildPackage: cascade discipline::chrome resting paint sits after the roles it must not lose to, and before the states",
     "theme_chrome::buildPackage: cascade discipline::the stamp moves with the package and is deterministic",
     "theme_chrome::buildPackage: cascade discipline::the legacy build() path is untouched by the package path",
     "theme_chrome::a fallback node stays hidden in EVERY state (RS-DIR6-F1)::the hide is re-asserted ABOVE the disabled dim, on the same property",
     "theme_chrome::a fallback node stays hidden in EVERY state (RS-DIR6-F1)::it carries the HIDE and nothing else — the fill and corner stay the chrome rule's",
     "theme_chrome::a fallback node stays hidden in EVERY state (RS-DIR6-F1)::every skinned slot gets it, and a flat package still emits none of it",
     "theme_chrome::chrome_props: the kinds the Step 2 vocabulary lacked::rects and sequences describe canonically and sort their stops",
     "theme_chrome::chrome_props: the kinds the Step 2 vocabulary lacked::rejects a degenerate slice centre rather than shipping a skin that is wrong at every other size",
     "theme_chrome::chrome_props: the kinds the Step 2 vocabulary lacked::materialize reports 'not handled' headlessly instead of crashing",
     "theme_chrome::chrome_slots: classification::maps the renderer's existing hints to the theme slot vocabulary",
     "theme_chrome::chrome_slots: classification::the backdrop, the scrim and chrome-less nodes get NO decoration",
     "theme_chrome::chrome_slots: classification::a badge classifies into the badge slot, and costs nothing until a package skins it",
     "theme_chrome::chrome_slots: classification::an explicit slot HINT beats every generic classification, and a bad one is an error",
     "theme_chrome::chrome_slots: classification::the hint round-trips through the blueprint's internal meta channel",
     "theme_chrome::chrome_slots: classification::a field stays a field even when it declares a selection binding",
     "theme_chrome::chrome_slots: the plan and the preload policy::a native recipe plans NOTHING; a nineSlice recipe plans exactly one image",
     "theme_chrome::chrome_slots: the plan and the preload policy::splits the preload set by declared policy",
     "theme_chrome::chrome_slots: the plan and the preload policy::a nineSlice recipe whose asset declares no sliceCenter is an authoring error",
     "theme_chrome::chrome_slots: the census and the fallback ledger::a flat theme costs zero decoration instances over the whole fixture",
     "theme_chrome::chrome_slots: the census and the fallback ledger::an ornate theme costs exactly one instance per slotted node, and only for slotted slots",
     "theme_chrome::chrome_slots: the census and the fallback ledger::the badge slot is reachable from the PUBLIC API and lands on the real fixture",
     "theme_chrome::chrome_slots: the census and the fallback ledger::the fallback tag flips once on failure and once on recovery, and never twice",
     "theme_chrome::chrome_slots: the census and the fallback ledger::a failure is per ASSET, so only the slots that use it fall back",
     "theme_chrome::chrome_slots: the census and the fallback ledger::the census counts creations and destructions, so a leak is visible",
     "theme_chrome::chrome_slots: the census and the fallback ledger::a palette-only swap reports NO chrome change, so no instance churns",
     "theme_chrome::chrome_slots: the census and the fallback ledger::swapping packages clears a stale failure ledger",
     "theme_chrome::decoration content insets reach the solver::a nineSlice recipe's insets ride the snapshot separately from the slot's own padding",
     "theme_chrome::decoration content insets reach the solver::a flat package reserves ZERO on every slot, so existing geometry is untouched",
     "theme_chrome::decoration content insets reach the solver::the renderer adds the chrome insets to a slotted node's padding, and nothing else",
     "theme_chrome::a LAYERED recipe's content insets reach the solver too::the snapshot publishes them for a layered slot, not only a nineSlice one",
     "theme_chrome::a LAYERED recipe's content insets reach the solver too::the measure seam and the PAINT seam read the same table",
     "theme_chrome::a LAYERED recipe's content insets reach the solver too::a content-sized button under the layered skin reserves the room its label is painted in",
     "theme_chrome::a LAYERED recipe's content insets reach the solver too::a flat package still reserves NOTHING, so flat geometry is untouched",
     "theme_chrome::theme chrome: metric-derived rule props are mapped for live repaint::buildPackage records every radii/stroke-derived rule prop",
     "theme_chrome::theme chrome: metric-derived rule props are mapped for live repaint::a flat package maps the same sites and nothing chrome-specific",
     "theme_chrome::the image IS the button: a skinned node's native plate is suppressed::emits fill/corner/stroke suppression per SKINNED slot, and only for skinned slots",
     "theme_chrome::the image IS the button: a skinned node's native plate is suppressed::a flat package suppresses only the VALUE slots, and nothing decorative",
     "theme_chrome::the image IS the button: a skinned node's native plate is suppressed::suppression beats every surface rule and loses to nothing that matters",
     "theme_chrome::the image IS the button: a skinned node's native plate is suppressed::the skinned tag round-trips through classifyTags, so the adapter's diff keeps it",
     "theme_chrome::the image IS the button: a skinned node's native plate is suppressed::the interaction tints are the only feedback left, so they are clearly separated",
     "theme_chrome::role tints paint the ART, not a slab behind it::every theme carries the role tint tokens, lerped from its own palette",
     "theme_chrome::role tints paint the ART, not a slab behind it::compiles a child-combinator tint rule per role on the control decoration",
     "theme_chrome::role tints paint the ART, not a slab behind it::the role tint owns the resting look and the role STATE beats the generic one",
     "theme_chrome::role tints paint the ART, not a slab behind it::the lifted label keeps its role TEXT colours (already shipped, not regressed)",
     "theme_chrome::role tints paint the ART, not a slab behind it::a flat package emits no role tint rules (there is no art to tint)",
     "theme_chrome::slider chrome: a rail and a thumb are their own slots::Fantasy Parchment resolves both, with the rail sliced and the thumb whole",
     "theme_chrome::slider chrome: a rail and a thumb are their own slots::a whole-image recipe compiles four image props and never a slice centre",
     "theme_chrome::slider chrome: a rail and a thumb are their own slots::a whole-image slot is the DEFAULT for a thumb and a seal, explicit for the rest",
     "theme_chrome::slider chrome: a rail and a thumb are their own slots::a flat theme leaves the slider exactly as it was: no plan, no ART rules",
     "theme_chrome::rung 2 of the ladder: a per-view image beats the theme for ONE node::a local override plans an UNTAGGED, whole-image decoration",
     "theme_chrome::rung 2 of the ladder: a per-view image beats the theme for ONE node::works on a FLAT theme too — the rung is independent of the package",
     "theme_chrome::rung 2 of the ladder: a per-view image beats the theme for ONE node::an absent or empty override falls straight back to the theme",
     "theme_chrome::rung 2 of the ladder: a per-view image beats the theme for ONE node::the overridden node keeps the theme's INSETS — only the image leaves",
     "theme_chrome::readable badges: the seal, the lift, and the inset the value sits in::Fantasy Parchment skins the badge as a whole image with a real inset",
     "theme_chrome::readable badges: the seal, the lift, and the inset the value sits in::`UI.Text{ surface = \"badge\" }` is the public authoring path, and it is NARROW",
     "theme_chrome::readable badges: the seal, the lift, and the inset the value sits in::a TextLabel is a lift class, which is what puts the count ABOVE the seal",
     "theme_chrome::readable badges: the seal, the lift, and the inset the value sits in::the badge's lifted label centres both axes; every other slot keeps its alignment",
     "theme_chrome::readable badges: the seal, the lift, and the inset the value sits in::the lift is INSET by the recipe's contentInsets, not full-bleed",
     "theme_chrome::readable badges: the seal, the lift, and the inset the value sits in::a zero-inset recipe still lifts full-bleed, byte-identical to the old behaviour",
     "theme_chrome::readable badges: the seal, the lift, and the inset the value sits in::a skinned badge gets the lifted-label rules; a flat one gets none",
     "theme_chrome::slot shadows: adapter-owned depth the sheet cannot express::`themes.define` validates a shadow against the bespoke contract",
     "theme_chrome::slot shadows: adapter-owned depth the sheet cannot express::the reference packages declare the shadows the director asked for",
     "theme_chrome::slot shadows: adapter-owned depth the sheet cannot express::a shadow rides a NATIVE recipe too, so a flat-but-deep theme costs no instances",
     "theme_chrome::slot shadows: adapter-owned depth the sheet cannot express::the census counts shadows, so a flat package's zero is a measurement",
     "theme_chrome::slot shadows: adapter-owned depth the sheet cannot express::a shadow change is part of the chrome signature, so the adapter re-materializes",
     "theme_chrome::slot shadows: adapter-owned depth the sheet cannot express::`::UIShadow` is STILL rejected as a rule — the shadow is adapter-owned on purpose",
     "theme_chrome::the new slots keep the contract backward-compatible::their insets are OPTIONAL and default to zero",
     "theme_chrome::the new slots keep the contract backward-compatible::the shipping package's chrome insets reach the solver for the new slots",
     "theme_chrome::the new slots keep the contract backward-compatible::an unknown slot is still rejected by name",
     "theme_chrome::the focus treatment: a themed ring, or a glow::defaults to the ring, so every package that says nothing is unchanged",
     "theme_chrome::the focus treatment: a themed ring, or a glow::the reference packages ask for the glows the director asked for",
     "theme_chrome::the focus treatment: a themed ring, or a glow::the glow parameters are a legal shadow at BOTH distance profiles",
     "theme_chrome::the focus treatment: a themed ring, or a glow::a colour token resolves per THEME, and a missing one is a compile error",
     "theme_chrome::the focus treatment: a themed ring, or a glow::rejects an unknown kind, an unknown field and an illegal shadow",
     "theme_chrome::the focus treatment: a themed ring, or a glow::a literal colour needs no theme, and an absent colour falls back to the accent",
     "theme_chrome::the focus treatment: a themed ring, or a glow::the census counts live glows, so a stuck one and a leaked one both show",
     "theme_chrome::the focus treatment: a themed ring, or a glow::focus is chrome vocabulary but NOT a decoration slot",
     "theme_chrome::a value control's chrome is SOLID, and its own::the thumb and the rail paint from the slot tag, opaquely",
     "theme_chrome::a value control's chrome is SOLID, and its own::colour comes from the SAME map the asset-failure fallback uses",
     "theme_chrome::a value control's chrome is SOLID, and its own::every package emits them — a flat theme's slider still has to be visible",
     "theme_chrome::a value control's chrome is SOLID, and its own::NO GRADIENT can reach them — not through a surface, not through a slot",
     "theme_chrome::a value control's chrome is SOLID, and its own::declaring a gradient on one is a COMPILE error, not a silently dropped rule",
     "theme_chrome::a value control's chrome is SOLID, and its own::the hint tag is emitted for the value family and for nothing else",
     "theme_chrome::a value control's chrome is SOLID, and its own::a skinned thumb still hides its own plate — paint first, then suppress",
     "theme_chrome::a value control's chrome is SOLID, and its own::the OTHER value nodes: badge solid, accent fill solid, toggle untagged",
     "theme_chrome::the toggle is palette-true, in every theme::the ON track is THAT theme's accent — never Studio Neutral's blue",
     "theme_chrome::the toggle is palette-true, in every theme::the OFF track is a RECESSED control surface, in light themes as well as dark",
     "theme_chrome::the toggle is palette-true, in every theme::knob vs track clears the 3:1 non-text floor in BOTH states, every theme",
     "theme_chrome::the toggle is palette-true, in every theme::derives the off track for a theme that never declared one",
     "theme_chrome::surface = plain opts out of decoration, not just of native paint::a plain Button classifies to NO slot, where a bare Button classifies to control",
     "theme_chrome::surface = plain opts out of decoration, not just of native paint::...and that holds for the classes that route by class, not by surface",
     "theme_chrome::surface = plain opts out of decoration, not just of native paint::a plain SELECTED button takes no selection card either",
     "theme_chrome::surface = plain opts out of decoration, not just of native paint::but a control's own HINT still wins — that is the control's considered decision",
     "theme_chrome::the scroll canvas reaches a theme's decoration bleed::a package's deepest shadow reach is published as ONE number",
     "theme_chrome::the scroll canvas reaches a theme's decoration bleed::a package with no depth at all publishes zero, so nothing without it moves",
     "theme_chrome::the scroll canvas reaches a theme's decoration bleed::the canvas a scrolling region declares grows by exactly that much",
     "theme_chrome::chrome full bleed: the plate covers the host's own box::pushes the padding back out on both axes",
     "theme_chrome::chrome full bleed: the plate covers the host's own box::is the IDENTITY when the host carries no padding, so nothing unpadded moves",
     "theme_chrome::chrome full bleed: the plate covers the host's own box::reproduces the reported row exactly: a 357px host lands its plate at the host's edge",
     "theme_chrome::chrome full bleed: the plate covers the host's own box::the adapter applies it on EVERY sync, not only at creation",
     "chrome_padding_refit::chrome full bleed: compensated for the padding the host ACTUALLY has::the measured Compact Pointer tray plate: a stale 12 against a live 8 paints [6..97] for a [10..93] plate",
     "chrome_padding_refit::chrome full bleed: compensated for the padding the host ACTUALLY has::...and compensating for the padding it carries is the identity: [10..93]",
     "chrome_padding_refit::chrome full bleed: compensated for the padding the host ACTUALLY has::an unpadded host is untouched, so nothing that was already flush moves",
     "chrome_padding_refit::chrome full bleed: compensated for the padding the host ACTUALLY has::screen_chrome exports refitFullBleed, and it covers the LAYER ladder as well as the single decoration",
     "chrome_padding_refit::chrome full bleed: compensated for the padding the host ACTUALLY has::the padding WRITE re-fits the bleed — the Button path",
     "chrome_padding_refit::chrome full bleed: compensated for the padding the host ACTUALLY has::...and the Text path, which writes a UIPadding on a node that can carry a decoration too",
     "screen_props_seam::the screen_props seam: READ == DECLARED == PASSED::every `ctx.<key>` the module reads is declared in `Ctx`",
     "screen_props_seam::the screen_props seam: READ == DECLARED == PASSED::every key `Ctx` declares is one the module actually reads",
     "screen_props_seam::the screen_props seam: READ == DECLARED == PASSED::the one construction site passes exactly the declared set — no more, no less",
     "screen_props_seam::the screen_props seam: READ == DECLARED == PASSED::there is exactly ONE construction site",
     "screen_props_seam::the screen_props seam: READ == DECLARED == PASSED::NEGATIVE CONTROL: each set comparison reports the pair that is out of step",
     "screen_props_seam::the screen_props seam: the (empty) shared WRITE surface, and the one-way require::setProp writes through NO ctx dep at all — the cost this extraction did not pay",
     "screen_props_seam::the screen_props seam: the (empty) shared WRITE surface, and the one-way require::NEGATIVE CONTROL: the write scan sees each of the three write shapes",
     "screen_props_seam::the screen_props seam: the (empty) shared WRITE surface, and the one-way require::the module offers exactly the one entry point it declares",
     "screen_props_seam::the screen_props seam: the (empty) shared WRITE surface, and the one-way require::the module requires the host NOWHERE — the require is one-way",
     "theme_layers::themes.define: a layered recipe compiles to inspectable layer data::accepts the whole R1 vocabulary in one stack",
     "theme_layers::themes.define: a layered recipe compiles to inspectable layer data::plans the stack with z-order = array order and one tag per layer",
     "theme_layers::themes.define: a layered recipe compiles to inspectable layer data::publishes the plaque overhang as reservation metadata, not a runtime surprise",
     "theme_layers::themes.define: a layered recipe compiles to inspectable layer data::derives that metadata from the LAYER BOXES, one kind at a time",
     "theme_layers::themes.define: a layered recipe compiles to inspectable layer data::RESERVES it: the snapshot publishes the plaque overhang as a chrome outset",
     "theme_layers::themes.define: a layered recipe compiles to inspectable layer data::a stack with no plaque, and every flat package, publishes NOTHING",
     "theme_layers::themes.define: a layered recipe compiles to inspectable layer data::EVERY censused extent is reserved: no slot may publish one without the other",
     "theme_layers::themes.define: a layered recipe compiles to inspectable layer data::carries every layer's assets into the preload seam",
     "theme_layers::themes.define: every layer rejection names the field, the rule and the fix::rejects an unknown layer kind, listing the legal set",
     "theme_layers::themes.define: every layer rejection names the field, the rule and the fix::rejects a geometry field borrowed from another kind",
     "theme_layers::themes.define: every layer rejection names the field, the rule and the fix::accepts `inset` on every full-bleed kind and rejects it on the rest",
     "theme_layers::themes.define: every layer rejection names the field, the rule and the fix::rejects a malformed inset: negative, infinite, non-table, wrong axis",
     "theme_layers::themes.define: every layer rejection names the field, the rule and the fix::the BOX math: symmetric per axis, and absent inset is byte-identical",
     "theme_layers::themes.define: every layer rejection names the field, the rule and the fix::no other reference package declares an inset — they are byte-unchanged",
     "theme_layers::themes.define: every layer rejection names the field, the rule and the fix::glossy-touch's stripe is MASKED to the trough, inset only off its lines",
     "theme_layers::themes.define: every layer rejection names the field, the rule and the fix::no other reference package declares a mask — one canvas in the library",
     "theme_layers::themes.define: every layer rejection names the field, the rule and the fix::accepts `mask` on every full-bleed kind and rejects it on the rest",
     "theme_layers::themes.define: every layer rejection names the field, the rule and the fix::the mask set is CLOSED to the shape rs-m9 measured",
     "theme_layers::themes.define: every layer rejection names the field, the rule and the fix::the plan carries the mask, and an undeclared one reads nil",
     "theme_layers::themes.define: every layer rejection names the field, the rule and the fix::rejects a stack past the cap, and an empty one, and one with a hole",
     "theme_layers::themes.define: every layer rejection names the field, the rule and the fix::rejects a sliced layer whose asset declares no slice centre",
     "theme_layers::themes.define: every layer rejection names the field, the rule and the fix::rejects an asset the package never declared, per state",
     "theme_layers::themes.define: every layer rejection names the field, the rule and the fix::rejects malformed per-kind geometry with the shape it wanted",
     "theme_layers::themes.define: every layer rejection names the field, the rule and the fix::rejects layers on a recipe that is not layered, and art on one that is",
     "theme_layers::themes.define: every layer rejection names the field, the rule and the fix::rejects a layered scrollbar for the canvas-space reason",
     "theme_layers::themes.define: every layer rejection names the field, the rule and the fix::rejects a layered barFill because the fill is clipped art, not a layer host",
     "theme_layers::themes.define: every layer rejection names the field, the rule and the fix::requires the declared native fallback, exactly as a nineSlice recipe does",
     "theme_layers::buildPackage: a layer stack compiles to one rule-owned paint rule per layer::carries the package's own values, in array order",
     "theme_layers::buildPackage: a layer stack compiles to one rule-owned paint rule per layer::gives a per-corner override its own tag so its three siblings are untouched",
     "theme_layers::buildPackage: a layer stack compiles to one rule-owned paint rule per layer::suppresses the native plate under the stack, exactly as under a nine-slice",
     "theme_layers::buildPackage: a layer stack compiles to one rule-owned paint rule per layer::tints and fails over per layer, and yields the whole stack for a field caret",
     "theme_layers::buildPackage: a layer stack compiles to one rule-owned paint rule per layer::a layered control still gets its semantic role tints, on every layer",
     "theme_layers::buildPackage: the layer census makes ornate cost a number::counts layers per KIND, the suppressions, and the overhang",
     "theme_layers::buildPackage: the layer census makes ornate cost a number::a flat package pays nothing at all",
     "theme_layers::buildPackage: the layer census makes ornate cost a number::the headless node census counts a stack per decorated node",
     "theme_layers::buildPackage: the layer census makes ornate cost a number::the per-target state counts layer instances per kind as they are created",
     "theme_layers::buildPackage: the layer census makes ornate cost a number::a broken layer fails the whole stack over, once",
     "theme_layers::the slot vocabulary is ONE list::themes.SLOTS and chrome_slots.SLOTS are the same list",
     "theme_layers::the slot vocabulary is ONE list::the v2 value-display and toggle slots are registered and classified",
     "theme_layers::the slot vocabulary is ONE list::the bar family owns solid native paint; the toggle keeps togglePalette",
     "theme_layers::the slot vocabulary is ONE list::no wash can reach a value control's chrome, one control down",
     "theme_layers::a clipping scroll container reserves the package's shadow bleed::the deepest control's glow lands INSIDE the clipper, in every shadowed package",
     "theme_layers::a clipping scroll container reserves the package's shadow bleed::a package with no shadow pays nothing, so a flat theme cannot move",
     "theme_layers::a clipping scroll container reserves the package's shadow bleed::nesting does NOT accumulate: reach is measured from each node's own box",
     "theme_layers::a clipping scroll container reserves the package's shadow bleed::the FIRST child's glow lands INSIDE the clipper's own leading edge too, in every shadowed package",
     "theme_layers::a clipping scroll container reserves the package's shadow bleed::...and a package with no shadow still pays nothing on the leading edge",
     "theme_layers::a clipping scroll container reserves the package's shadow bleed::a box too small for the bleed gives the reservation back instead of collapsing",
     "theme_variants::themes.normalizeVariant: one grammar for both rungs::a bare string and a map come back in the SAME shape",
     "theme_variants::themes.normalizeVariant: one grammar for both rungs::is the SAME function a rung-2 view prop calls, on a raw content URI",
     "theme_variants::themes.normalizeVariant: one grammar for both rungs::unstated states fall back to default, which is what lets an author ship less art",
     "theme_variants::themes.normalizeVariant: one grammar for both rungs::rejects an unknown state key, naming the legal set",
     "theme_variants::themes.normalizeVariant: one grammar for both rungs::requires `default` in map form",
     "theme_variants::themes.normalizeVariant: one grammar for both rungs::rejects a malformed reference and a malformed state entry",
     "theme_variants::themes.normalizeVariant: a state change may never RELAYOUT the tree::accepts insets that are identical across every declared state",
     "theme_variants::themes.normalizeVariant: a state change may never RELAYOUT the tree::rejects a per-axis difference, naming the slot, the state and the axis",
     "theme_variants::themes.normalizeVariant: a state change may never RELAYOUT the tree::rejects insets declared on ONE state only — the same reservation change",
     "theme_variants::themes.normalizeVariant: a state change may never RELAYOUT the tree::rejects a negative or non-table inset before it can corrupt a solve",
     "theme_variants::themes.define: a recipe takes the same grammar::compiles a per-state recipe and normalizes it to one canonical map",
     "theme_variants::themes.define: a recipe takes the same grammar::a bare string carries NO canonical sibling, so nothing about it moves",
     "theme_variants::themes.define: a recipe takes the same grammar::rejects a state naming art the package never declared, naming the state",
     "theme_variants::themes.define: a recipe takes the same grammar::every state's art joins the preload seam, by its own policy",
     "theme_variants::themes.define: a recipe takes the same grammar::a derived package inherits the states, and an override cannot leave a stale one",
     "theme_variants::themes.define: a recipe takes the same grammar::a layer takes the same grammar as a recipe",
     "theme_variants::buildPackage: a state map becomes GuiState-pseudo and tag-family rules::emits one Image rule per declared state, read from the PARENT's state",
     "theme_variants::buildPackage: a state map becomes GuiState-pseudo and tag-family rules::the per-state TINT rules still apply on top of the swapped art",
     "theme_variants::buildPackage: a state map becomes GuiState-pseudo and tag-family rules::a supplied `disabled` art does NOT remove the disabled treatment",
     "theme_variants::buildPackage: a state map becomes GuiState-pseudo and tag-family rules::the `error` state keys on a tag, because the engine has no error GuiState",
     "theme_variants::buildPackage: a state map becomes GuiState-pseudo and tag-family rules::counts the extra art it costs, and a bare-string package costs nothing",
     "theme_variants::buildPackage: a state map becomes GuiState-pseudo and tag-family rules::a bare-string package compiles to exactly what it did before variants existed",
     "theme_icons::themes.define: the icon map is validated like every other section::compiles a map of semantic names, variants included",
     "theme_icons::themes.define: the icon map is validated like every other section::a package with no icons carries no icons section, and stamps as it always did",
     "theme_icons::themes.define: the icon map is validated like every other section::rejects a name that is neither a framework icon nor namespaced",
     "theme_icons::themes.define: the icon map is validated like every other section::accepts a namespaced name, which is how a custom control contributes one",
     "theme_icons::themes.define: the icon map is validated like every other section::rejects art the package never declared, and a malformed reference",
     "theme_icons::themes.define: the icon map is validated like every other section::define is the ONLY icon wall: a map with no `default` is a compile error",
     "theme_icons::themes.define: the icon map is validated like every other section::resolveIcon is a LOOKUP: the compiled sibling carries the resolved art",
     "theme_icons::themes.define: the icon map is validated like every other section::rejects an unknown iconSizes role and a non-positive size",
     "theme_icons::themes.define: the icon map is validated like every other section::icon art joins the preload seam by its own policy",
     "theme_icons::the fallback glyph table can never be tofu::every glyph is plain ASCII, and the check that says so can fail",
     "theme_icons::the fallback glyph table can never be tofu::a private-use codepoint FAILS the check — the sci-fi caret defect",
     "theme_icons::themes.resolveIcon: three honest answers::the theme's art when it has some, per state",
     "theme_icons::themes.resolveIcon: three honest answers::the FRAMEWORK's own art when the theme declares none",
     "theme_icons::themes.resolveIcon: three honest answers::the ASCII glyph when a package DECLINES the framework set — never tofu",
     "theme_icons::themes.resolveIcon: three honest answers::both PAINT PATHS answer for the same tint roles — one word, two subsystems",
     "theme_icons::themes.resolveIcon: three honest answers::nothing at all for a name that means nothing, at every rung",
     "theme_pixel::themes.define: the pixel declaration::compiles rendering + an integer unit, and reports them",
     "theme_pixel::themes.define: the pixel declaration::a package that says nothing is standard, and has no unit at all",
     "theme_pixel::themes.define: the pixel declaration::rejects an unknown rendering mode, naming the modes",
     "theme_pixel::themes.define: the pixel declaration::rejects a pixel package with no unit, and a fractional or zero one",
     "theme_pixel::themes.define: the pixel declaration::rejects a unit on a package that is not pixel — it would do nothing",
     "theme_pixel::themes.define: the pixel declaration::locks SliceScale to integers, naming the slot that uses the art",
     "theme_pixel::themes.define: the pixel declaration::requires slice BORDERS on the pixel grid",
     "theme_pixel::themes.snapToPixelUnit: floors clamp UP::snaps up to the next multiple and leaves an exact one alone",
     "theme_pixel::themes.snapToPixelUnit: floors clamp UP::is idempotent, monotone, and never returns less than it was given",
     "theme_pixel::themes.snapToPixelUnit: floors clamp UP::a unit of 1 is the identity on integers",
     "theme_pixel::themes.snapToPixelUnit: floors clamp UP::float noise cannot bump an exact multiple a whole unit",
     "theme_pixel::themes.snapToPixelUnit: floors clamp UP::refuses a unit that is not an integer >= 1",
     "theme_pixel::buildPackage: the Pixelated stamp is a counted claim::every image-bearing rule carries it, and the census proves it",
     "theme_pixel::buildPackage: the Pixelated stamp is a counted claim::the per-state art rules carry it too — a hover swap must not go soft",
     "theme_pixel::buildPackage: the Pixelated stamp is a counted claim::a standard package carries NO ResampleMode anywhere, and counts zero",
     "theme_pixel::buildPackage: the Pixelated stamp is a counted claim::a pixel LAYER stack is stamped on every layer",
     "theme_assets::themes.define: `content` is canonical and `contentId` is the alias::both spell the same package — same data, same stamp",
     "theme_assets::themes.define: `content` is canonical and `contentId` is the alias::the exported data stays a plain string, so a package stays serializable",
     "theme_assets::themes.define: `content` is canonical and `contentId` is the alias::rejects declaring both, because that is two sources for one string",
     "theme_assets::themes.define: `content` is canonical and `contentId` is the alias::rejects an asset with neither",
     "theme_assets::sheet_model: object-sourced Content is a COMPILE error, not a silent blank::refuses a Content-shaped value on Image or ImageContent, citing the trap",
     "theme_assets::sheet_model: object-sourced Content is a COMPILE error, not a silent blank::an author-supplied rule goes through the same gate",
     "theme_assets::sheet_model: object-sourced Content is a COMPILE error, not a silent blank::a plain string passes, and every other property is left alone",
     "theme_assets::themes.define: a slice centre that paints a silent blank middle::accepts art whose borders leave a real centre",
     "theme_assets::themes.define: a slice centre that paints a silent blank middle::rejects 2*border == the dimension, which is an EMPTY centre",
     "theme_assets::themes.define: a slice centre that paints a silent blank middle::rejects borders that sum past the source even when the rect is not inverted",
     "theme_assets::themes.define: a slice centre that paints a silent blank middle::rejects a centre that falls outside the declared source",
     "theme_assets::themes.define: a slice centre that paints a silent blank middle::rejects an inverted or malformed centre WITHOUT a declared source size",
     "theme_assets::themes.define: a slice centre that paints a silent blank middle::rejects a non-positive declared source size",
     "theme_assets::themes.define: a slice centre that paints a silent blank middle::art with no declared size still compiles — the sizes are optional",
     "theme_layer_application::chrome_slots.layerBoxes: rs-m3's idiom, expressed as numbers::a fill, a frame and a tile are one full-bleed child each",
     "theme_layer_application::chrome_slots.layerBoxes: rs-m3's idiom, expressed as numbers::a corners layer is FOUR fixed-px ornaments, anchored at the four corners",
     "theme_layer_application::chrome_slots.layerBoxes: rs-m3's idiom, expressed as numbers::only a corner with its OWN art carries the second tag",
     "theme_layer_application::chrome_slots.layerBoxes: rs-m3's idiom, expressed as numbers::an edges layer is one rail per DECLARED side, in canonical order",
     "theme_layer_application::chrome_slots.layerBoxes: rs-m3's idiom, expressed as numbers::a left/right rail stretches on the OTHER axis",
     "theme_layer_application::chrome_slots.layerBoxes: rs-m3's idiom, expressed as numbers::a plaque overhangs its edge by the DECLARED pixels",
     "theme_layer_application::chrome_slots.layerBoxes: rs-m3's idiom, expressed as numbers::a plaque's TEXT sub-slot is the plaque rect minus its declared insets",
     "theme_layer_application::chrome_slots.layerBoxes: rs-m3's idiom, expressed as numbers::layers and INSTANCES are different numbers, and both are published",
     "theme_layer_application::chrome_slots.layerBoxes: rs-m3's idiom, expressed as numbers::the text lift rides above the WHOLE ladder",
     "theme_layer_application::the ladder is a BACKDROP: every layer sits below the node's own content::the band is <= 0, and the topmost layer lands where a single decoration does",
     "theme_layer_application::the ladder is a BACKDROP: every layer sits below the node's own content::no layer of any depth can reach the CONTENT band, which starts at 1",
     "theme_layer_application::the ladder is a BACKDROP: every layer sits below the node's own content::the ladder is strictly ordered, so no two layers share a slot",
     "theme_layer_application::the ladder is a BACKDROP: every layer sits below the node's own content::...and the lift still clears the whole ladder AND the content band",
     "theme_layer_application::screen_target: the layer ladder is materialized as the plan describes::one ImageLabel per BOX, ZIndex = array order, Active=false",
     "theme_layer_application::screen_target: the layer ladder is materialized as the plan describes::layers are DIRECT children of the decorated node (the child combinator)",
     "theme_layer_application::screen_target: the layer ladder is materialized as the plan describes::the tag is added BEFORE the instance joins the tree",
     "theme_layer_application::screen_target: the layer ladder is materialized as the plan describes::a masked layer wraps its art in an adapter-owned, UNTAGGED CanvasGroup",
     "theme_layer_application::screen_target: the layer ladder is materialized as the plan describes::the wrapper is TRANSPARENT by class rule, not by an adapter write",
     "theme_layer_application::screen_target: the layer ladder is materialized as the plan describes::the wrapper falls with its layer, census included",
     "theme_layer_application::screen_target: the layer ladder is materialized as the plan describes::the focus ring hugs the WRAPPER when the plate layer is masked",
     "theme_layer_application::screen_target: the layer ladder is materialized as the plan describes::NOT ONE paint property is written in native mode",
     "theme_layer_application::screen_target: the layer ladder is materialized as the plan describes::the per-corner tag lands only on the corner that declared art",
     "theme_layer_application::screen_target: the layer ladder is materialized as the plan describes::one asset-failure grace timer per LAYER, not per ornament",
     "theme_layer_application::screen_target: the layer ladder is materialized as the plan describes::every layer instance is addressable by path, so GetStyled can read it",
     "theme_layer_application::screen_target: the layer ladder is materialized as the plan describes::the census counts LAYERS, and the DataModel count is taken independently",
     "theme_layer_application::screen_target: the layer ladder is materialized as the plan describes::the LIFTED LABEL falls with the ladder (live-found orphan)",
     "theme_layer_application::screen_target: the layer ladder is materialized as the plan describes::the ladder falls with the node, in BOTH teardown paths",
     "theme_layer_application::screen_target: the stack branch keeps the fix round's ORDER::a hinted node's own identity still lands before any return",
     "theme_layer_application::screen_target: the stack branch keeps the fix round's ORDER::it paints BEFORE it suppresses, exactly as the nine-slice branch does",
     "theme_layer_application::screen_target: the stack branch keeps the fix round's ORDER::the skinned tag is synced AFTER the ladder exists, never before",
     "theme_layer_application::screen_target: the stack branch keeps the fix round's ORDER::a RUNG-2 override beats the ladder (the standing opt-out rule)",
     "theme_layer_application::screen_target: the stack branch keeps the fix round's ORDER::a changed ladder shape REBUILDS, because the tags are what rules key on",
     "theme_layer_application::the decorated node's tags survive the syncTags diff::the stack yield rides it (it is on the NODE, not the decoration)",
     "theme_layer_application::the decorated node's tags survive the syncTags diff::the error state rides it",
     "theme_layer_application::the decorated node's tags survive the syncTags diff::classifyTags is the ONE builder, and it emits both",
     "theme_layer_application::the decorated node's tags survive the syncTags diff::the emitted yield rule is a child combinator off that exact tag",
     "theme_layer_application::the decorated node's tags survive the syncTags diff::a TextBox over a STACK flips the tag; over one decoration it tags the child",
     "theme_layer_application::suppression (R9) reaches a layered node through the SAME machinery::a layered slot emits the three suppression rules a nine-sliced one does",
     "theme_layer_application::suppression (R9) reaches a layered node through the SAME machinery::the tag those rules select on is the one the stack branch sets",
     "theme_layer_application::suppression (R9) reaches a layered node through the SAME machinery::and it is removed the moment the ladder goes",
     "theme_layer_application::suppression (R9) reaches a layered node through the SAME machinery::every LAYER's fallback hide beats the disabled dim too",
     "theme_layer_application::the whole stack fails and recovers as a UNIT::one broken layer puts the slot in fallback",
     "theme_layer_application::the whole stack fails and recovers as a UNIT::the adapter flips the tags on EVERY layer instance, PER LAYER",
     "theme_layer_application::the whole stack fails and recovers as a UNIT::a fallback stack cannot flag its OWN siblings as failed (live-found)",
     "theme_layer_application::the whole stack fails and recovers as a UNIT::a layered handle — and a BAR — is re-synced when an asset's state changes",
     "theme_layer_application::rung 2: one grammar, one normalizer, both rungs::a bare string and a per-state map both plan an untagged local decoration",
     "theme_layer_application::rung 2: one grammar, one normalizer, both rungs::the precedence the adapter picks with IS the emitted cascade",
     "theme_layer_application::rung 2: one grammar, one normalizer, both rungs::unstated states fall back to default, and disabled beats pressed",
     "theme_layer_application::rung 2: one grammar, one normalizer, both rungs::a bad rung-2 map is an authoring error naming the prop",
     "theme_layer_application::rung 2: the per-state map REACHES the adapter::blueprint -> mount -> renderer -> adapter.create carries the normalized map",
     "theme_layer_application::rung 2: the per-state map REACHES the adapter::a slider with no override stays on the theme",
     "theme_layer_application::rung 2: the per-state map REACHES the adapter::the adapter turns that hint into an override plan, not a theme plan",
     "theme_layer_application::rung 2: the per-state map REACHES the adapter::the adapter reads the hint's map, watches state, and writes the art",
     "theme_layer_application::rung 2: the per-state map REACHES the adapter::the rung-2 disabled floor is the SAME number rung 1's rule carries",
     "theme_layer_application::rung 2: the per-state map REACHES the adapter::the watch is disconnected with the node, so nothing outlives it",
     "theme_layer_application::focus hugs the art, not the padded rect::the plate of a stack is its back-most FULL-BLEED layer, never an ornament",
     "theme_layer_application::focus hugs the art, not the padded rect::the adapter parents the ring and the glow to the ART host, not the node",
     "theme_layer_application::focus hugs the art, not the padded rect::hit geometry is NOT the ring's instance, so the 47px floor is untouched",
     "focus_ring_float_geometry::screen_target.setFocusVisual: the float ring's geometry (device fix round)::found the function body (the anchor still names a real member)",
     "focus_ring_float_geometry::screen_target.setFocusVisual: the float ring's geometry (device fix round)::(a) computes the lift scale before the float is sized, not after",
     "focus_ring_float_geometry::screen_target.setFocusVisual: the float ring's geometry (device fix round)::(a) the float's Size is the rect scaled by the lift factor on both axes",
     "focus_ring_float_geometry::screen_target.setFocusVisual: the float ring's geometry (device fix round)::(a) the float's Position stays UNSCALED (the lift anchors top-left)",
     "focus_ring_float_geometry::screen_target.setFocusVisual: the float ring's geometry (device fix round)::(b) falls back to a resolved radius when no literal UICorner clone exists",
     "focus_ring_float_geometry::screen_target.setFocusVisual: the float ring's geometry (device fix round)::(b) the fallback mirrors applySurface's bespoke role -> radius mapping",
     "focus_ring_float_geometry::screen_target.setFocusVisual: the float ring's geometry (device fix round)::(b) the fallback corner is parented on the float, not on the styled host",
     "focus_ring_float_geometry::screen_target.setFocusVisual: the float ring's geometry (device fix round)::(c1) the corner sync runs after float creation, and only once per call",
     "focus_ring_float_geometry::screen_target.setFocusVisual: the float ring's geometry (device fix round)::(c1) an existing float's corner is UPDATED in place, never duplicated",
     "focus_ring_float_geometry::screen_target.setFocusVisual: the float ring's geometry (device fix round)::(c2) the live package's radii win over the frozen construction-time style",
     "activate_handler_reconnect::screen_pointer.setActivateHandler: no leaked connection on a recycled instance::found the function body (the anchor still names a real member)",
     "activate_handler_reconnect::screen_pointer.setActivateHandler: no leaked connection on a recycled instance::delegates activatedConn to the shared reconnect helper, not a raw assignment",
     "activate_handler_reconnect::screen_pointer.setActivateHandler: no leaked connection on a recycled instance::the connect call lives inside the reconnect factory, after the delegation",
     "conn_reconnect_sweep::handle_conn.reconnect: disconnects any prior connection before making a fresh one::a first call with no prior field just takes the factory's connection",
     "conn_reconnect_sweep::handle_conn.reconnect: disconnects any prior connection before making a fresh one::a second call disconnects the PRIOR connection before installing the fresh one",
     "conn_reconnect_sweep::handle_conn.reconnect: disconnects any prior connection before making a fresh one::a nil answer from factory() clears the field (and still disconnects the prior one)",
     "conn_reconnect_sweep::handle_conn.reconnectOnChange: no-ops on an unchanged identity, rewires on a changed one::a first call with no prior field connects and stamps the identity",
     "conn_reconnect_sweep::handle_conn.reconnectOnChange: no-ops on an unchanged identity, rewires on a changed one::an unchanged identity is a total no-op, even if factory() would answer something new",
     "conn_reconnect_sweep::handle_conn.reconnectOnChange: no-ops on an unchanged identity, rewires on a changed one::a changed identity disconnects the prior connection and installs a genuinely fresh one",
     "conn_reconnect_sweep::handle_conn.reconnectOnChange: no-ops on an unchanged identity, rewires on a changed one::retries on every call while the field stays nil (a track/target not found yet), regardless of identity",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::activatedConn (screen_pointer.luau): wired through reconnect",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::activatedConn (screen_pointer.luau): no raw `handle.activatedConn = ` assignment survives beside the helper",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::pointerBeganConn (screen_pointer.luau): wired through reconnect",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::pointerBeganConn (screen_pointer.luau): no raw `handle.pointerBeganConn = ` assignment survives beside the helper",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::hoverEnterConn (screen_pointer.luau): wired through reconnect",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::hoverEnterConn (screen_pointer.luau): no raw `handle.hoverEnterConn = ` assignment survives beside the helper",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::hoverLeaveConn (screen_pointer.luau): wired through reconnect",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::hoverLeaveConn (screen_pointer.luau): no raw `handle.hoverLeaveConn = ` assignment survives beside the helper",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::scrollConn (screen_pointer.luau): wired through reconnect",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::scrollConn (screen_pointer.luau): no raw `handle.scrollConn = ` assignment survives beside the helper",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::hitExpanderPointerConn (screen_pointer.luau): wired through reconnectOnChange",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::hitExpanderPointerConn (screen_pointer.luau): no raw `handle.hitExpanderPointerConn = ` assignment survives beside the helper",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::barConn (screen_chrome.luau): wired through reconnectOnChange",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::barConn (screen_chrome.luau): no raw `handle.barConn = ` assignment survives beside the helper",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::barWindowConn (screen_chrome.luau): wired through reconnect",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::barWindowConn (screen_chrome.luau): no raw `handle.barWindowConn = ` assignment survives beside the helper",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::hoverStateConn (screen_target.luau): wired through reconnectOnChange",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::hoverStateConn (screen_target.luau): no raw `handle.hoverStateConn = ` assignment survives beside the helper",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::discloseEnterConn (screen_target.luau): wired through reconnectOnChange",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::discloseEnterConn (screen_target.luau): no raw `handle.discloseEnterConn = ` assignment survives beside the helper",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::discloseLeaveConn (screen_target.luau): wired through reconnectOnChange",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::discloseLeaveConn (screen_target.luau): no raw `handle.discloseLeaveConn = ` assignment survives beside the helper",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::discloseTouchConn (screen_target.luau): wired through reconnectOnChange",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::discloseTouchConn (screen_target.luau): no raw `handle.discloseTouchConn = ` assignment survives beside the helper",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::textChangedConn (screen_target.luau): wired through reconnect",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::textChangedConn (screen_target.luau): no raw `handle.textChangedConn = ` assignment survives beside the helper",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::focusedConn (screen_target.luau): wired through reconnect",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::focusedConn (screen_target.luau): no raw `handle.focusedConn = ` assignment survives beside the helper",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::focusLostConn (screen_target.luau): wired through reconnect",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::focusLostConn (screen_target.luau): no raw `handle.focusLostConn = ` assignment survives beside the helper",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::oskReturnConn (screen_target.luau): wired through reconnect",
     "conn_reconnect_sweep::CENSUS_SLOTS completeness: every Finding-1/4-named *Conn field is swept::oskReturnConn (screen_target.luau): no raw `handle.oskReturnConn = ` assignment survives beside the helper",
     "conn_reconnect_sweep::chromeTextConns: rebuilds (not merely reconnects) on a recycle, keyed on handle.path::syncChromeText forces a destroy+rebuild when chromeTextPath no longer matches handle.path",
     "conn_reconnect_sweep::chromeTextConns: rebuilds (not merely reconnects) on a recycle, keyed on handle.path::the identity is stamped when the label is (re)built, and cleared when it is destroyed",
     "conn_reconnect_sweep::chromeOverrideConn: argued safe, not swept (Finding 1 named it; investigation found it already correct)::still disconnects before nil-ing when its plan clears (unchanged by this round)",
     "conn_reconnect_sweep::chromeOverrideConn: argued safe, not swept (Finding 1 named it; investigation found it already correct)::still connects only once per identity via its own `== nil` guard (unchanged by this round: its callback re-reads handle.chromeOverrideVariants/-Rest at FIRE time rather than closing over a per-life value, so a never-rewired connection cannot answer with stale data — see task-conn-report.md)",
     "conn_reconnect_sweep::screen_target: hitExpanderPointerConn's SECOND writer (Finding 4 residual)::adapter.adopt resets handle.pointer to nil on every recycle",
     "conn_reconnect_sweep::screen_target: hitExpanderPointerConn's SECOND writer (Finding 4 residual)::setHitRect's wiring runs OUTSIDE the `expander == nil` block (the block closes right after mirrorToHitExpander)",
     "conn_reconnect_sweep::screen_target: hitExpanderPointerConn's SECOND writer (Finding 4 residual)::no raw `== nil` guard survives at this site (the exact pre-fix pattern the re-review quoted)",
     "conn_reconnect_sweep::screen_target: hitExpanderPointerConn's SECOND writer (Finding 4 residual)::a nil handle.pointer disconnects any stale connection rather than leaving it wired",
     "conn_reconnect_sweep::screen_target.setFocusVisual: the ten-foot lift also respects the nearest scroll/clip host's window (item 5)::found the function",
     "conn_reconnect_sweep::screen_target.setFocusVisual: the ten-foot lift also respects the nearest scroll/clip host's window (item 5)::the host-window cap runs AFTER (and narrows) the root-frame lift, not instead of it",
     "conn_reconnect_sweep::screen_target.setFocusVisual: the ten-foot lift also respects the nearest scroll/clip host's window (item 5)::only applies when `lift` is already true (AND, not OR — never widens a refusal into an allowance)",
     "conn_reconnect_sweep::screen_target.setFocusVisual: the ten-foot lift also respects the nearest scroll/clip host's window (item 5)::reads AbsoluteWindowSize for a ScrollingFrame host, AbsoluteSize otherwise",
     "theme_value_displays::chrome_slots: the window clip, expressed as numbers::the art inside the window is the WHOLE track on the driven axis",
     "theme_value_displays::chrome_slots: the window clip, expressed as numbers::direction decides the axis and the pinned edge, and nothing else",
     "theme_value_displays::chrome_slots: the window clip, expressed as numbers::an end cap is CENTRED on the track's edge and survives every percent",
     "theme_value_displays::chrome_slots: the window clip, expressed as numbers::'start' follows the fill's origin when the direction flips",
     "theme_value_displays::chrome_slots: the window clip, expressed as numbers::the centrepiece is dead centre on both axes, direction-independently",
     "theme_value_displays::chrome_slots: the window clip, expressed as numbers::a per-end cap asset resolves, and both ends share one when it does not",
     "theme_value_displays::chrome_slots: the window clip, expressed as numbers::the declared direction reaches the plan, and an absent one is ltr",
     "theme_value_displays::themes.define: the bar family's declared geometry::an unknown fill direction names the vocabulary",
     "theme_value_displays::themes.define: the bar family's declared geometry::a field is legal only on the slot that can act on it",
     "theme_value_displays::themes.define: the bar family's declared geometry::an ImageRect crop is REFUSED, citing the probe that rejected it",
     "theme_value_displays::themes.define: the bar family's declared geometry::an ornament with no declared px box is an authoring error",
     "theme_value_displays::themes.define: the bar family's declared geometry::a per-end cap naming art the package does not declare is refused",
     "theme_value_displays::the bar's ornaments reserve the space they paint in::chrome_slots.barReservation derives the overhang from the declared boxes",
     "theme_value_displays::the bar's ornaments reserve the space they paint in::a vertical fill swaps which axis the caps overhang",
     "theme_value_displays::the bar's ornaments reserve the space they paint in::a package with no ornaments reserves NOTHING, and neither does a flat one",
     "theme_value_displays::the bar's ornaments reserve the space they paint in::the snapshot publishes it against the track's RESOLVED cross extent",
     "theme_value_displays::the bar's ornaments reserve the space they paint in::the SOLVER clears the label, the readout and the rows above and below",
     "theme_value_displays::insets that leave no content box are REFUSED or REPORTED::themes.define rejects insets that eat the track height the package resolves",
     "theme_value_displays::insets that leave no content box are REFUSED or REPORTED::it measures against the PIXEL-SNAPPED metric, like the reservation does",
     "theme_value_displays::insets that leave no content box are REFUSED or REPORTED::the SOLVER reports an inset the track could not afford, and the fill still draws",
     "theme_value_displays::sheet_model.buildPackage: the value displays' rules::the bar's four slots each get their resting art rule",
     "theme_value_displays::sheet_model.buildPackage: the value displays' rules::an end that named its OWN art gets a second, later-winning rule",
     "theme_value_displays::sheet_model.buildPackage: the value displays' rules::a flat bar keeps SOLID native paint through the facet-slot-* family",
     "theme_value_displays::sheet_model.buildPackage: the value displays' rules::the image is the element: a skinned bar's own plate is suppressed",
     "theme_value_displays::sheet_model.buildPackage: the value displays' rules::EVERY package suppresses the four value slots, flat ones included",
     "theme_value_displays::sheet_model.buildPackage: the value displays' rules::PAINT FIRST, THEN SUPPRESS: the ordering holds on every one of the four",
     "theme_value_displays::sheet_model.buildPackage: the value displays' rules::ONE rule per name, even when the package ALSO declares the recipe",
     "theme_value_displays::sheet_model.buildPackage: the value displays' rules::the RUNG-2 hole: a slot the package never declares still has its rule",
     "theme_value_displays::sheet_model.buildPackage: the value displays' rules::an UNSKINNED value node is byte-unchanged: solid paint, no skinned tag",
     "theme_value_displays::sheet_model.buildPackage: the value displays' rules::the census counts the pre-armed baseline instead of hiding it",
     "theme_value_displays::sheet_model.buildPackage: the value displays' rules::a switch's ON art keys on the VALUE tag, never on row selection",
     "theme_value_displays::sheet_model.buildPackage: the value displays' rules::the knob's per-state art is a CHILD COMBINATOR off the interactive node",
     "theme_value_displays::sheet_model.buildPackage: the value displays' rules::every cap asset joins the preload seam, per-end overrides included",
     "theme_value_displays::sheet_model.buildPackage: the value displays' rules::the dot spinner's five plates differ in `tint` and in NOTHING else",
     "theme_value_displays::sheet_model.buildPackage: the value displays' rules::that plate is exactly the one a skin suppresses — the trap, in two rules",
     "theme_value_displays::sheet_model.buildPackage: the value displays' rules::themes.define REFUSES a spinner recipe, and names the knobs that do work",
     "theme_value_displays::sheet_model.buildPackage: the value displays' rules::no wash may reach a value display, and the compiler says so",
     "theme_value_displays::chrome_slots.resolveSlot: the stepper plate's fallback::a package with no stepperPlate keeps skinning the stepper as a control",
     "theme_value_displays::chrome_slots.resolveSlot: the stepper plate's fallback::a package that DOES declare one wins the slot back",
     "theme_value_displays::chrome_slots.resolveSlot: the stepper plate's fallback::the bar family is deliberately absent from the fallback map",
     "theme_value_displays::sheet_model.classifyTags: the switch's value is a tag::ON emits facet-toggle-on and never the row-selection tag",
     "theme_value_displays::screen_target: the bar assembly is materialized as rs-m4 measured::the window CLIPS and is full-bleed over the solved percent rect",
     "theme_value_displays::screen_target: the bar assembly is materialized as rs-m4 measured::the art inside it is sized from the PURE box, never from arithmetic here",
     "theme_value_displays::screen_target: the bar assembly is materialized as rs-m4 measured::the TRACK is found through the PATH, not through instance.Parent",
     "theme_value_displays::screen_target: the bar assembly is materialized as rs-m4 measured::the art's tag is added BEFORE the instance joins the tree",
     "theme_value_displays::screen_target: the bar assembly is materialized as rs-m4 measured::NOT ONE paint property is written in native mode",
     "theme_value_displays::screen_target: the bar assembly is materialized as rs-m4 measured::the caps and the centrepiece hang off the TRACK, above the window",
     "theme_value_displays::screen_target: the bar assembly is materialized as rs-m4 measured::a value node paints its own plate FIRST and suppresses it after",
     "theme_value_displays::screen_target: the bar assembly is materialized as rs-m4 measured::the assembly's connection and census fall with the node",
     "theme_value_displays::screen_target: the bar assembly is materialized as rs-m4 measured::a zero-sized image is not a FAILED image (live-found)",
     "theme_value_displays::screen_target: the toggle's art rides the instances that already exist::both parts are ImageLabels and DIRECT children of the button",
     "theme_value_displays::screen_target: the toggle's art rides the instances that already exist::TRAVEL is the same two positions, and art never moves them",
     "theme_value_displays::screen_target: the toggle's art rides the instances that already exist::a SKINNED part's real pill and hairline are turned off — no rule can reach them",
     "theme_value_displays::screen_target: the toggle's art rides the instances that already exist::...and RESTORED when the package stops skinning it, so flat is byte-identical",
     "theme_value_displays::screen_target: the toggle's art rides the instances that already exist::a decoration built AFTER its texture arrived reports loaded (live-found recovery)",
     "theme_value_displays::screen_target: the toggle's art rides the instances that already exist::it acts on the two parts only, and moves no geometry",
     "theme_value_displays::screen_target: the toggle's art rides the instances that already exist::the ON tag goes through syncTags, so the next pass cannot strip it",
     "theme_value_displays::screen_target: the toggle's art rides the instances that already exist::art is applied the moment the assembly exists, and on every value change",
     "theme_value_displays::screen_target: the toggle's art rides the instances that already exist::the skinned tag rides both parts, so one suppression set covers them",
     "theme_value_displays::the value controls declare their slots on the internal channel::a ProgressView's track and fill reach adapter.create as hints",
     "theme_value_displays::the value controls declare their slots on the internal channel::the fill's solved rect is still the percent — geometry stays solver-owned",
     "theme_value_displays::the value controls declare their slots on the internal channel::a Stepper's two affordances declare the plate slot",
     "theme_value_displays::the bar family's census falls on EVERY teardown path::the ledger itself pairs create with destroy and returns to zero",
     "theme_value_displays::the bar family's census falls on EVERY teardown path::the fill extent comes from the SOLVED px, not from post-scale AbsoluteSize",
     "theme_value_displays::the bar family's census falls on EVERY teardown path::destroyRoot runs the SAME bar teardown remove does, in reverse order",
     "theme_icons_applied::iconSizes is metric authority, not a package-only field::every snapshot answers all three roles, authored or not",
     "theme_icons_applied::iconSizes is metric authority, not a package-only field::a METRIC package resizes icons — the whole reason it rides the snapshot",
     "theme_icons_applied::iconSizes is metric authority, not a package-only field::a PARTIAL declaration is filled, never half-broken",
     "theme_icons_applied::iconSizes is metric authority, not a package-only field::the role names are a resolvable metric path on every package",
     "theme_icons_applied::iconSizes is metric authority, not a package-only field::adding it moved NO package's content stamp",
     "theme_icons_applied::the tofu defect, closed at its source::the disclosure caret's geometric shapes are GONE from the source",
     "theme_icons_applied::the tofu defect, closed at its source::the stepper's U+2212 minus sign is gone and both glyphs are icon requests",
     "theme_icons_applied::the tofu defect, closed at its source::the glyphs a control actually renders are plain ASCII",
     "theme_icons_applied::the tofu defect, closed at its source::the disclosure header PUBLISHES the chevron icon request, both ways",
     "theme_icons_applied::the tofu defect, closed at its source::every framework glyph a control can request is ASCII by construction",
     "theme_icons_applied::the icon request travels the internal hint channel::blueprint -> mount -> renderer -> adapter.create carries slot AND icon",
     "theme_icons_applied::the icon request travels the internal hint channel::attachHint rejects an asset id where a meaning belongs",
     "theme_icons_applied::icon paint: the package emits, the adapter applies::a resolvable, tinted icon emits exactly one rule on its own tag",
     "theme_icons_applied::icon paint: the package emits, the adapter applies::the tag SANITIZES the semantic name, because dots are selector syntax",
     "theme_icons_applied::icon paint: the package emits, the adapter applies::a rule with a dotted icon name compiles and selects one class",
     "theme_icons_applied::icon paint: the package emits, the adapter applies::a PIXEL package stamps Pixelated on the icon rule too, and counts it",
     "theme_icons_applied::icon paint: the package emits, the adapter applies::the glyph suppression is a TAG, because TextTransparency is sheet-owned",
     "theme_icons_applied::icon paint: the package emits, the adapter applies::the suppression rule is LAST in the cascade, so it beats the disabled value",
     "theme_icons_applied::icon paint: the package emits, the adapter applies::the ADAPTER resolves through the one ruling and applies all four halves",
     "theme_icons_applied::icon paint: the package emits, the adapter applies::native mode writes NO sheet-owned paint on the picture (rs-m2's defeat rule)",
     "theme_icons_applied::icon paint: the package emits, the adapter applies::the lift dies with the glyph, or the character reappears ABOVE the art",
     "theme_icons_applied::icon paint: the package emits, the adapter applies::syncChrome runs the icon seam AHEAD of every early return",
     "theme_icons_applied::icon paint: the package emits, the adapter applies::the picture and its census FALL with the node",
     "theme_icons_applied::icon paint: the package emits, the adapter applies::the SNAPSHOT reaches the adapter inside the swap transaction",
     "theme_icons_applied::image paint is declared sheet-owned, with the adapter's instances named::all eight image properties are native-sheet-owned",
     "theme_icons_applied::image paint is declared sheet-owned, with the adapter's instances named::the sanctioned Image exception is REAL: no emitted Icon rule carries Image",
     "theme_icons_applied::image paint is declared sheet-owned, with the adapter's instances named::every adapter-owned instance family is declared, and the names come from chrome_slots",
     "theme_icons_applied::image paint is declared sheet-owned, with the adapter's instances named::the icon picture is TAGGED BEFORE IT IS PARENTED, like every other create path",
     "theme_selectby::selectBy resolves at INSTALL from the facts already published::the profile's package is the one that installs — never default-then-swap",
     "theme_selectby::selectBy resolves at INSTALL from the facts already published::a pointer profile takes the pointer package",
     "theme_selectby::selectBy resolves at INSTALL from the facts already published::an UNMAPPED profile falls back to the positional default package",
     "theme_selectby::selectBy resolves at INSTALL from the facts already published::PRIMARY is the decision, not the whole class set (a hybrid has several)",
     "theme_selectby::selectBy resolves at INSTALL from the facts already published::the vocabulary is CLOSED — a name outside the classes is an authoring error",
     "theme_selectby::selectBy resolves at INSTALL from the facts already published::a selectBy entry must be a compiled package, and the rule needs a core",
     "theme_selectby::a profile change swaps ONCE, after it settles::dock: touch -> pointer swaps the package and the metrics together",
     "theme_selectby::a profile change swaps ONCE, after it settles::a hybrid FLAP produces one swap per settlement, not one per flap",
     "theme_selectby::a profile change swaps ONCE, after it settles::a flap that returns to where it started swaps NOTHING",
     "theme_selectby::a profile change swaps ONCE, after it settles::an unmapped profile ARRIVING swaps back to the default, deterministically",
     "theme_selectby::a manual swapPackage wins until the next profile change::it stands, and it warns exactly once",
     "theme_selectby::a manual swapPackage wins until the next profile change::...and the NEXT settled profile change takes it back",
     "theme_selectby::a manual swapPackage wins until the next profile change::the manual flag clears on a settlement even when the package does not move",
     "theme_selectby::the subscription lives and dies with the controller::uninstall drops it, and a later profile change touches nothing",
     "theme_selectby::the subscription lives and dies with the controller::a controller with no selectBy publishes no selectBy state at all",
     "theme_selectby::the subscription lives and dies with the controller::the core's OWN observer count returns to baseline across install/uninstall",
     "theme_selectby::the subscription lives and dies with the controller::uninstall CANCELS a settle already in flight (runtime-F6)",
     "theme_selectby::the subscription lives and dies with the controller::the source keeps ONE swap path with an explicit ORIGIN (contract)",
     "theme_selectby::selectBy under adversarial input (verifier fix round)::a capability write whose PRIMARY did not change revokes nothing",
     "theme_selectby::selectBy under adversarial input (verifier fix round)::a FAILED manual swap latches nothing and spends no warning",
     "theme_selectby::selectBy under adversarial input (verifier fix round)::a throwing core.observe leaves the environment installable (no half-install)",
     "theme_selectby::selectBy under adversarial input (verifier fix round)::a throwing adapter.setThemePackage rolls the WHOLE identity back",
     "theme_selectby::selectBy under adversarial input (verifier fix round)::a touch-only device whose PreferredInput says KeyboardAndMouse takes the TOUCH package",
     "theme_pixel_content::a pixel package's snapshot is an integer coordinate system::every LENGTH snaps up to a multiple of the unit",
     "theme_pixel_content::a pixel package's snapshot is an integer coordinate system::it snaps UP, so the accessibility floor survives the rendering flag",
     "theme_pixel_content::a pixel package's snapshot is an integer coordinate system::a value already ON the grid is not bumped by float noise",
     "theme_pixel_content::a pixel package's snapshot is an integer coordinate system::DURATIONS and RATIOS are not lengths and are left alone",
     "theme_pixel_content::a pixel package's snapshot is an integer coordinate system::an explicit OVERRIDE stays the exact number the caller asked for",
     "theme_pixel_content::a pixel package's snapshot is an integer coordinate system::a NON-pixel package never enters the path — no key, no movement",
     "theme_pixel_content::a pixel package's snapshot is an integer coordinate system::pixelUnit without the rendering mode is an authoring error, not a no-op",
     "theme_pixel_content::a pixel package's snapshot is an integer coordinate system::generation rule 1 is COUNTED: every image rule carries Pixelated",
     "theme_pixel_content::a pixel package's snapshot is an integer coordinate system::generation rule 2: a fractional SliceScale is a compile error naming the slot",
     "theme_pixel_content::content adoption: `contentId` is an alias, never a second path::both spellings compile to the same bytes — same assets, same STAMP",
     "theme_pixel_content::content adoption: `contentId` is an alias, never a second path::both emit the same rules, carrying the same plain strings",
     "theme_pixel_content::content adoption: `contentId` is an alias, never a second path::the compiled value is a plain STRING on one field, both ways round",
     "theme_pixel_content::content adoption: `contentId` is an alias, never a second path::declaring BOTH is an authoring error, because they are one field",
     "theme_pixel_content::content adoption: `contentId` is an alias, never a second path::the EXPORT round-trip stays plain strings (the theme_sync path)",
     "theme_reference_packages::reference theme packages compile and resolve through the public API only::classic_desktop: define() accepts it and resolve() produces a usable snapshot",
     "theme_reference_packages::reference theme packages compile and resolve through the public API only::classic_desktop: compiles to a sheet model with a stable stamp",
     "theme_reference_packages::reference theme packages compile and resolve through the public API only::classic_desktop: declares the theme classes it actually exercises",
     "theme_reference_packages::reference theme packages compile and resolve through the public API only::glossy_mobile: define() accepts it and resolve() produces a usable snapshot",
     "theme_reference_packages::reference theme packages compile and resolve through the public API only::glossy_mobile: compiles to a sheet model with a stable stamp",
     "theme_reference_packages::reference theme packages compile and resolve through the public API only::glossy_mobile: declares the theme classes it actually exercises",
     "theme_reference_packages::reference theme packages compile and resolve through the public API only::scifi_hud: define() accepts it and resolve() produces a usable snapshot",
     "theme_reference_packages::reference theme packages compile and resolve through the public API only::scifi_hud: compiles to a sheet model with a stable stamp",
     "theme_reference_packages::reference theme packages compile and resolve through the public API only::scifi_hud: declares the theme classes it actually exercises",
     "theme_reference_packages::reference theme packages compile and resolve through the public API only::fantasy_parchment_stub: define() accepts it and resolve() produces a usable snapshot",
     "theme_reference_packages::reference theme packages compile and resolve through the public API only::fantasy_parchment_stub: compiles to a sheet model with a stable stamp",
     "theme_reference_packages::reference theme packages compile and resolve through the public API only::fantasy_parchment_stub: declares the theme classes it actually exercises",
     "theme_reference_packages::reference theme packages compile and resolve through the public API only::fantasy_parchment: define() accepts it and resolve() produces a usable snapshot",
     "theme_reference_packages::reference theme packages compile and resolve through the public API only::fantasy_parchment: compiles to a sheet model with a stable stamp",
     "theme_reference_packages::reference theme packages compile and resolve through the public API only::fantasy_parchment: declares the theme classes it actually exercises",
     "theme_reference_packages::reference theme packages compile and resolve through the public API only::fantasy_ornate: define() accepts it and resolve() produces a usable snapshot",
     "theme_reference_packages::reference theme packages compile and resolve through the public API only::fantasy_ornate: compiles to a sheet model with a stable stamp",
     "theme_reference_packages::reference theme packages compile and resolve through the public API only::fantasy_ornate: declares the theme classes it actually exercises",
     "theme_reference_packages::reference theme packages compile and resolve through the public API only::pixel_quest: define() accepts it and resolve() produces a usable snapshot",
     "theme_reference_packages::reference theme packages compile and resolve through the public API only::pixel_quest: compiles to a sheet model with a stable stamp",
     "theme_reference_packages::reference theme packages compile and resolve through the public API only::pixel_quest: declares the theme classes it actually exercises",
     "theme_reference_packages::reference theme packages compile and resolve through the public API only::glossy_touch: define() accepts it and resolve() produces a usable snapshot",
     "theme_reference_packages::reference theme packages compile and resolve through the public API only::glossy_touch: compiles to a sheet model with a stable stamp",
     "theme_reference_packages::reference theme packages compile and resolve through the public API only::glossy_touch: declares the theme classes it actually exercises",
     "theme_reference_packages::reference theme packages compile and resolve through the public API only::compact_pointer: define() accepts it and resolve() produces a usable snapshot",
     "theme_reference_packages::reference theme packages compile and resolve through the public API only::compact_pointer: compiles to a sheet model with a stable stamp",
     "theme_reference_packages::reference theme packages compile and resolve through the public API only::compact_pointer: declares the theme classes it actually exercises",
     "theme_reference_packages::the corpus covers all four theme classes::palette-only, metric-changing, font-changing and asset-backed are all represented",
     "theme_reference_packages::the corpus covers all four theme classes::PALETTE-ONLY is the two-theme package: a swap moves colour and no geometry at all",
     "theme_reference_packages::the corpus covers all four theme classes::METRIC-CHANGING moves the numbers a control resolves on every solve",
     "theme_reference_packages::the corpus covers all four theme classes::FONT-CHANGING keys measurement on the effective descriptor, not a short name",
     "theme_reference_packages::the corpus covers all four theme classes::ASSET-BACKED declares content IDs only inside package data",
     "theme_reference_packages::the failure fixtures fail the way the scenario needs them to::a bogus content ID still COMPILES — asset failure is a runtime fact, not a compile one",
     "theme_reference_packages::the failure fixtures fail the way the scenario needs them to::an unknown font family compiles and RESERVES conservatively rather than clipping",
     "theme_reference_packages::the failure fixtures fail the way the scenario needs them to::an incompatible schema is REJECTED with the offending field and the fix",
     "theme_reference_packages::namespaced custom-control conformance (ns:role)::declares its needs with type, fallback, authority and capability",
     "theme_reference_packages::namespaced custom-control conformance (ns:role)::a COVERING package satisfies every need and the control follows the theme",
     "theme_reference_packages::namespaced custom-control conformance (ns:role)::an UNCOVERING package produces a useful pre-play error, not a crash",
     "theme_reference_packages::namespaced custom-control conformance (ns:role)::the compiler itself accepts the namespaced entries SILENTLY — which is why the fixture checks",
     "theme_reference_packages::the gradient-carrying packages stay READABLE::glossy_mobile: compiles and every gradient alpha stop is subtle",
     "theme_reference_packages::the gradient-carrying packages stay READABLE::scifi_hud: compiles and every gradient alpha stop is subtle",
     "theme_reference_packages::the gradient-carrying packages stay READABLE::the gradients still reach the compiled sheet as ::UIGradient rules",
     "theme_reference_packages::fantasy_parchment carries its metrics in the theme_sync region::the region is a loadable, self-contained metrics chunk",
     "theme_reference_packages::fantasy_parchment carries its metrics in the theme_sync region::the region's data is exactly what the compiled package resolves — zero drift",
     "theme_reference_packages::fantasy_parchment carries its metrics in the theme_sync region::consuming the region left the package value-identical (the stored captures stay honest)",
     "theme_reference_packages::fantasy_parchment carries its metrics in the theme_sync region::a hand edit inside the region is detectable as drift, naming the token",
     "theme_reference_packages::the P5 reference packages are CONSUMER code::fantasy_ornate: reaches the framework only through the public surface",
     "theme_reference_packages::the P5 reference packages are CONSUMER code::fantasy_ornate: names an asset, never an id, in every recipe",
     "theme_reference_packages::the P5 reference packages are CONSUMER code::pixel_quest: reaches the framework only through the public surface",
     "theme_reference_packages::the P5 reference packages are CONSUMER code::pixel_quest: names an asset, never an id, in every recipe",
     "theme_reference_packages::the P5 reference packages are CONSUMER code::glossy_touch: reaches the framework only through the public surface",
     "theme_reference_packages::the P5 reference packages are CONSUMER code::glossy_touch: names an asset, never an id, in every recipe",
     "theme_reference_packages::the P5 reference packages are CONSUMER code::compact_pointer: reaches the framework only through the public surface",
     "theme_reference_packages::the P5 reference packages are CONSUMER code::compact_pointer: names an asset, never an id, in every recipe",
     "theme_reference_packages::RS-A12 Fantasy Ornate exercises every v2 capability it claims::R1: the panel declares the WHOLE layer vocabulary in one stack",
     "theme_reference_packages::RS-A12 Fantasy Ornate exercises every v2 capability it claims::R2: per-state art on five slots, and no variant moves a reservation",
     "theme_reference_packages::RS-A12 Fantasy Ornate exercises every v2 capability it claims::R3: the bar family is complete, with an ornament at BOTH ends",
     "theme_reference_packages::RS-A12 Fantasy Ornate exercises every v2 capability it claims::R4: six framework icons, one NAMESPACED, and two left to the glyph",
     "theme_reference_packages::RS-A12 Fantasy Ornate exercises every v2 capability it claims::R6: authored as `content`, compiled to one plain string",
     "theme_reference_packages::RS-A12 Fantasy Ornate exercises every v2 capability it claims::two contrast-gated themes, each with its own focus glow",
     "theme_reference_packages::RS-A12 Fantasy Ornate exercises every v2 capability it claims::the census counts what the skin COSTS (RS-A17 reads exactly this)",
     "theme_reference_packages::RS-A12 Fantasy Ornate exercises every v2 capability it claims::the failure fixture breaks ONE middle layer and still compiles",
     "theme_reference_packages::RS-A13 Pixel Quest is a real pixel package::declares pixel mode and keeps every SliceScale an integer",
     "theme_reference_packages::RS-A13 Pixel Quest is a real pixel package::every emitted image rule is nearest-neighbour",
     "theme_reference_packages::RS-A13 Pixel Quest is a real pixel package::every resolved LENGTH lands on the 4px grid",
     "theme_reference_packages::RS-A13 Pixel Quest is a real pixel package::SELECTION CHANGES THE WHOLE STYLE: a different plate AND ornaments",
     "theme_reference_packages::RS-A14 the platform pair is two snapshots, not two palettes::declares its two halves as data",
     "theme_reference_packages::RS-A14 the platform pair is two snapshots, not two palettes::METRICS move geometry on the swap: 44px rows against ~22px ones",
     "theme_reference_packages::RS-A14 the platform pair is two snapshots, not two palettes::the compact half respects its art's minimum rendered sizes",
     "theme_reference_packages::RS-A14 the platform pair is two snapshots, not two palettes::hover art exists on the pointer half and NOT on the touch half",
     "theme_reference_packages::RS-A14 the platform pair is two snapshots, not two palettes::the STRIPE is a tile layer, and only the touch half has one",
     "theme_reference_packages::RS-A14 the platform pair is two snapshots, not two palettes::focus disagrees on purpose: a halo for the finger, a ring for the pointer",
     "theme_reference_packages::RS-A14 the platform pair is two snapshots, not two palettes::neither half maps an icon, so both draw the framework's ASCII glyph",
     "theme_reference_packages::rung 3: the OrnateGauge contributes namespaced roles and ships its own art::declares one need per contribution kind, all namespaced, all with fallbacks",
     "theme_reference_packages::rung 3: the OrnateGauge contributes namespaced roles and ships its own art::its `check` IS themes.checkCoverage — one gate, so the two cannot drift",
     "theme_reference_packages::rung 3: the OrnateGauge contributes namespaced roles and ships its own art::TWO covering packages give the gauge two different answers",
     "theme_reference_packages::rung 3: the OrnateGauge contributes namespaced roles and ships its own art::an UNCOVERING package produces a useful pre-play error, and the gauge still runs",
     "theme_reference_packages::rung 3: the OrnateGauge contributes namespaced roles and ships its own art::its art is the control's own, published, and never a theme's asset",
     "theme_reference_packages::RS-DIR12: a package's selected palette colour is what its selected ART paints::re-hashes every sampled PNG, so a stale sample is a failure and not a drift",
     "theme_reference_packages::RS-DIR12: a package's selected palette colour is what its selected ART paints::every package whose SELECTION slot is image art declares that art's colour",
     "theme_reference_packages::RS-DIR12: a package's selected palette colour is what its selected ART paints::...and the label the resolver picks CLEARS the floor against the art, not just the token",
     "theme_reference_packages::RS-DIR12: a package's selected palette colour is what its selected ART paints::censuses the OTHER art-backed pairing — a primary label over a skinned control",
     "theme_reference_packages::RS-DIR12: the picker's active row is a SELECTION, not a borrowed fill::declares `selected`, the framework's own state, and keeps one surface",
     "theme_authoring_scenario::theme_authoring scenario: it mounts the shared fixture and exposes every step::mounts the all-controls fixture through the real presenter",
     "theme_authoring_scenario::theme_authoring scenario: it mounts the shared fixture and exposes every step::registers the steps the acceptance rows drive, by name",
     "theme_authoring_scenario::theme_authoring scenario: it mounts the shared fixture and exposes every step::the decode judgement step reports the ledger it explains, and survives a headless target",
     "theme_authoring_scenario::theme_authoring scenario: it mounts the shared fixture and exposes every step::the value fixture mounts the three value displays and sweeps a percent",
     "theme_authoring_scenario::theme_authoring scenario: it mounts the shared fixture and exposes every step::the rung-3 gauge mounts, checks both ways, and re-themes without a rebuild",
     "theme_authoring_scenario::theme_authoring scenario: it mounts the shared fixture and exposes every step::the circle fixture mounts six discs, squares every one, and re-sizes on a metric swap",
     "theme_authoring_scenario::theme_authoring scenario: it mounts the shared fixture and exposes every step::is registered in the scenario registry",
     "theme_authoring_scenario::theme_authoring scenario: installing and swapping::installing a package re-solves the mounted screen instead of rebuilding it",
     "theme_authoring_scenario::theme_authoring scenario: installing and swapping::a palette-only theme swap moves paint and no geometry, and churns no decoration",
     "theme_authoring_scenario::theme_authoring scenario: installing and swapping::swapping packages cycles through the corpus and reports the new identity every time",
     "theme_authoring_scenario::theme_authoring scenario: installing and swapping::an ornate package pays a census and a flat one pays nothing",
     "theme_authoring_scenario::theme_authoring scenario: installing and swapping::the operator's chosen package opens the fixture before any step runs",
     "theme_authoring_scenario::theme_authoring scenario: live authoring and export degrade without the controller::editMetricLive drives the same transaction and says which path it took",
     "theme_authoring_scenario::theme_authoring scenario: live authoring and export degrade without the controller::exportDump falls back to the committed package data and names the missing seam",
     "theme_authoring_scenario::theme_authoring scenario: the failure fixtures::a missing asset flips the fallback and leaves focus and hit geometry alone",
     "theme_authoring_scenario::theme_authoring scenario: the failure fixtures::a missing font degrades conservatively: boxes grow, nothing shrinks",
     "theme_authoring_scenario::theme_authoring scenario: the failure fixtures::an incompatible package is refused and the screen stays mounted and usable",
     "theme_authoring_scenario::theme_authoring scenario: the failure fixtures::a package name that does not exist reports the names that do",
     "theme_authoring_scenario::theme_authoring scenario: the report pairs the evidence::carries geometry, snapshot identity, census, focus, mount identity and the chrome probe together",
     "theme_authoring_scenario::theme_authoring scenario: the report pairs the evidence::survives a package whose recipes carry PER-STATE asset maps",
     "theme_authoring_scenario::theme_authoring scenario: the report pairs the evidence::the snapshot identity reports the chrome insets the solver actually used",
     "theme_authoring_scenario::theme_authoring scenario: the report pairs the evidence::the custom-control step answers both directions and the active resolution",
     "theme_matrix_audit::the headless matrix audit: nineteen canonical rows, solved and checked::covers the five canonical views x three packages, plus the four pair rows",
     "theme_matrix_audit::the headless matrix audit: nineteen canonical rows, solved and checked::(a) no chrome extent paints on a neighbour's text or control — RS-A16-D2",
     "theme_matrix_audit::the headless matrix audit: nineteen canonical rows, solved and checked::(b) no value display has a zero content box — RS-A16-D3",
     "theme_matrix_audit::the headless matrix audit: nineteen canonical rows, solved and checked::(c) every text node fits the rect it was given",
     "theme_matrix_audit::the headless matrix audit: nineteen canonical rows, solved and checked::(d) the solver reports nothing on any row",
     "theme_matrix_audit::the headless matrix audit: nineteen canonical rows, solved and checked::(e) every tag a row emits is selected by a rule in that row's live sheet — RS-A16-D4",
     "theme_matrix_audit::the headless matrix audit: nineteen canonical rows, solved and checked::(e) the five studio-neutral rows really do run the BASE seed",
     "theme_matrix_audit::every tag the framework can emit has a rule that applies it (RS-A16-D4)::the vocabulary really is the whole classifier (a new tag lands in it automatically)",
     "theme_matrix_audit::every tag the framework can emit has a rule that applies it (RS-A16-D4)::studio-neutral (the BASE seed): no tag it can carry is left unselected",
     "theme_matrix_audit::every tag the framework can emit has a rule that applies it (RS-A16-D4)::fantasy_ornate: no tag it can carry is left unselected",
     "theme_matrix_audit::every tag the framework can emit has a rule that applies it (RS-A16-D4)::pixel_quest: no tag it can carry is left unselected",
     "theme_matrix_audit::every tag the framework can emit has a rule that applies it (RS-A16-D4)::glossy_touch: no tag it can carry is left unselected",
     "theme_matrix_audit::every tag the framework can emit has a rule that applies it (RS-A16-D4)::compact_pointer: no tag it can carry is left unselected",
     "theme_matrix_audit::every tag the framework can emit has a rule that applies it (RS-A16-D4)::the conditional tags are not dead vocabulary: the corpus applies every one",
     "theme_asset_judgement::art clipped away by an ancestor is not evidence about its asset (RS-A16-D5)::art entirely outside a ClipsDescendants host is NOT judgeable",
     "theme_asset_judgement::art clipped away by an ancestor is not evidence about its asset (RS-A16-D5)::art the player can see any pixel of IS judgeable, including a partial row",
     "theme_asset_judgement::art clipped away by an ancestor is not evidence about its asset (RS-A16-D5)::a one-pixel overlap is enough, and a shared edge is not",
     "theme_asset_judgement::art clipped away by an ancestor is not evidence about its asset (RS-A16-D5)::EVERY clipping ancestor has to be satisfied, not just the nearest",
     "theme_asset_judgement::art clipped away by an ancestor is not evidence about its asset (RS-A16-D5)::a clipping host with no area clips everything inside it",
     "theme_asset_judgement::art clipped away by an ancestor is not evidence about its asset (RS-A16-D5)::an ancestor that does not clip cannot make art unjudgeable",
     "theme_asset_judgement::art clipped away by an ancestor is not evidence about its asset (RS-A16-D5)::the four earlier faces still answer, and each names itself",
     "theme_asset_judgement::art clipped away by an ancestor is not evidence about its asset (RS-A16-D5)::the D5 row itself: off-window consumers no longer condemn a shared asset",
     "theme_asset_judgement::art clipped away by an ancestor is not evidence about its asset (RS-A16-D5)::a deferred judgement is not a verdict of healthy: the same art, scrolled in",
     "theme_asset_judgement::a condemned asset can still report its own recovery (RS-A16-D5)::the mute is for the HEALTHY siblings; the failed asset's art keeps drawing",
     "theme_asset_judgement::a condemned asset can still report its own recovery (RS-A16-D5)::...and that art is JUDGEABLE, which is what breaks the deadlock",
     "theme_asset_judgement::a condemned asset can still report its own recovery (RS-A16-D5)::a recovery clears the slot and un-mutes every sibling, once",
     "theme_asset_judgement::a condemned asset can still report its own recovery (RS-A16-D5)::NO OSCILLATION: the inferred ledger is a monotone lattice",
     "theme_asset_judgement::a condemned asset can still report its own recovery (RS-A16-D5)::the authoritative provider is still authoritative",
     "theme_asset_judgement::a condemned asset can still report its own recovery (RS-A16-D5)::a package swap clears the decode history with the failure ledger",
     "theme_docs::documentation gate: the shipped theme surface and its docs agree::the live repository passes every documentation obligation",
     "theme_docs::documentation gate: the shipped theme surface and its docs agree::names every obligation it enforces (the CLI's --list output)",
     "theme_docs::documentation gate: the shipped theme surface and its docs agree::fails when the guide index stops linking the custom-theme chapter",
     "theme_docs::documentation gate: the shipped theme surface and its docs agree::fails when a required document is missing",
     "theme_docs::documentation gate: the shipped theme surface and its docs agree::fails when a public Facet.themes export is undocumented",
     "theme_docs::documentation gate: the shipped theme surface and its docs agree::fails when a guide stops naming the surface it teaches",
     "theme_docs::documentation gate: the shipped theme surface and its docs agree::fails when the walkthrough's worked example stops using the public API",
     "theme_docs::documentation gate: the shipped theme surface and its docs agree::fails when an example package or its art/provenance disappears",
     "theme_docs::documentation gate: the shipped theme surface and its docs agree::fails when the upload manifest and the package disagree about a content ID",
     "theme_docs::documentation gate: the shipped theme surface and its docs agree::fails when a local link in a new document points at nothing",
     "theme_docs::documentation gate: the shipped theme surface and its docs agree::fails when the styling guide goes back to calling the contract unshipped",
     "theme_docs::documentation gate: the shipped theme surface and its docs agree::fails when a scenario step the guide teaches no longer exists",
     "theme_docs::documentation gate: the rich-skinning surface and its docs agree::names the v2 obligations in the CLI's --list output",
     "theme_docs::documentation gate: the rich-skinning surface and its docs agree::fails when the guide index stops linking the rich-skinning chapter",
     "theme_docs::documentation gate: the rich-skinning surface and its docs agree::fails when either v2 document is missing",
     "theme_docs::documentation gate: the rich-skinning surface and its docs agree::fails when the rich-skinning chapter stops naming a v2 capability",
     "theme_docs::documentation gate: the rich-skinning surface and its docs agree::fails when the rung-3 playbook stops naming the contribution contract",
     "theme_docs::documentation gate: the rich-skinning surface and its docs agree::fails when the rung-3 example stops going through the public contract",
     "theme_docs::documentation gate: the rich-skinning surface and its docs agree::fails when the rung-3 example REQUIRES anything — a clean consumer takes arguments",
     "theme_docs::documentation gate: the rich-skinning surface and its docs agree::fails when the CONTROL's own art, generator, provenance or manifest disappears",
     "theme_docs::documentation gate: the rich-skinning surface and its docs agree::fails when the control's manifest and the control disagree about a content ID",
     "theme_docs::documentation gate: the rich-skinning surface and its docs agree::fails when a document goes back to describing the v2 surface as unshipped",
     "theme_docs::documentation gate: the rich-skinning surface and its docs agree::fails when the chapter goes back to calling the per-view gradient unshipped",
     "theme_docs::documentation gate: the rich-skinning surface and its docs agree::fails when a local link in either v2 document points at nothing",
     "theme_docs::documentation gate: a comparison document sources what it compares against::the live tree's comparison document passes, and an absent one is not an error",
     "theme_docs::documentation gate: a comparison document sources what it compares against::passes on a minimal well-formed document, and reports that it read it",
     "theme_docs::documentation gate: a comparison document sources what it compares against::fails when a framework it compares against carries no source URL",
     "theme_docs::documentation gate: a comparison document sources what it compares against::fails when the comparison carries no ISO research date",
     "theme_docs::documentation gate: a comparison document sources what it compares against::fails when the comparison pins no version, tag or commit",
     "theme_docs::documentation gate: a comparison document sources what it compares against::fails when the comparison never marks a claim as an inference",
     "theme_docs::documentation gate: a comparison document sources what it compares against::names the sourcing obligations in the CLI's --list output",
     "theme_docs::the guide index catalogs the live public surface::counts a catalog entry for every namespace member, export and playbook",
     "theme_docs::the guide index catalogs the live public surface::fails when the catalog stops naming a shipped control",
     "theme_docs::the guide index catalogs the live public surface::fails when the catalog advertises something the library does not export",
     "theme_docs::the guide index catalogs the live public surface::fails when the catalog invents a top-level export",
     "theme_docs::the guide index catalogs the live public surface::fails when the catalog invents a member of a namespace that is not UI or Controls",
     "theme_docs::the guide index catalogs the live public surface::fails when the catalog invents a client module row",
     "theme_docs::the guide index catalogs the live public surface::fails when a catalog row links an API-reference heading that does not exist",
     "theme_docs::the guide index catalogs the live public surface::fails when an extension playbook is not linked from the index",
     "maintainer_map::maintainer map: every row still points at the tree it describes::the live repository passes every obligation",
     "maintainer_map::maintainer map: every row still points at the tree it describes::checks a real amount, so a gutted map cannot pass by describing nothing",
     "maintainer_map::maintainer map: every row still points at the tree it describes::names every obligation it enforces (the command's --list output)",
     "maintainer_map::maintainer map: every row still points at the tree it describes::reports a src area no row claims",
     "maintainer_map::maintainer map: every row still points at the tree it describes::reports a claimed path that does not exist",
     "maintainer_map::maintainer map: every row still points at the tree it describes::reports two src paths claimed by the same two areas",
     "maintainer_map::maintainer map: every row still points at the tree it describes::reports the two tables falling out of step",
     "maintainer_map::maintainer map: every row still points at the tree it describes::reports a spec filed under an area it does not require",
     "maintainer_map::maintainer map: every row still points at the tree it describes::reports a spec file that has been renamed away",
     "maintainer_map::maintainer map: every row still points at the tree it describes::reports a Studio scenario that is not a registered scenario",
     "maintainer_map::maintainer map: every row still points at the tree it describes::reports an empty scenario cell that gives no reason",
     "maintainer_map::maintainer map: every row still points at the tree it describes::reports a gate row the manifest does not have",
     "maintainer_map::maintainer map: every row still points at the tree it describes::reports a gate stage the manifest does not have",
     "maintainer_map::maintainer map: every row still points at the tree it describes::reports a public seam the library has stopped exporting",
     "maintainer_map::maintainer map: every row still points at the tree it describes::reports a boundary rule check_boundary does not enforce",
     "maintainer_map::maintainer map: every row still points at the tree it describes::reports a link that resolves nowhere",
     "maintainer_map::maintainer map: every row still points at the tree it describes::reports a link whose anchor is not a heading in the target",
     "maintainer_map::maintainer map: every row still points at the tree it describes::reports a shipped extension playbook the map stops linking",
     "maintainer_map::maintainer map: every row still points at the tree it describes::reports a missing marker rather than silently checking nothing",
     "maintainer_map::maintainer map: every row still points at the tree it describes::derives a per-area covering count from the require graph",
     "motion_spring::motion classes (the vocabulary)::ships the four ratified shapes under framework-neutral names",
     "motion_spring::motion classes (the vocabulary)::overshoot is earned: reward is the ONLY under-damped built-in",
     "motion_spring::motion classes (the vocabulary)::refuses an inline params table and names registerClass instead",
     "motion_spring::motion classes (the vocabulary)::an unknown class name is an error with a did-you-mean, never a fallback",
     "motion_spring::motion classes (the vocabulary)::registers a game class, validates its range, and resets back to the built-ins",
     "motion_spring::motion classes (the vocabulary)::a registered class is frozen (nobody re-tunes a shared shape by reference)",
     "motion_spring::spring solver (semi-implicit Euler)::a critically damped class never overshoots its target",
     "motion_spring::spring solver (semi-implicit Euler)::the one under-damped class overshoots (earned, and only there)",
     "motion_spring::spring solver (semi-implicit Euler)::setTarget touches neither value nor velocity (the interruptibility invariant)",
     "motion_spring::spring solver (semi-implicit Euler)::an interrupted flight carries its velocity: no brick wall against a cut twin",
     "motion_spring::spring solver (semi-implicit Euler)::a seeded release velocity produces a continuous first frame (no seam)",
     "motion_spring::spring solver (semi-implicit Euler)::clamps a frame spike and substeps it: no NaN, and a spike equals MAX_DT",
     "motion_spring::spring solver (semi-implicit Euler)::rejects non-finite input and keeps the last good state",
     "motion_spring::spring solver (semi-implicit Euler)::snap sets value and target and zeroes velocity (the reduced-motion primitive)",
     "motion_spring::spring solver (semi-implicit Euler)::settles within eps on BOTH value and velocity, and a coarser eps settles sooner",
     "motion_spring::spring solver (semi-implicit Euler)::a zero or negative dt advances nothing (a rewound clock is not motion)",
     "motion_clock::motion clock: stepping and the active set::a value is a Readable the rest of the framework already knows how to bind",
     "motion_clock::motion clock: stepping and the active set::derives dt from the injected now() when step() is called bare",
     "motion_clock::motion clock: stepping and the active set::commits every value in ONE transaction per stepped frame",
     "motion_clock::motion clock: stepping and the active set::settled values leave the active set: rest costs zero steps and zero writes",
     "motion_clock::motion clock: stepping and the active set::lands EXACTLY on the target and fires onSettle exactly once per flight",
     "motion_clock::motion clock: stepping and the active set::a mid-flight re-target continues from the current value and velocity",
     "motion_clock::motion clock: stepping and the active set::a function target is re-read every step, so a moved target still lands",
     "motion_clock::motion clock: stepping and the active set::a vanished live target freezes the aim on the last good read and settles",
     "motion_clock::motion clock: stepping and the active set::stop abandons the aim where the value stands, and fires no arrival",
     "motion_clock::motion clock: stepping and the active set::snap is placement, not arrival: value+target set, no settle event",
     "motion_clock::motion clock: stepping and the active set::refuses inline class params at the call site",
     "motion_clock::motion clock: stepping and the active set::refuses an unknown motion kind",
     "motion_clock::motion clock: reduced motion (information parity)::a decorative value snaps to the target and fires the SAME settle event",
     "motion_clock::motion clock: reduced motion (information parity)::a velocity seed is inert under reduced motion (there is no seam to smooth)",
     "motion_clock::motion clock: reduced motion (information parity)::the policy is read LIVE: a flight in progress snaps on its next re-target",
     "motion_clock::motion clock: reduced motion (information parity)::an informational value KEEPS RUNNING under RM, quantized to 250 ms writes",
     "motion_clock::motion clock: informational timers::depletes linearly and reaches its terminus on the exact wall-clock frame",
     "motion_clock::motion clock: informational timers::under RM it still reaches the terminus at the same moment, with stepped writes",
     "motion_clock::motion clock: informational timers::a DECORATIVE timer is over immediately under RM (declared, not defaulted)",
     "motion_clock::motion clock: informational timers::a zero-duration timer is already at its terminus and still announces it",
     "motion_clock::motion clock: informational timers::a timer has no target and no velocity to seed",
     "motion_clock::motion clock: glides (a value RESAMPLED at a fixed cadence)::crosses the gap at CONSTANT velocity — the assertion a spring cannot pass",
     "motion_clock::motion clock: glides (a value RESAMPLED at a fixed cadence)::re-aiming mid-flight restarts the ramp FROM WHERE IT IS — no teleport, no stall",
     "motion_clock::motion clock: glides (a value RESAMPLED at a fixed cadence)::is INFORMATIONAL by default: under RM it keeps gliding, quantized, never snapping",
     "motion_clock::motion clock: glides (a value RESAMPLED at a fixed cadence)::a zero duration is legal and lands immediately (a cadence that is one frame)",
     "motion_clock::motion clock: glides (a value RESAMPLED at a fixed cadence)::refuses an unknown key and a negative duration (constitution §4)",
     "motion_clock::motion clock: counters (the numeral a player reads)::publishes WHOLE numbers on the way, and lands exactly on the count",
     "motion_clock::motion clock: counters (the numeral a player reads)::NEVER overshoots the count, even on an under-damped class",
     "motion_clock::motion clock: counters (the numeral a player reads)::writes ONCE PER CHANGED NUMERAL, not once per frame",
     "motion_clock::motion clock: counters (the numeral a player reads)::re-aims mid-count without restarting, and lands on the LAST count asked for",
     "motion_clock::motion clock: counters (the numeral a player reads)::under reduced motion the FINAL COUNT is instant, and the settle event still fires",
     "motion_clock::motion clock: counters (the numeral a player reads)::a fractional target still lands on a numeral, and getTarget says which",
     "motion_clock::motion clock: counters (the numeral a player reads)::snap places the count with no arrival, and rest costs zero",
     "motion_clock::motion clock: lifetime::dispose releases every value it built (and is idempotent)",
     "motion_clock::motion clock: lifetime::a scope owns the clock (surface teardown disposes motion with everything else)",
     "motion_clock::motion clock: lifetime::50 build/step/dispose cycles return the core counters to baseline",
     "motion_clock::motion facade: the four entries the audit called dead::resolveClass returns the registered class, and refuses an unknown name by suggestion",
     "motion_clock::motion facade: the four entries the audit called dead::isRegisteredClass answers without throwing — which is why RascalRally guards with it",
     "motion_clock::motion facade: the four entries the audit called dead::resetClasses drops what was registered and restores the four built-ins",
     "motion_clock::motion facade: the four entries the audit called dead::resolveCurve returns a registered curve and refuses an unknown one",
     "motion_chase::motion chase: perceptual arrival::fires on the radius frame, well before the settle epsilon would",
     "motion_chase::motion chase: perceptual arrival::the chase detaches on arrival: nothing keeps stepping for it",
     "motion_chase::motion chase: perceptual arrival::a wider radius arrives earlier than a tight one (the knob is real)",
     "motion_chase::motion chase: perceptual arrival::chases a target that moves mid-flight and lands on the MOVED one",
     "motion_chase::motion chase: perceptual arrival::a target that keeps running is never declared arrived at a stale pixel",
     "motion_chase::motion chase: perceptual arrival::a vanished target resolves by SETTLE and says so (targetLost)",
     "motion_chase::motion chase: perceptual arrival::cancel abandons the chase without an arrival event",
     "motion_chase::motion chase: perceptual arrival::under reduced motion the flight is instant and the arrival still fires",
     "motion_chase::motion chase: perceptual arrival::under reduced motion a vanished target still reports the honest outcome",
     "motion_chase::motion chase: perceptual arrival::requires both axes and a target function (2-D motion is two springs)",
     "motion_tween::motion curves: the registry is the only dial::refuses an inline spec table and names registerCurve instead",
     "motion_tween::motion curves: the registry is the only dial::ships EMPTY — there is no built-in duration to inherit by accident",
     "motion_tween::motion curves: the registry is the only dial::registers, resolves, defaults direction to out, and freezes the result",
     "motion_tween::motion curves: the registry is the only dial::refuses an out-of-range duration, an unknown style and an unknown direction",
     "motion_tween::motion curves: the registry is the only dial::suggests the curve you meant",
     "motion_tween::motion curves: the pure twin IS the engine's curve::matches the frozen engine corpus at every sampled alpha",
     "motion_tween::motion curves: the pure twin IS the engine's curve::carries Roblox's eleven styles, not a private vocabulary",
     "motion_tween::motion curves: the pure twin IS the engine's curve::every style is anchored at both ends, in all three directions",
     "motion_tween::motion curves: the pure twin IS the engine's curve::clamps an alpha outside [0,1] rather than extrapolating",
     "motion_tween::motion curves: the pure twin IS the engine's curve::hands the adapter the engine's own enum spelling",
     "motion_tween::clock:tween — a duration that means what it says::ARRIVES EXACTLY ON TIME, and not one frame early",
     "motion_tween::clock:tween — a duration that means what it says::a curve is not a straight line — quad/out is ahead of linear at the midpoint",
     "motion_tween::clock:tween — a duration that means what it says::a re-aim mid-flight redirects from where it IS, with no jump",
     "motion_tween::clock:tween — a duration that means what it says::refuses setVelocity: a curve's speed is its shape",
     "motion_tween::clock:tween — a duration that means what it says::reports the curve's INSTANTANEOUS slope, not its average",
     "motion_tween::clock:tween — a duration that means what it says::a zero-duration curve lands on the NEXT step, never on an intermediate value",
     "motion_tween::clock:tween — a duration that means what it says::rests for free: a fresh tween attaches nothing to the clock",
     "motion_tween::clock:tween — a duration that means what it says::fires settle EXACTLY ONCE per arrival, after the writes are committed",
     "motion_tween::clock:tween — a duration that means what it says::advances by RAW dt: a frame spike does not stretch the duration",
     "motion_tween::clock:tween — a duration that means what it says::refuses an unknown curve, an inline spec, and a non-finite initial",
     "motion_tween::clock:tween — reduced motion is inherited, not re-decided::DECORATIVE (the default) places the terminus instantly and fires the same settle",
     "motion_tween::clock:tween — reduced motion is inherited, not re-decided::INFORMATIONAL keeps running to the same terminus, quantized — a frozen cooldown is a hung game",
     "motion_tween::clock:tween — reduced motion is inherited, not re-decided::writes far fewer times under reduced motion, and lands on the same number",
     "motion_tween::clock:tween — the clock entry cannot leak::a scope owns the clock, and a tween dies with it",
     "motion_tween::clock:tween — the clock entry cannot leak::build/aim/settle/dispose churn returns the graph to baseline",
     "motion_tween::clock:tween — the clock entry cannot leak::a disposed clock refuses to build a tween rather than leaking one",
     "motion_tween::clock:setEasing — the engine owns the curve::routes every evaluation through the installed evaluator",
     "motion_tween::clock:setEasing — the engine owns the curve::is read per evaluation, so a bind upgrades values built before it",
     "motion_tween::clock:setEasing — the engine owns the curve::refuses a non-function rather than failing on the first frame of motion",
     "motion_timeline::motion timeline: playing out::fires beats in order at their declared clock times",
     "motion_timeline::motion timeline: playing out::is deterministic: the same scripted clock produces the same log twice",
     "motion_timeline::motion timeline: playing out::onDone fires exactly once, and a late interrupt is a no-op",
     "motion_timeline::motion timeline: interrupt and skip::interrupt at EVERY beat boundary leaves the declared end state, in order",
     "motion_timeline::motion timeline: interrupt and skip::interrupt MID-beat resolves the PLAYING beat and the remaining beats to terminals",
     "motion_timeline::motion timeline: interrupt and skip::skip fast-forwards the remaining content with zero elapsed time",
     "motion_timeline::motion timeline: interrupt and skip::a beat may skip its own timeline without double-firing anything",
     "motion_timeline::motion timeline: interrupt and skip::a beat may start motion on the same clock, and it runs from that frame",
     "motion_timeline::motion timeline: interrupt and skip::beats with no declared terminal are simply not painted over by interrupt",
     "motion_timeline::motion timeline: reduced motion::fires every beat immediately, in order, with durations zeroed",
     "motion_timeline::motion timeline: reduced motion::every beat's run fires in BOTH modes: the run sets match exactly",
     "motion_timeline::motion timeline: authoring errors::rejects beats declared out of order",
     "motion_timeline::motion timeline: authoring errors::rejects a negative or non-finite beat time and a non-function run",
     "motion_timeline::motion timeline: authoring errors::an empty timeline is done immediately (and still reports it)",
     "reduced_motion_seam::reduced motion: the fact reaches the target with NO consumer wiring::full motion is the default, and a press plays a tween",
     "reduced_motion_seam::reduced motion: the fact reaches the target with NO consumer wiring::reduced motion ON: an adapter-side tween must NOT run",
     "reduced_motion_seam::reduced motion: the fact reaches the target with NO consumer wiring::a mid-session flip lands on the very next press, both directions",
     "reduced_motion_seam::reduced motion: the fact reaches the target with NO consumer wiring::a surface attached while the preference is already ON is born reduced",
     "reduced_motion_seam::reduced motion: the fact reaches the target with NO consumer wiring::the sheet half and the bespoke half stay in step",
     "reduced_motion_seam::reduced motion: the fact reaches the target with NO consumer wiring::a target WITHOUT setReducedMotion degrades by name",
     "reduced_motion_seam::reduced motion: the LIVE ScreenTarget resolves it from the pushed fact::its one resolver consults the pushed flag, not only the legacy closure",
     "reduced_motion_seam::reduced motion: the LIVE ScreenTarget resolves it from the pushed fact::the pushed seam writes that same flag",
     "reduced_motion_seam::reduced motion: the LIVE ScreenTarget resolves it from the pushed fact::the deprecated construction opt can no longer say NO on the player's behalf",
     "transitions::mount retire model: When::a declared transition retires the branch instead of disposing it",
     "transitions::mount retire model: When::re-entry mid-exit reuses the SAME mounted subtree — identity, scope, factory runs",
     "transitions::mount retire model: When::the branch scope disposes exactly once, at done — never at retire, never twice",
     "transitions::mount retire model: When::the scope handed to thenView survives the RETIRE and dies at done, once",
     "transitions::mount retire model: When::without a transition the removal path is byte-identical to before",
     "transitions::mount retire model: When::disposing the region mid-exit releases the coordinator's motion, once",
     "transitions::mount retire model: ForEach::a removed row holds its previous slot while it exits",
     "transitions::mount retire model: ForEach::the same key re-added mid-exit keeps its mounted identity",
     "transitions::mount retire model: ForEach::a retiring row is not resurrected by an unrelated reorder",
     "transitions::mount retire model: ForEach::churn with exits in flight returns the core to baseline",
     "transitions::transition coordinator: the forms::a fade drives group transparency 1 -> 0 and clears at rest",
     "transitions::transition coordinator: the forms::a slide seeds the offset from the edge it travels from, in theme space",
     "transitions::transition coordinator: the forms::materialize scales 0.96 -> 1 and fades together",
     "transitions::transition coordinator: the forms::the mirrored exit displaces the node to the SAME absent place as the enter",
     "transitions::transition coordinator: the forms::a fading form on a node that is not a fade group is an authoring error that names the fix",
     "transitions::transition coordinator: lifetime::an exit defers disposal until the motion lands",
     "transitions::transition coordinator: lifetime::the exit cap disposes a motion that has not landed by 500 ms",
     "transitions::transition coordinator: lifetime::a re-entry mid-exit reverses from the value it had, without a jump",
     "transitions::transition coordinator: lifetime::an `instant` exit never retires at all",
     "transitions::transition coordinator: reduced motion is parity, not deletion::an enter lands on the frame it starts and still emits arrive",
     "transitions::transition coordinator: reduced motion is parity, not deletion::an exit completes in the frame it begins — the beat is instant, never dropped",
     "transitions::transition coordinator: reduced motion is parity, not deletion::the full-motion path emits the same arrive, one frame later than the pixels",
     "transitions::presenter: a retiring subtree is not interactive::focus order drops the branch the moment it starts to exit, and taps stop landing",
     "transitions::presenter: a surface transition::targets the root's declared fade group and enters on the first frame",
     "transitions::presenter: a surface transition::dismiss leaves the stack immediately and the screen late",
     "transitions::presenter: a surface transition::the deferred teardown is bounded by the same flat cap",
     "transitions::presenter: a surface transition::surfaces without a transition tear down synchronously, exactly as before",
     "backdrop_fade::backdrop-before-content: the pure structural finder::an opaque surface as a DIRECT child of the fade group is the finding",
     "backdrop_fade::backdrop-before-content: the pure structural finder::names the fix in plain language",
     "backdrop_fade::backdrop-before-content: the pure structural finder::`surface = \"plain\"` never paints, so it is never a finding",
     "backdrop_fade::backdrop-before-content: the pure structural finder::no surface anywhere is zero findings",
     "backdrop_fade::backdrop-before-content: the pure structural finder::a surface TWO levels in is ordinary content, not a second backdrop — deliberately out of scope",
     "backdrop_fade::backdrop-before-content: the pure structural finder::an opaque tint on a Box, DIRECT child of the fade group, is a finding — via = tint",
     "backdrop_fade::backdrop-before-content: the pure structural finder::a tint with `transparency = 1` (fully invisible) never paints, so it is never a finding",
     "backdrop_fade::backdrop-before-content: the pure structural finder::a REACTIVE tint (no readable `.transparency`) is treated as opaque — the safe default direction",
     "backdrop_fade::backdrop-before-content: the pure structural finder::a `tint` on a NON-Box class (Text/Path/Image/Stage colour a glyph/stroke/picture, never a plate) is not a finding",
     "backdrop_fade::backdrop-before-content: the pure structural finder::the SELF-CASE (declared node IS the canvasGroup) is caught by the identical finder — no special case needed",
     "backdrop_fade::backdrop-before-content: the pure structural finder::MUTATION PROOF: the fixed shape is clean, and re-nesting the plate reddens it",
     "backdrop_fade::backdrop-before-content: the live coordinator::RED — the buggy shape: the coordinator reports the finding, and the plate itself fades",
     "backdrop_fade::backdrop-before-content: the live coordinator::GREEN — the fixed shape: zero findings, and the plate NEVER fades while the content does",
     "backdrop_fade::backdrop-before-content: the live coordinator::THE SPLIT: transform still rides the WHOLE card (materialize scales `Wrap`, not just `Group`)",
     "backdrop_fade::backdrop-before-content: the real Glade modals (newpop.mov's family)::openWisp — the exact repro (tapping a Recent-visitors row) is clean",
     "backdrop_fade::backdrop-before-content: the real Glade modals (newpop.mov's family)::every OTHER sheet()-based modal (charm, provisions, picker) is clean too — the shared fix, not a one-off",
     "toast_schedule::toast schedule: showing, queueing and timing out::shows up to maxVisible and queues the rest, newest last",
     "toast_schedule::toast schedule: showing, queueing and timing out::a toast times out after its duration and the queue promotes in that frame",
     "toast_schedule::toast schedule: showing, queueing and timing out::durations and read floors are per-toast, not global",
     "toast_schedule::toast schedule: showing, queueing and timing out::the defaults are the ratified numbers",
     "toast_schedule::toast schedule: priority::the queue is priority-ordered, FIFO within a priority",
     "toast_schedule::toast schedule: priority::a more urgent toast NEVER truncates a showing toast's read floor",
     "toast_schedule::toast schedule: priority::preemption takes the WEAKEST showing toast, not the oldest",
     "toast_schedule::toast schedule: priority::a queued toast no more urgent than the weakest showing one just waits",
     "toast_schedule::toast schedule: supersede and capacity::a same-key toast replaces its QUEUED predecessor immediately",
     "toast_schedule::toast schedule: supersede and capacity::a same-key toast replaces a SHOWING one only once its floor is met",
     "toast_schedule::toast schedule: supersede and capacity::at the queue cap the LOWEST-priority queued toast is dropped, with a reason",
     "toast_schedule::toast schedule: supersede and capacity::a newcomer that IS the least urgent thing drops itself, and says so",
     "toast_schedule::toast schedule: supersede and capacity::a SHOWING toast is never dropped for capacity",
     "toast_schedule::toast schedule: supersede and capacity::a burst never stacks unbounded",
     "toast_schedule::toast schedule: explicit dismissal::dismiss retires a showing toast and promotes the queue in the same call",
     "toast_schedule::toast schedule: explicit dismissal::dismiss finds a QUEUED toast too, and an unknown id is a false, not an error",
     "toast_schedule::toast schedule: explicit dismissal::clear retires everything, each with its reason",
     "toast_presentation::toast presentation: the layer is input-transparent::a control beneath a live toast still activates",
     "toast_presentation::toast presentation: the layer is input-transparent::the toast layer is not a presented surface: it never joins the stack",
     "toast_presentation::toast presentation: the layer is input-transparent::focus is untouched before, during and after",
     "toast_presentation::toast presentation: the layer is input-transparent::no toast node is focusable, so no navigation can ever reach one",
     "toast_presentation::toast presentation: stacking::the toast layer sits above every base screen and below every modal",
     "toast_presentation::toast presentation: stacking::a bottom-docked strip grows UPWARD from the safe-area inset (review R2-F10)",
     "toast_presentation::toast presentation: stacking::a base screen presented AFTER a modal still sits below it (bands, not a counter)",
     "toast_presentation::toast presentation: stacking::400 present/dismiss cycles never push a base screen's displayOrder out of its band",
     "toast_presentation::toast presentation: stacking::400 present/dismiss cycles leave the NEXT present's displayOrder identical to a first-ever present's",
     "toast_presentation::toast presentation: the layer's lifetime::the layer appears with the first toast and retires once the last one has left",
     "toast_presentation::toast presentation: the layer's lifetime::a toast retires itself on schedule and says why",
     "toast_presentation::toast presentation: the layer's lifetime::an explicit dismiss retires it early with reason `manual`",
     "toast_presentation::toast presentation: the layer's lifetime::churning toasts through mount and retire returns the core to baseline",
     "toast_presentation::toast presentation: reduced motion keeps the whole information surface::the same toasts appear for the same time; only the transition is instant",
     "toast_presentation::toast presentation: reduced motion keeps the whole information surface::under reduced motion a toast is fully painted on its first frame",
     "toast_presentation::toast presentation: reduced motion keeps the whole information surface::with motion the same toast starts displaced and transparent, then settles",
     "toast_presentation::toast presentation: reduced motion keeps the whole information surface::a bottom-docked toast comes up from its own edge instead",
     "feedback::feedback bus: the taxonomy is closed::every v1 verb is accepted",
     "feedback::feedback bus: the taxonomy is closed::an invented verb is an authoring error that lists the vocabulary",
     "feedback::feedback bus: the taxonomy is closed::a malformed event is refused rather than delivered half-formed",
     "feedback::feedback bus: subscriptions::unsubscribing stops delivery and leaves no slot behind",
     "feedback::feedback bus: subscriptions::a handler may unsubscribe itself from inside its own delivery",
     "feedback::feedback bus: subscriptions::a handler subscribing during a delivery does not receive that same event",
     "feedback::presenter feedback: activate::fires exactly once per press, on the causal frame, after the effect",
     "feedback::presenter feedback: activate::a disabled control emits nothing — nothing happened",
     "feedback::presenter feedback: activate::handle.onFeedback sees only its own surface's events",
     "feedback::presenter feedback: dismiss and the session registration point::a dismissed modal reports it on the frame the decision was taken",
     "feedback::presenter feedback: dismiss and the session registration point::presenter.emitFeedback is the seam a drag session commits through",
     "feedback::presenter feedback: dismiss and the session registration point::onModalPresented is the modal-cancels-drag hook, and it unsubscribes",
     "sensory_feedback::UI.sensoryFeedback: the declaration is ruled on at the call site::refuses an event outside the closed twelve, listing the vocabulary",
     "sensory_feedback::UI.sensoryFeedback: the declaration is ruled on at the call site::accepts every one of the twelve verbs",
     "sensory_feedback::UI.sensoryFeedback: the declaration is ruled on at the call site::refuses a trigger that is not a Signal/Memo — a constant never changes",
     "sensory_feedback::UI.sensoryFeedback: the declaration is ruled on at the call site::refuses an unknown spec key rather than dropping it",
     "sensory_feedback::UI.sensoryFeedback: the declaration is ruled on at the call site::refuses a structural region — it would mount no node to emit for",
     "sensory_feedback::UI.sensoryFeedback: the declaration is ruled on at the call site::returns a NEW frozen blueprint and leaves the original untouched",
     "sensory_feedback::UI.sensoryFeedback: what reaches the bus::emits the declared verb with the mounted path when the trigger changes",
     "sensory_feedback::UI.sensoryFeedback: what reaches the bus::fires once per change, and not at all for a write of the same value",
     "sensory_feedback::UI.sensoryFeedback: what reaches the bus::emits synchronously inside the write that moved the trigger",
     "sensory_feedback::UI.sensoryFeedback: what reaches the bus::stops emitting the moment the node unmounts",
     "sensory_feedback::UI.sensoryFeedback: what reaches the bus::composes: two declarations on one node both fire",
     "sensory_feedback::UI.sensoryFeedback: what reaches the bus::observes NOTHING when no sink is wired, and exactly one thing when one is",
     "sensory_feedback::UI.sensoryFeedback: through the real presenter::reaches presenter.onFeedback, carrying the surface it happened on",
     "sensory_feedback::UI.sensoryFeedback: through the real presenter::is visible on handle.onFeedback, the per-surface filter",
     "sensory_profile::sensory profile: the three defaults are the product, pinned exactly::press is `contact` — one short, crisp tap when the action goes down",
     "sensory_profile::sensory profile: the three defaults are the product, pinned exactly::release is `settle` — a lighter, rounder answer when the action completes",
     "sensory_profile::sensory profile: the three defaults are the product, pinned exactly::select is `tick` — the smallest audible-to-the-hand step for a changed choice",
     "sensory_profile::sensory profile: the three defaults are the product, pinned exactly::the three names are distinct, and PHASES lists exactly the three phases",
     "sensory_profile::sensory profile: the three defaults are the product, pinned exactly::no default key authors an intensity below the documented trigger floor",
     "sensory_profile::sensory profile: the three defaults are the product, pinned exactly::no default outlasts the overlap budget, and every key list rises in time from 0",
     "sensory_profile::sensory profile: the three defaults are the product, pinned exactly::every mode names a real Enum.KeyInterpolationMode member",
     "sensory_profile::sensory profile: the three defaults are the product, pinned exactly::the defaults are FROZEN all the way down — a consumer cannot edit the product",
     "sensory_profile::sensory profile: resolve() merges a partial over the defaults::nothing supplied is the defaults, value for value",
     "sensory_profile::sensory profile: resolve() merges a partial over the defaults::a game replaces ONE phase and keeps the other two",
     "sensory_profile::sensory profile: resolve() merges a partial over the defaults::a game swaps a phase for a PRESET without touching any control",
     "sensory_profile::sensory profile: resolve() merges a partial over the defaults::a game SILENCES one phase, and silence is a decision rather than an omission",
     "sensory_profile::sensory profile: resolve() merges a partial over the defaults::all three at once, in one call",
     "sensory_profile::sensory profile: resolve() merges a partial over the defaults::the result is frozen too, so a resolved profile cannot be edited behind the adapter",
     "sensory_profile::sensory profile: resolve() merges a partial over the defaults::an unknown phase key is an authoring error that names the three phases",
     "sensory_profile::sensory profile: resolve() merges a partial over the defaults::a Custom phase with NO keys is refused — it is the guaranteed silent no-op",
     "sensory_profile::sensory profile: resolve() merges a partial over the defaults::a spec whose `kind` is not one of the three is refused",
     "sensory_profile::sensory profile: resolve() merges a partial over the defaults::a preset naming something that is not a HapticEffectType is refused",
     "sensory_profile::sensory profile: resolve() merges a partial over the defaults::`Custom` may not be named as a PRESET — it is a silent no-op by construction",
     "sensory_profile::sensory profile: the preset FALLBACK, and the limitation it carries::names one preset per phase, and every one is a real HapticEffectType",
     "sensory_profile::sensory profile: the preset FALLBACK, and the limitation it carries::is TOTAL over the phases — a fourth phase would be a visible gap",
     "sensory_profile::sensory profile: the preset FALLBACK, and the limitation it carries::release and select SHARE UIHover, and that is the documented limitation",
     "sensory_profile::sensory profile: the preset FALLBACK, and the limitation it carries::fallbackFor(phase) hands back the phase's preset SPEC, ready to pool",
     "sensory_profile::sensory profile: key() is what makes pooling possible::two specs that would FEEL the same share a key; different ones do not",
     "sensory_profile::sensory profile: key() is what makes pooling possible::the three default keys are distinct — three waveforms, three instances",
     "sensory_profile::sensory profile: key() is what makes pooling possible::label() names a sensation for a reader — the waveform, or the preset, or nothing",
     "sensory_profile::sensory profile: it is ENGINE-FREE, which is why any of this is provable::requires nothing and names no engine global — in CODE, not in prose",
     "haptics::haptics: default OFF means the engine is never touched::constructs nothing and plays nothing until it is switched on",
     "haptics::haptics: default OFF means the engine is never touched::publishes no platform claim while it is off",
     "haptics::haptics: default OFF means the engine is never touched::switching it on, then off, stops it again",
     "haptics::haptics: the map is TOTAL over the twelve verbs::every verb in the taxonomy has an explicit entry — no silent drop",
     "haptics::haptics: the map is TOTAL over the twelve verbs::the map invents no verb the taxonomy does not have",
     "haptics::haptics: the map is TOTAL over the twelve verbs::exactly five verbs map to nothing, and they are the five decided",
     "haptics::haptics: the map is TOTAL over the twelve verbs::every mapped verb names a REAL HapticEffectType, and never Custom",
     "haptics::haptics: the map is TOTAL over the twelve verbs::`activate` names no preset on the bus — the two edges belong to two owners",
     "haptics::haptics: the map is TOTAL over the twelve verbs::the two verbs the SELECT phase claims have NO down edge of their own",
     "haptics::haptics: the map is TOTAL over the twelve verbs::`pressSpecFor` answers the four cases a control can be in",
     "haptics::haptics: the map is TOTAL over the twelve verbs::`activationIsFelt` is the OTHER question, and the two are not the same",
     "haptics::haptics: what an enabled adapter actually plays::plays one pooled effect per mapped verb, and reuses it forever",
     "haptics::haptics: what an enabled adapter actually plays::the five silent verbs never play, however often they arrive",
     "haptics::haptics: what an enabled adapter actually plays::a played effect carries the resolved enum value, never a bare Custom",
     "haptics::haptics: `adjust` is rate-limited (a slider is not a buzzsaw)::coalesces a per-tick storm down to one play per interval",
     "haptics::haptics: `adjust` is rate-limited (a slider is not a buzzsaw)::the next tick after the interval plays again",
     "haptics::haptics: `adjust` is rate-limited (a slider is not a buzzsaw)::coalescing DROPS — a swallowed tick never arrives late as a phantom",
     "haptics::haptics: `adjust` is rate-limited (a slider is not a buzzsaw)::the rate limit is the SELECT PHASE's — discrete verbs are never swallowed",
     "haptics::haptics: the capability probe is a LATTICE, never a boolean::touch is `unknown`, not `unsupported` — there is no probe for a phone",
     "haptics::haptics: the capability probe is a LATTICE, never a boolean::the pre-first-gamepad state is `unknown` too",
     "haptics::haptics: the capability probe is a LATTICE, never a boolean::a CONNECTED pad answering false is the one case that means `unsupported`",
     "haptics::haptics: the capability probe is a LATTICE, never a boolean::a connected pad answering true is `supported`",
     "haptics::haptics: the capability probe is a LATTICE, never a boolean::no input service at all is `unknown`, never a negative claim",
     "haptics::haptics: the capability probe is a LATTICE, never a boolean::re-probes on hot-plug instead of caching the boot answer",
     "haptics::haptics: the capability probe is a LATTICE, never a boolean::re-probes on LastInputTypeChanged as well",
     "haptics::haptics: the capability probe is a LATTICE, never a boolean::disconnects its device listeners when switched off",
     "haptics::haptics: the enum is resolved defensively, and never falls back::a missing member plays NOTHING rather than a silent `Custom`",
     "haptics::haptics: the enum is resolved defensively, and never falls back::an enum source that THROWS on index is survived, not propagated",
     "haptics::haptics: a client without the class degrades, it does not crash::an uncreatable class is `absent`, with the engine's own words kept",
     "haptics::haptics: a client without the class degrades, it does not crash::any other refusal is `blocked` — the two are not the same fact",
     "haptics::haptics: a client without the class degrades, it does not crash::with NO seams injected at all it still never throws (this very process)",
     "haptics::haptics: `activate` takes the PROPERTY route (the engine plays it)::assigns PressHapticEffect and NEVER calls Play",
     "haptics::haptics: `activate` takes the PROPERTY route (the engine plays it)::leaves HoverHapticEffect alone — a felt cursor is nobody's request",
     "haptics::haptics: `activate` takes the PROPERTY route (the engine plays it)::shares ONE press effect across every button on the surface",
     "haptics::haptics: `activate` takes the PROPERTY route (the engine plays it)::decorates a button that appears later",
     "haptics::haptics: `activate` takes the PROPERTY route (the engine plays it)::touches nothing that is not a GuiButton",
     "haptics::haptics: `activate` takes the PROPERTY route (the engine plays it)::assigns nothing at all while the adapter is off",
     "haptics::haptics: `activate` takes the PROPERTY route (the engine plays it)::switching off CLEARS the references, so the engine stops too",
     "haptics::haptics: `activate` takes the PROPERTY route (the engine plays it)::detaching ONE surface leaves the others decorated",
     "haptics::haptics: `activate` takes the PROPERTY route (the engine plays it)::attaching the same root twice does not double-count or rebuild",
     "haptics::haptics: `activate` takes the PROPERTY route (the engine plays it)::detaching clears them and stops watching for new buttons",
     "haptics::haptics: an effect is DESTROYED, never merely dropped::five enable/disable cycles leave nothing in the DataModel",
     "haptics::haptics: an effect is DESTROYED, never merely dropped::`pooled` counts what actually exists — the press effect included",
     "haptics::haptics: an effect is DESTROYED, never merely dropped::dispose destroys the pool AND the press effect",
     "haptics::haptics: an effect is DESTROYED, never merely dropped::disposing an already-OFF adapter destroys nothing twice",
     "haptics::haptics: a detach is LOCAL::detaching a surface that contributed nothing builds and destroys nothing",
     "haptics::haptics: a detach is LOCAL::a detach does not re-walk the surviving roots",
     "haptics::haptics: a detach is LOCAL::a button under TWO attached roots keeps its reference until the last lets go",
     "haptics::haptics: a detach is LOCAL::a surface DESTROYED without a detach releases its entry",
     "haptics::haptics: a detach is LOCAL::a root with no `Destroying` signal at all still attaches and detaches",
     "haptics::haptics: a detach is LOCAL::another writer clearing the property does not leave the button unfelt forever",
     "haptics::haptics: an `absent` class is asked exactly once::50 events cost ONE construction attempt, not 50",
     "haptics::haptics: an `absent` class is asked exactly once::...and `blocked` is NOT short-circuited — the two verdicts are not the same fact",
     "haptics::haptics: lifetime::unbind stops delivery and leaves the bus clean",
     "haptics::haptics: lifetime::dispose unsubscribes, releases the pool and switches off",
     "haptics::haptics: lifetime::attachButtons AFTER dispose opens no connection for nothing to close",
     "haptics::haptics: lifetime::bind AFTER dispose opens no subscription for nothing to drain",
     "haptics::haptics: lifetime::binding something that is not a presenter is an authoring error that NAMES the seam",
     "haptics::haptics: the press phase is the ENGINE's, and it is the Facet waveform::an undeclared button gets the Custom `contact` effect, keyed exactly once",
     "haptics::haptics: the press phase is the ENGINE's, and it is the Facet waveform::a control's DECLARED verb still selects its mapped preset, exactly as before",
     "haptics::haptics: the press phase is the ENGINE's, and it is the Facet waveform::the phase waveform is built ONCE however many buttons want it",
     "haptics::haptics: the press phase is the ENGINE's, and it is the Facet waveform::a DISABLED control is skipped, and a control that BECOMES disabled is cleared",
     "haptics::haptics: the press phase is the ENGINE's, and it is the Facet waveform::a remount re-decorates idempotently — no duplicate effect, no second key write",
     "haptics::haptics: a hit expander feels EXACTLY like the control it belongs to::...for every verb an author can declare, including the undeclared default",
     "haptics::haptics: a hit expander feels EXACTLY like the control it belongs to::a `select` control's band is as silent as its face",
     "haptics::haptics: a hit expander feels EXACTLY like the control it belongs to::a DISABLED host silences its band too",
     "haptics::haptics: a hit expander feels EXACTLY like the control it belongs to::...and a host DISABLED AFTER decorate takes its band with it on the next sweep",
     "haptics::haptics: a hit expander feels EXACTLY like the control it belongs to::a TEXT FIELD's band obeys its host too, and its host is not a button at all",
     "haptics::haptics: a hit expander feels EXACTLY like the control it belongs to::an UNMIRRORED expander is the defect, and this is what it looked like",
     "haptics::haptics: the release phase is the COMPLETED edge, and it is new::a completed activation plays `settle` — once, on the pooled release effect",
     "haptics::haptics: the release phase is the COMPLETED edge, and it is new::ONE pulse per cause: fifty completed activations build one effect and play fifty",
     "haptics::haptics: the release phase is the COMPLETED edge, and it is new::A CANCELED PRESS IS SILENT STRUCTURALLY — there is no bus completion to hear",
     "haptics::haptics: the release phase is the COMPLETED edge, and it is new::the bus NEVER replays the down edge: no activation event plays the press effect",
     "haptics::haptics: the release phase is the COMPLETED edge, and it is new::an input-method switch mid-press is at most one press + one release",
     "haptics::haptics: the release phase is the COMPLETED edge, and it is new::a control whose verb the MAP silences is unfelt on BOTH edges",
     "haptics::haptics: the release phase is the COMPLETED edge, and it is new::focus movement alone fires nothing — moving is not activating",
     "haptics::haptics: an activation is the RELEASE phase's, whatever verb it names::a select-declared control's completed press plays `settle`, not `tick`",
     "haptics::haptics: an activation is the RELEASE phase's, whatever verb it names::...and the VALUE CHANGE for the same verb plays `tick` — the two are distinct",
     "haptics::haptics: an activation is the RELEASE phase's, whatever verb it names::a select-declared button is handed NO press effect at all",
     "haptics::haptics: an activation is the RELEASE phase's, whatever verb it names::a `none` control is still unfelt on BOTH edges — silencing did not move",
     "haptics::haptics: press and completed-release in one instant collapse to one sensation::a non-pointer activation plays `settle` ONCE, and nothing else in that instant",
     "haptics::haptics: press and completed-release in one instant collapse to one sensation::the collapse is ONE INSTANT wide — the next frame is a new gesture",
     "haptics::haptics: press and completed-release in one instant collapse to one sensation::it is keyed to the PATH — another control pressed in the same frame is its own gesture",
     "haptics::haptics: press and completed-release in one instant collapse to one sensation::a POINTER press is NOT collapsed — its two moments are genuinely apart",
     "haptics::haptics: press and completed-release in one instant collapse to one sensation::an echo that escaped the presenter's window is collapsed rather than replayed",
     "haptics::haptics: press and completed-release in one instant collapse to one sensation::the window is named, and it is one frame at 60 Hz",
     "haptics::haptics: the select phase, and its leading-edge coalescing::`select` and `adjust` both play the pooled `tick` — one instance for the two",
     "haptics::haptics: the select phase, and its leading-edge coalescing::the two verbs share ONE limiter — a scrubbed picker cannot alternate past it",
     "haptics::haptics: the select phase, and its leading-edge coalescing::the default floor is at least the 50 ms the design asks for",
     "haptics::haptics: the select phase, and its leading-edge coalescing::a hold-to-repeat stepper fires SELECT pulses and never press or release",
     "haptics::haptics: the select phase, and its leading-edge coalescing::a suppressed pulse is DROPPED, never deferred — advance the clock and nothing arrives",
     "haptics::haptics: the select phase, and its leading-edge coalescing::passive pointer hover never reaches the select phase",
     "haptics::haptics: the select phase, and its leading-edge coalescing::a DISCRETE verb is never swallowed by the select limiter",
     "haptics::haptics: a game's own profile, per phase::replacing ONE phase leaves the other two on the Facet defaults",
     "haptics::haptics: a game's own profile, per phase::a phase swapped for a PRESET needs no waveform at all",
     "haptics::haptics: a game's own profile, per phase::a SILENCED phase builds nothing and plays nothing — without touching a control",
     "haptics::haptics: a game's own profile, per phase::silencing PRESS clears the property route and leaves the other phases alone",
     "haptics::haptics: a game's own profile, per phase::an unknown phase name in a game's profile is an authoring error at CONSTRUCTION",
     "haptics::haptics: the FALLBACK when a Custom effect will not build::a client with no KeyInterpolationMode gets the phase's PRESET, and says so",
     "haptics::haptics: the FALLBACK when a Custom effect will not build::under fallback, release and select SHARE UIHover and press keeps UIClick",
     "haptics::haptics: the FALLBACK when a Custom effect will not build::with no fallback available either, the phase is silent and nothing throws",
     "haptics::haptics: the pool is BOUNDED, and the Instances are the evidence::every phase and every verb at once stays far under the platform's 100 cap",
     "haptics::haptics: the pool is BOUNDED, and the Instances are the evidence::teardown destroys every phase effect too — Instances, not the module's opinion",
     "haptics::haptics: the pool is BOUNDED, and the Instances are the evidence::five enable/disable cycles across all three phases leave nothing behind",
     "haptics::haptics: the timing is the CONTROL PATH's, and this module adds no listener::no raw-input API is reached for a press, a release or a select",
     "haptics::haptics: the timing is the CONTROL PATH's, and this module adds no listener::the profile module is engine-free and the adapter is the only file that builds keys",
     "haptics::haptics: the framework cannot reach it::no haptics symbol exists anywhere outside src/client",
     "haptics::haptics: the framework cannot reach it::src/present and src/layout in particular are clean",
     "haptics::haptics: the framework cannot reach it::the shared/server-safe entry point does not export it",
     "haptics::haptics: the framework cannot reach it::the adapter itself calls Play only on a POOLED effect, never on a button's",
     "control_feedback::the per-control declaration is ruled on at the call site::accepts every one of the twelve verbs, plus `none`",
     "control_feedback::the per-control declaration is ruled on at the call site::refuses a verb outside the closed twelve, listing the whole vocabulary",
     "control_feedback::the per-control declaration is ruled on at the call site::refuses a non-string activation, NAMING the type rather than the value",
     "control_feedback::the per-control declaration is ruled on at the call site::refuses MIXING the two forms in one spec — they have different causes",
     "control_feedback::the per-control declaration is ruled on at the call site::refuses a spec that declares NEITHER form",
     "control_feedback::the per-control declaration is ruled on at the call site::refuses a SECOND activation on the same node — one control, one sensation",
     "control_feedback::the per-control declaration is ruled on at the call site::composes with the CHANGE form on the same node — different causes, both kept",
     "control_feedback::the per-control declaration is ruled on at the call site::refuses a structural region, exactly as the change form does",
     "control_feedback::the cascade: nearest declaration wins, and costs nothing when absent::a declaration on the control itself resolves onto its mounted node",
     "control_feedback::the cascade: nearest declaration wins, and costs nothing when absent::INHERITS: a declaration on a container reaches every control inside it",
     "control_feedback::the cascade: nearest declaration wins, and costs nothing when absent::a NEARER declaration overrides a farther one",
     "control_feedback::the cascade: nearest declaration wins, and costs nothing when absent::crosses a structural region: a When branch inherits from outside it",
     "control_feedback::the cascade: nearest declaration wins, and costs nothing when absent::crosses a ForEach: every row inherits, including rows added later",
     "control_feedback::the cascade: nearest declaration wins, and costs nothing when absent::an undeclared tree carries the field NOWHERE — the off path is free",
     "control_feedback::the cascade: nearest declaration wins, and costs nothing when absent::buys NO observer: the control form has no Readable to watch",
     "control_feedback::what a press emits::an undeclared control still emits `activate` — the default is unchanged",
     "control_feedback::what a press emits::a declared control emits ITS verb instead of `activate`",
     "control_feedback::what a press emits::`none` emits NOTHING — the control is deliberately unfelt",
     "control_feedback::what a press emits::the same verb arrives from EVERY input class, not only a pointer",
     "control_feedback::what a press emits::reaches a COMPOSITE control with no per-control plumbing",
     "control_feedback::what a press emits::a disabled declared control still emits nothing",
     "control_feedback::the render seam publishes the resolved verb::hands the adapter the verb for the controls that can be activated",
     "control_feedback::the render seam publishes the resolved verb::does NOT spend the seam on nodes that cannot be activated",
     "control_feedback::the render seam publishes the resolved verb::a recycled instance never inherits the PREVIOUS control's verb",
     "control_feedback::the haptics adapter plays the declared sensation through the engine::an UNDECLARED button gets the PRESS PHASE — Facet's own `contact` waveform",
     "control_feedback::the haptics adapter plays the declared sensation through the engine::a DECLARED button gets the effect its verb maps to, not the generic press",
     "control_feedback::the haptics adapter plays the declared sensation through the engine::`none` leaves the button with NO effect reference at all",
     "control_feedback::the haptics adapter plays the declared sensation through the engine::a verb the MAP deliberately silences is also unfelt",
     "control_feedback::the haptics adapter plays the declared sensation through the engine::effects are POOLED per verb: ten commit buttons share one effect",
     "control_feedback::the haptics adapter plays the declared sensation through the engine::NEVER REPLAYS THE DOWN EDGE: an activation plays the RELEASE phase, not the press",
     "control_feedback::the haptics adapter plays the declared sensation through the engine::...but a GAME-emitted verb of the same name still plays from the bus",
     "control_feedback::the haptics adapter plays the declared sensation through the engine::DEFAULT OFF: a disabled adapter decorates nothing and builds nothing",
     "control_feedback::the haptics adapter plays the declared sensation through the engine::the verb map is TOTAL over the taxonomy, so a thirteenth verb is a visible gap",
     "control_feedback::the haptics adapter plays the declared sensation through the engine::the two ends of the attribute channel spell it the SAME",
     "control_feedback::the haptics adapter plays the declared sensation through the engine::the HIT EXPANDER is handed the same three facts as its host (review F1)",
     "control_feedback::the haptics adapter plays the declared sensation through the engine::the enabled seam mirrors for EVERY host class that can carry an expander",
     "control_feedback::the haptics adapter plays the declared sensation through the engine::a sub-floor control really produces the population F1 is about",
     "control_feedback::the showcase fixture drives the real thing::every phase identity on the page is READ off the adapter, and it is the SHIPPED default",
     "control_feedback::the showcase fixture drives the real thing::pressing the five leaves writes four lines and Mute writes none",
     "control_feedback::the showcase fixture drives the real thing::the CASCADE panel: three controls, one declaration, one verb",
     "control_feedback::the showcase fixture drives the real thing::a NON-activation event on the same surface writes no line",
     "control_feedback::the showcase fixture drives the real thing::a press on ANOTHER surface of the same presenter writes no line",
     "control_feedback::the showcase fixture drives the real thing::no diagnostic, in any package, at any of the three viewports",
     "control_feedback::the showcase fixture drives the real thing::...and no two chips in a row ever overlap, in any package, at any viewport",
     "control_feedback::the sensory-feedback demo installs the real adapter and reports a TRI-STATE::opens ON, installs exactly one bus adapter, and never claims a sensation",
     "control_feedback::the sensory-feedback demo installs the real adapter and reports a TRI-STATE::says nothing, anywhere, about the player FEELING anything",
     "control_feedback::the sensory-feedback demo installs the real adapter and reports a TRI-STATE::the haptics panel is above every control it affects, in RENDERED order",
     "control_feedback::the sensory-feedback demo installs the real adapter and reports a TRI-STATE::the toggle, its status and its explanation are inside the FIRST VIEWPORT, Largest text included",
     "control_feedback::the sensory-feedback demo installs the real adapter and reports a TRI-STATE::a connected pad that answers NO is a platform refusal, and the request STAYS ON",
     "control_feedback::the sensory-feedback demo installs the real adapter and reports a TRI-STATE::NO PAD is NOT a refusal — it is could-not-determine, and the request STAYS ON",
     "control_feedback::the sensory-feedback demo installs the real adapter and reports a TRI-STATE::a supported pad with press effects out says INSTALLED, never PLAYED",
     "control_feedback::the sensory-feedback demo installs the real adapter and reports a TRI-STATE::the OFF line claims nothing whatever about the platform",
     "control_feedback::the sensory-feedback demo installs the real adapter and reports a TRI-STATE::the three shipped waveforms are the Step 13 defaults, key for key",
     "control_feedback::the sensory-feedback demo installs the real adapter and reports a TRI-STATE::a pointer press asks for PRESS then RELEASE, once each and in that order",
     "control_feedback::the sensory-feedback demo installs the real adapter and reports a TRI-STATE::a CANCELLED press is a visible '(no request)', and nothing is asked for",
     "control_feedback::the sensory-feedback demo installs the real adapter and reports a TRI-STATE::the selection tick fires only when the VALUE CHANGES — a re-pick and a hover ask for nothing",
     "control_feedback::the sensory-feedback demo installs the real adapter and reports a TRI-STATE::the CUSTOM row goes through the public profile seam, and resolves against the shipped defaults",
     "control_feedback::the sensory-feedback demo installs the real adapter and reports a TRI-STATE::the override adapter hears NO control on the presenter's bus — no gesture can pulse twice",
     "control_feedback::the sensory-feedback demo installs the real adapter and reports a TRI-STATE::every input path asks for exactly ONE release per activation — pointer, touch, keyboard, gamepad",
     "control_feedback::the sensory-feedback demo installs the real adapter and reports a TRI-STATE::off / on / off / on installs exactly one bus adapter each time, never a second",
     "control_feedback::the sensory-feedback demo installs the real adapter and reports a TRI-STATE::switching it off destroys every effect and takes the claim back",
     "control_feedback::the sensory-feedback demo installs the real adapter and reports a TRI-STATE::a REMOUNT restores the ON default and installs again",
     "control_feedback::the sensory-feedback demo installs the real adapter and reports a TRI-STATE::teardown with the switch ON leaves no adapter, no effect and no connection",
     "control_feedback::the sensory-feedback demo installs the real adapter and reports a TRI-STATE::the history is BOUNDED to ten lines, newest first",
     "control_feedback::the sensory-feedback demo installs the real adapter and reports a TRI-STATE::every step survives the RUNNER'S call shape — session first, payload second",
     "control_feedback::the sensory-feedback demo installs the real adapter and reports a TRI-STATE::a client that cannot build the class is a refusal, and the engine's own words are KEPT",
     "control_feedback::the sensory-feedback demo installs the real adapter and reports a TRI-STATE::a refusal for some OTHER reason is `blocked`, and blocked is could-not-tell",
     "control_feedback::the sensory-feedback demo installs the real adapter and reports a TRI-STATE::an adapter that could not be LOADED is undetermined, not a platform claim",
     "control_feedback::the sensory-feedback demo installs the real adapter and reports a TRI-STATE::the three answers stay THREE: three states, three headlines, no overlap",
     "control_feedback::the sensory-feedback demo installs the real adapter and reports a TRI-STATE::with NOTHING injected it still installs, and names what it could not reach",
     "control_feedback::control feedback: composing the change form and the control form::a tap on a control declaring BOTH raises two causes, and both are felt",
     "control_feedback::control feedback: composing the change form and the control form::...and `activation = \"none\"` beside it is the way to feel only the change",
     "async_completeness::bounded retry: the schedule::a failure retries up to `count` times and then reports failed",
     "async_completeness::bounded retry: the schedule::a retry that succeeds resolves normally, and the failed attempt stays stale",
     "async_completeness::bounded retry: the schedule::a spaced retry is invisible to the transport until its delay has elapsed",
     "async_completeness::bounded retry: the schedule::releasing the last waiter cancels a retry that is still waiting out its delay",
     "async_completeness::bounded retry: the give-up is permanent for the session::a spent budget fails the key, and a later acquire does not start it over",
     "async_completeness::bounded retry: the give-up is permanent for the session::invalidate is the explicit reset",
     "async_completeness::bounded retry: the give-up is permanent for the session::the LEGACY retryAttempts option keeps its old promise: a failed key re-requests",
     "async_completeness::bounded retry: the give-up is permanent for the session::a per-acquire retry overrides the provider's policy for that key",
     "async_completeness::provider construction is strict (F-12)::maxConcurrent = 0 is refused instead of silently wedging every request",
     "async_completeness::provider construction is strict (F-12)::maxConcurrent must be a positive INTEGER",
     "async_completeness::provider construction is strict (F-12)::cacheBudget must be a non-negative integer (0 is legal: cache nothing)",
     "async_completeness::provider construction is strict (F-12)::a negative retry count or delay is reported, not silently discarded",
     "async_completeness::provider construction is strict (F-12)::no opts at all, and an empty opts table, still build the documented defaults",
     "async_completeness::preload: warming a declared set::preloads exactly the keys it was handed, under the same concurrency window",
     "async_completeness::preload: warming a declared set::a released preload skips work that has not started",
     "async_completeness::preload: warming a declared set::a warmed key is already ready when the view finally acquires it — no placeholder",
     "async_completeness::preload: warming a declared set::a live view keeps a preloaded request alive after the warm set is released",
     "async_completeness::preload: warming a declared set::preload never re-opens a cached or given-up key",
     "async_completeness::preload: warming a declared set::preload churn returns the provider's counters to baseline",
     "async_completeness::AsyncImage: silent failure and the dim treatment::failure keeps the placeholder — there is no spinner and no broken glyph",
     "async_completeness::AsyncImage: silent failure and the dim treatment::a retry option reaches the provider for that key alone",
     "async_completeness::AsyncImage: silent failure and the dim treatment::a bound `dimmed` blends the image toward the surface, and back",
     "async_completeness::AsyncImage: silent failure and the dim treatment::an image with no `dimmed` declaration claims no continuous colour at all",
     "interaction_tokens::interaction tokens: promotion thresholds (SF-D3)::ships the ratified per-class values, inside their published ranges",
     "interaction_tokens::interaction tokens: promotion thresholds (SF-D3)::non-pointer classes carry ZERO: an armed drag has no travel to measure",
     "interaction_tokens::interaction tokens: promotion thresholds (SF-D3)::maps engine pointer types to classes, and an unknown type is a pointer",
     "interaction_tokens::interaction tokens: promotion thresholds (SF-D3)::promotion is a MAGNITUDE test, not two axis tests",
     "interaction_tokens::interaction tokens: promotion thresholds (SF-D3)::the same travel is a drag on a mouse and still a tap on a finger",
     "interaction_tokens::interaction tokens: promotion thresholds (SF-D3)::per-call overrides win, and absent keys still fall through to the token",
     "interaction_tokens::interaction tokens: promotion thresholds (SF-D3)::Table's reorder threshold is now the TOKEN, not a private copy",
     "interaction_tokens::interaction tokens: the responder priority bands (framework-gaps-phase2 item 41)::publishes the three bands presenter.luau's own comment states",
     "interaction_tokens::interaction tokens: the responder priority bands (framework-gaps-phase2 item 41)::matches presenter.luau's own literals — a drift here fails loudly instead of shipping quietly wrong",
     "interaction_tokens::interaction tokens: the responder priority bands (framework-gaps-phase2 item 41)::is frozen — a consumer cannot mutate the shared band table",
     "interaction_tokens::drag velocity: the rolling release window (SF-D2)::answers zero until it has two samples",
     "interaction_tokens::drag velocity: the rolling release window (SF-D2)::estimates first-vs-last across the retained window",
     "interaction_tokens::drag velocity: the rolling release window (SF-D2)::drops samples older than the window: a flick after a pause reads as a FLICK",
     "interaction_tokens::drag velocity: the rolling release window (SF-D2)::keeps TWO samples even when both are stale: a paused gesture answers a real zero",
     "interaction_tokens::drag velocity: the rolling release window (SF-D2)::a non-advancing clock is zero, never a division by zero",
     "interaction_tokens::drag velocity: the rolling release window (SF-D2)::refuses non-finite samples rather than poisoning the estimate",
     "interaction_tokens::drag velocity: the rolling release window (SF-D2)::reset clears the buffer (the read order contract: velocity THEN reset)",
     "interaction_tokens::drag velocity: the rolling release window (SF-D2)::honours a custom window and reports it",
     "autoscroll::autoscroll: band membership is the POINTER point::the middle of the viewport is in no band",
     "autoscroll::autoscroll: band membership is the POINTER point::the top band arms upward and the bottom band arms downward",
     "autoscroll::autoscroll: band membership is the POINTER point::a point outside the host horizontally is in no band",
     "autoscroll::autoscroll: band membership is the POINTER point::bands never overlap: a host shorter than two bands splits at the midpoint",
     "autoscroll::autoscroll: dwell is what makes a flick-through safe::does not move before the dwell elapses",
     "autoscroll::autoscroll: dwell is what makes a flick-through safe::jitter INSIDE the band accrues dwell; it never resets it",
     "autoscroll::autoscroll: dwell is what makes a flick-through safe::a flick THROUGH the band never arms",
     "autoscroll::autoscroll: dwell is what makes a flick-through safe::crossing to the other band restarts the dwell",
     "autoscroll::autoscroll: dwell is what makes a flick-through safe::re-entering the band it just left re-pays the dwell",
     "autoscroll::autoscroll: dwell is what makes a flick-through safe::justArmed is true on exactly ONE frame per arming",
     "autoscroll::autoscroll: speed is linear in penetration, behind a start ramp::deeper into the band is faster",
     "autoscroll::autoscroll: speed is linear in penetration, behind a start ramp::the penetration mapping is v = vMin + (vMax - vMin) * p",
     "autoscroll::autoscroll: speed is linear in penetration, behind a start ramp::the start ramp eases from zero over rampS, from the ARMING instant",
     "autoscroll::autoscroll: speed is linear in penetration, behind a start ramp::leaving while active decays to zero over exitEaseS instead of cutting",
     "autoscroll::autoscroll: speed is linear in penetration, behind a start ramp::a DIRECT band cross coasts the old velocity out instead of cutting it (D-11)",
     "autoscroll::autoscroll: speed is linear in penetration, behind a start ramp::arming supersedes a coast rather than adding to it",
     "autoscroll::autoscroll: speed is linear in penetration, behind a start ramp::leaving the band mid-coast keeps decaying instead of re-cutting",
     "autoscroll::autoscroll: the ends of the world::clamps at the canvas end and STAYS armed (no re-paying the dwell)",
     "autoscroll::autoscroll: the ends of the world::never scrolls past zero going the other way",
     "autoscroll::autoscroll: the ends of the world::short content is INERT: bands never arm when there is nothing to scroll",
     "autoscroll::autoscroll: the ends of the world::a nil pointer (no gesture) is the same as leaving the band",
     "autoscroll::autoscroll: the ends of the world::reset returns it to idle and forgets the dwell",
     "autoscroll::autoscroll: the published defaults::ships the ratified numbers, and reports the tuning in force",
     "autoscroll::autoscroll: the published defaults::portrait takes the taller band",
     "autoscroll::autoscroll: the published defaults::garbage options fall back rather than producing a NaN delta",
     "scroll_snap::ADAPT-17 (a): the snap resolver, in numbers::a settle between two items lands on the nearest boundary",
     "scroll_snap::ADAPT-17 (a): the snap resolver, in numbers::a settle already ON a boundary asks for no travel at all",
     "scroll_snap::ADAPT-17 (a): the snap resolver, in numbers::a FLICK advances at least one item even when it died short of the midpoint",
     "scroll_snap::ADAPT-17 (a): the snap resolver, in numbers::a hard fling that already crossed several items is not pulled back to one",
     "scroll_snap::ADAPT-17 (a): the snap resolver, in numbers::a slow drag is not a flick: it falls back where it came from",
     "scroll_snap::ADAPT-17 (a): the snap resolver, in numbers::the END of the list is a resting place, and settling there does not oscillate",
     "scroll_snap::ADAPT-17 (a): the snap resolver, in numbers::content that fits the viewport has exactly one resting place and never moves",
     "scroll_snap::ADAPT-17 (a): the snap resolver, in numbers::an item TALLER than the viewport disables snap for that settle",
     "scroll_snap::ADAPT-17 (a): the snap resolver, in numbers::VARIABLE extents snap to their MEASURED boundaries, from the same index",
     "scroll_snap::ADAPT-17 (a): the snap resolver, in numbers::the uniform and variable indexes answer identically when the extents are equal",
     "scroll_snap::ADAPT-17 (a): the snap resolver, in numbers::keep-visible rounds OUTWARD to a boundary, and leaves an in-band item alone",
     "scroll_snap::ADAPT-17 (a): the snap resolver, in numbers::keep-visible keeps the PLAIN answer for an item taller than the viewport",
     "scroll_snap::ADAPT-17 (b): a list with snap = 'item', driven by the engine::DEFAULT is 'none': a list nobody asked lands exactly where the engine left it",
     "scroll_snap::ADAPT-17 (b): a list with snap = 'item', driven by the engine::settles to the nearest boundary after the gesture stops",
     "scroll_snap::ADAPT-17 (b): a list with snap = 'item', driven by the engine::a FLICK that dies inside the first item still advances one card",
     "scroll_snap::ADAPT-17 (b): a list with snap = 'item', driven by the engine::REDUCED MOTION places the offset with zero travel, on the settling frame",
     "scroll_snap::ADAPT-17 (b): a list with snap = 'item', driven by the engine::...and full motion TRAVELS: the same ten frames leave it still in flight",
     "scroll_snap::ADAPT-17 (b): a list with snap = 'item', driven by the engine::the END of the list holds, with no overscroll oscillation",
     "scroll_snap::ADAPT-17 (b): a list with snap = 'item', driven by the engine::keep-visible lands the focused row ON a boundary",
     "scroll_snap::ADAPT-17 (b): a list with snap = 'item', driven by the engine::a programmatic scrollTo honours snap",
     "scroll_snap::ADAPT-17 (b): a list with snap = 'item', driven by the engine::a row TALLER than the viewport is left where the player parked it",
     "scroll_snap::ADAPT-17 (b): a list with snap = 'item', driven by the engine::VARIABLE extents snap to the boundaries the window itself uses",
     "scroll_snap::ADAPT-17 (b): a list with snap = 'item', driven by the engine::a hand landing on a moving rail wins: the flight is abandoned where the finger is",
     "scroll_snap::ADAPT-17 (b): a list with snap = 'item', driven by the engine::an anchor correction lands where the anchor says, not one card further",
     "scroll_snap::ADAPT-17 (b): a list with snap = 'item', driven by the engine::the LAST PAGE settles to the end when the end is the nearer resting place",
     "scroll_snap::ADAPT-17 (b): a list with snap = 'item', driven by the engine::a decisive drag back from the end still leaves it",
     "scroll_snap::ADAPT-17 (b): a list with snap = 'item', driven by the engine::a flick AFTER a settle is still measured in seconds, not in whole windows",
     "scroll_snap::ADAPT-17 (b): a list with snap = 'item', driven by the engine::a framework write is announced as a REST: it clears the travel it did not make",
     "scroll_snap::ADAPT-17 (b): a list with snap = 'item', driven by the engine::CHURN GUARD: an idle snapping list runs zero per-frame work",
     "scroll_snap::ADAPT-17 (b): a list with snap = 'item', driven by the engine::a snap value the vocabulary does not contain is refused at construction",
     "scroll_snap::ADAPT-17 (c): the same substrate on a VirtualGrid's scroll axis::a grid settles to a LINE boundary",
     "scroll_snap::ADAPT-17 (c): the same substrate on a VirtualGrid's scroll axis::...and a grid nobody asked keeps the engine's own offset",
     "paradigm_cards::ADAPT-17: the card rail's paradigm comes from the facts::COMPACT + TOUCH is one card per view, with a peek of the next",
     "paradigm_cards::ADAPT-17: the card rail's paradigm comes from the facts::REGULAR and WIDE are multi-up rails, and snap is off unless asked",
     "paradigm_cards::ADAPT-17: the card rail's paradigm comes from the facts::the compact rung is TOUCH-PRIMARY, not merely touch-capable (ADAPT-9)",
     "paradigm_cards::ADAPT-17: the card rail's paradigm comes from the facts::the flip is LIVE: the same control changes paradigm when the space does",
     "paradigm_cards::ADAPT-17: the card rail's paradigm comes from the facts::AUTHORED cards-per-view wins, at every width",
     "paradigm_cards::ADAPT-17: the card rail's paradigm comes from the facts::AUTHORED snap wins, in both directions",
     "paradigm_cards::ADAPT-17: the card rail's paradigm comes from the facts::cards.peek and cards.minWidth move the two numbers the facts otherwise pick",
     "paradigm_cards::ADAPT-17: the card rail's paradigm comes from the facts::an adaptive rail with NO environment refuses to construct, naming the fix",
     "paradigm_cards::ADAPT-17: the card rail's paradigm comes from the facts::...but a rail that PINS its arrangement asks the environment nothing",
     "paradigm_cards::ADAPT-17: the card rail's paradigm comes from the facts::`cards` without the paradigm it configures is refused, not ignored",
     "paradigm_cards::ADAPT-17: the shipped card rail switches paradigm with no per-screen code::a compact touch phone gets ONE card and a snap; a desktop gets a multi-up rail",
     "paradigm_cards::ADAPT-17: the shipped card rail switches paradigm with no per-screen code::the fixture holds NO size branch of its own: the source names no breakpoint",
     "paradigm_cards::ADAPT-17: the shipped card rail switches paradigm with no per-screen code::the card still fits its rail at every emulated width, peek included",
     "paradigm_cards::ADAPT-17: no card surface may be silent about compact::every control that accepts `snap` has a declared compact answer",
     "paradigm_cards::ADAPT-17: no card surface may be silent about compact::every declared compact answer is TRUE of the live control",
     "drag_public::public drag: acquisition and promotion (SF-D1, SF-D3)::mounts the declarations: the card acquires a detector, the slots register",
     "drag_public::public drag: acquisition and promotion (SF-D1, SF-D3)::a press under the promotion token never becomes a session (the tap survives)",
     "drag_public::public drag: acquisition and promotion (SF-D1, SF-D3)::past the token the session begins and emits `select`",
     "drag_public::public drag: acquisition and promotion (SF-D1, SF-D3)::touch takes the touch token, not the pointer one",
     "drag_public::public drag: acquisition and promotion (SF-D1, SF-D3)::declineTouch declines the capture so a native scroll host keeps the pan",
     "drag_public::public drag: acquisition and promotion (SF-D1, SF-D3)::falls back to the framework pointer capture when the engine has no detector",
     "drag_public::public drag: hover, legality and the predicted verdict (SF-D1)::enter/leave fire exactly once per boundary",
     "drag_public::public drag: hover, legality and the predicted verdict (SF-D1)::the predicted verdict is readable while hovering, with the game's reason",
     "drag_public::public drag: hover, legality and the predicted verdict (SF-D1)::a legal hover publishes the target and no reason",
     "drag_public::public drag: hover, legality and the predicted verdict (SF-D1)::the same payload is legal for one target and refused by the other",
     "drag_public::public drag: terminals (SF-D1, SF-D5)::a legal drop calls the game's onDrop and emits commit then land",
     "drag_public::public drag: terminals (SF-D1, SF-D5)::an ILLEGAL drop rejects ONCE, carrying the game's reason code",
     "drag_public::public drag: terminals (SF-D1, SF-D5)::a release over NOTHING is the free-area cancel, not a reason-less reject (I-7)",
     "drag_public::public drag: terminals (SF-D1, SF-D5)::cancel fires a leave for the hovered target and emits `cancel`",
     "drag_public::public drag: terminals (SF-D1, SF-D5)::rapid retry never double-fires: a terminal session is inert",
     "drag_public::public drag: geometry stays live (SF-D1, SF-L2)::overlapping targets resolve to the TOPMOST drawn one",
     "drag_public::public drag: geometry stays live (SF-D1, SF-L2)::removing the hovered target mid-session leaves ONCE and clears the verdict (F5)",
     "drag_public::public drag: geometry stays live (SF-D1, SF-L2)::removing the SOURCE mid-session CANCELS it — no orphan ghost, no wedge (F5's mirror)",
     "drag_public::public drag: geometry stays live (SF-D1, SF-L2)::...but a RECYCLED source keeps its session: virtualization is not removal",
     "drag_public::public drag: geometry stays live (SF-D1, SF-L2)::the source contract CARRIES `recycled` — a dropped key would be silent",
     "drag_public::public drag: geometry stays live (SF-D1, SF-L2)::a target inside a SCROLLED host hit-tests where it is DRAWN, not where it solved",
     "drag_public::public drag: hit geometry follows the PAINT (PLAT-4)::a target under a live surface shift hit-tests where it PAINTS",
     "drag_public::public drag: hit geometry follows the PAINT (PLAT-4)::...and a SOLVED coordinate the paint has left behind resolves to nothing",
     "drag_public::public drag: hit geometry follows the PAINT (PLAT-4)::an ANCESTOR's slide accumulates onto its whole subtree",
     "drag_public::public drag: hit geometry follows the PAINT (PLAT-4)::a cleared transform returns the hit rect to the solved one",
     "drag_public::public drag: the non-pointer paradigm (SF-D4)::arm -> navigate -> commit drives the SAME session and the SAME legality",
     "drag_public::public drag: the non-pointer paradigm (SF-D4)::an armed commit on an ILLEGAL target rejects — it does not silently do nothing",
     "drag_public::public drag: the non-pointer paradigm (SF-D4)::cancel disarms and emits `cancel`",
     "drag_public::public drag: the non-pointer paradigm (SF-D4)::armStaging parks the ghost at the PICKUP and the aim never moves it",
     "drag_public::public drag: the non-pointer paradigm (SF-D4)::without armStaging the ghost RIDES the aim (the unchanged default)",
     "drag_public::public drag: the non-pointer paradigm (SF-D4)::a second arm while one is live is refused (one gesture at a time)",
     "drag_public::ADAPT-16: the public drag verb has a pad and keyboard pickup by default::ButtonA on a focused card ARMS it, DPad aims it, ButtonA commits",
     "drag_public::ADAPT-16: the public drag verb has a pad and keyboard pickup by default::Return does the identical thing, because it is the same verb",
     "drag_public::ADAPT-16: the public drag verb has a pad and keyboard pickup by default::ButtonB puts it back down, and the card is not dropped anywhere",
     "drag_public::ADAPT-16: the public drag verb has a pad and keyboard pickup by default::a card with its own onActivate keeps it — the consumer's verb wins",
     "drag_public::public drag: the presentation collaborators::a modal presented mid-drag CANCELS the session (F7)",
     "drag_public::public drag: the presentation collaborators::the proxy layer tracks the pointer 1:1 and is released on the terminal",
     "drag_public::public drag: the presentation collaborators::grabAnchor = 'preserve' keeps the grab-point offset exactly",
     "drag_public::public drag: the presentation collaborators::without a motion clock the terminal resolves instantly instead of flying",
     "drag_public::public drag: the presentation collaborators::with a motion clock the reject flies home, seeded with the release velocity",
     "drag_public::public drag: the presentation collaborators::a NON-gestural cancel seeds ZERO through the same path",
     "drag_public::public drag: authoring errors name the fix::refuses a draggable with no payload",
     "drag_public::public drag: authoring errors name the fix::refuses a drop target with no onDrop",
     "drag_public::public drag: authoring errors name the fix::resolves the motion class at DECLARATION time, not mid-gesture",
     "drag_public::public drag: authoring errors name the fix::refuses an unknown onCommitProxy form",
     "drag_public::public drag: authoring errors name the fix::declarations ride `meta`, so strict prop validation is untouched",
     "drag_public::UI.draggable `enabled` — acquisition refused, the control still speaks (SF-D6)::a refused source ARMS NOTHING on tap — and the tap reaches the consumer instead",
     "drag_public::UI.draggable `enabled` — acquisition refused, the control still speaks (SF-D6)::a refused source does not PROMOTE either, however far the pointer travels",
     "drag_public::UI.draggable `enabled` — acquisition refused, the control still speaks (SF-D6)::the gate is LIVE: a Readable flipping true makes the same tap a pickup",
     "drag_public::UI.draggable `enabled` — acquisition refused, the control still speaks (SF-D6)::absent, nothing changes: the source arms exactly as it always did",
     "drag_public::UI.draggable `enabled` — acquisition refused, the control still speaks (SF-D6)::a non-boolean, non-Readable gate is refused at construction, naming the reason",
     "drag_public::F-18: a drag declaration is refused on a structural node::UI.draggable on a UI.When names the class and the fix",
     "drag_public::F-18: a drag declaration is refused on a structural node::UI.dropTarget on a UI.When is refused the same way",
     "drag_public::F-18: a drag declaration is refused on a structural node::UI.draggable on a UI.ForEach names the class and the fix",
     "drag_public::F-18: a drag declaration is refused on a structural node::UI.dropTarget on a UI.ForEach is refused the same way",
     "drag_public::F-18: a drag declaration is refused on a structural node::UI.draggable on a UI.ErrorBoundary names the class and the fix",
     "drag_public::F-18: a drag declaration is refused on a structural node::UI.dropTarget on a UI.ErrorBoundary is refused the same way",
     "drag_public::F-18: a drag declaration is refused on a structural node::a RENDERED node still takes both declarations",
     "row_actions_state::row_actions_state::axis verdict stays pending under the lock radius",
     "row_actions_state::row_actions_state::ties go vertical (scroll wins ambiguity)",
     "row_actions_state::row_actions_state::drag tracks linearly inside the tray",
     "row_actions_state::row_actions_state::rubber-bands past a non-fullSwipe tray",
     "row_actions_state::row_actions_state::tracks the finger past a fullSwipe tray",
     "row_actions_state::row_actions_state::an absent edge rubber-bands from zero",
     "row_actions_state::row_actions_state::settles open at half tray width",
     "row_actions_state::row_actions_state::settles closed under half tray width",
     "row_actions_state::row_actions_state::velocity projects the release (flick opens from a short drag)",
     "row_actions_state::row_actions_state::commits on full swipe past 60% of row width",
     "row_actions_state::row_actions_state::never commits when fullSwipe is off for that edge",
     "row_actions_state::row_actions_state::never commits when rowWidth is 0 (unmeasured), at any offset — even a huge one",
     "row_actions_state::row_actions_state::never commits when rowWidth is negative (defensive: same guard as zero)",
     "row_actions::RowActions control::validates unknown spec keys",
     "row_actions::RowActions control::rejects an empty action list",
     "row_actions::RowActions control::rejects an unknown ActionSpec key",
     "row_actions::RowActions control::rejects an ActionSpec with an unknown role",
     "row_actions::RowActions control::rejects a coordinator-sharing row with no explicit id",
     "row_actions::RowActions control::a coordinator-sharing row WITH an explicit id builds normally",
     "row_actions::RowActions control::is an inert passthrough with no actions",
     "row_actions::RowActions control::mounts trailing tray buttons with ids Action:<id> only once revealed",
     "row_actions::RowActions control::destructive action paints danger tokens; normal action paints the default role",
     "row_actions::RowActions control::labels survive 1.4x pseudo-loc without clipping (button never narrower than buttonMinWidth)",
     "row_actions::RowActions control::_open slides content by tray width; _close returns it; dump reflects openEdge",
     "row_actions::RowActions control::_open animates offset toward tray width over stepped frames",
     "row_actions::RowActions control::buttons grow proportionally with offset",
     "row_actions::RowActions control::an icon action wears its icon at EVERY width — settled and mid-swipe (icon-first)",
     "row_actions::RowActions control::a label-only action keeps its word — icon-first is a default, not a ban on text",
     "row_actions::RowActions control::reduced motion snaps the reveal in one frame",
     "row_actions::RowActions control::dispose mid-flight leaks nothing",
     "row_actions::RowActions control::dispose() a second time is a silent no-op (task SHOWCASE2 defect B)",
     "row_actions::RowActions control::_settleTo retargets the spring at the verdict (Task 5 wires the gesture that produces one)",
     "row_actions::RowActions control::tray button width re-derives when preferredTextSize changes (not frozen)",
     "row_actions::RowActions control::_open on an edge with no actions is a no-op",
     "row_actions::RowActions control::dispose unmounts and releases the scope",
     "row_actions::RowActions control::RowActions pointer: a tap on a revealed tray action activates it (device-true)",
     "row_actions::RowActions control::RowActions touch: a touch tap on a revealed tray action activates it (device-true)",
     "row_actions::RowActions control::a throwing tray action is contained (not a bare call) and still closes the tray",
     "row_actions::RowActions control::a successful (non-destructive) tray action fires exactly once and closes the tray",
     "row_actions::RowActions control::RowActions keyboard: focus + Return activates a revealed tray action",
     "row_actions::RowActions control::RowActions gamepad: focus + ButtonA activates a revealed tray action",
     "row_actions::RowActions control::RowActions pointer affordance: the pointer structural idiom (hover on a revealed tray action)",
     "row_actions::RowActions control::RowActions touch affordance: the touch structural idiom (44px floor on a revealed tray action)",
     "row_actions::RowActions control::RowActions keyboard affordance: the keyboard structural idiom (focus ring/Navigate/Activate)",
     "row_actions::RowActions control::RowActions gamepad affordance: the gamepad structural idiom (ten-foot focus on a revealed tray action)",
     "row_actions::RowActions control::rejects an editing value that is not a Readable",
     "row_actions::RowActions control::the minus is absent while not editing",
     "row_actions::RowActions control::the minus is absent when the row has no destructive action, even while editing",
     "row_actions::RowActions control::the minus is absent when the caller never wires `editing` (nil, not false)",
     "row_actions::RowActions control::editing shows the minus; tapping it DELETES the row directly (one funnel: commitDestructive)",
     "row_actions::RowActions control::the ruling is about the MINUS only — a swipe still reveals the trailing tray and fires nothing",
     "row_actions::RowActions control::the minus wears the destructive action's own accessible name, and now that name is true",
     "row_actions::RowActions control::editing (leading-only destructive): tapping the minus opens the LEADING tray, not trailing",
     "row_actions::RowActions control::RowActions keyboard: focus + Return activates the minus, which DELETES the row",
     "row_actions::RowActions control::RowActions gamepad: focus + ButtonA activates the minus, which DELETES the row",
     "row_actions::RowActions control::two activations of the minus DELETE EXACTLY ONCE (two independent guards, both required)",
     "row_actions::RowActions control::a full trailing reveal EXPOSES the same tray with or without editing (the gutter is an inset, never a lost reveal)",
     "row_actions::RowActions control::toggling editing off does not force-close an already-open tray (documented behavior)",
     "row_actions::RowActions control::editing shifts Content right by the editAffordance gutter (geometry)",
     "row_actions::RowActions control::leading actions + editing compose: the minus sits outside (before) the leading tray's reveal",
     "row_actions::RowActions control::the minus's own hit target floors at 44px despite its 28px visual diameter",
     "row_actions::RowActions control::the close flight is a rigid translation that decelerates onto the settled geometry (no terminal step)",
     "row_actions::RowActions control::the edit gutter never collapses: the minus stays mounted and the inset holds at EVERY offset",
     "row_actions::RowActions control::the minus paints OVER Content — the director's 'stays pinned during a swipe'",
     "row_actions::RowActions control::adjacent tray buttons carry the theme's trayGap, and the travel distance includes it",
     "row_actions::RowActions control::the compact grammar carries `prefer`, and its absence still means degrade-only",
     "row_actions::device bug round 2026-08-12 — the tray's negative space (findings A1a, A2)::a trailing plate never touches the row: the band carries the theme's rowGutter",
     "row_actions::device bug round 2026-08-12 — the tray's negative space (findings A1a, A2)::...and neither does a leading one, on the mirrored edge",
     "row_actions::device bug round 2026-08-12 — the tray's negative space (findings A1a, A2)::the edit-mode minus sits in the same negative space, not against the row (A2)",
     "row_actions::device bug round 2026-08-12 — a tray label fits the package's frame (finding A3)::every shipped package leaves a tray label the room it measured for it",
     "row_actions::device bug round 2026-08-12 — a tray label fits the package's frame (finding A3)::...and a package with no frame is untouched to the pixel",
     "row_actions::device bug round 2026-08-12 — actions that cannot fit the row (finding A1b)::a band wider than the row shares out the row instead of hanging off its edge",
     "row_actions::device bug round 2026-08-12 — actions that cannot fit the row (finding A1b)::a row with no cage is never clamped to its own stale width (the ratchet)",
     "row_actions::device bug round 2026-08-12 — a letter is not an icon (finding A4)::no framework fallback glyph contains an uppercase letter",
     "row_actions::device bug round 2026-08-12 — a letter is not an icon (finding A4)::a tray plate under a package that declines framework art draws the mark, never a letter",
     "row_actions_dump_seam::the row_actions dump seam: READ == DECLARED == PASSED::every `p.<key>` the module reads is declared in `Params`",
     "row_actions_dump_seam::the row_actions dump seam: READ == DECLARED == PASSED::every key `Params` declares is one the module actually reads",
     "row_actions_dump_seam::the row_actions dump seam: READ == DECLARED == PASSED::the one construction site passes exactly the declared set — no more, no less",
     "row_actions_dump_seam::the row_actions dump seam: READ == DECLARED == PASSED::there is exactly ONE construction site",
     "row_actions_dump_seam::the row_actions dump seam: READ == DECLARED == PASSED::NEGATIVE CONTROL: each set comparison reports the pair that is out of step",
     "row_actions_dump_seam::the row_actions dump seam: the (empty) shared WRITE surface, and the one-way require::build() writes through NO param at all — a value has nothing to write through",
     "row_actions_dump_seam::the row_actions dump seam: the (empty) shared WRITE surface, and the one-way require::NEGATIVE CONTROL: the write scan sees each of the three write shapes",
     "row_actions_dump_seam::the row_actions dump seam: the (empty) shared WRITE surface, and the one-way require::the module offers exactly the one entry point it declares",
     "row_actions_dump_seam::the row_actions dump seam: the (empty) shared WRITE surface, and the one-way require::the module requires the host NOWHERE — the require is one-way",
     "row_capability_optouts::row capability opt-outs — VirtualList::rowSelectable = false STOPS the selection at the funnel, not just at the affordance",
     "row_capability_optouts::row capability opt-outs — VirtualList::rowMovable = false refuses the GRAB — a pinned row never arms, on any input",
     "row_capability_optouts::row capability opt-outs — VirtualList::an absent predicate changes nothing — every row participates, byte-identical to today",
     "row_capability_optouts::row capability opt-outs — VirtualList::a predicate that THROWS is quarantined and fails CLOSED (the row does not participate)",
     "row_capability_optouts::row capability opt-outs — construction refusals (a predicate may only subtract)::rowSelectable on a selection = 'none' list is refused, naming the missing capability",
     "row_capability_optouts::row capability opt-outs — construction refusals (a predicate may only subtract)::rowMovable without onReorder is refused, naming the missing capability",
     "row_capability_optouts::row capability opt-outs — construction refusals (a predicate may only subtract)::rowDeletable without rowActions is refused, naming the missing capability",
     "row_capability_optouts::row capability opt-outs — Table::rowSelectable = false refuses at api.select, in EVERY selection mode path",
     "row_capability_optouts::row capability opt-outs — Table::rowMovable = false refuses at api.moveRow — Table has FIVE reorder entry points",
     "row_capability_optouts::row capability opt-outs — Table::a pinned row is not a DESTINATION either: a movable row cannot displace it",
     "row_capability_optouts::row capability opt-outs — Table::an absent predicate changes nothing on Table either",
     "row_capability_optouts::flow-wrap x virtualization: refused, and the refusal names the conflict::newVirtualList refuses `wrap` and explains the index x itemExtent arithmetic",
     "row_capability_optouts::rowDeletable — Table: a protected row survives ALL FOUR delete routes::ROUTE 1 (full swipe): the protected row refuses to commit; an unprotected one deletes",
     "row_capability_optouts::rowDeletable — Table: a protected row survives ALL FOUR delete routes::ROUTE 1b (revealed tray button): tapping Delete on a protected row fires nothing",
     "row_capability_optouts::rowDeletable — Table: a protected row survives ALL FOUR delete routes::ROUTE 2 (edit-mode ⊖): the minus on a protected row deletes nothing",
     "row_capability_optouts::rowDeletable — Table: a protected row survives ALL FOUR delete routes::ROUTE 3 (Delete key): the keyboard route is refused on a protected row",
     "row_capability_optouts::rowDeletable — Table: a protected row survives ALL FOUR delete routes::ROUTE 4 (action menu): the menu's destructive row is refused too",
     "row_capability_optouts::rowDeletable — Table: a protected row survives ALL FOUR delete routes::an absent rowDeletable deletes through every route, unchanged",
     "row_capability_optouts::rowDeletable — Table: a protected row survives ALL FOUR delete routes::the predicate subtracts DELETE ONLY: a protected row still flags, archives, pins",
     "row_capability_optouts::rowDeletable — hosted VirtualList: the same predicate, the other host::ROUTE 1 (full swipe): a protected row refuses; an unprotected one deletes",
     "row_capability_optouts::rowDeletable — hosted VirtualList: the same predicate, the other host::ROUTE 3 (Delete key): the list's own key surface is refused on a protected row",
     "row_capability_optouts::rowDeletable — hosted VirtualList: the same predicate, the other host::...and the SAME drive on an UNPROTECTED row deletes — the control that makes it evidence",
     "row_capability_optouts::rowDeletable — hosted VirtualList: the same predicate, the other host::ROUTE 4 (action menu): the menu's destructive row is refused too",
     "row_capability_optouts::rowDeletable — hosted VirtualList: the same predicate, the other host::an absent rowDeletable deletes through the hosted funnel, unchanged",
     "row_capabilities_scenario::row_capabilities showcase: the three refusals, through the routes a player takes::rowSelectable: a tap on the LOCKED row leaves the selection where it was",
     "row_capabilities_scenario::row_capabilities showcase: the three refusals, through the routes a player takes::rowMovable: the PINNED row refuses to move, and the order does not change",
     "row_capabilities_scenario::row_capabilities showcase: the three refusals, through the routes a player takes::rowDeletable: swipe the PROTECTED row, press Delete — and it is still there",
     "row_capabilities_scenario::row_capabilities showcase: the refusal is legible, and the PREDICATE is what wrote it::it names the row the FRAMEWORK asked about, not the row the player pressed",
     "row_capabilities_scenario::row_capabilities showcase: the refusal is legible, and the PREDICATE is what wrote it::each of the three refusals writes its OWN question, so the line says which capability said no",
     "row_capabilities_scenario::row_capabilities showcase: the refusal is legible, and the PREDICATE is what wrote it::NEGATIVE CONTROL: nothing a permitted row does ever writes a line or moves the counter",
     "row_capabilities_scenario::row_capabilities showcase: standing rule 4 — registered in BOTH places::both round-3 fixtures are in ORDER and in DEMOS — the direction O-15 fell through",
     "row_capabilities_scenario::row_capabilities showcase: standing rule 4 — registered in BOTH places::...and the always-on sweep names it, so dropping it is a failure and not a smaller green run",
     "row_capabilities_scenario::row_capabilities showcase: it holds under every shipped package::no finding of its OWN in any package at any of the three viewports",
     "row_capabilities_scenario::row_capabilities showcase: the copy survives a ~1.4x pseudo-localization::every player-visible string expands, and nothing this fixture owns overflows",
     "row_actions_input::RowActions pointer gesture (Task 5: axis lock, drag, flick, commit)::horizontal drag reveals; release past half-tray snaps open",
     "row_actions_input::RowActions pointer gesture (Task 5: axis lock, drag, flick, commit)::release under half-tray snaps closed",
     "row_actions_input::RowActions pointer gesture (Task 5: axis lock, drag, flick, commit)::vertical drag never moves the offset (axis lock)",
     "row_actions_input::RowActions pointer gesture (Task 5: axis lock, drag, flick, commit)::a leftward flick (velocity) opens from a short drag",
     "row_actions_input::RowActions pointer gesture (Task 5: axis lock, drag, flick, commit)::down/move/up with ZERO ticks between (no syncGeometry ever ran): a real drag still opens",
     "row_actions_input::RowActions pointer gesture (Task 5: axis lock, drag, flick, commit)::full swipe commits (destructive): row slides off, collapses, onAction fires exactly once",
     "row_actions_input::RowActions pointer gesture (Task 5: axis lock, drag, flick, commit)::a re-swipe the instant a commit clears cannot fire the action twice",
     "row_actions_input::RowActions pointer gesture (Task 5: axis lock, drag, flick, commit)::full swipe commits (non-destructive): fires once, springs back to CLOSED — no offscreen slide, no height change",
     "row_actions_input::RowActions pointer gesture (Task 5: axis lock, drag, flick, commit)::full-swipe commit (destructive) reaches the OFFSCREEN target, not just the tray width (regression: syncGeometry re-aim)",
     "row_actions_input::RowActions pointer gesture (Task 5: axis lock, drag, flick, commit)::a throwing destructive commit still returns the (un-removed) row to full height, closed, trays unmounted",
     "row_actions_input::RowActions pointer gesture (Task 5: axis lock, drag, flick, commit)::a successful destructive commit ALSO recovers when the owner keeps the row alive: offset 0, full height, and a later commit is reachable again",
     "row_actions_input::RowActions pointer gesture (Task 5: axis lock, drag, flick, commit)::commit with a throwing onAction still closes and does not refire",
     "row_actions_input::RowActions pointer gesture (Task 5: axis lock, drag, flick, commit)::fullSwipe = { trailing = false } refuses to commit on trailing",
     "row_actions_input::RowActions pointer gesture (Task 5: axis lock, drag, flick, commit)::_commitFirst('trailing') fires the first trailing action once",
     "row_actions_input::RowActions pointer gesture (Task 5: axis lock, drag, flick, commit)::a SECOND (and third) short swipe on the same row also snaps back to closed",
     "row_actions_input::RowActions pointer gesture (Task 5: axis lock, drag, flick, commit)::a CANCELLED drag on a row that has already been opened once springs back too",
     "row_actions_input::RowActions pointer gesture (Task 5: axis lock, drag, flick, commit)::pointer cancel mid-drag springs back to closed",
     "row_actions_input::RowActions pointer gesture (Task 5: axis lock, drag, flick, commit)::a pen press at the hit surface declines the capture (not mouse, not touch)",
     "row_actions_input::RowActions pointer gesture (Task 5: axis lock, drag, flick, commit)::re-grab mid snap-open flight: syncGeometry never fights a live drag (regression)",
     "row_actions_input::RowActions pointer gesture (Task 5: axis lock, drag, flick, commit)::flick momentum reaches 90% of target in fewer stepped frames than a calm release",
     "row_actions_input::RowActions pointer gesture (Task 5: axis lock, drag, flick, commit)::dispose mid-slide-off never fires onAction and never errors; a normal commit still fires once",
     "row_actions_input::RowActions touch gesture (Task 6: touch accepted, same axis-lock dance as mouse)::TOUCH: horizontal drag reveals; release past half-tray snaps open",
     "row_actions_input::RowActions touch gesture (Task 6: touch accepted, same axis-lock dance as mouse)::TOUCH: release under half-tray snaps closed",
     "row_actions_input::RowActions touch gesture (Task 6: touch accepted, same axis-lock dance as mouse)::TOUCH: vertical drag never moves the offset (axis lock, same inert dance as mouse)",
     "row_actions_input::RowActions touch gesture (Task 6: touch accepted, same axis-lock dance as mouse)::TOUCH: a leftward flick (velocity) opens from a short drag",
     "row_actions_input::RowActions touch gesture (Task 6: touch accepted, same axis-lock dance as mouse)::TOUCH: full swipe commits (destructive): row slides off, collapses, onAction fires exactly once",
     "row_actions_input::RowActions touch gesture (Task 6: touch accepted, same axis-lock dance as mouse)::TOUCH: fullSwipe = { trailing = false } refuses to commit on trailing",
     "row_actions_input::RowActions touch gesture (Task 6: touch accepted, same axis-lock dance as mouse)::hotSwitch: a touch press arriving mid-mouse-drag is declined; the mouse drag CARRIES uninterrupted",
     "row_actions_input::RowActions touch gesture (Task 6: touch accepted, same axis-lock dance as mouse)::hotSwitch: a mouse press arriving mid-touch-drag is declined; the touch drag CARRIES uninterrupted",
     "row_actions_input::RowActions touch gesture (Task 6: touch accepted, same axis-lock dance as mouse)::hotSwitch: touch pointer cancel mid-drag springs back to closed (capability lost => CANCEL)",
     "row_actions_input::RowActions Task 7: open-state coordinator + outside-tap/scroll dismissal::opening row B closes row A (one-open policy)",
     "row_actions_input::RowActions Task 7: open-state coordinator + outside-tap/scroll dismissal::row B starting a horizontal gesture closes row A's OPEN MENU too, not just its tray",
     "row_actions_input::RowActions Task 7: open-state coordinator + outside-tap/scroll dismissal::scroll movement closes the open row (fire the observeScroll callback)",
     "row_actions_input::RowActions Task 7: open-state coordinator + outside-tap/scroll dismissal::outside tap closes AND the tapped control still receives its press (does-not-swallow-unrelated-clicks)",
     "row_actions_input::RowActions Task 7: open-state coordinator + outside-tap/scroll dismissal::Cancel (handleCancel) closes an open row and reports handled",
     "row_actions_input::RowActions Task 7: open-state coordinator + outside-tap/scroll dismissal::a row unmounting while open clears its coordinator entry",
     "row_actions_input::RowActions Task 7: open-state coordinator + outside-tap/scroll dismissal::a VStack of wrapped rows sharing newRowActionsCoordinator gets one-open + scroll-close + no-swallow behavior",
     "row_actions_input::RowActions Task 8: keyboard Delete::Delete on a focused row fires the first destructive action",
     "row_actions_input::RowActions Task 8: keyboard Delete::Backspace does the same as Delete",
     "row_actions_input::RowActions Task 8: keyboard Delete::Delete searches trailing before leading, and finds the destructive action even when it is not first in its edge's list",
     "row_actions_input::RowActions Task 8: keyboard Delete::Delete does nothing when no destructive action exists anywhere — and never registers a binding (the key reaches a sibling consumer untouched)",
     "row_actions_input::RowActions Task 8: keyboard Delete::Delete does nothing while focus is outside this row (scoped to the content subtree)",
     "row_actions_input::RowActions Task 8: the action menu — WHERE it lands::THE CASE: a trigger near the bottom would put the menu PAST the viewport",
     "row_actions_input::RowActions Task 8: the action menu — WHERE it lands::...so it FLIPS above the trigger, wholly inside the viewport",
     "row_actions_input::RowActions Task 8: the action menu — WHERE it lands::a trigger with room BELOW still opens below it — the flip is conditional",
     "row_actions_input::RowActions Task 8: the action menu — WHERE it lands::the predicted height the flip DECIDES on is the height the solver PAINTS",
     "row_actions_input::RowActions Task 8: the action menu — WHERE it lands::NEGATIVE CONTROL: with no room ABOVE either, it stays BELOW rather than flipping",
     "row_actions_input::RowActions Task 8: the action menu — WHERE it lands::a standalone row with no `env` cannot flip, and says so by not flipping",
     "row_actions_input::RowActions Task 8: the action menu — WHERE it lands::the menu is placed at its TRIGGER, not at the origin of its own surface",
     "row_actions_input::RowActions Task 8: the action menu (ButtonX)::ButtonX opens the menu, entering focus on its first row; Activate on a non-destructive item runs it exactly once and closes",
     "row_actions_input::RowActions Task 8: the action menu (ButtonX)::lists every action, leading then trailing, in document order",
     "row_actions_input::RowActions Task 8: the action menu (ButtonX)::a destructive action from the menu runs the full commit sequence (slide-off + collapse), not a bare callback",
     "row_actions_input::RowActions Task 8: the action menu (ButtonX)::Cancel (gamepad ButtonB) closes the menu without firing anything, and restores focus",
     "row_actions_input::RowActions Task 8: the action menu (ButtonX)::menu traps focus while open (transientScope): Navigate cannot escape into content or a sibling",
     "row_actions_input::RowActions Task 8: the action menu (ButtonX)::toggling the menu 10 times leaks nothing",
     "row_actions_input::RowActions Task 8: the action menu (ButtonX)::Delete while the menu is open does not bypass it — the open menu owns input",
     "row_actions_input::RowActions Task 8: the action menu (ButtonX)::a full-swipe commit under an OPEN MENU on the SAME row closes the menu first, not stranding it",
     "row_actions_input::RowActions Task 8: the action menu (ButtonX)::two rows on one screen: focusing row A and pressing Delete commits only A (the scope actually gates, not merely exists)",
     "row_actions_input::RowActions Task 8: the action menu (ButtonX)::popup parity: a tap OUTSIDE the open menu on a sibling closes the menu AND swallows the press (opposite of the tray's own no-swallow)",
     "row_actions_input::RowActions Task 8: the action menu (ButtonX)::menu row labels survive 1.4x pseudo-loc without wrapping or clipping (fill-inside-a-hugging-parent widens instead)",
     "row_actions_input::RowActions Task 8: the action menu (ButtonX)::the same action wears its ICON in the tray and its WORD in the menu row",
     "row_actions_input::RowActions Task 8b: the action menu (Shift+Return)::Shift+Return opens the menu, entering focus on its first row — identical outcome to ButtonX",
     "row_actions_input::RowActions Task 8b: the action menu (Shift+Return)::Shift+Return while the menu is open closes it (toggle, no distinct close key needed)",
     "row_actions_input::RowActions Task 8b: the action menu (Shift+Return)::Shift+Return fires ONLY the menu toggle — the row content's own onActivate does not also fire (no double-fire)",
     "row_actions_input::RowActions Task 8b: the action menu (Shift+Return)::plain Return still activates the focused row content normally — byte-identical to before Task 8b",
     "row_actions_input::RowActions Task 8b: the action menu (Shift+Return)::holding plain Return, tapping shift mid-hold, then releasing it does not swallow the NEXT plain Return activation",
     "row_actions_input::RowActions Task 8b: the action menu (Shift+Return)::Delete while the menu is open (opened via Shift+Return) stays inert — same carve-out as the ButtonX path",
     "row_actions_input::RowActions Task 8b: the action menu (Shift+Return)::opening via Shift+Return and closing via ButtonX (and vice versa) both work — one action, two bindings",
     "row_actions_input::RowActions redteam item 2: the menu contributes zero to row/list measure::opening row A's menu leaves row B's solved rect (y, x, height) byte-identical — VStack list",
     "row_actions_input::RowActions redteam item 2: the menu contributes zero to row/list measure::wrapper dispose while its menu is open dismisses the menu's own surface too — no leak",
     "row_actions_input::row_actions.composeWithReorder (Task 10)::no reorderHandlers: returns rowActionsHandlers unchanged (pass-through, zero overhead)",
     "row_actions_input::row_actions.composeWithReorder (Task 10)::touch: routed to rowActions from onPointerDown, every subsequent event too — reorder never sees it",
     "row_actions_input::row_actions.composeWithReorder (Task 10)::mouse vertical: replays the down at the ORIGIN into reorder; rowActions never sees this gesture at all",
     "row_actions_input::row_actions.composeWithReorder (Task 10)::mouse horizontal: rowActions owns the gesture; reorder's rowDrag is never seeded",
     "row_actions_input::row_actions.composeWithReorder (Task 10)::ties go vertical, matching row_actions_state.axisVerdict's own rule",
     "row_actions_input::row_actions.composeWithReorder (Task 10)::a tap that never crosses the lock radius calls neither engine's down, and up is a no-op for both",
     "row_actions_input::row_actions.composeWithReorder (Task 10)::cancel mid-vertical-drag forwards to reorder's own onPointerCancel only",
     "row_actions_input::row_actions.composeWithReorder (Task 10)::a second down while one is pending declines (never steals a live capture)",
     "row_actions_input::row_actions.composeWithReorder (Task 10)::a slow creep then a two-frame flick settles the SAME way (open, not commit) through the composed path as the direct path",
     "row_actions_input::RowActions final fix wave (redteam item 1, BLOCKER): irrevocable commit::probe B: close() mid-commit does not cancel — fires exactly once and the row still completes its collapse (not stuck at offset 0)",
     "row_actions_input::RowActions final fix wave (redteam item 1, BLOCKER): irrevocable commit::probe C: a new gesture cannot start mid-commit — pointerDown declines, exactly one fire",
     "row_actions_input::RowActions final fix wave (redteam item 1, BLOCKER): irrevocable commit::a sibling row starting a gesture (coordinator claim) during A's commit: A's commit completes, fires exactly once",
     "row_actions_input::RowActions final fix wave (redteam item 1, BLOCKER): irrevocable commit::scroll movement during A's commit: A's commit completes, fires exactly once",
     "row_actions_input::RowActions final fix wave (redteam item 1, BLOCKER): irrevocable commit::an outside tap during A's commit: A's commit completes, fires exactly once, and the tapped control still activates (round-7 no-swallow, unaffected)",
     "row_actions_input::RowActions final fix wave (redteam item 7, MINOR): passive-surface catcher consume::outside tap on the catcher over a passive HUD: tray closes, HUD is NOT engaged, gameplay input is still delivered",
     "row_actions_input::RowActions ruling 6: the Grip is on top, and forwards what it does not claim::the Grip is painted ABOVE the button in the content — the half the engine reads",
     "row_actions_input::RowActions ruling 6: the Grip is on top, and forwards what it does not claim::a SWIPE that lands on the Grip over a button opens the tray — the reported defect",
     "row_actions_input::RowActions ruling 6: the Grip is on top, and forwards what it does not claim::...and a TAP on the same spot still activates the button underneath",
     "row_actions_input::RowActions ruling 6: the Grip is on top, and forwards what it does not claim::a tap that misses every button forwards to nothing, silently",
     "row_actions_input::RowActions ruling 6: the Grip is on top, and forwards what it does not claim::a VERTICAL drag is declined, and a declined scroll activates nothing",
     "row_actions_input::RowActions ruling 6: the Grip is on top, and forwards what it does not claim::the forwarded tap is scoped to THIS row: it can never reach a sibling",
     "row_actions_input::RowActions ruling 6: the Grip is on top, and forwards what it does not claim::...and `skip` is what stops the Grip forwarding the press back to ITSELF",
     "row_actions_input::RowActions ruling 6: the Grip is on top, and forwards what it does not claim::a POINTER-ZONE control under the Grip gets its press too, not just Buttons",
     "row_actions_hosted::RowActions hosted mode (Task 2): buildHosted — the engine with no blueprint::mounts NOTHING: the hosted engine carries no blueprint at all",
     "row_actions_hosted::RowActions hosted mode (Task 2): buildHosted — the engine with no blueprint::(a) a horizontal drag past the axis lock slides monotonically and reveals the trailing tray",
     "row_actions_hosted::RowActions hosted mode (Task 2): buildHosted — the engine with no blueprint::(b) releasing past the open threshold leaves the row open at the tray width",
     "row_actions_hosted::RowActions hosted mode (Task 2): buildHosted — the engine with no blueprint::(c) a full swipe past the commit threshold fires the first trailing action exactly once",
     "row_actions_hosted::RowActions hosted mode (Task 2): buildHosted — the engine with no blueprint::(d) close() slides back to 0 and clears trayState",
     "row_actions_hosted::RowActions hosted mode (Task 2): buildHosted — the engine with no blueprint::(e) two hosted engines share one coordinator: opening B closes A",
     "row_actions_hosted::RowActions hosted mode (Task 2): buildHosted — the engine with no blueprint::(f) a vertical drag never slides (axis lock: scrolling wins an ambiguous gesture)",
     "row_actions_hosted::RowActions hosted mode (Task 2): buildHosted — the engine with no blueprint::refuses the spec keys a hosted row cannot have (closed spec, constitution §4)",
     "row_actions_hosted::RowActions hosted mode (Task 2): buildHosted — the engine with no blueprint::requires `paths.content` and `onSlide` — the two things only the host can supply",
     "row_actions_hosted::RowActions hosted mode (Task 2): buildHosted — the engine with no blueprint::a leading tray reveals from the other edge through the same engine",
     "row_actions_hosted::RowActions hosted mode (Task 2): buildHosted — the engine with no blueprint::(finding 1) refuses trailing actions with no paths.trayTrailing, by name",
     "row_actions_hosted::RowActions hosted mode (Task 2): buildHosted — the engine with no blueprint::(finding 1) refuses leading actions with no paths.trayLeading, by name",
     "row_actions_hosted::RowActions hosted mode (Task 2): buildHosted — the engine with no blueprint::(finding 2) a focused hosted tray button counts as inside the row, keeping the keys context enabled",
     "row_actions_hosted::RowActions hosted mode (Task 2): buildHosted — the engine with no blueprint::(finding 3) forwards outsideDismiss — active tracks isOpen, dismiss is close, never consumed",
     "row_actions_hosted::RowActions hosted mode (Task 2): buildHosted — the engine with no blueprint::(finding 4) an ordinary close with nothing to collapse sends no spurious onSlide/onCommitHeight",
     "row_actions_hosted::RowActions hosted mode (Task 2): buildHosted — the engine with no blueprint::(finding 4) a real close still forwards the settle's own onSlide(0) — the gate is on VALUE, not on close()",
     "row_actions_hosted::RowActions hosted mode (Task 2): buildHosted — the engine with no blueprint::(finding 5) labels a hosted action-spec error with the hosted entry point, not the standalone one",
     "row_actions_hosted::RowActions hosted mode (Task 2): buildHosted — the engine with no blueprint::(finding 5) labels a hosted coordinator-type error with the hosted entry point too",
     "row_actions_hosted::RowActions hosted mode: `editing` (Table-hosting order, stage 1)::accepts `editing` — it is no longer refused from the hosted spec by name",
     "row_actions_hosted::RowActions hosted mode: `editing` (Table-hosting order, stage 1)::hands the host an edit-affordance VIEW and a gutter Readable, and nothing to a row without both halves",
     "row_actions_hosted::RowActions hosted mode: `editing` (Table-hosting order, stage 1)::routes an Activate on the host's declared minus path into commitDestructive",
     "row_actions_hosted::RowActions hosted mode: `editing` (Table-hosting order, stage 1)::does not answer an Activate on some OTHER path that merely ends in the same node ids",
     "row_actions_hosted::RowActions hosted mode: `editing` (Table-hosting order, stage 1)::refuses a hosted row that can show the minus but named no path for it",
     "row_actions_hosted::RowActions hosted mode: `editing` (Table-hosting order, stage 1)::still refuses `editing` that is not a Readable, with the hosted label",
     "row_actions_hosted::RowActions hosted mode: `editing` (Table-hosting order, stage 1)::the gutter it hands the host is the theme's own editAffordance + rowGutter, on the leading edge too",
     "row_actions_scenario::row_actions scenario: it builds and mounts clean::mounts headlessly with no core error and empty diagnostics",
     "row_actions_scenario::row_actions scenario: it builds and mounts clean::opens on the list surface; the table pane is not mounted at all (lazy UI.When)",
     "row_actions_scenario::row_actions scenario: it builds and mounts clean::every seed row mounts as a wrapped RowActions node with a unique id",
     "row_actions_scenario::row_actions scenario: it builds and mounts clean::registers every step the device matrix drives, by name",
     "row_actions_scenario::row_actions scenario: list surface (hand-wrapped ScrollView>VStack)::_open reveals the trailing tray; dump reports openEdge = trailing",
     "row_actions_scenario::row_actions scenario: list surface (hand-wrapped ScrollView>VStack)::close returns an open row to closed",
     "row_actions_scenario::row_actions scenario: list surface (hand-wrapped ScrollView>VStack)::commit on trailing fires Delete (the first trailing action) and the row leaves the model",
     "row_actions_scenario::row_actions scenario: list surface (hand-wrapped ScrollView>VStack)::the deletion's re-flow animates: withAnimation records are live at the removal",
     "row_actions_scenario::row_actions scenario: list surface (hand-wrapped ScrollView>VStack)::under reduced motion the same deletion installs no records, is CALLED, and does not refuse",
     "row_actions_scenario::row_actions scenario: list surface (hand-wrapped ScrollView>VStack)::commit on leading fires Mark Read (the first leading action), row survives",
     "row_actions_scenario::row_actions scenario: list surface (hand-wrapped ScrollView>VStack)::opening one row closes another (shared newRowActionsCoordinator, one-open policy)",
     "row_actions_scenario::row_actions scenario: list surface (hand-wrapped ScrollView>VStack)::keyboard Delete on a focused row fires the destructive action end to end",
     "row_actions_scenario::row_actions scenario: list surface (hand-wrapped ScrollView>VStack)::Shift+Return opens the action menu on a focused row",
     "row_actions_scenario::row_actions scenario: list surface (hand-wrapped ScrollView>VStack)::RULING 6: the mail row's content is a Button, and the row still swipes AND still taps",
     "row_actions_scenario::row_actions scenario: list surface (hand-wrapped ScrollView>VStack)::bindScroll wires the coordinator's scroll-close after present",
     "row_actions_scenario::row_actions scenario: table surface (reorderable, editable, rowActions)::switching to table mode mounts the Table and unmounts the list",
     "row_actions_scenario::row_actions scenario: table surface (reorderable, editable, rowActions)::the table surface's onPrimaryAction opens a row on a double-click, and one click only selects",
     "row_actions_scenario::row_actions scenario: table surface (reorderable, editable, rowActions)::Edit toggle flips the editing signal and the report reflects it",
     "row_actions_scenario::row_actions scenario: table surface (reorderable, editable, rowActions)::the editing step drives the same signal Table reads for its edit-mode minus",
     "row_actions_scenario::row_actions scenario: table surface (reorderable, editable, rowActions)::THE PAD REACHES THE EDIT-MODE MINUS on the showcase surface, and it deletes the row",
     "row_actions_scenario::row_actions scenario: table surface (reorderable, editable, rowActions)::rows still reorder via mouse drag: a vertical drag on the row moves it, not the tray",
     "row_actions_scenario::row_actions scenario: table surface (reorderable, editable, rowActions)::a horizontal drag on the same table row reveals its tray instead of reordering",
     "row_actions_scenario::row_actions scenario: table surface (reorderable, editable, rowActions)::RowActions touch: a swipe reveals the tray on a table NOT in edit mode and survives its release Activate (after)",
     "row_actions_scenario::row_actions scenario: table surface (reorderable, editable, rowActions)::RowActions touch: a swipe reveals the tray on a table NOT in edit mode and survives its release Activate (before)",
     "row_actions_scenario::row_actions scenario: table surface (reorderable, editable, rowActions)::RowActions mouse: a swipe reveals the tray on a table NOT in edit mode and survives its release Activate (after)",
     "row_actions_scenario::row_actions scenario: table surface (reorderable, editable, rowActions)::RowActions mouse: a swipe reveals the tray on a table NOT in edit mode and survives its release Activate (before)",
     "row_actions_scenario::row_actions scenario: table surface (reorderable, editable, rowActions)::RowActions: a swipe whose release produces no Activate does not swallow the NEXT tap",
     "row_actions_scenario::row_actions scenario: table surface (reorderable, editable, rowActions)::RowActions: the swallowed activate is the swipe's OWN — the next tap still closes the tray",
     "row_actions_scenario::row_actions scenario: virtual list surface (hosted newVirtualList.rowActions)::switching to vlist mode mounts the VirtualList and unmounts the other two surfaces",
     "row_actions_scenario::row_actions scenario: virtual list surface (hosted newVirtualList.rowActions)::a closed row holds no gesture engine (hosted mode's own laziness guarantee)",
     "row_actions_scenario::row_actions scenario: virtual list surface (hosted newVirtualList.rowActions)::a horizontal drag on a row reveals its trailing tray; a full swipe commits Delete end to end",
     "row_actions_scenario::row_actions scenario: virtual list surface (hosted newVirtualList.rowActions)::KEYBOARD DELETE on a row nobody has swiped removes it, building exactly one engine",
     "row_actions_scenario::row_actions scenario: virtual list surface (hosted newVirtualList.rowActions)::GAMEPAD ButtonX on an unswiped row opens its action menu (the pad's route to Flag/Mark Read)",
     "row_actions_scenario::row_actions scenario: virtual list surface (hosted newVirtualList.rowActions)::dispose() tears the hosted list down cleanly (engine, overlay and slide transform all live at teardown)",
     "row_actions_scenario::row_actions scenario: the showcase picker's fixture ctx (demo catalogue entry)::builds, presents, ticks and disposes under the picker's ctx",
     "row_actions_scenario::row_actions scenario: a short landscape spends its height on the list (B5)::✓ the heading is dropped on a short viewport and kept on a tall one",
     "row_actions_scenario::row_actions scenario: a short landscape spends its height on the list (B5)::✓ list: the first mail row is on screen, and nothing overflows",
     "row_actions_scenario::row_actions scenario: a short landscape spends its height on the list (B5)::✓ table: the first mail row is on screen, and nothing overflows",
     "row_actions_scenario::row_actions scenario: a short landscape spends its height on the list (B5)::✓ vlist: the first mail row is on screen, and nothing overflows",
     "row_actions_motion::RowActions flight — C9a: the tray is UNCOVERED by the row, never revealed ahead of it::a programmatic open never paints more band than the row has travelled",
     "row_actions_motion::RowActions flight — C9a: the tray is UNCOVERED by the row, never revealed ahead of it::a live drag never paints more band than the finger has dragged",
     "row_actions_motion::RowActions flight — C9a: the tray is UNCOVERED by the row, never revealed ahead of it::a drag paints the same band the settled row does at the same distance",
     "row_actions_motion::RowActions flight — C9b: the reveal ends when the row arrives, not when the spring's epsilon does::the tray unmounts within a frame of the row arriving, not 35 frames later",
     "row_actions_motion::RowActions flight — C9c/C9d: a leading reveal TRANSLATES the row, it does not squeeze it::a leading reveal leaves the row's content box exactly as wide as it was",
     "row_actions_motion::RowActions flight — C9c/C9d: a leading reveal TRANSLATES the row, it does not squeeze it::a trailing reveal is unchanged (the edge that never had the defect)",
     "row_actions_motion::RowActions flight — C8a: a destructive commit leaves nothing standing behind it::nothing is painted where the row was once the commit has carried it off",
     "row_actions_motion::RowActions flight — C8a: a destructive commit leaves nothing standing behind it::the row is never repainted at full height on its way home after the action fires",
     "row_actions_motion::RowActions flight — the commit's own handoff (director device round 2026-08-13)::the collapse begins as the slide ARRIVES, not when it numerically settles",
     "row_actions_motion::RowActions flight — the commit's own handoff (director device round 2026-08-13)::the action still fires exactly once, and the row still ends up gone",
     "row_actions_motion::RowActions flight — reduced motion::an open lands instantly, with the band fully out on the frame after the mount",
     "row_actions_motion::RowActions flight — reduced motion::a destructive commit under reduced motion leaves nothing standing either",
     "row_actions_motion::RowActions flight — the plate's DECORATION never paints past the band::no plate's decoration is ever wider than the plate, on any frame of open or close",
     "focus_skip::focus-skip: the live predicate (SF-L3)::bare string entries stay eligible forever (every pre-existing caller)",
     "focus_skip::focus-skip: the live predicate (SF-L3)::navigation skips a false predicate",
     "focus_skip::focus-skip: the live predicate (SF-L3)::the predicate is re-read every navigation, never cached",
     "focus_skip::focus-skip: the live predicate (SF-L3)::entry focus lands on the first ELIGIBLE node",
     "focus_skip::focus-skip: the live predicate (SF-L3)::a ring with nothing eligible leaves focus where it stands",
     "focus_skip::focus-skip: the live predicate (SF-L3)::skipping never turns a step into a jump: a ±1 press advances ONE eligible slot",
     "focus_skip::focus-skip: the live predicate (SF-L3)::pointer focus REFUSES an ineligible node — but that is not refusing the tap",
     "focus_skip::focus-skip: grouped scopes (SF-L3)::steps along the axis skipping ineligible entries",
     "focus_skip::focus-skip: grouped scopes (SF-L3)::wrap finds the next eligible entry around the ring",
     "focus_skip::focus-skip: grouped scopes (SF-L3)::a group with NO eligible entries is transparent to navigation",
     "focus_skip::focus-skip: grouped scopes (SF-L3)::entry = \"nearest\" lands on the nearest ELIGIBLE index",
     "focus_skip::focus-skip: grouped scopes (SF-L3)::entry = \"restore\" ignores a remembered node that is no longer eligible",
     "focus_skip::focus-skip: the active-interaction exemption (SF-L3, binding)::the interaction's target keeps its focusability while its predicate says no",
     "focus_skip::focus-skip: the active-interaction exemption (SF-L3, binding)::the exemption MOVES with the target: the graph holds exactly one",
     "focus_skip::focus-skip: the active-interaction exemption (SF-L3, binding)::removing the exempt node clears the exemption (it cannot protect a ghost)",
     "focus_skip::focus-skip: the active-interaction exemption (SF-L3, binding)::an exempt node is still focusable through the pointer path",
     "focus_skip::focus-skip: structural updates carry predicates::setOrder replaces the predicate map with the new order's",
     "focus_skip::focus-skip: structural updates carry predicates::setGroupOrder replaces one group's predicates and lands on an eligible fallback",
     "focus_skip::focus-skip: structural updates carry predicates::replaceGroups normalizes predicates on the new map",
     "focus_skip::focus-skip: structural updates carry predicates::a removed node's predicate is dropped with it (no stale entry for a reused id)",
     "collection_list::unified collection: selection (SF-L1)::selection defaults to \"none\": activation never selects",
     "collection_list::unified collection: selection (SF-L1)::single selection: an Activate from ANY paradigm selects the row",
     "collection_list::unified collection: selection (SF-L1)::selection PRUNES with the data: a removed row is never a phantom selection",
     "collection_list::unified collection: selection (SF-L1)::selection survives a re-sort that keeps the row",
     "collection_list::unified collection: selection (SF-L1)::clearSelection empties it",
     "collection_list::unified collection: selection (SF-L1)::the SELECTED row declares the native state, so the theme paints it (R2-F6)",
     "collection_list::unified collection: selection (SF-L1)::selectionPaint='none' keeps the SELECTION and drops only the PAINT",
     "collection_list::unified collection: selection (SF-L1)::selectionPaint refuses an unknown value (constitution §4)",
     "collection_list::unified collection: selection (SF-L1)::a list with selection OFF declares nothing: those rows are unchanged",
     "collection_list::unified collection: pointer reorder over the public contract (SF-L1)::a row lifts past the promotion token and reorders on release",
     "collection_list::unified collection: pointer reorder over the public contract (SF-L1)::a drop that reproduces the current order emits NOTHING",
     "collection_list::unified collection: pointer reorder over the public contract (SF-L1)::an upward move lands above the row it was dropped on",
     "collection_list::unified collection: pointer reorder over the public contract (SF-L1)::the drop line tracks the insertion point through the PRESENTATION channel",
     "collection_list::unified collection: pointer reorder over the public contract (SF-L1)::touch DECLINES the row body so the native pan keeps scrolling",
     "collection_list::unified collection: pointer reorder over the public contract (SF-L1)::a plain (non-reorderable) list declares no drag sources at all",
     "collection_list::unified collection: the armed (non-pointer) reorder (SF-D4)::Activate arms, Navigate moves the predicted slot, Activate commits",
     "collection_list::unified collection: the armed (non-pointer) reorder (SF-D4)::Cancel disarms without reordering",
     "collection_list::unified collection: the armed (non-pointer) reorder (SF-D4)::toggleGrab is the dedicated verb: grab then commit, same session",
     "collection_list::unified collection: the armed (non-pointer) reorder (SF-D4)::the armed navigation SCROLLS the list, so rows past the window are reachable",
     "collection_list::unified collection: rows as drop targets (SF-L1)::a foreign payload lands on the row the game asked for",
     "collection_list::unified collection: rows as drop targets (SF-L1)::the game's refusal reason reaches the verdict and the reject event",
     "collection_list::unified collection: rows as drop targets (SF-L1)::a reorder payload from ANOTHER list is refused",
     "collection_list::unified collection: rows as drop targets (SF-L1)::the hit-test is over VISIBLE rows only: an unmounted row is not a target",
     "collection_list::unified collection: edge autoscroll wired to the host (SF-L2)::dwelling at the bottom edge arms, scrolls, and re-resolves the verdict IN THE SAME frame",
     "collection_list::unified collection: edge autoscroll wired to the host (SF-L2)::short content is inert: a list with nothing to scroll never arms",
     "collection_list::unified collection: edge autoscroll wired to the host (SF-L2)::a NON-pointer (armed) session never autoscrolls: focus-follow already does",
     "collection_list::unified collection: edge autoscroll wired to the host (SF-L2)::with no session at all the model is reset, not left armed",
     "collection_list::unified collection: edge autoscroll wired to the host (SF-L2)::autoscroll is off by default on a plain list (no drag, no bands)",
     "collection_list::unified collection: identity and focus under live churn (SF-L1)::a re-sort MID-DRAG keeps the session, the payload and mounted identity",
     "collection_list::unified collection: identity and focus under live churn (SF-L1)::selection and focus survive a re-sort during a drag",
     "collection_list::unified collection: identity and focus under live churn (SF-L1)::removing the HOVERED row mid-drag clears the verdict and keeps the session (F5)",
     "collection_list::unified collection: identity and focus under live churn (SF-L1)::reorder motion slides the moved rows through presentation, never a re-solve",
     "collection_list::unified collection: focus-skip on rows (SF-L3)::an ineligible row is skipped by navigation but still activates on a tap",
     "collection_list::unified collection: focus-skip on rows (SF-L3)::the predicate flips mid-navigation and the ring follows on the NEXT press",
     "collection_list::unified collection: focus-skip on rows (SF-L3)::the row a live session is aiming at never loses focusability mid-gesture",
     "collection_list::unified collection: uniform derived row height (finding F13)::accepts a Readable row height and re-derives the whole list from it",
     "collection_list::unified collection: uniform derived row height (finding F13)::accepts a Readable VIEWPORT height, so a rotation re-windows in place (R2-F7)",
     "collection_list::unified collection: uniform derived row height (finding F13)::ACCEPTS a per-row height, and windows on the running offsets it implies",
     "collection_list::unified collection: teardown::build/mount/reorder/drop/dispose returns the core to baseline",
     "collection_list::unified collection: the control dump (CTRL-08b)::list.dump() is deterministic and carries its schema string",
     "sponsor_scenarios::sponsor fixtures: long-lived-core churn (RR-8)::sponsor_motion: three build/dispose cycles in one core leave zero residue",
     "sponsor_scenarios::sponsor fixtures: long-lived-core churn (RR-8)::sponsor_drop: three build/dispose cycles in one core leave zero residue",
     "sponsor_scenarios::sponsor fixtures: long-lived-core churn (RR-8)::sponsor_avatars: three build/dispose cycles in one core leave zero residue",
     "sponsor_scenarios::sponsor fixtures: the module contract::every scenario is registered in the gallery registry, in order",
     "sponsor_scenarios::sponsor fixtures: the module contract::every scenario exports title, ledgerRows and build",
     "sponsor_scenarios::sponsor fixtures: the module contract::every scenario mounts its screen through the real presenter",
     "sponsor_scenarios::sponsor fixtures: the module contract::every step §8 names exists and is drivable by name",
     "sponsor_scenarios::sponsor fixtures: the module contract::every report carries its ledger key and the fixture's own counters",
     "sponsor_scenarios::sponsor fixtures: 20 mount/reset/dispose cycles return the core to baseline (SF-C1)::sponsor_motion leaves no signals, memos, observers or scopes behind",
     "sponsor_scenarios::sponsor fixtures: 20 mount/reset/dispose cycles return the core to baseline (SF-C1)::sponsor_celebration leaves no signals, memos, observers or scopes behind",
     "sponsor_scenarios::sponsor fixtures: 20 mount/reset/dispose cycles return the core to baseline (SF-C1)::sponsor_list leaves no signals, memos, observers or scopes behind",
     "sponsor_scenarios::sponsor fixtures: 20 mount/reset/dispose cycles return the core to baseline (SF-C1)::sponsor_drop leaves no signals, memos, observers or scopes behind",
     "sponsor_scenarios::sponsor fixtures: 20 mount/reset/dispose cycles return the core to baseline (SF-C1)::sponsor_toast leaves no signals, memos, observers or scopes behind",
     "sponsor_scenarios::sponsor fixtures: 20 mount/reset/dispose cycles return the core to baseline (SF-C1)::sponsor_avatars leaves no signals, memos, observers or scopes behind",
     "sponsor_scenarios::sponsor fixtures: 20 mount/reset/dispose cycles return the core to baseline (SF-C1)::sponsor_markers leaves no signals, memos, observers or scopes behind",
     "sponsor_scenarios::sponsor fixtures: 20 mount/reset/dispose cycles return the core to baseline (SF-C1)::sponsor_billboard leaves no signals, memos, observers or scopes behind",
     "sponsor_scenarios::sponsor_motion (SF-M1/M2/M3/M5/M7/M8)::a flight arrives on the perceptual radius and emits `arrive` exactly once",
     "sponsor_scenarios::sponsor_motion (SF-M1/M2/M3/M5/M7/M8)::a target that MOVES mid-flight is chased: the puck lands on the live rect (SF-M2)",
     "sponsor_scenarios::sponsor_motion (SF-M1/M2/M3/M5/M7/M8)::a seeded release velocity opens the flight moving (SF-D2's handoff, read as motion)",
     "sponsor_scenarios::sponsor_motion (SF-M1/M2/M3/M5/M7/M8)::`reward` is the one class that overshoots, and it settles back on its target",
     "sponsor_scenarios::sponsor_motion (SF-M1/M2/M3/M5/M7/M8)::a re-hit mid-decay RE-SETS the wash instead of ratcheting it down",
     "sponsor_scenarios::sponsor_motion (SF-M1/M2/M3/M5/M7/M8)::the panel region reverses inside one settle (no restart, no finish-then-reopen)",
     "sponsor_scenarios::sponsor_motion (SF-M1/M2/M3/M5/M7/M8)::the stress scene runs 20 concurrent springs and RESTS at zero cost (SF-M8)",
     "sponsor_scenarios::sponsor_motion (SF-M1/M2/M3/M5/M7/M8)::under reduced motion the SAME events fire and the terminal states match (SF-M5)",
     "sponsor_scenarios::sponsor_celebration (SF-M4)::a clean run fires four beats in order and completes once",
     "sponsor_scenarios::sponsor_celebration (SF-M4)::interrupting at EVERY boundary leaves the stage terminal on the interrupt frame",
     "sponsor_scenarios::sponsor_celebration (SF-M4)::an interrupt MID-beat lands every remaining terminal and nothing persists half-painted",
     "sponsor_scenarios::sponsor_celebration (SF-M4)::skip delivers the remaining content instantly and reports `skip`",
     "sponsor_scenarios::sponsor_celebration (SF-M4)::replay restarts from a clean stage with no orphan state",
     "sponsor_scenarios::sponsor_celebration (SF-M4)::under reduced motion every beat fires immediately, in order, with `reduced`",
     "sponsor_scenarios::sponsor_list (SF-L1/L3/P1/P3)::the row height is DERIVED from the theme snapshot, never typed",
     "sponsor_scenarios::sponsor_list (SF-L1/L3/P1/P3)::the window RE-DERIVES on a live rotation, with no reset (R2-F7)",
     "sponsor_scenarios::sponsor_list (SF-L1/L3/P1/P3)::selection is single and survives a live re-sort (SF-L1)",
     "sponsor_scenarios::sponsor_list (SF-L1/L3/P1/P3)::churn never remounts a row: one cell factory run per key (SF-L1's no-blink)",
     "sponsor_scenarios::sponsor_list (SF-L1/L3/P1/P3)::navigation SKIPS ineligible rows and never lands on one (SF-L3)",
     "sponsor_scenarios::sponsor_list (SF-L1/L3/P1/P3)::the armed row keeps its focusability while the interaction lives (SF-L3's exemption)",
     "sponsor_scenarios::sponsor_list (SF-L1/L3/P1/P3)::the wash is a role BLEND on its own node, and the card keeps its state fill (SF-P1)",
     "sponsor_scenarios::sponsor_list (SF-L1/L3/P1/P3)::the stroke pulse rides ONE bound prop and leaves nothing behind (SF-P3)",
     "sponsor_scenarios::sponsor_list (SF-L1/L3/P1/P3)::an ineligible row is skipped for navigation and still answers Activate (I-9)",
     "sponsor_scenarios::sponsor_drop (SF-D1..D5, SF-L2, SF-P4, SF-M2)::a release under the promotion gate starts NO session (SF-D3)",
     "sponsor_scenarios::sponsor_drop (SF-D1..D5, SF-L2, SF-P4, SF-M2)::promotion opens one session, publishes a legal verdict and mounts the ghost (SF-D1/P4)",
     "sponsor_scenarios::sponsor_drop (SF-D1..D5, SF-L2, SF-P4, SF-M2)::a legal drop commits on the causal frame and LANDS after the flight (SF-M2/M3)",
     "sponsor_scenarios::sponsor_drop (SF-D1..D5, SF-L2, SF-P4, SF-M2)::the RR-default commit is INSTANT: land on the release frame, wash on the row",
     "sponsor_scenarios::sponsor_drop (SF-D1..D5, SF-L2, SF-P4, SF-M2)::an illegal drop rejects EXACTLY once, with the fixture's reason code (SF-D5)",
     "sponsor_scenarios::sponsor_drop (SF-D1..D5, SF-L2, SF-P4, SF-M2)::edge autoscroll dwells, ramps, clamps, and keeps the verdict fresh EVERY frame (SF-L2)",
     "sponsor_scenarios::sponsor_drop (SF-D1..D5, SF-L2, SF-P4, SF-M2)::short content never arms and shows no affordance (the honest-affordance rule)",
     "sponsor_scenarios::sponsor_drop (SF-D1..D5, SF-L2, SF-P4, SF-M2)::a re-sort mid-drag retargets the verdict on the SAME frame (SF-L1)",
     "sponsor_scenarios::sponsor_drop (SF-D1..D5, SF-L2, SF-P4, SF-M2)::the non-pointer path drives ONE session to both terminals (SF-D4)",
     "sponsor_scenarios::sponsor_drop (SF-D1..D5, SF-L2, SF-P4, SF-M2)::DEVICE-DRIVEN armed flow: ring rides the aim, ghost hops, A commits on the aimed row",
     "sponsor_scenarios::sponsor_drop (SF-D1..D5, SF-L2, SF-P4, SF-M2)::DEVICE-DRIVEN cancel and re-arm: B ends the session, the next A starts clean",
     "sponsor_scenarios::sponsor_drop (SF-D1..D5, SF-L2, SF-P4, SF-M2)::the hand row navigates HORIZONTALLY: right walks Card A -> B -> C",
     "sponsor_scenarios::sponsor_drop (SF-D1..D5, SF-L2, SF-P4, SF-M2)::arming PRESENTS: the source shows it and focus moves into the list (R2-F4)",
     "sponsor_scenarios::sponsor_drop (SF-D1..D5, SF-L2, SF-P4, SF-M2)::...and the armed card carries the selected state WHILE it is armed (R2-F4)",
     "sponsor_scenarios::sponsor_drop (SF-D1..D5, SF-L2, SF-P4, SF-M2)::the chevron hides at a CLAMPED canvas end and fades between states (R2-F8)",
     "sponsor_scenarios::sponsor_drop (SF-D1..D5, SF-L2, SF-P4, SF-M2)::all three cancel affordances end the session (I-7)",
     "sponsor_scenarios::sponsor_drop (SF-D1..D5, SF-L2, SF-P4, SF-M2)::a modal presented mid-drag cancels the session (finding F7)",
     "sponsor_scenarios::sponsor_toast (SF-T1/T2/T3/M6)::a toast never joins the stack and never moves focus (SF-T1)",
     "sponsor_scenarios::sponsor_toast (SF-T1/T2/T3/M6)::the control beneath a live toast still activates (SF-T1's whole point)",
     "sponsor_scenarios::sponsor_toast (SF-T1/T2/T3/M6)::a burst shows three, queues the rest, and drops at the cap WITH a reason (SF-T2)",
     "sponsor_scenarios::sponsor_toast (SF-T1/T2/T3/M6)::priority never truncates a showing toast's read floor (finding F2)",
     "sponsor_scenarios::sponsor_toast (SF-T1/T2/T3/M6)::a visible toast PAINTS: body spans the row and the title has real width",
     "sponsor_scenarios::sponsor_toast (SF-T1/T2/T3/M6)::a same-subject toast replaces in place (supersede, not churn)",
     "sponsor_scenarios::sponsor_toast (SF-T1/T2/T3/M6)::re-presenting a subject mid-exit reverses instead of finishing (SF-M6)",
     "sponsor_scenarios::sponsor_toast (SF-T1/T2/T3/M6)::the OTHER edge is drivable: a bottom-docked strip, on a fresh layer (R2-F10)",
     "sponsor_scenarios::sponsor_toast (SF-T1/T2/T3/M6)::dismissal never defers past the flat 500 ms cap, and the counters come back",
     "sponsor_scenarios::sponsor_toast (SF-T1/T2/T3/M6)::under reduced motion the same toasts appear for the same durations (SF-T3)",
     "sponsor_scenarios::sponsor_avatars (SF-A1..A4, SF-P2)::a staggered load ends with every avatar ready",
     "sponsor_scenarios::sponsor_avatars (SF-A1..A4, SF-P2)::failure keeps the placeholder and reports `failed` for instrumentation only (SF-A1)",
     "sponsor_scenarios::sponsor_avatars (SF-A1..A4, SF-P2)::retry spends its budget a second apart and then gives up for the session (SF-A2)",
     "sponsor_scenarios::sponsor_avatars (SF-A1..A4, SF-P2)::a completion for a released row is STALE and never paints (SF-A2's churn clause)",
     "sponsor_scenarios::sponsor_avatars (SF-A1..A4, SF-P2)::preload warms a declared set so a debuting composite skips the placeholder (SF-A3)",
     "sponsor_scenarios::sponsor_avatars (SF-A1..A4, SF-P2)::the dim treatment rides the authored tint channel, both ends themable (SF-P2)",
     "sponsor_scenarios::sponsor_avatars (SF-A1..A4, SF-P2)::the live-transport step degrades honestly with no engine seam (SF-A4 stays E3)",
     "sponsor_scenarios::sponsor_markers (SF-P5)::twelve markers track u,v for a thousand updates with ZERO remounts",
     "sponsor_scenarios::sponsor_markers (SF-P5)::a simultaneous burst is arrange-only: no mount, no remount",
     "sponsor_scenarios::sponsor_markers (SF-P5)::keyed enter/exit leaves every survivor's identity untouched",
     "sponsor_scenarios::sponsor_markers (SF-P5)::u,v = 0 and 1 put a marker's CENTRE exactly on the map's corners",
     "sponsor_scenarios::sponsor_markers (SF-P5)::marker #12 carries the DECLARED theming-exempt hue, and nothing else does",
     "sponsor_scenarios::sponsor_billboard (SF-W1)::the world-anchored steps degrade honestly when the engine seam is absent",
     "sponsor_scenarios::sponsor_billboard (SF-W1)::the depleting ring is an INFORMATIONAL timer that reaches empty on wall clock",
     "sponsor_scenarios::sponsor_billboard (SF-W1)::under reduced motion the ring still empties at the SAME wall-clock moment (F10)",
     "sponsor_scenarios::sponsor_billboard (SF-W1)::the render-vs-record predicate is pure, and recording never stops with rendering",
     "sponsor_scenarios::sponsor_billboard (SF-W1)::the omen unmounts and remounts with the scope, and the value keeps its terminus",
     "band_policy::§9: `bandSafeContent` is the policy that spends `platformChrome.bandInsets`::the renderer accepts it, and names it among the valid values when a typo arrives",
     "band_policy::§9: `bandSafeContent` is the policy that spends `platformChrome.bandInsets`::its content rect starts where the BAND starts, not below the platform's whole row",
     "band_policy::§9: `bandSafeContent` is the policy that spends `platformChrome.bandInsets`::with NO band reported its TOP is `deviceSafeContent`'s — no branch for a consumer",
     "band_policy::§9: `bandSafeContent` floors its other three edges at the theme's own gutter::a platform reporting NO lateral inset still leaves the theme's gutter",
     "band_policy::§9: `bandSafeContent` floors its other three edges at the theme's own gutter::a REAL inset bigger than the gutter still wins — the floor is a floor, not a replacement",
     "band_policy::§9: `bandSafeContent` floors its other three edges at the theme's own gutter::THE OTHER TWO CONTENT POLICIES ARE UNTOUCHED, and that is a DEFERRAL rather than a decision",
     "band_policy::§9: `bandSafeContent` floors its other three edges at the theme's own gutter::CONTROL: `edgeToEdge` is EXEMPT — a scrim still reaches the glass on all four sides",
     "band_policy::§9: the `topbar` zone is inset to `platformChrome.band`, by the framework::the row sits INSIDE the free strip — level with the platform's buttons, never on them",
     "band_policy::§9: the `topbar` zone is inset to `platformChrome.band`, by the framework::...and the lanes start BELOW the band, so the top-left cluster is clear by construction",
     "band_policy::§9: the `topbar` zone is inset to `platformChrome.band`, by the framework::a composition that declares NO topbar region reserves the platform's whole row anyway",
     "band_policy::§9: the `topbar` zone is inset to `platformChrome.band`, by the framework::a DECLARED topbar region on a platform reporting NO strip is NOT zero-width",
     "band_policy::§9: the `topbar` zone is inset to `platformChrome.band`, by the framework::the RESERVE still floors the lanes on a sentinel band with exclusions declared and an active topbar row",
     "composition::the arrangement is chosen from the BOX, on both axes::a short wide offer resolves the richest arrangement (landscape phone)",
     "composition::the arrangement is chosen from the BOX, on both axes::a tall narrow offer falls to the column, and SAYS why the lanes lost",
     "composition::the arrangement is chosen from the BOX, on both axes::a large offer stays on the richest arrangement (desktop)",
     "composition::the arrangement is chosen from the BOX, on both axes::the choice follows the OFFER, not a viewport: the same width decides differently by height",
     "composition::the arrangement is chosen from the BOX, on both axes::Q9: with twoLane withheld, an offer that fails threeLane falls to the column",
     "composition::the arrangement is chosen from the BOX, on both axes::when NO candidate is legal the LAST one shows, flagged as the declared fallback",
     "composition::step down before dropping, both in descending rank::a lane that overflows steps its LEAST important region down first",
     "composition::step down before dropping, both in descending rank::squeezing further walks UP the rank order, one region at a time",
     "composition::step down before dropping, both in descending rank::NOTHING is dropped while any region in the failing lane can still step down",
     "composition::step down before dropping, both in descending rank::only then are regions dropped, still in descending rank",
     "composition::step down before dropping, both in descending rank::a RANK TIE breaks by declaration order — the later-declared steps down first",
     "composition::step down before dropping, both in descending rank::a region with ONE form cannot step down, so rank 1 keeps its plate",
     "composition::step down before dropping, both in descending rank::a step-down that cannot relieve the failure is not taken (it would only cheapen the screen)",
     "composition::step down before dropping, both in descending rank::a lane PINNED by a region that cannot narrow gives up nothing at all",
     "composition::step down before dropping, both in descending rank::...but two regions that can BOTH give way still do, one at a time",
     "composition::floors are content, and absolute::a `lines` floor resolves through the live theme's type role, CEILED as the solve is",
     "composition::floors are content, and absolute::a `targets` floor resolves through the theme's minimum touch target",
     "composition::floors are content, and absolute::both stated takes the LARGER — a floor is a floor",
     "composition::floors are content, and absolute::a bigger type scale raises the floor by itself, with no author involvement",
     "composition::floors are content, and absolute::no declared floor means the minimum-viable form's own measure",
     "composition::floors are content, and absolute::the FILL region never sinks below its floor — the lane is refused instead",
     "composition::slack flows to fill; reserved holds; empty is absent::every pixel left over goes to the fill region, on both axes",
     "composition::slack flows to fill; reserved holds; empty is absent::a reserved region holds its box when its content rests — neighbours do not move",
     "composition::slack flows to fill; reserved holds; empty is absent::a reserved region is never dropped, however tight the offer",
     "composition::slack flows to fill; reserved holds; empty is absent::a NON-reserved region whose content resolves to nothing is not mounted and costs no gap",
     "composition::slack flows to fill; reserved holds; empty is absent::maxMeasure caps both axes and centres the result",
     "composition::rule 9 (DV6-2): an all-empty lane COLLAPSES and gives up its width::measured empty -> collapsed=true",
     "composition::rule 9 (DV6-2): an all-empty lane COLLAPSES and gives up its width::dropped by rule 3 -> collapsed=true",
     "composition::rule 9 (DV6-2): an all-empty lane COLLAPSES and gives up its width::reserved, schedule OVER (the quiet round) -> collapsed=true",
     "composition::rule 9 (DV6-2): an all-empty lane COLLAPSES and gives up its width::reserved, schedule RUNNING — unchanged -> collapsed=false",
     "composition::rule 9 (DV6-2): an all-empty lane COLLAPSES and gives up its width::one region still paintable beside an empty one -> collapsed=false",
     "composition::rule 9 (DV6-2): an all-empty lane COLLAPSES and gives up its width::empty + dropped + released-reserve together -> collapsed=true",
     "composition::rule 9 (DV6-2): an all-empty lane COLLAPSES and gives up its width::the released width AND its lane gap go to the fill lane, to the pixel",
     "composition::rule 9 (DV6-2): an all-empty lane COLLAPSES and gives up its width::with NO fill lane the release is simply not spent — the composition measures narrower",
     "composition::rule 9 (DV6-2): an all-empty lane COLLAPSES and gives up its width::MID-SEQUENCE STABILITY survives the release: an ACTIVE reserve holds its box between pieces",
     "composition::rule 9 (DV6-2): an all-empty lane COLLAPSES and gives up its width::the dump carries `collapsed`, and every lane a reader already saw is unchanged",
     "composition::rule 9 (DV6-2): an all-empty lane COLLAPSES and gives up its width::releasing the FLAG is not enough: a form that paints unconditionally is never empty",
     "composition::rule 9 (DV6-2): an all-empty lane COLLAPSES and gives up its width::a `reserved` that is not a boolean is refused where it is written",
     "composition::2026-07-31: a group may SPAN the composition as its own row::the spanning row is full width, above the lanes, in EVERY arrangement",
     "composition::2026-07-31: a group may SPAN the composition as its own row::a `below` row closes the flow, under the lane band",
     "composition::2026-07-31: a group may SPAN the composition as its own row::the row takes its height from the LANES' budget — it is not a free band",
     "composition::2026-07-31: a group may SPAN the composition as its own row::an EMPTY spanning row is absent, and the lanes get every pixel back",
     "composition::2026-07-31: a group may SPAN the composition as its own row::a spanning region steps down and drops by RANK, like any other",
     "composition::2026-07-31: a group may SPAN the composition as its own row::a spanning row never narrows a lane, so a WIDTH failure never degrades it",
     "composition::2026-07-31: a group may SPAN the composition as its own row::a hug-width composition still MEASURES its content, not the offer it spans",
     "composition::2026-07-31: a group may SPAN the composition as its own row::the dump carries the row, and a declaration WITHOUT one is byte-unchanged",
     "composition::2026-07-31: a group may SPAN the composition as its own row::a declaration that both rides a lane and spans is refused where it is written",
     "composition::2026-07-31: a group may SPAN the composition as its own row::a spanning group needs NO home in any arrangement — that is the point",
     "composition::exactly one scroll region::a second mayScroll region is refused at DECLARATION, not resolved around",
     "composition::exactly one scroll region::the resolution names the one scroller when it is mounted",
     "composition::exactly one scroll region::a lane that overflows with no scroller in it makes the arrangement illegal",
     "composition::a bad declaration is refused where it is written::an unknown region field",
     "composition::a bad declaration is refused where it is written::an unknown group field",
     "composition::a bad declaration is refused where it is written::an unknown floor key",
     "composition::a bad declaration is refused where it is written::a floor that is a pixel count in disguise",
     "composition::a bad declaration is refused where it is written::a region naming a group that does not exist",
     "composition::a bad declaration is refused where it is written::a duplicate region id",
     "composition::a bad declaration is refused where it is written::a non-integer rank",
     "composition::a bad declaration is refused where it is written::a region with no forms",
     "composition::a bad declaration is refused where it is written::reserved AND mayDrop together",
     "composition::a bad declaration is refused where it is written::an unknown arrangement name",
     "composition::a bad declaration is refused where it is written::a group with no home in some arrangement — the silent-disappearance case",
     "composition::a bad declaration is refused where it is written::a custom arrangement with no lanes",
     "composition::the resolution is deterministic and dumpable::two resolutions of the same offer are byte-identical dumps",
     "composition::the resolution is deterministic and dumpable::the dump carries WHICH arrangement, WHICH form, and WHY each loser lost",
     "composition::the resolution is deterministic and dumpable::regions appear in DECLARATION order, whatever the arrangement",
     "composition::UI.Composition, mounted::places the lanes side by side on a short wide offer",
     "composition::UI.Composition, mounted::stacks them down a tall narrow offer, same tree",
     "composition::UI.Composition, mounted::the losing form keeps its mount, gets a zero rect, and is marked hidden",
     "composition::UI.Composition, mounted::a dropped region's whole subtree leaves the focus ring",
     "composition::UI.Composition, mounted::the resolution is readable from the controller, with the losing candidate's rule",
     "composition::UI.Composition, mounted::an AXIS FLIP re-solves in place: zero factory reruns, zero creates, zero removes",
     "composition::UI.Composition, mounted::a region that dropped and comes back keeps its identity",
     "composition::UI.Composition, mounted::DV6-2: a live `reserved` releases the whole lane, as a RE-SOLVE not a rebuild",
     "composition::UI.Composition, mounted::a SPANNING row mounts as a full-width band above the lanes, and flips with them",
     "composition::UI.Composition, mounted::a Composition child that is not a Region is refused at construction",
     "composition::UI.Composition, mounted::DV6-2: a LIVE `reserved` is still a reservation at construction",
     "composition::UI.Composition, mounted::a Region without an explicit id is refused (the dump has to be stable)",
     "composition::UI.Composition, mounted::a group minWidth may be a theme METRIC NAME, resolved every solve",
     "composition::UI.Composition, mounted::a group's own GAP may be a spacing step, and it reaches the solver as pixels",
     "composition::scenario 'composition': the fixture the device matrix drives::mounts headlessly and resolves an arrangement with no core error",
     "composition::scenario 'composition': the fixture the device matrix drives::every step the driver names exists and lands its declared state",
     "composition::scenario 'composition': the fixture the device matrix drives::the five device offers resolve the arrangements the design intends",
     "composition::scenario 'composition': the fixture the device matrix drives::the short offer STEPS DOWN and the big one does not — same declaration",
     "composition::scenario 'composition': the fixture the device matrix drives::exactly one region scrolls, in every offer",
     "composition::scenario 'composition': the fixture the device matrix drives::a rested transient does not move the region under it (reserved holds its box)",
     "composition::scenario 'composition': the fixture the device matrix drives::an emptied region is ABSENT and its space goes to the field",
     "composition::scenario 'composition': the fixture the device matrix drives::the SPANNING caption is a full-width band in every offer, and free when empty",
     "composition::scenario 'composition': the fixture the device matrix drives::a live axis flip is a RE-SOLVE: state survives and nothing is rebuilt",
     "composition::scenario 'composition': the fixture the device matrix drives::the report carries the resolution, so a device round can read the WHY",
     "composition::scenario 'composition': the fixture the device matrix drives::twenty offer changes leave the registry and the core exactly where they started",
     "composition::composition — arrangement eligibility (2026-08-02)::an ineligible candidate is SKIPPED even where it would have fitted",
     "composition::composition — arrangement eligibility (2026-08-02)::being skipped is NOT losing: the resolution stays legal and reports why",
     "composition::composition — arrangement eligibility (2026-08-02)::the LAST candidate is never gated out — a screen always resolves",
     "composition::composition — arrangement eligibility (2026-08-02)::absent `eligible` is the pre-existing behaviour, byte for byte",
     "composition::composition — arrangement eligibility (2026-08-02)::rejects a non-boolean gate at normalization, naming the arrangement",
     "composition::composition — hug-lane overflow is reported, not silent::names the region whose form filled the offer inside a hug lane",
     "composition::composition — hug-lane overflow is reported, not silent::a well-formed declaration reports NOTHING — the check is not noise",
     "composition::composition — hug-lane overflow is reported, not silent::it is a REPORT, never a failure: the screen still resolves",
     "composition::composition — hug-lane overflow is reported, not silent::a FILL group's region filling the offer is not a defect and is not reported",
     "composition::ruling 8: the declared-fallback finding is marked as designed behaviour::the fallback finding carries designed = true, and names the fallback it is showing",
     "composition::ruling 8: the declared-fallback finding is marked as designed behaviour::NEGATIVE CONTROL: an ordinary defect in the SAME solve is NOT marked",
     "composition::ruling 8: the declared-fallback finding is marked as designed behaviour::a composition whose declared arrangement FITS reports nothing at all",
     "composition::framework-gaps-phase2 item 12: ARRANGEMENTS.leadFirst::is the mirror of twoLane: lead takes the SOLO lane, main+trail share the second",
     "composition::framework-gaps-phase2 item 12: ARRANGEMENTS.leadFirst::twoLane itself is UNCHANGED — this ships an additive sibling, not a rewrite",
     "composition::framework-gaps-phase2 item 12: ARRANGEMENTS.leadFirst::resolves through the real preset NAME, nav first — no custom arrangement needed",
     "composition::framework-gaps-phase2 item 12: ARRANGEMENTS.leadFirst::an unknown arrangement name's error message lists leadFirst among the built-ins",
     "composition_exclusions::§8: a lane starts below an exclusion its own CONTENT reaches into::the covered column gives way and the columns beside it stay LEVEL with the chrome",
     "composition_exclusions::§8: a lane starts below an exclusion its own CONTENT reaches into::a column the chrome BARELY clips still gives way when its content reaches in",
     "composition_exclusions::§8: a lane starts below an exclusion its own CONTENT reaches into::...and a column whose content clears the chrome entirely stays where it was",
     "composition_exclusions::§8: a lane starts below an exclusion its own CONTENT reaches into::the LOWEST exclusion a lane meets wins — two rows are not two reservations",
     "composition_exclusions::§8: a lane starts below an exclusion its own CONTENT reaches into::a lane that gives way is SHORTER by exactly what it gave, so its content still fits its box",
     "composition_exclusions::§8: a lane starts below an exclusion its own CONTENT reaches into::ADDITIVITY: declaring none is byte-identical to the composition that never heard of them",
     "composition_exclusions::§8: a lane starts below an exclusion its own CONTENT reaches into::a malformed rect is SKIPPED, not clamped — a zero-width rect excludes nothing",
     "composition_exclusions::§8: a lane that gave way is shorter at BOTH ends — the PS5/phone bottom overlap::the PS5 shape: the kill feed ends AT the lane's bottom edge, not past it",
     "composition_exclusions::§8: a lane that gave way is shorter at BOTH ends — the PS5/phone bottom overlap::...and the PHONE shape is the same term, not a second bug",
     "composition_exclusions::§8: a lane that gave way is shorter at BOTH ends — the PS5/phone bottom overlap::the displacement was EXACTLY the inset, so it is gone at EVERY inset",
     "composition_exclusions::§8: a lane that gave way is shorter at BOTH ends — the PS5/phone bottom overlap::a `fill` group in a shortened lane is shortened, not merely moved",
     "composition_exclusions::§8: a lane that gave way is shorter at BOTH ends — the PS5/phone bottom overlap::A SHORTENED LANE PLACES LIKE A SHORT LANE — the invariant, for every `place`",
     "composition_exclusions::§8: a lane that gave way is shorter at BOTH ends — the PS5/phone bottom overlap::the solver was SILENT while it happened — and now has nothing to say",
     "composition_exclusions::§8: a lane that gave way is shorter at BOTH ends — the PS5/phone bottom overlap::INVARIANCE, over a MATRIX: a declaration that reaches no exclusion dumps byte for byte",
     "composition_exclusions::§8: a lane that gave way is shorter at BOTH ends — the PS5/phone bottom overlap::ADDITIVITY: a lane that gave way NOTHING places exactly as it always did",
     "composition_exclusions::§8: the decision is a FIXED POINT, so no latch is needed::re-resolving the same declaration twice lands on the same answer",
     "composition_exclusions::§8: the decision is a FIXED POINT, so no latch is needed::...and a lane squeezed past its floor still holds the exclusion rather than buying it back",
     "composition_exclusions::§8: `UI.Composition{ exclusions }` — the declaration, through the mounted path::a declared chrome row pushes only the lanes its own content reaches into",
     "composition_exclusions::§8: `UI.Composition{ exclusions }` — the declaration, through the mounted path::it is REACTIVE: a chrome row that arrives after the first solve re-runs the pass",
     "composition_exclusions::§8: `UI.Composition{ exclusions }` — the declaration, through the mounted path::...and the bottom-anchored zone lands INSIDE the composition, on a phone",
     "region_expand::EXPAND 1 — the resolution states which form is standing and whether it is a dead end::a region at its richest form reports activeForm 1 and nothing to expand to",
     "region_expand::EXPAND 1 — the resolution states which form is standing and whether it is a dead end::a stepped-down region reports the form it landed on, and that an expansion exists",
     "region_expand::EXPAND 1 — the resolution states which form is standing and whether it is a dead end::a DROPPED region has no active form, so it is never expandable — drops stay sheet-only",
     "region_expand::EXPAND 1 — the resolution states which form is standing and whether it is a dead end::expand = \"none\" is the authored escape: stepped down, and nothing to expand",
     "region_expand::EXPAND 1 — the resolution states which form is standing and whether it is a dead end::formInteractive is the ACTIVE form's own answer, read from the declaration",
     "region_expand::EXPAND 1 — the resolution states which form is standing and whether it is a dead end::a dropped region's formInteractive is false — there is no form to be interactive",
     "region_expand::EXPAND 1 — the resolution states which form is standing and whether it is a dead end::the declaration refuses an expand mode it cannot honour",
     "region_expand::EXPAND 2 — the plate is sized by the same pure pass, and says when it cannot be one::the plate is the richest form's own width, capped at the viewport minus gutters",
     "region_expand::EXPAND 2 — the plate is sized by the same pure pass, and says when it cannot be one::...and reports sheet = false when the richest form fits the plate",
     "region_expand::EXPAND 2 — the plate is sized by the same pure pass, and says when it cannot be one::a region with nothing to expand is not measured for a plate",
     "region_expand::EXPAND 2b — `unshown` alone is not the census, and here is the state that proves it::a stepped-down region with a lossless ladder is invisible in `unshown` and visible in `activeForm`",
     "region_expand::EXPAND 3 — the dump carries the facts, and two calls are equal::every region row states the form standing, the richest one, and the route",
     "region_expand::EXPAND 4 — the authoring surface is one word, and it is refused where it cannot mean anything::a multi-form region gets ONE affordance, whatever the depth of its ladder",
     "region_expand::EXPAND 4 — the authoring surface is one word, and it is refused where it cannot mean anything::the affordance says what it does, in the words a screen reader will read",
     "region_expand::EXPAND 4 — the authoring surface is one word, and it is refused where it cannot mean anything::expand = \"none\" is the authored escape and synthesizes nothing at all",
     "region_expand::EXPAND 4 — the authoring surface is one word, and it is refused where it cannot mean anything::recover = \"none\" states nothing is missing, so nothing is synthesized either",
     "region_expand::EXPAND 4 — the authoring surface is one word, and it is refused where it cannot mean anything::a ONE-FORM region never simplifies, so declaring an expand on one is refused",
     "region_expand::EXPAND 4 — the authoring surface is one word, and it is refused where it cannot mean anything::an expand under recover = \"none\" is refused — the declaration contradicts itself",
     "region_expand::EXPAND 4 — the authoring surface is one word, and it is refused where it cannot mean anything::an unknown expand mode is refused at the call site",
     "region_expand::EXPAND 5 — one gesture, one meaning: the form's own controls decide the shape::a PASSIVE form is the target itself — the affordance covers it and draws nothing",
     "region_expand::EXPAND 5 — one gesture, one meaning: the form's own controls decide the shape::an INTERACTIVE form keeps its own meanings and gets a chevron beside it",
     "region_expand::EXPAND 5 — one gesture, one meaning: the form's own controls decide the shape::interactivity is the class CONTRACT's answer, not a list of button names",
     "region_expand::EXPAND 5 — one gesture, one meaning: the form's own controls decide the shape::a UI.Foreign box owns its own gestures, so the cover never goes over one",
     "region_expand::EXPAND 5 — one gesture, one meaning: the form's own controls decide the shape::a Box that declares `active` SINKS input, so it takes the chevron too",
     "region_expand::EXPAND 5 — one gesture, one meaning: the form's own controls decide the shape::content the framework CANNOT see is taken as interactive — a lazy region is never covered",
     "region_expand::EXPAND 6 — the framework's affordance IS the self route::a ladder that is passive at one rung and interactive at another takes the chevron",
     "region_expand::EXPAND 6 — the framework's affordance IS the self route::recover = \"self\" no longer needs a hand-authored control in every reduced form",
     "region_expand::EXPAND 6 — the framework's affordance IS the self route::...but with expand = \"none\" the route has to be there, and the boundary still says so",
     "region_expand::EXPAND 7 — the affordance stands exactly while a reduced form does::at the richest form it has a zero rect and is hidden, so nothing can focus it",
     "region_expand::EXPAND 7 — the affordance stands exactly while a reduced form does::stepped down, a PASSIVE form IS the target: the cover is the region's whole box",
     "region_expand::EXPAND 7 — the affordance stands exactly while a reduced form does::stepped down, an INTERACTIVE form keeps its box and the chevron takes the trailing edge",
     "region_expand::EXPAND 7 — the affordance stands exactly while a reduced form does::expand = \"none\" synthesizes nothing, so the region is exactly its forms",
     "region_expand::EXPAND 7 — the affordance stands exactly while a reduced form does::the affordance is not a FORM: the ladder still has exactly two rungs",
     "region_expand::EXPAND 8 — the plate, and every way out of it::opens ANCHORED against the region's own rect, at the width the composition sized",
     "region_expand::EXPAND 8 — the plate, and every way out of it::falls back to the full-width SHEET where the richest form cannot meet its floor",
     "region_expand::EXPAND 8 — the plate, and every way out of it::opens NOTHING at the richest form — there is nothing more to show",
     "region_expand::EXPAND 8 — the plate, and every way out of it::a second activation closes it, so the affordance is never inert under a thumb",
     "region_expand::EXPAND 8 — the plate, and every way out of it::an EPOCH change closes it: the rect it was opened against moved",
     "region_expand::EXPAND 8 — the plate, and every way out of it::...and so does the region GOING: a zero rect is a source that is not there",
     "region_expand::EXPAND 8 — the plate, and every way out of it::...and so does the region climbing back to its richest form under the plate",
     "region_expand::EXPAND 8 — the plate, and every way out of it::a dismiss the PRESENTER drove is counted too, and the plate can open again",
     "region_expand::EXPAND 8 — the plate, and every way out of it::an authored handler REPLACES the presentation, and the framework presents nothing",
     "region_expand::EXPAND 8 — the plate, and every way out of it::declares NO transition, so there is no travel to reduce",
     "region_expand::EXPAND 9 — the whole loop, with no per-region code anywhere in the declaration::a tap on the stepped-down region opens a plate carrying the richest form",
     "region_expand::EXPAND 9 — the whole loop, with no per-region code anywhere in the declaration::...and gamepad B closes it, because the plate is an ordinary presented surface",
     "region_expand::EXPAND 9 — the whole loop, with no per-region code anywhere in the declaration::a screen with room opens nothing at all: the affordance is not even standing",
     "region_expand::EXPAND 10 — the stop exists only while a reduced form does, and never moves its neighbours::a screen with room has no expand stop at all",
     "region_expand::EXPAND 10 — the stop exists only while a reduced form does, and never moves its neighbours::stepped down, the stop appears ONCE and the neighbours keep their order",
     "region_expand::EXPAND 10 — the stop exists only while a reduced form does, and never moves its neighbours::on a form that carries a control, the expand stop comes AFTER the form's own",
     "region_expand::EXPAND 10 — the stop exists only while a reduced form does, and never moves its neighbours::gamepad A opens the plate and B returns focus to the stop that opened it",
     "region_expand::EXPAND 11 — a rotation under an open plate closes it, cleanly::the box the plate was opened against moved, so the plate is not there either",
     "region_expand::EXPAND 12 — a keyboard-only player can open the plate and close it again::Return opens it, and the ring inside it is not empty",
     "region_expand::EXPAND 12 — a keyboard-only player can open the plate and close it again::...and Return on the focused close control closes it — no pad, no pointer",
     "region_expand::EXPAND 12 — a keyboard-only player can open the plate and close it again::Space activates it too, where the surface owns Space",
     "region_expand::EXPAND 12 — a keyboard-only player can open the plate and close it again::the CONTENT keeps the first stop when it has one, and the close is reachable after it",
     "region_expand::EXPAND 12 — a keyboard-only player can open the plate and close it again::the SHEET fallback is a real surface, and it carries the same way out",
     "region_expand::EXPAND 19 — the panel a plate becomes never exceeds the gutter allowance::a form INSIDE the chrome band cannot mount a panel wider than the allowance",
     "region_expand::EXPAND 19 — the panel a plate becomes never exceeds the gutter allowance::a WRAPPING form cannot hug past the cap either — the panel the review found off-screen",
     "region_expand::EXPAND 19 — the panel a plate becomes never exceeds the gutter allowance::...and a form that fits the CONTENT cap still gets its panel, chrome included",
     "region_expand::EXPAND 13 — the ladder chooses a rung against the width it will actually get::a wrapping compact form is measured against the box the chevron leaves it",
     "region_expand::EXPAND 14 — a form that spliced itself away cannot be indexed, and is not::with the richest form spliced away, the standing rung's own answer is reported",
     "region_expand::EXPAND 14 — a form that spliced itself away cannot be indexed, and is not::...and with the gate open the indexes line up again, unchanged",
     "region_expand::EXPAND 15 — the framework paints nothing over the author's content::a passive form gets a cover UNDER it — same box, and every z below the form's",
     "region_expand::EXPAND 15 — the framework paints nothing over the author's content::no framework instance covers any node of the screen, painted OR reachable (R18)",
     "region_expand::EXPAND 15 — the framework paints nothing over the author's content::the hit expander is paint-INERT by construction, not by transparency (R18)",
     "region_expand::EXPAND 15 — the framework paints nothing over the author's content::the role is the forms' own answer, and only the cover is allowed to be under one",
     "region_expand::EXPAND 16 — the plate opens OVER a live screen, and hands it back::every base node keeps its rect and its visibility while the plate is up",
     "region_expand::EXPAND 16 — the plate opens OVER a live screen, and hands it back::...and the plate contributes no opaque node of its own: no tail, no seam",
     "region_expand::EXPAND 16 — the plate opens OVER a live screen, and hands it back::the base is interactive again the moment the plate goes",
     "region_expand::EXPAND 17 — the way out is a disc CENTRED on the plate's corner::the plate's padding is UNIFORM — one token on all four sides, no reserved band",
     "region_expand::EXPAND 17 — the way out is a disc CENTRED on the plate's corner::the disc's CENTRE is the plate's corner, and the panel still contains the disc",
     "region_expand::EXPAND 17 — the way out is a disc CENTRED on the plate's corner::R18 — the disc's incursion lands on the PADDING and never on content, on BOTH presentations",
     "region_expand::EXPAND 17 — the way out is a disc CENTRED on the plate's corner::...and the disc's HIT floor takes nothing from an interactive control on the plate",
     "region_expand::EXPAND 17 — the way out is a disc CENTRED on the plate's corner::...and the guarantee is a TOKEN inequality, so a package that breaks it goes red here",
     "region_expand::EXPAND 17 — the way out is a disc CENTRED on the plate's corner::...and the disc's inset is measured against the RING's own authority, package by package",
     "region_expand::EXPAND 17 — the way out is a disc CENTRED on the plate's corner::...and it is still the same control: one focus stop, the standard band, Return closes",
     "region_expand::EXPAND 18 — a plate does not truncate what it was opened to show::a fill-width richest form gets the whole plate, not its own content width",
     "region_expand::EXPAND 18 — a plate does not truncate what it was opened to show::...and a HUGGING richest form still gets exactly what it measured, INSIDE the plate",
     "hud_composition::the nine anchors PARTITION the screen::the HUD preset resolves three equal columns that tile the offer exactly",
     "hud_composition::the nine anchors PARTITION the screen::each zone lands in the column and the band its name says",
     "hud_composition::the nine anchors PARTITION the screen::an aligned zone takes ITS OWN width inside the column, not the whole column",
     "hud_composition::the nine anchors PARTITION the screen::no two zones intersect, at any height a landscape phone can lose",
     "hud_composition::holdsLane pins a column that has nothing in it::an empty centre column keeps its third and the right cluster does not move",
     "hud_composition::holdsLane pins a column that has nothing in it::without the flag the lane still releases — rule 9 is unchanged",
     "hud_composition::holdsLane pins a column that has nothing in it::holdsLane is refused on a spanning group — a span row has no lane to hold",
     "hud_composition::holdsLane pins a column that has nothing in it::holdsLane must be a boolean",
     "hud_composition::the `topbar` zone is a span row, and it is free when unused::a HUD that declares nothing in it resolves BYTE-IDENTICALLY to one without the group",
     "hud_composition::the `topbar` zone is a span row, and it is free when unused::a region declared in it rides ABOVE every lane and spends the height from the lane budget",
     "hud_composition::a height shrink DEGRADES, in rank order::the least important zone in the failing column gives way first",
     "hud_composition::a height shrink DEGRADES, in rank order::a column that runs out of forms drops its droppable zone, and no other",
     "hud_composition::a height shrink DEGRADES, in rank order::a cluster WIDER than its column steps down too — width is space",
     "hud_composition::a height shrink DEGRADES, in rank order::a zone that was never in the failing column keeps its richest form",
     "hud_composition::a zone painting over its neighbour is REPORTED::a well-behaved HUD reports NO collisions",
     "hud_composition::a zone painting over its neighbour is REPORTED::a zone wider than its column names the zone it is painting over",
     "hud_composition::a zone painting over its neighbour is REPORTED::a wide zone with nothing beside it is not a collision",
     "hud_composition::a zone painting over its neighbour is REPORTED::the SCROLL region is excluded — its content is meant to be bigger than its box",
     "hud_composition::a zone painting over its neighbour is REPORTED::the collisions ride the dump, deterministically ordered",
     "hud_composition::a zone painting over its neighbour is REPORTED::ADDITIVE: a declaration with no collision dumps `collisions = {}`",
     "hud_composition::a mounted HUD loses 200px of height::re-solves in place: the zones stay pinned and nothing overlaps",
     "hud_composition::a mounted HUD loses 200px of height::the solver files a NAMED finding when one zone paints over another",
     "hud_composition::DIR5: the HUD demo exercises BOTH expand roles::the passive pills are COVERS and the button cluster keeps its arrow",
     "hud_composition::nothing the HUD paints lands on the platform's own controls::clears the chrome on studio live 735x413 (measured 2026-08-14), riding the strip and not",
     "hud_composition::nothing the HUD paints lands on the platform's own controls::clears the chrome on the same window with a URL bar open (735x213), riding the strip and not",
     "hud_composition::nothing the HUD paints lands on the platform's own controls::clears the chrome on notched landscape phone (812x375, notch 47 + home 21), riding the strip and not",
     "hud_composition::nothing the HUD paints lands on the platform's own controls::clears the chrome on narrow portrait phone (320x640) — the cluster is HALF the width, riding the strip and not",
     "hud_composition::nothing the HUD paints lands on the platform's own controls::clears the chrome on an engine that reports NO strip, riding the strip and not",
     "hud_composition::nothing the HUD paints lands on the platform's own controls::...and it is USING the strip: the objective chip is level with Roblox's own buttons",
     "hud_composition::nothing the HUD paints lands on the platform's own controls::the timer is a live ring, the health bar is bound, and the buttons are icon discs",
     "hud_composition::nothing the HUD paints lands on the platform's own controls::every string on the HUD survives a 1.4x pseudo-localization",
     "hud_composition::nothing the HUD paints lands on the platform's own controls::the rung with no strip is EXACTLY what deviceSafeContent applied: the top band is spent whole",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::the sweep covers every boundary the constitution's §4 claim names",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newChip refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newChip still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newLabel refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newLabel still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newTable refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newTable still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newVirtualList refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newVirtualList still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newVirtualGrid refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newVirtualGrid still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newPopupButton refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newPopupButton still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newStepper refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newStepper still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newSlider refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newSlider still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newRating refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newRating still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newProgressView refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newProgressView still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newPicker refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newPicker still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newDisclosureGroup refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newDisclosureGroup still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newTextInput refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newTextInput still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newAsyncImage refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newAsyncImage still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::valueModel.new refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::valueModel.new still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newResourceProvider refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newResourceProvider still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newResourceProvider opts.retry refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newResourceProvider opts.retry still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::provider.acquire refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::provider.acquire still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::provider.preload refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::provider.preload still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::adaptive.conditions refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::adaptive.conditions still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::inputHint refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::inputHint still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newAutoscroll refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newAutoscroll still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newDragVelocity refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newDragVelocity still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newDragSession refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newDragSession still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newDragSession target refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newDragSession target still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newDragRegistry refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newDragRegistry still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::mount refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::mount still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::renderer.attach refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::renderer.attach still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::motion.newClock refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::motion.newClock still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newPresenter refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::newPresenter still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::presenter.present refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::presenter.present still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::presenter.presentModal refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::presenter.presentModal still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::presenter.presentCritical refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::presenter.presentCritical still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::presenter.presentToast refuses 'definitelyNotAKey' and names itself",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::presenter.presentToast still accepts its legal call",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::a near-miss key gets a did-you-mean",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::nil and empty tables stay legal — the guard is a closed set, not a schema",
     "spec_guard_sweep::ARCH-1: every guarded boundary refuses an unknown key::a NON-STRING key is the same authoring mistake and is refused too",
     "typography_weight_roles::the ramp grows two WEIGHT roles and moves nothing else::Studio Neutral answers all eight roles",
     "typography_weight_roles::the ramp grows two WEIGHT roles and moves nothing else::the six roles that shipped are byte-identical: every one still BuilderSans Regular",
     "typography_weight_roles::the ramp grows two WEIGHT roles and moves nothing else::strong is body's size in the family's SemiBold face; numeral is control's size in Bold",
     "typography_weight_roles::the ramp grows two WEIGHT roles and moves nothing else::both are legal typography-role names and an invented one is not",
     "typography_weight_roles::one role, both seams: measured and painted read the same descriptor::a strong label measures with its OWN descriptor and paints that same string",
     "typography_weight_roles::one role, both seams: measured and painted read the same descriptor::the size travels with the face, so a numeral is 18px Bold and not 16px Bold",
     "typography_weight_roles::one role, both seams: measured and painted read the same descriptor::an INTRINSIC role paints its face too — a Button wears `control`, not `body`",
     "typography_weight_roles::one role, both seams: measured and painted read the same descriptor::a role swap re-solves and repaints the face without a rebuild",
     "typography_weight_roles::construction refuses an unknown role, naming the eight::UI.Text{ textSize = 'bolder' } errors",
     "typography_weight_roles::construction refuses an unknown role, naming the eight::...and the refusal names every legal role, including the two new ones",
     "typography_weight_roles::construction refuses an unknown role, naming the eight::a theme package refuses an unknown typography role the same way",
     "typography_weight_roles::a package owns its weight faces::an AUTHORED weight role beats the derivation",
     "typography_weight_roles::a package owns its weight faces::an UNAUTHORED one derives from the package's OWN family and style",
     "typography_weight_roles::a package owns its weight faces::the two shipped reference packages keep a coherent ramp",
     "typography_weight_roles::a package owns its weight faces::no package's content STAMP moved: the roles are derived at resolve, never authored",
     "typography_weight_roles::tokens.compile derives the two sizes::a game schema that omits them still answers both",
     "typography_weight_roles::tokens.compile derives the two sizes::`control` is preferred over `heading` for numeral, matching the theme derivation",
     "typography_weight_roles::tokens.compile derives the two sizes::an authored size wins",
     "typography_weight_roles::native paint carries the same face through a tag and a rule::a node's role becomes a facet-type-<role> tag",
     "typography_weight_roles::native paint carries the same face through a tag and a rule::buildPackage emits one FontFace rule per role, from type.<role>.font",
     "typography_weight_roles::native paint carries the same face through a tag and a rule::the package-less built-in model states the same two weight faces",
     "with_animation::withAnimation: the solver stays exact and instant::the solved rect is the NEW one on the frame the call returns",
     "with_animation::withAnimation: the solver stays exact and instant::the PAINTED position starts at the old place and travels to the new one",
     "with_animation::withAnimation: the solver stays exact and instant::records are dropped at settle, so the channel is clean afterwards",
     "with_animation::withAnimation: records are relative, and only at animation roots::a child riding its moved parent installs NO record of its own",
     "with_animation::withAnimation: records are relative, and only at animation roots::the riding child is painted at ONE delta, never two",
     "with_animation::withAnimation: what must NOT animate::a discrete change made BEFORE the call does not animate (the unarmed pre-drain)",
     "with_animation::withAnimation: what must NOT animate::an ordinary refresh outside a call installs nothing",
     "with_animation::withAnimation: reduced motion installs nothing at all::the layout is exact and there is no flight",
     "with_animation::withAnimation: refusals::an unknown motion class is refused with the registered list",
     "with_animation::withAnimation: refusals::an inline params table is refused — motion classes are NAMES",
     "with_animation::withAnimation: refusals::a call inside an already-open transaction is refused, not silently inert",
     "with_animation::withAnimation: refusals::a throwing body re-raises AND leaves nothing armed",
     "with_animation::withAnimation: nesting refuses the INNER call and leaves the outer flight intact::the inner call is refused by its own cause, not the transaction one",
     "with_animation::withAnimation: nesting refuses the INNER call and leaves the outer flight intact::the OUTER animation still flies — the exact regression, measured",
     "with_animation::withAnimation: nesting refuses the INNER call and leaves the outer flight intact::the refusal does not latch: the next call animates normally",
     "with_animation::withAnimation: called during a core commit::the message names the commit as well as the transaction, and warns about the retry",
     "with_animation::withAnimation: called during a core commit::`fn`'s mutation HAS landed when it raises — which is why the warning is there",
     "with_animation::withAnimation: a path that did not exist before::a node that APPEARS inside the call installs no record of its own",
     "with_animation::withAnimation: a path that did not exist before::the sibling it displaced DOES fly, so the case is not vacuous",
     "with_animation::withAnimation: interruption carries velocity::a second call mid-flight re-targets instead of restarting from rest",
     "with_animation::withAnimation: a registered CURVE drives the flight::flies on the curve, lands exactly, and cleans up its records",
     "with_animation::withAnimation: a registered CURVE drives the flight::ARRIVES ON THE CURVE'S DURATION — the thing a class cannot promise",
     "with_animation::withAnimation: a registered CURVE drives the flight::an interruption re-bases without seeding a velocity the curve cannot take",
     "with_animation::withAnimation: a registered CURVE drives the flight::reduced motion installs nothing for a curve either",
     "with_animation::withAnimation: a registered CURVE drives the flight::an unknown name is refused, and the message names BOTH vocabularies",
     "with_animation::withAnimation: the painted SIZE travels, and the solved size is still exact::the solved size is the NEW one on the frame the call returns",
     "with_animation::withAnimation: the painted SIZE travels, and the solved size is still exact::the PAINTED size starts at the old one and travels to the new one",
     "with_animation::withAnimation: the painted SIZE travels, and the solved size is still exact::a SIZE-ONLY change is claimed — the node never moved, and it still animates",
     "with_animation::withAnimation: the painted SIZE travels, and the solved size is still exact::ONE spring drives both halves: the growth and the displacement are the same number",
     "with_animation::withAnimation: the painted SIZE travels, and the solved size is still exact::SIZE DOES NOT ACCUMULATE: the child of a growing node keeps its own size",
     "with_animation::withAnimation: the painted SIZE travels, and the solved size is still exact::the record CLEARS at settle — the transform goes back to nil, not to a zero residue",
     "with_animation::withAnimation: the painted SIZE travels, and the solved size is still exact::reduced motion installs no size record — and spends no adapter write to do it",
     "with_animation::withAnimation: the painted SIZE travels, and the solved size is still exact::a second call mid-flight RE-BASES the size instead of snapping it",
     "with_animation::withAnimation: the painted SIZE travels, and the solved size is still exact::an ordinary refresh outside a call paints the size instantly, as it always did",
     "with_animation::withAnimation: two calls in one frame::two sequential calls are two COMMITS, not one",
     "animation_precedence::withAnimation survives a keep-visible writer on the root (BLOCKER 1)::a surface holding a UI.TextInput still installs records and flies",
     "animation_precedence::withAnimation survives a keep-visible writer on the root (BLOCKER 1)::`present(bp, { keepVisibleOffset = signal })` reproduces it without any control",
     "animation_precedence::withAnimation survives a keep-visible writer on the root (BLOCKER 1)::the owned node itself is still NOT claimed — one writer per slot",
     "animation_precedence::withAnimation survives a keep-visible writer on the root (BLOCKER 1)::a live keep-visible shift COMPOSES with a record beneath it rather than being absorbed",
     "animation_precedence::withAnimation survives a keep-visible writer on the root (BLOCKER 1)::the no-TextInput surface is unchanged (the control case)",
     "authored_presentation::authored presentation A — the props, and the refusals::declaring `opacity` makes the node a fade group with no `canvasGroup` beside it",
     "authored_presentation::authored presentation A — the props, and the refusals::a plain sibling is NOT promoted to a fade group",
     "authored_presentation::authored presentation A — the props, and the refusals::all three are reactive, and a change repaints without a remount",
     "authored_presentation::authored presentation A — the props, and the refusals::`opacity` on a LEAF is refused at construction, because a leaf fade would defeat the sheet",
     "authored_presentation::authored presentation A — the props, and the refusals::THE DISABLED-DIMMING CASE: the refusal names the defeat it exists to prevent",
     "authored_presentation::authored presentation A — the props, and the refusals::...and it names the WORKAROUND, spelled out, not a doc reference",
     "authored_presentation::authored presentation A — the props, and the refusals::`canvasGroup` on a leaf is refused with the SAME message — one fact, both spellings",
     "authored_presentation::authored presentation A — the props, and the refusals::a genuinely unknown property is UNCHANGED — the refusal did not swallow the suggester",
     "authored_presentation::authored presentation A — the props, and the refusals::THE ASYMMETRY: `opacity` is on exactly two classes and every other one refuses it BY NAME",
     "authored_presentation::authored presentation A — the props, and the refusals::a STRUCTURAL class is not given the fade refusal — it has no instance to fade",
     "authored_presentation::authored presentation A — the props, and the refusals::...and `scale`/`rotation` are offered on EVERY rendered class and refused on none",
     "authored_presentation::authored presentation A — the props, and the refusals::`scale` and `rotation` ARE offered on a leaf — neither touches a sheet-owned property",
     "authored_presentation::authored presentation A — the props, and the refusals::the wrap fades the GROUP and writes nothing at all on the control inside it",
     "authored_presentation::authored presentation A — the props, and the refusals::the authored domain is refused rather than clamped: opacity outside 0..1, scale <= 0",
     "authored_presentation::authored presentation A — the props, and the refusals::a REACTIVE value out of domain is refused at the read site too, not silently painted",
     "authored_presentation::authored presentation B — the composition rule::OPACITY MULTIPLIES: authored 0.5 under a framework fade of 0.5 paints 0.25 opacity",
     "authored_presentation::authored presentation B — the composition rule::SCALE MULTIPLIES: authored 2 under a presentation scale of 1.5 paints 3",
     "authored_presentation::authored presentation B — the composition rule::ROTATION ADDS: authored 30 under a presentation rotation of 15 paints 45",
     "authored_presentation::authored presentation B — the composition rule::clearing the FRAMEWORK's contribution leaves the AUTHORED value standing",
     "authored_presentation::authored presentation B — the composition rule::the offset half is untouched by an authored scale: x/y still compose onto the rect",
     "authored_presentation::authored presentation B — the composition rule::NESTED groups do NOT double-compose: the engine multiplies across nodes, the framework within one",
     "authored_presentation::authored presentation C — withAnimation reaches all three::an OPACITY change seeds at the old value and travels to the new",
     "authored_presentation::authored presentation C — withAnimation reaches all three::a SCALE change travels",
     "authored_presentation::authored presentation C — withAnimation reaches all three::a ROTATION change travels",
     "authored_presentation::authored presentation C — withAnimation reaches all three::ONE SPRING: a fade and a move started in the same call are at the same FRACTION every frame",
     "authored_presentation::authored presentation C — withAnimation reaches all three::the record is RELEASED on settle — an animated fade leaks nothing",
     "authored_presentation::authored presentation C — withAnimation reaches all three::REDUCED MOTION deletes the flight and never the destination",
     "authored_presentation::authored presentation C — withAnimation reaches all three::an UNARMED change lands instantly: only an armed commit installs a record",
     "authored_presentation::authored presentation D — off-path cost, and one write site::a surface authoring NONE of the three writes no presentation property at all",
     "authored_presentation::authored presentation D — off-path cost, and one write site::the refusal is on the ERROR PATH ONLY — one caller, and it is the failing constructor",
     "authored_presentation::authored presentation D — off-path cost, and one write site::ONE SITE writes each presentation engine property — the manifest's promise, in the source",
     "authored_presentation::authored presentation E — an authored scale survives a press::NO press-dip target in screen_paint is an absolute 1 — every one derives from the resting scale",
     "authored_presentation::authored presentation E — an authored scale survives a press::...and all FOUR sites read it through one named helper, not four literals",
     "authored_presentation::authored presentation E — an authored scale survives a press::the presentation authority RECORDS the resting scale at the moment it writes it",
     "authored_presentation::authored presentation F — offset (gap 16)::is authorable and reactive: a change moves the PAINTED position with no remount",
     "authored_presentation::authored presentation F — offset (gap 16)::changes NOTHING the solver sees — same rect as an unoffset sibling, same hit target",
     "authored_presentation::authored presentation F — offset (gap 16)::composes ADDITIVELY with the framework's own imperative channel write",
     "authored_presentation::authored presentation F — offset (gap 16)::refuses a non-table value, loudly, naming the path",
     "authored_presentation::authored presentation F — offset (gap 16)::refuses a NaN component",
     "authored_presentation::authored presentation F — offset (gap 16)::a surface authoring no offset at all pays the same off-path cost D already proved",
     "preferred_transparency::the preference composes with a theme's backdrop alpha, and nothing else::is base x preference — 1 is the identity and 0 is fully opaque",
     "preferred_transparency::the preference composes with a theme's backdrop alpha, and nothing else::a garbage preference is the IDENTITY, never a silent paint change",
     "preferred_transparency::the scope is a measurement: one partial background in the whole sheet::the ONLY rule painting a see-through background is the backdrop",
     "preferred_transparency::the scope is a measurement: one partial background in the whole sheet::...and in a compiled theme package too, whatever its own dim is",
     "preferred_transparency::the scope is a measurement: one partial background in the whole sheet::the declared patch scope is exactly the measured set of translucent backgrounds",
     "preferred_transparency::the scope is a measurement: one partial background in the whole sheet::...and the same rule name is the one BOTH builders emit, so a package is covered too",
     "preferred_transparency::why the sweep is refused rather than merely unfashionable::scaling an INVISIBLE background does not dim it, it reveals it",
     "preferred_transparency::the refusals: what the preference deliberately does not touch::the DISABLED dim is untouched — the alpha there is the state, not a backdrop",
     "preferred_transparency::the refusals: what the preference deliberately does not touch::the HAIRLINE is untouched — a stroke is a border, and the setting names backgrounds",
     "preferred_transparency::an AUTHORED opacity is the author's number and the preference never scales it::opacity 0.5 stays 0.5 at every preference, including fully opaque",
     "preferred_transparency::the renderer pushes the preference at attach and on every change::arrives at attach with no consumer wiring at all",
     "preferred_transparency::the renderer pushes the preference at attach and on every change::...and again when the player moves the setting mid-session",
     "preferred_transparency::the renderer pushes the preference at attach and on every change::a garbage platform fact never reaches the adapter — the memo clamps first",
     "preferred_transparency::the two paint modes compose the SAME number::the bespoke scrim write goes through backdropTransparency, not a bare token read",
     "preferred_transparency::the two paint modes compose the SAME number::the native rule patch goes through it too, over the DECLARED scope",
     "surface_overlap::cross-surface overlap: the pure pair scan::two undeclared surfaces whose cover rects intersect are ONE reported pair",
     "surface_overlap::cross-surface overlap: the pure pair scan::rects that only touch at an edge do not intersect — a 0px overlap is not an overlap",
     "surface_overlap::cross-surface overlap: the pure pair scan::a FULL COVER is reported as a different shape from a partial overlap",
     "surface_overlap::cross-surface overlap: the pure pair scan::the report is deterministic: pairs sorted by id, and each pair filed once",
     "surface_overlap::cross-surface overlap: the pure pair scan::a surface that covers nothing (nil rect) is in no pair at all",
     "surface_overlap::cross-surface overlap: the pure pair scan::a zero-area cover rect is not a cover",
     "surface_overlap::cross-surface overlap: the pure pair scan::one surface alone can collide with nothing",
     "surface_overlap::cross-surface overlap: what counts as declaring an intent to cover::edgeToEdge on EITHER side is a declaration — a scrim means to cover, and means to be covered",
     "surface_overlap::cross-surface overlap: what counts as declaring an intent to cover::deviceSafeContent is NOT a declaration — it is an inset choice, not a layering one",
     "surface_overlap::cross-surface overlap: what counts as declaring an intent to cover::two DIFFERENT display bands is a declaration — a modal over a base screen is what a modal is for",
     "surface_overlap::cross-surface overlap: what counts as declaring an intent to cover::...but the SAME band is not, however far apart the two orders are",
     "surface_overlap::cross-surface overlap: what counts as declaring an intent to cover::an UNSET display order declares nothing — a raw attach that never ordered itself is not layered",
     "surface_overlap::cross-surface overlap: what counts as declaring an intent to cover::the four SURFACE_LAYER bands land in four DISTINCT bands — the presenter's table and this scan agree",
     "surface_overlap::cross-surface overlap: it arrives through controller.diagnostics()::two base screens covering each other file the finding on BOTH surfaces",
     "surface_overlap::cross-surface overlap: it arrives through controller.diagnostics()::...and it is the only new finding: a single surface on the target reports nothing",
     "surface_overlap::cross-surface overlap: it arrives through controller.diagnostics()::surfaces whose painted content does not intersect are not a pair",
     "surface_overlap::cross-surface overlap: it arrives through controller.diagnostics()::a container that FILLS claims the whole box it fills, and the alarm says so",
     "surface_overlap::cross-surface overlap: it arrives through controller.diagnostics()::a MODAL over a base screen reports nothing — the band is the declaration",
     "surface_overlap::cross-surface overlap: it arrives through controller.diagnostics()::the modal's own SCRIM reports nothing either — it is edgeToEdge",
     "surface_overlap::cross-surface overlap: it arrives through controller.diagnostics()::a dismissed surface stops being a surface — the pair leaves with it",
     "surface_overlap::cross-surface overlap: it arrives through controller.diagnostics()::a dismissed surface with a live EXIT TRANSITION is out immediately, tree and all",
     "surface_overlap::cross-surface overlap: it arrives through controller.diagnostics()::a raw renderer.attach surface leaves at dispose — nothing needs a presenter",
     "surface_overlap::cross-surface overlap: it arrives through controller.diagnostics()::a disposed surface leaves the REGISTRY: the target's bucket does not grow with every mount",
     "surface_overlap::cross-surface overlap: the covering set::a surface whose content is `hidden` covers nothing",
     "surface_overlap::cross-surface overlap: the covering set::a surface at `opacity = 0` covers nothing — a geometry-only test would report it",
     "surface_overlap::cross-surface overlap: the covering set::...and at any opacity above zero it covers again — 0 is the edge, not `opacity` itself",
     "surface_overlap::cross-surface overlap: the covering set::a framework fade to nothing covers nothing — the same rule, from the other contributor",
     "surface_overlap::cross-surface overlap: the covering set::a surface whose ROOT is not visible covers nothing",
     "surface_overlap::cross-surface overlap: the covering set::coverRect is the union of what a surface paints BELOW its root, not the root's own claim",
     "surface_overlap::cross-surface overlap: the cost is zero off the inspection path::a frame NEVER scans — the whole mechanism runs only when diagnostics() is read",
     "surface_overlap::cross-surface overlap: the cost is zero off the inspection path::...and a single-surface target does not scan even when it IS read",
     "surface_overlap::cross-surface overlap: the cost is zero off the inspection path::...and two surfaces scan exactly once per read",
     "surface_overlap::cross-surface overlap: the cost is zero off the inspection path::two surfaces on DIFFERENT targets never see each other",
     "hud_chrome_rotation::the HUD under a viewport that changes UNDER the mounted tree::a portrait -> landscape rotation lands every zone where a FRESH landscape mount does",
     "hud_chrome_rotation::the HUD under a viewport that changes UNDER the mounted tree::...and back again: the portrait layout is recovered exactly, so the change is not one-way",
     "hud_chrome_rotation::director 2026-08-15 round 2: anchored content never touches the physical edge::401x777: every anchored zone clears BOTH lateral edges by the theme's own step",
     "hud_chrome_rotation::director 2026-08-15 round 2: anchored content never touches the physical edge::749x380: every anchored zone clears BOTH lateral edges by the theme's own step",
     "hud_chrome_rotation::director 2026-08-15 round 2: anchored content never touches the physical edge::a REAL safe-area inset still wins when it is bigger than the gutter",
     "hud_chrome_rotation::director item 4: the APP's chrome is reserved per column::a column the chrome does not cover starts LEVEL with it, not below it",
     "hud_chrome_rotation::director item 4: the APP's chrome is reserved per column::...and a column the chrome barely clips still gives way when its CONTENT reaches in",
     "hud_chrome_rotation::director item 4: the APP's chrome is reserved per column::NOTHING declared is byte-for-byte the bounding box this HUD always spent",
     "hud_chrome_rotation::director item 4: the APP's chrome is reserved per column::the declared chrome is REPORTED as occupied, so a collision check can see it",
     "hud_chrome_rotation::director item 5: the URL bar is a box you can see, and the HUD re-flows around it::the drawn bar is EXACTLY the height the HUD lost",
     "hud_chrome_rotation::director item 5: the URL bar is a box you can see, and the HUD re-flows around it::it RE-FLOWS first: the zones the player reaches for survive, and what leaves is the tail of the rank ladder",
     "hud_chrome_rotation::director 2026-08-15 round 3: the ladder may not buy the app's chrome::749x380: the left column never paints into the app's chrome, bar closed or open",
     "hud_chrome_rotation::director 2026-08-15 round 3: the ladder may not buy the app's chrome::749x300: the left column never paints into the app's chrome, bar closed or open",
     "hud_chrome_rotation::director 2026-08-15 round 3: the ladder may not buy the app's chrome::749x240: the left column never paints into the app's chrome, bar closed or open",
     "hud_chrome_rotation::director 2026-08-15 round 3: the ladder may not buy the app's chrome::749x180: the left column never paints into the app's chrome, bar closed or open",
     "hud_chrome_rotation::director 2026-08-15 round 3: the ladder may not buy the app's chrome::401x777: the left column never paints into the app's chrome, bar closed or open",
     "hud_chrome_rotation::director 2026-08-15 round 3: the ladder may not buy the app's chrome::320x640: the left column never paints into the app's chrome, bar closed or open",
     "hud_chrome_rotation::director 2026-08-15 round 3: the ladder may not buy the app's chrome::the reservation is never DROPPED while the chrome is declared — and the ladder was under real pressure",
     "hud_chrome_rotation::director 2026-08-15 round 3: the ladder may not buy the app's chrome::when NOTHING fits: the exclusion holds, the tail is gone, and what is left sits BELOW it",
     "hud_chrome_rotation::director 2026-08-15 round 3: the ladder may not buy the app's chrome::the pseudo URL bar leaves the app's chrome its room — and takes the old height where there is none",
     "hud_chrome_rotation::director 2026-08-15 round 3: the ladder may not buy the app's chrome::NULL RESULT: the ladder already steps a rank-1 region down before it drops a rank-9 one",
     "hud_chrome_rotation::DIR-5: give-way decisions re-evaluate when the platform facts settle::portrait -> landscape: the six facts UNBATCHED settle on the batched decision, zone for zone",
     "hud_chrome_rotation::DIR-5: give-way decisions re-evaluate when the platform facts settle::landscape -> portrait: the six facts UNBATCHED settle on the batched decision, zone for zone",
     "hud_chrome_rotation::DIR-5: give-way decisions re-evaluate when the platform facts settle::a theme swap re-runs the give-way pass: swapped == mounted-there, both ways",
     "hud_chrome_rotation::DIR-5: give-way decisions re-evaluate when the platform facts settle::app chrome that arrives after the first solve re-runs the pass too",
     "hud_chrome_rotation::DIR-2: the beside-chrome band gives way at the measured collision shape::360x691 under fantasy-ornate: NOTHING the HUD paints lands inside the chip row",
     "hud_chrome_rotation::DIR-2: the beside-chrome band gives way at the measured collision shape::...and the right column WRAPPED BELOW the row rather than being deleted",
     "hud_chrome_rotation::DIR-2: the beside-chrome band gives way at the measured collision shape::DIR-7: the objective chip fits its styled text or swaps to the short rung — never spills",
     "hud_chrome_rotation::DIR-2: the beside-chrome band gives way at the measured collision shape::CONTROL: narrow the row until the right column clears it and the rail is level again",
     "hud_chrome_rotation::H1: every input that can move a give-way decision settles where a fresh mount lands::viewport: changing only this input settles where a FRESH mount at that state lands",
     "hud_chrome_rotation::H1: every input that can move a give-way decision settles where a fresh mount lands::preferredTextOffset: changing only this input settles where a FRESH mount at that state lands",
     "hud_chrome_rotation::H1: every input that can move a give-way decision settles where a fresh mount lands::themeMetrics: changing only this input settles where a FRESH mount at that state lands",
     "hud_chrome_rotation::H1: every input that can move a give-way decision settles where a fresh mount lands::appChromeRects: changing only this input settles where a FRESH mount at that state lands",
     "hud_chrome_rotation::H1: every input that can move a give-way decision settles where a fresh mount lands::riding: changing only this input settles where a FRESH mount at that state lands",
     "hud_chrome_rotation::H1: every input that can move a give-way decision settles where a fresh mount lands::urlBar: changing only this input settles where a FRESH mount at that state lands",
     "hud_chrome_rotation::H1: every input that can move a give-way decision settles where a fresh mount lands::COVERAGE: every input that can move a give-way decision has a case, and none is listed without one",
     "elision_recovery::D7.1 — a region states where the content a reduced form stops showing went::REQUIRES `recover` on a region with more than one form, and names it",
     "elision_recovery::D7.1 — a region states where the content a reduced form stops showing went::...and a THREE-form region is refused the same way — it is not a two-form rule",
     "elision_recovery::D7.1 — a region states where the content a reduced form stops showing went::REFUSES `recover` on a one-form region: there is nothing there to choose",
     "elision_recovery::D7.1 — a region states where the content a reduced form stops showing went::...and the pure normalizer does NOT, because a `When`-gated form comes and goes",
     "elision_recovery::D7.1 — a region states where the content a reduced form stops showing went::refuses an unknown route — a closed spec, never a silent no-op",
     "elision_recovery::D7.1 — a region states where the content a reduced form stops showing went::refuses \"none\" together with mayDrop — dropping shows NOTHING",
     "elision_recovery::D7.1 — a region states where the content a reduced form stops showing went::accepts all three routes, and carries the declared one through normalize",
     "elision_recovery::D7.1 — `elided` beside `dropped`, and the list an overflow surface reads::with room, nothing is elided and the list is EMPTY — that is what makes it additive",
     "elision_recovery::D7.1 — `elided` beside `dropped`, and the list an overflow surface reads::a stepped-down region reports `elided`, and `dropped` stays false",
     "elision_recovery::D7.1 — `elided` beside `dropped`, and the list an overflow surface reads::a DROPPED region is `elided = false`: it is showing nothing, not showing less",
     "elision_recovery::D7.1 — `elided` beside `dropped`, and the list an overflow surface reads::the unshown list OMITS a `recover = \"none\"` region and KEEPS the other two",
     "elision_recovery::D7.1 — `elided` beside `dropped`, and the list an overflow surface reads::a dropped region's route is ALWAYS overflow, whatever its `recover` said",
     "elision_recovery::D7.1 — `elided` beside `dropped`, and the list an overflow surface reads::the list is in DECLARATION order, so it reads with `regions` and never hash order",
     "elision_recovery::D7.1 — `elided` beside `dropped`, and the list an overflow surface reads::the dump carries both, and two calls are equal",
     "elision_recovery::D7.1 — `elided` beside `dropped`, and the list an overflow surface reads::`simplified` lists exactly the ON-SCREEN regions whose active form is not their richest",
     "elision_recovery::D7.1 — `elided` beside `dropped`, and the list an overflow surface reads::a DROPPED region never appears in `simplified` — it is showing nothing, not showing less",
     "elision_recovery::D7.1 — `elided` beside `dropped`, and the list an overflow surface reads::each entry carries the ACTIVE form index, matching `regionById`",
     "elision_recovery::D7.1 — `elided` beside `dropped`, and the list an overflow surface reads::empty when nothing has stepped down — additive: a reader that never heard of it sees what it always did",
     "elision_recovery::D7.1 — `elided` beside `dropped`, and the list an overflow surface reads::the dump carries it too, in the same order as `regions`",
     "elision_recovery::D7.2 — a `self` route with no route left in it is still an authoring error::refuses a LAST form with nothing focusable in it — under expand = \"none\"",
     "elision_recovery::D7.2 — a `self` route with no route left in it is still an authoring error::...and a MIDDLE form too — the ladder can stop at any rung",
     "elision_recovery::D7.2 — a `self` route with no route left in it is still an authoring error::...and accepts every one of them once the framework is supplying the route",
     "elision_recovery::D7.2 — a `self` route with no route left in it is still an authoring error::accepts a form whose focusable is NESTED — the walk is the whole subtree",
     "elision_recovery::D7.2 — a `self` route with no route left in it is still an authoring error::asks NOTHING of an \"overflow\" or \"none\" route — the sink is what carries those",
     "elision_recovery::D7.2 — a `self` route with no route left in it is still an authoring error::...and the RICHEST form is exempt: it is not showing less than anything",
     "elision_recovery::D7.6 — every elided or dropped region has a live recovery route, at every viewport::sweeps the device matrix in both URL-bar states",
     "elision_recovery::D7.6 — every elided or dropped region has a live recovery route, at every viewport::the sink's host cannot elide and cannot drop, by construction",
     "elision_recovery::D7.6 — every elided or dropped region has a live recovery route, at every viewport::...and declaring a second form on it is refused, because `recover` would then be required",
     "elision_recovery::D7 — the HUD showcase discloses what it stops showing::the sink OPENS and lists the content the screen is not showing",
     "elision_recovery::D7 — the HUD showcase discloses what it stops showing::the sink's own copy survives a 1.4x pseudo-localization with zero findings",
     "elision_recovery::D7 — the HUD showcase discloses what it stops showing::the drawn bar is the height it steals, and its field is the theme's, not a slab",
     "elision_recovery::D7 — the HUD showcase discloses what it stops showing::the fps readout is now the first thing to give way, and the scores have a route",
     "elision_recovery::D7 — the HUD showcase discloses what it stops showing::the objective chip is inside the platform's band at 749x380, not above the screen",
     "adaptation_reversibility::the path BETWEEN viewports is reversible — a ladder that steps down steps back up::A -> B -> A lands on the resolution a FRESH mount at A produces, for every ordered pair",
     "adaptation_reversibility::the path BETWEEN viewports is reversible — a ladder that steps down steps back up::...and a URL-bar episode in the middle leaves nothing behind",
     "adaptation_reversibility::the path BETWEEN viewports is reversible — a ladder that steps down steps back up::the overflow sink is still reachable after every round trip",
     "adaptation_reversibility::the recovery route discloses the SAME content the richest form shows::the task panel's rows reach the sink whole — names, rewards and bars, not a caption",
     "adaptation_reversibility::the recovery route discloses the SAME content the richest form shows::the sink says 'nothing is hidden' only when the dump agrees, in both directions",
     "adaptation_reversibility::the recovery route discloses the SAME content the richest form shows::...and the weapon rail's ammo counts reach it the same way",
     "reference/glade_spec::Glade reference proof — module contract::exposes the scenario build contract",
     "reference/glade_spec::Glade reference proof — module contract::mounts headlessly through the public presenter with no core error",
     "reference/glade_spec::Glade reference proof — module contract::publishes its spec substitutions in the report rather than only in comments",
     "reference/glade_spec::Glade S1: the overview grid (spec §7)::mounts one card per seeded glade, in creation order",
     "reference/glade_spec::Glade S1: the overview grid (spec §7)::shows all three supply states on the first frame — full, low and empty",
     "reference/glade_spec::Glade S1: the overview grid (spec §7)::the favourite star is a SIBLING of the card, not inside its activation surface",
     "reference/glade_spec::Glade S1: the overview grid (spec §7)::search filters case-insensitively and says so when nothing matches",
     "reference/glade_spec::Glade S1: the overview grid (spec §7)::a visit suggestion fills the filter with that glade's name",
     "reference/glade_spec::Glade S1: the overview grid (spec §7)::the charm offer card is present while the tier is none and leaves on Dismiss",
     "reference/glade_spec::Glade: supply drain, low and empty (spec §1, §6)::nectar crosses low at 7.5 minutes and empty at 9, on the injected clock alone",
     "reference/glade_spec::Glade: supply drain, low and empty (spec §1, §6)::dew uses its own constants, and the two never share a threshold",
     "reference/glade_spec::Glade: supply drain, low and empty (spec §1, §6)::a refill stamps the clock, restores the level, and the ring drains again",
     "reference/glade_spec::Glade S2: the detail, in both of its hosts (spec §8)::regular and wide mount the detail in the shell's own lane",
     "reference/glade_spec::Glade S2: the detail, in both of its hosts (spec §8)::compact presents the same body as a modal with a Back button",
     "reference/glade_spec::Glade S2: the detail, in both of its hosts (spec §8)::the dew row is an instant refill: no confirmation, a commit event and a keyed toast",
     "reference/glade_spec::Glade S2: the detail, in both of its hosts (spec §8)::a glade with no seeded visits shows the empty state, and one with visits lists them",
     "reference/glade_spec::Glade S2: the detail, in both of its hosts (spec §8)::the low and empty chips are FORM, not a second hue: same ring tint in every state",
     "reference/glade_spec::Glade: the stray corner stroke and the circle doctrine (task PAINT, director items 11/12)::item 11: the card's activation Button classifies to NO chrome slot at all — surface = plain",
     "reference/glade_spec::Glade: the stray corner stroke and the circle doctrine (task PAINT, director items 11/12)::item 11 fix round 1: a plain surface no longer bundles chrome suppression with interaction suppression (native)",
     "reference/glade_spec::Glade: the stray corner stroke and the circle doctrine (task PAINT, director items 11/12)::item 11 fix round 1: a plain surface no longer bundles chrome suppression with interaction suppression (fallback)",
     "reference/glade_spec::Glade: the stray corner stroke and the circle doctrine (task PAINT, director items 11/12)::item 11 fix round 1: the press-dip and the hover/pressed fill are the mechanism this restores — both gate on exactly the flag the branches above now leave false for `plain`",
     "reference/glade_spec::Glade: the stray corner stroke and the circle doctrine (task PAINT, director items 11/12)::item 12: the supply ring is a TRUE 1:1 box via shape = circle, byte-identical geometry to before",
     "reference/glade_spec::Glade: the stray corner stroke and the circle doctrine (task PAINT, director items 11/12)::item 12: the dew ring's identity dot is CENTRED, not pinned to the top-left corner",
     "reference/glade_spec::Glade: the stray corner stroke and the circle doctrine (task PAINT, director items 11/12)::item 12: the nectar ring's identity mark (the Jar swap) is centred the same way",
     "reference/glade_spec::Glade: the stray corner stroke and the circle doctrine (task PAINT, director items 11/12)::item 12: whatisthis.png IS the deliberate glance plate, not a leaked mount — same rings, same fix",
     "reference/glade_spec::Glade S3/S4: the purchase command's four phases (spec §9)::premium with stock offers Use 1 and decrements the satchel; standard offers Choose",
     "reference/glade_spec::Glade S3/S4: the purchase command's four phases (spec §9)::a premium nectar with nothing in the satchel opens the shop instead of using one",
     "reference/glade_spec::Glade S3/S4: the purchase command's four phases (spec §9)::a standard nectar assigns and refills in one act",
     "reference/glade_spec::Glade S3/S4: the purchase command's four phases (spec §9)::idle → pending → confirmed: the button says Confirming…, then the count counts up",
     "reference/glade_spec::Glade S3/S4: the purchase command's four phases (spec §9)::the second attempt is the scripted `declined` rejection, with its copy under the card",
     "reference/glade_spec::Glade S3/S4: the purchase command's four phases (spec §9)::every declared rejection reason has player-facing copy, including the fallback",
     "reference/glade_spec::Glade S3/S4: the purchase command's four phases (spec §9)::while a purchase is pending, the picker's card for that nectar refuses to pretend",
     "reference/glade_spec::Glade S3/S4: the purchase command's four phases (spec §9)::the price is a service string the UI never composes",
     "reference/glade_spec::Glade S5: the Keeper's Charm and its upgrade-only mode (spec §10)::buying a tier removes it from the offer and toasts the new standing",
     "reference/glade_spec::Glade S5: the Keeper's Charm and its upgrade-only mode (spec §10)::the scripted `owned` rejection shows its own copy and the retry then confirms",
     "reference/glade_spec::Glade S5: the Keeper's Charm and its upgrade-only mode (spec §10)::a tier at or below the one held can never be sold again, whatever the fixture says next",
     "reference/glade_spec::Glade S5: the Keeper's Charm and its upgrade-only mode (spec §10)::Lumen drops the Frostwisp gate AND opens its early arrival window",
     "reference/glade_spec::Glade S5: the Keeper's Charm and its upgrade-only mode (spec §10)::the offer card leaves S1 the moment a tier is held",
     "reference/glade_spec::Glade S6/S7: wisp info, restore, the edit form and Fresh Start (spec §10)::a visitor row opens the wisp modal, which offers the charm while gated",
     "reference/glade_spec::Glade S6/S7: wisp info, restore, the edit form and Fresh Start (spec §10)::the wisp modal states when and where a species was last seen",
     "reference/glade_spec::Glade S6/S7: wisp info, restore, the edit form and Fresh Start (spec §10)::restore runs the same three-phase command and toasts on confirm",
     "reference/glade_spec::Glade S6/S7: wisp info, restore, the edit form and Fresh Start (spec §10)::the edit form commits on Done and discards on Cancel",
     "reference/glade_spec::Glade S6/S7: wisp info, restore, the edit form and Fresh Start (spec §10)::Fresh Start asks first, keeps the world on Keep, and reseeds it on Start over",
     "reference/glade_spec::Glade: determinism — same seed, same steps, same dump::two independent worlds driven identically produce identical dumps",
     "reference/glade_spec::Glade: determinism — same seed, same steps, same dump::Fresh Start returns a played world to its opening dump, byte for byte",
     "reference/glade_spec::Glade: determinism — same seed, same steps, same dump::the dump is stable across repeated reads with nothing changed in between",
     "reference/glade_spec::Glade: toasts (spec §12)::a refill toast is keyed per glade, so the same subject never doubles up",
     "reference/glade_spec::Glade: toasts (spec §12)::two different glades are two different subjects",
     "reference/glade_spec::Glade: toasts (spec §12)::nothing in the loop depends on reading a toast: the state is visible in place",
     "reference/glade_spec::Glade: adaptation, one tree (spec §4)::navPlacement gives every canvas its home: sidebar / bottom tabs / inline tabs / top tabs",
     "reference/glade_spec::Glade: adaptation, one tree (spec §4)::the top bar HUGS its tabs, centered — never full width (tablet / ten-foot shape)",
     "reference/glade_spec::Glade: adaptation, one tree (spec §4)::compact puts the TAB BAND BELOW the content, never over it",
     "reference/glade_spec::Glade: adaptation, one tree (spec §4)::no VISIBLE bottom tab is cut off in the normal text range",
     "reference/glade_spec::Glade: adaptation, one tree (spec §4)::the terse vocabulary is REAL: the thumb-zone band reads a different word than the rail",
     "reference/glade_spec::Glade: adaptation, one tree (spec §4)::a live resize moves the nav between homes and the world survives the move",
     "reference/glade_spec::Glade: adaptation, one tree (spec §4)::the four sections are reachable from the nav in every home",
     "reference/glade_spec::Glade: adaptation, one tree (spec §4)::the detail panes still adapt by MEASUREMENT: beside, then folded under",
     "reference/glade_spec::Glade: focus and reachability (spec §4, §14)::the base surface opens on the first glade card, not on the search field",
     "reference/glade_spec::Glade: focus and reachability (spec §4, §14)::focus order follows the nav's PLACEMENT: a leading rail leads the ring, a bottom band trails it",
     "reference/glade_spec::Glade: focus and reachability (spec §4, §14)::the linear and directional readings cover the same nodes",
     "reference/glade_spec::Glade: focus and reachability (spec §4, §14)::every modal can reach its own dismissal control from its focus map",
     "reference/glade_spec::Glade: focus and reachability (spec §4, §14)::the charm sheet opens on the recommended tier, and never on a card that is gone",
     "reference/glade_spec::Glade: focus and reachability (spec §4, §14)::the picker opens on the first premium card's action button",
     "reference/glade_spec::Glade nav — every placement is traversable (enter, through, away, action)::sidebar (pointer desktop)",
     "reference/glade_spec::Glade nav — every placement is traversable (enter, through, away, action)::bottom tabs (phone portrait)",
     "reference/glade_spec::Glade nav — every placement is traversable (enter, through, away, action)::inline bottom tabs (phone landscape)",
     "reference/glade_spec::Glade nav — every placement is traversable (enter, through, away, action)::top tabs, driven as a PAD drives them: DPad to the bar, across it, ButtonA, DPad away",
     "reference/glade_spec::Glade: localization and the expansion locale (spec §13)::every key exists in both locales and `xa` expands by exactly 1.4×",
     "reference/glade_spec::Glade: localization and the expansion locale (spec §13)::expansion never eats a placeholder",
     "reference/glade_spec::Glade: localization and the expansion locale (spec §13)::a locale swap relabels the live tree without a remount",
     "reference/glade_spec::Glade: localization and the expansion locale (spec §13)::an unknown locale falls back rather than blanking the screen",
     "reference/glade_spec::Glade: localization and the expansion locale (spec §13)::remaining and relative times are proof-owned and single-unit",
     "reference/glade_spec::Glade: motion (spec §11)::the wisp fly-in runs once per visit and completes",
     "reference/glade_spec::Glade: motion (spec §11)::reduced motion places the same states and fires the same events",
     "reference/glade_spec::Glade: motion (spec §11)::the ring's painted arc chases the level and lands on it",
     "reference/glade_spec::Glade: the responsibility ledger's forbidden list::scans the whole proof, and there is a whole proof to scan",
     "reference/glade_spec::Glade: the responsibility ledger's forbidden list::no wall clock and no random anywhere in the proof",
     "reference/glade_spec::Glade: the responsibility ledger's forbidden list::no engine reach-around: no Instance, no service, no input listener, no adapter write",
     "reference/glade_spec::Glade: the responsibility ledger's forbidden list::no device-name or platform-name branch: facts and size classes only",
     "reference/glade_spec::Glade: the responsibility ledger's forbidden list::no raw colour reaches a role's place: identity hues ride the declared tint form",
     "reference/glade_spec::Glade: the responsibility ledger's forbidden list::the bite is real: the scanner sees the patterns it bans when they are present",
     "reference/glade_spec::Glade: the stage axes it can answer headlessly (RA-M3/M4/M5)::a theme-package swap re-solves the same tree — no remount, no source edit",
     "reference/glade_spec::Glade: the stage axes it can answer headlessly (RA-M3/M4/M5)::every preferred-text step re-solves without an error or a lost selection",
     "reference/glade_spec::Glade: the stage axes it can answer headlessly (RA-M3/M4/M5)::the expansion locale reflows the whole loop with no error and no lost state",
     "reference/glade_spec::Glade: the scenario surface::every step the scenario runner exposes is callable and answers something",
     "reference/glade_spec::Glade: the scenario surface::Flora is browse-only: cards with a name and a species line, and no modal",
     "reference/glade_spec::Glade: the scenario surface::the shop's hero card is the declared best value, with its own chip",
     "reference/glade_spec::Glade: the scenario surface::two fresh builds are identical (reset determinism) and dispose cleanly",
     "reference/glade_spec::Glade: the scenario surface::the five-view sweep is diagnostic-clean with a live scrollbar (matrix pin)",
     "reference/glade_spec::Glade detail — the locale x width x selection sweep the matrix pin could not see::an OPEN detail is diagnostic-clean in both locales at every canonical width",
     "reference/glade_spec::Glade — the played task: seeded start, both orders, depletion, and the reset::the task names a glade, a wisp and a STANDARD nectar, so the shop is never on the path",
     "reference/glade_spec::Glade — the played task: seeded start, both orders, depletion, and the reset::the seeded start has BOTH rows undone, and the opening screen says what to do",
     "reference/glade_spec::Glade — the played task: seeded start, both orders, depletion, and the reset::the named first action opens the named glade, and each act ticks exactly its own row",
     "reference/glade_spec::Glade — the played task: seeded start, both orders, depletion, and the reset::either order finishes it: nectar first leaves the DEW row as the one thing left",
     "reference/glade_spec::Glade — the played task: seeded start, both orders, depletion, and the reset::the clock can take a row back, and the task SAYS SO and stays finishable",
     "reference/glade_spec::Glade — the played task: seeded start, both orders, depletion, and the reset::the wisp ARRIVES when the glade is ready — not when it is merely opened",
     "reference/glade_spec::Glade — the played task: seeded start, both orders, depletion, and the reset::the completion state is unmistakable, says what was accomplished, and offers ONE reset",
     "reference/glade_spec::Glade — the played task: seeded start, both orders, depletion, and the reset::'Prepare again' returns EVERY observable to the seeded start",
     "reference/glade_spec::Glade — the played task: seeded start, both orders, depletion, and the reset::'What this shows' is collapsed by default and comes AFTER the play task",
     "reference/glade_spec::Glade — the played task: seeded start, both orders, depletion, and the reset::the task is placed ON the app: every glade, section and browse verb survives it",
     "reference/glade_spec::Glade — the played task: seeded start, both orders, depletion, and the reset::on a landscape phone the strip yields and the CARD still carries the whole task",
     "reference/glade_spec::Glade — the played task: seeded start, both orders, depletion, and the reset::the strip costs ONE line box at every preference and in both locales",
     "reference/glade_spec::Glade — the player surface names no stage, proof or ledger::no locale string a player can read carries test vocabulary",
     "reference/glade_spec::Glade — the standalone place and the showcase scenario import one module::the scenario resolves the reference module and forks nothing",
     "reference/glade_spec::Glade — the standalone place and the showcase scenario import one module::the standalone place maps the same two folders the gallery does",
     "reference/glade_spec::Glade — the standalone place and the showcase scenario import one module::exactly one directory on disk IS this app",
     "reference/cartwheel_spec::Cartwheel reference proof — module contract::exposes the scenario build contract",
     "reference/cartwheel_spec::Cartwheel reference proof — module contract::mounts headlessly, presents itself, and leaves the core error-free",
     "reference/cartwheel_spec::Cartwheel reference proof — module contract::declares its stage content against the Stage node's own path",
     "reference/cartwheel_spec::Cartwheel reference proof — module contract::borrows only art the framework actually ships (ids pinned)",
     "reference/cartwheel_spec::Cartwheel — the shell::opens on The Cart with the nav lane showing and no pushed detail",
     "reference/cartwheel_spec::Cartwheel — the shell::a sidebar row selects its section — through the row, not the signal",
     "reference/cartwheel_spec::Cartwheel — the shell::SELECTION RESETS PUSHED DETAIL: a sidebar change lands on the section root",
     "reference/cartwheel_spec::Cartwheel — the shell::compact stacks: picking a section empties the nav lane, Back pops one level",
     "reference/cartwheel_spec::Cartwheel — every section mounts::visits all nine and leaves the core error-free, in both locales",
     "reference/cartwheel_spec::Cartwheel — brews: modes, search, sort::regular mounts the table; compact mounts the sectioned list",
     "reference/cartwheel_spec::Cartwheel — brews: modes, search, sort::the DEFAULT SORT is Status ascending, ties Placed descending",
     "reference/cartwheel_spec::Cartwheel — brews: modes, search, sort::sorting by a column cycles it and re-derives the order",
     "reference/cartwheel_spec::Cartwheel — brews: modes, search, sort::search matches the brew number, its potions AND their ingredients",
     "reference/cartwheel_spec::Cartwheel — brews: modes, search, sort::the sectioned list omits empty status groups and heads the rest",
     "reference/cartwheel_spec::Cartwheel — brews: modes, search, sort::BULK COMPLETE EXISTS IN LIST MODE TOO (the reference's table-only gap)",
     "reference/cartwheel_spec::Cartwheel — brews: modes, search, sort::an empty search says so rather than showing an empty page",
     "reference/cartwheel_spec::Cartwheel — brews: modes, search, sort::Cancel exits selection mode before it pops navigation",
     "reference/cartwheel_spec::Cartwheel — the status machine::New -> Brewing -> Done through the one primary control",
     "reference/cartwheel_spec::Cartwheel — the status machine::a seeded Ready brew collects straight to Done",
     "reference/cartwheel_spec::Cartwheel — the status machine::collecting an already-collected brew is REJECTED, with copy, and reverts",
     "reference/cartwheel_spec::Cartwheel — the status machine::the command's pending state is REAL: the control disables while it runs",
     "reference/cartwheel_spec::Cartwheel — the status machine::THE COUNTDOWN SURVIVES NAVIGATION (the service owns the fact, not the view)",
     "reference/cartwheel_spec::Cartwheel — the status machine::at zero the countdown says Ready to finish and the status does NOT advance",
     "reference/cartwheel_spec::Cartwheel — the status machine::the countdown wears ONE time form: a depleting M:SS",
     "reference/cartwheel_spec::Cartwheel — Cancel is really wired (the contribution, not the function)::ButtonB pops one compact level and is a NO-OP at the root",
     "reference/cartwheel_spec::Cartwheel — a rejection is never silent::presents a toast carrying the specific reason",
     "reference/cartwheel_spec::Cartwheel — the completion celebration::opens on the confirming frame and closes on Done",
     "reference/cartwheel_spec::Cartwheel — the completion celebration::the scripted beats run in order and a mid-sequence dismissal interrupts",
     "reference/cartwheel_spec::Cartwheel — the completion celebration::a celebrate event reaches the feedback bus",
     "reference/cartwheel_spec::Cartwheel — scripted arrivals::arrive in id order at seeded intervals and then stop",
     "reference/cartwheel_spec::Cartwheel — scripted arrivals::the seeded intervals are inside the declared 3-8 s band",
     "reference/cartwheel_spec::Cartwheel — the dashboard reacts to an arrival::keys the newest brew into the hero slot and re-aims the tally",
     "reference/cartwheel_spec::Cartwheel — the tally's pulse is declared, not written per frame::drives no imperative transform for the footer, and still paints the nudge",
     "reference/cartwheel_spec::Cartwheel — the unwired popup adapts to the surface (ADAPT-1)::a touch phone resolves the SHEET; a desktop pointer never does",
     "reference/cartwheel_spec::Cartwheel — the unwired popup adapts to the surface (ADAPT-1)::the same device, one input class later: the idiom follows the thumb",
     "reference/cartwheel_spec::Cartwheel — gallery and workbench::the composite sort re-derives the order per key",
     "reference/cartwheel_spec::Cartwheel — gallery and workbench::grid and table are two forms of ONE selection",
     "reference/cartwheel_spec::Cartwheel — gallery and workbench::a gallery cell opens the Workbench on that potion",
     "reference/cartwheel_spec::Cartwheel — gallery and workbench::an edit moves the attributes and the preview on the same frame — no Save",
     "reference/cartwheel_spec::Cartwheel — gallery and workbench::an edit survives leaving the Workbench and coming back",
     "reference/cartwheel_spec::Cartwheel — entitlement, gate and lock::BOTH purchase outcomes are reachable: the first declines, the second confirms",
     "reference/cartwheel_spec::Cartwheel — entitlement, gate and lock::a gated timeframe is LOCKED while unentitled: the paths are not mounted",
     "reference/cartwheel_spec::Cartwheel — entitlement, gate and lock::the upsell has two states and disappears once entitled",
     "reference/cartwheel_spec::Cartwheel — entitlement, gate and lock::the feed gate flips the whole section on one entitlement fact",
     "reference/cartwheel_spec::Cartwheel — the Guild Card (Variant B, unmasked)::ships the UNMASKED variant with its disclosure caption, and says so on screen",
     "reference/cartwheel_spec::Cartwheel — the Guild Card (Variant B, unmasked)::is validation-gated and rejects a taken name at the field",
     "reference/cartwheel_spec::Cartwheel — signing out asks first::opens a confirm modal and only then drops the fact",
     "reference/cartwheel_spec::Cartwheel — the Plaza stage and its fallback::uses the host when the adapter has the seam, and paints no plate",
     "reference/cartwheel_spec::Cartwheel — the Plaza stage and its fallback::can open straight on a plaza, so the runner's one-shot stage content lands",
     "reference/cartwheel_spec::Cartwheel — the Plaza stage and its fallback::PAINTS THE DECLARED FALLBACK PLATE when stageHost answers nil",
     "reference/cartwheel_spec::Cartwheel — the Plaza stage and its fallback::the proof drives the orbit through the PUBLIC handle, and parks it when motion is reduced",
     "reference/cartwheel_spec::Cartwheel — the Plaza stage and its fallback::the recommendation is the first plaza with no damp spell",
     "reference/cartwheel_spec::Cartwheel — the loop closes: charts reflect the change::a collected brew's bottles reach StatsService, Top Five and the Ledger",
     "reference/cartwheel_spec::Cartwheel — the solver's own verdict::reports NO layout diagnostics on any real viewport, in any section",
     "reference/cartwheel_spec::Cartwheel — the solver's own verdict::...and none on the pushed detail, the celebration, or the sign-up form either",
     "reference/cartwheel_spec::Cartwheel — the solver's own verdict::every structural band takes the ten-foot factor, not just the one that was remembered",
     "reference/cartwheel_spec::Cartwheel — the solver's own verdict::below every shipping device the shell falls back to the column, and SAYS the minimum it could not meet",
     "reference/cartwheel_spec::Cartwheel — the solver's own verdict::mounts under BOTH reference theme packages with no source edit",
     "reference/cartwheel_spec::Cartwheel — determinism and lifecycle::SAME SEED + SAME STEPS => IDENTICAL REPORT",
     "reference/cartwheel_spec::Cartwheel — determinism and lifecycle::a different seed produces a different world (the seed is really injected)",
     "reference/cartwheel_spec::Cartwheel — determinism and lifecycle::build + drive + dispose is registry-neutral",
     "reference/cartwheel_spec::Cartwheel — strings and the pseudo-locale::every `xa` string is at least 1.4x its English source",
     "reference/cartwheel_spec::Cartwheel — strings and the pseudo-locale::the pseudo-locale keeps every {placeholder} intact",
     "reference/cartwheel_spec::Cartwheel — strings and the pseudo-locale::the locale fact reaches the screens",
     "reference/cartwheel_spec::Cartwheel — reduced motion::mounts and drives identically with motion reduced",
     "reference/cartwheel_spec::Cartwheel — reduced motion::the five-view sweep is diagnostic-clean with a live scrollbar (matrix pin)",
     "reference/cartwheel_spec::Cartwheel — the rows that WRAP (parity round 3)::THE PLAZA SUMMARY IS ONE ROW: each chip at ONE path, none at a zero rect",
     "reference/cartwheel_spec::Cartwheel — the rows that WRAP (parity round 3)::THE TAG CLOUD IS RAGGED AND MULTI-LINE, where a uniform grid gave one width",
     "reference/cartwheel_spec::Cartwheel — the rows that WRAP (parity round 3)::A POST'S TAGS AND THE CHART LEGEND KEEP THEIR OWN WIDTHS",
     "reference/cartwheel_spec::Cartwheel — backdrop-finding sweep (task POP)::every named section, the subscribe/guild/brew overlays, and a purchase pass are clean",
     "reference/cartwheel_spec::Cartwheel — the declaration does not leak into the rest of the suite::retires its app namespace",
     "reference/sipworks_spec::Sipworks reference proof — module contract and first mount::exposes the scenario build contract",
     "reference/sipworks_spec::Sipworks reference proof — module contract and first mount::mounts headlessly with no core error and the whole menu visible",
     "reference/sipworks_spec::Sipworks reference proof — module contract and first mount::every player-facing string comes from the locale table (no key is missing)",
     "reference/sipworks_spec::Sipworks §5 — the shell flips, and the model survives the flip::navPlacement gives every canvas its home: sidebar / bottom tabs / inline tabs / top tabs",
     "reference/sipworks_spec::Sipworks §5 — the shell flips, and the model survives the flip::compact puts the TAB BAND BELOW the content and the accessory ABOVE the tabs",
     "reference/sipworks_spec::Sipworks §5 — the shell flips, and the model survives the flip::no bottom-bar tab ellipsizes in the normal range (the short vocabulary is the point)",
     "reference/sipworks_spec::Sipworks §5 — the shell flips, and the model survives the flip::the top bar HUGS its tabs, centered — never full width (tablet/ten-foot shape)",
     "reference/sipworks_spec::Sipworks §5 — the shell flips, and the model survives the flip::the flip is a re-solve: search text, selection and section all survive it",
     "reference/sipworks_spec::Sipworks §5 — the shell flips, and the model survives the flip::compact pushes the detail as its own surface; widening dismisses it and the pane keeps the selection",
     "reference/sipworks_spec::Sipworks §6 — one row blueprint, search, suggestions, favorites::search matches a title OR a botanical name, and never the filler",
     "reference/sipworks_spec::Sipworks §6 — one row blueprint, search, suggestions, favorites::suggestions are botanical names, exclude the filler and the exact match, and cap at six",
     "reference/sipworks_spec::Sipworks §6 — one row blueprint, search, suggestions, favorites::the query is shared across sections",
     "reference/sipworks_spec::Sipworks §6 — one row blueprint, search, suggestions, favorites::unfavoriting every blend reaches the empty state, and Browse returns to the menu",
     "reference/sipworks_spec::Sipworks §6 — one row blueprint, search, suggestions, favorites::the row heart is a real control on the shared favorites signal",
     "reference/sipworks_spec::Sipworks §7 — the adaptive header, the tiles, and the facts flip::the wide header candidate wins wide and loses on a phone",
     "reference/sipworks_spec::Sipworks §7 — the adaptive header, the tiles, and the facts flip::a tile opens the botanical card, whose facts are scaled to THIS blend's measure",
     "reference/sipworks_spec::Sipworks §7 — the adaptive header, the tiles, and the facts flip::the flip swaps faces at the midpoint and only the visible face is mounted",
     "reference/sipworks_spec::Sipworks §7 — the adaptive header, the tiles, and the facts flip::Cancel on the facts face flips to the front first; a second Cancel dismisses",
     "reference/sipworks_spec::Sipworks §8 — the order flow: guard, rejection, confirmation, ready::the guard shows a reason, presents an alert on Activate, and never starts an order",
     "reference/sipworks_spec::Sipworks §8 — the order flow: guard, rejection, confirmation, ready::the CTA runs idle -> pending -> rejected, shows a toast, and returns to idle",
     "reference/sipworks_spec::Sipworks §8 — the order flow: guard, rejection, confirmation, ready::the second order confirms, presents Order-Placed, and the disc flips at t+4s",
     "reference/sipworks_spec::Sipworks §9 — Steam Stamps: accrual, the once-per-visit pop, redemption::one stamp per confirmed order, and a rejected order earns nothing",
     "reference/sipworks_spec::Sipworks §9 — Steam Stamps: accrual, the once-per-visit pop, redemption::the pop plays once per visit and the card is static on return",
     "reference/sipworks_spec::Sipworks §9 — Steam Stamps: accrual, the once-per-visit pop, redemption::the plural caption selects other / one / zero across the threshold",
     "reference/sipworks_spec::Sipworks §9 — Steam Stamps: accrual, the once-per-visit pop, redemption::at ten stamps the CTA is the redeem verb, and redeeming debits a flat ten",
     "reference/sipworks_spec::Sipworks §10 — the Blend Book unlock and the recipe view::locked rows are ABSENT until the purchase confirms, then they enter the list",
     "reference/sipworks_spec::Sipworks §10 — the Blend Book unlock and the recipe view::no buy button is mounted while the price is still loading",
     "reference/sipworks_spec::Sipworks §10 — the Blend Book unlock and the recipe view::the batch stepper rescales every measured quantity, and the checks are session-local",
     "reference/sipworks_spec::Sipworks §13 — localization: expansion, plurals, lists, measures::the pseudo-locale expands every string by at least 1.4x and keeps placeholders",
     "reference/sipworks_spec::Sipworks §13 — localization: expansion, plurals, lists, measures::plural forms are selected in BOTH locales",
     "reference/sipworks_spec::Sipworks §13 — localization: expansion, plurals, lists, measures::lists and measures are service calls, and the measure separator follows the locale",
     "reference/sipworks_spec::Sipworks §13 — localization: expansion, plurals, lists, measures::the locale step reflows every mounted surface with no remount",
     "reference/sipworks_spec::Sipworks §11 — the compact entry flow shares the same blueprints::boots straight into compact-link from an initial fact, deep-linked to the item",
     "reference/sipworks_spec::Sipworks §11 — the compact entry flow shares the same blueprints::the compact shell hides the nav, the sections and every favorite affordance",
     "reference/sipworks_spec::Sipworks §11 — the compact entry flow shares the same blueprints::the CTA is always pay-shaped in the compact entry, even at ten stamps",
     "reference/sipworks_spec::Sipworks §11 — the compact entry flow shares the same blueprints::BOTH entries reach the SAME row blueprint function",
     "reference/sipworks_spec::Sipworks §11 — the compact entry flow shares the same blueprints::enterFull restores the whole shell with the model intact",
     "reference/sipworks_spec::Sipworks §17 — reset determinism::same seed + same steps => identical dump, in two independent worlds",
     "reference/sipworks_spec::Sipworks §17 — reset determinism::reset returns a world to its seeded start, and replaying the loop reproduces the dump",
     "reference/sipworks_spec::Sipworks §17 — reset determinism::the seed decides the starting favorites, and a different seed differs",
     "reference/sipworks_spec::Sipworks — the five-view sweep the solver itself signs off::phone-portrait / en / preferred-text +0: no solver diagnostics",
     "reference/sipworks_spec::Sipworks — the five-view sweep the solver itself signs off::phone-portrait / en / preferred-text +14: no solver diagnostics",
     "reference/sipworks_spec::Sipworks — the five-view sweep the solver itself signs off::phone-portrait / xa / preferred-text +0: no solver diagnostics",
     "reference/sipworks_spec::Sipworks — the five-view sweep the solver itself signs off::phone-portrait / xa / preferred-text +14: no solver diagnostics",
     "reference/sipworks_spec::Sipworks — the five-view sweep the solver itself signs off::phone-landscape / en / preferred-text +0: no solver diagnostics",
     "reference/sipworks_spec::Sipworks — the five-view sweep the solver itself signs off::phone-landscape / en / preferred-text +14: no solver diagnostics",
     "reference/sipworks_spec::Sipworks — the five-view sweep the solver itself signs off::phone-landscape / xa / preferred-text +0: no solver diagnostics",
     "reference/sipworks_spec::Sipworks — the five-view sweep the solver itself signs off::phone-landscape / xa / preferred-text +14: no solver diagnostics",
     "reference/sipworks_spec::Sipworks — the five-view sweep the solver itself signs off::tablet / en / preferred-text +0: no solver diagnostics",
     "reference/sipworks_spec::Sipworks — the five-view sweep the solver itself signs off::tablet / en / preferred-text +14: no solver diagnostics",
     "reference/sipworks_spec::Sipworks — the five-view sweep the solver itself signs off::tablet / xa / preferred-text +0: no solver diagnostics",
     "reference/sipworks_spec::Sipworks — the five-view sweep the solver itself signs off::tablet / xa / preferred-text +14: no solver diagnostics",
     "reference/sipworks_spec::Sipworks — the five-view sweep the solver itself signs off::desktop / en / preferred-text +0: no solver diagnostics",
     "reference/sipworks_spec::Sipworks — the five-view sweep the solver itself signs off::desktop / en / preferred-text +14: no solver diagnostics",
     "reference/sipworks_spec::Sipworks — the five-view sweep the solver itself signs off::desktop / xa / preferred-text +0: no solver diagnostics",
     "reference/sipworks_spec::Sipworks — the five-view sweep the solver itself signs off::desktop / xa / preferred-text +14: no solver diagnostics",
     "reference/sipworks_spec::Sipworks — the five-view sweep the solver itself signs off::ten-foot / en / preferred-text +0: no solver diagnostics",
     "reference/sipworks_spec::Sipworks — the five-view sweep the solver itself signs off::ten-foot / en / preferred-text +14: no solver diagnostics",
     "reference/sipworks_spec::Sipworks — the five-view sweep the solver itself signs off::ten-foot / xa / preferred-text +0: no solver diagnostics",
     "reference/sipworks_spec::Sipworks — the five-view sweep the solver itself signs off::ten-foot / xa / preferred-text +14: no solver diagnostics",
     "reference/sipworks_spec::Sipworks — reduced motion, themes, and reachability::reduced motion fires the same events with no travel",
     "reference/sipworks_spec::Sipworks — reduced motion, themes, and reachability::mounts under BOTH reference theme packages with no source edit",
     "reference/sipworks_spec::Sipworks — reduced motion, themes, and reachability::the Steam Stamps tab PRESENTS the rewards card on a wide screen — it is not a destination",
     "reference/sipworks_spec::Sipworks — reduced motion, themes, and reachability::...and on a COMPACT shape the same tab is a real destination",
     "reference/sipworks_spec::Sipworks — reduced motion, themes, and reachability::every verb in the loop is a real focusable, on every input class",
     "reference/sipworks_spec::Sipworks — reduced motion, themes, and reachability::Cancel dismisses the compact detail and returns to the shell",
     "reference/sipworks_spec::Sipworks — reduced motion, themes, and reachability::build, drive and dispose are registry-neutral",
     "reference/sipworks_spec::Sipworks — reduced motion, themes, and reachability::the five-view sweep is diagnostic-clean with a live scrollbar (matrix pin)",
     "reference/sipworks_spec::Sipworks nav — every placement is gamepad-traversable (enter, through, away, action)::sidebar (pointer desktop)",
     "reference/sipworks_spec::Sipworks nav — every placement is gamepad-traversable (enter, through, away, action)::bottom tabs (phone portrait)",
     "reference/sipworks_spec::Sipworks nav — every placement is gamepad-traversable (enter, through, away, action)::inline bottom tabs (phone landscape)",
     "reference/sipworks_spec::Sipworks nav — every placement is gamepad-traversable (enter, through, away, action)::every structural number takes the ten-foot factor, not just the one with a helper",
     "reference/sipworks_spec::Sipworks nav — every placement is gamepad-traversable (enter, through, away, action)::top tabs, driven as a PAD drives them: DPad across, ButtonA to act, DPad away",
     "reference/sipworks_spec::Sipworks — backdrop-finding sweep (task POP)::every section, the blend detail, a botanical tile and the order/rewards overlays are clean",
     "reference/sipworks_spec::Sipworks — the played task: seeded start, the loop, and the reset::the seeded start is ONE stamp short, and the opening screen says so",
     "reference/sipworks_spec::Sipworks — the played task: seeded start, the loop, and the reset::the named first action opens the blend it names, through the row's own path",
     "reference/sipworks_spec::Sipworks — the played task: seeded start, the loop, and the reset::a refused order explains itself, keeps every stamp, and the retry is the same verb",
     "reference/sipworks_spec::Sipworks — the played task: seeded start, the loop, and the reset::one accepted order takes the last stamp and the task becomes the free pour",
     "reference/sipworks_spec::Sipworks — the played task: seeded start, the loop, and the reset::redeeming the free pour ends in an unmistakable completion state with one reset",
     "reference/sipworks_spec::Sipworks — the played task: seeded start, the loop, and the reset::'Serve another customer' returns EVERY observable to the seeded start",
     "reference/sipworks_spec::Sipworks — the played task: seeded start, the loop, and the reset::'What this shows' is collapsed by default and comes AFTER the play task",
     "reference/sipworks_spec::Sipworks — the played task: seeded start, the loop, and the reset::the task is placed ON the app: the catalogue and every destination survive it",
     "reference/sipworks_spec::Sipworks — the played task: seeded start, the loop, and the reset::the compact-link entry carries NO task: there is no rewards context to finish it in",
     "reference/sipworks_spec::Sipworks — the played task: seeded start, the loop, and the reset::on a landscape phone the strip yields and the CARD still carries the whole task",
     "reference/sipworks_spec::Sipworks — the played task: seeded start, the loop, and the reset::the strip costs ONE line box at every preference and in both locales",
     "reference/sipworks_spec::Sipworks — the player surface names no stage, proof or ledger::the module title is the place's name and nothing else",
     "reference/sipworks_spec::Sipworks — the player surface names no stage, proof or ledger::no locale string a player can read carries test vocabulary",
     "reference/sipworks_spec::Sipworks — the standalone place and the showcase scenario import one module::the scenario resolves the reference module and forks nothing",
     "reference/sipworks_spec::Sipworks — the standalone place and the showcase scenario import one module::the standalone place maps the same two folders the gallery does",
     "reference/sipworks_spec::Sipworks — the standalone place and the showcase scenario import one module::exactly one directory on disk IS this app",
     "reference/sipworks_spec::Sipworks — the declaration does not leak into the rest of the suite::retires its app namespace",
     "reference/foyer_spec::Foyer proof — mount and structure (RA-P4)::mounts the home body with all four sections and no core error",
     "reference/foyer_spec::Foyer proof — mount and structure (RA-P4)::wide viewport shows the nav rail, not the bottom bar; compact shows the bottom bar",
     "reference/foyer_spec::Foyer proof — mount and structure (RA-P4)::a size-class flip swaps chrome but never remounts the home body",
     "reference/foyer_spec::Foyer nav — the four placement homes (adaptive.navPlacement)::navPlacement gives every canvas its home: rail / bottom band / inline band / top band",
     "reference/foyer_spec::Foyer nav — the four placement homes (adaptive.navPlacement)::the nav band sits BELOW the content at both bottom placements",
     "reference/foyer_spec::Foyer nav — the four placement homes (adaptive.navPlacement)::the search field keeps its home in the top chrome on EVERY placement",
     "reference/foyer_spec::Foyer nav — the four placement homes (adaptive.navPlacement)::no bottom wing is ever cut off: the ladder degrades the WORD to the GLYPH",
     "reference/foyer_spec::Foyer nav — the four placement homes (adaptive.navPlacement)::no top-band wing is cut off, and the glyph rung is REACHED at the expansion locale",
     "reference/foyer_spec::Foyer nav — the four placement homes (adaptive.navPlacement)::every placement is traversable: enter, through, away, and a real action",
     "reference/foyer_spec::Foyer nav — the four placement homes (adaptive.navPlacement)::top band, driven as a PAD drives it: DPad to the band, across it, ButtonA, DPad away",
     "reference/foyer_spec::Foyer proof — tabs, search, stubs::the charts tab swaps the feed to the approval-ranked section and back",
     "reference/foyer_spec::Foyer proof — tabs, search, stubs::search filters by title and creator; clearing restores the feed",
     "reference/foyer_spec::Foyer proof — tabs, search, stubs::a no-match query shows the honest empty state with a working clear button",
     "reference/foyer_spec::Foyer proof — tabs, search, stubs::a stub destination shows the labeled stub plate, never fake content",
     "reference/foyer_spec::Foyer proof — refresh command lifecycle::refresh pends (button disabled), then confirms with re-seeded sections",
     "reference/foyer_spec::Foyer proof — refresh command lifecycle::a scripted rejection leaves the feed unchanged and re-enables the button",
     "reference/foyer_spec::Foyer proof — tile detail and the visit command::activating a tile presents its detail modal; visit pends then confirms into Keep playing and dismisses",
     "reference/foyer_spec::Foyer proof — tile detail and the visit command::a rejected visit leaves Keep playing untouched and the modal up",
     "reference/foyer_spec::Foyer proof — notices, locale, images, determinism::the bell badge counts seeded notices and clears on presenting the modal",
     "reference/foyer_spec::Foyer proof — notices, locale, images, determinism::the locale swap reaches live copy without remounting the tab button",
     "reference/foyer_spec::Foyer proof — notices, locale, images, determinism::the scripted transport resolves art deterministically: two keys stay failed, the rest ready",
     "reference/foyer_spec::Foyer proof — notices, locale, images, determinism::two fresh builds report identical seeded state (reset determinism)",
     "reference/foyer_spec::Foyer proof — notices, locale, images, determinism::the five-view sweep is diagnostic-clean with a live scrollbar (matrix pin)",
     "reference/foyer_spec::Foyer proof — notices, locale, images, determinism::dispose tears the proof down without a core error",
     "reference/foyer_spec::Foyer — the tile meta row WRAPS where it used to ladder (parity round 3)::THE LADDER'S TWIN NODES ARE GONE: no MetaColumn, no ApprovalB, no ChipB",
     "reference/foyer_spec::Foyer — the tile meta row WRAPS where it used to ladder (parity round 3)::AT NORMAL TEXT they share a line and `distribute` pushes the chip to the TRAILING edge",
     "reference/foyer_spec::Foyer — the tile meta row WRAPS where it used to ladder (parity round 3)::AT RAISED PREFERRED TEXT it REFLOWS to a second line instead of squeezing",
     "reference/foyer_spec::Foyer — the tile meta row WRAPS where it used to ladder (parity round 3)::NOT ONE `Spacer` INSTANCE IS MOUNTED: `distribute` says it on the container",
     "reference/foyer_spec::Foyer — backdrop-finding sweep (task POP)::the notices, profile and detail modals are clean",
     "reference/foyer_spec::Foyer badgeBubble clears its own control's themed chrome (task FOYER)::with no package installed, both badges sit flush at the corner — shipped geometry, unchanged",
     "reference/foyer_spec::Foyer badgeBubble clears its own control's themed chrome (task FOYER)::under pixel_quest, both badges clear their own host's carved border, PER AXIS (T1)",
     "reference/foyer_spec::Foyer badgeBubble clears its own control's themed chrome (task FOYER)::under fantasy_ornate, both badges clear their own host's carved border, PER AXIS (T1)",
     "reference/wardrobe_spec::Wardrobe proof — mount, stage seam, arrangement (RA-P5)::mounts the boutique with categories, grids, pane, and no core error",
     "reference/wardrobe_spec::Wardrobe proof — mount, stage seam, arrangement (RA-P5)::the stage host is live headlessly (recording stub): lighting + camera recorded, fallback closed",
     "reference/wardrobe_spec::Wardrobe proof — mount, stage seam, arrangement (RA-P5)::an orbit step drives a new camera write through the public host",
     "reference/wardrobe_spec::Wardrobe proof — mount, stage seam, arrangement (RA-P5)::REDUCED MOTION STOPS THE TURNTABLE — and motion allowed still turns it",
     "reference/wardrobe_spec::Wardrobe proof — mount, stage seam, arrangement (RA-P5)::wide solves the split arrangement; a phone box solves stacked — and worn state survives the flip",
     "reference/wardrobe_spec::Wardrobe proof — try-on and history::activating a card equips it: Wearing chip, selected state, rig re-dressed; activating again unequips",
     "reference/wardrobe_spec::Wardrobe proof — try-on and history::undo/redo walk the equip history and disable at the stack ends",
     "reference/wardrobe_spec::Wardrobe proof — try-on and history::trying on an unowned item raises the BuyBar; an owned one does not",
     "reference/wardrobe_spec::Wardrobe proof — the purchase lifecycle::insufficient Sparks rejects with a visible reason; the wallet never moves; retry stays allowed",
     "reference/wardrobe_spec::Wardrobe proof — the purchase lifecycle::sold-out rejects once, then a retry confirms: balance debits, chip flips to Owned+Wearing, modal dismisses, BuyBar leaves",
     "reference/wardrobe_spec::Wardrobe proof — filters, sections, locale, determinism::owned-only shrinks the catalog; price sort reorders; reset restores",
     "reference/wardrobe_spec::Wardrobe proof — filters, sections, locale, determinism::section stubs are honest and the worn set survives leaving and returning",
     "reference/wardrobe_spec::Wardrobe proof — filters, sections, locale, determinism::the locale swap reaches the BuyBar copy live",
     "reference/wardrobe_spec::Wardrobe proof — filters, sections, locale, determinism::the locale swap reaches the SECTION PICKER's option labels too",
     "reference/wardrobe_spec::Wardrobe proof — filters, sections, locale, determinism::two fresh builds are identical (reset determinism) and dispose cleanly",
     "reference/wardrobe_spec::Wardrobe — the worn chips FLOW (parity round 3)::FIVE WORN PIECES SHARE ONE LINE, each at its own width — not five stacked lines",
     "reference/wardrobe_spec::Wardrobe — the worn chips FLOW (parity round 3)::IN A NARROW PANE THEY WRAP to a second line rather than paint past the plate",
     "reference/wardrobe_spec::Wardrobe — Picked-for-you cards fill their lane and never overflow their card (item 15)::the catalog column claims the whole phone viewport — not just the grid's minColumnWidth floor",
     "reference/wardrobe_spec::Wardrobe — Picked-for-you cards fill their lane and never overflow their card (item 15)::every Picked-for-you card is the same width, and the row tiles the grid exactly",
     "reference/wardrobe_spec::Wardrobe — Picked-for-you cards fill their lane and never overflow their card (item 15)::every Picked-for-you thumbnail is the SAME width — never a different width per card",
     "reference/wardrobe_spec::Wardrobe — Picked-for-you cards fill their lane and never overflow their card (item 15)::the thumbnail never paints past its own Col — at the floor lane, not just a wide one",
     "reference/wardrobe_spec::Wardrobe — backdrop-finding sweep (task POP)::the purchase Confirm modal and the Refine filter modal are clean",
     "tier::test tiers: the fast tier is the suite list minus named exclusions::the full order is parsed from tests/run.luau and every entry is a real file",
     "tier::test tiers: the fast tier is the suite list minus named exclusions::EVERY exclusion names a spec the suite actually runs (a rename reddens here)",
     "tier::test tiers: the fast tier is the suite list minus named exclusions::every exclusion carries a measured cost and a reason, not just a name",
     "tier::test tiers: the fast tier is the suite list minus named exclusions::the fast tier is a strict subset of the suite, in the same relative order",
     "tier::test tiers: the fast tier is the suite list minus named exclusions::smoke, the core spine and every parity-round-2 area are IN the fast tier",
     "tier::test tiers: the fast tier is the suite list minus named exclusions::the fast runner announces itself with the marker tools/test.sh refuses",
     "tier::test tiers: the fast tier is the suite list minus named exclusions::the fast runner does not hand-list specs: it derives them from run.luau",
     "selection_indicator::D4 geometry: one arithmetic, two skins, both axes::pill: the chip is the segment box inset on all four sides, in the layer's space",
     "selection_indicator::D4 geometry: one arithmetic, two skins, both axes::underline x: a bar on the segment's bottom edge, spanning its full width",
     "selection_indicator::D4 geometry: one arithmetic, two skins, both axes::underline y: a VERTICAL strip underlines the leading edge and spans the height",
     "selection_indicator::D4 geometry: one arithmetic, two skins, both axes::the transition is between RECTS, not indices: unequal segments give unequal bars",
     "selection_indicator::D4 geometry: one arithmetic, two skins, both axes::an underline is never thicker than the box it decorates",
     "selection_indicator::D4 geometry: one arithmetic, two skins, both axes::a pill never inverts: an inset wider than the box clamps to zero",
     "selection_indicator::D4 geometry: one arithmetic, two skins, both axes::nothing to point at resolves to nil rather than to the origin",
     "selection_indicator::D4 motion policy: place or slide, decided before anything moves::first paint PLACES: the indicator never slides in from nowhere",
     "selection_indicator::D4 motion policy: place or slide, decided before anything moves::a selection change SLIDES",
     "selection_indicator::D4 motion policy: place or slide, decided before anything moves::a re-solve under an unchanged selection PLACES, so a rotation cannot make the bar swim",
     "selection_indicator::D4 mounted: the bar tracks the SOLVED rect of the selected child::mounts one bar behind the strip and lands on the initial selection with no travel",
     "selection_indicator::D4 mounted: the bar tracks the SOLVED rect of the selected child::a selection change slides the bar between the two solved rects",
     "selection_indicator::D4 mounted: the bar tracks the SOLVED rect of the selected child::REDUCED MOTION SNAPS: one rect write, then twenty ticks of silence",
     "selection_indicator::D4 mounted: the bar tracks the SOLVED rect of the selected child::full motion spends MANY frames on the same change, which is what reduced motion removes",
     "selection_indicator::D4 mounted: the bar tracks the SOLVED rect of the selected child::the indicator is never a focus stop: wrapping a strip leaves its document order identical",
     "selection_indicator::D4 mounted: the bar tracks the SOLVED rect of the selected child::a viewport change under an UNCHANGED selection re-places the bar rather than flying it",
     "selection_indicator::D4 mounted: the bar tracks the SOLVED rect of the selected child::repeated geometry feeds with UNCHANGED rects cost nothing (the by-value guard)",
     "selection_indicator::D4 mounted: the bar tracks the SOLVED rect of the selected child::...and the comparator refuses a nested field rather than half-comparing it",
     "selection_indicator::D4 mounted: the bar tracks the SOLVED rect of the selected child::a geometry change that does not move the bar cannot interrupt a flight in progress",
     "selection_indicator::D4 mounted: the bar tracks the SOLVED rect of the selected child::a vertical pill moves along y, and the same mechanism draws it",
     "selection_indicator::D4 mounted: the bar tracks the SOLVED rect of the selected child::a selection change is a re-solve, not a rebuild: zero factory reruns",
     "selection_indicator::D4 mounted: the bar tracks the SOLVED rect of the selected child::a wider label widens the bar with it (localization-safe by construction)",
     "selection_indicator::D4 mounted: the bar tracks the SOLVED rect of the selected child::state() is deterministic and reports what a bug report needs",
     "selection_indicator::D4 mounted: the bar tracks the SOLVED rect of the selected child::build/mount/select/dispose returns every registry to baseline",
     "selection_indicator::D4 authoring: the spec is closed and the ids are checked::an unknown spec key is an authoring error, never a silent no-op",
     "selection_indicator::D4 authoring: the spec is closed and the ids are checked::a segment id that names no child of `content` is caught at build",
     "selection_indicator::D4 authoring: the spec is closed and the ids are checked::a strip built from a When/ForEach is trusted instead, and reports what never resolved",
     "selection_indicator::D4 authoring: the spec is closed and the ids are checked::an unknown skin or axis names the legal set",
     "selection_indicator::D4 authoring: the spec is closed and the ids are checked::with NO presenter (a bare mount) it still places the bar, instantly",
     "selection_indicator::D4 through the public property: newPicker's `indicator` and `axis`::indicator = \"none\" is byte-identical to the pre-D4 control",
     "selection_indicator::D4 through the public property: newPicker's `indicator` and `axis`::indicator = \"pill\" slides a chip between two unequal segments",
     "selection_indicator::D4 through the public property: newPicker's `indicator` and `axis`::turning it on makes the INDICATOR the selection paint, not a second one",
     "selection_indicator::D4 through the public property: newPicker's `indicator` and `axis`::axis = \"y\" is a real VERTICAL PILL, and it does not overload `inline`",
     "selection_indicator::D4 through the public property: newPicker's `indicator` and `axis`::the picker's own dump carries the indicator's state — there is no second schema",
     "selection_indicator::D4 through the public property: newPicker's `indicator` and `axis`::an unknown indicator or axis names the legal set",
     "selection_indicator::D4 through the public property: newPicker's `indicator` and `axis`::wrapping does not collapse a `fill` strip — the options keep their width",
     "selection_indicator::D4 through the public property: newPicker's `indicator` and `axis`::selection still works at the deeper path: a tap and a Return both choose",
     "selection_indicator::D4 through the public property: newPicker's `indicator` and `axis`::an indicated picker is still exactly two focus stops, in order",
     "selection_indicator::D4 through the public property: newPicker's `indicator` and `axis`::REDUCED MOTION snaps the picker's chip too: one write, then silence",
     "selection_indicator::D4 through the public property: newPicker's `indicator` and `axis`::an indicated picker is registry-neutral: build, mount, switch, dispose",
     "selection_indicator::D5 found: the paths are RE-RESOLVED when the strip is re-homed::re-homing the strip re-resolves the layer and every segment, and the bar follows",
     "selection_indicator::D5 found: the paths are RE-RESOLVED when the strip is re-homed::...and a selection change in the NEW home still moves the bar",
     "picker_segments::D6: the segmented presentation ADOPTS the sliding indicator::a segmented picker defaults to the pill — the chip is the shipped paint now",
     "picker_segments::D6: the segmented presentation ADOPTS the sliding indicator::...and so does an ADAPTIVE picker that resolves segmented",
     "picker_segments::D6: the segmented presentation ADOPTS the sliding indicator::the default drops the `selected` style tag, because a tag AND a chip say it twice",
     "picker_segments::D6: the segmented presentation ADOPTS the sliding indicator::`indicator = \"none\"` is the documented escape hatch back to the static tag",
     "picker_segments::D6: the segmented presentation ADOPTS the sliding indicator::a DECLARED inline picker keeps its current painting",
     "picker_segments::D6: the segmented presentation ADOPTS the sliding indicator::an explicit indicator still applies to `inline` — the DEFAULT changed, not the property",
     "picker_segments::D6: the segmented presentation ADOPTS the sliding indicator::the chip still SLIDES from the old rect to the new one under the default",
     "picker_segments::D6: the segmented presentation ADOPTS the sliding indicator::`automatic` is a legal spelling and the refusal message names the whole set",
     "picker_segments::D6: `icon` on Option — icon-only and icon+label::an icon rides `compactLabel`, so the glyph is the DEGRADE and the word survives",
     "picker_segments::D6: `icon` on Option — icon-only and icon+label::...and the ladder really RUNS: too narrow for the word, the segment wears the glyph",
     "picker_segments::D6: `icon` on Option — icon-only and icon+label::`iconOnly` INVERTS it: the glyph is what every segment wears at every width",
     "picker_segments::D6: `icon` on Option — icon-only and icon+label::an icon-only segment STILL CARRIES A NAME, in the dump and in the option",
     "picker_segments::D6: `icon` on Option — icon-only and icon+label::a nameless option is an AUTHORING ERROR — an icon with no name announces nothing",
     "picker_segments::D6: `icon` on Option — icon-only and icon+label::Guidance: icons for ALL options in a group, or none — a half-iconned group is refused",
     "picker_segments::D6: `icon` on Option — icon-only and icon+label::`iconOnly` with nothing to draw is refused rather than silently ignored",
     "picker_segments::D6: `icon` on Option — icon-only and icon+label::an icon+label segment survives 1.4x pseudo-localization without clipping",
     "picker_segments::D6: the VERTICAL pill is a rail, not the inline row list::`axis = \"y\"` on a segmented picker is a CONTENT-SIZED rail",
     "picker_segments::D6: the VERTICAL pill is a rail, not the inline row list::every segment of the rail still meets the 44px floor on BOTH axes",
     "picker_segments::D6: the VERTICAL pill is a rail, not the inline row list::the rail stacks along y and its segments share one width",
     "picker_segments::D6: the VERTICAL pill is a rail, not the inline row list::`dump().verticalPill` is what tells the pill and the row list apart",
     "picker_segments::D6: the VERTICAL pill is a rail, not the inline row list::the chip travels down the rail, not across it",
     "picker_segments::D6: the VERTICAL pill is a rail, not the inline row list::wrapping does not collapse the rail: the indicated rail matches the plain one",
     "picker_segments::D6: the VERTICAL pill is a rail, not the inline row list::flipping OUT of segmented gives the offer back: the rail becomes a column",
     "picker_segments::D6: the VERTICAL pill is a rail, not the inline row list::a rail's options are still reachable by pointer, keyboard and gamepad",
     "picker_segments::D6: the dump keeps schema /1 and stays deterministic::new fields, same schema string — every pre-D6 field keeps its name and meaning",
     "picker_segments::D6: the dump keeps schema /1 and stays deterministic::dispose is registry-neutral with icons, a rail and an indicator all on",
     "picker_segments::D5 seam: `sizing` is what makes a strip able to OVERFLOW::`sizing = \"hug\"` gives the band its natural width and its segments theirs",
     "picker_segments::D5 seam: `sizing` is what makes a strip able to OVERFLOW::...and the segments stop being equal-width, so a long label really is longer",
     "picker_segments::D5 seam: `sizing` is what makes a strip able to OVERFLOW::`sizing` is REACTIVE, because a tab bar's home changes under a live rotation",
     "picker_segments::D5 seam: `sizing` is what makes a strip able to OVERFLOW::an unknown `sizing` is an authoring error, not a silent fill",
     "picker_segments::D5 seam: `axis` is REACTIVE, because a placement flip is a re-solve::a live axis flip turns the band into a rail without rebuilding an option",
     "picker_segments::D5 seam: a badge is a count that lives on an UNSELECTED option::a badged option wears a `surface = \"badge\"` plate; an unbadged one wears nothing",
     "picker_segments::D5 seam: a badge is a count that lives on an UNSELECTED option::the badge is on the UNSELECTED option and stays live — that is the point of a badge",
     "picker_segments::D5 seam: a badge is a count that lives on an UNSELECTED option::the seal is a DECORATION: no focus stop, no second activation site",
     "picker_segments::D5 seam: a badge is a count that lives on an UNSELECTED option::a badge VALUE is reactive, and an empty one hides the plate rather than painting a bubble",
     "picker_segments::D5 seam: a badge is a count that lives on an UNSELECTED option::the plate does NOT eat the tap: a badged segment still selects",
     "picker_segments::D5 seam: a badge is a count that lives on an UNSELECTED option::a picker with NO badges mounts no badge node at all — the paths are unchanged",
     "picker_segments::D5 seam: an option LABEL may be a Readable, and `api.segments` names the row::a bound label satisfies the non-empty lint and is what dump() announces",
     "picker_segments::D5 seam: an option LABEL may be a Readable, and `api.segments` names the row::a bound label that is EMPTY right now is still the authoring error it would be as a string",
     "picker_segments::D5 seam: an option LABEL may be a Readable, and `api.segments` names the row::`api.segments` publishes the id this strip gave each option",
     "picker_segments::D5 found: a segmented strip navigates ACROSS on a pad, not down::the option stack is one HORIZONTAL group, so Right moves to the next segment",
     "picker_segments::D5 found: a segmented strip navigates ACROSS on a pad, not down::...and the VERTICAL rail stays a vertical group, because its axis says so",
     "picker_segments::D5 found: a segmented strip navigates ACROSS on a pad, not down::a LIVE axis flip re-derives the group with it",
     "picker_segments::§6 seam: a segment carries the strip's declared type role::`textSize` reaches every segment, and declaring none leaves the default",
     "picker_segments::§6 seam: a segment carries the strip's declared type role::...and it is REACTIVE, because the home a strip sits in moves under a rotation",
     "picker_segments::§6 seam: a selection and its onChange are ONE transaction::a redirect inside onChange never reaches an observer at all",
     "picker_segments::§6 seam: a selection and its onChange are ONE transaction::the register records it: the changelog carries the one-turn row",
     "picker_segments::§6 seam: a selection and its onChange are ONE transaction::...and a pick that STANDS still notifies exactly once",
     "menu::D2 Menu: the spec is closed, and an ambiguous item is a refusal::an unknown spec key is an authoring error, named with the whole legal set",
     "menu::D2 Menu: the spec is closed, and an ambiguous item is a refusal::declaring BOTH children and onSelect is a spec error, never a precedence rule",
     "menu::D2 Menu: the spec is closed, and an ambiguous item is a refusal::an item with neither an action nor children is refused rather than silently inert",
     "menu::D2 Menu: the spec is closed, and an ambiguous item is a refusal::Guidance: icons for ALL items in a group, or none — a half-iconned group is refused",
     "menu::D2 Menu: the spec is closed, and an ambiguous item is a refusal::...and a DIVIDER starts a new group, so the two sides may differ",
     "menu::D2 Menu: the spec is closed, and an ambiguous item is a refusal::a menu that drops `activate` without both key triggers is unreachable, and refused",
     "menu::D2 Menu: submenus nest without a cap, and say so past one level::a three-level tree BUILDS, and depth >= 2 is diagnosed rather than refused",
     "menu::D2 Menu: submenus nest without a cap, and say so past one level::one level of submenu is silent — the diagnostic is advice, not noise",
     "menu::D2 Menu: submenus nest without a cap, and say so past one level::Guidance: a group of more than about five items is diagnosed as wanting its own menu",
     "menu::D2 Menu: submenus nest without a cap, and say so past one level::Guidance: a destructive item that is not last is diagnosed",
     "menu::D2 Menu: every input class opens it, and the verbs are the same ones::a primary activate on the trigger opens the menu against the trigger's own rect",
     "menu::D2 Menu: every input class opens it, and the verbs are the same ones::a pointer SECONDARY click on the trigger opens the menu",
     "menu::D2 Menu: every input class opens it, and the verbs are the same ones::a touch LONG-PRESS on the trigger opens the menu, through the normalized gesture layer",
     "menu::D2 Menu: every input class opens it, and the verbs are the same ones::a tap gesture is NOT a long press: the menu stays shut",
     "menu::D2 Menu: every input class opens it, and the verbs are the same ones::the keyboard context chord opens the menu, and Return activates the focused item",
     "menu::D2 Menu: every input class opens it, and the verbs are the same ones::the gamepad menu button opens it, and ButtonB closes ONE level",
     "menu::D2 Menu: every input class opens it, and the verbs are the same ones::activating an item runs its onSelect exactly once and closes the menu",
     "menu::D2 Menu: every input class opens it, and the verbs are the same ones::every trigger declared is honoured, and one that was not is inert",
     "menu::D2 Menu: a menu whose every item is disabled still OPENS::a wholly unavailable menu opens rather than swallowing its trigger",
     "menu::D2 Menu: a menu whose every item is disabled still OPENS::selecting a disabled item does nothing and leaves the menu standing",
     "menu::D2 Menu: Cancel closes one level, tap-away closes all::tap-away closes EVERY level, not just the innermost",
     "menu::D2 Menu: Cancel closes one level, tap-away closes all::Cancel unwinds the chain one level at a time, and only then closes",
     "menu::D2 Menu: Cancel closes one level, tap-away closes all::Right enters the focused submenu and Left leaves it, in document order",
     "menu::D2 Menu: Cancel closes one level, tap-away closes all::Right on a plain ACTION row is not the menu's arrow, so the ring keeps it",
     "menu::D2 Menu: touch does not nest — the sheet replaces its contents::touch resolves to `sheet` through the ONE shared rule, never a device name",
     "menu::D2 Menu: touch does not nest — the sheet replaces its contents::MEDIUM-2: a class table with no `primary` keeps its pre-ADAPT-9 answer",
     "menu::D2 Menu: touch does not nest — the sheet replaces its contents::ADAPT-9: a hybrid follows its PRIMARY class, not merely what it has",
     "menu::D2 Menu: touch does not nest — the sheet replaces its contents::under `sheet` a submenu REPLACES the sheet's contents and grows a Back row",
     "menu::D2 Menu: touch does not nest — the sheet replaces its contents::...and the Back row walks the chain back up rather than closing the menu",
     "menu::D2 Menu: touch does not nest — the sheet replaces its contents::under `menu` a submenu STACKS beside its parent, anchored to the row it came from",
     "menu::D2 Menu: touch does not nest — the sheet replaces its contents::a live class flip while a submenu is open CARRIES the open path into the new presentation",
     "menu::D2 Menu: item 14 — COMPACT forces the single-panel idiom, not touch::a compact width forces `sheet` even with a POINTER-primary class (the exact screenshot repro)",
     "menu::D2 Menu: item 14 — COMPACT forces the single-panel idiom, not touch::...and the SAME pointer-primary class resolves `menu` once the width crosses into `regular`",
     "menu::D2 Menu: item 14 — COMPACT forces the single-panel idiom, not touch::this only WIDENS which environments choose `sheet` — touch still forces it at any width (ADAPT-9 unaffected)",
     "menu::D2 Menu: item 14 — COMPACT forces the single-panel idiom, not touch::an author-forced `presentation` is untouched — forcing stays the escape hatch even at a compact width",
     "menu::D2 Menu: item 14 — COMPACT forces the single-panel idiom, not touch::AUTOMATICALLY, with no forced idiom: entering a submenu on a compact pointer-primary surface REPLACES the panel instead of stacking a second one over it",
     "menu::D2 Menu: item 14 — COMPACT forces the single-panel idiom, not touch::touch (the Back row) closes one level on the AUTOMATIC compact idiom, exactly as the forced `sheet` case does",
     "menu::D2 Menu: item 14 — COMPACT forces the single-panel idiom, not touch::keyboard (Left) and gamepad (ButtonB) each close one level on the AUTOMATIC compact idiom",
     "menu::D2 Menu: item 14 — COMPACT forces the single-panel idiom, not touch::dismissal semantics are UNCHANGED at the root: an outside tap still closes every level on the automatic compact idiom",
     "menu::D2 Menu: item 14 — COMPACT forces the single-panel idiom, not touch::BACK restores focus to the row that led into the level just left, not the level's first row (state preserved)",
     "menu::D2 Menu: item 14 — COMPACT forces the single-panel idiom, not touch::repeated enter/back leaves no orphaned nodes once closed (no leaked scope)",
     "menu::D2 Menu: item 14 — COMPACT forces the single-panel idiom, not touch::`backLabel` reaches the Back row's rendered label",
     "menu::D2 Menu: item 14 — COMPACT forces the single-panel idiom, not touch::the literal 'Back' stands as the fallback when `backLabel` is absent (no regression)",
     "menu::D2 Menu: item 14 — COMPACT forces the single-panel idiom, not touch::a Back label past ~1.4x an English source degrades to the icon alone rather than clipping",
     "menu::D2: newMenu.presentation is a Readable<string> (gap 20)::a plain string still fails fast, at CONSTRUCTION — no regression",
     "menu::D2: newMenu.presentation is a Readable<string> (gap 20)::a Readable forcing 'sheet' opens sheeted even on a desktop-shaped surface",
     "menu::D2: newMenu.presentation is a Readable<string> (gap 20)::flipping the Readable while OPEN re-presents through the same hot-switch the automatic case uses",
     "menu::D2: newMenu.presentation is a Readable<string> (gap 20)::a Readable can flip INTO 'automatic', resolving the size/interaction facts as if authored from the start",
     "menu::D2: newMenu.presentation is a Readable<string> (gap 20)::a reactive value out of domain is refused loudly on core:lastError(), and the menu HOLDS its last valid state",
     "menu::D2: one menu row recipe, and one presentation rule::`resolvePresentation` lives in ONE place, and popup_button's name is an alias for it",
     "menu::D2: one menu row recipe, and one presentation rule::all three menus take their row height and gap from the same tokens",
     "menu::D2: one menu row recipe, and one presentation rule::a leaf row is the SHIPPED shape: the label on the button, the icon on compactLabel",
     "menu::D2: one menu row recipe, and one presentation rule::a SUBMENU row carries a trailing chevron, and keeps its label semantic",
     "menu::D2: one menu row recipe, and one presentation rule::`icon` and a divider joined popup_button.Option, and both render from the recipe",
     "menu::D2: one menu row recipe, and one presentation rule::popup_button refuses a half-iconned group through the SAME lint the Menu uses",
     "menu::D2: long-press ownership, settled in code::the disclosure plate KEEPS long-press outside a menu trigger — the a11y route survives",
     "menu::D2: long-press ownership, settled in code::...and inside a Menu trigger the MENU owns it: one gesture, one construct",
     "menu::D2: long-press ownership, settled in code::exactly two constructs know the word long-press, and `help` is not going to be a third",
     "menu::D2: long-press ownership, settled in code::a Menu declared WITHOUT a touch trigger takes nothing away from the plate",
     "menu::D2 Menu: the dump says what is on screen::the dump carries the schema, the open chain and the whole item tree",
     "menu::D2 Menu: the dump says what is on screen::dispose closes every live surface rather than stranding one",
     "menu_scenario::scenario 'menu': the surface D2 shipped without::is registered in BOTH places a player reaches it through, and names a root screen",
     "menu_scenario::scenario 'menu': the surface D2 shipped without::the five routes are partitioned across five cards, with exactly ONE owner of the chords",
     "menu_scenario::scenario 'menu': the surface D2 shipped without::every card's trigger is a focusable Button at the 44px floor, in document order",
     "menu_scenario::scenario 'menu': the surface D2 shipped without::every card's trigger fills the card's row — the press is the ROW, not the word",
     "menu_scenario::scenario 'menu': each route opens ITS OWN card, through the real seam::primary activate — a tap, and Return on the focused trigger",
     "menu_scenario::scenario 'menu': each route opens ITS OWN card, through the real seam::pointer secondary — a right-click, through the adapter's own MouseButton2 seam",
     "menu_scenario::scenario 'menu': each route opens ITS OWN card, through the real seam::touch long-press — the NORMALIZED gesture, at `began`, not at the release",
     "menu_scenario::scenario 'menu': each route opens ITS OWN card, through the real seam::the two chords — and the card a TAP deliberately does not open",
     "menu_scenario::scenario 'menu': the list, and what the framework says about it::icons per group, a divider, and the destructive verb last",
     "menu_scenario::scenario 'menu': the list, and what the framework says about it::a submenu two levels deep, and the standing advice PRINTED rather than swallowed",
     "menu_scenario::scenario 'menu': the list, and what the framework says about it::a menu whose every item is disabled OPENS, paints them disabled, and runs nothing",
     "menu_scenario::scenario 'menu': both idioms, from one control, on one machine::panel stacks a surface per level; sheet REPLACES and grows a Back row",
     "menu_scenario::scenario 'menu': both idioms, from one control, on one machine::a live idiom flip REBUILDS an open chain in place, at the depth the player reached",
     "menu_scenario::scenario 'menu': both idioms, from one control, on one machine::`automatic` is the environment's own answer, and it is not a device name",
     "menu_scenario::scenario 'menu': both idioms, from one control, on one machine::on a COMPACT viewport, forcing `panel` still resolves `sheet` — the screenshot's own defect, closed",
     "menu_scenario::scenario 'menu': it holds together where a phone squeezes it::the solver reports NOTHING at any swept viewport, closed or open",
     "menu_scenario::scenario 'menu': it holds together where a phone squeezes it::every player-visible string is in the exported `copy`, so the localization sweep can reach it",
     "tab_view_scenario::scenario 'tab_view': the surface D5 shipped without::is registered in BOTH places a player reaches it through, and names a root screen",
     "tab_view_scenario::scenario 'tab_view': the surface D5 shipped without::mounts BOTH skins of the sliding indicator — underline above, pill inside",
     "tab_view_scenario::scenario 'tab_view': two levels, and each one SAYS where it landed::on a phone the app takes the thumb zone and the page takes the top — both printed",
     "tab_view_scenario::scenario 'tab_view': two levels, and each one SAYS where it landed::on a pointer desktop the app becomes a rail and the page is STILL a top band",
     "tab_view_scenario::scenario 'tab_view': two levels, and each one SAYS where it landed::no focus scope is claimed by either level, and the back() stack is untouched",
     "tab_view_scenario::scenario 'tab_view': the eviction, as a number a tester can watch move::leaving and returning to a tab REBUILDS it, and the screen says so",
     "tab_view_scenario::scenario 'tab_view': the eviction, as a number a tester can watch move::state owned by the screen survives a switch; state owned by the tab does not",
     "tab_view_scenario::scenario 'tab_view': the eviction, as a number a tester can watch move::a badge climbs on an UNSELECTED tab while that tab's content is gone",
     "tab_view_scenario::scenario 'tab_view': the page bar overflows, on purpose::a hugging strip measures wider than its band at a phone width",
     "tab_view_scenario::scenario 'tab_view': the page bar overflows, on purpose::a selection change that moved no focus still brings the tab into view, once",
     "tab_view_scenario::scenario 'tab_view': everything on it is reachable, everywhere::every tab at BOTH levels is a Button at the 44px floor, read where it is painted",
     "tab_view_scenario::scenario 'tab_view': everything on it is reachable, everywhere::a keyboard and a pad both move the page bar, and the shoulders page it",
     "tab_view_scenario::scenario 'tab_view': everything on it is reachable, everywhere::the solver reports NOTHING at any swept viewport, on any tab",
     "tab_view_scenario::scenario 'tab_view': everything on it is reachable, everywhere::every player-visible string is reachable through the exported `copy`",
     "help::D3a help: the pointer pulls it, after a dwell::pointer hover presents the help plate only after the dwell elapses",
     "help::D3a help: the pointer pulls it, after a dwell::the pointer leaving takes it away",
     "help::D3a help: the pointer pulls it, after a dwell::activating the view takes it away — a player who pressed has stopped asking",
     "help::D3a help: the pointer pulls it, after a dwell::moving to a different helped view retires the first answer and re-earns the dwell",
     "help::D3a help: keyboard and gamepad get it on focus, immediately::focus entering a helped view presents the plate with no dwell",
     "help::D3a help: keyboard and gamepad get it on focus, immediately::focus moving off takes it away",
     "help::D3a help: NOTHING on touch, and that is the specification::a pure-touch world wires NO engagement zone for help at all",
     "help::D3a help: NOTHING on touch, and that is the specification::HELP BINDS NO GESTURE: with both classes live it wires hover and never a press",
     "help::D3a help: NOTHING on touch, and that is the specification::a touch tap moves focus but presents nothing — focusVisible is the gate",
     "help::D3a help: the plate is chrome, and the value outranks it::a truncated DISCLOSED label outranks help on the same engagement",
     "help::D3a help: the plate is chrome, and the value outranks it::the plate takes no focus and adds no focus stop",
     "help::D3a help: the plate is chrome, and the value outranks it::dismissing the surface takes its help with it",
     "help::D3a help: the plate is chrome, and the value outranks it::a help string wraps rather than clips at 1.4x expansion",
     "help::D3a help: the plate is chrome, and the value outranks it::the dump carries the schema, the source and every live declaration",
     "help::D3a help: the plate is chrome, and the value outranks it::help is a CONSTRUCTION-ONLY string: a Readable is refused by name",
     "help::D3a help: the reachability check, and why it is not clippedEssential::a help string that appears nowhere else is reported: a touch player cannot read it",
     "help::D3a help: the reachability check, and why it is not clippedEssential::...and one the screen also says out loud is not",
     "help::D3a help: the reachability check, and why it is not clippedEssential::HIDDEN text is not a route: a losing ViewThatFits candidate says nothing",
     "help::D3a help: the reachability check, and why it is not clippedEssential::...and neither is TRUNCATED text — an ellipsis is not the sentence",
     "help::D3a help: the reachability check, and why it is not clippedEssential::a `convenience` waiver silences the duplication finding",
     "help::D3a help: the reachability check, and why it is not clippedEssential::BUT NO WAIVER REACHES THE GESTURE: help nothing engages is reported anyway",
     "help::D3a help: the reachability check, and why it is not clippedEssential::a pointer-only world still has a route; a pure-touch one has none",
     "help::D3a help: the reachability check, and why it is not clippedEssential::the presenter reports the LIVE declarations, so the check runs on a real mount",
     "help::D3a help: the reachability check, and why it is not clippedEssential::...and in a pure-touch world the SAME mount reports no route, which is the finding",
     "help::D3a help: the reachability check, and why it is not clippedEssential::the help plate carries D1's arrow tail, pointing at its source",
     "help::D3a help: the reachability check, and why it is not clippedEssential::...and the tail is suppressed when the plate cannot stay over its source",
     "help::D3a help: the reachability check, and why it is not clippedEssential::the help tail wears no chrome slot either",
     "help::D3a help: the reachability check, and why it is not clippedEssential::NO NEW DEVICE BRANCH: the new modules name no platform",
     "callout::D3b Callout: eligibility is the app's rule, and the caller's state::a callout nobody has seen is eligible, presents, and reports itself showing",
     "callout::D3b Callout: eligibility is the app's rule, and the caller's state::`seen` is the caller's record, and a seen callout is never eligible",
     "callout::D3b Callout: eligibility is the app's rule, and the caller's state::`afterSessions` holds it back until the session count reaches it",
     "callout::D3b Callout: eligibility is the app's rule, and the caller's state::a feature already used is never coached about",
     "callout::D3b Callout: invalidation, and the persistence boundary::using the feature while it is showing takes it down and retires it FOREVER",
     "callout::D3b Callout: invalidation, and the persistence boundary::a player dismissal retires it once, with a reason, and never twice",
     "callout::D3b Callout: invalidation, and the persistence boundary::THE FRAMEWORK WRITES NO PLAYER STATE: `seen` is untouched, the report is the whole contract",
     "callout::D3b Callout: invalidation, and the persistence boundary::a callout with no way to report a retirement is an authoring error",
     "callout::D3b Callout: invalidation, and the persistence boundary::the spec is CLOSED: an invented key is refused rather than ignored",
     "callout::D3b Callout: at most one on screen — a queue, not a pile::a second callout WAITS rather than stacking beside the first",
     "callout::D3b Callout: at most one on screen — a queue, not a pile::...and takes its turn the moment the first retires",
     "callout::D3b Callout: at most one on screen — a queue, not a pile::priority orders the queue without ever showing two",
     "callout::D3b Callout: it never blocks::presenting one does not move the focus ring",
     "callout::D3b Callout: it never blocks::THE CONTROL IT POINTS AT STAYS OPERABLE: a tap on it still activates it",
     "callout::D3b Callout: it never blocks::its tap-away catcher sits BELOW the screen it points at, not over it",
     "callout::D3b Callout: it never blocks::a tap in empty space retires it through the presenter's own non-consuming catcher",
     "callout::D3b Callout: it never blocks::it is NOT a modal: the screen underneath keeps its own focus scope",
     "callout::D3b Callout: every input class can retire it::pointer: a click on the dismiss affordance retires it",
     "callout::D3b Callout: every input class can retire it::touch: a tap on the dismiss affordance retires it, and it meets the 44px floor",
     "callout::D3b Callout: every input class can retire it::keyboard: Return on the focused dismiss affordance retires it",
     "callout::D3b Callout: every input class can retire it::gamepad: ButtonA on the focused dismiss affordance retires it",
     "callout::D3b Callout: every input class can retire it::...and one arrow press reaches it, so a pad player is never stuck with a plate",
     "callout::D3b Callout: the f1 fixture::f1: a plate under a top-right +, arrow up at it, body shifted left",
     "callout::D3b Callout: the f1 fixture::the content is a BLUEPRINT, not a string: the plate carries the tree it was given",
     "callout::D3b Callout: the f1 fixture::player copy survives ~1.4x pseudo-localization by wrapping, never clipping",
     "callout::D3b Callout: the dump, and the teardown::the dump carries the schema, the state and the eligibility it resolved",
     "callout::D3b Callout: the dump, and the teardown::the queue's own dump names what is on screen and what is waiting",
     "callout::D3b Callout: the dump, and the teardown::dispose takes the plate down and gives the queue slot back",
     "callout::D3b Callout: the dump, and the teardown::dismissing the surface underneath takes the callout with it",
     "callout::D3b Callout: the dump, and the teardown::NO DEVICE BRANCH: the construct names no platform",
     "callout::D3b Callout: the dump, and the teardown::the standing warning ships with the documentation",
     "callout_scenario::scenario 'callout': a device pass a tester can run more than once::show -> retire -> RESET -> show again, without remounting the place",
     "callout_scenario::scenario 'callout': a device pass a tester can run more than once::the anchors survive a reset with their IDENTITY, not just their number",
     "callout_scenario::scenario 'callout': a device pass a tester can run more than once::nothing is left sitting on the anchors afterwards, and the tip shows again",
     "callout_scenario::scenario 'callout': a device pass a tester can run more than once::the reset is on screen, at the 44px floor, beside the other picks",
     "callout_scenario::scenario 'callout': the plate clears the glass, and the page stays in its gutter::the raised coach mark clears BOTH lateral edges by the theme's own step",
     "callout_scenario::scenario 'callout': the plate clears the glass, and the page stays in its gutter::a real safe-area inset bigger than the step still wins",
     "callout_scenario::scenario 'callout': the plate clears the glass, and the page stays in its gutter::nothing the page paints crosses a lateral edge, at any swept size",
     "hud_paint_probe::hud: the director's full sequence leaves the painted tree exact::the sink opened and closed, then TWO orientation cycles, paints a fresh mount's tree",
     "hud_paint_probe::hud: the director's full sequence leaves the painted tree exact::the round strip is on screen at the end, because no ladder may ever remove it",
     "hud_paint_probe::hud: the director's full sequence leaves the painted tree exact::the probe stays quiet through the whole sequence and never latches",
     "hud_paint_probe::hud: the paint probe fires when the engine disagrees with the framework::names the unpainted rows, the first painted row after them, and the node they are under",
     "hud_paint_probe::hud: the paint probe fires when the engine disagrees with the framework::a latched probe stops sampling until it is re-armed",
     "hud_paint_probe::hud: the paint probe fires when the engine disagrees with the framework::the ladder legitimately hiding a zone is NOT a loss",
     "hud_paint_probe::hud: the paint probe fires when the engine disagrees with the framework::degrades honestly when the host carries no engine read",
     "hud_paint_probe::hud: the paint probe never reports a verdict it did not measure::refuses to print a paint count when the host supplies no engine seam",
     "hud_paint_probe::hud: the paint probe never reports a verdict it did not measure::an engine seam that answers nothing at all is unavailable, not a healthy screen",
     "hud_paint_probe::hud: the paint probe never reports a verdict it did not measure::names every row it skipped and shows the denominator it actually watched",
     "hud_paint_probe::hud: the paint probe never reports a verdict it did not measure::a row the engine paints at ZERO PIXELS is LOST, not painting",
     "hud_paint_probe::hud: the paint probe never reports a verdict it did not measure::a row is painted only when EVERY path the model wants is painting",
     "hud_paint_probe::hud: the paint probe never reports a verdict it did not measure::a surface whose ROOT is disabled loses every row, with the root named",
     "hud_paint_probe::hud: the paint probe never reports a verdict it did not measure::`13 of 13 painting` cannot be printed while anything is lost or unmeasured",
     "hud_paint_probe::hud: the probe's plate is on a surface the HUD's own walk cannot reach::mounts its own screen, above the HUD's band, and survives the HUD being hidden",
     "hud_paint_probe::hud: the probe's plate is on a surface the HUD's own walk cannot reach::a second base surface's control is in NEITHER traversal ring (the reported gap)",
     "hud_paint_probe::hud: the probe's plate is on a surface the HUD's own walk cannot reach::the probe's surface is retired with the fixture",
     "hud_paint_probe::hud: the director's sequence with the gallery's other surfaces present::no HUD node is left parented under another surface's root",
     "hud_paint_probe::hud: the director's sequence with the gallery's other surfaces present::...and the painted tree still equals a fresh mount's",
     "hud_paint_probe::hud: a row skipped by a PLATFORM fact says so on the plate::no platform band takes Objective out of the denominator — the phone's exact reading",
     "hud_paint_probe::hud: a row skipped by a PLATFORM fact says so on the plate::the band coming back restores the row, which is the director's own recovery",
     "hud_paint_probe::hud: a row skipped by a PLATFORM fact says so on the plate::a healthy band never prints the reason line",
     "hud_paint_probe::hud: a node the engine draws somewhere else is NOT painting::a stale POSITION is a loss, and the plate names the offset",
     "hud_paint_probe::hud: a node the engine draws somewhere else is NOT painting::a node pushed OFF the viewport is a loss on the same rule",
     "hud_paint_probe::hud: a node the engine draws somewhere else is NOT painting::a ONE-PIXEL difference is still painting — the threshold is not zero",
     "hud_paint_probe::hud: a node the engine draws somewhere else is NOT painting::a drift seen ONCE never latches — the regression that blinded a device pass",
     "hud_paint_probe::hud: the line that says why comes before the line that says which::the culprit is above the names, and the names are capped at four",
     "hud_paint_probe::hud: the line that says why comes before the line that says which::four or fewer rows are all named — the cap is not a truncation",
     "hud_paint_probe::hud: the probe plate lays out as lines, not as one opaque block::each plate line is its own node, so a long one cannot hide the next",
     "hud_paint_probe::hud: the probe plate lays out as lines, not as one opaque block::the button is BELOW every line, which is the property that was failing",
     "hud_paint_probe::hud: the probe plate lays out as lines, not as one opaque block::a plate with fewer lines mounts fewer nodes — the slots are not padding",
     "hud_paint_probe::hud: a probe with no healthy baseline has nothing to have lost::never latches on a screen it has not yet seen working",
     "hud_paint_probe::hud: a probe with no healthy baseline has nothing to have lost::...and latches the moment a screen that WAS working stops",
     "hud_paint_probe::hud: the plate names every distinct culprit, by its tail::rows lost under DIFFERENT nodes are not reported as one",
     "hud_paint_probe::hud: the plate names every distinct culprit, by its tail::a path is shown by its TAIL, because every one of them shares a head",
     "hud_paint_probe::hud: a whole-surface coordinate offset is a space, not a defect::EVERY row offset by the same amount is the phone's inset, and accuses nobody",
     "hud_paint_probe::hud: a whole-surface coordinate offset is a space, not a defect::...and ONE row differing from its neighbours still is a defect",
     "hud_paint_probe::hud: a whole-surface coordinate offset is a space, not a defect::a shifted surface is not reported OFF SCREEN either",
     "hud_paint_probe::hud: the paint probe does not cover the HUD it is describing::closed, it covers no HUD node at all — landscape",
     "hud_paint_probe::hud: the paint probe does not cover the HUD it is describing::closed, it covers no HUD node at all — portrait",
     "hud_paint_probe::hud: the paint probe does not cover the HUD it is describing::opened, it shows every line — the cover is a thing the reader asked for",
     "hud_paint_probe::hud: the paint probe does not cover the HUD it is describing::the full readout is on `report()` either way, so a driver never has to open it",
     "hud_paint_probe::hud: faded and clipped are not painting::a node under a fade group at nothing is LOST, not painting",
     "hud_paint_probe::hud: faded and clipped are not painting::a node an ancestor CLIPS away is LOST, and the plate names the clipper",
     "hud_paint_probe::hud: faded and clipped are not painting::a fade group PART WAY is still painting — the threshold is nothing, not something",
     "hud_paint_probe::hud: faded and clipped are not painting::a host that answers neither fact still reports honestly",
     "hud_paint_probe::hud: the solver's own findings reach the plate::a clean layout says nothing, so the line is not decoration",
     "hud_paint_probe::hud: the solver's own findings reach the plate::a layout the framework is complaining about reaches the closed plate",
     "hit_expander_z_band::NM-H4a: the hit-expander z reservation is a fact about the class::a FIRST sync already reserves — the expanders cannot tie with the buttons",
     "hit_expander_z_band::NM-H4a: the hit-expander z reservation is a fact about the class::...and the band does not move when a later solve happens",
     "hit_expander_z_band::NM-H4a: the hit-expander z reservation is a fact about the class::a driven surface's band equals a FRESH one's — the live divergence, headless",
     "hit_expander_z_band::NM-H4a: the hit-expander z reservation is a fact about the class::a node with NO floor reserves nothing — the band is not padded",
     "hit_floor_region_clamp::a cover's touch floor takes nothing pressable (H1)::the fixture is the review's: a standing COVER over the region's whole box",
     "hit_floor_region_clamp::a cover's touch floor takes nothing pressable (H1)::it takes NO part of either neighbouring Button — 960 and 828 px2 become 0",
     "hit_floor_region_clamp::a cover's touch floor takes nothing pressable (H1)::...and in THIS fixture there is no room at all, so it grows nothing",
     "hit_floor_region_clamp::a cover's touch floor takes nothing pressable (H1)::no framework node of the affordance reaches any author node in another region",
     "hit_floor_region_clamp::a cover with ROOM keeps its whole floor — the other arm of R18::the HUD's three stepped-down covers keep 44px, because their floors reach nothing",
     "hit_floor_region_clamp::the F1 accessibility floor is UNTOUCHED by the clamp::a sub-floor Button keeps its WHOLE floor over a pressable node in ANOTHER subtree",
     "hit_floor_region_clamp::the F1 accessibility floor is UNTOUCHED by the clamp::a 20x20 Button still asks for the full 44x44, and it still leaves its parent",
     "hit_floor_region_clamp::a HIDDEN target is not a target::a hidden pressable rect is absent from the blocker set, so the floor grows",
     "hit_floor_region_clamp::a HIDDEN target is not a target::...and the walk really does drop one, on a live tree, both arms measured",
     "hit_floor_region_clamp::the rule itself, as a decision::with nothing in the way it grows the whole floor",
     "hit_floor_region_clamp::the rule itself, as a decision::each side stops at the first blocker, and keeps the room on the other side",
     "hit_floor_region_clamp::the rule itself, as a decision::boxed in on both sides it grows NOTHING and gives back exactly the painted rect",
     "hit_floor_region_clamp::the rule itself, as a decision::a blocker already overlapping the painted rect takes nothing away",
     "hit_floor_region_clamp::the rule itself, as a decision::NEGATIVE CONTROL: the rule reads the ASK, not the blockers alone",
     "tab_view::ADAPT-4: the centered homes are centered, and the overflowing one still scrolls::the ten-foot top bar is centered in its band, not parked in the corner",
     "tab_view::ADAPT-4: the centered homes are centered, and the overflowing one still scrolls::the landscape phone's inline band is centered too",
     "tab_view::ADAPT-4: the centered homes are centered, and the overflowing one still scrolls::...and a strip that does NOT fit fills the band, scrolls, and overflows nothing",
     "tab_view::D5: the strip IS a segmented picker, not a second option row::mounts newPicker's own option row, with the D4 indicator on it",
     "tab_view::D5: the strip IS a segmented picker, not a second option row::`indicator` defaults to the UNDERLINE every reference tab strip draws",
     "tab_view::D5: the strip IS a segmented picker, not a second option row::a tab id IS a name when no label was given, because a segment always has one",
     "tab_view::D5: the strip IS a segmented picker, not a second option row::an icon-only tab STILL carries a semantic label for the dump",
     "tab_view::D5: the strip IS a segmented picker, not a second option row::a badge rides an UNSELECTED tab and survives every switch — the strip is never evicted",
     "tab_view::D5: placement comes from the policy, and the strip is RE-HOMED not rebuilt::the four homes come from shape and input facts — no device name decides one",
     "tab_view::D5: placement comes from the policy, and the strip is RE-HOMED not rebuilt::the sidebar runs the strip DOWN the screen and every other home ACROSS it",
     "tab_view::D5: placement comes from the policy, and the strip is RE-HOMED not rebuilt::a rotation RE-HOMES one strip: the picker object survives and the bar follows it",
     "tab_view::D5: placement comes from the policy, and the strip is RE-HOMED not rebuilt::a DECLARED placement never consults the policy at all",
     "tab_view::D5: placement comes from the policy, and the strip is RE-HOMED not rebuilt::resolvePlacement is pure, so the rule is a test over strings",
     "tab_view::D5: placement comes from the policy, and the strip is RE-HOMED not rebuilt::the thumb-zone band FILLS and every other home HUGS — that is what lets a strip scroll",
     "tab_view::D5: placement comes from the policy, and the strip is RE-HOMED not rebuilt::an automatic placement with nothing to ask is an authoring error, not a guess",
     "tab_view::D5: NESTING — an inner TabView never claims the app-level placement::on one phone viewport the app takes the bottom bar and the page takes the top",
     "tab_view::D5: NESTING — an inner TabView never claims the app-level placement::...and it stays the top bar when the POLICY would have said sidebar",
     "tab_view::D5: NESTING — an inner TabView never claims the app-level placement::neither level claims a focus scope, and the two strips read in document order",
     "tab_view::D5: NESTING — an inner TabView never claims the app-level placement::neither level touches the presenter's back() stack",
     "tab_view::D5: NESTING — an inner TabView never claims the app-level placement::two selections are two Signals: moving one never moves the other",
     "tab_view::D5: NESTING — an inner TabView never claims the app-level placement::the inner TabView is CONTENT, so it is evicted with its host tab and re-nests on return",
     "tab_view::D5: NESTING — an inner TabView never claims the app-level placement::a content factory that THROWS still leaves the depth balanced",
     "tab_view::D5: LAZY content, EVICTED state — a real disposal, never a hide::only the selected tab's subtree exists at all",
     "tab_view::D5: LAZY content, EVICTED state — a real disposal, never a hide::switching TEARS THE PREVIOUS SUBTREE DOWN — the node is gone, not hidden",
     "tab_view::D5: LAZY content, EVICTED state — a real disposal, never a hide::the content region STRETCHES its tab to the offer rather than letting it overflow",
     "tab_view::D5: LAZY content, EVICTED state — a real disposal, never a hide::MEMORY NEUTRALITY: twenty switches return every counter to its baseline",
     "tab_view::D5: LAZY content, EVICTED state — a real disposal, never a hide::eviction disposes what the CALLER owned on the tab scope",
     "tab_view::D5: LAZY content, EVICTED state — a real disposal, never a hide::returning to a tab REPLAYS its entry cost — the accepted cost, stated as a test",
     "tab_view::D5: LAZY content, EVICTED state — a real disposal, never a hide::...and state held OUTSIDE the TabView survives it — the documented escape hatch",
     "tab_view::D5: an OVERFLOWING strip scrolls the selection into view::the strip really is WIDER than its band — otherwise there is nothing to test",
     "tab_view::D5: an OVERFLOWING strip scrolls the selection into view::selecting an off-screen tab scrolls it into view",
     "tab_view::D5: an OVERFLOWING strip scrolls the selection into view::a selection that is ALREADY visible issues no scroll at all",
     "tab_view::D5: an OVERFLOWING strip scrolls the selection into view::...and repeated refreshes on an unchanged selection issue no scroll either",
     "tab_view::D5: an OVERFLOWING strip scrolls the selection into view::a FILLING thumb-zone band has nothing to scroll, and that is correct",
     "tab_view::D5: four inputs, one story::TabView pointer: a tap activates through the presenter (device-true)",
     "tab_view::D5: four inputs, one story::TabView touch: a touch tap/pan drives the control (device-true)",
     "tab_view::D5: four inputs, one story::TabView keyboard: focus + Return drives the control",
     "tab_view::D5: four inputs, one story::TabView gamepad: focus + ButtonA drives the control",
     "tab_view::D5: four inputs, one story::TabView pointer affordance: the pointer structural idiom (hover/direct-drag/wheel)",
     "tab_view::D5: four inputs, one story::TabView touch affordance: the touch structural idiom (44px/naked-pan-scroll/edit-grip)",
     "tab_view::D5: four inputs, one story::TabView keyboard affordance: the keyboard structural idiom (focus ring/Navigate/Activate/Adjust)",
     "tab_view::D5: four inputs, one story::TabView gamepad affordance: the gamepad structural idiom (focus/grab/A-B/ten-foot)",
     "tab_view::D5: four inputs, one story::a bar strip is ONE horizontal group, so a pad crosses it instead of falling out",
     "tab_view::D5: four inputs, one story::the shoulders are UNBOUND while focus sits off the strip — the auto-wiring hazard",
     "tab_view::§6: the content branches carry the caller's enter transition::the declared transition reaches the content branch, and only it",
     "tab_view::§6: the content branches carry the caller's enter transition::the full transition contract passes through field-for-field, exit forced to instant",
     "tab_view::§6: the content branches carry the caller's enter transition::declaring none leaves the branch with none — p4_foyer's path is unchanged",
     "tab_view::§6: the content branches carry the caller's enter transition::a transition must be a table, not the name of one",
     "tab_view::D5: the closed spec, the dump, and disposal::an unknown key is an authoring error",
     "tab_view::D5: the closed spec, the dump, and disposal::`content` must be a FACTORY, and the refusal says why",
     "tab_view::D5: the closed spec, the dump, and disposal::two tabs may not share an id, because a tab id IS the selection value",
     "tab_view::D5: the closed spec, the dump, and disposal::`selection` must be a settable Signal the caller owns",
     "tab_view::D5: the closed spec, the dump, and disposal::the dump is deterministic and names every fact a bug report needs",
     "tab_view::D5: the closed spec, the dump, and disposal::dispose is registry-neutral, strip and indicator and all",
     "tab_view::D5: the closed spec, the dump, and disposal::hot-switch: nothing to carry or cancel — selection is atomic",
     "tab_view::§6: the strip carries the caller's type role, and it follows the home::a bound textSize reaches the segments and re-reads when the placement moves",
     "tab_view::§6: the strip carries the caller's type role, and it follows the home::declaring none leaves the segments at the Button's own default",
     "tab_view::§6: a declared `hug` bottom band centres and scrolls, like every other hugging home::the default is untouched: an automatic band still fills, in the shipped shape",
     "tab_view::§6: a declared `hug` bottom band centres and scrolls, like every other hugging home::the register records it: the changelog carries the hug-band row",
     "tab_view::§6: a declared `hug` bottom band centres and scrolls, like every other hugging home::a declared hug band is CENTRED — measured, not named",
     "tab_view::§6: a declared `hug` bottom band centres and scrolls, like every other hugging home::...and a hug band that does NOT fit takes the whole band and SCROLLS",
     "tab_view::§6: the sidebar rail's width is the caller's to state::the default is content-sized, and a declared band is honoured",
     "tab_view::§6: the sidebar rail's width is the caller's to state::a rail band declared for a placement that has no rail refuses",
     "tab_view_accessories::§6 the matrix: which placement hosts which slot, as strings::each slot names the placements that host it, and an unknown slot names none",
     "tab_view_accessories::§6 the matrix: which placement hosts which slot, as strings::the returned list is a COPY, so a caller cannot edit the framework's rule",
     "tab_view_accessories::§6 the rail's two ends: head above the strip, foot pinned to the bottom::the head sits ABOVE the strip and inside the rail",
     "tab_view_accessories::§6 the rail's two ends: head above the strip, foot pinned to the bottom::the foot is pinned to the rail's BOTTOM, not merely after the last tab",
     "tab_view_accessories::§6 the rail's two ends: head above the strip, foot pinned to the bottom::both ends together keep the strip between them",
     "tab_view_accessories::§6 the rail's two ends: head above the strip, foot pinned to the bottom::the factory is told WHICH placement asked, so one function can serve several",
     "tab_view_accessories::§6 the top band's trailing edge — and the centring that must survive it::the trailing accessory sits at the band's trailing edge",
     "tab_view_accessories::§6 the top band's trailing edge — and the centring that must survive it::the accessory never sits ON a tab, even when the strip overflows its band",
     "tab_view_accessories::§6 the top band's trailing edge — and the centring that must survive it::...and the strip is centred in the box the tabs have, with the accessory clear of it",
     "tab_view_accessories::§6 the top band's trailing edge — and the centring that must survive it::a band with no trailing accessory is centred on the WHOLE band, in the shipped shape",
     "tab_view_accessories::§6 the dock: an accessory directly above the thumb-zone band::on a portrait phone it sits between the content and the band",
     "tab_view_accessories::§6 the dock: an accessory directly above the thumb-zone band::the dock adds NO spacing of its own: the accessory's box meets the band's",
     "tab_view_accessories::§6 the dock: an accessory directly above the thumb-zone band::the landscape phone's inline dock hosts it too, and its band stays centred",
     "tab_view_accessories::§6 the slots follow the placement — mounted where hosted, absent everywhere else::one TabView carrying all four slots mounts exactly the ones its home hosts",
     "tab_view_accessories::§6 the slots follow the placement — mounted where hosted, absent everywhere else::a slot re-homed between its OWN two homes stays reported as mounted",
     "tab_view_accessories::§6 the slots follow the placement — mounted where hosted, absent everywhere else::a factory that returns nothing mounts nothing at all",
     "tab_view_accessories::§6 declaring nothing changes nothing — the shipped home paths are untouched::every one of the four homes keeps the exact path p4_foyer pins",
     "tab_view_accessories::§6 refuse, don't guess: a slot the placement cannot host is an authoring mistake::an unknown slot names the four that exist",
     "tab_view_accessories::§6 refuse, don't guess: a slot the placement cannot host is an authoring mistake::with two bad slots, the refusal names the FIRST in sorted order, every time",
     "tab_view_accessories::§6 refuse, don't guess: a slot the placement cannot host is an authoring mistake::a slot must be a FACTORY, because the placement is its argument",
     "tab_view_accessories::§6 refuse, don't guess: a slot the placement cannot host is an authoring mistake::a DECLARED placement that cannot host the slot refuses, and says which can",
     "tab_view_accessories::§6 refuse, don't guess: a slot the placement cannot host is an authoring mistake::an automatic placement refuses nothing, because every home is still reachable",
     "tab_view_accessories::§6 refuse, don't guess: a slot the placement cannot host is an authoring mistake::a NESTED TabView refuses a rail slot, because nesting always resolves topBar",
     "tab_view_accessories::§6 refuse, don't guess: a slot the placement cannot host is an authoring mistake::`accessories` itself must be a table of slots",
     "tab_view_accessories::§6 the slots are owned, not leaked::re-homing disposes the accessory's scope with its branch",
     "level_picker::LevelPicker: the value model and the closed spec::a read-only Memo is refused at BUILD, not on the first tap",
     "level_picker::LevelPicker: the value model and the closed spec::a count that is not a whole number >= 1 is a build error",
     "level_picker::LevelPicker: the value model and the closed spec::an unknown spec key is an authoring error that names the key",
     "level_picker::LevelPicker: the value model and the closed spec::an unknown segment is a build error naming the three that exist",
     "level_picker::LevelPicker: the value model and the closed spec::an unknown segmentSize is a build error, not a silently-default segment",
     "level_picker::LevelPicker: the value model and the closed spec::a glyph pair on a bar picker is refused rather than silently ignored",
     "level_picker::LevelPicker: the value model and the closed spec::the value is clamped to the run, and to zero only when zero is allowed",
     "level_picker::LevelPicker: the value model and the closed spec::semantic text reports the level IN ITS RANGE, never a bare number",
     "level_picker::LevelPicker: the value model and the closed spec::dump reports segment, count, value and semanticText, and is deterministic",
     "level_picker::LevelPicker: the value model and the closed spec::build/interact/dispose returns the registry to its baseline",
     "level_picker::LevelPicker: three ways to paint one level::segment = bar paints a Box per level and needs no assets",
     "level_picker::LevelPicker: three ways to paint one level::segment = glyph paints a Text per level, filled up to the value",
     "level_picker::LevelPicker: three ways to paint one level::segment = image paints an Image per level, and swaps the asset at the value",
     "level_picker::LevelPicker: three ways to paint one level::the bar's filled and empty halves carry DIFFERENT theme tints by default",
     "level_picker::LevelPicker: three ways to paint one level::tint.filled reaches the filled half and tint.empty the empty one",
     "level_picker::LevelPicker: three ways to paint one level::tint reaches a glyph and an image segment too, not only a bar",
     "level_picker::LevelPicker: three ways to paint one level::an omitted tint half is left to the theme, not painted with the other one",
     "level_picker::LevelPicker: one control, not N buttons::no segment is a Button or a focusable, so no theme can paint a plate behind one",
     "level_picker::LevelPicker: one control, not N buttons::the whole strip is ONE focus stop, not one per level",
     "level_picker::LevelPicker: one control, not N buttons::a read-only picker paints but is not focusable and refuses every input",
     "level_picker::LevelPicker: one control, not N buttons::a disabled picker refuses every input class, not just the pointer",
     "level_picker::LevelPicker reaches the same arithmetic from every input class::LevelPicker pointer: tapping a segment sets the level to that segment",
     "level_picker::LevelPicker reaches the same arithmetic from every input class::LevelPicker touch: a finger drag across the strip scrubs the level live",
     "level_picker::LevelPicker reaches the same arithmetic from every input class::LevelPicker keyboard: Adjust on the focused strip sets the level",
     "level_picker::LevelPicker reaches the same arithmetic from every input class::LevelPicker gamepad: the bumpers Adjust the focused level picker",
     "level_picker::LevelPicker reaches the same arithmetic from every input class::LevelPicker pointer affordance: ONE drag target covers the run, at every count",
     "level_picker::LevelPicker reaches the same arithmetic from every input class::LevelPicker touch affordance: the strip declares the 44px effective target",
     "level_picker::LevelPicker reaches the same arithmetic from every input class::LevelPicker keyboard affordance: no Adjust key is bound while focus sits outside",
     "level_picker::LevelPicker reaches the same arithmetic from every input class::LevelPicker gamepad affordance: at the limit the arrow goes back to navigation",
     "level_picker::LevelPicker reaches the same arithmetic from every input class::LevelPicker hot-switch: losing the pointer mid-scrub CANCELS back, it never commits",
     "level_picker::LevelPicker: a high count is diagnosed, never refused::a count within the threshold has no diagnostics at all",
     "level_picker::LevelPicker: a high count is diagnosed, never refused::a count above the threshold adds a diagnostic and still BUILDS",
     "level_picker::LevelPicker: a high count is diagnosed, never refused::the threshold is the MEASURED one: every package clears it, and one fails at +1",
     "level_picker::newRating is a thin preset over newLevelPicker::newRating is count 5, glyph segments and the star pair — nothing else",
     "level_picker::newRating is a thin preset over newLevelPicker::newRating still accepts starSize and maps it to the segment size",
     "level_picker::newRating is a thin preset over newLevelPicker::newRating keeps its own dump schema, and gains `segment` additively",
     "level_picker::newRating is a thin preset over newLevelPicker::newRating diagnoses a high count through exactly the same rule",
     "level_picker::LevelPicker: every mark paints a rectangle with AREA::a bar mark standing alone has HEIGHT as well as width",
     "level_picker::LevelPicker: every mark paints a rectangle with AREA::...and in the composition the showcase ships: a strip beside a stepper",
     "level_picker::LevelPicker: every mark paints a rectangle with AREA::the shipped gallery surface paints every tinted mark, at every swept viewport",
     "controls_namespace::the Controls namespace::exports exactly the nineteen composite controls the namespace names, and nothing else",
     "controls_namespace::the Controls namespace::every entry builds what the old two-argument form builds",
     "controls_namespace::the Controls namespace::every old-form builder carries its deprecation row",
     "controls_namespace::the Controls namespace::the eleven infrastructure constructors stay at the top level and stay out of Controls",
     "leaf_helpers::num: the predicate seventeen modules wrote by hand::isFinite refuses NaN, both infinities, and every non-number",
     "leaf_helpers::num: the predicate seventeen modules wrote by hand::isFinite is the same expression every copy carried",
     "leaf_helpers::num: the predicate seventeen modules wrote by hand::roundPx is floor(v + 0.5), which is NOT math.round at a negative half",
     "leaf_helpers::paths: the prefix test with two wrong copies in live routing::a prefix must land on a segment boundary",
     "leaf_helpers::paths: the prefix test with two wrong copies in live routing::disagrees with the raw string.sub form exactly where the defect was",
     "leaf_helpers::paths: the prefix test with two wrong copies in live routing::escape makes an id safe to interpolate into a Lua pattern",
     "leaf_helpers::rect: fifteen bodies, four epsilons, two edge conventions::touching is not overlapping, and the epsilon is the caller's",
     "leaf_helpers::rect: fifteen bodies, four epsilons, two edge conventions::intersection reports the depth on both axes, which is what a report prints",
     "leaf_helpers::rect: fifteen bodies, four epsilons, two edge conventions::containment is exact by default and forgiving with an epsilon",
     "leaf_helpers::rect: fifteen bodies, four epsilons, two edge conventions::the two edge conventions are two functions, and they disagree on the edge",
     "leaf_helpers::rect: fifteen bodies, four epsilons, two edge conventions::equality is field-for-field and nil-tolerant on both sides",
     "leaf_helpers::rect: fifteen bodies, four epsilons, two edge conventions::centre tolerates an unmeasured target, and round snaps all four fields",
     "leaf_helpers::REUSE-3: a tap on /MenuBar does not route to /Menu::the longer-named screen owns its own button, whichever order they were presented",
     "leaf_helpers::REUSE-3: a tap on /MenuBar does not route to /Menu::POSITIVE CONTROL: two screens with unrelated names route the same way",
     "text_distance::text_distance: the matrix five modules wrote by hand::distance counts insertions, deletions and substitutions",
     "text_distance::text_distance: the matrix five modules wrote by hand::a transposition costs two, because this is not Damerau",
     "text_distance::text_distance: the matrix five modules wrote by hand::an empty side costs the other side's length",
     "text_distance::text_distance: the matrix five modules wrote by hand::it measures BYTES, which is what an identifier is",
     "text_distance::text_distance: the matrix five modules wrote by hand::nearest refuses everything past the threshold the caller passed",
     "text_distance::text_distance: the matrix five modules wrote by hand::the threshold boundary is inclusive on the caller's number and nothing past it",
     "text_distance::text_distance: the matrix five modules wrote by hand::listOf sorts, and the separator is the caller's sentence",
     "text_distance::ARCH-8: the specGuard FACADE, not the module behind it::the export is the module, and carries both members",
     "text_distance::ARCH-8: the specGuard FACADE, not the module behind it::keySet through the facade builds a FROZEN set",
     "text_distance::ARCH-8: the specGuard FACADE, not the module behind it::assertKnownKeys through the facade refuses, names the boundary, and suggests",
     "text_distance::ARCH-8: the specGuard FACADE, not the module behind it::a conforming spec passes, and `kind` is the sentence's own word",
     "text_distance::ARCH-17: each caller kept its own threshold::spec_guard suggests at distance 2 and stops there — case-SENSITIVELY",
     "text_distance::ARCH-17: each caller kept its own threshold::motion/curves folds case and scales its threshold with the name's length",
     "client_host::client host: the six steps, in order, once::hands back a bound, presentable surface",
     "client_host::client host: the six steps, in order, once::connects exactly one frame source, and disposing takes it back",
     "client_host::client host: the six steps, in order, once::refuses an unknown opt by name, the way every other Facet boundary does",
     "client_host::REUSE-109: the frame drives the clock, not just the solve::a frame advances the motion clock with the frame's own dt",
     "client_host::REUSE-109: the frame drives the clock, not just the solve::ticks BEFORE it refreshes, so this frame's hook work is solved this frame",
     "client_host::REUSE-109: the frame drives the clock, not just the solve::a disposed host runs no further frames, even if the source fires again",
     "client_host::REUSE-115 / §3b-3: a presenter takes exactly one frame driver::the host claims it, and a second driver is refused by name",
     "client_host::REUSE-115 / §3b-3: a presenter takes exactly one frame driver::disposing the host releases the claim, so the surface can be stood back up",
     "client_host::REUSE-115 / §3b-3: a presenter takes exactly one frame driver::motion_driver.bind takes the same claim",
     "client_host::client host: teardown::dispose unbinds the environment and is idempotent",
     "client_host::client host: teardown::dispose releases the adapter and the input system, not only the frame and the binding",
     "client_host::client host: teardown::a throw AFTER the adapter and input system exist unwinds both of them",
     "client_host::client host: teardown::a caller-supplied piece with no dispose is left alone rather than errored on",
     "client_host::client host: teardown::a construction that fails halfway leaves nothing bound",
     "world_substrate::the headless world substrate (REUSE-88)::builds the six pieces, viewport first, one presenter per adapter",
     "world_substrate::the headless world substrate (REUSE-88)::the default viewport is a phone portrait, and every named fact is optional",
     "world_substrate::the headless world substrate (REUSE-88)::named facts land on the environment, and `env` takes the ones with no name",
     "world_substrate::the headless world substrate (REUSE-88)::settle is N frames of clock AND solve, and never zero of them",
     "world_substrate::the headless world substrate (REUSE-88)::press is both edges, so the same key twice is two activations",
     "world_substrate::REUSE-88: the retired tuple order stays retired::no spec returns the swapped order any more",
     "world_substrate::REUSE-88: the retired tuple order stays retired::the count of hand-rolled presenter builders only ever falls (R12 trigger)",
     "world_substrate::REUSE-88: the retired tuple order stays retired::every migrated file builds on the shared substrate rather than by hand",
     "session_lifetime::newActionSystem has a lifetime (ARCH-12)::dispose destroys every context it created and returns the core to baseline",
     "session_lifetime::newActionSystem has a lifetime (ARCH-12)::dispose is idempotent, and a disposed system delivers nothing",
     "session_lifetime::newActionSystem has a lifetime (ARCH-12)::a context destroyed by hand before dispose is not destroyed twice",
     "session_lifetime::newResourceProvider has a lifetime (ARCH-13)::dispose drops the cache, the in-flight requests and the give-up ledger",
     "session_lifetime::newResourceProvider has a lifetime (ARCH-13)::dispose is idempotent, and a disposed provider accepts no new work",
     "process_globals::ARCH-23: one of the two text-metric stores is content-addressed::a width measured under one core is the same answer under another",
     "process_globals::ARCH-23: one of the two text-metric stores is content-addressed::calibration is keyed by FONT ALONE, so one core's tuning changes what another measures",
     "process_globals::ARCH-23: one of the two text-metric stores is content-addressed::calibration is per-FONT and clamps, so a bad measurement cannot corrupt a good one",
     "process_globals::ARCH-24: the motion registries are shared, and that is the dial::two cores see one motion vocabulary, because a class is a design decision",
     "process_globals::ARCH-24: the motion registries are shared, and that is the dial::one consumer's resetClasses() makes another consumer's next motion call throw",
     "process_globals::ARCH-24: the motion registries are shared, and that is the dial::reset restores the built-in classes but leaves the curve registry empty",
     "hidden_roots_owner::REUSE-29: the three hidden-root walks share one owner::the DISCLOSURE walk skips a losing candidate, and raises for a winning one",
     "hidden_roots_owner::REUSE-29: the three hidden-root walks share one owner::the HELP walk skips a losing candidate",
     "hidden_roots_owner::REUSE-29: the three hidden-root walks share one owner::POSITIVE CONTROL: the same help text on a WINNING candidate does raise",
     "hidden_roots_owner::REUSE-29: the three hidden-root walks share one owner::the REVEAL walk skips a losing candidate, and marches for a winning one",
     "hidden_roots_owner::REUSE-29: the three hidden-root walks share one owner::no presentation module re-implements the owner",
     "adaptive_defaults::ADAPT-1: the environment is discoverable from the core it was built on::publishes itself, and a control on that core finds it",
     "adaptive_defaults::ADAPT-1: the environment is discoverable from the core it was built on::an unrelated core has nothing to find, and says which of the two reasons",
     "adaptive_defaults::ADAPT-1: the environment is discoverable from the core it was built on::TWO environments on one core are refused, never resolved by coin flip",
     "adaptive_defaults::ADAPT-1: the environment is discoverable from the core it was built on::re-publishing the SAME environment is not ambiguity",
     "adaptive_defaults::ADAPT-1: the environment is discoverable from the core it was built on::MEDIUM-3: an environment nothing else holds is still findable from its core",
     "adaptive_defaults::ADAPT-1: the environment is discoverable from the core it was built on::MEDIUM-3: a core nothing else holds is collected, registration and all",
     "adaptive_defaults::ADAPT-1: the environment is discoverable from the core it was built on::MEDIUM-3: the registration lives on the core, not in a module-level store",
     "adaptive_defaults::ADAPT-1: the environment is discoverable from the core it was built on::MEDIUM-3: unpublishing one of two environments restores the unambiguous answer",
     "adaptive_defaults::ADAPT-1: the six adaptive controls take their facts from the surface::Picker: a compact surface collapses four long labels to the row list, unwired",
     "adaptive_defaults::ADAPT-1: the six adaptive controls take their facts from the surface::Picker: an authored size class still wins over the surface's",
     "adaptive_defaults::ADAPT-1: the six adaptive controls take their facts from the surface::PopupButton: touch resolves the SHEET with nothing wired at all",
     "adaptive_defaults::ADAPT-1: the six adaptive controls take their facts from the surface::Menu: the same rung, through the same recipe, unwired",
     "adaptive_defaults::ADAPT-1: the six adaptive controls take their facts from the surface::LevelPicker: the touch run is wider than the pointer run, with no `env` key",
     "adaptive_defaults::ADAPT-1: the six adaptive controls take their facts from the surface::Table: a row on a touch surface clears the target floor with no `env` key (ADAPT-11)",
     "adaptive_defaults::ADAPT-1: the six adaptive controls take their facts from the surface::Table: an authored `env` still wins over the surface's, and a pinned height still wins over both",
     "adaptive_defaults::ADAPT-1: the six adaptive controls take their facts from the surface::Table: the row box and the COLUMN RESERVATION read one environment (R1-I4)",
     "adaptive_defaults::ADAPT-1: the six adaptive controls take their facts from the surface::TextInput: the field lifts itself off the keyboard with no `env` key",
     "adaptive_defaults::ADAPT-1: the six adaptive controls take their facts from the surface::TabView: an automatic placement no longer needs the author to hand it anything",
     "adaptive_defaults::ADAPT-1: a control that cannot get its facts refuses, naming the host::Picker, PopupButton, Menu and TextInput each name the fix, not the symptom",
     "adaptive_defaults::ADAPT-1: a control that cannot get its facts refuses, naming the host::...and a DECLARED presentation consults no ladder, so it never refuses",
     "adaptive_defaults::ADAPT-1: the client host is what makes this free on a real surface::dispose releases the environment the host registered",
     "adaptive_defaults::ADAPT-1: the client host is what makes this free on a real surface::a hosted screen's controls adapt with zero authoring",
     "adaptive_defaults::ADAPT-1: every control that can refuse for want of the environment says so in api.md::the refusing controls are read out of the sources, and there are seven today",
     "adaptive_defaults::ADAPT-1: every control that can refuse for want of the environment says so in api.md::a control that refuses in either shape is discovered by the scan itself",
     "adaptive_defaults::ADAPT-1: every control that can refuse for want of the environment says so in api.md::each discovered control's own section names the refusal and the environment",
     "adaptive_defaults::ADAPT-1: every control that can refuse for want of the environment says so in api.md::removing a control's section from api.md is reported",
     "ten_foot_metrics::ten-foot metrics — the factor::the metric factor agrees with the type floor's factor on every display class",
     "ten_foot_metrics::ten-foot metrics — proportion equality across the control set::every scaled metric moves by exactly the factor, in all nine configurations",
     "ten_foot_metrics::ten-foot metrics — proportion equality across the control set::a radius and a stroke are the PAINT family, and they scale in lockstep",
     "ten_foot_metrics::ten-foot metrics — proportion equality across the control set::a future scale tweak moves the whole family, measure and paint together",
     "ten_foot_metrics::ten-foot metrics — proportion equality across the control set::a scaled radius lands on a whole pixel, because a UDim offset is an integer",
     "ten_foot_metrics::ten-foot metrics — proportion equality across the control set::the capsule sentinel scales too, and the engine's clamp makes it the same pixel",
     "ten_foot_metrics::ten-foot metrics — proportion equality across the control set::the painted number and the measured number are ONE derivation, on every package",
     "ten_foot_metrics::ten-foot metrics — proportion equality across the control set::a package's own ten-foot radius declaration wins for the paint too, and is not double-scaled",
     "ten_foot_metrics::ten-foot metrics — proportion equality across the control set::the near paint derivation is the identity — the same table, not a copy",
     "ten_foot_metrics::ten-foot metrics — proportion equality across the control set::a slot inset scales its authored half and leaves its art half alone",
     "ten_foot_metrics::ten-foot metrics — proportion equality across the control set::the text-to-control proportion at ten-foot equals its near proportion",
     "ten_foot_metrics::ten-foot metrics — proportion equality across the control set::a pixel package's ten-foot ladder stays on its own grid",
     "ten_foot_metrics::ten-foot metrics — ADAPT-L1: the default path carries the density fact::a bare ten-foot mount reads ten-foot density with no theme controller",
     "ten_foot_metrics::ten-foot metrics — ADAPT-L1: the default path carries the density fact::…and its metric ladder is scaled, with no theme controller",
     "ten_foot_metrics::ten-foot metrics — ADAPT-L1: the default path carries the density fact::…and its grid measures CAPPED lanes, fewer than the same grid at desktop",
     "ten_foot_metrics::ten-foot metrics — ADAPT-L1: the default path carries the density fact::…and a metric NAME spent as a gap resolves to the scaled step",
     "ten_foot_metrics::ten-foot metrics — package authority::a package's own ten-foot declaration wins over the derived scale",
     "ten_foot_metrics::ten-foot metrics — package authority::a ten-foot declaration that names no metric is refused",
     "ten_foot_metrics::ten-foot metrics — what does NOT scale::the overscan margins compose with the safe insets and are not scaled",
     "ten_foot_metrics::ten-foot metrics — what does NOT scale::durations stay time-true",
     "ten_foot_metrics::ten-foot metrics — what does NOT scale::type sizes are scaled once, by the type floor, never twice",
     "ten_foot_metrics::ten-foot metrics — what does NOT scale::line counts are counts, not lengths",
     "ten_foot_metrics::ten-foot metrics — what does NOT scale::a percentage width is untouched at ten-foot",
     "ten_foot_metrics::ten-foot metrics — the hit floor has one owner::the effective focus-target floor is 66 at ten-foot",
     "ten_foot_metrics::ten-foot metrics — the hit floor has one owner::a sub-floor control's hit rect is expanded to the ten-foot floor",
     "ten_foot_metrics::ten-foot metrics — interplay: the expand plate and its Close::the plate opens at ten-foot and its Close is a 66px focus target",
     "ten_foot_metrics::ten-foot metrics — interplay: the table's collapse at distance::a ten-foot table takes the scaled distance rung for its rows",
     "ten_foot_metrics::ten-foot metrics — interplay: the table's collapse at distance::…and the priority-collapse ladder still fires at distance, and discloses",
     "ten_foot_metrics::ten-foot metrics — interplay: carousel snap with scaled cards::the scaled gutter changes the card's own extent",
     "ten_foot_metrics::ten-foot metrics — interplay: carousel snap with scaled cards::a fling settles exactly on a card boundary at the scaled extents",
     "ten_foot_metrics::ten-foot metrics — the transform is total::the near path is the identity — the same table, not a copy",
     "ten_foot_metrics::ten-foot metrics — the transform is total::is idempotent and reversible",
     "ten_foot_metrics::ten-foot metrics — the transform is total::every numeric leaf of every configuration is classified",
     "ten_foot_metrics::ten-foot metrics — the transform is total::a garbage metrics fact is passed through, never thrown on",
     "ten_foot_metrics::ten-foot metrics — the transform is total::a derivative of a distance-adapted read is refused, never applied twice",
     "ten_foot_metrics::ten-foot metrics — the transform is total::…and the refusal keys on the adapted shape alone",
     "ten_foot_metrics::ten-foot metrics — the transform is total::the environment publishes exactly one distance-adapted metric authority",
     "ten_foot_metrics::ten-foot metrics — the transform is total::a read held across a theme swap restores ITS package, not the incumbent",
     "ten_foot_metrics::ten-foot metrics — the paint family reaches the paint::the built-in sheet bakes the ten-foot corner, and moves nothing else",
     "ten_foot_metrics::ten-foot metrics — the paint family reaches the paint::every metric-derived rule of every package sheet carries the derived number",
     "ten_foot_metrics::ten-foot metrics — the paint family reaches the paint::the near sheet is byte-identical to the one built with no display class",
     "ten_foot_metrics::ten-foot metrics — the paint family reaches the paint::the compiled style a themed target is handed carries the same paint family",
     "ten_foot_metrics::ten-foot metrics — the paint family reaches the paint::every render-target constructor in this repository states the distance it paints at",
     "ten_foot_metrics::ten-foot metrics — the paint family reaches the paint::the render target and the host wire the display class through to the paint",
     "ten_foot_metrics::ten-foot metrics — the second door: an authored token::every module that can turn a paint token into a number is named, and both doors derive",
     "ten_foot_metrics::ten-foot metrics — the second door: an authored token::normalized corner and stroke data carry the token they were authored with",
     "ten_foot_metrics::ten-foot metrics — the second door: an authored token::the paint resolver re-derives a token against the style it paints with",
     "ten_foot_metrics::ten-foot metrics — the second door: an authored token::the segmented picker's indicator paints the ten-foot capsule at Large",
     "space_tight_step::space.tight — the missing spacing step::resolves to 6 at Studio Neutral (xs=4, s=8)",
     "space_tight_step::space.tight — the missing spacing step::is a recognized space step, resolvable by name",
     "space_tight_step::space.tight — the missing spacing step::an authored space.tight wins outright",
     "space_tight_step::space.tight — the missing spacing step::an unauthored space.tight derives as the midpoint of the package's own xs/s",
     "space_tight_step::space.tight — the missing spacing step::rides the ten-foot ladder like its neighbors (non-1.0 rung)",
     "space_tight_step::space.tight — the missing spacing step::resolves through a blueprint gap prop",
     "space_tight_step::space.tight — the missing spacing step::resolves through a fixed-dimension px prop",
     "space_tight_step::space.tight — the missing spacing step::resolves through the VirtualList/VirtualGrid gap_metric channel",
     "app_metrics::app metrics: declaring the namespace::declares names under its own group, reads them back frozen, and retires them",
     "app_metrics::app metrics: declaring the namespace::two apps in one process compose instead of clobbering",
     "app_metrics::app metrics: declaring the namespace::refuses a name that is not a legal metric identifier",
     "app_metrics::app metrics: declaring the namespace::refuses a value that is not a finite number",
     "app_metrics::app metrics: declaring the namespace::refuses a non-table declaration and an illegal group name",
     "app_metrics::app metrics: a declared name is spellable::isMetricPath accepts a declared app name and refuses an undeclared one",
     "app_metrics::app metrics: a declared name is spellable::a dim may carry an app metric name, and an undeclared one is refused at construction",
     "app_metrics::app metrics: a declared name is spellable::resolveNumber answers an app name against a live snapshot",
     "app_metrics::app metrics: every package carries them::resolves on Studio Neutral and on a package that never heard of them",
     "app_metrics::app metrics: every package carries them::publishes the section even for a group nobody declared",
     "app_metrics::app metrics: every package carries them::a snapshot resolved BEFORE the declaration still spells the names",
     "app_metrics::app metrics: every package carries them::a hand-built metrics table is passed through untouched",
     "app_metrics::app metrics: every package carries them::a base refreshed for one group is refreshed again for the next",
     "app_metrics::app metrics: a declaration reaches a warm environment::a group declared AFTER the first read is spellable on the next one",
     "app_metrics::app metrics: a declaration reaches a warm environment::retiring a group reaches a warm environment as well",
     "app_metrics::app metrics: a declaration reaches a warm environment::every observer is offered the generation, even when an earlier one fails",
     "app_metrics::app metrics: a declaration reaches a warm environment::a DIRECT call re-raises, and the vocabulary is live either way",
     "app_metrics::app metrics: a declaration reaches a warm environment::a declaration made inside a memo is quarantined, recorded, and healed later",
     "app_metrics::app metrics: a declaration reaches a warm environment::a signal whose value trails the generation is caught up by the next declaration",
     "app_metrics::app metrics: a declaration reaches a warm environment::re-declaring the same values is a no-op",
     "app_metrics::app metrics: the ten-foot ladder::classifies an app length as a length and scales it by the factor",
     "app_metrics::app metrics: the ten-foot ladder::classifies an app number whose name says it is not a length as exempt",
     "app_metrics::app metrics: a theme owns them like every other metric::a package may MOVE a declared app metric",
     "app_metrics::app metrics: a theme owns them like every other metric::refuses a package that names an app metric the app never declared",
     "app_metrics::app metrics: a theme owns them like every other metric::an override reaches an app metric, and an undeclared one still refuses",
     "app_metrics::app metrics: a theme owns them like every other metric::metrics.tenFoot may name an app metric, and wins over the derived ladder",
     "app_metrics::app metrics: a theme owns them like every other metric::a pixel package snaps app lengths and leaves app non-lengths alone",
     "app_metrics::app metrics: a theme owns them like every other metric::a group declared AFTER a pixel package resolved is snapped onto its grid too",
     "app_metrics::app metrics: the theme dump carries them::token_sync emits a record per declared app leaf a package authored",
     "layout_defaults::ADAPT-L2 — Grid lanes itself from the space it got::an undeclared Grid takes the offer's lanes at compact touch portrait",
     "layout_defaults::ADAPT-L2 — Grid lanes itself from the space it got::an undeclared Grid takes the offer's lanes at compact touch landscape",
     "layout_defaults::ADAPT-L2 — Grid lanes itself from the space it got::an undeclared Grid takes the offer's lanes at regular touch tablet",
     "layout_defaults::ADAPT-L2 — Grid lanes itself from the space it got::an undeclared Grid takes the offer's lanes at desktop pointer",
     "layout_defaults::ADAPT-L2 — Grid lanes itself from the space it got::an undeclared Grid takes the offer's lanes at ten-foot gamepad",
     "layout_defaults::ADAPT-L2 — Grid lanes itself from the space it got::an undeclared Grid takes the offer's lanes at hybrid pointer+touch",
     "layout_defaults::ADAPT-L2 — Grid lanes itself from the space it got::the undeclared answer IS the declared answer, at every combo",
     "layout_defaults::ADAPT-L2 — Grid lanes itself from the space it got::a television gets FEWER lanes than a desktop, with nothing declared",
     "layout_defaults::ADAPT-L2 — Grid lanes itself from the space it got::AUTHORED ALWAYS WINS: a declared lane count is untouched by the default",
     "layout_defaults::ADAPT-L2 — Grid lanes itself from the space it got::...and so is a declared minimum, in both directions",
     "layout_defaults::ADAPT-L2 — Grid lanes itself from the space it got::THE NULL HYPOTHESIS: a grid of fill children still gets one lane",
     "layout_defaults::ADAPT-L4 — AdaptiveStack refuses rather than silently not adapting::a bare AdaptiveStack REFUSES to construct",
     "layout_defaults::ADAPT-L4 — AdaptiveStack refuses rather than silently not adapting::...and the refusal names the host, the fact, and the unconditional class",
     "layout_defaults::ADAPT-L4 — AdaptiveStack refuses rather than silently not adapting::THE NULL HYPOTHESIS: a bound axis constructs and still flips in place",
     "layout_defaults::ADAPT-L4 — AdaptiveStack refuses rather than silently not adapting::the comment three fixtures carried is TRUE now: 320px is a column, 1280px a row",
     "layout_defaults::ADAPT-L3 — the axis is the SCREEN's width, and the solver says so::the band exists and is exactly the offer's, not the window's",
     "layout_defaults::ADAPT-L3 — the axis is the SCREEN's width, and the solver says so::...and it is NOT silent — the finding names the container, the amount and both fixes",
     "layout_defaults::ADAPT-L3 — the axis is the SCREEN's width, and the solver says so::the fact itself is honest about what it is: `viewportWidth` IS the raw window",
     "layout_defaults::ADAPT-L8 — a row grid says when it squeezed its columns::a squeeze is REPORTED, at every width the audit measured one",
     "layout_defaults::ADAPT-L8 — a row grid says when it squeezed its columns::...and the finding names the amount, the room, and the three ways out",
     "layout_defaults::ADAPT-L8 — a row grid says when it squeezed its columns::THE GEOMETRY DID NOT MOVE: the squeeze is still the squeeze",
     "layout_defaults::ADAPT-L5 — overflow is not silent, and says the same thing at every combo::a page taller than its box is DIAGNOSED at every one of the six combos",
     "layout_defaults::ADAPT-L5 — overflow is not silent, and says the same thing at every combo::...and so is a fixed row, and a Spacer row whose ends alone do not fit",
     "layout_defaults::ADAPT-L5 — overflow is not silent, and says the same thing at every combo::THE DIAGNOSIS IS COMPLETE at every combo: container, axis, amount, both fixes",
     "layout_defaults::ADAPT-L5 — overflow is not silent, and says the same thing at every combo::CLIPPING IS NEVER THE ANSWER: nothing was quietly made a clip host",
     "layout_defaults::B-6e — the ten-foot Composition measure caps at the ruled 900::the ten-foot main lane is 900, where it was the desktop answer with more pixels",
     "layout_defaults::B-6e — the ten-foot Composition measure caps at the ruled 900::...and the derivation holds: it IS the tablet measure times the distance factor",
     "layout_defaults::B-6e — the ten-foot Composition measure caps at the ruled 900::THE OTHER FIVE COMBOS DO NOT MOVE",
     "layout_defaults::B-6e — the ten-foot Composition measure caps at the ruled 900::the lanes re-absorb the slack and the band is CENTRED, not parked left",
     "layout_defaults::B-6e — the ten-foot Composition measure caps at the ruled 900::AUTHORED WINS: a declared `maxMeasure` is not second-guessed at ten-foot",
     "layout_defaults::B-6e — the ten-foot Composition measure caps at the ruled 900::...and so does a group that declares its own `maxWidth`, in both directions",
     "containment_diagnostic::containment: a stack child placed outside its own cross axis says so::names the child, the edge and the overhang in px",
     "containment_diagnostic::containment: a stack child placed outside its own cross axis says so::...and the sentence carries the same three facts a reader needs",
     "containment_diagnostic::containment: a stack child placed outside its own cross axis says so::a contained tree reports NOTHING — the finding is not a permanent fixture",
     "containment_diagnostic::containment: a stack child placed outside its own cross axis says so::a DECLARED ROUTE is a decision, not a defect",
     "containment_diagnostic::containment: a stack child placed outside its own cross axis says so::a parent that does not DECLARE its width is not policed, even when it is squeezed",
     "containment_diagnostic::containment: a stack child placed outside its own cross axis says so::the MAIN axis is not reported twice",
     "containment_diagnostic::containment: a stack child placed outside its own cross axis says so::the three exclusions the census bought are pinned, not merely absent",
     "containment_diagnostic::containment: a stack child placed outside its own cross axis says so::a subtree that never paints files nothing",
     "themed_containment::themed containment: the surfaces the director photographed::the screen-anchored HUD keeps every mark inside its themed plates (items 6 and 7)",
     "themed_containment::themed containment: the surfaces the director photographed::the nested tab view keeps every label inside its themed plates (item 8)",
     "themed_containment::themed containment: the surfaces the director photographed::the playlist table keeps every cell inside its themed plates",
     "themed_containment::themed containment: the surfaces the director photographed::...and in EDIT MODE, where the delete disc lives (item 9)",
     "themed_containment::themed containment: the four measurements, pinned::item 6: the round HUD chip has room for its own mark, and the framework can prove it",
     "themed_containment::themed containment: the four measurements, pinned::item 6, the framework half: a chrome inset may not eat through the node's own line box",
     "themed_containment::themed containment: the four measurements, pinned::item 7: the task panel's reward chip clears the plank frame it sits in",
     "themed_containment::themed containment: the four measurements, pinned::item 8: the pill covers the whole segment it marks instead of shrinking inside it",
     "themed_containment::themed containment: the four measurements, pinned::item 9: the edit-mode delete disc fits the gutter the row leaves for it",
     "themed_containment::themed containment: item 9's class, at every legal package::the edit affordance never exceeds the gutter the row reserved for it",
     "themed_containment::themed containment: the guard's new rules bite::a row inside the RECT but over the package's carved border is reported",
     "themed_containment::themed containment: the guard's new rules bite::...and the SAME screen with no package is silent, because the rect is the edge",
     "themed_containment::themed containment: the guard's new rules bite::...and one edit narrower it clears the frame and is silent under the package too",
     "themed_containment::themed containment: the guard's new rules bite::a LEAF laid straight on a sibling plate is judged against the plate's carved border",
     "themed_containment::themed containment: the guard's new rules bite::...and wrapping the ink in a container does not repair it — the guard still reports",
     "themed_containment::themed containment: the guard's new rules bite::...and with no package the sibling plate is not a boundary at all",
     "themed_containment::themed containment: the guard's new rules bite::two full-bleed plates in one stack report the ink ONCE and neither other",
     "themed_containment::themed containment: the HUD plate ledger cannot rot::every row in the HUD plate ledger still fires",
     "themed_containment::themed containment: the count seal clears its own segment's chrome (director item 2)::with no package installed, the badge sits flush at the corner — shipped geometry, unchanged",
     "themed_containment::themed containment: the count seal clears its own segment's chrome (director item 2)::under pixel_quest, the count seal clears the segment's own carved border, PER AXIS (T1)",
     "themed_containment::themed containment: the count seal clears its own segment's chrome (director item 2)::under fantasy_ornate, the count seal clears the segment's own carved border, PER AXIS (T1)",
     "themed_containment::themed containment: the count seal clears its own segment's chrome (director item 2)::...and overflow_guard itself reports NOTHING here — the class is structurally invisible to it",
     "themed_containment::themed containment: adaptive_controls' HUD-tile badge clears its own tile's chrome (director item 2, second consumer)::with no package installed, every tile's badge sits flush at the corner — shipped geometry, unchanged",
     "themed_containment::themed containment: adaptive_controls' HUD-tile badge clears its own tile's chrome (director item 2, second consumer)::under pixel_quest, every tile's badge clears its own tile's carved border, PER AXIS (T1)",
     "themed_containment::themed containment: adaptive_controls' HUD-tile badge clears its own tile's chrome (director item 2, second consumer)::under fantasy_ornate, every tile's badge clears its own tile's carved border, PER AXIS (T1)",
     "consumer_standalone::examples/consumer: the standalone project mounts::puts every part of the screen on the target",
     "consumer_standalone::examples/consumer: the standalone project mounts::reads its label out of the signal, not out of a literal",
     "consumer_standalone::examples/consumer: the theme decides the paint::repaints the accent tint when a theme package is committed",
     "consumer_standalone::examples/consumer: input and state::a press on Bump raises the count and repaints the label",
     "consumer_standalone::examples/consumer: input and state::a write to the signal repaints the label with no press at all",
     "consumer_standalone::examples/consumer: input and state::the toggle carries the signal the screen owns",
     "consumer_standalone::examples/consumer: input and state::Close reports itself, which is what the client script tears down on",
     "consumer_standalone::examples/consumer: it adapts by re-solving::the button stack arranges differently at a compact and a wide viewport",
     "consumer_standalone::examples/consumer: it adapts by re-solving::a viewport change re-solves the mounted surface without rebuilding it",
     "consumer_standalone::examples/consumer: it adapts by re-solving::the player's preferred text size grows the untouched label",
     "consumer_standalone::examples/consumer: teardown leaves nothing behind::returns the reactive registries to their pre-screen baseline",
     "consumer_standalone::examples/consumer: teardown leaves nothing behind::takes every node off the render target",
     "overflow_guard::overflow guard: a plate that does not hold its own content::names the node, the box it escaped, the edge and the overhang in px",
     "overflow_guard::overflow guard: a plate that does not hold its own content::...and the SAME plate, one edit wider, is silent",
     "overflow_guard::overflow guard: a plate that does not hold its own content::a fractional px is arithmetic, not an escape",
     "overflow_guard::overflow guard: what counts as paint::a transparent hit target paints nothing, so it cannot paint outside anything",
     "overflow_guard::overflow guard: what counts as paint::...and the same Button with a LABEL is paint, and is reported",
     "overflow_guard::overflow guard: a scroll host is a window on one axis and a wall on the other::a tall canvas past the window along the SCROLLED axis is silent",
     "overflow_guard::overflow guard: a scroll host is a window on one axis and a wall on the other::...and a row wider than the window on the CROSS axis is reported",
     "overflow_guard::overflow guard: a visible string in a box with no area::a visible non-empty Text with a zero-width box is reported",
     "overflow_guard::overflow guard: a visible string in a box with no area::...and the identical box inside a HIDDEN subtree is not, because visibility is the ancestor chain",
     "overflow_guard::overflow guard: the world runs it by default::a settle over an overflowing screen is RED with no fixture opting in",
     "overflow_guard::overflow guard: the world runs it by default::...and a fixture that states WHY it is exempt is not",
     "overflow_guard::overflow guard: the waiver registry cannot rot::a matching waiver silences its violation and is counted as used",
     "overflow_guard::overflow guard: the waiver registry cannot rot::...and the same waiver with a smaller cap refuses, naming itself in the message",
     "overflow_guard::overflow guard: the waiver registry cannot rot::NEGATIVE CONTROL: a waiver that matches nothing is NAMED as unused",
     "overflow_guard::overflow guard: the waiver registry cannot rot::every waiver in the shipped registry fired at least once across this suite",
     "overflow_guard::overflow guard: a composition region outside its box IS the declared fallback::A LEGAL composition keeps every region inside its own box — the property, positive",
     "overflow_guard::overflow guard: a composition region outside its box IS the declared fallback::...and twenty pixels later NOTHING is legal, so the regions leave the box AND the framework says so",
     "overflow_guard::overflow guard: a composition region outside its box IS the declared fallback::...AND IT HOLDS WITH A CHROME ROW DECLARED — the arm the first case could not fail on"
    ],
    "shell": "test -x tools/suite_transcript.sh && test -x ../../../games/RascalRally/code/tools/suite_transcript.sh && [ \"$(git ls-tree HEAD tools/suite_transcript.sh | cut -c1-6)\" = \"100755\" ] && [ \"$(git ls-tree HEAD tools/suite_cache_selftest.sh | cut -c1-6)\" = \"100755\" ] && [ \"$(cd ../../../games/RascalRally/code && git ls-tree HEAD tools/suite_transcript.sh | cut -c1-6)\" = \"100755\" ] && [ \"$(tools/test.sh --status)\" = \"hit\" ] && (cd ../../../games/RascalRally/code && cat \"$FACET_VERIFY_RR_TRANSCRIPT\" >/dev/null) && [ \"$(cd ../../../games/RascalRally/code && tools/suite_transcript.sh --status)\" = \"hit\" ]"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "navigation-and-menus::d0-one-run-per-sweep",
   "name": "d0-one-run-per-sweep",
   "note": "d0-one-run-per-sweep: 7883 named suite cases must pass; check_manifest_integrity, suite, rascalrally-suite must exit zero; its file-existence and text assertions must hold; and artifacts/navigation-and-menus/sweep-economy.md must exist. Requirements: UI-AGENT-001.",
   "phase": "navigation-and-menus",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite_cache_selftest"
    ]
   },
   "class": "exit0",
   "evidence": "artifacts/navigation-and-menus/suite-cache-selftest.md",
   "id": "navigation-and-menus::d0-cache-guards-bite",
   "name": "d0-cache-guards-bite",
   "note": "d0-cache-guards-bite: suite_cache_selftest must exit zero; and artifacts/navigation-and-menus/suite-cache-selftest.md must exist. Requirements: UI-AGENT-001.",
   "phase": "navigation-and-menus",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite",
     "rascalrally-suite"
    ],
    "resultIds": [
     "anchored_surface::D1 placement solver: the preferred edge, and the gap off the source::places the surface on the preferred edge, gapped off the source",
     "anchored_surface::D1 placement solver: the preferred edge, and the gap off the source::leading and trailing resolve to left and right",
     "anchored_surface::D1 placement solver: the preferred edge, and the gap off the source::refuses an unknown edge, alignment or overflow policy",
     "anchored_surface::D1 placement solver: the flip, and the rule that it never lands somewhere worse::flips to the opposite edge when the preferred one crosses the safe inset",
     "anchored_surface::D1 placement solver: the flip, and the rule that it never lands somewhere worse::never flips into a worse place — no room either way keeps the preferred edge",
     "anchored_surface::D1 placement solver: the flip, and the rule that it never lands somewhere worse::overflow 'clamp' pulls a surface that fits nowhere back inside the safe box",
     "anchored_surface::D1 placement solver: the along-edge shift::shifts along the edge to stay inside the safe box, and reports how far",
     "anchored_surface::D1 placement solver: the along-edge shift::a surface TALLER than the safe box pins to the top inset rather than hanging off the bottom",
     "anchored_surface::D1 placement solver: the along-edge shift::a surface wider than the safe box pins to the leading inset rather than centring off-screen",
     "anchored_surface::D1 placement solver: the arrow tail::the tail centres on the source and rides the shared border",
     "anchored_surface::D1 placement solver: the arrow tail::the tail keeps clear of the surface's own corners",
     "anchored_surface::D1 placement solver: the arrow tail::a shift that carries the tail off the source SUPPRESSES it",
     "anchored_surface::D1 placement solver: the arrow tail::a surface the overflow clamp pulled OFF its edge suppresses the tail too",
     "anchored_surface::D1 placement solver: the arrow tail::a surface too narrow to host the tail inside its corners suppresses it",
     "anchored_surface::D1 anchored surface: presented against a source view's screen rect::presentAnchored places the panel against the source path's screen rect",
     "anchored_surface::D1 anchored surface: presented against a source view's screen rect::an anchored surface joins the presenter's own stack rather than forking one",
     "anchored_surface::D1 anchored surface: presented against a source view's screen rect::a modal anchored surface traps focus through the presenter's own modal path",
     "anchored_surface::D1 anchored surface: presented against a source view's screen rect::a moving source is followed on the existing refresh cadence",
     "anchored_surface::D1 anchored surface: presented against a source view's screen rect::an unknown anchor key is an authoring error, not a silent no-op",
     "anchored_surface::D1 anchored surface: presented against a source view's screen rect::an anchored surface refuses a root policy its window-space offsets cannot mean",
     "anchored_surface::D1 anchored surface: presented against a source view's screen rect::the dump is deterministic and names the resolved edge",
     "anchored_surface::D1 anchored surface: the arrow tail is a decoration, never a claim::the tail paints behind the panel it belongs to",
     "anchored_surface::D1 anchored surface: the arrow tail is a decoration, never a claim::the tail is suppressed when the shift carries it off the source",
     "anchored_surface::D1 anchored surface: the tap-away catcher is the presenter's own::an anchored panel that declares outsideDismiss gets the presenter's catcher, non-consuming",
     "anchored_surface::D1 fixture f1: the avatar-editor callout::f1: anchored under a top-right button, arrow up at it, body shifted left",
     "anchored_surface::D1: the hand-rolled precedents now share one solver::the disclosure plate places through the shared solver and never flips into a worse place",
     "anchored_surface::D1: the hand-rolled precedents now share one solver::row actions' menu flip is the shared solver's rule, not a second copy",
     "text_disclosure::LTN-4 disclosure: the plate is presentation chrome, never a surface::the plate is clamped into the safe viewport",
     "row_actions_input::RowActions Task 8: the action menu — WHERE it lands::...so it FLIPS above the trigger, wholly inside the viewport",
     "row_actions_input::RowActions Task 8: the action menu — WHERE it lands::NEGATIVE CONTROL: with no room ABOVE either, it stays BELOW rather than flipping",
     "row_actions_input::RowActions Task 8: the action menu — WHERE it lands::a standalone row with no `env` cannot flip, and says so by not flipping"
    ],
    "shell": "test -f src/layout/anchor_placement.luau && test -f src/present/anchored.luau && cd ../../../games/RascalRally/code && out=\"$(cat \"$FACET_VERIFY_RR_TRANSCRIPT\")\" && echo \"$out\" | grep -q \"✓.*the anchored seam is LIVE through this package.s own require path\" && echo \"$out\" | grep -q \"✓.*this package declares NO anchored surface yet — restraint, measured off the source\" && echo \"$out\" | grep -q \"✓.*an anchored panel lands against a REAL game surface.s node, inside the safe area\" && echo \"$out\" | grep -q \"✓.*the DISCLOSURE plate this game raises still lands inside the safe box after the migration\" && echo \"$out\" | grep -q \"✓.*the shared solver answers the row-actions flip exactly as the ruling that shipped it says\""
   },
   "class": "result-ids",
   "evidence": null,
   "id": "navigation-and-menus::d1-anchored-surface",
   "name": "d1-anchored-surface",
   "note": "d1-anchored-surface: 31 named suite cases must pass; suite, rascalrally-suite must exit zero; its file-existence and text assertions must hold; and artifacts/navigation-and-menus/d1-anchored-surface.md must exist. Requirements: UI-LAYOUT-001, UI-INPUT-001.",
   "phase": "navigation-and-menus",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001",
    "UI-INPUT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_registration_cli",
     "check_surface_ledger",
     "check_source_size",
     "suite",
     "rascalrally-suite"
    ],
    "resultIds": [
     "menu::D2 Menu: the spec is closed, and an ambiguous item is a refusal::declaring BOTH children and onSelect is a spec error, never a precedence rule",
     "menu::D2 Menu: the spec is closed, and an ambiguous item is a refusal::an item with neither an action nor children is refused rather than silently inert",
     "menu::D2 Menu: the spec is closed, and an ambiguous item is a refusal::a menu that drops `activate` without both key triggers is unreachable, and refused",
     "menu::D2 Menu: submenus nest without a cap, and say so past one level::a three-level tree BUILDS, and depth >= 2 is diagnosed rather than refused",
     "menu::D2 Menu: submenus nest without a cap, and say so past one level::one level of submenu is silent — the diagnostic is advice, not noise",
     "menu::D2 Menu: submenus nest without a cap, and say so past one level::Guidance: a group of more than about five items is diagnosed as wanting its own menu",
     "menu::D2 Menu: submenus nest without a cap, and say so past one level::Guidance: a destructive item that is not last is diagnosed",
     "menu::D2 Menu: every input class opens it, and the verbs are the same ones::a primary activate on the trigger opens the menu against the trigger's own rect",
     "menu::D2 Menu: every input class opens it, and the verbs are the same ones::a pointer SECONDARY click on the trigger opens the menu",
     "menu::D2 Menu: every input class opens it, and the verbs are the same ones::a touch LONG-PRESS on the trigger opens the menu, through the normalized gesture layer",
     "menu::D2 Menu: every input class opens it, and the verbs are the same ones::a tap gesture is NOT a long press: the menu stays shut",
     "menu::D2 Menu: every input class opens it, and the verbs are the same ones::the keyboard context chord opens the menu, and Return activates the focused item",
     "menu::D2 Menu: every input class opens it, and the verbs are the same ones::the gamepad menu button opens it, and ButtonB closes ONE level",
     "menu::D2 Menu: every input class opens it, and the verbs are the same ones::activating an item runs its onSelect exactly once and closes the menu",
     "menu::D2 Menu: every input class opens it, and the verbs are the same ones::every trigger declared is honoured, and one that was not is inert",
     "menu::D2 Menu: a menu whose every item is disabled still OPENS::a wholly unavailable menu opens rather than swallowing its trigger",
     "menu::D2 Menu: a menu whose every item is disabled still OPENS::selecting a disabled item does nothing and leaves the menu standing",
     "menu::D2 Menu: Cancel closes one level, tap-away closes all::tap-away closes EVERY level, not just the innermost",
     "menu::D2 Menu: Cancel closes one level, tap-away closes all::Cancel unwinds the chain one level at a time, and only then closes",
     "menu::D2 Menu: Cancel closes one level, tap-away closes all::Right enters the focused submenu and Left leaves it, in document order",
     "menu::D2 Menu: Cancel closes one level, tap-away closes all::Right on a plain ACTION row is not the menu's arrow, so the ring keeps it",
     "menu::D2 Menu: touch does not nest — the sheet replaces its contents::touch resolves to `sheet` through the ONE shared rule, never a device name",
     "menu::D2 Menu: touch does not nest — the sheet replaces its contents::under `sheet` a submenu REPLACES the sheet's contents and grows a Back row",
     "menu::D2 Menu: touch does not nest — the sheet replaces its contents::...and the Back row walks the chain back up rather than closing the menu",
     "menu::D2 Menu: touch does not nest — the sheet replaces its contents::under `menu` a submenu STACKS beside its parent, anchored to the row it came from",
     "menu::D2 Menu: touch does not nest — the sheet replaces its contents::a live class flip while a submenu is open CARRIES the open path into the new presentation",
     "menu::D2: one menu row recipe, and one presentation rule::`resolvePresentation` lives in ONE place, and popup_button's name is an alias for it",
     "menu::D2: one menu row recipe, and one presentation rule::all three menus take their row height and gap from the same tokens",
     "menu::D2: one menu row recipe, and one presentation rule::a leaf row is the SHIPPED shape: the label on the button, the icon on compactLabel",
     "menu::D2: one menu row recipe, and one presentation rule::a SUBMENU row carries a trailing chevron, and keeps its label semantic",
     "menu::D2: one menu row recipe, and one presentation rule::`icon` and a divider joined popup_button.Option, and both render from the recipe",
     "menu::D2: one menu row recipe, and one presentation rule::popup_button refuses a half-iconned group through the SAME lint the Menu uses",
     "menu::D2 Menu: the spec is closed, and an ambiguous item is a refusal::Guidance: icons for ALL items in a group, or none — a half-iconned group is refused",
     "menu::D2: long-press ownership, settled in code::the disclosure plate KEEPS long-press outside a menu trigger — the a11y route survives",
     "menu::D2: long-press ownership, settled in code::...and inside a Menu trigger the MENU owns it: one gesture, one construct",
     "menu::D2: long-press ownership, settled in code::exactly two constructs know the word long-press, and `help` is not going to be a third",
     "menu::D2: long-press ownership, settled in code::a Menu declared WITHOUT a touch trigger takes nothing away from the plate",
     "text_disclosure::LTN-4 disclosure: the live engagement wiring (renderer -> adapter zones)::a pointer world wires hover; a pure-touch world wires long-press instead",
     "text_disclosure::fresh-review closures: the plate answers for the label as it IS::a touch class arriving MID-SESSION retrofits the long-press half onto mounted labels",
     "text_disclosure::LTN-4 disclosure: engagement by input class::touch long-press presents the plate immediately",
     "row_actions_input::RowActions Task 8: the action menu (ButtonX)::the same action wears its ICON in the tray and its WORD in the menu row",
     "row_actions_input::RowActions Task 8: the action menu (ButtonX)::menu row labels survive 1.4x pseudo-loc without wrapping or clipping (fill-inside-a-hugging-parent widens instead)",
     "row_actions_input::RowActions Task 8: the action menu (ButtonX)::ButtonX opens the menu, entering focus on its first row; Activate on a non-destructive item runs it exactly once and closes",
     "row_actions_input::RowActions Task 8: the action menu — WHERE it lands::the predicted height the flip DECIDES on is the height the solver PAINTS",
     "anchored_surface::D1: the hand-rolled precedents now share one solver::row actions' menu flip is the shared solver's rule, not a second copy",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Menu-panel: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes",
     "menu::D2 Menu: the dump says what is on screen::the dump carries the schema, the open chain and the whole item tree",
     "menu::D2 Menu: the dump says what is on screen::dispose closes every live surface rather than stranding one"
    ],
    "shell": "test -f src/controls/menu.luau && test -f src/controls/menu_recipe.luau && grep -q \"resolvePresentation\" src/controls/popup_button.luau && ! grep -q \"function popup_button.resolvePresentation\" src/controls/popup_button.luau && grep -q \"setSecondaryActivate\" src/render/target_contract.luau && grep -q \"attachContextGestures\" src/render/renderer.luau && grep -q \"contextTriggerOwns\" src/present/presenter.luau && grep -q \"menuRecipe.row(UI\" src/controls/row_actions.luau && ! grep -q \"compactLabel = if\" src/controls/row_actions.luau && grep -q \"menuRecipe.row(UI\" src/controls/popup_button.luau && cd ../../../games/RascalRally/code && out=\"$(cat \"$FACET_VERIFY_RR_TRANSCRIPT\")\" && echo \"$out\" | grep -q \"✓.*newMenu is LIVE through this package.s own require path, closed spec and all\" && echo \"$out\" | grep -q \"✓.*both new sibling modules are REACHABLE on this package.s require path\" && echo \"$out\" | grep -q \"✓.*the extraction is an EXTRACTION: one presentation rule, shared by identity\" && echo \"$out\" | grep -q \"✓.*newRowActions. menu rows still carry the props this package.s lists were written against\" && echo \"$out\" | grep -q \"✓.*TRIPWIRE: no shipped Rascal Rally source opens a menu or claims a context gesture yet\" && echo \"$out\" | grep -q \"✓.*A11Y: the truncated-label plate KEEPS touch long-press, which is what this package relies on\""
   },
   "class": "result-ids",
   "evidence": null,
   "id": "navigation-and-menus::d2-menu",
   "name": "d2-menu",
   "note": "d2-menu: 48 named suite cases must pass; check_registration_cli, check_surface_ledger, check_source_size, suite, rascalrally-suite must exit zero; its file-existence and text assertions must hold; and artifacts/navigation-and-menus/d2-menu.md must exist. Requirements: UI-INPUT-001, UI-INPUT-003, UI-A11Y-001.",
   "phase": "navigation-and-menus",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-001",
    "UI-INPUT-003",
    "UI-A11Y-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_prop_parity_cli",
     "check_theme_drift_cli",
     "suite",
     "rascalrally-suite"
    ],
    "resultIds": [
     "help::D3a help: the pointer pulls it, after a dwell::pointer hover presents the help plate only after the dwell elapses",
     "help::D3a help: the pointer pulls it, after a dwell::the pointer leaving takes it away",
     "help::D3a help: the pointer pulls it, after a dwell::activating the view takes it away — a player who pressed has stopped asking",
     "help::D3a help: the pointer pulls it, after a dwell::moving to a different helped view retires the first answer and re-earns the dwell",
     "help::D3a help: keyboard and gamepad get it on focus, immediately::focus entering a helped view presents the plate with no dwell",
     "help::D3a help: keyboard and gamepad get it on focus, immediately::focus moving off takes it away",
     "help::D3a help: NOTHING on touch, and that is the specification::a pure-touch world wires NO engagement zone for help at all",
     "help::D3a help: NOTHING on touch, and that is the specification::HELP BINDS NO GESTURE: with both classes live it wires hover and never a press",
     "help::D3a help: NOTHING on touch, and that is the specification::a touch tap moves focus but presents nothing — focusVisible is the gate",
     "help::D3a help: the plate is chrome, and the value outranks it::a truncated DISCLOSED label outranks help on the same engagement",
     "help::D3a help: the plate is chrome, and the value outranks it::the plate takes no focus and adds no focus stop",
     "help::D3a help: the plate is chrome, and the value outranks it::dismissing the surface takes its help with it",
     "help::D3a help: the plate is chrome, and the value outranks it::a help string wraps rather than clips at 1.4x expansion",
     "help::D3a help: the plate is chrome, and the value outranks it::the dump carries the schema, the source and every live declaration",
     "help::D3a help: the plate is chrome, and the value outranks it::help is a CONSTRUCTION-ONLY string: a Readable is refused by name",
     "help::D3a help: the reachability check, and why it is not clippedEssential::a help string that appears nowhere else is reported: a touch player cannot read it",
     "help::D3a help: the reachability check, and why it is not clippedEssential::...and one the screen also says out loud is not",
     "help::D3a help: the reachability check, and why it is not clippedEssential::HIDDEN text is not a route: a losing ViewThatFits candidate says nothing",
     "help::D3a help: the reachability check, and why it is not clippedEssential::...and neither is TRUNCATED text — an ellipsis is not the sentence",
     "help::D3a help: the reachability check, and why it is not clippedEssential::a `convenience` waiver silences the duplication finding",
     "help::D3a help: the reachability check, and why it is not clippedEssential::BUT NO WAIVER REACHES THE GESTURE: help nothing engages is reported anyway",
     "help::D3a help: the reachability check, and why it is not clippedEssential::a pointer-only world still has a route; a pure-touch one has none",
     "help::D3a help: the reachability check, and why it is not clippedEssential::the presenter reports the LIVE declarations, so the check runs on a real mount",
     "help::D3a help: the reachability check, and why it is not clippedEssential::...and in a pure-touch world the SAME mount reports no route, which is the finding",
     "help::D3a help: the reachability check, and why it is not clippedEssential::NO NEW DEVICE BRANCH: the new modules name no platform",
     "menu::D2: long-press ownership, settled in code::exactly two constructs know the word long-press, and `help` is not going to be a third",
     "menu::D2: long-press ownership, settled in code::the disclosure plate KEEPS long-press outside a menu trigger — the a11y route survives",
     "text_disclosure::LTN-4 disclosure: the live engagement wiring (renderer -> adapter zones)::a pointer world wires hover; a pure-touch world wires long-press instead",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Callout-plate: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes"
    ],
    "shell": "test -f src/present/help_plate.luau && grep -q \"help = HELP\" src/blueprint_schema.luau && ! grep -q \"longPress\" src/present/help_plate.luau && ! grep -q \"longPress\" src/controls/callout.luau && ! grep -q \"longPress\" src/present/callout_queue.luau && grep -q \"chrome = true\" src/present/help_plate.luau && grep -q \"function text_audit.helpRoutes\" src/layout/text_audit.luau && ! grep -qiE \"android|xbox|playstation|nintendo|switch_|windows\" src/present/help_plate.luau && ! python3 -c \"import sys, importlib.util; s = importlib.util.spec_from_file_location('g', 'tools/check_brand_drift.py'); m = importlib.util.module_from_spec(s); s.loader.exec_module(m); sys.exit(0 if any(m.VENDOR.search(l) and 'artifacts/' not in l for l in open('src/present/help_plate.luau')) else 1)\" && cd ../../../games/RascalRally/code && out=\"$(cat \"$FACET_VERIFY_RR_TRANSCRIPT\")\" && echo \"$out\" | grep -q \"✓.*A11Y: help BINDS NO GESTURE, and the truncated-label plate keeps its long press\" && echo \"$out\" | grep -q \"✓.*THE FOCUS-GRAPH CHANGE, both directions: a plain surface keeps the ring, a MODAL takes it\" && echo \"$out\" | grep -q \"✓.*...and the reachability check is available to this package the day it does\""
   },
   "class": "result-ids",
   "evidence": null,
   "id": "navigation-and-menus::d3a-help",
   "name": "d3a-help",
   "note": "d3a-help: 29 named suite cases must pass; check_prop_parity_cli, check_theme_drift_cli, suite, rascalrally-suite must exit zero; its file-existence and text assertions must hold; and artifacts/navigation-and-menus/d3a-help.md must exist. Requirements: UI-INPUT-001, UI-A11Y-001.",
   "phase": "navigation-and-menus",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-001",
    "UI-A11Y-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_registration_cli",
     "check_surface_ledger",
     "check_docs_cli",
     "check_source_size",
     "suite",
     "rascalrally-suite"
    ],
    "resultIds": [
     "callout::D3b Callout: eligibility is the app's rule, and the caller's state::a callout nobody has seen is eligible, presents, and reports itself showing",
     "callout::D3b Callout: eligibility is the app's rule, and the caller's state::`seen` is the caller's record, and a seen callout is never eligible",
     "callout::D3b Callout: eligibility is the app's rule, and the caller's state::`afterSessions` holds it back until the session count reaches it",
     "callout::D3b Callout: eligibility is the app's rule, and the caller's state::a feature already used is never coached about",
     "callout::D3b Callout: invalidation, and the persistence boundary::using the feature while it is showing takes it down and retires it FOREVER",
     "callout::D3b Callout: invalidation, and the persistence boundary::a player dismissal retires it once, with a reason, and never twice",
     "callout::D3b Callout: invalidation, and the persistence boundary::THE FRAMEWORK WRITES NO PLAYER STATE: `seen` is untouched, the report is the whole contract",
     "callout::D3b Callout: invalidation, and the persistence boundary::a callout with no way to report a retirement is an authoring error",
     "callout::D3b Callout: invalidation, and the persistence boundary::the spec is CLOSED: an invented key is refused rather than ignored",
     "callout::D3b Callout: at most one on screen — a queue, not a pile::a second callout WAITS rather than stacking beside the first",
     "callout::D3b Callout: at most one on screen — a queue, not a pile::...and takes its turn the moment the first retires",
     "callout::D3b Callout: at most one on screen — a queue, not a pile::priority orders the queue without ever showing two",
     "callout::D3b Callout: it never blocks::presenting one does not move the focus ring",
     "callout::D3b Callout: it never blocks::THE CONTROL IT POINTS AT STAYS OPERABLE: a tap on it still activates it",
     "callout::D3b Callout: it never blocks::a tap in empty space retires it through the presenter's own non-consuming catcher",
     "callout::D3b Callout: it never blocks::it is NOT a modal: the screen underneath keeps its own focus scope",
     "callout::D3b Callout: every input class can retire it::pointer: a click on the dismiss affordance retires it",
     "callout::D3b Callout: every input class can retire it::touch: a tap on the dismiss affordance retires it, and it meets the 44px floor",
     "callout::D3b Callout: every input class can retire it::keyboard: Return on the focused dismiss affordance retires it",
     "callout::D3b Callout: every input class can retire it::gamepad: ButtonA on the focused dismiss affordance retires it",
     "callout::D3b Callout: every input class can retire it::...and one arrow press reaches it, so a pad player is never stuck with a plate",
     "callout::D3b Callout: the f1 fixture::f1: a plate under a top-right +, arrow up at it, body shifted left",
     "callout::D3b Callout: the f1 fixture::the content is a BLUEPRINT, not a string: the plate carries the tree it was given",
     "callout::D3b Callout: the f1 fixture::player copy survives ~1.4x pseudo-localization by wrapping, never clipping",
     "callout::D3b Callout: the dump, and the teardown::the dump carries the schema, the state and the eligibility it resolved",
     "callout::D3b Callout: the dump, and the teardown::the queue's own dump names what is on screen and what is waiting",
     "callout::D3b Callout: the dump, and the teardown::dispose takes the plate down and gives the queue slot back",
     "callout::D3b Callout: the dump, and the teardown::dismissing the surface underneath takes the callout with it",
     "callout::D3b Callout: the dump, and the teardown::NO DEVICE BRANCH: the construct names no platform",
     "callout::D3b Callout: the dump, and the teardown::the standing warning ships with the documentation",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'callout': the solver reports NOTHING at any swept viewport, at any text size",
     "gallery_demo_picker::showcase host: advancing the picker actually mounts the demo it names::one showNext per demo leaves EXACTLY that demo's own screen in the tree, all 34"
    ],
    "shell": "test -f src/controls/callout.luau && test -f src/present/callout_queue.luau && test -f examples/gallery/scenarios/callout.luau && grep -q \"newCallout\" src/init.luau && grep -q \"maxVisible = 1,\" src/present/callout_queue.luau && grep -q \"toast_schedule\" src/present/callout_queue.luau && grep -qF \"Use tips sparingly\" docs/reference/api.md && grep -q \"function callout.plate\" src/controls/callout.luau && ! grep -qiE \"android|xbox|playstation|nintendo|switch_|windows\" src/controls/callout.luau && ! python3 -c \"import sys, importlib.util; s = importlib.util.spec_from_file_location('g', 'tools/check_brand_drift.py'); m = importlib.util.module_from_spec(s); s.loader.exec_module(m); sys.exit(0 if any(m.VENDOR.search(l) and 'artifacts/' not in l for l in open('src/controls/callout.luau')) else 1)\" && cd ../../../games/RascalRally/code && out=\"$(cat \"$FACET_VERIFY_RR_TRANSCRIPT\")\" && echo \"$out\" | grep -q \"✓.*newCallout and the .help. prop are LIVE in the framework this package requires\" && echo \"$out\" | grep -q \"✓.*both new sibling modules are REACHABLE on this package.s require path\" && echo \"$out\" | grep -q \"✓.*PERSISTENCE IS THIS PACKAGE.S: the framework reads player state and writes none of it\" && echo \"$out\" | grep -q \"✓.*TRIPWIRE: no shipped Rascal Rally source builds a callout, and every .help =. is the CARD FAMILY\""
   },
   "class": "result-ids",
   "evidence": null,
   "id": "navigation-and-menus::d3b-callout",
   "name": "d3b-callout",
   "note": "d3b-callout: 32 named suite cases must pass; check_registration_cli, check_surface_ledger, check_docs_cli, check_source_size, suite, rascalrally-suite must exit zero; its file-existence and text assertions must hold; and artifacts/navigation-and-menus/d3b-callout.md must exist. Requirements: UI-LAYOUT-001, UI-A11Y-001, UI-INPUT-001.",
   "phase": "navigation-and-menus",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001",
    "UI-A11Y-001",
    "UI-INPUT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite",
     "check_registration_cli",
     "rascalrally-suite"
    ],
    "resultIds": [
     "selection_indicator::D4 geometry: one arithmetic, two skins, both axes::pill: the chip is the segment box inset on all four sides, in the layer's space",
     "selection_indicator::D4 geometry: one arithmetic, two skins, both axes::underline y: a VERTICAL strip underlines the leading edge and spans the height",
     "selection_indicator::D4 geometry: one arithmetic, two skins, both axes::the transition is between RECTS, not indices: unequal segments give unequal bars",
     "selection_indicator::D4 motion policy: place or slide, decided before anything moves::first paint PLACES: the indicator never slides in from nowhere",
     "selection_indicator::D4 motion policy: place or slide, decided before anything moves::a re-solve under an unchanged selection PLACES, so a rotation cannot make the bar swim",
     "selection_indicator::D4 mounted: the bar tracks the SOLVED rect of the selected child::mounts one bar behind the strip and lands on the initial selection with no travel",
     "selection_indicator::D4 mounted: the bar tracks the SOLVED rect of the selected child::a selection change slides the bar between the two solved rects",
     "selection_indicator::D4 mounted: the bar tracks the SOLVED rect of the selected child::REDUCED MOTION SNAPS: one rect write, then twenty ticks of silence",
     "selection_indicator::D4 mounted: the bar tracks the SOLVED rect of the selected child::full motion spends MANY frames on the same change, which is what reduced motion removes",
     "selection_indicator::D4 mounted: the bar tracks the SOLVED rect of the selected child::the indicator is never a focus stop: wrapping a strip leaves its document order identical",
     "selection_indicator::D4 mounted: the bar tracks the SOLVED rect of the selected child::repeated geometry feeds with UNCHANGED rects cost nothing (the by-value guard)",
     "selection_indicator::D4 mounted: the bar tracks the SOLVED rect of the selected child::...and the comparator refuses a nested field rather than half-comparing it",
     "selection_indicator::D4 mounted: the bar tracks the SOLVED rect of the selected child::a geometry change that does not move the bar cannot interrupt a flight in progress",
     "selection_indicator::D4 mounted: the bar tracks the SOLVED rect of the selected child::a vertical pill moves along y, and the same mechanism draws it",
     "selection_indicator::D4 mounted: the bar tracks the SOLVED rect of the selected child::a wider label widens the bar with it (localization-safe by construction)",
     "selection_indicator::D4 authoring: the spec is closed and the ids are checked::with NO presenter (a bare mount) it still places the bar, instantly",
     "selection_indicator::D4 through the public property: newPicker's `indicator` and `axis`::indicator = \"none\" is byte-identical to the pre-D4 control",
     "selection_indicator::D4 through the public property: newPicker's `indicator` and `axis`::indicator = \"pill\" slides a chip between two unequal segments",
     "selection_indicator::D4 through the public property: newPicker's `indicator` and `axis`::turning it on makes the INDICATOR the selection paint, not a second one",
     "selection_indicator::D4 through the public property: newPicker's `indicator` and `axis`::axis = \"y\" is a real VERTICAL PILL, and it does not overload `inline`",
     "selection_indicator::D4 through the public property: newPicker's `indicator` and `axis`::the picker's own dump carries the indicator's state — there is no second schema",
     "selection_indicator::D4 through the public property: newPicker's `indicator` and `axis`::wrapping does not collapse a `fill` strip — the options keep their width",
     "selection_indicator::D4 through the public property: newPicker's `indicator` and `axis`::selection still works at the deeper path: a tap and a Return both choose",
     "selection_indicator::D4 through the public property: newPicker's `indicator` and `axis`::an indicated picker is still exactly two focus stops, in order",
     "selection_indicator::D4 through the public property: newPicker's `indicator` and `axis`::REDUCED MOTION snaps the picker's chip too: one write, then silence",
     "selection_indicator::D4 through the public property: newPicker's `indicator` and `axis`::an indicated picker is registry-neutral: build, mount, switch, dispose",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Picker-indicator: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes"
    ],
    "shell": "grep -qF \"CONTRIBUTION_INTAKE_ONLY\" tools/lune/check_registration.luau && ! grep -q \"newSelectionIndicator\" src/init.luau && cd ../../../games/RascalRally/code && rr=\"$(cat \"$FACET_VERIFY_RR_TRANSCRIPT\")\" && echo \"$rr\" | grep -q \"✓.*TRIPWIRE: no shipped Rascal Rally source builds a picker yet\" && echo \"$rr\" | grep -q \"✓.*POSITIVE CONTROL: in THIS build the chip lands on the selected option and then flies\" && echo \"$rr\" | grep -q \"✓.*and REDUCED MOTION snaps it, counted in frames rather than read off the flag\""
   },
   "class": "result-ids",
   "evidence": null,
   "id": "navigation-and-menus::d4-sliding-indicator",
   "name": "d4-sliding-indicator",
   "note": "d4-sliding-indicator: 27 named suite cases must pass; suite, check_registration_cli, rascalrally-suite must exit zero; its file-existence and text assertions must hold; and artifacts/navigation-and-menus/d4-sliding-indicator.md must exist. Requirements: UI-LAYOUT-001, UI-A11Y-001.",
   "phase": "navigation-and-menus",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001",
    "UI-A11Y-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite",
     "rascalrally-suite"
    ],
    "resultIds": [
     "tab_view::D5: the strip IS a segmented picker, not a second option row::mounts newPicker's own option row, with the D4 indicator on it",
     "tab_view::D5: the strip IS a segmented picker, not a second option row::a badge rides an UNSELECTED tab and survives every switch — the strip is never evicted",
     "tab_view::D5: placement comes from the policy, and the strip is RE-HOMED not rebuilt::the four homes come from shape and input facts — no device name decides one",
     "tab_view::D5: placement comes from the policy, and the strip is RE-HOMED not rebuilt::a rotation RE-HOMES one strip: the picker object survives and the bar follows it",
     "tab_view::D5: placement comes from the policy, and the strip is RE-HOMED not rebuilt::a DECLARED placement never consults the policy at all",
     "tab_view::D5: placement comes from the policy, and the strip is RE-HOMED not rebuilt::the thumb-zone band FILLS and every other home HUGS — that is what lets a strip scroll",
     "tab_view::D5: placement comes from the policy, and the strip is RE-HOMED not rebuilt::an automatic placement with nothing to ask is an authoring error, not a guess",
     "tab_view::D5: NESTING — an inner TabView never claims the app-level placement::on one phone viewport the app takes the bottom bar and the page takes the top",
     "tab_view::D5: NESTING — an inner TabView never claims the app-level placement::...and it stays the top bar when the POLICY would have said sidebar",
     "tab_view::D5: NESTING — an inner TabView never claims the app-level placement::neither level claims a focus scope, and the two strips read in document order",
     "tab_view::D5: NESTING — an inner TabView never claims the app-level placement::neither level touches the presenter's back() stack",
     "tab_view::D5: NESTING — an inner TabView never claims the app-level placement::two selections are two Signals: moving one never moves the other",
     "tab_view::D5: NESTING — an inner TabView never claims the app-level placement::the inner TabView is CONTENT, so it is evicted with its host tab and re-nests on return",
     "tab_view::D5: NESTING — an inner TabView never claims the app-level placement::a content factory that THROWS still leaves the depth balanced",
     "tab_view::D5: LAZY content, EVICTED state — a real disposal, never a hide::only the selected tab's subtree exists at all",
     "tab_view::D5: LAZY content, EVICTED state — a real disposal, never a hide::switching TEARS THE PREVIOUS SUBTREE DOWN — the node is gone, not hidden",
     "tab_view::D5: LAZY content, EVICTED state — a real disposal, never a hide::MEMORY NEUTRALITY: twenty switches return every counter to its baseline",
     "tab_view::D5: LAZY content, EVICTED state — a real disposal, never a hide::eviction disposes what the CALLER owned on the tab scope",
     "tab_view::D5: LAZY content, EVICTED state — a real disposal, never a hide::returning to a tab REPLAYS its entry cost — the accepted cost, stated as a test",
     "tab_view::D5: LAZY content, EVICTED state — a real disposal, never a hide::...and state held OUTSIDE the TabView survives it — the documented escape hatch",
     "tab_view::D5: LAZY content, EVICTED state — a real disposal, never a hide::the content region STRETCHES its tab to the offer rather than letting it overflow",
     "tab_view::D5: an OVERFLOWING strip scrolls the selection into view::the strip really is WIDER than its band — otherwise there is nothing to test",
     "tab_view::D5: an OVERFLOWING strip scrolls the selection into view::selecting an off-screen tab scrolls it into view",
     "tab_view::D5: an OVERFLOWING strip scrolls the selection into view::a selection that is ALREADY visible issues no scroll at all",
     "tab_view::D5: an OVERFLOWING strip scrolls the selection into view::a FILLING thumb-zone band has nothing to scroll, and that is correct",
     "tab_view::D5: four inputs, one story::TabView pointer: a tap activates through the presenter (device-true)",
     "tab_view::D5: four inputs, one story::TabView touch: a touch tap/pan drives the control (device-true)",
     "tab_view::D5: four inputs, one story::TabView keyboard: focus + Return drives the control",
     "tab_view::D5: four inputs, one story::TabView gamepad: focus + ButtonA drives the control",
     "tab_view::D5: four inputs, one story::TabView touch affordance: the touch structural idiom (44px/naked-pan-scroll/edit-grip)",
     "tab_view::D5: four inputs, one story::TabView gamepad affordance: the gamepad structural idiom (focus/grab/A-B/ten-foot)",
     "tab_view::D5: four inputs, one story::the shoulders are UNBOUND while focus sits off the strip — the auto-wiring hazard",
     "tab_view::D5: four inputs, one story::a bar strip is ONE horizontal group, so a pad crosses it instead of falling out",
     "tab_view::D5: the closed spec, the dump, and disposal::hot-switch: nothing to carry or cancel — selection is atomic",
     "picker_segments::D5 seam: `sizing` is what makes a strip able to OVERFLOW::...and the segments stop being equal-width, so a long label really is longer",
     "picker_segments::D5 seam: `sizing` is what makes a strip able to OVERFLOW::`sizing` is REACTIVE, because a tab bar's home changes under a live rotation",
     "picker_segments::D5 seam: `axis` is REACTIVE, because a placement flip is a re-solve::a live axis flip turns the band into a rail without rebuilding an option",
     "picker_segments::D5 seam: a badge is a count that lives on an UNSELECTED option::the badge is on the UNSELECTED option and stays live — that is the point of a badge",
     "picker_segments::D5 seam: a badge is a count that lives on an UNSELECTED option::the seal is a DECORATION: no focus stop, no second activation site",
     "picker_segments::D5 seam: a badge is a count that lives on an UNSELECTED option::the plate does NOT eat the tap: a badged segment still selects",
     "picker_segments::D5 seam: a badge is a count that lives on an UNSELECTED option::a picker with NO badges mounts no badge node at all — the paths are unchanged",
     "picker_segments::D5 seam: an option LABEL may be a Readable, and `api.segments` names the row::a bound label satisfies the non-empty lint and is what dump() announces",
     "picker_segments::D5 seam: an option LABEL may be a Readable, and `api.segments` names the row::`api.segments` publishes the id this strip gave each option",
     "picker_segments::D5 found: a segmented strip navigates ACROSS on a pad, not down::the option stack is one HORIZONTAL group, so Right moves to the next segment",
     "picker_segments::D5 found: a segmented strip navigates ACROSS on a pad, not down::...and the VERTICAL rail stays a vertical group, because its axis says so",
     "selection_indicator::D5 found: the paths are RE-RESOLVED when the strip is re-homed::re-homing the strip re-resolves the layer and every segment, and the bar follows",
     "selection_indicator::D5 found: the paths are RE-RESOLVED when the strip is re-homed::...and a selection change in the NEW home still moves the bar",
     "reference/foyer_spec::Foyer nav — the four placement homes (adaptive.navPlacement)::no bottom wing is ever cut off: the ladder degrades the WORD to the GLYPH",
     "reference/foyer_spec::Foyer nav — the four placement homes (adaptive.navPlacement)::no top-band wing is cut off, and the glyph rung is REACHED at the expansion locale",
     "reference/foyer_spec::Foyer nav — the four placement homes (adaptive.navPlacement)::the search field keeps its home in the top chrome on EVERY placement",
     "reference/foyer_spec::Foyer nav — the four placement homes (adaptive.navPlacement)::navPlacement gives every canvas its home: rail / bottom band / inline band / top band",
     "reference/foyer_spec::Foyer nav — the four placement homes (adaptive.navPlacement)::every placement is traversable: enter, through, away, and a real action",
     "reference/foyer_spec::Foyer nav — the four placement homes (adaptive.navPlacement)::top band, driven as a PAD drives it: DPad to the band, across it, ButtonA, DPad away",
     "reference/foyer_spec::Foyer proof — tabs, search, stubs::a stub destination shows the labeled stub plate, never fake content",
     "reference/glade_spec::Glade: the scenario surface::the five-view sweep is diagnostic-clean with a live scrollbar (matrix pin)",
     "reference/cartwheel_spec::Cartwheel — reduced motion::the five-view sweep is diagnostic-clean with a live scrollbar (matrix pin)",
     "reference/sipworks_spec::Sipworks — reduced motion, themes, and reachability::the five-view sweep is diagnostic-clean with a live scrollbar (matrix pin)",
     "reference/foyer_spec::Foyer proof — notices, locale, images, determinism::the five-view sweep is diagnostic-clean with a live scrollbar (matrix pin)",
     "overflow_sweep::overflow sweep: reference proofs::proof 'p4_foyer': the solver reports NOTHING at any swept viewport, at any text size",
     "overflow_sweep::overflow sweep: the theme ledger::every ledger row still fires — a stale row is a fixed defect, delete the line",
     "large_text_matrix::LT-8 control matrix: the sweep's own coverage and mirrors::LT8-COVER every controls_registry row is swept or carries a declared reason"
    ],
    "shell": "test -f src/controls/tab_view.luau && grep -q \"THE APP-LEVEL PLACEMENT BELONGS TO THE OUTERMOST TABVIEW\" src/controls/tab_view.luau && grep -qF \"local tabView = require(\" src/init.luau && grep -qF \"@self/controls/tab_view\" src/init.luau && grep -qF \"newTabView = tabView.build\" src/init.luau && grep -qF \"TabView = function(core: any, spec: tabView.Spec)\" src/init.luau && grep -q \"tab_view.spec\" tests/run.luau && grep -q \"adaptive.conditions\" src/controls/tab_view.luau && ! grep -q \"preferredInput\" src/controls/tab_view.luau && ! grep -q \"displaySize\" src/controls/tab_view.luau && grep -q \"picker.build\" src/controls/tab_view.luau && ! grep -q \"UI.Button\" src/controls/tab_view.luau && ! grep -q \"transientScope = \" src/controls/tab_view.luau && ! grep -q \"handleCancel\" src/controls/tab_view.luau && ! grep -q \"outsideDismiss\" src/controls/tab_view.luau && grep -q \"Facet.newTabView\" examples/reference/p4_foyer/init.luau && ! grep -q \"navItems(\" examples/reference/p4_foyer/init.luau && ! grep -q \"NavTopWhen\" examples/reference/p4_foyer/init.luau && ! grep -q \"BottomFullWhen\" examples/reference/p4_foyer/init.luau && grep -q \"^### .newTabView.\" docs/reference/api.md && grep -qF \"newTabView\" docs/reference/api.md && test -f tests/tab_view.spec.luau && grep -q \"facet_tab_view_contract.spec\" ../../../games/RascalRally/code/tests/run.luau && cd ../../../games/RascalRally/code && rr=\"$(cat \"$FACET_VERIFY_RR_TRANSCRIPT\")\" && echo \"$rr\" | grep -q \"✓.*it exists, takes the documented shape, and refuses an unknown key\" && echo \"$rr\" | grep -q \"✓.*two levels nest: the inner strip never claims the app-level placement\" && echo \"$rr\" | grep -q \"✓.*switching a tab is a REAL disposal, counted on this package.s core\" && echo \"$rr\" | grep -q \"✓.*this game mounts no .newTabView. — the tripwire that says a caller edit is owed\" && echo \"$rr\" | grep -q \"✓.*the shipped sponsor table.s .Split. contributes no group, on the axis that flips\" && echo \"$rr\" | grep -q \"✓.*the BOUND itself: a Split-shaped stack is not a run, however its axis reads\" && echo \"$rr\" | grep -q \"✓.*POSITIVE CONTROL: a real x-axis run DOES become one horizontal group\""
   },
   "class": "result-ids",
   "evidence": null,
   "id": "navigation-and-menus::d5-tabview",
   "name": "d5-tabview",
   "note": "d5-tabview: 61 named suite cases must pass; suite, rascalrally-suite must exit zero; its file-existence and text assertions must hold; and artifacts/navigation-and-menus/d5-tabview.md must exist. Requirements: UI-LAYOUT-001, UI-INPUT-001, UI-A11Y-001.",
   "phase": "navigation-and-menus",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001",
    "UI-INPUT-001",
    "UI-A11Y-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite",
     "check_docs_cli",
     "check_theme_drift_cli",
     "check_source_size",
     "rascalrally-suite"
    ],
    "resultIds": [
     "picker_segments::D6: the segmented presentation ADOPTS the sliding indicator::a segmented picker defaults to the pill — the chip is the shipped paint now",
     "picker_segments::D6: the segmented presentation ADOPTS the sliding indicator::...and so does an ADAPTIVE picker that resolves segmented",
     "picker_segments::D6: the segmented presentation ADOPTS the sliding indicator::the default drops the `selected` style tag, because a tag AND a chip say it twice",
     "picker_segments::D6: the segmented presentation ADOPTS the sliding indicator::`indicator = \"none\"` is the documented escape hatch back to the static tag",
     "picker_segments::D6: the segmented presentation ADOPTS the sliding indicator::a DECLARED inline picker keeps its current painting",
     "picker_segments::D6: the segmented presentation ADOPTS the sliding indicator::an explicit indicator still applies to `inline` — the DEFAULT changed, not the property",
     "picker_segments::D6: the segmented presentation ADOPTS the sliding indicator::the chip still SLIDES from the old rect to the new one under the default",
     "picker_segments::D6: the segmented presentation ADOPTS the sliding indicator::`automatic` is a legal spelling and the refusal message names the whole set",
     "picker_segments::D6: `icon` on Option — icon-only and icon+label::an icon rides `compactLabel`, so the glyph is the DEGRADE and the word survives",
     "picker_segments::D6: `icon` on Option — icon-only and icon+label::...and the ladder really RUNS: too narrow for the word, the segment wears the glyph",
     "picker_segments::D6: `icon` on Option — icon-only and icon+label::`iconOnly` INVERTS it: the glyph is what every segment wears at every width",
     "picker_segments::D6: `icon` on Option — icon-only and icon+label::an icon-only segment STILL CARRIES A NAME, in the dump and in the option",
     "picker_segments::D6: `icon` on Option — icon-only and icon+label::a nameless option is an AUTHORING ERROR — an icon with no name announces nothing",
     "picker_segments::D6: `icon` on Option — icon-only and icon+label::Guidance: icons for ALL options in a group, or none — a half-iconned group is refused",
     "picker_segments::D6: `icon` on Option — icon-only and icon+label::`iconOnly` with nothing to draw is refused rather than silently ignored",
     "picker_segments::D6: `icon` on Option — icon-only and icon+label::an icon+label segment survives 1.4x pseudo-localization without clipping",
     "picker_segments::D6: the VERTICAL pill is a rail, not the inline row list::`axis = \"y\"` on a segmented picker is a CONTENT-SIZED rail",
     "picker_segments::D6: the VERTICAL pill is a rail, not the inline row list::every segment of the rail still meets the 44px floor on BOTH axes",
     "picker_segments::D6: the VERTICAL pill is a rail, not the inline row list::the rail stacks along y and its segments share one width",
     "picker_segments::D6: the VERTICAL pill is a rail, not the inline row list::`dump().verticalPill` is what tells the pill and the row list apart",
     "picker_segments::D6: the VERTICAL pill is a rail, not the inline row list::the chip travels down the rail, not across it",
     "picker_segments::D6: the VERTICAL pill is a rail, not the inline row list::wrapping does not collapse the rail: the indicated rail matches the plain one",
     "picker_segments::D6: the VERTICAL pill is a rail, not the inline row list::flipping OUT of segmented gives the offer back: the rail becomes a column",
     "picker_segments::D6: the VERTICAL pill is a rail, not the inline row list::a rail's options are still reachable by pointer, keyboard and gamepad",
     "picker_segments::D6: the dump keeps schema /1 and stays deterministic::new fields, same schema string — every pre-D6 field keeps its name and meaning",
     "picker_segments::D6: the dump keeps schema /1 and stays deterministic::dispose is registry-neutral with icons, a rail and an indicator all on",
     "display_controls::B-DSP2: Picker presentation adapts from space, not device names::segmented lays options along x; inline stacks them along y",
     "display_controls::B-DSP2: Picker presentation adapts from space, not device names::selection rides the `selected` style binding, never a bespoke fill",
     "selection_indicator::D4 through the public property: newPicker's `indicator` and `axis`::indicator = \"none\" is byte-identical to the pre-D4 control",
     "large_text_matrix::LT-8 control matrix: every registry row at four preferences x two themes::LT8-CTL Picker-indicator: no sibling overlap, no undisclosed clip beyond its declared allowlist, and no NEW sub-floor control at 0/4/10/14 px x both reference themes"
    ],
    "shell": "grep -qF \"all options in a group, or none of them\" src/controls/picker.luau && grep -qF \"D5 COMPOSES THIS STRIP\" src/controls/picker.luau && grep -qF \"targetSizes.minimum\" src/controls/picker.luau && grep -qF \"A segmented picker used as a tab bar IS a\" docs/reference/api.md && grep -qF \"The vertical pill is not the inline row list\" docs/reference/api.md && ! grep -qiE \"android|xbox|playstation|nintendo|switch_|windows\" src/controls/picker.luau && ! python3 -c \"import sys, importlib.util; s = importlib.util.spec_from_file_location('g', 'tools/check_brand_drift.py'); m = importlib.util.module_from_spec(s); s.loader.exec_module(m); sys.exit(0 if any(m.VENDOR.search(l) and 'artifacts/' not in l for l in open('src/controls/picker.luau')) else 1)\" && grep -qF \"flat-theme byte-compatibility claim\" tools/lune/_theme_baseline.luau && cd ../../../games/RascalRally/code && rr=\"$(cat \"$FACET_VERIFY_RR_TRANSCRIPT\")\" && echo \"$rr\" | grep -q \"✓.*the SEGMENTED default is the sliding pill in the framework this package requires\" && echo \"$rr\" | grep -q \"✓.*a DECLARED inline picker still paints the way it always did\" && echo \"$rr\" | grep -q \"✓.*an ICON-ONLY segment draws the glyph and keeps its name for the readout\" && echo \"$rr\" | grep -q \"✓.*without .iconOnly. the word survives and the glyph is only the degrade\" && echo \"$rr\" | grep -q \"✓.*the option lint REFUSES a half-iconned group and a nameless option in this build\" && echo \"$rr\" | grep -q \"✓.*the VERTICAL rail hugs its segments; the inline column takes the whole width\" && echo \"$rr\" | grep -q \"✓.*the spec is still CLOSED: .iconOnly. is known, an invented key is not\" && echo \"$rr\" | grep -q \"✓.*TRIPWIRE: no shipped Rascal Rally source builds a picker yet\""
   },
   "class": "result-ids",
   "evidence": null,
   "id": "navigation-and-menus::d6-segmented",
   "name": "d6-segmented",
   "note": "d6-segmented: 30 named suite cases must pass; suite, check_docs_cli, check_theme_drift_cli, check_source_size, rascalrally-suite must exit zero; its file-existence and text assertions must hold; and artifacts/navigation-and-menus/d6-segmented-picker.md must exist. Requirements: UI-LAYOUT-001, UI-A11Y-001.",
   "phase": "navigation-and-menus",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001",
    "UI-A11Y-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "elision_recovery::D7.1 — a region states where the content a reduced form stops showing went::REQUIRES `recover` on a region with more than one form, and names it",
     "elision_recovery::D7.1 — a region states where the content a reduced form stops showing went::REFUSES `recover` on a one-form region: there is nothing there to choose",
     "elision_recovery::D7.1 — a region states where the content a reduced form stops showing went::refuses an unknown route — a closed spec, never a silent no-op",
     "elision_recovery::D7.1 — a region states where the content a reduced form stops showing went::refuses \"none\" together with mayDrop — dropping shows NOTHING",
     "elision_recovery::D7.1 — `elided` beside `dropped`, and the list an overflow surface reads::a stepped-down region reports `elided`, and `dropped` stays false",
     "elision_recovery::D7.1 — `elided` beside `dropped`, and the list an overflow surface reads::a DROPPED region is `elided = false`: it is showing nothing, not showing less",
     "elision_recovery::D7.1 — `elided` beside `dropped`, and the list an overflow surface reads::the unshown list OMITS a `recover = \"none\"` region and KEEPS the other two",
     "elision_recovery::D7.1 — `elided` beside `dropped`, and the list an overflow surface reads::a dropped region's route is ALWAYS overflow, whatever its `recover` said",
     "elision_recovery::D7.1 — `elided` beside `dropped`, and the list an overflow surface reads::the list is in DECLARATION order, so it reads with `regions` and never hash order",
     "elision_recovery::D7.2 — a `self` route with no route left in it is still an authoring error::refuses a LAST form with nothing focusable in it — under expand = \"none\"",
     "elision_recovery::D7.2 — a `self` route with no route left in it is still an authoring error::...and a MIDDLE form too — the ladder can stop at any rung",
     "elision_recovery::D7.6 — every elided or dropped region has a live recovery route, at every viewport::sweeps the device matrix in both URL-bar states",
     "elision_recovery::D7.6 — every elided or dropped region has a live recovery route, at every viewport::the sink's host cannot elide and cannot drop, by construction",
     "elision_recovery::D7 — the HUD showcase discloses what it stops showing::the sink OPENS and lists the content the screen is not showing",
     "elision_recovery::D7 — the HUD showcase discloses what it stops showing::the sink's own copy survives a 1.4x pseudo-localization with zero findings",
     "elision_recovery::D7 — the HUD showcase discloses what it stops showing::the drawn bar is the height it steals, and its field is the theme's, not a slab",
     "elision_recovery::D7 — the HUD showcase discloses what it stops showing::the fps readout is now the first thing to give way, and the scores have a route",
     "elision_recovery::D7 — the HUD showcase discloses what it stops showing::the objective chip is inside the platform's band at 749x380, not above the screen",
     "overflow_sweep::overflow sweep: gallery scenarios::scenario 'hud': the solver reports NOTHING at any swept viewport, at any text size"
    ],
    "shell": "grep -q elision_recovery.spec tests/run.luau && grep -q unshown docs/reference/api.md"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "navigation-and-menus::d7-elision-discloses",
   "name": "d7-elision-discloses",
   "note": "d7-elision-discloses: 19 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold; and artifacts/navigation-and-menus/d7-elision-discloses.md must exist. Requirements: UI-LAYOUT-001, UI-A11Y-001.",
   "phase": "navigation-and-menus",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-001",
    "UI-A11Y-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite",
     "rascalrally-suite"
    ],
    "resultIds": [
     "playlist_columns::playlist columns: the 320x640 edit-mode measurement that kept the third column::at the narrowest swept viewport, in edit mode, no cell collapses and the numbers are these",
     "playlist_columns::playlist columns: the 320x640 edit-mode measurement that kept the third column::the solver files NO collapse diagnostic — which is the whole difference from the fixed column",
     "playlist_columns::playlist columns: the header is on screen and legible::pinning one column leaves a SIBLING to absorb the remainder — the row still fills its region",
     "playlist_columns::playlist columns: the three routes all move the same column::a pointer drag on the divider MOVES the column, and the live readout follows the commit",
     "playlist_columns::playlist columns: the three routes all move the same column::a KEYBOARD reaches it with no consumer wiring: the heading, Return, then the arrows",
     "playlist_columns::playlist columns: the three routes all move the same column::a GAMEPAD reaches it: the bumper steps the focused heading without selecting anything",
     "playlist_columns::playlist columns: the three routes all move the same column::a LOCKED column binds no Adjust key at all — the bumper stays the game's",
     "playlist_columns::playlist columns: the three routes all move the same column::SELECTING A COLUMN PAINTS NOTHING, so the hint line is the only thing that says the arrows moved house",
     "playlist_columns::playlist sort: the owner sorts, and the sort composes with the filter::the FILTER runs over the SORTED view, so both derivations are live at once",
     "playlist_columns::playlist sort: the owner sorts, and the sort composes with the filter::RATING opts in with `sortable = true` and sorts by the SIGNAL, live",
     "playlist_columns::playlist sort: the owner sorts, and the sort composes with the filter::Artist sorts by `value` too, and duplicate artists tie-break on the SOURCE index",
     "playlist_columns::playlist sort: a manual reorder wins, and takes the sort with it::a drop BAKES the sorted order into the source and clears the sort",
     "playlist_columns::playlist sort: a manual reorder wins, and takes the sort with it::`Top` bakes too — a move a live sort would undo is a button that lies",
     "playlist_columns::playlist sort: a manual reorder wins, and takes the sort with it::REMOVING a row is not a reorder: the sort survives it",
     "playlist_columns::playlist sort: a manual reorder wins, and takes the sort with it::reordering is still REFUSED under a filter, sort or no sort",
     "playlist_columns::playlist sort: a manual reorder wins, and takes the sort with it::a reorder that does not happen does not clear the sort either",
     "playlist_columns::playlist columns: the tutorial's contract::the header IS reachable by a Tab walk from the page's first focus stop",
     "playlist_columns::playlist columns: the tutorial's contract::the device funnels each move the model, and the readouts follow",
     "playlist_columns::playlist columns: the header is on screen and legible::a resizable column grows a divider and a locked one refuses — both invisibly, so the readout names them",
     "playlist_columns::the resize grip's hit floor lands on top of its own header button::asks for 44x44 centred on an 8x28 grip, which covers 26px of the header cell it sits in",
     "playlist_columns::newTable: the header's column boundaries are the body's::every column's header spans exactly its cells, with the bar reserved",
     "playlist_columns::playlist columns: how big the divider actually is::the grip is the theme's small spacing step in every paradigm, touch included",
     "playlist_columns::playlist columns: the divider forwards a press it did not turn into a drag::a press and release with no travel sorts the column and commits NO width",
     "hit_expander_overhang::hit-expander overhang: the table divider's floor is reachable::the divider outranks the NEIGHBOUR column its 44px floor reaches into",
     "gallery_demo_picker::showcase catalogue::lists every tutorial example plus the twenty-seven fixtures",
     "example_readouts::readout sweep: every tutorial example renders the content it declares::example '01_temperature_converter': every declared readout has a real rect at every swept viewport, at every text size",
     "example_readouts::readout sweep: every tutorial example renders the content it declares::example '02_playlist_table': every declared readout has a real rect at every swept viewport, at every text size",
     "example_readouts::readout sweep: every tutorial example renders the content it declares::example '03_settings_sync': every declared readout has a real rect at every swept viewport, at every text size",
     "example_readouts::readout sweep: every tutorial example renders the content it declares::example '04_confirm_dialog': every declared readout has a real rect at every swept viewport, at every text size",
     "example_readouts::readout sweep: every tutorial example renders the content it declares::example '05_word_game': every declared readout has a real rect at every swept viewport, at every text size",
     "example_readouts::readout sweep: every tutorial example renders the content it declares::example '06_tile_game': every declared readout has a real rect at every swept viewport, at every text size",
     "example_readouts::readout sweep: every tutorial example renders the content it declares::example '07_match3': every declared readout has a real rect at every swept viewport, at every text size"
    ],
    "shell": "test ! -f examples/gallery/scenarios/table_columns.luau && test ! -f tests/table_columns.spec.luau && grep -q playlist_columns.spec tests/run.luau && ! grep -q \".table_columns.,\" examples/gallery/scenarios/init.luau && ! grep -q \"module = .table_columns.\" examples/gallery/client/demo_picker.luau && grep -q \"drag a divider to resize a column\" examples/gallery/client/demo_picker.luau && grep -q \"sortOrder = sortOrder\" examples/gallery/examples/02_playlist_table.luau && (cd ../../../games/RascalRally/code && cat \"$FACET_VERIFY_RR_TRANSCRIPT\" >/dev/null)"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "navigation-and-menus::d8-playlist-sort-resize",
   "name": "d8-playlist-sort-resize",
   "note": "d8-playlist-sort-resize: 32 named suite cases must pass; suite, rascalrally-suite must exit zero; its file-existence and text assertions must hold; and artifacts/navigation-and-menus/d8-playlist-sort-resize.md must exist. Requirements: UI-INPUT-003, UI-LAYOUT-001.",
   "phase": "navigation-and-menus",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-003",
    "UI-LAYOUT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "for spec in anchored_surface_contract menu_contract help_callout_contract selection_indicator_contract segmented_picker_contract; do test -f \"../../../games/RascalRally/code/tests/facet_${spec}.spec.luau\" || { echo \"missing game-side contract spec: $spec\"; exit 1; }; grep -q \"facet_${spec}.spec\" ../../../games/RascalRally/code/tests/run.luau || { echo \"$spec is not REGISTERED - an unregistered spec is a silent zero\"; exit 1; }; done && (cd ../../../games/RascalRally/code && cat \"$FACET_VERIFY_RR_TRANSCRIPT\" >/dev/null)"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "navigation-and-menus::rider-rascalrally-consumer",
   "name": "rider-rascalrally-consumer",
   "note": "rider-rascalrally-consumer: rascalrally-suite must exit zero; its file-existence and text assertions must hold; and artifacts/navigation-and-menus/consumer-impact.md must exist. Requirements: UI-AGENT-001.",
   "phase": "navigation-and-menus",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "shell": "test -f build/Facet.rbxm && test -f examples/places/Facet-Showcase.rbxl"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "release-candidate-review::rename-canonical-identity",
   "name": "rename-canonical-identity",
   "note": "rename-canonical-identity: its file-existence and text assertions must hold; and artifacts/release-candidate-review/rename/after-inventory.md must exist. Requirements: UI-AGENT-001.",
   "phase": "release-candidate-review",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_brand_drift-skip-builds",
     "check_brand_drift-selftest"
    ]
   },
   "class": "exit0",
   "evidence": null,
   "id": "release-candidate-review::rename-drift-guard-bites",
   "name": "rename-drift-guard-bites",
   "note": "rename-drift-guard-bites: check_brand_drift-skip-builds, check_brand_drift-selftest must exit zero; and artifacts/release-candidate-review/rename/drift-guard-proof.md must exist. Requirements: UI-AGENT-001.",
   "phase": "release-candidate-review",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "shell": "! ls -d ../*/ 2>/dev/null | python3 -c \"import sys, importlib.util; s = importlib.util.spec_from_file_location('g', 'tools/check_brand_drift.py'); m = importlib.util.module_from_spec(s); s.loader.exec_module(m); sys.exit(0 if any(m.BRAND.search(l) for l in sys.stdin) else 1)\""
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "release-candidate-review::clean-clone-runs-as-facet",
   "name": "clean-clone-runs-as-facet",
   "note": "clean-clone-runs-as-facet: its file-existence and text assertions must hold; and artifacts/release-candidate-review/rename/clean-clone-proof.md must exist. Requirements: UI-AGENT-001.",
   "phase": "release-candidate-review",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite"
    ],
    "shell": "test -f ../../../games/RascalRally/docs/migrations/facet-attribute-migration.md && test -f ../../../games/RascalRally/code/src/client/FacetFlags.luau"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "release-candidate-review::rr-consumer-sync",
   "name": "rr-consumer-sync",
   "note": "rr-consumer-sync: its file-existence and text assertions must hold; and artifacts/release-candidate-review/rename/rr-consumer-ledger.md must exist. Requirements: UI-AGENT-001.",
   "phase": "release-candidate-review",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "shell": "git remote get-url origin | python3 -c \"import sys, importlib.util; s = importlib.util.spec_from_file_location('g', 'tools/check_brand_drift.py'); m = importlib.util.module_from_spec(s); s.loader.exec_module(m); sys.exit(0 if any(m.BRAND.search(l) for l in sys.stdin) else 1)\""
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "release-candidate-review::step14-remote-packet",
   "name": "step14-remote-packet",
   "note": "step14-remote-packet: its file-existence and text assertions must hold; and artifacts/release-candidate-review/step14-remote-packet.md must exist. Requirements: UI-AGENT-001.",
   "phase": "release-candidate-review",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_reuse_ledger-selftest",
     "check_reuse_ledger",
     "check_boundary",
     "check_call_shape_drift-selftest",
     "check_call_shape_drift",
     "check_brand_drift-selftest",
     "check_brand_drift",
     "suite"
    ],
    "resultIds": [
     "world_substrate::REUSE-88: the retired tuple order stays retired::no spec returns the swapped order any more",
     "world_substrate::REUSE-88: the retired tuple order stays retired::the count of hand-rolled presenter builders only ever falls (R12 trigger)",
     "leaf_helpers::REUSE-3: a tap on /MenuBar does not route to /Menu::the longer-named screen owns its own button, whichever order they were presented",
     "hidden_roots_owner::REUSE-29: the three hidden-root walks share one owner::no presentation module re-implements the owner",
     "text_distance::text_distance: the matrix five modules wrote by hand::the threshold boundary is inclusive on the caller's number and nothing past it",
     "client_host::REUSE-109: the frame drives the clock, not just the solve::ticks BEFORE it refreshes, so this frame's hook work is solved this frame",
     "client_host::client host: teardown::dispose releases the adapter and the input system, not only the frame and the binding",
     "client_host::REUSE-115 / §3b-3: a presenter takes exactly one frame driver::the host claims it, and a second driver is refused by name",
     "session_lifetime::newResourceProvider has a lifetime (ARCH-13)::dispose drops the cache, the in-flight requests and the give-up ledger",
     "process_globals::ARCH-23: one of the two text-metric stores is content-addressed::calibration is keyed by FONT ALONE, so one core's tuning changes what another measures",
     "process_globals::ARCH-24: the motion registries are shared, and that is the dial::one consumer's resetClasses() makes another consumer's next motion call throw",
     "text_distance::ARCH-8: the specGuard FACADE, not the module behind it::assertKnownKeys through the facade refuses, names the boundary, and suggests"
    ]
   },
   "class": "result-ids",
   "evidence": "tools/lune/verify/data/reuse-ledger.md",
   "id": "release-candidate-review::reuse-consolidation",
   "name": "reuse-consolidation",
   "note": "reuse-consolidation: 12 named suite cases must pass; check_reuse_ledger-selftest, check_reuse_ledger, check_boundary, check_call_shape_drift-selftest, check_call_shape_drift, check_brand_drift-selftest, check_brand_drift, suite must exit zero; and tools/lune/verify/data/reuse-ledger.md must exist. Requirements: UI-AGENT-001.",
   "phase": "release-candidate-review",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_input_authority-selftest",
     "check_input_authority",
     "suite",
     "rascalrally-suite"
    ],
    "resultIds": [
     "gamepad_contention::gamepad_contention (client module, headless structural spec)::disableLegacyControls() is INERT when IAS owns the player scripts (DF-9)",
     "platform_adapters_recovery::disableLegacyControls only claims what it freed (PLAT-21)::the IAS inert check runs BEFORE either engine remedy is attempted"
    ],
    "shell": "( cd ../../../games/RascalRally/code && rrout=\"$(cat \"$FACET_VERIFY_RR_TRANSCRIPT\")\" && echo \"$rrout\" | grep -q \"✓.*delivers to sponsorCancel ALONE while the sponsor surface is up\" && echo \"$rrout\" | grep -q \"✓.*delivers to resultsSkip ALONE while the results tail is skippable\" && echo \"$rrout\" | grep -q \"✓.*keeps every band strictly below Facet.s base screen priority\" && echo \"$rrout\" | grep -q \"✓.*ButtonA still reaches the Facet screen.s Activate with EVERY game context live\" && echo \"$rrout\" | grep -q \"✓.*MUTATION: a game context re-banded to 3000 starves the Facet screen\" && echo \"$rrout\" | grep -q \"✓.*stays single-delivery even with the camera context left ENABLED\" && echo \"$rrout\" | grep -q \"✓.*no module Fires anything but a Scriptable binding\" && echo \"$rrout\" | grep -q \"✓.*keeps no manual staging machinery at all\" )"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "release-candidate-review::ias-owns-semantic-input",
   "name": "ias-owns-semantic-input",
   "note": "ias-owns-semantic-input: 2 named suite cases must pass; check_input_authority-selftest, check_input_authority, suite, rascalrally-suite must exit zero; its file-existence and text assertions must hold; and artifacts/release-candidate-review/input/ias-authority.md must exist. Requirements: UI-INPUT-001.",
   "phase": "release-candidate-review",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-001"
   ],
   "state": null
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "release-candidate-review::df7-modifier-sink-measured",
   "name": "df7-modifier-sink-measured",
   "note": "df7-modifier-sink-measured: and artifacts/release-candidate-review/input/df7-measurement.md must exist. Requirements: UI-INPUT-001.",
   "phase": "release-candidate-review",
   "releaseBlocking": false,
   "requirements": [
    "UI-INPUT-001"
   ],
   "state": "FAIL_ENVIRONMENT"
  },
  {
   "check": {
    "shell": "test -f src/client/sensory_profile.luau"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "release-candidate-review::haptics-defaults",
   "name": "haptics-defaults",
   "note": "haptics-defaults: its file-existence and text assertions must hold; and artifacts/release-candidate-review/haptics/defaults.md must exist. Requirements: UI-AGENT-001.",
   "phase": "release-candidate-review",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "release-candidate-review::haptics-device-similarity",
   "name": "haptics-device-similarity",
   "note": "haptics-device-similarity: and artifacts/release-candidate-review/haptics/device-review-packet.md must exist. Requirements: UI-AGENT-001.",
   "phase": "release-candidate-review",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "FAIL_ENVIRONMENT"
  },
  {
   "check": {
    "producers": [
     "check_docs_cli",
     "check_registration_cli",
     "suite"
    ],
    "resultIds": [
     "theme_docs::the guide index catalogs the live public surface::counts a catalog entry for every namespace member, export and playbook",
     "theme_docs::the guide index catalogs the live public surface::fails when the catalog stops naming a shipped control",
     "theme_docs::the guide index catalogs the live public surface::fails when the catalog advertises something the library does not export",
     "theme_docs::the guide index catalogs the live public surface::fails when a catalog row links an API-reference heading that does not exist",
     "theme_docs::the guide index catalogs the live public surface::fails when an extension playbook is not linked from the index",
     "theme_docs::the guide index catalogs the live public surface::fails when the catalog invents a top-level export",
     "theme_docs::the guide index catalogs the live public surface::fails when the catalog invents a member of a namespace that is not UI or Controls",
     "theme_docs::the guide index catalogs the live public surface::fails when the catalog invents a client module row"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "release-candidate-review::guide-catalog-current",
   "name": "guide-catalog-current",
   "note": "guide-catalog-current: 8 named suite cases must pass; check_docs_cli, check_registration_cli, suite must exit zero; and artifacts/release-candidate-review/docs/catalog-rebuild.md must exist. Requirements: UI-AGENT-001.",
   "phase": "release-candidate-review",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_brand_drift-selftest",
     "check_brand_drift"
    ],
    "shell": "grep -c \"comparison:begin\" docs/guide/10-rich-skinning.md >/dev/null"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "release-candidate-review::product-language-scan-bites",
   "name": "product-language-scan-bites",
   "note": "product-language-scan-bites: check_brand_drift-selftest, check_brand_drift must exit zero; its file-existence and text assertions must hold; and artifacts/release-candidate-review/docs/product-language-proof.md must exist. Requirements: UI-AGENT-001.",
   "phase": "release-candidate-review",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_comment_codes-selftest",
     "check_comment_codes",
     "check_doc_style"
    ],
    "shell": "! grep -qF \"UI.Custom\" src/themes/package.luau && grep -qF \"is NOT deprecated\" src/client/roblox_input.luau && grep -qF \"an earlier version of this comment said\" src/client/roblox_input.luau"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "release-candidate-review::comments-plain",
   "name": "comments-plain",
   "note": "comments-plain: check_comment_codes-selftest, check_comment_codes, check_gate_pins-selftest, check_gate_pins, check_doc_style must exit zero; its file-existence and text assertions must hold; and artifacts/release-candidate-review/docs/comment-audit.md must exist. Requirements: UI-AGENT-001.",
   "phase": "release-candidate-review",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_doc_style-selftest",
     "check_doc_style"
    ]
   },
   "class": "exit0",
   "evidence": null,
   "id": "release-candidate-review::clear-writing-checked",
   "name": "clear-writing-checked",
   "note": "clear-writing-checked: check_doc_style-selftest, check_doc_style must exit zero; and artifacts/release-candidate-review/docs/clear-writing-proof.md must exist. Requirements: UI-AGENT-001.",
   "phase": "release-candidate-review",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_maintainer_map_cli-selftest",
     "check_maintainer_map_cli",
     "suite"
    ],
    "resultIds": [
     "maintainer_map::maintainer map: every row still points at the tree it describes::reports a src area no row claims",
     "maintainer_map::maintainer map: every row still points at the tree it describes::reports a spec filed under an area it does not require",
     "maintainer_map::maintainer map: every row still points at the tree it describes::reports a gate row the manifest does not have",
     "maintainer_map::maintainer map: every row still points at the tree it describes::reports a boundary rule check_boundary does not enforce",
     "maintainer_map::maintainer map: every row still points at the tree it describes::reports a shipped extension playbook the map stops linking",
     "maintainer_map::maintainer map: every row still points at the tree it describes::reports a public seam the library has stopped exporting",
     "maintainer_map::maintainer map: every row still points at the tree it describes::derives a per-area covering count from the require graph"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "release-candidate-review::maintainer-map-current",
   "name": "maintainer-map-current",
   "note": "maintainer-map-current: 7 named suite cases must pass; check_maintainer_map_cli-selftest, check_maintainer_map_cli, suite must exit zero; and artifacts/release-candidate-review/maintainer-guide-proof.md must exist. Requirements: UI-AGENT-001.",
   "phase": "release-candidate-review",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_public_surface",
     "check_call_shape_drift-selftest",
     "check_call_shape_drift",
     "check_types-selftest",
     "check_types",
     "check_brand_drift-selftest",
     "check_brand_drift",
     "suite"
    ],
    "resultIds": [
     "controls_namespace::the Controls namespace::exports exactly the nineteen composite controls the namespace names, and nothing else",
     "controls_namespace::the Controls namespace::every entry builds what the old two-argument form builds",
     "controls_namespace::the Controls namespace::every old-form builder carries its deprecation row",
     "controls_namespace::the Controls namespace::the eleven infrastructure constructors stay at the top level and stay out of Controls"
    ],
    "shell": "grep -qF \"VERSION 0.10.0\" artifacts/verify/public-surface.txt && grep -qF \"Controls : table(frozen)\" artifacts/verify/public-surface.txt && test \"$(grep -c \"^  Controls[.]\" artifacts/verify/public-surface.txt)\" = \"$(grep -oE \"Controls[.][A-Za-z]+\" tests/conformance/controls_registry.luau | sort -u | grep -c .)\" && test \"$(grep -c \"since=0[.]10[.]0 | removeNoEarlierThan=0[.]12[.]0 | replacement=Facet[.]Controls[.]\" artifacts/verify/public-surface.txt)\" = \"19\""
   },
   "class": "result-ids",
   "evidence": null,
   "id": "release-candidate-review::naming-adr-implemented",
   "name": "naming-adr-implemented",
   "note": "naming-adr-implemented: 4 named suite cases must pass; _probe_public_surface, check_call_shape_drift-selftest, check_call_shape_drift, check_types-selftest, check_types, check_brand_drift-selftest, check_brand_drift, suite must exit zero; its file-existence and text assertions must hold. Requirements: UI-AGENT-001.",
   "phase": "release-candidate-review",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "receipt": "tools/lune/verify/evidence/release-candidate-review--perf-requalification.json"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "release-candidate-review::perf-requalification",
   "name": "perf-requalification",
   "note": "perf-requalification: and artifacts/release-candidate-review/perf/requalification.md must exist. Requirements: UI-AGENT-001.",
   "phase": "release-candidate-review",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "shell": "[ \"$(tools/test.sh --status)\" = \"hit\" ]"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "release-candidate-review::suites-green-at-close",
   "name": "suites-green-at-close",
   "note": "suites-green-at-close: suite must exit zero; its file-existence and text assertions must hold; and artifacts/release-candidate-review/close-out.md must exist. Requirements: UI-AGENT-001.",
   "phase": "release-candidate-review",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "example-games-and-standalones::verification-mutations-bite",
   "name": "verification-mutations-bite",
   "note": "verification-mutations-bite: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "example-games-and-standalones",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "example-games-and-standalones::verification-verdict-parity",
   "name": "verification-verdict-parity",
   "note": "verification-verdict-parity: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "example-games-and-standalones",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "example-games-and-standalones::producer-runs-once-per-identity",
   "name": "producer-runs-once-per-identity",
   "note": "producer-runs-once-per-identity: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "example-games-and-standalones",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "example-games-and-standalones::headless-budget-twenty-minutes",
   "name": "headless-budget-twenty-minutes",
   "note": "headless-budget-twenty-minutes: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "example-games-and-standalones",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {
    "producers": [
     "rascalrally-suite",
     "suite"
    ],
    "shell": "grep -q \"FACET-FAST-TIER\" run-tests.sh && grep -q \"FACET-FAST-TIER\" tools/test.sh && grep -q \"A BASH MATCH, NOT A PIPELINE\" tools/test.sh && test -f ../../../games/RascalRally/code/tests/run_one.luau && grep -qF \"tier, so there is no partial-run\" ../../../games/RascalRally/code/tools/suite_transcript.sh"
   },
   "class": "evidence-pin",
   "evidence": null,
   "id": "example-games-and-standalones::affected-and-fast-loops-trustworthy",
   "name": "affected-and-fast-loops-trustworthy",
   "note": "affected-and-fast-loops-trustworthy: rascalrally-suite, suite must exit zero; its file-existence and text assertions must hold; and artifacts/example-games-and-standalones/test-optimization/baseline.md must exist. Requirements: UI-AGENT-001.",
   "phase": "example-games-and-standalones",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "example-games-and-standalones::verification-coverage-preserved",
   "name": "verification-coverage-preserved",
   "note": "verification-coverage-preserved: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "example-games-and-standalones",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {
    "producers": [
     "build_word_lists-selftest",
     "build_word_lists-check",
     "suite"
    ],
    "resultIds": [
     "example_word_data::example word data: provenance is recorded, not asserted::names a specific licensed archive, not just 'a dictionary'",
     "example_word_data::example word data: provenance is recorded, not asserted::carries the licence notices the source requires",
     "example_word_data::example word data: the files on disk are the files that were generated::every generated file still hashes to what the manifest recorded",
     "example_word_data::example word data: the banks are searchable::every bank is strictly ascending and exactly divisible by its width",
     "example_word_data::example word data: the banks are searchable::the solution bank is ascending too, and every answer is an accepted guess",
     "example_word_data::example word data: the lookup is cheap enough to sit under a keystroke::a hit and a miss both cost a bisection, not a scan"
    ],
    "shell": "grep -qF \"Kevin Atkinson\" examples/gallery/examples/words/PROVENANCE.md && grep -qF \"Permission to use, copy, modify, distribute\" examples/gallery/examples/words/PROVENANCE.md && grep -qE \"[0-9a-f]{64}\" examples/gallery/examples/words/manifest.luau && ! grep -rqF \"tutorial word list\" examples/ docs/guide/ docs/reference/"
   },
   "class": "result-ids",
   "evidence": "examples/gallery/examples/words/PROVENANCE.md",
   "id": "example-games-and-standalones::word-game-dictionary",
   "name": "word-game-dictionary",
   "note": "word-game-dictionary: 6 named suite cases must pass; build_word_lists-selftest, build_word_lists-check, suite must exit zero; its file-existence and text assertions must hold; and examples/gallery/examples/words/PROVENANCE.md must exist. Requirements: UI-AGENT-001.",
   "phase": "example-games-and-standalones",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "example_word_data::example word data: it accepts what a player would type::accepts the words the old hand-written list rejected",
     "example_word_data::example word data: it accepts what a player would type::accepts an ordinary word starting with every letter of the alphabet",
     "example_word_data::example word data: it accepts what a player would type::refuses obvious nonwords, wrong lengths, and anything that is not plain letters",
     "example_word_game::examples/05 word game — thirty cells that are actually there, in every shipped theme::every one of the thirty cells is present AND painted at startup, under every theme",
     "example_word_game::examples/05 word game — thirty cells that are actually there, in every shipped theme::...and the floor BITES: the plate this example used to paint cannot clear it anywhere",
     "example_word_game::examples/05 word game — the active row's cue is not a colour::the caret's PAINT is the empty cell's paint — so colour cannot be the cue"
    ],
    "shell": "grep -qE \"empty = [{] role = .contentSecondary.\" examples/gallery/examples/05_word_game.luau && ! grep -qF \"EXTRA_WORDS\" examples/gallery/examples/05_word_game.luau && ! grep -qF \"local SOLUTIONS\" examples/gallery/examples/05_word_game.luau && grep -qF \"words.isWord\" examples/gallery/examples/05_word_game.luau"
   },
   "class": "result-ids",
   "evidence": "examples/gallery/examples/05_word_game.luau",
   "id": "example-games-and-standalones::word-game-board-visible",
   "name": "word-game-board-visible",
   "note": "word-game-board-visible: 6 named suite cases must pass; suite must exit zero; its file-existence and text assertions must hold; and examples/gallery/examples/05_word_game.luau must exist. Requirements: UI-AGENT-001.",
   "phase": "example-games-and-standalones",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "example-games-and-standalones::tile-game-crossword-loop",
   "name": "tile-game-crossword-loop",
   "note": "tile-game-crossword-loop: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "example-games-and-standalones",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "example-games-and-standalones::match3-motion-public-surface",
   "name": "match3-motion-public-surface",
   "note": "match3-motion-public-surface: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "example-games-and-standalones",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "example-games-and-standalones::sensory-demo-opens-useful",
   "name": "sensory-demo-opens-useful",
   "note": "sensory-demo-opens-useful: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "example-games-and-standalones",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "example-games-and-standalones::sensory-demo-four-comparisons",
   "name": "sensory-demo-four-comparisons",
   "note": "sensory-demo-four-comparisons: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "example-games-and-standalones",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {
    "producers": [
     "check_boundary",
     "check_registration_cli",
     "check_docs_cli",
     "suite"
    ],
    "resultIds": [
     "surface_target::surface_target: the canvas the solver is handed::canvasRect is the canvas, at the origin, and is pure",
     "surface_target::surface_target: every refusal says why::face is required, and the refusal explains the trap rather than just refusing",
     "surface_target::surface_target: every refusal says why::the queryable precondition is refused BEFORE any Instance exists",
     "surface_target::surface_target: what it withholds is real::every capability it deletes is one the contract actually declares",
     "spatial::the world render target is declared AND shipped (XP-D3)::every still-open question has a capability the adapter withholds",
     "spatial::the world render target is declared AND shipped (XP-D3)::AlwaysOnTop is pinned false and is not an option"
    ],
    "shell": "test -f src/client/surface_target.luau && grep -qF \"surface_target = true\" tools/lune/check_boundary.luau && grep -qF \"thirteen\" docs/reference/constitution.md && grep -qF \"client.surface_target\" docs/reference/api.md && grep -qF \"SHIPPED 2026-08-29\" src/render/target_contract.luau && ! grep -qF \"not implemented; no adapter\" src/render/target_contract.luau"
   },
   "class": "result-ids",
   "evidence": "src/client/surface_target.luau",
   "id": "example-games-and-standalones::surface-target-public-adapter",
   "name": "surface-target-public-adapter",
   "note": "surface-target-public-adapter: 6 named suite cases must pass; check_boundary, check_registration_cli, check_docs_cli, suite must exit zero; its file-existence and text assertions must hold; and src/client/surface_target.luau must exist. Requirements: UI-AGENT-001, UI-WORLD-001.",
   "phase": "example-games-and-standalones",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001",
    "UI-WORLD-001"
   ],
   "state": null
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "example-games-and-standalones::world-terminal-plays",
   "name": "world-terminal-plays",
   "note": "world-terminal-plays: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001, UI-WORLD-001.",
   "phase": "example-games-and-standalones",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001",
    "UI-WORLD-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "example-games-and-standalones::world-terminal-guide-recipe",
   "name": "world-terminal-guide-recipe",
   "note": "world-terminal-guide-recipe: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "example-games-and-standalones",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {
    "producers": [
     "check_device_sweep-selftest",
     "check_example_drift_cli",
     "suite"
    ],
    "resultIds": [
     "reference/wardrobe_spec::Wardrobe proof — filters, sections, locale, determinism::section stubs are honest and the worn set survives leaving and returning",
     "reference/wardrobe_spec::Wardrobe proof — try-on and history::undo/redo walk the equip history and disable at the stack ends"
    ],
    "shell": "test -f tests/fixtures/retired/README.md && test -f tests/fixtures/retired/p5_wardrobe/init.luau && grep -qF \"not a current example\" tests/fixtures/retired/README.md && grep -qF \"fixtures/retired/p5_wardrobe\" tests/reference/wardrobe_spec.luau && for gone in examples/reference/p5_wardrobe examples/gallery/scenarios/ref_wardrobe.luau examples/places/Facet-Ref-Wardrobe.rbxl; do test ! -e \"$gone\" || { echo \"retired surface is back: $gone\"; exit 1; }; done && ! grep -qE \"^[[:space:]]*.ref_wardrobe.,[[:space:]]*$\" examples/gallery/scenarios/init.luau && [ \"$(grep -cE \"^  .ref_[a-z]+[|]Facet-Ref-\" tools/build_reference_places.sh)\" -eq 4 ] && ! grep -qi wardrobe examples/gallery/client/demo_picker.luau && ! grep -q \"p4_foyer.*p5_wardrobe\" tests/overflow_sweep.spec.luau && ! grep -q \"p4_foyer.*p5_wardrobe\" tools/lune/triage_overflow_waivers.luau && ! grep -q \"surface = .p5_wardrobe\" tests/lib/theme_sweep_ledger.luau && ! grep -q \"scenario.: .ref_wardrobe\" tools/studio/device_sweep_matrix.json && grep -qF wasScenario tools/studio/device_sweep_matrix.json && grep -qF retiredCells tools/studio/device_sweep_matrix.json && ! grep -q \"file = .examples/reference/p5_wardrobe\" tools/lune/check_example_drift.luau"
   },
   "class": "result-ids",
   "evidence": null,
   "id": "example-games-and-standalones::wardrobe-retired",
   "name": "wardrobe-retired",
   "note": "wardrobe-retired: 2 named suite cases must pass; check_device_sweep-selftest, check_example_drift_cli, suite must exit zero; its file-existence and text assertions must hold; and artifacts/example-games-and-standalones/wardrobe-inventory.md must exist. Requirements: UI-AGENT-001.",
   "phase": "example-games-and-standalones",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "example-games-and-standalones::sipworks-loop-complete",
   "name": "sipworks-loop-complete",
   "note": "sipworks-loop-complete: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "example-games-and-standalones",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "example-games-and-standalones::glade-loop-complete",
   "name": "glade-loop-complete",
   "note": "glade-loop-complete: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "example-games-and-standalones",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "example-games-and-standalones::one-manifest-drives-places",
   "name": "one-manifest-drives-places",
   "note": "one-manifest-drives-places: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "example-games-and-standalones",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "example-games-and-standalones::places-are-playable-not-fixtures",
   "name": "places-are-playable-not-fixtures",
   "note": "places-are-playable-not-fixtures: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "example-games-and-standalones",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "example-games-and-standalones::shared-theme-and-motion-chrome",
   "name": "shared-theme-and-motion-chrome",
   "note": "shared-theme-and-motion-chrome: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "example-games-and-standalones",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "example-games-and-standalones::dead-example-audit",
   "name": "dead-example-audit",
   "note": "dead-example-audit: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "example-games-and-standalones",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "example-games-and-standalones::rascalrally-consumer",
   "name": "rascalrally-consumer",
   "note": "rascalrally-consumer: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "example-games-and-standalones",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "example-games-and-standalones::step-13-guards-hold",
   "name": "step-13-guards-hold",
   "note": "step-13-guards-hold: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "example-games-and-standalones",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "example-games-and-standalones::prior-gates-unregressed",
   "name": "prior-gates-unregressed",
   "note": "prior-gates-unregressed: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "example-games-and-standalones",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "example-games-and-standalones::fresh-phase-gate-review",
   "name": "fresh-phase-gate-review",
   "note": "fresh-phase-gate-review: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "example-games-and-standalones",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "example-games-and-standalones::suites-green-at-close",
   "name": "suites-green-at-close",
   "note": "suites-green-at-close: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "example-games-and-standalones",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::registered-before-work",
   "name": "registered-before-work",
   "note": "registered-before-work: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::state-frozen-at-open",
   "name": "state-frozen-at-open",
   "note": "state-frozen-at-open: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::history-audit-no-must-purge",
   "name": "history-audit-no-must-purge",
   "note": "history-audit-no-must-purge: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::provenance-and-third-party-notices",
   "name": "provenance-and-third-party-notices",
   "note": "provenance-and-third-party-notices: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::mit-license-root-files",
   "name": "mit-license-root-files",
   "note": "mit-license-root-files: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::public-allowlist-and-private-archive",
   "name": "public-allowlist-and-private-archive",
   "note": "public-allowlist-and-private-archive: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {
    "producers": [
     "check_no_fusion",
     "check_no_fusion-selftest"
    ],
    "shell": "! test -e vendor && ! test -e src/core/fusion_adapter.luau"
   },
   "class": "exit0",
   "evidence": null,
   "id": "distribution-readiness::no-fusion-anywhere",
   "name": "no-fusion-anywhere",
   "note": "no-fusion-anywhere: check_no_fusion and its selftest must exit zero, and neither the vendored tree nor the adapter may exist. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::private-comparison-archived-links-removed",
   "name": "private-comparison-archived-links-removed",
   "note": "private-comparison-archived-links-removed: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::framework-choice-guide-fair",
   "name": "framework-choice-guide-fair",
   "note": "framework-choice-guide-fair: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::public-docs-refreshed-no-stale-links",
   "name": "public-docs-refreshed-no-stale-links",
   "note": "public-docs-refreshed-no-stale-links: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::agents-md-and-skill-route",
   "name": "agents-md-and-skill-route",
   "note": "agents-md-and-skill-route: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::standalone-consumer-proof",
   "name": "standalone-consumer-proof",
   "note": "standalone-consumer-proof: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::package-metadata-and-manifest",
   "name": "package-metadata-and-manifest",
   "note": "package-metadata-and-manifest: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::package-interface-refusals",
   "name": "package-interface-refusals",
   "note": "package-interface-refusals: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::protected-manual-release",
   "name": "protected-manual-release",
   "note": "protected-manual-release: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::structured-results-replace-greps",
   "name": "structured-results-replace-greps",
   "note": "structured-results-replace-greps: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::single-execution-per-identity",
   "name": "single-execution-per-identity",
   "note": "single-execution-per-identity: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::invalidation-rejects-bad-evidence",
   "name": "invalidation-rejects-bad-evidence",
   "note": "invalidation-rejects-bad-evidence: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::tiers-and-explain",
   "name": "tiers-and-explain",
   "note": "tiers-and-explain: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::coverage-map-no-silent-loss",
   "name": "coverage-map-no-silent-loss",
   "note": "coverage-map-no-silent-loss: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::mutation-parity-old-vs-new",
   "name": "mutation-parity-old-vs-new",
   "note": "mutation-parity-old-vs-new: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::timings-and-budget",
   "name": "timings-and-budget",
   "note": "timings-and-budget: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::prior-gates-reevaluated-not-replayed",
   "name": "prior-gates-reevaluated-not-replayed",
   "note": "prior-gates-reevaluated-not-replayed: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::rascalrally-synchronized",
   "name": "rascalrally-synchronized",
   "note": "rascalrally-synchronized: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::public-tree-reproducible",
   "name": "public-tree-reproducible",
   "note": "public-tree-reproducible: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::fresh-clone-works",
   "name": "fresh-clone-works",
   "note": "fresh-clone-works: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::example-places-rebuild-from-clone",
   "name": "example-places-rebuild-from-clone",
   "note": "example-places-rebuild-from-clone: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::package-from-clone-matches",
   "name": "package-from-clone-matches",
   "note": "package-from-clone-matches: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::fresh-agent-builds-screen",
   "name": "fresh-agent-builds-screen",
   "note": "fresh-agent-builds-screen: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::fresh-agent-extends-behavior",
   "name": "fresh-agent-extends-behavior",
   "note": "fresh-agent-extends-behavior: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::fresh-reviewer-chooses-from-guide",
   "name": "fresh-reviewer-chooses-from-guide",
   "note": "fresh-reviewer-chooses-from-guide: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::owner-packet-complete",
   "name": "owner-packet-complete",
   "note": "owner-packet-complete: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::private-package-id-and-update-proof",
   "name": "private-package-id-and-update-proof",
   "note": "private-package-id-and-update-proof: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {},
   "class": "declared",
   "evidence": null,
   "id": "distribution-readiness::repository-renamed-and-verified",
   "name": "repository-renamed-and-verified",
   "note": "repository-renamed-and-verified: declared evidence, recorded rather than executed. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": "PENDING"
  },
  {
   "check": {
    "producers": [
     "suite"
    ],
    "resultIds": [
     "text_premeasure::text_metrics: the request set a solve emits::nothing is collected outside a collect bracket (the solver stays request-free by default)",
     "text_premeasure::text_metrics: the request set a solve emits::reports whether a measurement was exact end to end",
     "text_premeasure::text_metrics: the request set a solve emits::a word the engine could not measure is never asked for again",
     "text_premeasure::text_metrics: the request set a solve emits::collects one request per unknown word plus the space, deduped and ordered",
     "text_premeasure::text_metrics: the per-word exact store::rejects a measurement that could cause a clip, keeping the safe estimate",
     "text_premeasure::text_metrics: the request set a solve emits::keys on (font, size, word) — NOT maxWidth, so a resize costs no new measurement",
     "text_premeasure::renderer: engine premeasurement round::an engine that cannot measure a word leaves the safe estimate and stops asking",
     "text_calibration::text_metrics calibration (NS-A10)::calibration is per-font; unknown fonts keep the full-em pending fallback",
     "text_fit::text_fit.fit — the state of the measure at the chosen size (F-32)::an uncalibrated font says `pending`, not merely `exact = false`",
     "text_fit::text_fit.fit — the largest size that actually fits::reports whether the answer is EXACT or the conservative fallback"
    ]
   },
   "class": "result-ids",
   "evidence": null,
   "id": "phase-1-minimal-screen::text-premeasurement-contract",
   "name": "text-premeasurement-contract",
   "note": "text-premeasurement-contract: the ten suite cases that prove the premeasurement contract must pass — a provider that yields nothing, a ready/pending/failed answer, a deduped engine queue, a conservative fallback, and a cache key that excludes the offered width. Requirements: UI-LAYOUT-003.",
   "phase": "phase-1-minimal-screen",
   "releaseBlocking": false,
   "requirements": [
    "UI-LAYOUT-003"
   ],
   "state": null
  },
  {
   "check": {
    "producers": [
     "check_links_cli",
     "check_links_cli-selftest"
    ]
   },
   "class": "exit0",
   "evidence": null,
   "id": "distribution-readiness::public-docs-no-stale-links",
   "name": "public-docs-no-stale-links",
   "note": "public-docs-no-stale-links: check_links_cli and its selftest must exit zero — every link a published document offers resolves, and none points into a tree that is leaving. This is DR-10's mechanical half; the refresh half stays with DR-10. Requirements: UI-AGENT-001.",
   "phase": "distribution-readiness",
   "releaseBlocking": false,
   "requirements": [
    "UI-AGENT-001"
   ],
   "state": null
  }
 ],
 "schema": "facet-verify-graph/1",
 "unresolvedPatterns": []
}
