# Facet repository/instruction inventory (Phase 0)

Read before building; stricter repository rules are preserved over the design where they overlap (none conflict as of 2026-07-19).

| Source | Governs | Load-bearing points for Facet |
|---|---|---|
| `docs/superpowers/specs/2026-07-19-luauui-crossplatform-ui-design.md` | Product/architecture source of truth | Phases 0–5, hard requirements §2.1, gates §16–17 |
| `prompt.md` (repo root) | Kickoff + verified repository state | Scaffold/mounts exist — verify, never recreate; Opus subagent format |
| `/CLAUDE.md` (repo root) | Studio constitution | GameStudio/ = game-agnostic; never put game specifics there |
| `GameStudio/ENGINEERING.md` | Engineering standards | Simplicity ladder; platform-native first (verify current docs); every non-trivial change leaves a runnable check; deterministic tests; spec-count check ("green suite with unregistered spec is a silent zero"); module header comments; `ponytail:` shortcut convention |
| `games/RascalRally/CLAUDE.md` | Game constitution | Server authority mandatory (first-party Server Authority + IAS); cross-platform day one; Lune+testkit is the recorded runner (not TestEZ); localization via UI_ENGINEER rules |
| `GameStudio/specialists/ROBLOX.md` | Platform practice | Verify-live prime directive; Studio MCP; MicroProfiler loop (read per-mission for platform work) |
| `GameStudio/specialists/UI_DESIGNER.md`, `UI_ENGINEER.md` | UI roles | Updated in Adoption step 5 (§18) after the pilot gate — not before |
| `games/RascalRally/code/src/client/SettingsGui.luau` + `GearDockModel` + `tests/settings_model.spec.luau` | Phase 2 parity target | ~485 lines; topbar gear dock; device history in `docs/missions/M10_HANDBACK.md` |
| Memory: `facet-initiative.md` | Session continuity | Scaffold + mounts + parity guard done; do not redo |
| `prompt.md` "Expansion: input paradigms" (repo root) | Input-paradigms mission (2026-07-21) | Console 10-foot IN scope; TV-remote input DEFERRED (separate mission); three axes: UI / input / paradigm |
| `docs/research/2026-07-21-affordance-research.md` | Affordance design authority | per-platform adaptation, sourced; consumed by `artifacts/input-paradigms/affordance-matrix.md` |
| `docs/research/2026-07-21-preferredinput-gamepad-research.md` | Engine input truths | Studio on this development platform pad forwarding failure class; PreferredInput semantics; ViewportDisplaySize surface |
| `docs/adr/ADR-0015-interaction-classes.md` | Affordance derivation rule | Live class set from capabilities + preferredInput; never the preference alone |
| `docs/plans/facet-consolidated-roadmap.md` | Current execution entry point | Seventeen runnable goals: Steps 1–14 plus theme-package Step 3.5, cleanup Step 5.5, and large-text Step 8.5; model ownership and every later gate are defined there. Its "Milestone 1 — parity round 2" section carries the post-Step-11 work that is in flight and is not a numbered step |
| `docs/plans/agent-execution-contract.md` | Evidence, autonomy, model-handoff, and consumer/framework ownership contract | Fable/Opus lead split; UI Designer and motion-quality loop for Sponsor work; headless/Studio/physical/human evidence ladder; no proxy passes; ownership ledgers prevent consumer-local substitutes; every Facet change synchronizes Rascal Rally callers or compatibility tests/evidence in the same stage |
| `docs/plans/theme-packages-and-skinning.md` | Metric-aware, reusable theme architecture for Step 3.5 | Public versioned packages; one solver/adapter metric snapshot; Style Editor synchronization; font-aware measurement; Fantasy Parchment nine-slice proof; custom-theme guide and canonical documentation command; flat-versus-ornate cost evidence |
| `docs/plans/code-simplicity-cleanup.md` | Fresh-context cleanup gate between Steps 5 and 6 | Audit-first deletion/consolidation; behavior and API preservation; no speculative redesign; candidate ledger and before/after evidence |
| `docs/plans/studio-device-verification.md` | Scriptable Studio device/input matrix for Steps 3–14, Steps 3.5/8.5, and visible Step 5.5 changes | Five representative view rows; `StudioDeviceSimulatorService`, `VirtualInput`, and `StudioTestService`; runtime-discovered presets; honest touch/gamepad/performance limits |
| `docs/plans/api-architecture-consistency.md` | Step 7 public API constitution and consistency audit | Predictable constructor/spec/result/lifecycle/extension patterns; complete public-surface classification; compatible fixes only; fresh-author exercise and drift enforcement |
| `docs/reference/constitution.md` | The authoritative rule set for how any Facet public surface is shaped (adopted 2026-08-02, v0.8.0) | Kind ladder, naming, constructor/spec/result/callback/ownership/error rules, input, adaptation, styling, adapters, the four extension seams, versioning, the §15 evidence list, and §16's numbered approved exceptions; changed by ADR in the same commit as the rule-affecting change; `check_registration`/`check_docs`/`check_prop_parity`/`check_boundary` enforce the mechanical half |
| `artifacts/api-architecture-consistency/` (`surface-ledger.md`, `ledger/*.md`, `dispositions.md`, `decision-packets.md`, `enforcement-gap-list.txt`) | Step 7 audit evidence (2026-08-02) | Five fragments with file:line evidence per public item; every finding dispositioned exactly once (FIX / DOC / DEP / EXC / PKT / ENF / NOTE); the classification row every export needs; deferred renames preserved as decision packets; the enforcement gap list the DOC package closed |
| `docs/plans/desktop-keyboard-navigation.md` | Step 8 desktop keyboard behavior | Tab/Shift+Tab focus chain, Space/Return activation, arrow adjustment, text-editing and responder ownership, exactly-once input, Studio proof |
| `docs/plans/large-text-accessibility.md` | Step 8.5 large-text accessibility and Sponsor proof | Exact live Roblox preferred-text seam; reflow-before-truncation policy; bounded engaged reveal; compact portrait/landscape Sponsor matrix; performance linkage |
| `docs/plans/performance-stress-places.md` | Real-engine/device performance proof | Publish-ready stress place, dense-scroll/native-reference workloads, MicroProfiler/LibMP loop, low-end Android gate |
| `docs/plans/example-quality-pass.md` | Tutorial gallery acceptance | Play all seven examples; keep domain logic in thin declarative consumers while reusable fixes land in Facet; native styling authority; settings-sync teaching; complete Wordle-like mechanics; tile/match-3 validation |
| `docs/plans/reference-app-validation.md` | Step 11 clean-room reference-app proof | Three runnable representative loops; complete official-feature classification; explicit Facet/game-service/host-platform boundaries; no copied third-party code or assets |
| `docs/plans/declarative-3d-architecture.md` | Step 12 declarative 3D decision | Compare Facet extension, sibling, and separate systems; isolated server/client topology and lifecycle spike; no production or VR capability claim |
| `docs/plans/release-candidate-review.md` | Step 13 fresh Fable review | Whole production surface reviewed before edits; confirmed defects remediated; clear guide/API catalogs rebuilt from exports; platform-neutral language boundary and exact comparison allowlist drift-checked |
| `docs/plans/distribution-readiness.md` | Step 14 source sharing and agent onboarding | Reproducible allowlisted source export; root `AGENTS.md` plus thin `use-facet` skill; clean-context agent tasks and real-adapter consumer proof; Package publishing optional; internal evidence excluded, not destroyed |
| `artifacts/native-substrate/acceptance-ledger.md` | Step-1 native-adoption evidence ledger (2026-07-23) | Per-row honest statuses; feasibility spikes under `feasibility/`; review packet for physical rows |
| `docs/adr/ADR-0017-native-substrate-adoption.md` | Native-substrate decisions | ScrollingFrame host, Path2D, UIPageLayout rejection, GetInsetArea authority, selection policy, PreloadAsync honesty |
| `docs/adr/ADR-0019-theme-packages.md` + `artifacts/theme-packages-and-skinning/acceptance-ledger.md` | Theme-package decisions and Step 3.5 evidence (2026-07-25) | Versioned `ThemePackage` + frozen `ThemeSnapshot` as the one metric authority; Style Editor sync with a freshness gate; closed decoration-slot/recipe vocabulary and the `::UIShadow`-is-inert allowlist; per-target controller and the atomic swap. Docs surface: `docs/guide/09-custom-themes.md`, `docs/extending/new-theme.md`, canonical command `lune run tools/lune/check_docs_cli` |
| `docs/adr/ADR-0020-rich-skinning-v2.md` + `artifacts/rich-skinning-v2/acceptance-ledger.md` | Rich-skinning decisions and evidence (2026-07-25) | Layered decoration slots (bounded, closed kind vocabulary, censused per kind); ONE per-state asset-variant grammar shared by the theme and per-view rungs; image value displays (bar family clipped by a window, toggle and stepper slots); semantic icons with ASCII-safe fallback glyphs; `rendering = "pixel"` + `pixelUnit`; `content`/`contentId`; `selectBy` on the controller; the three-rung customization ladder. Docs surface: `docs/guide/10-rich-skinning.md`, `docs/extending/skinned-control.md`, same canonical command |
| `docs/plans/parity-round2-brief.md` + `docs/plans/parity-round2.md` | Milestone 1: the parity round-2 mission (brief = binding scope; design = the per-phase build plan) | Phases 0–6 grouped M1 = 1–2, M2 = 3–4, M3 = 5–6; §2.1 the silently-inert placement-prop audit, §2.2 `UI.GridRow`, §2.3 the VirtualList axis + the `itemExtent`/`viewportExtent` renames, §2.4 the shrink pass, §2.5 `containerRelativeFrame`, §2.6 `distribute`, plus `presenter.withAnimation`; four recorded departures from the brief, each argued where it happens |
| `phases.json` + `tools/lune/gate_manifest.luau` → `swiftui-parity-round2` | The Milestone-1 gate stage, registered 2026-08-13 **after** the mission closed — which is itself the finding: `tools/gate.sh` had no stage to run for M1/M2/M3, so all three closed on hand-run evidence and nothing in the repo could have reddened afterwards | Twelve checks. Suite floor **4589** (fixes the 4136-vs-4589 slack; measured from a pristine `git archive 13fd3c6` export, and it is a FLOOR, not an equality); `withAnimation`; the layout vocabulary incl. the solver-memo key; indicators + `sensoryFeedback`; the **four-input proofs** for `Table.onPrimaryAction` and hosted `rowActions` (which `check_registration` structurally cannot see, because it keys `inputProofs` on CONTROLS and these are PROPS — enforced here as `✓`-anchored suite greps, which is stronger than that checker's source-string rule; the *completeness* half is a NAMED GAP, and `sensoryFeedback` is outside the rule because it declares no input of its own); gallery coverage; the overflow sweep by scenario name (a dropped scenario leaves the suite green — proven); the eight-checker battery; the parity doc's falsifiability; the Rascal Rally round-2 contract specs run live; the carried `traversal-document-order`/`studio-evidence` red, guarded so it cannot be erased instead of re-recorded; and the prior-gates sweep, registered and **owed** |
| `docs/plans/device-bug-round-2026-08-12.md` | Milestone 1's device phase: nine findings from the director on real hardware | Three causes, not nine bugs (tray geometry / overflow with nothing to scroll / the row-actions flight); the overflow sweep is the instrument Group B leaves behind; the 8px scroll-bar reserve decision of 2026-08-12 REVERSES the 2026-08-09 overlay ruling — do not "restore" the zero reserve; carries the owed **TD-13/TD-14 Studio re-record** that keeps the `traversal-document-order` gate honestly red |
| `docs/plans/unfulfilled-placement-intents.md` | The director's decision queue opened by the §2.1 audit | Deleting an inert prop moves zero pixels and needed no authorization; MIGRATING one to a spelling that works moves pixels and is a product call — every such row is queued here with a measured before/after rect, never taken unilaterally |
| `examples/gallery/scenarios/` + `tools/lune/studio_sync.luau` + `tools/studio/inject.luau` | Reusable Studio verification surface (contract §5) | Named deterministic scenarios; BindableFunction MCP API (list/steps/step/report/reset/freezeEnv); source-stamped injection |

Historical Phase 0 toolchain baseline (verified 2026-07-19): Rojo 7.7.0-rc.1,
Lune 0.10.4, Facet suite green, game suite 2377 green. Re-run the current doctor and
both suites before quoting present versions or counts.
