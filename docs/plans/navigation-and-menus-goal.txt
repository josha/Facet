/goal Build LuauUI's missing navigation and transient-menu constructs, fix two showcases,
and fix the sweep — per the binding brief at
GameStudio/ui/LuauUI/docs/plans/navigation-and-menus-brief.md. Read it in full, then the
media in docs/plans/reference-media/2026-08-16-roblox-app-navigation/ (f1/r1-r3 = the
Roblox app, the .movs carry the motion; hud-a/b/c = our own HUD failing). Trust §1's
today-state at 37e9dad; don't re-audit it.

TEN DELIVERABLES, D0 first then D1-D7 in order, each the substrate for the next; D8 is
independent. §2 is the spec; these are the decisions:
  D0.1 One suite run per sweep, not 161. MEASURED: 83.4s x 161 gate checks re-running it
     to grep an identical transcript ≈ 3h44m per sweep. Cache it
     in tools/test.sh (it already judges validity) behind a CONTENT fingerprint,
     add tools/suite_transcript.sh, repoint the checks. The exit code MUST ride with the
     cache or FORM A passes over a red suite; prove invalidation BITES.
  D0.2 Verify the greps still MATCH, not merely that they're anchored. check_manifest_
     integrity.py enforces the `✓.*` anchor but never runs the suite, so a rename stays
     anchored and matches nothing — found by hand twice. D0.1's transcript makes this
     free. MEASURED: 13 of 990 patterns match ZERO lines in green runs; fix those here or
     the check lands red. Traps in §2 D0.2.
  D1 anchored-surface seam — present against a source view's SCREEN RECT; safe-area edge
     flip/shift; arrow tail. Reuse syncPopupCatcher, focus_graph, modal zones.
  D2 Menu — freestanding ACTION menu (verbs + icons + dividers) on any view; right-click /
     long-press / gamepad / keyboard triggers. SUBMENUS SHIP, structurally UNBOUNDED with a
     diagnostic at depth >= 2 (SwiftUI caps nothing; HIG advises one). `icon` joins
     popup_button.Option.
  D3a help — pointer hover-dwell and gamepad/keyboard focus only. NOTHING on touch, and
     MUST NOT bind long-press (D2 owns it).
  D3b Callout — the f1 plate: an APP-PUSHED coach mark; eligibility rules, arrow tail, one
     at a time, non-blocking. Persistence is the caller's, never a save layer.
  D4 sliding indicator — animates between the selected children's rects. Underline and
     pill skins, one mechanism; reduced motion snaps.
  D5 TabView — selection Signal, placement from navPlacement, NESTABLE (test two levels),
     overflowing strip autoscrolls the selection into view, content lazy and EVICTED on
     switch (real disposal, memory-neutrality spec; never evict the strip).
  D6 Picker segmented — icons, a real VERTICAL pill (today axis="y" is the inline row
     list), plus D4's indicator. "Segmented as a tab bar" IS TabView.
  D7 HUD repair (hud-a/b/c) — ELISION MUST DISCLOSE, NOT DELETE. The rank ladder DID ship
     (UI.Region rank/forms/mayDrop, ADR-0025) and works; it just has no notion of where
     elided content went. §2 D7 has six fixes.
  D8 Playlist sort + resize, retire `table_columns`. Both ALREADY SHIP. Rating stays
     LOCKED, but two columns is degenerate — pinning the sole `fill` one leaves nothing to
     absorb the remainder, so add a THIRD `fill` resizable column, re-measured at 320x640
     in edit mode. RE-POINT its ~700-line spec FIRST.

DIRECTOR RULINGS 2026-08-16 are brief §6 — DECIDED, NOT OPEN. Don't relitigate submenu
depth, the help/Callout split, tab eviction, or D8's overrule of a prior round.

STANDING CONSTRAINT, the director on the dropdown — "this doesn't have to literally work
this way — the key thing is a pop-up menu that works across platforms." Copy the
capability, not the chrome: presentation resolves from space and live input class, never a
device name; every action reachable on keyboard, gamepad AND touch.

STAGE IT so each ships green: red tests, the change, then §5's verification block verbatim
(binding, never from memory). Rascal Rally rides along per root CLAUDE.md and §3.

Opus orchestrates, sonnet/opus workers, haiku reads.
