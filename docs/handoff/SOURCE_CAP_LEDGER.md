# Source-cap ledger — the modules inside the warning band

**What this file is.** `tools/check_source_size.py` fails any module in `src/`
that reaches **190,000** characters unless it has a row here, and fails outright
at **200,000** — the length at which Roblox refuses a `Script.Source` write and
a module silently stops live-syncing into an open Studio session
(`docs/lessons/the-200k-source-cap-is-on-writing-not-loading.md`).

**Why a ledger rather than a warning.** A warning is a thing you scroll past.
Before the band existed (release-candidate review MAINT-1, 2026-08-17) the
check's only size branch was `>= CAP`, so the first signal was the commit that
made a file unsyncable — and the remedy the checker's own header prescribes
(find a seam by the mutable-upvalue test, prove it with a live Studio A/B) is a
multi-hour mission, not an edit. Five modules were inside 10 KB of the cap and
`renderer.luau` was **350 characters** from it. The band converts that cliff
into a requirement to have done the thinking while it is still cheap.

**What a row must carry**, all three mechanically enforced:

1. **its size when the row was last touched** — within 2,000 characters of the
   file's current size, or the check fails and asks for both the number and a
   re-read of the analysis. A file that moved is a file whose seam analysis is
   worth looking at again;
2. **a seam analysis** — the one-way extraction candidates by name, or the
   recorded reason none exists. "One-way" is the checker header's own test: a
   block that reads no mutable upvalue of its host, so moving it cannot create a
   require back;
3. **the trigger that ends the deferral** — the condition under which the next
   extraction happens, not a date.

## The band

| module | size | seam analysis | next-extraction trigger |
|---|---|---|---|
| `src/controls/row_actions.luau` | 192,979 | `dump()` is 42 KB — 22% of the file — and is the strongest candidate in the framework: a dump builder reads state and writes nothing, so a `row_actions_dump.luau` taking the tray/edge/commit records as arguments cannot require back. It is NOT yet one-way as written (it closes over ~a dozen live upvalues), but unlike table's builders it needs no behavioural change, only an argument list. `doCommitAction` (10 KB) and `commitDestructive` (7 KB) share the irrevocable-commit state machine and must stay together with it. | Extract `dump()` on the next mission that changes the dump schema — the argument list is written for free when the fields are already being touched — or immediately if this file passes 196,000. |
| `src/render/renderer.luau` | 198,982 | **One seam TAKEN, 2026-08-17:** the prop-channel manifest (`BINDING_PROPS`, `STYLE_PROPS`, `STYLE_PROP_ORDER`, `BINDING_PROP_CLASSES`, `emitsBinding`, `DIRECT_PROPS` and their two load-time completeness assertions) moved to `render/prop_channels.luau` — 8.1 KB, module-level data plus one pure predicate, reading no `attach` upvalue at all. Remaining candidates, in order of honesty: `sameGeometryValue` (pure, ~600 chars — real but small); `findNode`/`rectsEqual`-class helpers (pure, ~1 KB combined). Everything else is the three entangled blocks the campaign already named — `solveAndApply` (28 KB), `ensureTree` (22 KB), `structuralSync` (10 KB) — each reading `handles`, `lastRects`, `lastZ`, `stats` and the solve caches, i.e. the mutable-upvalue test fails by construction. | **RE-RECORDED 2026-08-18 (wave TABLE, stage 1): 194,791 -> 198,982, which is 1,982 characters past this row's own 197,000 trigger and 1,018 from the 200,000 write cap — the closest any module in this repository has been since the cap was measured.** The growth is the ADAPT-FIX wave's (`1983392`, the non-pointer drag arm: 195,138 -> 198,982); this row was the one of the six that round did not re-record. The seam analysis beside it was re-read on re-recording and STANDS unchanged — `sameGeometryValue` and the `findNode`/`rectsEqual` helpers are still the only pure candidates, and `solveAndApply`/`ensureTree`/`structuralSync` still fail the mutable-upvalue test by construction. What changed is the deadline, not the analysis: the next extraction is `rect_pass`-shaped — lift the RECT WRITE half of `solveAndApply` (the diff-and-write loop, not the solve) once `lastRects` is owned by a record object rather than a closure upvalue, which is the same two-scalars-into-one-record move `table.luau` proved on 2026-08-18 above. **IT IS OWED NOW AND MUST PRECEDE the next change of any size to this file, not accompany it** — at 1,018 characters of headroom the next ordinary comment is what makes it unsyncable. |
| `src/present/presenter.luau` | 196,647 | `makeHandle` is 84,819 characters — 45% of the file — and the checker's own header already records the judgement that it is too entangled to split (it is the surface lifecycle: mount, render, focus, transitions, dismissal and the critical-screen fallback, all reading the presenter's live stacks). That judgement stands and is re-confirmed here. The one-way candidates outside it are small and real: `syncFocusVisuals` (7 KB) reads `focus` and the controller only; `refreshBody` (6 KB) reads the surface list. Neither is worth a file on its own; together with the dismissal region they would be a "surface lifecycle helpers" module of ~20 KB. | **RE-ATTRIBUTED 2026-08-20 (review round 1, ISSUE-3) — the 2026-08-19 re-record credited this growth to the wrong commit. Measured per commit: 194,563 at `d783648`; 195,908 at `1983392`, the ADAPT-FIX commit that also grew `renderer.luau` and left BOTH re-records undone; 195,908 still at wave TABLE's anchor `30cec7f`; 196,647 after wave TABLE. So wave TABLE wrote 739 characters, not 2,084 — the 1,345 before them are ADAPT-FIX's. THE CONSEQUENCE IS THE POINT: the 195,000 trigger had ALREADY FIRED before wave TABLE opened, so those 739 (`discloseScope`) went in WITHOUT the extraction preceding them, which this row's own words forbid. The writer read the stale recorded size and believed the trigger was 437 away — the identical failure the `renderer.luau` row documents, one file over, in the same week.** The growth is one seam and its reasoning — `discloseScope`, the contribution hook that lets a control widen the disclosure walk to its own row when the focus stop is a SIBLING of the disclosed label rather than its ancestor (a Table's row `Hit` beside `Row/Cells/...`). The seam analysis was re-read on re-recording and STANDS: `makeHandle` is still 45% of the file and still the surface lifecycle, and the one-way candidates outside it are still `syncFocusVisuals` and `refreshBody`. What has changed is that the extraction is now OWED rather than approaching — the trigger fired. (Previously: the trigger was 437 characters away, re-recorded 2026-08-18, R5 fix round 1: 190,326 -> 194,563.) The REVEAL wave took most of it; R5 fix round 1 added `claimFrameDriver` (~2.3 KB, the one-clock-one-driver claim that `motion_driver`'s private table could not see) and gave back the shadowing `isHiddenPath` copy. Extract the focus-visual + refresh pair when a third member joins them, or immediately if this file passes 195,000 — which is now the NEXT change of any size, and it must PRECEDE that change rather than accompany it. `makeHandle` is not a candidate at any size — if the presenter needs room, it comes from around `makeHandle`, never from inside it. |
| `src/controls/virtual_list.luau` | 192,195 | **Two seams TAKEN in the wave that put this file in the band (CAROUSEL, 2026-08-20), and they are why it is 348 characters in rather than 9,000.** The card arrangement went to `controls/card_rail.luau` (9.5 KB: the options refusal plus the perView/peek/extent derivation, one-way today — it reads nothing of the list, every input arrives through a nine-field parameter object), and the `snap` vocabulary/refusal/read went to `controls/scroll_snap.luau` as `modeReader`, shared with `newVirtualGrid` so the two collections cannot drift into two refusal messages. Three earlier seams stand: `virtual_window.luau` (the windowing index and its memos), `virtual_reorder.luau` (the drag model) and `native_scroll_binding.luau` (the mirror arbitration). **What is left, honestly ranked.** The HOSTED ROW-ACTIONS block (`hostedItemExtentIn` through `hostedRestoreFocusOnDisengage`) is **43,615 characters — 23% of the file** and by far the largest candidate; it is NOT one-way as written, because it reads six REASSIGNED scalars (`hostedMotionClock`, `hostedNow`, `hostedFocusGraph`, `hostedActionSystem`, `hostedPresentModal`, `hostedDismiss`) plus `boundController`, and a reassigned local cannot be shared by reference at all. That is the exact shape `table.luau` solved on 2026-08-18 — the six become one record the two halves both hold — so the technique is shipped and proven, not theoretical. `buildFocusGroups` (10,374) reads `mountedScrollPath` and the hosted engines and fails the same test. `dump()` (4,074) is the strongest SMALL candidate for `row_actions.luau`'s reason (a dump builder reads state and writes nothing), but it closes over roughly twenty upvalues and is not worth a file on its own. | **NEW ROW 2026-08-20 (wave CAROUSEL): 181,028 -> 192,195, and the number was WRONG for a day before this line corrected it.** The wave spent 11,167 characters on scroll snapping and the compact card paradigm, and gave 9.5 KB of it straight back through `card_rail.luau` — without that extraction the file would have been 201,691, i.e. PAST the write cap. **THE RECORDED SIZE READ 190,348 WHILE THE FILE WAS 192,195** — a drift of 1,847, which is 92% of the checker's 2,000 allowance and therefore invisible to `check_source_size`; the wave's own reviewer found it, not the gate. The cause is a measurement taken in the MIDDLE of the work: the row was written when the feature landed, and the file then gained the snap machine's test-only travel reader and a `stylua` pass. **The discipline this row now carries: measure LAST, after the final format pass, or a ledger row is a claim about a file that has since moved.** It is the third occurrence in this document of a row telling the truth about a file that had already grown, and the first where the author of the row and the grower of the file were the same person. Extract the HOSTED BLOCK (`controls/virtual_list_hosted.luau`, with the six reassigned scalars as one `hostedDelivery` record) on the next mission that changes hosted row actions, or immediately if this file passes 193,000 — **which is 805 characters away, i.e. one ordinary comment block.** Read the three rows above this one before deciding that is a comfortable margin: `renderer.luau`, `presenter.luau` and `solver.luau` each crossed their own trigger exactly this way, by a change nobody thought was large. Treat the trigger as ARRIVED: the next change of any size to this file is PRECEDED by the extraction, not accompanied by it. `dump()` (4,074) follows it if the room is still needed. |
| `src/client/screen_target.luau` | 193,714 | **Five seams already TAKEN** — `screen_chrome`, `screen_scroll_indicators`, `screen_paint`, `screen_presentation`, `screen_pointer` — so this file's extraction technique is proven and its cost is known: every new sibling also owes a part in `tests/lib/adapter_source.luau`, whose `live()` is what the source-scanning specs read. **What is left splits cleanly in two.** The module-level prefix (14 KB, everything before `screen_target.new`) holds the genuinely one-way remainder: the engine-vocabulary tables `CLASS_TO_INSTANCE`, `TEXT_ALIGN_MAP`, `SCALE_MODE_ENGINE`, `TINT_REPAINTING_PROPS`, the pure `toColor3` and `fontFaceFor`, and the three stage/foreign disposal helpers — ~7 KB that reads no closure upvalue at all, because it is declared before the closure exists. Everything large is INSIDE the one `screen_target.new` closure and fails the mutable-upvalue test by construction: `setProp` (25.8 KB), `syncTags` (20.6 KB), `create` (18.6 KB) and `setFocusVisual` (11.6 KB) each read `style`, `nativeMode`, `instancesByPath`, `assertBespokePaint` and the live handle map. | **RE-RECORDED AGAIN 2026-08-21 (native-default fix round 1): 193,992 -> 193,714, and it is the row's own eight characters being paid back.** The round below left this file 8 from the trigger; a fresh-context review of the default flip then asked for the paint DECISION to be extracted so the branch could have a headless witness, and the extraction gave 278 characters back in the same edit. `native_style.paintPlan` now owns the default, the capability probe, the escape hatch and the transitions seed, so `screen_target.new` holds one call and a `plan.native` test where it held a resolve, a two-clause gate and a three-line transitions expression. **286 characters from the trigger**, and the vocabulary module is still the one owed — the seam that answered this review is not the seam this row is about. (Previously: the paint-family lockstep round, ADR-0040 row B-17: 193,314 -> 193,992, of which 678 are this round's — and that leaves EIGHT characters before the 194,000 trigger below, so the vocabulary module is now OWED BY THE NEXT ROUND THAT OPENS THIS FILE, not by a future one.** The round's own edit was three lines of code and its comments were cut twice to land under the trigger, which is the shape the trigger is meant to produce: the derivation itself lives in `themes/snapshot.paintForDisplay` and this file only spends it. (Previously: the native-paint default flip, ADR-0040 row B-15: 193,077 -> 193,314, of which 108 are this round's.) The flip's own edit went the RIGHT way for this file — the default-resolution branch left the closure for `native_style.resolveOpt`, which is a seam rather than a line, and it is why a +237 net drift is the whole cost of a product-behaviour change. The seam analysis beside this row was re-read and STANDS unchanged. **686 characters from the trigger.** (Previously: 190,697 -> 193,077, R5 fix round 1 — the REVEAL wave plus `adapter.dispose`, the target teardown seam `client.host` needs, 923 from the trigger.) Take the **vocabulary module** (`screen_vocabulary.luau`, ~7 KB, one-way today and costing only an `adapter_source` part) on the next mission that touches any of those tables, or immediately if this file passes 194,000. `setProp`'s per-class property chain is the only large candidate and it is NOT one-way as written — it becomes so when the paint/style context it reads is a parameter object rather than four upvalues, which is a refactor of the adapter rather than a move, and must PRECEDE rather than accompany the change that needs the room. |

## Cleared the band

A module that lands under 190,000 stops being enforced — `check_source_size`
reads a row only for a file inside the band. The row stays anyway: the next
reader of a file that took a hard split deserves the analysis and the seam that
is next, not a blank page and a re-derivation.

| module | size | seam analysis | next-extraction trigger |
|---|---|---|---|
| `src/controls/table.luau` | 189,197 | **THE BUILDER SEAM WAS TAKEN, 2026-08-18, and it is the row this table was waiting on.** `rowBlueprint` + `rowPointerHandlers` — with `isHandleZonePress`, the ≡ handle's mouse-presenting wrapper, `cellContentFor` and the drag-promotion token only they read — went to `controls/table_rows.luau` behind a 34-field parameter object, exactly the shape the previous trigger predicted would make them one-way. **What it cost was TWO SCALARS**: `rowDrag` and `suppressActivatePath` were reassigned locals, and a reassigned local cannot be shared by reference at all — they are now one record (`rowGesture`) held by both halves. The mutable TABLE they also share (`rowActionsByKey`) cost nothing, because a table already IS a reference. `tests/table_rows_seam.spec.luau` mechanises the property: READ == DECLARED == PASSED as three set comparisons over the live sources, the shared write surface pinned to those two names, and the require asserted not to come back — nine cases, three mutations proved to bite. **The two earlier seams stand**: the DIR-4 width clamp (`table_columns.luau`, 2026-08-17) and the pure decisions that left before it (`virtual_extents`, `row_capability`, `row_actions_state`, `native_scroll_binding`, `text_fit`). What is left is still ONE `newTable` closure, and every large block in it still reads mutable instance upvalues — but the technique that answers that is no longer theoretical in this file, it is shipped and tested. | **RE-RECORDED 2026-08-18 after wave TABLE stage 2: 173,384 -> 185,949, which is 14,051 characters of headroom and 4,051 below the 190,000 warning line.** The column priority-collapse paradigm (ADAPT-18/19) spent 12.6 KB of what the builder extraction bought, and it paid part of that back the same day: the disclosure — the plate's facts, its list and its sheet — went to `controls/table_disclosure.luau` (10.9 KB) the moment writing it in place put this file back inside the band. That is the technique working as intended rather than a surprise, and it is the third seam this file has taken. **The margin is now thin enough to name: the NEXT feature of this size must extract before it writes.** This row is now advisory rather than enforced (`check_source_size` reads a row only for a file inside the band), and it is kept because the next reader deserves the analysis rather than a blank page. **THAT SEAM WAS TAKEN THE SAME DAY.** The trigger it set ("this file passes 185,000") fired on the very next feature, which is what a trigger is for: the header band — the headings, their sort marks, `gripFor`, `forwardUnclaimedGripTap` — went to `controls/table_header.luau` (18.3 KB), and the analysis's own prediction held exactly: `resizeDrag` was the one reassigned scalar and became a record (`resizeGesture`), while `renderController` — reassigned too, but only READ across the seam — took an accessor instead, the `screen_paint` split. THE NEXT SEAM, and it is smaller: what is left inside `newTable` is the drag MODEL (`applyDragPosition`/`liveDragRects`/`cancelDrag`/`insertSlotAt`, ~4 KB), the selection and grab model (~6 KB), `api.handleActivate` (~10 KB) and `buildFocusGroups` (~5 KB). The activate ROUTER is the one with a shape — it is a chain of independent path matchers, each of which could be a rule in a table — but it reads eight mutable upvalues and moving it means passing them, which is a refactor rather than a move. Trigger: this file passes 188,000, or the next mission that adds a route to `handleActivate`. **RE-RECORDED 2026-08-20 (review round 1, ISSUE-3), and the correction is the finding: this row said 181,588 while the file was 185,190 — a drift of 3,602, which is 1.8x the allowance that would have FAILED it in band, and invisible to the checker only because the split had moved this row OUT of the enforced band. That is exactly the defect this wave's own opening commit corrected on `renderer.luau` — "the ledger telling the truth about a file somebody else grew" — reproduced on the file the wave itself grew. The number above is now the size after the review's fix round.** **AND THE 188,000 TRIGGER HAS FIRED: 189,197, which is 811 below the warning line.** The fix round spent the remaining slack on the one-state disclosure contract (review HIGH-1) and the reasoning that has to travel with it. THE NEXT CHANGE OF ANY SIZE MUST BE PRECEDED BY AN EXTRACTION, not accompanied by it — and the candidate is now cheaper than anything above it, because this round created it: the COLLAPSE/DISCLOSURE STATE (`hiddenColumns`, `anyColumnHidden`, `moreLabel`, `columnHidden`, `columnById` and the `MoreWhen` band, ~4 KB) belongs in `controls/table_disclosure.luau`, the file already named for it, and is one-way TODAY: it reads `columnLayout`, `scope`, `core`, `UI` and `bandPadding`, and NOT ONE mutable scalar — so it needs neither a record nor an accessor, which no previous seam here could say. |
| `src/layout/solver.luau` | 186,040 | **THE MEASURE-FACTS SEAM WAS TAKEN, 2026-08-21, and it is the row this file was waiting on.** `layout/measure_facts.luau` carries what the analysis below predicted, to the name: `Dim` and the dimension readers (`CONTENT`, `dim`, `SPACER_FILL`/`mainDimOf`, `sides`), `textTypography` + `recordTextFacts` + the text-fact record, and the memo's per-node classification (`MEMO_ARM_DEPTH`, `HEIGHT_COUPLED_KINDS`, the `PLAN_*` verdicts and `memoPlan`). **THE PREDICTION THAT MATTERED HELD: the argument list was already written.** `SHRINK_DEPS` and `GRID_DEPS` had been threading four of these into `./shrink` and `./grid` as explicit arguments since those splits, so this is the first seam in this document that cost NEITHER a record (`table.luau`'s `rowGesture`, `virtual_list`'s `hostedDelivery`) NOR an accessor (`table_header`'s `renderController`) — every input already arrived on the per-solve `ctx` or as a parameter. **Two corrections to the analysis it replaces**, both found by writing it: `definite` and `roundRect` DO NOT EXIST in this file (`roundPx` comes from `../num` and there is no `definite`), and `Dim` had to travel WITH `dim`/`mainDimOf` or the new module would have re-declared it — the solver re-exports it (`export type Dim = measureFacts.Dim`) so there is one schema rather than two, which is the same copy-goes-stale rule the seam exists to keep. **WHAT IS LEFT IS STILL ONE FILE-SCOPED MUTUAL RECURSION**: `measure` / `measureUncached` / `contentSize` / `arrange` / `arrangeStack` all read the per-solve `ctx` AND call each other, so none of them is one-way at any size, and `chosenCandidate` + the composition resolution (~12 KB) read `ctx.fitProbe`, `ctx.fitCuts`, `ctx.compositions` and `ctx.metrics` and call `measure` back — moving either creates a require cycle rather than a seam. The next candidates are real but small, and both are pure: `flowPartition`/`flowPlan` (~3 KB — `flowPartition` is ALREADY exported as `solver.flowPartition` because it reads no node and no ctx, which is the whole test), and the placement arithmetic `alignOffset`/`offsetPx`/`offsetFill`/`distributionOf`/`ANCHOR_FACTORS` (~2 KB, read by the arrange branches and by nothing else). | **RE-RECORDED 2026-08-21 (the solver split round): 197,810 -> 186,040, which CLEARS THE 190,000 WARNING LINE by 3,960 and leaves 13,960 characters to the write cap.** The file entered this round with **2,190** — the number the previous trigger recorded as the whole remaining margin — and the extraction PRECEDED the round's own changes rather than accompanying them, which is what this row's previous wording demanded and what `renderer.luau`, `presenter.luau` and `table.luau` each learned the hard way one file over. **The evidence, because a split is a behaviour claim**: the full suite is 6,915 -> 6,928 passed with the verdict SET diffed line-for-line against a content-pinned baseline — the thirteen added rows are the seam spec's own and NOTHING ELSE MOVED, i.e. zero re-verdicts; and a differential oracle over 800 seeded trees (rects, `compact`, `textState`, all thirteen published text facts, the diagnostic set and the work counters, 10,838 comparison lines) is byte-identical between the pre-split and post-split trees. `tests/measure_facts_seam.spec.luau` mechanises what keeps it one-way: READ == DECLARED == HOSTED as set comparisons over the live sources (this seam has no parameter object — everything arrives on the solve's own `ctx` — so the third set is the solver's own `Ctx` block), the four-name shared write surface, EXPORTED == BOUND in both directions, the one consumer, the one-way require, and that the module holds no module-level mutable state. Eleven mutations proved to bite, and TWO OF THEM WERE FINDINGS RATHER THAN DRILLS: the "bound but unused" rule was decoration until the binding line (`local X = measureFacts.X`, which names X twice) was excluded from its own count, and once it could report it reported — `PLAN_KEYED`'s only reader left with `memoPlan`, so the export was dropped. This row is now ADVISORY rather than enforced (`check_source_size` reads a row only for a file inside the band) and is kept because the next reader deserves the analysis rather than a blank page. Trigger: this file passes **188,000**, or the next mission that changes the measure recursion or the composition resolution — and at that point the two pure candidates named beside this are ~5 KB together, so the extraction must PRECEDE the change rather than accompany it. |

## Modules approaching the band

Not required to have a row, listed so the next reader knows where the pressure
is going next. Re-measure with `python3 tools/check_source_size.py`.

