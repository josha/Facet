# Your list — 2026-08-15

Five things. Two are quick, one matters more than the rest, and two are waiting
on me to hand you something.

---

## 1. Connect Rojo again — should just work now

**Do:** in Studio, Rojo → Connect → port **34873** (Showcase) or **34874**
(Performance Lab).

**Why it failed before:** you had a new plugin talking to old servers. The
plugin was reinstalled at 7.7.0, but the two `rojo serve` processes had been
started hours earlier with a stale 7.7.0-rc.1 binary that was shadowing the
real one on `PATH`. 7.7.0 answers `/api/rojo` in a different wire format, so the
new plugin read a number where it expected a table — that is the
`attempt to index number with 'protocolVersion'` you saw. Both servers were
restarted at 11:31 on the correct binary.

**If it fails again**, the servers may have been stopped. Restart them from
`GameStudio/ui/Facet`:

```
rojo serve examples/showcase.project.json --port 34873
rojo serve examples/performance.project.json --port 34874
```

Check the version matches your plugin:
`curl -s localhost:34873/api/rojo | strings | grep -oE '7\.7\.0(-rc\.[0-9]+)?'`

---

## 2. Touch-test on a real phone — **the important one**

**Do:** open the showcase on your handset and try these three, with your finger:

1. **Swipe a mail row sideways** (the `row-actions` demo) — the action buttons
   should reveal, and swiping a second row should close the first.
2. **Drag a column divider** (the `table-columns` demo) — the column should
   resize while you drag, and the divider should be easy to grab.
3. **Scroll a long list** and watch the right-hand edge — nothing should sit
   under the scrollbar.

**Why this one matters most:** we cannot test real touch. Studio's device
simulator has a broken pointer mapping — `GetMouseLocation()` freezes and
returns the same position for different aim requests — so every gesture we have
"verified" was actually at desktop size. The worst defect of this whole session
was a list that could not be swiped **at all** on a phone, and it looked perfect
on desktop. This is the exact blind spot.

**Tell me:** what happened, and if something feels wrong, roughly where on the
screen you were touching. "The buttons appeared but the row snapped back" is
plenty — I do not need numbers.

---

## 3. Re-capture `table-unified` — the last one missed

**Do:** Performance Lab → Play → cycle to **`table-unified`** → press
**Profile** → let it run 3–4 laps → `Cmd+F6`, then `Ctrl+P` to pause →
**Dump → Dump in binary format** → send me the file.

**Why:** your last capture was taken correctly, but the ~1.3 s dump window
happened to land in a quiet stretch instead of on the selection work it was
testing. Bad luck on timing, nothing you did wrong. The claim being tested is
that selecting 201 rows — including rows not currently on screen — causes **zero**
re-layouts.

**Before you dump, glance at the status line.** It now prints the work done per
lap. If it reads `arrange=0`, the capture will be worthless — stop and tell me
instead of spending the dump.

---

## 4. Two new profiles — **waiting on me**

I am building two workloads aimed at the two costs we know about and have never
been able to act on:

- **`arrange`** — the single biggest cost on your phone (9–10 ms every time it
  runs, worst case 299 ms). Right now it is one opaque blob: a capture can say
  "this is expensive" and nothing more. The new workload is designed to show
  *which part* is expensive.
- **a list edit** — adding or removing one row currently costs 26 full layout
  passes even though nothing structural changed. Never investigated.

**You do nothing until I hand you a note** telling you which workload, which
button, and what the status line should say first. Same shape as the note you
already used.

---

## 5. Nothing else

There was a ledger row I described as needing you. That was me phrasing my own
work as your task — I will close it.

---

## Where things stand

Suite **5355 passing, 0 failing**; Rascal Rally **3226 passing, 0 failing**.
Every source file is under Roblox's 200,000-character limit, so live sync works
for all of them. Two agents are running: extracting the grid out of the layout
solver, and building the two profiling workloads above.
