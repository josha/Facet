# SDD ledger — plan: docs/superpowers/plans/2026-09-06-facet-parity-D.md

Goal: docs/plans/2026-09-06-facet-parity-D-goal.md. Authority: FacetBench docs/studio-runs/2026-09-03-facet-parity.md §after.
Base (Facet): b315dc34 (main, Plan C merged). Branch: `facet-parity-d` (created 2026-09-06 before any edit).
FacetBench: da1b433. RascalRally: 218aa83.
Floors at base: Facet suite 8683/0, RR 3599/0. Only ratchet up. renderer.luau 197,472 (STOP 197,500). solver.luau 187,617.
Operating rules: every commit — `tools/test.sh 8683`, `tools/verify.sh affected --jobs 1` FOREGROUND (pre-commit), `python3 tools/check_source_size.py`, stylua; RR lockstep on every src change; no public API/behaviour change; never amend; nothing merged/pushed; one lune at a time (`pgrep -x lune`); commit BEFORE the long gate, evidence file per gate, ledger row as soon as the commit exists.

## Scouting (2026-09-06)
Four read-only scouts → S1-commit-residue.md, S2-settle-drain.md, S3-structural-mount.md, S4-harness.md (copied here). Attribution at HEAD: attr-head/ (attr-*-L.txt = HEAD all five; d1-*-armA/armB = memoPlan trial; d1-probe-solver.diff = the in-measure probe, reverted; attr-battle_hud-L-noprof.txt = hooks control).

## §D1 finding (the ranking evidence)
`measure_facts.memoPlan` is memoised PER SOLVE (`ctx.memoPlans`) and recurses the whole subtree of a height-free vstack: 0.768 ms + 303 KB of the 0.949 ms `battle_hud L updateItem-hp` (78 %), 12 calls. Blind to wrap profiling because the solver binds it at load. Trial (node-literal cache): battle_hud hp 0.984→0.363, setState 1.105→0.405, war_room setState 1.471→0.451, killfeed hp 0.296→0.195. The "seven flat cw.* walks" hypothesis is REFUTED at HEAD (six read 0.001–0.002; harvest 0.066).

## Execution

### Task D0 — instruments + §D1 registered
- FacetBench `0c2dccd` (lib_attr `mf.memoPlan` early shim + nine named sub-measure wraps, `d1_uncached`, `d1_mount`, README, §D1 in the run doc); `tools/check.sh` green (d0-check.txt). FacetBench `1e9369e`: `attr` statFields `lastPlanVisits`.

### Task D1 — memoPlan on the node literal
- Plan committed `7c65c97e` (docs only, after D1 by accident of ordering — the plan was written first, the commit landed second).
- **Facet `7b97dd80`**: `node.mPlan` cache, `ctx.planVisits` → `work.planVisits` → `stats.lastPlanVisits`; `tests/plan_cache.spec` (4 cases: counter, noop, production flip witness, negative control). Red arm (cache read disabled, counter kept): `plans=3011` on deep_stack_scene → green `plans=4 builds=4` (text) / `plans=3 builds=3` (width), `measured` unmoved (5 / 6). measure_facts seam write-set pin → `planVisits`.
- Gates: `tools/test.sh 8683` → **8,687/0** (d1-test.txt); `verify.sh affected --jobs 1` PASS_PARTIAL, 0 FAIL_RECOVERABLE, 496.7 s (d1-verify-affected.txt); stylua clean; check_source_size PASS (solver 187,903, renderer 197,472 untouched); differential md5 `3db66551b80fe677d314f2092b0a0038` both trees, 0 diff lines (d1-diff-{before,after}.txt).
- RR `d625688`: `plans=9 builds=9` rider in facet_measure_fanout_contract; suite **3,599/0** (d1-rr-suite.txt); no existing pin moved.
- ABBA arms: A `b315dc34`, B `7b97dd80`, C = B with the cache read disabled (uncommitted patch in the C worktree) — interleaved B A A B C, 3 passes, all five workloads → d1-abba/. Live drive + RR canary owed before §D2 is written.
- ABBA (d1-abba/): battle_hud hp A 1.022 / C 0.984 / B 0.364; setState 1.446→0.418; war_room setState 1.683→0.490; killfeed hp 0.290→0.199; damage_fountain + nameplates 0.98–1.04 (controls). Safety pair identical on all arms.
- LIVE (d1-studio-live.txt, fresh VM per workload, settled): battle_hud hp 1.539→0.621, setState 1.602→0.624, [S] 2.952→0.949, addItem [S] 5.667→3.476, removeItem 4.011→2.719; war_room setState 1.942→0.679, power 2.883→1.048, tier 2.493→0.772, addItem 8.109→5.340, removeItem 18.6→15.8, reorder 36.5→32.6; killfeed hp 0.448→0.314. Wide-key [S] rows unmoved (49.5/69.6/77.6) — D3.
- RR canary (d1-rr-canary.txt): 57 nodes, 56 moved, 8 distinct Position writes (hosts only), kid offsets byte-equal, frame p50 17.65, 0 Facet errors.
- FacetBench `§D2` committed. **Task D1 COMPLETE** at Facet 7b97dd80 / RR d625688 / FacetBench (§D2 commit).
- Worktrees: <scratch>/d1/{A,B,C}/{Facet,FacetBench} still present (A=b315dc34, B=7b97dd80, C=B+cache-off patch). Remove at close.

### Task D2 — the three O(children) scans on a leaf edit (dispatched to an implementer, opus)

#### D2a — the per-index child patch (SHIPPED, Facet `7a17a43d1` / RR `dec1733`)
- Mechanism: when every kept-array gate term holds and only the dirty-child scan refused, `toLayoutNode` `table.move`s the previous array into a fresh one and re-runs itself only at the dirty indices. Indices come from a LAZY index over the SPLICED flat child list (`kidIndex` path->idx, `kidMounts` idx->mount node, `kidRegions` flat triples), carried on the entry with the array and dropped when the array is rebuilt. The DIRTY SET is walked (O(depth)), not the children; budget `max(4, n // 4)`.
- Validation, three layers, none walking: container children identity (T15's own term); each spliced region's children table by identity AND length (exact — `mount.luau` only replaces wholesale or `table.remove`s); and on the index-building pass `store.byNode[child].built == childArray[n]`.
- The post-children fold and the placement audit's fast reject became POPULATION COUNTS on the entry (`kidScroll`/`kidComposition`/`kidInside`/`kidPlaced`) — counts, not booleans, because an OR cannot be undone. The kept arm now answers all four with no walk at all.
- New store field `patchRefused` (NOT on `controller.stats()` — renderer is 28 chars from the STOP line; the spec drives `layout_node.build` directly).
- Counters: `deep_stack_scene` 1000-row LEAF write `lastBuildChildVisits` 1003 -> 3, `lastNodeBuilds` 4 and `lastLayoutNodes` 3002 UNMOVED. Direct drive 1000 children, one dirty: visits 1000 -> 1.
- Spec `tests/build_index_patch.spec.luau` 19 cases (owed list of 12 channels, three negative controls, budget, collecting build, both directions of all four counts, the production ForEach-rows shape, full-solve oracle x 9 views). Mutations: 15 run, 12 bite, 3 recorded nulls (cost-only) — `d2a-mutations.txt`.
- Moved pins re-read off the run: `build_children_reuse` "a DIRTY CHILD refuses the arm" visits 8 -> 1; its headline 1003 -> 3. `node_reuse`'s mounted-node call audit gains the three new file-locals.
- Gates: `tools/test.sh` **8,706/0** (d2a-test.txt, floor ratchets 8,687 -> 8,706); `verify.sh affected --jobs 1` PASS_PARTIAL, 0 FAIL_RECOVERABLE, 424.6 s (d2a-verify-affected.txt); `check_source_size` PASS (renderer 197,472 UNTOUCHED); stylua clean; differential md5 `3db66551b80fe677d314f2092b0a0038` (d2a-diff-after.txt).
- RR `dec1733`: `facet_measure_fanout_contract` `visits(warm)` 15 -> 10 (rider says why: `SponsorListModel.rows` hands a NEW children table so identity fails at the list itself), `builds=9`/`plans=9`/cold `visits=36` unmoved; suite **3,599/0**.

#### D2a — ABBA (paired arms, `d2a-abba2/`; the first matrix `d2a-abba/` is VOID — see below)
Arms: A = Facet `7b97dd80` + FacetBench `107dab8`; B = Facet `7a17a43d` + FacetBench `107dab8`; C = B with `patchAt = nil` after `patchIndices` (the index and the dirty scan still run, the patch does not). Order B A A B C per workload, `attr <wl> L 3`.

| class | A | C | B | B-A | toLayoutNode A→B | bcv A→B |
|---|---:|---:|---:|---:|---|---|
| battle_hud updateItem-hp | 0.363 | 0.464 | **0.229** | −37.1 % | 0.147 → 0.013 | 1126 → 2 |
| battle_hud setState | 0.413 | 0.535 | 0.376 | −9.2 % | 0.143 → 0.114 | 1126 → 2 |
| war_room setState | 0.486 | 0.657 | **0.291** | −40.2 % | 0.215 → 0.032 | 1471 → 2 |
| killfeed updateItem-hp | 0.190 | 0.225 | 0.155 | −17.9 % | 0.050 → 0.045 | 333 → 2 |
| killfeed setState | 0.204 | 0.248 | 0.170 | −16.2 % | 0.045 → 0.009 | 331 → 1 |
| war_room reorder | 27.085 | 27.452 | 27.145 | +0.2 % | 0.506 → 0.546 | 1469 (unmoved) |
| war_room removeItem-items | 12.380 | 12.635 | 12.191 | −1.5 % | 0.546 → 0.577 | 1470 (unmoved) |
| nameplates removeItem-plates | 0.454 | 0.476 | 0.490 | +8.0 % | 0.062 → 0.080 | 247 (unmoved) |
| damage_fountain updateItems-numbers | 1.502 | 1.504 | 1.498 | −0.2 % | 0.406 → 0.422 | 200 (unmoved) |

- **Safety pair `lastLayoutNodes` and `lastNodeBuilds` IDENTICAL on all three arms, every class, all five workloads.**
- Arm C is SLOWER than A on every class the patch serves (0.464 vs 0.363 on `battle_hud hp`): the index build and the dirty-set walk are a real cost, and B beats both — which is what says the win is the patch and not the plumbing.
- No-regression gate MET: `reorder` +0.2 %, `removeItem-items` −1.5 %.
- **Attributed cost, recorded not chased:** structural (collecting) classes pay ~+5 % on `nameplates removeItem-plates` (A 0.454 / C 0.476 / B 0.490; the B-vs-C gap on identical code paths is the ~3 % noise floor for that class, so the shared overhead is ~+0.022 ms). The mechanism is the FOURTH per-child read on the full-rebuild arm: the old post-children fold read three flags per child, `appendChild` now reads those three plus `placementProps`. The lever that would remove it is booked, not built: cache `inertPlacement` on the entry and, on the patch arm, run the audit only when the prior result was non-nil or a REBUILT child carries a placement prop — exact, O(dirty), and it drops `kidPlaced` entirely. The class is far under its structural target either way.
- ⚠ **A measurement trap, recorded:** the FIRST matrix (`d2a-abba/`) reused D1's arm-A worktree, whose **FacetBench was at `0c2dccd` while B and C were at `107dab8`** — an unpaired harness. It reported `battle_hud removeItem-damage` A 1.504 → B 1.712 (+13.8 %) and `removeItem-items` +3.9 %; with the harness paired both fall inside the A/A and B/B spread (removeItem-damage B1 1.739 / B4 1.531). The rule the campaign already has — paired worktrees per arm — covers the BENCH repo too, not just Facet.

#### D2b — `cw.harvest`'s descend list from the walked set: **KEPT NULL, BLOCKED** (nothing committed)
Two independent findings, both read off the source at `7a17a43d`:
1. **The plan's premise is false at this HEAD.** `walkedIds` never leaves the solver: `grep -rn walkedIds src/` gives `solve_ctx` (the field) and `solver.luau` (the writer plus the diagnostics replay that reads it). It is NOT on the `work` record, `rect_pass` does NOT prune by it, and `commitWalks.harvest(root.node, result.rects, commitScopeEnabled, builtDirty, paintScale, metrics, prefOffset, builtCommitDirty)` does not receive it. Routing it there costs renderer characters (a new argument at the call site, ~40+), and the renderer is at 197,472 of the 197,500 STOP with 28 left. Per the task's hard rule, that stops the sub-lever. (A zero-renderer-char channel does exist — `render_stats.publish` runs BEFORE the commit span and `commit_walks`' ctx already holds `stats` — but it would put an internal set on the `table.clone`d public `stats()`, which is a public-API change the task forbids.)
2. **The probe-filter form was already built, measured and refused in Plan C.** `commit_walks.luau`'s `buildDescend` header records it verbatim: C14 was written as exactly this set-membership filter, built at `48f010cd`, and produced **the identical probe count on every class** — a child a walked-set filter may prune must also own an entry and must also have been committed before, both of which are the hash lookups the branch already does, so the membership test is a third lookup deciding nothing. `cw.harvest` 0.098 → 0.078 ms with it against **0.074 ms without**, and +11 %/+14 % on the two structural classes.
3. What is left is a different lever: build the descend LIST from the walked set instead of scanning the children — which removes `scanCount` (1,137) rather than `probeCount`. It needs a per-container index over the container's MOUNT children (D2a's `kidIndex` is over the LAYOUT children, which are the SPLICED list — a different indexing), i.e. a new cross-commit cache in `commit_walks`, plus the routing above. Booked with its size stated; not built.
`cw.harvest` therefore stands at 0.067-0.068 ms on `battle_hud updateItem-hp` and is the largest single remaining term on that class.

#### D2c — the arrange replay's two sibling walks (IN FLIGHT; gate cleared before the spec was written)
- Mechanism: `render/layout_node` stamps each container with `kidToken` — a FRESH table whenever it rebuilt the children array, the SAME table on every build that kept (T15) or patched (D2a) one. The replay records it with the rest of its key (`cmemo.pToken`) and, when it matches AND the recording pass filed no containment finding (`cmemo.pNoted ~= true`), enumerates the DIRTY SET through an id->index map built once per token (`cmemo.pIdx`/`pIdxToken`) instead of scanning the siblings. The commit walk takes the same short list, in the same ascending document order.
- New counter `ctx.placeScanned` -> `work.placeScanned` -> `stats.lastPlaceScanned` (zero renderer chars: `render_stats.publish` reads `result.work`). `lastPlaceSkipped` KEEPS its old meaning, so none of the eleven pins in `stack_replay.spec` that read it moves.
- **Profile gate, measured BEFORE the spec was written** (main FacetBench at `107dab8` against the working tree, `attr battle_hud L 3`): `span:Facet/arrange` **0.065 -> 0.027 ms** (−0.038, gate is ≥ 0.03) and the class p50 **0.229 -> 0.192**. `lastPlaceSkipped` 1124 unmoved, `lastArranged` 5 unmoved, `lastLayoutNodes` 5127 unmoved, `lastMeasured` 6 unmoved.
- Moved pins re-read off the run: `solve_ctx_seam` declared/written 66 -> 67; `stack_seam` NODE_FIELDS gains `kidToken`, ctx WRITES gains `ctx.placeScanned` (and the write scan became a SET rather than a transcript — the indexed arm gives each counter one site per exit), module-level locals gain `function` (the crude scan reads the keyword of `local function candidateIndices`); `translate_arm`'s arrange node-read list gains `kidToken`; `structural_scope`'s publisher PROXIMITY heuristic 1,500 -> 1,600 (reads 1,501 off this run).

#### D2c — SHIPPED, Facet `4f906a5c` / RR `3f1a43c`
- Spec `tests/stack_replay_candidates.spec.luau` (7 cases): the owed list of ten channels, the two counter headlines (`placeScanned` 2,000 -> 3 on a 1,000-row list, 4 on a leaf text write), the SPLICE the scan was written for, the zero-dirty-children shape proved byte-equal against a cold surface, and a full-solve differential over the nine device views on three fixtures. Mutations 9 run / 6 bite / 3 nulls (`d2c-mutations.txt`); N2 (the token gate dropped) reddens **26** cases across the suite including "a CLEAN child whose INDEX moved under a two-region splice refuses" — that is the defect this mechanism could have shipped, and it is caught.
- Gates: `tools/test.sh` **8,713/0** (d2c-test.txt; floor ratchets 8,706 -> 8,713); `verify.sh affected --jobs 1` PASS_PARTIAL, 0 FAIL_RECOVERABLE, 497.9 s (d2c-verify-affected.txt); `check_source_size` PASS (renderer 197,472 UNTOUCHED — D2c spends ZERO renderer characters: the counter rides `work` into `render_stats.publish`); stylua clean; differential md5 `3db66551b80fe677d314f2092b0a0038` (d2c-diff-after.txt).
- RR `3f1a43c`: `facet_measure_fanout_contract` gains `placeScanned=2` beside the `placeSkipped=0`/`arranged=24` pair (read off the run; the list refuses the replay outright, so the two are the small stacks inside a row that DID replay through the short list). No existing pin moved — `lastPlaceSkipped` keeps its meaning by design. Suite **3,599/0**.
- ABBA (`d2c-abba/`), arms A = `7a17a43d` (D2a), B = `4f906a5c`, C = B with `order = nil` after `candidateIndices` (the token compare and the index build still run, the short list does not). Order B A A B C, all five workloads, FacetBench `107dab8` on every arm.

| class | A | C | B | B-A | span:arrange A→B |
|---|---:|---:|---:|---:|---|
| battle_hud updateItem-hp | 0.231 | 0.236 | **0.194** | −15.8 % | 0.064 → **0.027** |
| battle_hud setState | 0.375 | 0.386 | 0.336 | −10.5 % | 0.052 → 0.068 |
| war_room setState | 0.295 | 0.303 | **0.256** | −13.4 % | 0.065 → 0.029 |
| killfeed updateItem-hp | 0.155 | 0.162 | 0.148 | −4.5 % | 0.033 → 0.039 |
| killfeed setState | 0.174 | 0.172 | 0.168 | −3.4 % | 0.015 → 0.009 |
| battle_hud addItem-damage | 1.798 | 1.742 | 1.702 | −5.4 % | |
| war_room reorder | 28.012 | 28.307 | 28.341 | +1.2 % | |
| war_room removeItem-items | 12.817 | 12.906 | 13.041 | +1.7 % | |

- **`lastPlaceSkipped`, `lastArranged`, `lastArrangeEntries` and `lastLayoutNodes` IDENTICAL on all three arms, every class, all five workloads.** The gate's ≥ 0.03 ms on `span:Facet/arrange` is met (−0.037).
- The `reorder` / `removeItem-items` +1.2 % / +1.7 % are **inside the between-session noise floor**, and the evidence is in this campaign's own data: arm A here is the SAME commit `7a17a43d` the D2a matrix measured, and it read `reorder` 27.085 there against 28.012 here — a 3.4 % drift on identical code between two sittings. Within one interleaved session B−A is 1.2 % against an A/A spread of 0.6 % and a B/B spread of 0.2 %, and arm C sits between them, so what the number prices is the shared `candidateIndices` attempt on a tick that then refuses — not the short list.

### Task D2 — SUMMARY
`battle_hud L updateItem-hp` Lune p50 across the campaign: **0.984 (b315dc34) → 0.363 (D1) → 0.229 (D2a) → 0.194 (D2c)**, −80 % from the Plan D base. Live projection (`x1.37 + 0.129`) ≈ **0.39 ms**, under the 0.5 ms update target for the first time. The remaining terms on that class are `span:Facet/commit` 0.085 (of which `cw.harvest` 0.067 — D2b, blocked) and `span:Facet/measure` 0.068.
OWED, not done here (stated per the task brief): the Studio canary and the live fresh-VM matrix for D2a/D2c — the orchestrator runs them after D2. No FacetBench §D3 written: the orchestrator owns it; the raw numbers are the tables above.
Worktrees left in place for the orchestrator: `<scratch>/d2/{A,B,C}/{Facet,FacetBench}` (A `7b97dd80`, B/C `7a17a43d`) and `<scratch>/d2c/{B,C}/{Facet,FacetBench}` (`4f906a5c`, C carrying the arm-C patch). Remove at close.

### Task D2 — orchestrator close (2026-09-06)
- LIVE (d2-studio-live.txt, fresh VM per workload, settled, Facet 4f906a5c, marker 107dab8): battle_hud hp **0.323 MET**, setState **0.361 MET**, [S] 0.723, facing 0.527, addItem [S] 3.428, removeItem 2.562; war_room setState **0.336 MET**, tier **0.359 MET**, power 0.615, [S] 0.629, addItem 5.134, removeItem 15.8, reorder 33.2; killfeed hp **0.263**, setState [S] **0.411**, plates 1.034/1.466, feed 4.355/2.327. Wide-key [S] rows unmoved (47.5/70.4/75.6) — D3.
- RR canary (d2-rr-canary.txt): 57/56/8 hosts, kids byte-equal, frame p50 16.65, 0 Facet errors.
- FacetBench `f7c643d` §D3 (+ marker `cb1a103`). **Task D2 CLOSED** at Facet 4f906a5c / RR 3f1a43c.
- Booked from D2: D2b (harvest descend list from the walked set) needs a renderer seam to route `walkedIds` — `cw.harvest` 0.067 ms is the largest remaining term on the target class; D2a's +0.02 ms on the full-rebuild arm (cache `inertPlacement` on the entry).

### Task D3a — in-flight words stop eating the collector budget (dispatched to an implementer, opus; brief d3a-brief.md)

### Task D3a — an in-flight word stops eating the collector budget (SHIPPED, Facet `a72f71db7`)
- Code parent `4f906a5c` (D2c); branch head at start `7f85b15f` (docs-only plan commit).
- **Mechanism.** Process-global refcount `inFlight: { [string]: number }` in `layout/text_metrics`, keyed exactly as the collector keys, written ONLY by `render/premeasure_round`: `+1` at each dispatch (`textInFlight[key] = true`), `-1` at each final answer, and `-1` per remaining key in `dispose` (guarded on `disposed`, so a second dispose cannot double-decrement a count another surface holds). One term in `wordWidth`: a word already in flight is **COLLECTED but NOT CHARGED** to `collectCount`. New counter `text_metrics.collectTruncated()` — incremented once per collector that runs out of budget (latched per `beginCollect`) — published as `stats.textCollectTruncated`.
- **DEPARTURE FROM THE PLAN'S LINE, measured, not argued.** The plan/brief specify `(inFlight[key] or 0) == 0` as a term of the collect condition — an in-flight word "neither re-requested nor charged". Built as mutation **M7** and run: it reddens **five** cases in `tests/text_premeasure.spec.luau`, headed by "...AND SO DOES A SECOND HOST THAT ASKED FOR THE SAME WORD ITSELF" (`B batches 0` where the shipped contract is 1). That case is a live-found, shipped fix: two hosts open cold in the same frame, both ask, and each decides its own re-solve from its OWN in-flight set — the process-global store cannot answer a per-surface question. A skipping collector leaves host B with no batch, no answer and no re-solve (the estimator's box for the session), and it also reddens the boot-window correction and the `revealWhenTextExact` gate (a word parked in the `"answered"` state is in flight forever, so nothing ever re-collects it). UNCHARGED achieves the identical measured goal and is the fail-safe direction: a leaked count only relaxes the budget, where a skip strands a word permanently. The plan's ledger line for D3a should be read as "not charged".
- **Zero renderer characters** (197,472, untouched). `stats.textCollectTruncated` crosses through `premeasure_round.request` (once per solve, already holds both the store and the record) rather than `render_stats.publish` as the brief says — `render_stats`' seam claim is that **it requires nothing** (`tests/render_stats_seam.spec.luau` case 1 pins `requiresIn(source) == {}`), so the brief's route would have reddened the module's own architecture pin. The FIELD and all of its prose still live in `render_stats.new()`, exactly as `textMeasureBatches` does.
- **Red-first spec** `tests/text_collect_inflight.spec.luau` (6 cases): 1,200 distinct words at one (font,size) — over `COLLECT_CAP` by 176 — one `UI.Text` each, fake target QUEUEING, first batch never answered. Per round: edit one leaf to a NEW word, then answer THAT batch alone (new `fake_target.deliverLatestTextWidths`, the boot-window asymmetry the existing `deliverTextWidths` cannot reproduce), which teaches one word, announces the (font,size), marks 1,200 paths over the push cap and takes the wide-key `full` route — a whole-tree cold solve, the shape whose collector is the subject.
- **THE RED ARM (S2's owed experiment, the digit).** Vocabulary words the surface has ASKED about, five rounds:

| arm | round 1..5 | hi | truncated | batches |
|---|---|---|---:|---:|
| C (counters only, no uncharged term) | 1025, 1026, 1027, 1028, 1029 | zqxw1029 | 7 | 11 |
| B (shipped) | 1200, 1200, 1200, 1200, 1200 | zqxw1200 | 1 | 6 |

  One new word per whole-tree solve at arm C — S2's predicted `n=1` walk, reproduced exactly, with 171 of the 176 dropped words never asked about at all and a truncating solve on every round. After: the whole tail rides out on the first solve following the mount, and the mount is the only truncation left.
- **Oracle that does not share the switch:** a third arm with the whole 1,200-word vocabulary `setMeasured` BEFORE mount (`batches=0`, never enters `wordWidth`'s collect branch at all) — `painted()` (every live node's rect + scalar props) and `text_metrics.measuredDump()` byte-equal against the settle-driven arm. Green on BOTH arms, which is what makes it an oracle rather than a second copy of the mechanism.
- **Nulls recorded:** the SETTLED drive (answer everything) is unchanged on both arms — `pending=false batches=2 solves=3 deferrals=0 truncated=1` — because a drive that answers what it asked for never builds a backlog. `drainDeferrals` 0 on both.
- **Mutations 6 run / 6 bite** (`d3a-mutations.txt`): M1 drop the uncharged term → the red arm (arm C's numbers); M2 drop the dispose sweep → 2 red (`after dispose: 2`); M3 drop the `+1` at dispatch → 3 red; M4 unguarded `-1` (drop `state ~= nil`) → 1 red, and it needed a case built for it (an `answerAgain` replay must not decrement a count another surface still holds); M5 latch never reset in `beginCollect` → 1 red; M7 the plan's skip form → 5 red in `text_premeasure.spec` (above).
- **Gates:** `tools/test.sh 8713` → **8,719/0** (`d3a-test.txt`; floor ratchets 8,713 → 8,719 = exactly the six new cases, no existing pin moved); `verify.sh affected --jobs 1` PASS_PARTIAL, **0 FAIL_RECOVERABLE**, 493.2 s (`d3a-verify-affected.txt`); `check_source_size` PASS (renderer 197,472 UNTOUCHED); stylua clean.
- **Differential:** `lune run tools/lune/text_reentry_differential` → md5 **`3db66551b80fe677d314f2092b0a0038`, 144 blocks — IDENTICAL to the campaign baseline** (`d3a-diff-after.txt`). No block differs and none is owed an explanation: every fixture in that sweep has a vocabulary far under the 1,024 cap, so the uncharged term never fires and no batch count can move. The brief predicted the md5 WOULD move; under the uncharged form it does not, which is the stronger result the Global Constraint asks for.
- **RR `60d6c08`**: `facet_text_settle_contract` gains `truncated=0` beside the settle's `solves=2 notifies=2` — which did NOT move (fewer rounds was the permitted direction; the change is invisible to a surface whose vocabulary fits under the cap, and this game's whole HUD vocabulary is a dozen names, a lap readout and a clock). Suite **3,599/0** (`d3a-rr-suite.txt`). No existing pin moved. Studio canary NOT run (owed to the orchestrator with the live drive).
- **ABBA, three arms, PAIRED worktrees** (`<scratch>/d3a/{A,B,C}/{Facet,FacetBench}`; FacetBench `cb1a103` on EVERY arm — the brief's sha, two docs-only commits behind FacetBench HEAD `0e9478a`, which touches nothing under `tools/`). A = `4f906a5c` (D2c), B = `a72f71db` (shipped), C = B with the uncharged term switched off (`if false and inFlight[key] ~= nil`, counters kept — an uncommitted patch in the C worktree).
  - `drain_rounds battle_hud L` (600 steps, boot mode), order A B C A B: **identical on all three arms** — `mount: solves=3 rounds=2 batches=2`, `MAX ROUNDS = 2`, `totals: solves=669 batches=99 textPending=true`. A NULL, and the predicted one: the fake target answers INLINE, so no backlog forms and the budget is never contended. `d3a-abba/drain_rounds.txt`.
  - `attr battle_hud L 3`, interleaved **B A A B C** (`d3a-abba/attr-{1..5}-*.txt`), p50 ms:

| class | A (2) | A (3) | B (1) | B (4) | C (5) |
|---|---:|---:|---:|---:|---:|
| updateItem-hp | 0.195 | 0.191 | 0.194 | 0.193 | 0.194 |
| setState | 0.365 | 0.389 | 0.295 | 0.352 | 0.303 |
| updateItem-facing | 0.004 | 0.004 | 0.004 | 0.004 | 0.004 |
| addItem-damage | 1.744 | 1.726 | 1.769 | 1.732 | 1.730 |
| removeItem-damage | 1.753 | 1.754 | 1.739 | 1.776 | 1.776 |
| noop | 0.002 | 0.002 | 0.002 | 0.002 | 0.002 |
| mount (ms) | 111.63 | 108.16 | 110.84 | 110.21 | 111.70 |

  - **Every work counter is IDENTICAL on all six A-vs-{B,C} pairings** — `lastLayoutNodes`, `lastArranged`, `lastArrangeEntries`, `lastMeasured`, `lastMeasureCalls`, `lastMeasureServed`, `lastPlanVisits`, `lastNodeBuilds`, `lastBuildChildVisits`, `lastChildVisits`, `lastChildIterations`, `lastCommitScans`, `lastCommitProbes`, `lastCommitVisits`, `lastPlaceSkipped`, `lastRectInserts`, `rectWrites`, `engineWrites`, every `cv.*` — with only the `ss*` millisecond fields differing (they are timings). The per-step classes did not move, and they were not supposed to: **this lever cannot fire headless**, which is exactly why S2's brief calls the live number the point.
- **LIVE: NOT RUN — the orchestrator's, per the brief.** What to read: `battle_hud` L in a FRESH VM, loop mode `samples=100` — the 890 ms `stepP95Ms` and the drain-cap line from §8b, `drainDeferrals` 0, and the new `textCollectTruncated` (it should read 1-2 for the session instead of climbing with the drain). Then the three `[S]` wide-key rows in the settled probe: UNCHANGED per step, reached far less often in the boot window. No Studio was driven from this task, and no FacetBench §Dn was written (the orchestrator owns the run doc; the raw numbers are the tables above).
- **Worktrees left in place:** `<scratch>/d3a/{A,B,C}/{Facet,FacetBench}` (A `4f906a5c`, B/C `a72f71db` + the arm-C patch, FacetBench `cb1a103` on each). Remove at close.
- **Follow-up `aeaf17def`** (its own commit, its own gates). M8, run after the main commit: deleting `textCollectTruncated = 0` from `render_stats.new()` reddened **NOTHING** — it is the only field in that record whose writer ASSIGNS rather than increments, so the write creates the key on first use and every equality in the new spec reads a surface that has already solved. A declared-but-unenforced field. The pin now lives in `render_stats_seam.spec`'s sample block (the file's own home for exactly this class), and it catches the deletion (`collectTruncated=nil`). Gates: `tools/test.sh 8719` → **8,719/0** (`d3a2-test.txt`, no new case — the pin rides an existing one); stylua clean; `check_source_size` PASS; `verify.sh affected --jobs 1` PASS_PARTIAL, **0 FAIL_RECOVERABLE**, `suite_cache_selftest` green (`d3a2-verify-affected.txt`).
  - ⚠ **Trap, recorded:** that verify's FIRST run came back FAIL (3 FAIL_RECOVERABLE) on ONE producer — `suite_cache_selftest`, case "a valid cache is served verbatim and exits 0", 749.8 s — with `navigation-and-menus::d0-cache-guards-bite` and two `example-games-and-standalones` rows failing DOWNSTREAM of it. Nothing in `src/` was involved. Cause: the `tools/test.sh 8719` run minutes earlier in the same tree state raced the selftest's own manipulation of `artifacts/suite_cache` — the concurrency hazard `tools/test.sh`'s own header (guard 5) and the campaign's lessons both name. The documented recovery cleared it: `tools/verify.sh affected --rerun suite_cache_selftest` → PASS, then a clean full `affected` tier PASS_PARTIAL with the producer reused green at 6.97 s. Rule for the next task in this campaign: do NOT run `tools/test.sh` and a verify tier back-to-back in the same tree state without letting the cache settle, and read a `suite_cache_selftest` failure as an infrastructure race before reading it as a regression.
- D3a orchestrator close: LIVE loop-mode witness (d3a-studio-live.txt): battle_hud L samples=100 stepP50 3.279→**0.336**, stepP95 **890.2→3.24**, stepP99 5.01, drain-cap lines 1→**0**. RR canary owed — combined with D3b-fix's canary (one drive covers both commits; stated).
- D3b cutoff sweep (d3b/): PUSH at aeaf17de vs b315dc34 — 50: 7.7 vs 11.3; 200: 11.7 vs 15.4; 400: 20.8 vs 16.1; 638: **32.1 vs 19.1**; 1000: **62.6 vs 24.2** → a wide-push REGRESSION from D2's O(dirty)-per-container enumerations. Fix dispatched (D3b-fix: probe budget in stack_measure's shape; bisect D1/D2a/D2c first).

#### D3b-fix — the two dirty-set walks bounded (SHIPPED, Facet `99524098` / RR `7d5c1c0`)

- **BISECT** (`lune run tools/profile/t18_cutoff 1000 5`, detached worktrees `<scratch>/d3b/{A,D1,D2a,D2c,B}/{Facet,FacetBench}`, FacetBench `d242388` on every new arm, the `_G.T18A_CAP` hook uncommitted in each Facet worktree; raw `d3b/cutoff-*.txt`). PUSH ms:

| usersA | base `b315dc34` | D1 `7b97dd80` | D2a `7a17a43d` | D2c `4f906a5c` | D3a `aeaf17de` |
|---|---:|---:|---:|---:|---:|
| 50 | 11.3 | 6.9 | 6.9 | 7.2 | 7.7 |
| 200 | 15.4 | 12.0 | 9.2 | 11.1 | 11.7 |
| 400 | 16.1 | 12.1 | 13.6 | 17.4 | 20.8 |
| 638 | 19.1 | 16.0 | 19.6 | 28.2 | 32.1 |
| 1000 | 24.2 | 26.6 | **25.6** | **55.1** | 62.6 |

  **D2c is the lever that regresses** — monotone in the push width and +29.5 ms at the 1,000 point alone. D2a shows no regression on this route, and the COUNTER says why: a scratch probe on the sweep's own scene (`d3b_probes`, deleted after) reads `buildVisits=0` there — the settle push re-solves the layout tree already standing, so `patchIndices` is not on that route at all — against `replayProbes` **3,002,000** unbounded vs **1,002** bounded (400: 480,800 vs 1,002). `patchIndices` has the identical unbounded shape and is bounded with it, priced on the direct drive that CAN reach it (2,003,001 → 1,001), not on churn.
- **Mechanism.** A bare probe COUNT before anything else in both `render/layout_node.patchIndices` and `layout/stack.candidateIndices` (`stack_measure`'s owed row A10 shape: a tick that bails has paid for nothing — no index built, no region triples re-read, no inverse map). Past the budget the container takes the arm the lever replaced — the full child pass / the sibling scan — so the fallback is the exact pre-D2 arm and the total is O(children) either way. **And the count is paid once per PASS, not once per container**: the dirty set is one fixed table for the length of a build (and of a solve), so a walk that stopped at `budget + 1` proved it wider and a walk that finished measured it; `store.dirtySeen` / `ctx.dirtySeen` carry the larger, the outermost container goes first with the widest budget, and everything under it refuses on a comparison. Both numbers are pure functions of the set's SIZE and of document order, never of hash order.
- **The constant: `DIRT_PROBE_FLOOR = 64`, `probeBudget = math.max(FLOOR, n)`.** The floor is what the choice turns on, and it is load-bearing rather than tuned: the irreducible dirty set on the shape both arms exist for is the edited leaf's ancestor chain — the tree's DEPTH — so a budget that does not clear a realistic depth refuses on the very tick the arm was built for. At 4 (`M5`) the battery reddens `build_index_patch`'s production-dirt case (`visits` 3 → 4) and moves the replay's probe count 65 → 70; at 64 every narrow pin in both specs is UNMOVED. 64 is the same "small enough not to bother" constant `render/text_index`'s push cap already spends — an order of magnitude above any UI depth, an order of magnitude below the wide-push sets it bounds. `n` is the container's own child count: one hash probe per child you would otherwise visit, against a child visit measured at ~13x a probe.
- **Red-first, both mechanisms** (3 new cases, and the narrow pins beside them unmoved): `build_index_patch.spec` gains a 1,000-row direct drive — narrow then wide on the SAME tree, `probes` 6 → 1,001 with `visits` 1 → 3,000 (the trade pinned as a trade: the fallback IS the pre-D2a full pass) — plus "the budget is spent BEFORE the index is", which reads the lazy index back. `stack_replay_candidates.spec` gains the wide signal write AND **the production route**: a wide-key settle push under the SHIPPED push cap (`max(64, nodes // 8)`, so 64 rows is the only door), where every container keeps its token and reaches `candidateIndices` with the whole push in `dirtyContains` — `probes=65`, one bounded walk for the solve, `scanned` 4 → 382 showing the fallback is the sibling walk. `tests/lib/deep_stack_scene` gains `textMany` and `settleText`/`pushWord` for those two drives.
- **Mutations 5 run / 5 bite** (`d3b-mutations.txt`): M1 stack early return removed → 65 → 130; M2 stack per-pass bound removed → 65 → **4,290**; M3 layout early return removed → 1,001 → 2,001; M4 layout per-pass bound removed → 1,001 → **66,001**; M5 floor 64 → 4 → reddens a narrow D2a pin AND the replay probes. Every reader the fix adds has its own revert-and-redden.
- **Seam pins re-read off the run** (not guessed): `solve_ctx_seam` 67 → **69** (`replayProbes`, `dirtySeen`); `stack_seam` CTX_FIELDS + WRITES gain both and the module locals gain `DIRT_PROBE_FLOOR`. `ctx.dirtySeen` is the ONE field in `stack.luau` that is written and read back in the same solve — recorded as a design fact in the WRITES comment rather than smuggled under "counter", with the argument that it selects only the ENUMERATION and the differential oracle is what answers for both arms. `check_theme_drift` ALLOWLIST gains `DIRT_PROBE_FLOOR` with its reason (a probe budget in hash-table entries, not a distance).
- **Gates:** `tools/test.sh 8719` → **8,723/0** (`d3b-test.txt`; floor ratchets 8,719 → 8,723 = exactly the 3 new cases + 1 from the theme-drift file, no existing pin moved); `verify.sh affected --jobs 1` PASS_PARTIAL, **0 FAIL_RECOVERABLE**, 875.7 s (`d3b-verify-affected.txt`), then `git checkout -- examples/places/`; `check_source_size` PASS with **renderer 197,472 UNTOUCHED** (the counters ride the store and `work`, never a renderer seam); stylua clean.
- **Differential:** md5 **`3db66551b80fe677d314f2092b0a0038`, 144 blocks — identical to the campaign baseline** (`d3b-diff-after.txt`). Observables byte-identical.
- **ABBA, three arms, `attr battle_hud L 3`** (`d3b/abba-attr/`, order B A C C A B then a second C-force/B/C-force interleave; A = `aeaf17de` worktree with the sweep hook reverted, B = this commit, C = B with `probeBudget = -1` so the budget refuses everything):

| class | A (`aeaf17de`) | B (this commit) | C (forced refuse) |
|---|---:|---:|---:|
| `updateItem-hp` | 0.1925 | **0.1940** | 0.3605 |
| `setState` | 0.3695 | 0.380 | 0.417 |
| `addItem-damage` | 1.8185 | 1.8135 | 1.679 |
| `removeItem-damage` | 1.7975 | 1.808 | 1.782 |
| `lastBuildChildVisits` (hp) | 2 | **2** | 1,126 |

  B == A within noise on every narrow class, and arm C is what makes that non-vacuous: forcing the budget to refuse costs **+0.166 ms / +86 %** on `updateItem-hp` and puts `lastBuildChildVisits` back at the pre-D2a 1,126. The D2 levers are still alive at B.
- **The sweep at this head** (`t18_cutoff 1000 9`, interleaved A B B A, two sessions pooled n=4 per arm; `d3b/abba9-*.txt`, `d3b/final-*.txt`):

| usersA | base `b315dc34` | **this commit** | `aeaf17de` |
|---|---:|---:|---:|
| 50 | 7.81 | **7.22** | 7.68 |
| 200 | 11.20 | **11.01** | 11.70 |
| 400 | 14.20 | **13.44** | 20.77 |
| 638 | 18.27 | 19.61 | 32.14 |
| 1000 | 23.66 | **24.42** | 62.62 |

  Every point at or under the base within the measured noise floor: the A/A spread reaches 22 % at 638 and 17 % at 1,000, and the FULL arm — constant work on every arm — reads 15.8-22.8 ms across the whole campaign. The 1,000 point is **−61 %** against `aeaf17de`.
- **RR `7d5c1c0`**: `facet_measure_fanout_contract` gains `replayProbes=44 visits=10` on the WARM one-row update — the drive where the walk actually happens (the sort-swap block reads 0 because the replay refuses at the list outright, so the pin went where it is non-vacuous). Nothing else moved: `visits`, `builds`, `plans`, `placeScanned`, `placeSkipped`, `arranged`, `arrangeEntries` all unchanged. Suite **3,599/0** (`d3b-rr-suite.txt`). Studio canary NOT run — owed to the orchestrator, and D3a's canary is owed on the same drive.
- **Worktrees left in place:** `<scratch>/d3b/{A,D1,D2a,D2c,B}/{Facet,FacetBench}` (A `b315dc34`, D1 `7b97dd80`, D2a `7a17a43d`, D2c `4f906a5c`, B `aeaf17de`; each Facet carries the uncommitted `_G.T18A_CAP` sweep hook except B, whose hook was reverted for the ABBA). Remove at close.
- D3a + D3b-fix RR canary (d3-rr-canary.txt): 57/56/8, kids byte-equal, frame p50 16.72, results screen mounted (124 desc vs T10's 126 — OPEN, compare at D7), 0 Facet errors. FacetBench §D4 `82d105f`. D3b cutoff decision: the band is unchanged after the fix (PUSH ≤ base at every point, crossover still ~638/3,002 = 21 %); `max(64, lastLayoutNodes // 8)` STAYS — recorded, not moved.

### Task D3c — the over-cap route (dispatched, opus; arm A 99524098)

### Task D3c — over the cap, the leaves are asked before the tree is (SHIPPED, Facet `97aeca6bd` + `1ae7ae87e` / RR `08e4210` / FacetBench `46aaf37`)

- **Code parent** `99524098` (D3b-fix). Two commits, seam first as the plan requires.
- **THE PUBLISH LIST, written before the mechanism** (S2 §4b's "channel this can change"). For a leaf the probe DROPS, each channel and its verdict:
  | channel | verdict | why |
  |---|---|---|
  | rect (`out[id].rect`) | **SERVED, and provably unchanged** | the probe's whole question is `m.width`, and a one-word label's box is `min(m.width, availW) + pl + pr` with `availW` untouched by a word delivery |
  | `textFacts.truncated` | **SERVED, and provably unchanged** | `m.truncated or clampedWidth`; `clampedWidth` is `m.width > availW`, `m.truncated` is `natural > lines` and both are 1 for a one-word unbounded measure |
  | `textFacts.lines` / `naturalLines` / `policy` / `wraps` / `size` / `lineHeight` | **SERVED, unchanged** | all functions of (`lines`, `truncated`, node props, the resolved pair); none can move while the width does not |
  | `compact` (`ctx.compact[id]`) | **SERVED, unchanged** | the ladder's rung is `natural.width > availW`, and for a one-word label `natural.width == m.width` |
  | `textStates` (`m.state`) | **SERVED, unchanged** | `state` is a function of `KNOWN_FONTS[canon]`, not of any word width |
  | `fitCuts` | **NOT REACHED** | `ctx.fitProbe` is a `ViewThatFits` candidate pass; a settle solve sets none |
  | `textFacts.naturalWidth` (reveal) | **SERVED, unchanged** | it IS `measure(text, huge).width` — the number the probe compared |
  | `textRequests` / `textMeasureBatches` | **UNCHANGED, and GATED** | the probe runs with the collector suspended (`text_metrics.suspendCollect`) |
  | `lastTextDirtyPaths` | **STATED — it MOVES by design**, over the cap it is the MOVED set, not the key's bucket |
  | `lastTextProbeMeasures` | **STATED — new** |
  Everything the probe cannot answer for (`always`/fit, phrases, `compactPrefer`, an unseen path) is pushed unconditionally, so no channel is at risk there at all.
- **Seam `97aeca6bd`** (own commit, byte-identical): the push BUDGET, the `textIndex.apply` call and the `lastTextDirtyPaths` publish became `render/settle_solve.spend`. The DECISION stayed in `text_index`; the tail clear stayed at the call site (it must run after `solveOpts.reuse` reads the same pair). renderer **197,472 → 197,305**, back under the 197,500 STOP with 195 of room; suite 8,723/0 with no case added; md5 `3db66551b80fe677d314f2092b0a0038` identical; verify affected PASS_PARTIAL 0 FAIL_RECOVERABLE (491.9 s).
- **Mechanism `1ae7ae87e`.** Over the cap only, each pushed leaf is re-measured at an UNBOUNDED offer and compared against `node.mTextW`, cached by the solver's text branch for ONE-WORD labels (`node.singleLine == true`, where `wrapWidth == math.huge`). That restriction is what makes the drop EXACT rather than likely: the offer cannot enter the answer (no two-offers problem), `lines` is 1 whatever `lineLimit` says, `truncated` and the compact rung are both `<width> > availW` against an unchanged box, and `m.width` is `ceil(widest)` so a ceiling-absorbed correction moves nothing. `text_index` holds the layout node LITERAL (written before `record`'s unchanged-key guard, because the key can stand still across a rebuild while the table is replaced). solver **187,975 → 188,273 (+298**, under the task's +300); renderer UNTOUCHED.
- **ROUTE (c) IS REFUTED AT THE SOURCE, not measured.** `render/structural_scope`'s own header: *"Whatever enclosing boundary you re-root at, the set the solver needs is the SAME set — the ancestor closure ... Rooting 'at' a boundary and rooting at the solve root produce identical work. What the boundary decides is whether a partial solve is ALLOWED AT ALL."* An LCA boundary would hand the solver the closure it already has. Not built; recorded in the commit, the run doc and here.
- **Red-first spec `tests/text_settle_probe.spec.luau`** (11 cases, registered): 1,001 one-word labels at one key on a 3,004-node tree, cap 375. A word nobody draws → `probes=1001 paths=0 measured=0 arranged=0`; a word ten draw → `paths=10`; a word every label draws → still over the cap, still `full`, `arranged == lastLayoutNodes`; under the cap → `probes=0`; a phrase is never probed and always pushed (`probes=996 paths=5`); a fit node bypasses (`probes=1001 paths=1`); a relabelled row is probed as it is NOW; the probe adds no batch. **Oracle that does not share the switch:** a FORCED COLD arm (`incrementalLayout=false` + `measureReuse=false`) driven identically and compared on `painted` (rects + hit rects + every scalar prop), `controller.textAt()`, `rectOf`/`screenRectOf` and the diagnostic set — byte-equal on three drives, with the cold arm asserting its own coldness. Plus **the truncation-boundary fixture the brief names**: a 60px fixed-width label whose `truncated` flips while its rect cannot move — `paths=1`, verdict flips, `painted` still byte-equal to the cold arm.
- **Mutations 7 run / 6 bite / 1 NULL** (`d3c-mutations.txt`): M1 probe disabled → 8 red; M2 `always` bypass removed → 1; M4 the literal filed after the guard → 10; M5 the solver caching for every text node → 1; M6 every probed leaf dropped → 6; M7 the still-over-cap fallback removed → 1. **THE NULL, M3** (the collector left open across the probe): reddens nothing, and it is a design fact about two OTHER mechanisms — an answered word is a number and a refused one is `false`, so neither reaches `wordWidth`'s collect branch, and an in-flight one is refused a second dispatch by `premeasure_round.request`'s own set. The only window is a word never asked about, i.e. a collector that hit `COLLECT_CAP`. Kept anyway; the case says why.
- **A FIXTURE DEFECT FOUND EN ROUTE, worth the line:** a settle taken straight off a FULL solve serves nothing (`solveOnce` calls `measure_reuse.clear` whenever the solve it is about to run has no reuse set), so an empty closure there reads `measured=3004` on a 3,004-node tree and says nothing about the narrowing. One ordinary incremental tick first and the same settle reads 0. That is T6's known cost, booked as D5.
- **INSTRUMENTS: neither headless driver could reach the route, and that is half the finding.** `t18_settle` writes `next(h.ctx.signals)` — the 4-user strip / 2-user header — so `lastTextDirtyPaths` reads 4 and 2 against a cap of 638/938. A three-arm × two-pass matrix (order B A C A B, `t18_settle <wl> L 7`) is therefore a **NULL by construction**, and it is recorded as one: SETTLE p50 0.135–0.176 ms (`battle_hud`) / 0.157–0.212 (`war_room`), UNUSED KEY 0.032–0.050, CONTROL 0.158–0.497, **every counter identical on all three arms**. And `t18_cutoff`'s scene names rows `alpha {i}` — two words — where both production wide keys are integers. So the tool gained `shape = "word"` and a third arm `PROBE` (the SHIPPED cap), which is the only arm the two SHAs can differ on.
- **THE SETTLE TABLE — `t18_cutoff 1000 5 word`, PROBE arm (p50 ms):**

| usersA | % of tree | A `99524098` | **B `1ae7ae87`** | C (probe on, nothing dropped) | B counters |
|---|---:|---:|---:|---:|---|
| 50 | 1.7 | 10.86 | 9.21 | — | under the cap, `probes=0`, unchanged route |
| 200 | 6.7 | 9.67 | 9.75 | — | under the cap, `probes=0`, unchanged route |
| 400 | 13.3 | 16.07 | **7.08** | 21.10 | `probed=400 pushed=1 measured=3004 arranged=4` |
| 638 | 21.3 | 17.90 | **9.16** | 18.53 | `probed=638 pushed=1 measured=3004 arranged=4` |
| 1,000 | 33.3 | 13.03 | **8.82** | 16.89 | `probed=1000 pushed=1 measured=3004 arranged=4` |

  A reads `pushed=0 measured=8003 arranged=3002` at 400/638/1,000 — `full`, the whole tree. **−56 % / −49 % / −32 %**, and arm C is what makes it non-vacuous. The PHRASE control (`shape phrase`): PROBE arm A 17.56 / 22.88 / 17.18 vs B 17.00 / 16.24 / 16.92 with `probed=1` on every B row — no benefit and no measurable cost where the probe can answer for nothing.
- **`attr` all five workloads, interleaved B A A B:** no leaf class moved (largest |Δ| `war_room addItem-items` −5.1 %, `battle_hud setState` +6.7 %, everything else inside ±3 %; `nameplates updateItem-level` is 0.004–0.010 ms on both arms, timer noise). **Every work counter identical A vs B on all five workloads.**
- **Gates:** `tools/test.sh 8723` → **8,734/0** (floor ratchets 8,723 → 8,734, exactly the 11 new cases, no existing pin moved — `measure_serve`'s closed write-set list gains `mTextW` and `render_stats_seam`'s sample gains `lastTextProbeMeasures`, neither adding a case); `verify.sh affected --jobs 1` PASS_PARTIAL, **0 FAIL_RECOVERABLE**, 890.1 s (`d3c-verify-affected.txt`), then `git checkout -- examples/places/`; `check_source_size` PASS; stylua clean; differential md5 **`3db66551b80fe677d314f2092b0a0038`, 144 blocks — IDENTICAL** (`d3c-diff-after.txt`), and identical BY CONSTRUCTION: the probe runs only over the cap, so every under-cap route is byte-unchanged.
- **RR `08e4210`**: `facet_text_settle_contract` gains `pushed=1 probes=0` — an equality, not a bound, because a non-zero value would mean a Rascal Rally surface had grown a key wide enough to change which route its settle takes. `solves=2 notifies=2`, `truncated=0`, the cold-arm geometry equality and the 17px lap box all re-read off the run and unmoved. Suite **3,599/0** (`d3c-rr-suite.txt`). Studio canary NOT run — owed to the orchestrator with the live drive, and D3a/D3b's canary is owed on the same drive.
- **LIVE: NOT RUN — the orchestrator's.** What to read: the three `[S]` wide-key rows in a FRESH VM per workload, settled (`battle_hud updateItem-facing [S]` 47.5, `war_room updateItem-power [S]` 70.4, `addItem-items [S]` 75.6), plus `lastTextProbeMeasures` / `lastTextDirtyPaths` on those steps. The headless PROBE arm predicts the route flips from `full` to a 1–3 path push; the live number is what says by how much.
- **Worktrees left in place:** `<scratch>/d3c/{A,B,C}/{Facet,FacetBench}` (A `99524098`, B/C `1ae7ae87` + the arm-C `keep … or true` patch; every Facet carries the uncommitted `_G.T18A_CAP` sweep hook, in `settle_solve` on B/C and in `renderer` on A; every FacetBench at `8d5bbca` with `t18_cutoff.luau` copied from `46aaf37`). Remove at close.
- D3c orchestrator close: LIVE (d3c-studio-live.txt, fresh VM per workload): battle_hud updateItem-facing [S] **47.5 → 1.614** (probed 1,000, pushed 1, measured 9); war_room updateItem-power [S] **70.4 → 2.343** (probed 1,464, pushed 1; p95 26.8), addItem-items [S] **75.6 → 7.605**; every narrow class unmoved (battle_hud hp 0.318, setState 0.328; war_room setState 0.318, tier 0.391, power 0.549). RR canary owed — combined with D4's. **Task D3 (a/b-fix/c) CLOSED** at Facet 1ae7ae87 / RR 08e4210 / FacetBench 46aaf37+2432991.

### Task D4 — the structural constant (dispatched, opus; arm A 1ae7ae87; brief d4-brief.md)

### Task D4 — the structural per-rect constant (arm A = Facet `1ae7ae87`, FacetBench `2432991`)

#### D4a — the harness split (FacetBench `3ae0b03`; no Facet change)
- `lib_attr` wraps `fake_target.new`'s returned table in place: `target.setRect` / `target.setProp`. In place and not a copy, because `rect_pass.new` does `local adapter = ctx.adapter` and every call is a member lookup.
- **`attr war_room_inventory L 3` (`d4/d4a-attr-war_room-L.txt`)**:

| class | p50 | `rectPass.apply` | `target.setRect` | **Facet's own `rectPass.apply`** | harness share of the CLASS |
|---|---:|---:|---:|---:|---:|
| `reorder` | 29.345 | 12.264 (3,209.3 KB) | 9.776 · 7,335 calls (3,209.1 KB) | **2.488 ms (20.3 %)** | **33.3 %** |
| `removeItem-items` | 13.362 | 5.179 (1,194.6 KB) | 4.135 · 3,460 calls (1,194.4 KB) | **1.044 ms (20.2 %)** | **30.9 %** |
| `addItem-items` | 3.559 | 0.016 | 0.011 · 5 calls | 0.005 | 0.4 % |

- **§C10's rule bites: the harness is 33 % / 31 % of the two structural classes, so their Lune numbers are NOT quotable** — the number to carry is the LIVE one (36.5 ms at §after, 32.6 after D1) and every sub-lever below is judged on Facet's own share plus its live delta.
- And the allocation attribution in the run doc is the harness's too: **3,209.1 of `rectPass.apply`'s 3,209.3 KB is `fake_target.setRect`**, not the rect pass.
- Arms below pair FacetBench at **`3ae0b03`**, not `2432991`: the split IS the instrument the gate reads. Strict superset of `2432991` (one commit, tools/profile only).

#### D4b-1 — the write authority is asked once per `apply` (SHIPPED, Facet `40b11f30`)
- Mechanism: `authority.assertWrite("common", "size", "layout")` moved out of `applyOne` (7,335 calls on one `war_room_inventory` reorder) to the top of `apply`, beside the origin-epoch bump. Three string literals, and the answer is `MANIFEST.common.size` — a module-load constant — so the claim is about the PASS, not the row. Strictly more eager: a commit that applies no rect now asserts anyway.
- Spec `tests/rect_pass_constant.spec` (new, 2 cases at this commit; drives `rect_pass.new` directly with a stub ctx). (a) SAFETY, shares no switch: `authorityFor` patched to a WRONG OWNER for `common.size` → `apply` throws, `written=0`. (b) COUNTER: `lookups=1 written=4` (read off the run; **4** before). Mutation — the assert deleted outright — reddens BOTH.
- **The gate, in Facet's own share (D4a's split), `attr war_room_inventory L 3`, paired worktrees at Facet `1ae7ae87` / FacetBench `d6314fd`, order A B C C B A ×3, n=6 per arm** (`d4/rp/`):

| metric | A (parent) | C (= lever 1 only) | B (levers 1+2) |
|---|---:|---:|---:|
| `reorder` Facet-own `rectPass.apply` | 2.625 [2.551–2.733] | **2.389 [2.315–2.531]** −9.0 % | 2.294 [2.242–2.363] −12.6 % |
| `removeItem-items` Facet-own `rectPass.apply` | 1.137 [1.037–1.179] | **0.947 [0.870–1.084]** −16.7 % | 0.888 [0.777–0.932] −21.9 % |
| `reorder` Facet-own CLASS p50 | 19.736 | 19.518 | 19.422 (−1.6 %) |
| `killfeed addItem-feed` / `removeItem-feed` own `rectPass.apply` | 0.183 / 0.203 | 0.159 / 0.174 | 0.151 / 0.169 |

- A and C ranges are DISJOINT on `reorder` (A min 2.551 > C max 2.531). Safety pair `rectWrites` / `engineWrites` / `lastRectInserts` / `lastLayoutNodes` / `zVisits` / `zPasses` / `lastCommitVisits` **IDENTICAL on all three arms on all five workloads**. No leaf-edit class moves outside its own A/A spread (the largest, `nameplates addItem-plates`, was chased to n=10 per arm and the ranges then overlap fully: A 0.601 [0.593–0.612] / B 0.615 [0.593–0.628] / C 0.607).
- **THE GATE IS AMBIGUOUS AND BOTH READINGS ARE RECORDED.** The plan's D4b text says "ship only if `rectPass.apply` moves ≥ 3 % on `reorder`" → PASS at −9.0 %. The brief says "moves `reorder` OR `removeItem-items` by ≥ 3 % in Facet's own share" read as the CLASS → FAIL at −1.6 % / −1.3 %, and under that reading NO per-rect lever can ever pass, because Facet's own `rectPass.apply` is only 13 % of Facet's own `reorder`. Shipped on the span reading with the class number stated beside it; the orchestrator can overrule with this table.
- Gates: `tools/test.sh 8734` → **8,736/0** (floor ratchets 8,734 → 8,736, exactly the two new cases; `d4/d4b1-test.txt`); `verify.sh affected --jobs 1` **PASS_PARTIAL, 0 FAIL_RECOVERABLE, 881.5 s** (`d4/d4b1-verify-affected.txt`), then `git checkout -- examples/places/`; `check_source_size` PASS (renderer 197,305 UNTOUCHED); stylua clean; differential md5 **`3db66551b80fe677d314f2092b0a0038`** (`d4/d4b1-diff.txt`).

#### D4b-4 — `cw.hitRects`: §7.2's attribution REFUTED; the fix stopped as the brief instructs (no commit)
- New probe `tools/profile/d4_hitrects` (FacetBench `3ae0b03`) censuses the three populations `pushHitRects` spends itself on. `war_room_inventory L` **nodes=7,505 inputSinks=0 authorSinks=0 frameworkChrome=0 hitFloorNodes=0**; `battle_hud L` 5,109/0/0/0/0; `nameplates L` 1,502/0/0/0/0.
- `authorPressableRects` is reachable only from a node whose `props.expandTarget.role` is `cover`/`close`, and only INSIDE the hit-floor branch. **It is never called on any arena workload**, so it cannot be the 69.2 KB. S3's doubt upheld.
- Bigger consequence: exactly four classes in the whole contract declare `minHitSize` (`Grip`, `Button`, `Toggle`, `TextField`) and they are exactly the four `INPUT_SINKING` classes. `war_room_inventory` has none, so `effectiveHitFloor` returns nil for all 7,337 visited nodes and **the walk visits the whole moved subtree to write nothing**.
- **The real allocator, named:** `hitRects` is the only walk that visits the moved set (`cv.hitRects` 7,337; harvest/visible/padding/textScale/textVerdicts 2 each; `cv.rectPass` 1,469), so it is the walk that BUILDS C4's descend cache for the whole tree — `lastCommitScans` 7,346 + `lastCommitProbes` 7,335 + `buildDescend`'s forked prefix arrays are all paid under its span. Not a composed rect.
- Per the brief ("if the allocation is elsewhere, say where and stop"), **no fix built**. BOOKED with its size: refuse the `hitRects` walk outright when `inputSinks` is empty — **2.85 of 19.7 ms of Facet's own `reorder` (14.5 %)**, **1.05 of 9.1 on `removeItem-items` (11.5 %)**, the largest single term D4 found. It needs a correctness argument about `lastHitRects`' departure sweep (a node that leaves the tree while sinks exist), not a per-visit constant, which is why it is a D6 candidate and not a D4 sub-lever.
- Also read for the brief: **`stats.ssFullPasses` is 0 on every step of the `reorder` script** (median and max), so `compactNext` is NOT silently upgrading later syncs to whole-tree on this workload. A null, recorded.

#### D4b-2 — the bar-gutter write is guarded (SHIPPED, Facet `b7e5cf38`)
- Mechanism: `lastBarInsets[path] = entry.barInset` guarded on `entry.barInset ~= nil or lastBarInsets[path] ~= nil`. The second term is the exactness half: a gutter that GOES AWAY arrives as nil with a live entry, and dropping it leaves last frame's gutter standing forever (mutation M2 reddens exactly that).
- Gate (same matrix as D4b-1, arm C = the authority hoist alone): Facet's own `rectPass.apply` `reorder` C 2.389 → **B 2.294 (−4.0 %)**, `removeItem-items` C 0.947 → **B 0.888 (−6.2 %)**. Class-own −1.3 %.
- Spec case counts the write through a PROXY table, because the value written is nil and an unconditional write of nil leaves the same empty map behind as no write at all: `barWrites=0` where it read 3; the appear and disappear directions both driven.
- Gates: `test.sh 8736` → **8,737/0**; `verify.sh affected` PASS_PARTIAL 0 FAIL_RECOVERABLE 502.3 s (`d4/d4b2-verify-affected.txt`); md5 unchanged; stylua clean; `check_source_size` PASS.

#### D4b-5 — the z walk's per-visit constant (SHIPPED, Facet `18795d8e`)
- Mechanism, three exact reductions with `zVisits` untouched: (a) `effectiveHitFloor` memoised BY CLASS for the length of one walk (`zMetrics` is read once at the top and is what the module already forces a whole-tree pass over when it changes) — 7,323 readings → 6; (b) `hitLift.touches` skipped when `hitLift.any` is false, the same latch its own first line reads — 1,465 calls → 0; (c) `node.path` read once per visit instead of up to eleven times, and the memo key once instead of up to five.
- Spec `tests/z_order_visit_constant.spec` (5 cases), driving `z_order.new` directly: the `class_contract.forClass` count (**4, was 8**; `forClass` and not `effectiveHitFloor`, because `z_order` binds the latter at module scope — §D1's own lesson), `touches=0` with an empty lift set and `touches=3` with a non-empty one, the memo give-back on an emptied container, and the WHOLE z assignment byte-for-byte including the slot the one `Button` reserves.
- **Mutations 5 run / 5 bite** (`d4/d4b5-mutations.txt`): mis-keyed memo, hard-false lift gate, reservation dropped, memo never stored, `wasIn` hoist neutered. The last was a NULL until a case was added for it (it reddened 2 cases in `zorder_bounded.spec` — the memo's own file — but nothing in the file that introduced the read).
- **Gate: `st.ssZOrder` on `reorder` A 3.032 [3.021–3.127] → B 2.549 [2.522–2.569] = −15.9 %** (gate was ≥ 5 %), ranges disjoint, arm C (every added read, none of the saving) at 3.039 = exactly A. Falls on every workload where the walk runs: battle_hud 0.008→0.006 / 0.007→0.006, killfeed 0.030→0.026 / 0.018→0.014, nameplates 0.026→0.024 / 0.022→0.020, damage_fountain 0.025→0.020 / 0.035→0.029. `zVisits` 7,323 / `zPasses` 1 / `zMemoEntries` / every write counter identical on all three arms.
- Gates: `test.sh 8737` → **8,742/0**; `verify.sh affected` PASS_PARTIAL 0 FAIL_RECOVERABLE 504.2 s (`d4/d4b5-verify-affected.txt`); md5 unchanged; stylua clean; `check_source_size` PASS.
- RR `ff2ca0a`: `facet_structural_incremental`'s differential gains a LITERAL reserved-slot pin on the from-scratch arm, `assigned=43 maxZ=46 reserved=3`, read off the run. It had to be a literal and not a differential: both arms run the same `z_order`, so a memo bug agrees with itself, and `zRank` compares ORDER, which a uniformly-dropped gap leaves identical. Shown to redden (`reserved=0`) under the mis-keyed memo. `zVisits=78/138/54/164`, `zPasses=2`, `ssPasses=1`, `full=0` all unmoved. Suite **3,599/0** (`d4/d4-rr-suite.txt`).

#### D4b-3 — `applyHitExpander` / `refitIconArt` behind a guard: **KEPT NULL** (nothing committed)
- The form measured is BETTER than the plan's: a call-site guard reading the same fields the callee reads first (`handle.hitExpander ~= nil`; `handle.iconInstance ~= nil or handle.chromeText ~= nil`) — no new state, and it cannot go stale, where per-handle booleans set at every assignment site can.
- The arena is blind to it by construction (both paths are the LIVE adapter's; `fake_target` has neither), so it was priced on a micro-bench driving the SHIPPED `applyRect` headlessly through `tests/lib/gui_value_shim` — 2,000 rows in one host, 40 alternating shifts. Interleaved B A A B ×4, n=8: **A 318.9 ns/rect [313.8–324.0] vs B 315.2 [307.3–332.9], −1.2 %, ranges overlapping.** Below the gate and below the arena's noise floor.
- The finding is the number: two nil-checking calls are ~4 ns of a 319 ns rect write, and the guard hands most of that back by reading the same field the callee would. "An unconditional cheap call on the hot path" was worth ~1 % here, not the 6–12 % two function calls were assumed to be. Bench kept at `d4/d4bench.luau` + `d4/d4b3-bench.txt`.

#### D4 close (A = `1ae7ae87`, B = `18795d8e`, order B A A B ×3, n=6, all five workloads — `d4/close/`)

| class | A (Facet-own) | B (Facet-own) | Δ |
|---|---:|---:|---:|
| `war_room_inventory reorder` | 19.834 | **19.052** | **−3.9 %** |
| `war_room_inventory removeItem-items` | 9.284 | **8.917** | **−4.0 %** |
| `war_room_inventory addItem-items` | 3.701 | 3.664 | −1.0 % |
| `killfeed addItem-feed` / `removeItem-feed` | 1.380 / 1.387 | 1.343 / 1.361 | −2.7 / −1.9 % |
| `damage_fountain addItem-numbers` / `removeItem-numbers` | 0.259 / 0.254 | 0.254 / 0.248 | −1.9 / −2.4 % |
| `battle_hud updateItem-hp` / `setState` | 0.191 / 0.349 | 0.193 / 0.352 | +1.0 / +0.9 %, ranges overlap |
| `nameplates` (all classes) | — | — | flat |

- **Facet-own `rectPass.apply` on `reorder` 2.631 → 2.312 (−12.1 %); `st.ssZOrder` 3.074 → 2.533 (−17.6 %).** Raw class p50 (harness included) 29.848 → 28.904.
- **Every work counter identical A vs B on every class of every workload.**
- So: each sub-lever clears its SPAN gate and none clears 3 % of the CLASS alone, while D4 AS A WHOLE clears 3 % of Facet's own share on both structural classes (−3.9 % / −4.0 %). §C10 still refuses the raw Lune totals for these two: a third of them is the arena.
- FacetBench `f610a77` registers §D7. **Worktrees left in place:** `<scratch>/d4/{A,B,C}/{Facet,FacetBench}` (A `1ae7ae87`, B `18795d8e`, C clean at `18795d8e`; FacetBench all at `d6314fd`). Remove at close.
- **LIVE: NOT RUN — the orchestrator's.** What to read: `war_room reorder` (32.6 after D1) and `removeItem-items` (15.8), plus `addItem-items`; the headless arms predict −4 % of Facet's own share, which is a smaller fraction live because the real adapter's `setRect` is the larger term there.
- D4 orchestrator ruling on the gate ambiguity: the PLAN's wording stands — a per-rect sub-lever ships on ≥ 3 % of the `rectPass.apply` span (Facet's own share) with the safety counters identical; "3 % of the class" would make every per-rect lever unshippable by construction. The three shipped sub-levers cleared it; D4 as a whole moved `reorder` −3.9 % / `removeItem-items` −4.0 %. Booked for D6: the hitRects walk refusal when `inputSinks` is empty (2.85 of 19.7 ms on reorder), rows-as-hosts (an insert moves 1,500 row hosts, not 7,335 boxes), the per-word settle index. **Task D4 CLOSED** at Facet 18795d8e / RR ff2ca0a / FacetBench f610a77. Live structural rows + canary: this drive.

- D4 orchestrator close: LIVE war_room (d4-studio-live.txt): reorder 33.16→32.78, removeItem 15.66→15.09, addItem flat — the live constant is the engine's; the count is the bill. RR canary D3c+D4 (d4-rr-canary.txt): 57/56/8, kids byte-equal, frame p50 17.26, 0 Facet errors.
### Task D5 — mount (dispatched, opus; arm A 18795d8e; brief d5-brief.md)

### Task D5 — mount: **KEPT NULL, BUILT AND REVERTED** (arm A = Facet `18795d8e`, FacetBench `19a976a`)

- **The mechanism, as the brief specified it, was built, gated, tested and measured — and its own ABBA refutes the premise.** `record` re-gated from `ctx.reuse == nil` to `ctx.measureStamp == nil` (the renderer is the only caller that threads one — census pinned as a source scan), the serve's dirt gate given a second arm (`node.mSolve == ctx.solveToken`, written only where `ctx.reuse == nil` so the two arms are disjoint), and a full solve dropping both slots on a node's first record.
- **THE COUNTER: the within-solve serve fires 4 times out of 14,215 measure calls on `battle_hud L`** (war_room 328/21,009, killfeed 2/2,403, damage_fountain 0/201, nameplates 422/3,251). The premise — "the arrange pass re-measures every node the measure pass already answered AT THE SAME OFFER" — is true of `deep_stack_scene` (a `fill` list: measure-pass offer == arrange-pass offer, and the fixture reads `lastMeasured` 5,003 -> 3,002, exactly one per node) and FALSE of every arena workload, whose content-sized containers hand the arrange pass a RESOLVED inner box that is not the measure pass's offer. **The mount's 2.78 measures per node are 2.78 distinct questions.**
- **And the record costs 9.9 % of a mount.** Three arms, paired worktrees, B A A B C x2 (`d5/abba/`): `battle_hud` total A 120.92 / C 132.05 / B 132.88, `solver.solve` **41.77 -> 54.35 (+30 %)**; war_room 182.10 / 197.88 / 198.31; killfeed 20.31 / 21.84 / 22.12; nameplates 33.37 / 37.11 / 35.87; damage_fountain 5.32 / 5.52 / 5.66. **Arm C (every counter and write, none of the serve) is arm B on all five**, so the cost is the RECORD: it calls `offerHeightKey` -> `memoPlan` for a key it cannot spend, and `lastPlanVisits` goes 0 -> 5,106 (a cold solve never reached `memoPlan` at all). Safety pair `lastLayoutNodes`/`lastNodeBuilds`/`lastArranged`/`creates`/`rectWrites`/`engineWrites` identical on all three arms of all five.
- **Gate: FAILED, and the plan's rule is "a kept null is recorded, never shipped".** Facet `403174fd` reverted at **`2c4638910`** (tree byte-identical to `18795d8e`); RR `19d3c5d` reverted at **`88475e0`** (identical to `ff2ca0a`). Suites back at 8,742/0 and 3,599/0.
- Evidence taken on the shipped-then-reverted tree, all under `d5/`: `test.sh` 8,754/0 (the floor would have ratcheted to 8,754 with `mount_serve.spec`'s 12 cases); `verify.sh affected --jobs 1` **PASS_PARTIAL, 0 FAIL_RECOVERABLE, 935.4 s** (`d5-verify-affected.txt`); `check_source_size` PASS (solver 189,883, ledger row re-recorded; renderer 197,305 UNTOUCHED — D5 spent zero renderer characters); stylua clean; differential (`d5-diff-after.txt`) **18 of 2,881 lines moved, all `viewport` rows, all only `calls`/`measured`** — every rect, verdict, diagnostic and notify count byte-identical, and the revert restores md5 `3db66551b80fe677d314f2092b0a0038` by construction. Mutations 5 run / 4 bite / 1 kept null (`d5-mutations.txt`).
- **`tests/mount_serve.spec.luau` (12 cases) went with the mechanism**, and two of its findings are carried into FacetBench §D8 rather than lost: the caller census (the renderer is the ONLY `measureStamp` producer in `src/`, so the purity swap was exact) and the differential whose slow arm is a solve with no stamp — i.e. the pre-D5 code path — agreeing byte-for-byte on five fixtures x nine device views.
- **A REAL DEFECT FOUND ON THE WAY, booked with its diff (in the reverted commit):** `text_metrics`' collector DROPS the words past its 1,024-word budget. Nothing marks their owners dirty, so the only thing that ever brought the tail out was the next solve re-measuring the labels for its own reasons — an accident, not a design. With a 1,200-word vocabulary the surface settles believing it is settled while 176 labels keep an estimated width for the life of the surface. Fix: hold the surplus and let the next `beginCollect` take it, charged against the same budget. Small, strictly better than the accident, and independent of D5.
- **BOOKED for the assessment gate (D6), with sizes** — mount attribution at arm A, `battle_hud L`, 119.15 ms / 5,106 nodes: `solver.solve` 41.11 (measure 14.28 + arrange 26.78) = 8.05 µs/node; `ssTotal` 25.09 of which `ssEnsureTree` 20.82 = 4.07 µs/create; `commit` 20.02 of which `rectPass.apply` 11.62 — **9.16 of that is the harness's `setRect`, so Facet's own rect pass is 2.46 ms = 0.48 µs/rect**; `toLayoutNode` 13.50 = 2.64 µs and 2.43 kb per node; `tm.measure` 8.43 (6,012 calls) and `tm.minWidth` 6.33 (3,000). **No further mount lever in Plan D**: the two terms over 15 % are `ssEnsureTree` (per-CREATE Instance work a cold mount cannot recycle) and `arrange` (the fan-out this task proved is real work), and neither is O(1)-shaped. The lever that removes the fan-out stops `stack.arrange`'s PASS 1 asking — T14's replay for a settled stack, which a mount has nothing to replay — and that is a structural change.
- FacetBench **`2f83206`** registers §D8 (the instrument commit `19a976a` adds `lastMeasureServed`/`lastPlanVisits` to `d1_mount` and is KEPT: it is what makes the refutation readable).
- **No Studio drive on this task** — the orchestrator owns `mountMs` live, and after the revert there is nothing to measure there.
- Worktrees left in place: `<scratch>/d5/{A,B,C}/{Facet,FacetBench}` (A `18795d8e`, B `403174fd`, C = B + the serve-off patch; FacetBench all at `19a976a`). Remove at close.
- D5 orchestrator close: KEPT NULL (built `403174fd`, refuted by the three-arm ABBA — the serve fires 4 of 14,215 calls on battle_hud because content-sized containers ask the arrange pass a DIFFERENT question, and the record's `offerHeightKey → memoPlan` cost +30 % on `solver.solve`; reverted `2c463891`, byte-identical to 18795d8e; RR `19d3c5d` → `88475e0`). Mount attribution booked (§D8): `ssEnsureTree` 4.07 µs/create, `arrange` 26.8 ms real fan-out, Facet's own rect pass 0.48 µs/rect — no O(1)-shaped mount lever in Plan D. A real defect found: the collector DROPS words past `COLLECT_CAP` and nothing marks their owners dirty → D6d (correctness), first. **Task D5 CLOSED (null)**.

### Task D6 — the assessment gate (dispatched, opus; arm A 2c463891; brief d6-brief.md + D6d)
- OPEN READING CLOSED (2026-09-07): a base-lineage place (`tools/mkpair.sh` Facet b315dc34 + RR 218aa83, marker solver 187,617 / renderer 197,472 / no settle_solve) driven through the SAME rig steps: `Facet_Results` = **124** descendants on the base too (T10's 126 was a different fixture moment, not a change). `Facet_SponsorHud` descendant counts vary with scenario state (base: 472 at the results hold, 322 after release, 287 held live-normal; Plan D heads read 286 after release / 250 at results) — not a pin; the stable rows match: held live-normal `/Split abs=(16,24) size=(1520,385)`, `/Table abs=(8,16) size=(1536,505)` — byte-equal to §12. Evidence: this row; base place deleted.

#### D6d — the words the budget refused are held, not dropped (SHIPPED, Facet `0cb8bc0a`)
- The mechanism is D5's, extracted alone from the reverted `403174fd` (the overflow queue drained by the next `beginCollect`, charged against the same budget, ceilinged at `EXACT_CAP`, cleared by `resetMeasured`) and given the red-first case it never had there. `textCollectTruncated` keeps its meaning and still fires once per truncating collector.
- **The red arm had to be FOUND: three fixture shapes were built and refuted before one reproduced it.** At this head the drop is covered by an accident — a solve that follows a MOUNT re-measures the whole tree for want of anything to serve from, and an ANSWER's correction re-solve does too (`lastMeasured=305` of 304 nodes with `lastTextDirtyPaths=1`), so a truncation at mount, or one whose owner the next answer re-measures, always comes back out. Measured, recorded, and the reason the case is settle-first.
- **The shape that reproduces it** (`tests/text_collect_inflight.spec.luau`, "THE BOOT WINDOW"): a SETTLED 300-row surface whose body label becomes a 1,200-word paragraph on an INCREMENTAL solve. It asks for 1,024 and drops 176; the next FOUR solves — rows changing while the engine is still silent, which is the live boot window — ask for none of them (arm A `asked=1024 batches=2` on every one). After: `asked=1200 batches=3` from the first of them, and the drain then finishes in ONE answer round whose settle re-solve measures **4 nodes instead of 305**.
- **And the accident is one this campaign nearly removed:** with D5's mount serve standing (built, refuted, reverted at `2c463891`) the 176 labels kept an estimated width for the life of the surface. That is how the defect was found, and why this is correctness and not perf.
- Four cases: the boot window; the mechanism driven through `text_metrics` alone (2,300 words, one measure, then THREE collectors that measure nothing — `first=1024 then=1024,253,0`, i.e. the budget is still the bound); a held word the store has since learned is dropped rather than re-asked; `resetMeasured` forgets the queue. **Mutations 5 run / 5 bite** (`d6/d6d-mutations.txt`).
- Gates: `tools/test.sh 8742` → **8,746/0** (floor ratchets 8,742 → 8,746, exactly the four new cases; `d6/d6d-test.txt`); `verify.sh affected --jobs 1` **PASS_PARTIAL, 0 FAIL_RECOVERABLE, 502.3 s** (`d6/d6d-verify-affected.txt`), then `git checkout -- examples/places/`; `check_source_size` PASS (renderer 197,305 UNTOUCHED — D6d spends zero renderer characters); stylua clean; differential md5 **`3db66551b80fe677d314f2092b0a0038`** (144 blocks, 0 diff lines).

#### D6a — rows as coordinate-space hosts: **SHIPPED** (Facet `0149c31b` + `53c9fec1` + `b844915e`, RR `ac573a9`)
- **The fix round found the defect the spike could not: `layout/translate_arm.translateDescendants` re-based every descendant of a moved subtree, COORDINATE-SPACE HOSTS INCLUDED** — the host's own rect (right: it lives in its parent's space) and then, a second time and by the same delta, every rect stored in the host's own space. `solver.luau` already makes the opposite ruling for a host it meets as the translate ROOT (an EMPTY run); it was made nowhere for a host reached one level down. Localised in one XRAY print on the six failing oracles: `translateDescendants recurses INTO host d=(0,-60) kids=4`, and the one-row rect print is `/Scroll/List/Rows/[r10]/Row/B` incremental `113,261` vs full `113,321` with the row host's own rect AGREEING — the 60 is the dropped banner.
- **It is a SHIPPED defect, not spike scaffolding.** `RowActions-<key>` is an `Anchor` holding the sliding tray; the tray's `offsetX` is reactive, so the tray is a coordinate space and the anchor is not, and a row leaving above it moves the anchor while the tray stands still — **98 such runs in `tests/playlist_columns.spec` at the parent commit, each writing 17 entries where 1 is correct and re-basing 16 tray descendants that had not moved.** No spec in the tree read one; `host_space_oracle`'s own fixture has zero (the XRAY fired 0 times there), which is the blind spot. Fixed at `0149c31b` with two cases in `tests/translate_host.spec` (RED-FIRST: the tray leaf's host-relative stored rect reads `0,-40` against `0,0`), the composition refusal via `ctx.spaceCompositions` (the pair `arrange`'s anchor arm already asks), and the `translate_arm_seam` `child` field set widened to three with a rider. Mutations 3/3.
- **The trigger, and it is NARROWER than the brief's** (`b844915e`): a `ForEach` ITEM ROOT, spliced into a stack, with two or more children. `mount.markRow` stamps the item roots (mount is the only pass that still knows the authored parent); `createOptsFor` reads it as a fifth arm. The brief's "every direct child of a stack" was refused ON A MEASUREMENT: it gave back 40 of 61 elisions on `check_elision_census`'s 20-row fixture where the narrow rule gives back **none**, because only a keyed region produces a list shift. `ROW_HOST` gained `ZStack` and `STACK_PARENT` gained `ScrollView` because **Facet's own `Table` builds rows as ZStacks inside a ScrollView Body** — without both the lever reached FacetBench's hand-written list and NOT the framework's shipped list control (RR mutations M4/M5 are the proof).
- **Counters (`tests/lib/row_host_scene.luau`, a 300-row list whose banner leaves and returns):** hosts off `1,501 rects / 1,501 engine writes / 1,500 translated / elided 302`; hosts on `301 / 301 / 300 / elided 2 / 300 spaces`. **The price is pinned in the same assertion as the win**: a space is a real Instance, so 300 rows leave the elidable set — 1,200 writes per shift against 300 instances for the life of the surface.
- **POOL: MEASURED, NO REGRESSION.** 50 removals then 50 inserts on a 200-row list give IDENTICAL `creates=186 parked=64 recycled=64 removes=250` with hosts on and off, to the unit. No length gate is needed; the brief's fallback is unspent.
- **ABBA `war_room_inventory L`, 2 passes, B A A B + C (C = trigger present, `ROW_HOST` empty):**

| class | A / A | C | B / B |
|---|---:|---:|---:|
| `reorder` rectWrites = engineWrites | 7,335 | 7,335 | **1,467 (5.00x)** |
| `reorder` raw p50 | 25.360 / 25.634 | 25.775 | 22.904 / 22.866 |
| `reorder` **Facet's own** (raw − `target.setRect`) | 15.780 / 15.937 | 15.980 | **10.142 / 9.857 (−37.0 %)** |
| `removeItem-items` rectWrites | 3,460 | 3,460 | **692 (5.00x)** |
| `removeItem-items` own | 7.810 / 7.404 | 8.003 | **5.237 / 5.650 (−28.4 %)** |
| `addItem-items` own | 3.475 / 3.648 | 3.628 | 3.455 / 3.625 |
| `setState` / `updateItem-power` / `updateItem-tier` / `noop` | unmoved on every arm | | |

  Arm C is arm A to the noise. No leaf class moved on `battle_hud`, `killfeed_nameplates`, `damage_fountain` or `nameplates` either (A vs B, 2 passes each; every class within 3 % except `killfeed addItem-feed`, which IMPROVED 6.1 %). **The arena UNDERSTATES it:** the fake's `setRect` recomposes a host subtree, so its per-call cost goes 1.31 → 8.79 µs and `rectPass.apply` RISES while Facet's own share falls. Live the constant is the engine's, per write, in C++ (§D7.L).
- **RR `ac573a9`, suite 3,600/0** (+1 for the new contract): a full 24-kart position reversal on the shipped racer list writes **114 rects / 132 engine Positions where it wrote 264 / 300**, `lastCommitVisits` 1,412 → 1,274 — both arms measured through `translateHosts`, so it is a differential and not a remembered number. `facet_translate_host_contract`'s CONTROL keeps `scrollToVisible = 78` while its host census goes 1 → 25 (the target of the subtraction is now a rect that changed space; the offset standing still IS the composition proof). `facet_measure_fanout_contract` moves on the arrange side only, `measured` unmoved at 20: `arranged` 24→14, `translated` 2→0, `skipped` 11→23, `lastCommitVisits` 182→160, measure pair 75/34→65/24 (difference unchanged at 41), arrange quartet 25/0/24/2→17/8/14/24. No other pin moved — the minimap's rows are `ForEach` items under an `Anchor`.
- Mutations **5 run / 5 bite** (`d6/d6a-mutations.txt`), two of them on the GAME side (M4 `ZStack`, M5 `ScrollView`). M2/M3 had NO producer in the tree and one was built for them (`a row earns its coordinate space by being in a stack, with children`: `stack2=3 stack1=0 anchor=0`).
- Gates: `tools/test.sh 8756` → **8,760/0** (floor ratchets 8,754 → 8,756 at the fix, → 8,760 here); `verify.sh affected --jobs 1` **PASS_PARTIAL, 0 FAIL_RECOVERABLE, 529.4 s**; `check_source_size` PASS (renderer 197,305 UNTOUCHED); `check_elision_census` PASS; stylua clean; differential md5 **`3db66551b80fe677d314f2092b0a0038`** identical on both commits. Evidence `d6/d6a-*.txt` (untracked, as D6b/c/d's are — `53c9fec1` removed the four the fix commit had wrongly put in the tree; `check_public_allowlist` is the check that says why).
- **TWO PRE-EXISTING DEFECTS THE NEW FIXTURE UNCOVERED — booked, NOT D6a's.** Both reproduce with `hosts = false`, i.e. in the pre-D6a world, on `tests/lib/row_host_scene.luau`'s shape (reproduction kept at `<scratch>/d6a/probe_rows_prereg_defect.spec.luau`):
  1. **A `When` branch returning after an INSERT below it leaves a content-height ancestor short by exactly the inserted row** — `Body` reads `0,0,359,428` against a forced full solve's `458`, and the incremental arm files a spurious "content overflows this vstack by 30px". Drive: `branch off → insert → branch on`.
  2. **A `ScrollView` around the list goes stale on `canvas.contentW` after a reorder followed by a branch toggle** — `canvas=8,2000` against a full solve's `190,2000`. Drive: `reorder → branch off → branch on`.
  The D6a oracle's step order and the fixture's fixed heights avoid conflating either with a D6a regression, and both avoidances are stated at the site rather than left as a silent gap.
- **NEXT LEVER, SIZED:** the trigger still misses a `ForEach` under an `Anchor` (the minimap, the nameplates, and the Table's VIRTUALIZED body, whose rows sit inside a `Canvas` anchor) — those rows are placed by their own offsets, so the win there is not a shared shift but a per-row one, and the C1 trigger already hosts them when the offset is reactive. Nothing owed. What IS owed is the live `war_room` fresh-VM read of `reorder` / `removeItem-items` with the count at 1,467.

#### D6b — a walk with nothing to write does not walk (SHIPPED, Facet `3062e3bc` / RR `5d4653d`)
- Mechanism: `commit_walks`' `hitRects` returns before it starts when `next(inputSinks) == nil and next(lastHitRects) == nil`, publishing 0 visits. The premise is a CONTRACT claim, pinned as a source scan of both sets: exactly four classes declare `minHitSize` (`Grip`, `Button`, `Toggle`, `TextField`) and they are exactly the four `INPUT_SINKING` classes the renderer files into `inputSinks` — which is besides a strict superset (any node with `active = true`). The departure objection is answered by `dropPath`, which already clears `lastHitRects[path]`; mutation M5 deletes that line and reddens the departure case, which is what makes the sweep the owner.
- Spec `tests/hit_rect_refusal.spec.luau` (5 cases): the contract scan; a 400-row list refusing the walk on a whole-list write (arm A `hitRects=1203` → 0, and no hit rect asked for anywhere); ONE sub-floor Button on the same tree keeping the walk and its 44x44 expander; an `active` Box keeping it too though it writes nothing (the superset direction); the Button unmounting, the NEXT commit refusing, and the sink coming back to be re-asked. **Mutations 5 run / 4 bite / 1 recorded null** (`d6/d6b-mutations.txt`) — M2 (the `lastHitRects` term alone dropped) has no producer in `src/` and is kept as belt to `dropPath`'s brace, with the argument written at the case.
- **Gate** (paired worktrees Facet `0cb8bc0a` / FacetBench `2f83206`, `attr war_room_inventory L 2`, order B A A B + C where C computes the gate and walks anyway):

| metric | A | C | B |
|---|---:|---:|---:|
| `cw.hitRects` | 2.864 / 2.913 | 2.869 | **0.001** |
| `reorder`, Facet's own | 19.44 / 19.07 | 19.53 | **16.11 / 16.40 (−15.6 %)** |
| `removeItem-items` raw | 13.015 / 12.981 | 13.002 | **11.531 / 11.668 (−10.7 %)** |

  Arm C is arm A to the digit. `rectWrites`, `engineWrites`, `lastRectInserts`, `lastArranged`, `lastLayoutNodes`, `zVisits` IDENTICAL on all three arms on every class.
- Pins re-read with riders: `nameplates_baseline`'s exact aggregate is now SEVEN walks (the eighth is refused and pinned separately); `commit_translate` ×2 and `commit_dirt_classes`' control arm go to `hitRects=0` (the prune they described is unchanged and is still driven, in the new spec's Button case); `commit_descend_cache`/`commit_probe_filter` scan counts fall by the walk's own root guard (1,011→1,010, 6→5, 30→29, 34→29 visits) and the structural-churn arm 193/183 → 72/63.
- Gates: `tools/test.sh 8746` → **8,751/0** (floor ratchets 8,746 → 8,751); `verify.sh affected --jobs 1` **PASS_PARTIAL, 0 FAIL_RECOVERABLE, 893.5 s** (`d6/d6b-verify-affected.txt`), then `git checkout -- examples/places/`; `check_source_size` PASS (renderer 197,305 UNTOUCHED — D6b spends zero renderer characters); stylua clean; differential md5 **`3db66551b80fe677d314f2092b0a0038`** (144 blocks).
- RR `5d4653d`: `facet_commit_dirt_classes` gains the other side of the gate — the shipped minimap sits inside a screen with pressable controls, so `inputSinks` is non-empty, `hitRects={HIT_VISITS}` did NOT move, and a second reader pins `walk refused=false` because a zero there would mean the game had silently lost its target-floor expanders. Suite **3,599/0**, no other pin moved.

#### D6c — the word decides which labels moved, not a re-measure (SHIPPED, Facet `eceb7145` / RR `00793da` / FacetBench `ac98e4d`)
- Mechanism: `announceWord(font, size, word)` carries the WORD; `text_index` marks the key WITH the words that moved under it and keeps a per-path drawn-word set (`text_metrics.wordsOf`, re-derived only when the literal moves, on the unconditional side of `record`'s guard — the case that guard lets through). Inside the over-cap probe's `was ~= nil` arm, a label that draws none of the marked words is dropped without a measure.
- **The placement IS the safety argument:** every leaf the probe already refused to answer for (a `fit` node, a wrapping or multi-word label, a `compactPrefer` node, a path never seen measured) has no `mTextW`, so the check never sees it. The narrowing only ever replaces a MEASURE with a lookup on the population the measure already served. M2 (the check one gate up) reddens the case built for it (`skips=1001`, a phrase dropped for a string it does not draw).
- `wordsOf` is a SECOND derivation of `wrap`'s split and is CHECKED, not assumed: the spec drives the COLLECTOR over five edge strings (one word, two, a run of spaces, leading/trailing whitespace, a tab) and compares its request set word for word. M3 deletes the space term and it reddens.
- **Counters (the pair is the proof):** `lastTextProbeMeasures` + the new `lastTextWordSkips` sum to the probe's population, and `lastTextDirtyPaths` — what the narrowing BUYS — is unchanged on every case in the file. 1,001-leaf fixture: a word ten labels draw `probes=10 skips=991 paths=10` (was `probes=1001 paths=10`); a word nobody draws `probes=0` (was 1,001); a relabelled leaf `probes=1` (was 201); the worst case stated — a word EVERY label draws still probes 1,000 and still answers `full`. **Mutations 5 run / 5 bite** (`d6/d6c-mutations.txt`); M5 (two words at one key, the second overwriting the first) lands on the ORACLE.
- **Price, headless, on the shipped code: 1,000 probe measures 0.439 ms against 1,000 word lookups 0.0186 ms — 23.6x.** And the arena's limit is RECORDED rather than worked around: `t18_settle`'s settle step pushes 4 paths of 5,106 nodes on `battle_hud L`, so `lastTextProbeMeasures` is **0 on BOTH arms** there — the over-cap route this lever serves is only reachable LIVE, on the `[S]` rows (the 1,000 / 1,464 probes D3c counted). FacetBench `ac98e4d` makes `t18_settle` print both counters so the live drive reads the pair.
- Gates: `tools/test.sh 8751` → **8,754/0** (floor ratchets 8,751 → 8,754); `verify.sh affected --jobs 1` **PASS_PARTIAL, 0 FAIL_RECOVERABLE, 512.0 s** (`d6/d6c-verify-affected.txt`), then `git checkout -- examples/places/`; `check_source_size` PASS (renderer 197,305 UNTOUCHED — the counter rides `settle_solve`); stylua clean; differential md5 **`3db66551b80fe677d314f2092b0a0038`** (144 blocks).
- RR `00793da`: `facet_text_settle_contract` reads the PAIR (`pushed=1 probes=0 skips=0`) — this game is structurally under the cap (a dozen racer names, a lap readout, a clock; the widest key has one user), so neither route runs and a non-zero `skips` would say what a non-zero `probes` says. Suite **3,599/0**, no other pin moved.
- **OWED to the orchestrator:** the live `[S]` rows (`battle_hud updateItem-facing`, `war_room updateItem-power`, `addItem-items`) in a fresh VM — that is the only place D6a/D6b/D6c's class numbers can be read, and D6c's in particular cannot be read headlessly at all.
- D6 orchestrator: D6a SHIPPED after its fix round (Facet `0149c31b` the correctness fix + `53c9fec1` the evidence untrack + `b844915e` the trigger; RR `ac573a9`); D6d/D6b/D6c SHIPPED (0cb8bc0a / 3062e3bc / eceb7145; RR 00793da; FacetBench ac98e4d). The six red oracles were ONE defect in `layout/translate_arm` — the descendant re-base walked THROUGH a coordinate-space host — and it was already reaching shipped code (98 runs in `playlist_columns` alone). Count lever landed: reorder rectWrites 7,335 → 1,467 (5.00x), Facet's own reorder 15.86 → 10.00 headless (−37 %), RR racer list 300 → 132 engine writes. Live rows for D6b/c/d + RR canary: this drive.
- D6 orchestrator (2026-09-07): LIVE war_room (d6-studio-live.txt): reorder 32.78→**28.68** (−12.5 %), removeItem 15.09→14.18, addItem 5.34→5.18; battle_hud unchanged. **D6c DOES NOT FIRE LIVE**: probes=1000 skips=0 with ONE label drawing the announced word — `noteWords` accepts only string text and every arena label (every HUD number) carries a NUMBER. Fix staged as a full-file copy `d6c-numeric.patch` (tostring a numeric text; spec case with numeric texts owed) — applied + gated after the D6a fix round frees the lune lock. RR canary D6 (d6-rr-canary.txt): 57/56/8, kids byte-equal, Split/Table rows byte-equal, frame p50 16.66, 0 Facet errors.
